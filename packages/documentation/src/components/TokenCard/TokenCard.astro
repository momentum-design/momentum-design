---
import './TokenCard.css';

type Props = {
  name: string;
  link: string;
};

const { name, link } = Astro.props as Props;
---

<a id={name} class="card" href={link}>
  {name.replace('-', ' ')}
</a>
