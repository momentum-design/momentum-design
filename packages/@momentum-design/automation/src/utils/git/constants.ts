const MAX_COMMITS = 10;
const COMMIT_INDEX_OFFSET = 1;

const FORMAT = {
  commit: '%H',
  subject: '%s',
};

const CONSTANTS = {
  COMMIT_INDEX_OFFSET,
  FORMAT,
  MAX_COMMITS,
};

export default CONSTANTS;
