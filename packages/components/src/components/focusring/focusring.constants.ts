/* eslint-disable implicit-arrow-linebreak */
import utils from '../../utils/tag-name';

const TAG_NAME = utils.constructTagName('focusring');

const DEFAULTS = {
  SHAPE_CLASS: 'mds-div' as const,
};

export { DEFAULTS, TAG_NAME };
