import utils from '../../utils/tag-name';

const TAG_NAME = utils.constructTagName('radio');

const VALIDATION_MESSAGE = 'Please select one of these options.';

export { TAG_NAME, VALIDATION_MESSAGE };
