import utils from '../../utils/tag-name';

const TAG_NAME = utils.constructTagName('virtualizedlist');

const DEFAULT_ESTIMATE_SIZE = () => 0;
const DEFAULT_COUNT = 0;

export { TAG_NAME, DEFAULT_ESTIMATE_SIZE, DEFAULT_COUNT };
