// AI-Assisted generated by scripts/generate-component-registry.js. Do not edit manually.
import type { ComponentRegistryEntry } from './registry.types';

const componentsRegistry: ComponentRegistryEntry[] = [
  {
    "name": "accordion",
    "title": "Accordion",
    "tagName": "mdc-accordion",
    "summary": "Usage, guidelines, and accessibility for the mdc-accordion component — a vertically stacked header/body component with dedicated expand/collapse controls.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/accordion",
      "knowledgeBasePath": "components/src/components/accordion/knowledge-base/accordion.component.md"
    },
    "docs": "## Overview\n\nThe accordion is a vertically stacked component with a header and an expandable/collapsible body section. The header displays an optional prefix icon, header text, optional leading and trailing control slots, and a dedicated expand/collapse button. Unlike `mdc-accordionbutton`, only the expand/collapse button is interactive — the rest of the header is not clickable.\n\n### When to use\n\n- Use `mdc-accordion` when you need additional interactive controls (chips, badges, buttons, icons) in the header alongside the expand/collapse behaviour.\n\n### When not to use\n\n- Use `mdc-accordionbutton` instead when you only need a simple clickable header without extra controls in the header row.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/accordion/index.js\"; // custom element registration\n// or via React wrapper\nimport { Accordion } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-accordion\n  heading-text=\"Section title\"\n  open-button-aria-label=\"Expand section\"\n  close-button-aria-label=\"Collapse section\"\n>\n  Body content goes here.\n</mdc-accordion>\n```\n\n### Content guidance\n\n- Keep header text concise — the heading communicates the topic of the body content.\n\n### Limitations\n\n- The `shown` event fired by child elements inside the header (e.g. slotted controls) is stopped from bubbling to prevent confusion with the accordion's own `shown` event.\n- Screen readers may lose focus when toggling if the accordion is expanded by default on initial render.\n\n## Accessibility\n\n### Built-in features\n\nThe component manages expand/collapse state and exposes it to assistive technologies automatically.\n\n#### Internal ARIA managed by the component\n\n- The header text is wrapped in a `role=\"heading\"` element with `aria-level` reflecting the `data-aria-level` property (default H3).\n- The toggle button has `aria-expanded` set to the current expanded state.\n- The toggle button has `aria-controls` pointing to the body section's `id`.\n\n### Implementation requirements\n\n#### General\n\n- Use Accordion component when you need additional interactive controls (chips, badges, buttons, icons) in the header alongside the expand/collapse behaviour.\n- Set `data-aria-level` to match the heading hierarchy of the page context where the accordion is embedded (e.g. `data-aria-level=\"2\"` if the accordion is a second-level heading).\n\n#### Labelling\n\n- **Always** provide both `open-button-aria-label` and `close-button-aria-label`. Without these, screen reader users cannot determine what the toggle button does.",
    "sections": {
      "Overview": "The accordion is a vertically stacked component with a header and an expandable/collapsible body section. The header displays an optional prefix icon, header text, optional leading and trailing control slots, and a dedicated expand/collapse button. Unlike `mdc-accordionbutton`, only the expand/collapse button is interactive — the rest of the header is not clickable.\n\n### When to use\n\n- Use `mdc-accordion` when you need additional interactive controls (chips, badges, buttons, icons) in the header alongside the expand/collapse behaviour.\n\n### When not to use\n\n- Use `mdc-accordionbutton` instead when you only need a simple clickable header without extra controls in the header row.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/accordion/index.js\"; // custom element registration\n// or via React wrapper\nimport { Accordion } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-accordion\n  heading-text=\"Section title\"\n  open-button-aria-label=\"Expand section\"\n  close-button-aria-label=\"Collapse section\"\n>\n  Body content goes here.\n</mdc-accordion>\n```\n\n### Content guidance\n\n- Keep header text concise — the heading communicates the topic of the body content.\n\n### Limitations\n\n- The `shown` event fired by child elements inside the header (e.g. slotted controls) is stopped from bubbling to prevent confusion with the accordion's own `shown` event.\n- Screen readers may lose focus when toggling if the accordion is expanded by default on initial render.",
      "Accessibility": "### Built-in features\n\nThe component manages expand/collapse state and exposes it to assistive technologies automatically.\n\n#### Internal ARIA managed by the component\n\n- The header text is wrapped in a `role=\"heading\"` element with `aria-level` reflecting the `data-aria-level` property (default H3).\n- The toggle button has `aria-expanded` set to the current expanded state.\n- The toggle button has `aria-controls` pointing to the body section's `id`.\n\n### Implementation requirements\n\n#### General\n\n- Use Accordion component when you need additional interactive controls (chips, badges, buttons, icons) in the header alongside the expand/collapse behaviour.\n- Set `data-aria-level` to match the heading hierarchy of the page context where the accordion is embedded (e.g. `data-aria-level=\"2\"` if the accordion is a second-level heading).\n\n#### Labelling\n\n- **Always** provide both `open-button-aria-label` and `close-button-aria-label`. Without these, screen reader users cannot determine what the toggle button does."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "closeButtonAriaLabel",
          "attribute": "close-button-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dataAriaLevel",
          "attribute": "data-aria-level",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "expanded",
          "attribute": "expanded",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "openButtonAriaLabel",
          "attribute": "open-button-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "togglePosition",
          "attribute": "toggle-position",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "leading-header-text",
          "description": "The leading header text slot of the accordion on the header section. Placed after the prefix icon."
        },
        {
          "name": "leading-controls",
          "description": "The leading controls slot of the accordion on the header section. Placed after the header text."
        },
        {
          "name": "trailing-controls",
          "description": "The trailing controls slot of the accordion on the header section. Placed before the expand/collapse button."
        },
        {
          "name": "default",
          "description": "The default slot contains the body section of the accordion. User can place anything inside this body slot."
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) This event is triggered when the accordion is toggled (expanded or collapsed)."
        }
      ],
      "cssParts": [
        {
          "name": "body-section",
          "description": "The body section of the accordion."
        },
        {
          "name": "header-section",
          "description": "The header section of the accordion."
        },
        {
          "name": "leading-header",
          "description": "The leading header of the accordion."
        },
        {
          "name": "leading-header-text",
          "description": "The leading header text of the accordion button."
        },
        {
          "name": "trailing-header",
          "description": "The trailing header of the accordion."
        },
        {
          "name": "toggle-button",
          "description": "The expand/collapse toggle button of the accordion."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-accordionbutton-border-color",
          "description": "The border color of the accordion."
        },
        {
          "name": "--mdc-accordionbutton-disabled-color",
          "description": "The disabled text color of the accordion."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/accordion/index.js\"; // custom element registration\n// or via React wrapper\nimport { Accordion } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-accordion\n  heading-text=\"Section title\"\n  open-button-aria-label=\"Expand section\"\n  close-button-aria-label=\"Collapse section\"\n>\n  Body content goes here.\n</mdc-accordion>"
      }
    ],
    "reactImports": [
      "Accordion"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "accordion",
      "and",
      "body",
      "button",
      "collapse",
      "component",
      "controls",
      "dedicated",
      "expand",
      "for",
      "guidelines",
      "header",
      "icon",
      "mdc",
      "overview",
      "stacked",
      "text",
      "the",
      "usage",
      "vertically",
      "with"
    ]
  },
  {
    "name": "accordionbutton",
    "title": "Accordion Button",
    "tagName": "mdc-accordionbutton",
    "summary": "Usage, guidelines, and accessibility for the mdc-accordionbutton component — a vertically stacked component with a fully clickable header and an expandable/collapsible body section.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/accordionbutton",
      "knowledgeBasePath": "components/src/components/accordionbutton/knowledge-base/accordionbutton.component.md"
    },
    "docs": "## Overview\n\nThe accordion button is a vertically stacked component with a clickable header and an expandable/collapsible body section. The entire header is clickable to toggle the visibility of the body content. The header contains an optional prefix icon, header text (default H3, customizable via `data-aria-level`), and an expand/collapse arrow icon as visual indicator. The body contains a default slot for any content. The component supports different border styles through the `variant` attribute and different spacing through the `size` attribute, and can be disabled to prevent the header from being clickable.\n\n### When to use\n\n- Use `mdc-accordionbutton` for simple clickable headers without additional controls.\n\n### When not to use\n\n- Use `mdc-accordion` instead if you need extra controls (chips, badges, icons) in the header itself.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/accordionbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { AccordionButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-accordionbutton header-text=\"Section title\">\n  Body content goes here.\n</mdc-accordionbutton>\n```\n\n## Accessibility\n\n### Implementation requirements\n\n#### General\n\n- Adjust `data-aria-level` based on the heading hierarchy in your page (e.g., `data-aria-level=\"2\"` if the accordion button is a second-level heading).\n\n### Notes\n\n- Screen readers may lose focus when toggling if the accordion button is expanded by default on initial render.",
    "sections": {
      "Overview": "The accordion button is a vertically stacked component with a clickable header and an expandable/collapsible body section. The entire header is clickable to toggle the visibility of the body content. The header contains an optional prefix icon, header text (default H3, customizable via `data-aria-level`), and an expand/collapse arrow icon as visual indicator. The body contains a default slot for any content. The component supports different border styles through the `variant` attribute and different spacing through the `size` attribute, and can be disabled to prevent the header from being clickable.\n\n### When to use\n\n- Use `mdc-accordionbutton` for simple clickable headers without additional controls.\n\n### When not to use\n\n- Use `mdc-accordion` instead if you need extra controls (chips, badges, icons) in the header itself.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/accordionbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { AccordionButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-accordionbutton header-text=\"Section title\">\n  Body content goes here.\n</mdc-accordionbutton>\n```",
      "Accessibility": "### Implementation requirements\n\n#### General\n\n- Adjust `data-aria-level` based on the heading hierarchy in your page (e.g., `data-aria-level=\"2\"` if the accordion button is a second-level heading).\n\n### Notes\n\n- Screen readers may lose focus when toggling if the accordion button is expanded by default on initial render."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "dataAriaLevel",
          "attribute": "data-aria-level",
          "type": "number",
          "default": "3",
          "values": [],
          "description": "Specifies the heading level for accessibility purposes.\nShould match the heading hierarchy of the page (e.g., 2 for H2, 3 for H3).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "expanded",
          "attribute": "expanded",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "Controls whether the accordion body content is visible.\n- When true, the body section is shown; when false, it is hidden.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "'small'",
          "values": [],
          "description": "/\nclass AccordionButton extends KeyDownHandledMixin(KeyToActionMixin(DisabledMixin(Component))) {\n/**\nControls the spacing and padding of the accordion.\n- Small provides 1rem (16px) padding, large provides 1.5rem (24px) padding.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "togglePosition",
          "attribute": "toggle-position",
          "type": "select",
          "default": "DEFAULTS.TOGGLE_POSITION",
          "values": [
            "leading",
            "trailing"
          ],
          "description": "Text content displayed in the accordion header.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'default'",
          "values": [
            "default",
            "borderless"
          ],
          "description": "Visual style variant of the accordion.\n- Default includes borders, borderless removes all border styling.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "The default slot contains the body section of the accordion. User can place anything inside this body slot."
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) This event is triggered when the accordion button is toggled (expanded or collapsed)."
        }
      ],
      "cssParts": [
        {
          "name": "body-section",
          "description": "The body section of the accordion button."
        },
        {
          "name": "header-button-section",
          "description": "The header button section of the accordion button."
        },
        {
          "name": "header-section",
          "description": "The header section of the accordion button."
        },
        {
          "name": "leading-header",
          "description": "The leading header of the accordion button."
        },
        {
          "name": "leading-header-text",
          "description": "The leading header text of the accordion button."
        },
        {
          "name": "trailing-header",
          "description": "The trailing header of the accordion button."
        },
        {
          "name": "toggle-icon",
          "description": "The expand/collapse toggle icon of the accordion button."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-accordionbutton-border-color",
          "description": "The border color of the accordion button."
        },
        {
          "name": "--mdc-accordionbutton-hover-color",
          "description": "The hover background color of the accordion button header."
        },
        {
          "name": "--mdc-accordionbutton-active-color",
          "description": "The active background color of the accordion button header."
        },
        {
          "name": "--mdc-accordionbutton-disabled-color",
          "description": "The disabled text color of the accordion button."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/accordionbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { AccordionButton } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-accordionbutton header-text=\"Section title\">\n  Body content goes here.\n</mdc-accordionbutton>"
      }
    ],
    "reactImports": [
      "AccordionButton"
    ],
    "constants": {
      "ICON_NAME": {
        "ARROW_UP": "'arrow-up-bold' as Extract<IconNames, 'arrow-up-bold'>",
        "ARROW_DOWN": "'arrow-down-bold' as Extract<IconNames, 'arrow-down-bold'>"
      },
      "VARIANT": {
        "DEFAULT": "default",
        "BORDERLESS": "borderless"
      },
      "TOGGLE_POSITION": {
        "LEADING": "leading",
        "TRAILING": "trailing"
      },
      "DEFAULTS": {
        "EXPANDED": false,
        "DATA_ARIA_LEVEL": 3,
        "VARIANT": "default",
        "ICON_NAME": "'arrow-up-bold' as Extract<IconNames, 'arrow-up-bold'>",
        "TOGGLE_POSITION": "trailing"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "accordion",
      "accordionbutton",
      "an",
      "and",
      "body",
      "button",
      "clickable",
      "collapsible",
      "component",
      "expandable",
      "for",
      "fully",
      "guidelines",
      "header",
      "icon",
      "mdc",
      "overview",
      "section",
      "stacked",
      "text",
      "the",
      "usage",
      "vertically",
      "with"
    ]
  },
  {
    "name": "accordiongroup",
    "title": "Accordion Group",
    "tagName": "mdc-accordiongroup",
    "summary": "Usage, guidelines, and accessibility for the mdc-accordiongroup component — a container that manages multiple accordion or accordionbutton components as a unified set.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/accordiongroup",
      "knowledgeBasePath": "components/src/components/accordiongroup/knowledge-base/accordiongroup.component.md"
    },
    "docs": "## Overview\n\nThe accordion group is a container that manages multiple accordion or accordionbutton components as a unified set. It controls the visual styling, spacing, and expansion behavior of all child accordions. The group applies consistent `variant` and `size` attributes to all children automatically. By default, expanding one accordion collapses the others (`allow-multiple` is false); set `allow-multiple` to true to allow multiple expanded items.\n\n### When to use\n\n- Use `mdc-accordiongroup` when you need to render two or more related `mdc-accordion` or `mdc-accordionbutton` items with consistent styling, spacing, and coordinated expansion behaviour.\n\n### When not to use\n\n- Use a single `mdc-accordion` or `mdc-accordionbutton` directly when only one collapsible section is needed.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/accordiongroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { AccordionGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-accordiongroup>\n  <mdc-accordionbutton header-text=\"Section one\">Body one</mdc-accordionbutton>\n  <mdc-accordionbutton header-text=\"Section two\">Body two</mdc-accordionbutton>\n</mdc-accordiongroup>\n```\n\n### Content guidance\n\n- Only `mdc-accordion` and `mdc-accordionbutton` elements are managed by the group; any other slotted elements are ignored.\n\n## Accessibility\n\n### Implementation requirements\n\n#### General\n\n- Set the `data-aria-level` of the child accordions to match the heading hierarchy of the surrounding page.\n\n### Notes\n\n- Screen readers may lose focus when toggling if the first accordion is expanded by default on initial render.",
    "sections": {
      "Overview": "The accordion group is a container that manages multiple accordion or accordionbutton components as a unified set. It controls the visual styling, spacing, and expansion behavior of all child accordions. The group applies consistent `variant` and `size` attributes to all children automatically. By default, expanding one accordion collapses the others (`allow-multiple` is false); set `allow-multiple` to true to allow multiple expanded items.\n\n### When to use\n\n- Use `mdc-accordiongroup` when you need to render two or more related `mdc-accordion` or `mdc-accordionbutton` items with consistent styling, spacing, and coordinated expansion behaviour.\n\n### When not to use\n\n- Use a single `mdc-accordion` or `mdc-accordionbutton` directly when only one collapsible section is needed.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/accordiongroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { AccordionGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-accordiongroup>\n  <mdc-accordionbutton header-text=\"Section one\">Body one</mdc-accordionbutton>\n  <mdc-accordionbutton header-text=\"Section two\">Body two</mdc-accordionbutton>\n</mdc-accordiongroup>\n```\n\n### Content guidance\n\n- Only `mdc-accordion` and `mdc-accordionbutton` elements are managed by the group; any other slotted elements are ignored.",
      "Accessibility": "### Implementation requirements\n\n#### General\n\n- Set the `data-aria-level` of the child accordions to match the heading hierarchy of the surrounding page.\n\n### Notes\n\n- Screen readers may lose focus when toggling if the first accordion is expanded by default on initial render."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "accordionItems",
          "attribute": "allow-multiple",
          "type": "Array<HTMLElement>",
          "default": "false",
          "values": [],
          "description": "Controls whether multiple accordions can be expanded simultaneously.\n- When false, expanding one accordion automatically collapses others.\n- When true, multiple accordions can remain open at the same time.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "allowMultiple",
          "attribute": "allow-multiple",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "'small'",
          "values": [
            "small",
            "large"
          ],
          "description": "/\nclass AccordionGroup extends Component {\n/**\nControls the spacing and padding for all accordion items in the group.\nThis size is applied to all child accordions.\n- Small provides 1rem (16px) padding, large provides 1.5rem (24px) padding.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'stacked'",
          "values": [
            "borderless",
            "contained",
            "stacked"
          ],
          "description": "Visual style variant applied to all accordions in the group.\n- Stacked: accordions have 1.5rem gap between them.\n- Borderless: removes all borders from accordions and group.\n- Contained: accordions have continuous borders with no gaps.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "The default slot can contain the `accordion` or `accordionbutton` components."
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-accordiongroup-items-border-color",
          "description": "The border color applied to all child accordion items and their separators."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/accordiongroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { AccordionGroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-accordiongroup>\n  <mdc-accordionbutton header-text=\"Section one\">Body one</mdc-accordionbutton>\n  <mdc-accordionbutton header-text=\"Section two\">Body two</mdc-accordionbutton>\n</mdc-accordiongroup>"
      }
    ],
    "reactImports": [
      "AccordionGroup"
    ],
    "constants": {
      "SIZE": {
        "SMALL": "small",
        "LARGE": "large"
      },
      "VARIANT": {
        "BORDERLESS": "borderless",
        "CONTAINED": "contained",
        "STACKED": "stacked"
      },
      "DEFAULTS": {
        "SIZE": "small",
        "VARIANT": "stacked"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "accordion",
      "accordionbutton",
      "accordiongroup",
      "and",
      "as",
      "component",
      "components",
      "container",
      "for",
      "group",
      "guidelines",
      "manages",
      "mdc",
      "multiple",
      "or",
      "overview",
      "set",
      "that",
      "the",
      "unified",
      "usage"
    ]
  },
  {
    "name": "alertchip",
    "title": "Alert Chip",
    "tagName": "mdc-alertchip",
    "summary": "Usage, guidelines, and accessibility for the mdc-alertchip component — an interactive chip used to represent an alert with a leading icon and label.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/alertchip",
      "knowledgeBasePath": "components/src/components/alertchip/knowledge-base/alertchip.component.md"
    },
    "docs": "## Overview\n\nThe alert chip is an interactive chip whose color and leading icon indicate one of five visual states — `error`, `informational`, `neutral`, `success`, and `warning`. The variant only changes the visual styling; it does not announce or read out any alert content to assistive technologies on its own.\n\n### When to use\n\n- Use `mdc-alertchip` to surface a short, interactive status (error, success, warning, informational, neutral) inline within content.\n\n### When not to use\n\n- Use a non-interactive status indicator (e.g. `mdc-badge` or `mdc-staticchip`) when the chip should not be clickable or focusable.\n- Use `mdc-toast` or `mdc-banner` when the message needs more length, dedicated actions, or system-level prominence.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/alertchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { AlertChip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-alertchip variant=\"warning\" label=\"Connection unstable\"></mdc-alertchip>\n```\n\n### Content guidance\n\n- Limit the label text to a **maximum of 20 characters**, including empty spaces to split words.\n\n### Property/Attribute details\n\n- `variant` selects the visual tone and drives the default leading icon. Supported values: `neutral` (default), `error`, `success`, `warning`, `informational`.\n- `icon-name` overrides the default per-variant icon when a custom icon is required.\n\n## Accessibility\n\n### Built-in features\n\nThe alert chip behaves as a button.\n\n#### Internal ARIA managed by the component\n\n- The host element exposes `role=\"button\"` and participates in tab order like any other button.\n- Standard button keyboard interaction (Enter/Space activation) is provided.\n\n### Implementation requirements\n\n#### General\n\n- Treat the alert chip as a button when integrating it: ensure the surrounding context makes the action it triggers clear.\n- The variant color/icon is visual only and is not announced. If the alert state must be conveyed to assistive technologies, include the state in the `label` text (e.g. `label=\"Error: connection lost\"`) or in surrounding context.\n\n#### Labelling\n\n- Always set a meaningful `label`. The label is the accessible name announced by screen readers.\n- When the chip is icon-only (no label) for visual reasons, provide an `aria-label` so screen reader users still receive a name for the button.",
    "sections": {
      "Overview": "The alert chip is an interactive chip whose color and leading icon indicate one of five visual states — `error`, `informational`, `neutral`, `success`, and `warning`. The variant only changes the visual styling; it does not announce or read out any alert content to assistive technologies on its own.\n\n### When to use\n\n- Use `mdc-alertchip` to surface a short, interactive status (error, success, warning, informational, neutral) inline within content.\n\n### When not to use\n\n- Use a non-interactive status indicator (e.g. `mdc-badge` or `mdc-staticchip`) when the chip should not be clickable or focusable.\n- Use `mdc-toast` or `mdc-banner` when the message needs more length, dedicated actions, or system-level prominence.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/alertchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { AlertChip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-alertchip variant=\"warning\" label=\"Connection unstable\"></mdc-alertchip>\n```\n\n### Content guidance\n\n- Limit the label text to a **maximum of 20 characters**, including empty spaces to split words.\n\n### Property/Attribute details\n\n- `variant` selects the visual tone and drives the default leading icon. Supported values: `neutral` (default), `error`, `success`, `warning`, `informational`.\n- `icon-name` overrides the default per-variant icon when a custom icon is required.",
      "Accessibility": "### Built-in features\n\nThe alert chip behaves as a button.\n\n#### Internal ARIA managed by the component\n\n- The host element exposes `role=\"button\"` and participates in tab order like any other button.\n- Standard button keyboard interaction (Enter/Space activation) is provided.\n\n### Implementation requirements\n\n#### General\n\n- Treat the alert chip as a button when integrating it: ensure the surrounding context makes the action it triggers clear.\n- The variant color/icon is visual only and is not announced. If the alert state must be conveyed to assistive technologies, include the state in the `label` text (e.g. `label=\"Error: connection lost\"`) or in surrounding context.\n\n#### Labelling\n\n- Always set a meaningful `label`. The label is the accessible name announced by screen readers.\n- When the chip is icon-only (no label) for visual reasons, provide an `aria-label` so screen reader users still receive a name for the button."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "neutral",
          "values": [
            "neutral",
            "error",
            "success",
            "warning",
            "informational"
          ],
          "description": "/\nclass AlertChip extends IconNameMixin(Buttonsimple) {\n/**\nThe variant of the alertchip. It supports 5 variants\n- neutral\n- error\n- success\n- warning\n- informational",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the alertchip is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the alertchip."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the alertchip."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the alertchip receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "icon",
          "description": "The alert icon"
        },
        {
          "name": "label",
          "description": "The text label of the alertchip"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-chip-color",
          "description": "The color of the label text"
        },
        {
          "name": "--mdc-chip-icon-color",
          "description": "The color of the icon"
        },
        {
          "name": "--mdc-chip-border-color",
          "description": "The border color of the alertchip"
        },
        {
          "name": "--mdc-chip-background-color",
          "description": "The background color of the alertchip"
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/alertchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { AlertChip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-alertchip variant=\"warning\" label=\"Connection unstable\"></mdc-alertchip>"
      }
    ],
    "reactImports": [
      "AlertChip"
    ],
    "constants": {
      "VARIANTS": {
        "NEUTRAL": "neutral",
        "ERROR": "error",
        "SUCCESS": "success",
        "WARNING": "warning",
        "INFORMATIONAL": "informational"
      },
      "DEFAULTS": {
        "VARIANT": "neutral"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "alert",
      "alertchip",
      "an",
      "and",
      "chip",
      "component",
      "for",
      "guidelines",
      "icon",
      "interactive",
      "label",
      "leading",
      "mdc",
      "overview",
      "represent",
      "text",
      "the",
      "to",
      "usage",
      "used",
      "with"
    ]
  },
  {
    "name": "animation",
    "title": "Animation",
    "tagName": "mdc-animation",
    "summary": "Usage, guidelines, and accessibility for the mdc-animation component — a wrapper around the Lottie animation library for rendering decorative or labelled animations.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/animation",
      "knowledgeBasePath": "components/src/components/animation/knowledge-base/animation.component.md"
    },
    "docs": "## Overview\n\nThe animation component is a wrapper around the Lottie animation library. It fetches the animation data dynamically based on the provided name (or a `src` URL) and renders it. It is a display-only component with no interactive functionality and is treated as a decorative image by default.\n\n### When to use\n\n- Use `mdc-animation` to play short, looping or one-shot Lottie animations inline within UI — for example illustrations, loading indicators, or success/empty-state visuals.\n\n### When not to use\n\n- Use `mdc-icon` for a static glyph that does not need motion.\n- Use `mdc-illustration` for a non-animated illustration asset.\n- Use `mdc-progressspinner` or `mdc-progressbar` when the animation needs to communicate determinate or indeterminate progress with proper progress semantics.\n- Use `mdc-spinner` when you only need a lightweight, generic loading indicator without a dedicated Lottie asset.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/animation/index.js\"; // custom element registration\n// or via React wrapper\nimport { Animation } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-animation name=\"success\" loop=\"false\"></mdc-animation>\n```\n\n### Property/Attribute details\n\n- `name` — name of a bundled animation; resolved through the shipped animation manifest.\n- `src` — URL pointing to a Lottie JSON file. When set, it takes precedence over `name`.\n- `loop` — `\"true\"` (infinite, default), `\"false\"` (no loop), or a numeric string for a fixed loop count.\n- `autoplay` — starts the animation automatically when loaded (default `true`).\n\n## Accessibility\n\n### Built-in features\n\nThe internal animation container is rendered with `aria-hidden=\"true\"` so the underlying SVG/Lottie nodes are not exposed to assistive technologies.\n\n#### Internal ARIA managed by the component\n\n- When neither `aria-label` nor `aria-labelledby` is provided, the host has no role and is treated as a decorative image (skipped by assistive technologies).\n- When `aria-label` or `aria-labelledby` is set, the host receives `role=\"img\"` so screen readers announce it as a labelled image.\n\n### Implementation requirements\n\n#### Labelling\n\n- If the animation conveys meaning (e.g. a success state), provide an `aria-label` or `aria-labelledby` describing the meaning, not the motion.\n- Leave both labelling attributes unset when the animation is purely decorative; the component will keep it out of the accessibility tree.",
    "sections": {
      "Overview": "The animation component is a wrapper around the Lottie animation library. It fetches the animation data dynamically based on the provided name (or a `src` URL) and renders it. It is a display-only component with no interactive functionality and is treated as a decorative image by default.\n\n### When to use\n\n- Use `mdc-animation` to play short, looping or one-shot Lottie animations inline within UI — for example illustrations, loading indicators, or success/empty-state visuals.\n\n### When not to use\n\n- Use `mdc-icon` for a static glyph that does not need motion.\n- Use `mdc-illustration` for a non-animated illustration asset.\n- Use `mdc-progressspinner` or `mdc-progressbar` when the animation needs to communicate determinate or indeterminate progress with proper progress semantics.\n- Use `mdc-spinner` when you only need a lightweight, generic loading indicator without a dedicated Lottie asset.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/animation/index.js\"; // custom element registration\n// or via React wrapper\nimport { Animation } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-animation name=\"success\" loop=\"false\"></mdc-animation>\n```\n\n### Property/Attribute details\n\n- `name` — name of a bundled animation; resolved through the shipped animation manifest.\n- `src` — URL pointing to a Lottie JSON file. When set, it takes precedence over `name`.\n- `loop` — `\"true\"` (infinite, default), `\"false\"` (no loop), or a numeric string for a fixed loop count.\n- `autoplay` — starts the animation automatically when loaded (default `true`).",
      "Accessibility": "### Built-in features\n\nThe internal animation container is rendered with `aria-hidden=\"true\"` so the underlying SVG/Lottie nodes are not exposed to assistive technologies.\n\n#### Internal ARIA managed by the component\n\n- When neither `aria-label` nor `aria-labelledby` is provided, the host has no role and is treated as a decorative image (skipped by assistive technologies).\n- When `aria-label` or `aria-labelledby` is set, the host receives `role=\"img\"` so screen readers announce it as a labelled image.\n\n### Implementation requirements\n\n#### Labelling\n\n- If the animation conveys meaning (e.g. a success state), provide an `aria-label` or `aria-labelledby` describing the meaning, not the motion.\n- Leave both labelling attributes unset when the animation is purely decorative; the component will keep it out of the accessibility tree."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "undefined",
          "values": [],
          "description": "/\nclass Animation extends Component {\n/**\nName of the animation (= filename)",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "Aria-labelledby attribute to be set for accessibility",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "autoplay",
          "attribute": "autoplay",
          "type": "boolean",
          "values": [],
          "description": "starts the animation automatically when loaded (default `true`).",
          "source": "storybook"
        },
        {
          "name": "loop",
          "attribute": "loop",
          "type": "text",
          "values": [],
          "description": "`\"true\"` (infinite, default), `\"false\"` (no loop), or a numeric string for a fixed loop count.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "select",
          "values": [],
          "description": "name of a bundled animation; resolved through the shipped animation manifest.",
          "source": "storybook"
        },
        {
          "name": "src",
          "attribute": "src",
          "type": "text",
          "values": [],
          "description": "URL pointing to a Lottie JSON file. When set, it takes precedence over `name`.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "load",
          "description": "(React: onLoad) This event is dispatched when the animation is loaded"
        },
        {
          "name": "complete",
          "description": "(React: onComplete) This event is dispatched when all animation loops completed"
        },
        {
          "name": "error",
          "description": "(React: onError) This event is dispatched when animation loading failed"
        }
      ],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "name of a bundled animation; resolved through the shipped animation manifest.",
        "URL pointing to a Lottie JSON file. When set, it takes precedence over `name`.",
        "`\"true\"` (infinite, default), `\"false\"` (no loop), or a numeric string for a fixed loop count.",
        "starts the animation automatically when loaded (default `true`)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/animation/index.js\"; // custom element registration\n// or via React wrapper\nimport { Animation } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-animation name=\"success\" loop=\"false\"></mdc-animation>"
      }
    ],
    "reactImports": [
      "Animation"
    ],
    "constants": {
      "DEFAULTS": {
        "AUTO_PLAY": true,
        "LOOP": "true"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "animation",
      "animations",
      "around",
      "component",
      "decorative",
      "for",
      "guidelines",
      "labelled",
      "library",
      "lottie",
      "mdc",
      "or",
      "overview",
      "rendering",
      "the",
      "usage",
      "wrapper"
    ]
  },
  {
    "name": "announcementdialog",
    "title": "Announcement Dialog",
    "tagName": "mdc-announcementdialog",
    "summary": "Usage, guidelines, and accessibility for the mdc-announcementdialog component — a modal dialog used to display announcements with an illustration, content, and footer actions.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/announcementdialog",
      "knowledgeBasePath": "components/src/components/announcementdialog/knowledge-base/announcementdialog.component.md"
    },
    "docs": "## Overview\n\nThe announcement dialog is a modal dialog used to display announcements. Consumers provide an illustration, content, and footer actions through slots. It is available in four sizes — `medium`, `large`, `xlarge`, and `fullscreen` — and may also receive custom styling/sizing. The dialog interrupts the user and blocks interaction with the rest of the application until it is closed.\n\n### When to use\n\n- Use `mdc-announcementdialog` to highlight a one-off announcement, onboarding moment, or feature reveal that benefits from a prominent illustration alongside the message and primary action.\n\n### When not to use\n\n- Use `mdc-dialog` for a generic modal that does not need an illustration-led layout.\n- Use `mdc-toast` for non-blocking, transient feedback that should not interrupt the user.\n- Use `mdc-banner` for persistent in-page messaging that should sit within the flow rather than blocking it.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/announcementdialog/index.js\"; // custom element registration\n// or via React wrapper\nimport { AnnouncementDialog } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-announcementdialog\n  visible\n  size=\"medium\"\n  illustration=\"threetwozero-success\"\n  header-text=\"You're all set\"\n  aria-label=\"You're all set\"\n>\n  <span slot=\"description-container\">Your workspace is ready to use.</span>\n  <mdc-button slot=\"footer-button-primary\">Get started</mdc-button>\n</mdc-announcementdialog>\n```\n\nThe dialog is a controlled component — toggle the `visible` property to show or hide it, and handle the `close` event (fired when the Close button is clicked or Escape is pressed) to update your state. The `close` event does not hide the dialog on its own.\n\nIf a `triggerId` is provided, the dialog manages focus return to that element; otherwise it remembers the previously focused element before opening.\n\n### Content guidance\n\n- Choose an illustration that matches the dialog size — the illustration filename encodes its pixel size (e.g. `threetwozero` = 320px).\n- Prefer the `footer-link`, `footer-button-secondary`, and `footer-button-primary` slots over the generic `footer` slot so footer layout stays consistent.\n\n### Property/Attribute details\n\n- `size` — `medium` (656px), `large` (992px), `xlarge` (90% width), or `fullscreen` (100% width). When the responsive settings context has `forceFullscreenDialog` enabled, the effective size is always `fullscreen` regardless of the prop.\n\n## Accessibility\n\n### Built-in features\n\n- Renders as a modal dialog and traps focus inside the dialog while it is open.\n- Closes when the user presses Escape (fires the `close` event).\n- Prevents the underlying page from scrolling while the dialog is visible.\n- Returns focus to the previously focused element when the dialog closes; when `triggerId` is set, focus is returned to that element instead.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute       | Value                                                                                  |\n| -------------- | --------------- | -------------------------------------------------------------------------------------- |\n| Host           | `role`          | `dialog` (default) or `alertdialog` when set by the consumer                           |\n| Host           | `aria-modal`    | `true` while the dialog is visible                                                     |\n| Host           | `aria-label`    | Mirrored from `header-text` when neither `aria-label` nor `aria-labelledby` is set     |\n| Host           | `aria-labelledby` | Set to `triggerID` when provided and no labelling attribute is already set           |\n| Close button   | `aria-label`    | Reflected from the `close-button-aria-label` attribute                                 |\n\n### Implementation requirements\n\n#### Labelling\n\n- The dialog **must** have either an `aria-label` or `aria-labelledby` attribute so screen readers announce a name when it opens.\n- Use `aria-labelledby` to reference the `id` of the element that labels the dialog when there is no visible title.",
    "sections": {
      "Overview": "The announcement dialog is a modal dialog used to display announcements. Consumers provide an illustration, content, and footer actions through slots. It is available in four sizes — `medium`, `large`, `xlarge`, and `fullscreen` — and may also receive custom styling/sizing. The dialog interrupts the user and blocks interaction with the rest of the application until it is closed.\n\n### When to use\n\n- Use `mdc-announcementdialog` to highlight a one-off announcement, onboarding moment, or feature reveal that benefits from a prominent illustration alongside the message and primary action.\n\n### When not to use\n\n- Use `mdc-dialog` for a generic modal that does not need an illustration-led layout.\n- Use `mdc-toast` for non-blocking, transient feedback that should not interrupt the user.\n- Use `mdc-banner` for persistent in-page messaging that should sit within the flow rather than blocking it.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/announcementdialog/index.js\"; // custom element registration\n// or via React wrapper\nimport { AnnouncementDialog } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-announcementdialog\n  visible\n  size=\"medium\"\n  illustration=\"threetwozero-success\"\n  header-text=\"You're all set\"\n  aria-label=\"You're all set\"\n>\n  <span slot=\"description-container\">Your workspace is ready to use.</span>\n  <mdc-button slot=\"footer-button-primary\">Get started</mdc-button>\n</mdc-announcementdialog>\n```\n\nThe dialog is a controlled component — toggle the `visible` property to show or hide it, and handle the `close` event (fired when the Close button is clicked or Escape is pressed) to update your state. The `close` event does not hide the dialog on its own.\n\nIf a `triggerId` is provided, the dialog manages focus return to that element; otherwise it remembers the previously focused element before opening.\n\n### Content guidance\n\n- Choose an illustration that matches the dialog size — the illustration filename encodes its pixel size (e.g. `threetwozero` = 320px).\n- Prefer the `footer-link`, `footer-button-secondary`, and `footer-button-primary` slots over the generic `footer` slot so footer layout stays consistent.\n\n### Property/Attribute details\n\n- `size` — `medium` (656px), `large` (992px), `xlarge` (90% width), or `fullscreen` (100% width). When the responsive settings context has `forceFullscreenDialog` enabled, the effective size is always `fullscreen` regardless of the prop.",
      "Accessibility": "### Built-in features\n\n- Renders as a modal dialog and traps focus inside the dialog while it is open.\n- Closes when the user presses Escape (fires the `close` event).\n- Prevents the underlying page from scrolling while the dialog is visible.\n- Returns focus to the previously focused element when the dialog closes; when `triggerId` is set, focus is returned to that element instead.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute       | Value                                                                                  |\n| -------------- | --------------- | -------------------------------------------------------------------------------------- |\n| Host           | `role`          | `dialog` (default) or `alertdialog` when set by the consumer                           |\n| Host           | `aria-modal`    | `true` while the dialog is visible                                                     |\n| Host           | `aria-label`    | Mirrored from `header-text` when neither `aria-label` nor `aria-labelledby` is set     |\n| Host           | `aria-labelledby` | Set to `triggerID` when provided and no labelling attribute is already set           |\n| Close button   | `aria-label`    | Reflected from the `close-button-aria-label` attribute                                 |\n\n### Implementation requirements\n\n#### Labelling\n\n- The dialog **must** have either an `aria-label` or `aria-labelledby` attribute so screen readers announce a name when it opens.\n- Use `aria-labelledby` to reference the `id` of the element that labels the dialog when there is no visible title."
    },
    "dependencies": [
      "mdc-illustration",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaDescribedby",
          "attribute": "aria-describedby",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaDescription",
          "attribute": "aria-description",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "closeButtonAriaLabel",
          "attribute": "close-button-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "focusTrap",
          "attribute": "focus-trap",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "headerTagName",
          "attribute": "header-tag-name",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "illustration",
          "attribute": "illustration",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "role",
          "attribute": "role",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "shouldFocusTrapWrap",
          "attribute": "should-focus-trap-wrap",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "values": [],
          "description": "`medium` (656px), `large` (992px), `xlarge` (90% width), or `fullscreen` (100% width). When the responsive settings context has `forceFullscreenDialog` enabled, the effective size is always `fullscreen` regardless of the prop.",
          "source": "storybook"
        },
        {
          "name": "triggerId",
          "attribute": "trigger-id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "visible",
          "attribute": "visible",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "zIndex",
          "attribute": "z-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "header-prefix",
          "description": "Slot for the dialog header content. This can be used to pass custom header content."
        },
        {
          "name": "dialog-body",
          "description": "Slot for the dialog body content"
        },
        {
          "name": "illustration-container",
          "description": "Slot for the illustration container section"
        },
        {
          "name": "content-container",
          "description": "Slot for the content (header, description, etc) section"
        },
        {
          "name": "description-container",
          "description": "Slot for the description in the content (below header) - pass in Text, Links etc as needed."
        },
        {
          "name": "footer-link",
          "description": "This slot is for passing `mdc-link` component within the footer section."
        },
        {
          "name": "footer-button-secondary",
          "description": "This slot is for passing secondary variant of `mdc-button` component"
        },
        {
          "name": "footer-button-primary",
          "description": "This slot is for passing primary variant of"
        },
        {
          "name": "footer",
          "description": "This slot is for passing custom footer content. Only use this if really needed,"
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) Dispatched when the dialog is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) Dispatched when the dialog is hidden"
        },
        {
          "name": "created",
          "description": "(React: onCreated) Dispatched when the dialog is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) Dispatched when the dialog is destroyed (removed from the DOM)"
        },
        {
          "name": "close",
          "description": "(React: onClose) Dispatched when the Close Button is clicked or Escape key is pressed"
        }
      ],
      "cssParts": [
        {
          "name": "body",
          "description": "The main body container that holds the illustration and content sections"
        },
        {
          "name": "illustration-container",
          "description": "The container for the illustration section"
        },
        {
          "name": "content-container",
          "description": "The container for the content section"
        },
        {
          "name": "header-text",
          "description": "The header text"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-announcementdialog-illustration-background-color",
          "description": "background color of the illustration section"
        },
        {
          "name": "--mdc-dialog-primary-background-color",
          "description": "primary background color of the dialog"
        },
        {
          "name": "--mdc-dialog-border-color",
          "description": "border color of the dialog"
        },
        {
          "name": "--mdc-dialog-header-text-color",
          "description": "text color of the header/title of the dialog"
        },
        {
          "name": "--mdc-dialog-description-text-color",
          "description": "text color of the below header description of the dialog"
        },
        {
          "name": "--mdc-dialog-elevation-3",
          "description": "elevation of the dialog"
        },
        {
          "name": "--mdc-dialog-width",
          "description": "width of the dialog"
        },
        {
          "name": "--mdc-dialog-height",
          "description": "height of the dialog"
        }
      ],
      "rules": [
        "`medium` (656px), `large` (992px), `xlarge` (90% width), or `fullscreen` (100% width). When the responsive settings context has `forceFullscreenDialog` enabled, the effective size is always `fullscreen` regardless of the prop."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/announcementdialog/index.js\"; // custom element registration\n// or via React wrapper\nimport { AnnouncementDialog } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-announcementdialog\n  visible\n  size=\"medium\"\n  illustration=\"threetwozero-success\"\n  header-text=\"You're all set\"\n  aria-label=\"You're all set\"\n>\n  <span slot=\"description-container\">Your workspace is ready to use.</span>\n  <mdc-button slot=\"footer-button-primary\">Get started</mdc-button>\n</mdc-announcementdialog>"
      }
    ],
    "reactImports": [
      "AnnouncementDialog"
    ],
    "constants": {
      "DEFAULTS": {
        "SIZE": "medium"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "actions",
      "an",
      "and",
      "announcement",
      "announcementdialog",
      "announcements",
      "component",
      "content",
      "dialog",
      "display",
      "footer",
      "for",
      "guidelines",
      "illustration",
      "mdc",
      "modal",
      "overview",
      "text",
      "the",
      "to",
      "usage",
      "used",
      "with"
    ]
  },
  {
    "name": "appheader",
    "title": "App Header",
    "tagName": "mdc-appheader",
    "summary": "Usage, guidelines, and accessibility for the mdc-appheader component — a structured app header layout with leading, center, and trailing sections.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/appheader",
      "knowledgeBasePath": "components/src/components/appheader/knowledge-base/appheader.component.md"
    },
    "docs": "## Overview\n\nThe app header provides a structured layout for the top of an application with three sections: leading, center, and trailing. Each section automatically handles flex alignment — leading aligns left, center aligns center, trailing aligns right.\n\n### When to use\n\n- Use `mdc-appheader` as the top-level bar of an application to host branding, primary navigation/search, and account or action controls in a consistent layout.\n\n### When not to use\n\n- Use a plain `<header>` element when you only need a simple top bar without the leading/center/trailing three-section structure.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/appheader/index.js\"; // custom element registration\n// or via React wrapper\nimport { Appheader } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-appheader>\n  <mdc-brandvisual slot=\"leading\" name=\"logo\"></mdc-brandvisual>\n  <mdc-searchfield slot=\"center\"></mdc-searchfield>\n  <mdc-avatarbutton slot=\"trailing\" initials=\"AB\"></mdc-avatarbutton>\n</mdc-appheader>\n```\n\n### Content guidance\n\n- **Leading** — typically the brand logo, brand name, or a menu/back icon.\n- **Center** — search bar, primary navigation links, or quick action controls.\n- **Trailing** — profile avatar, settings, notifications, or other account-level controls.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders its container as a `<header>` landmark element, exposing the app header as a `banner` landmark to assistive technologies by default when used at the top of the document.\n\n#### Internal ARIA managed by the component\n\n| Element              | Attribute | Value                                       |\n| -------------------- | --------- | ------------------------------------------- |\n| Container (`header`) | `role`    | Implicit `banner` (from the `<header>` tag) |\n\n### Implementation requirements\n\n#### General\n\n- Use only one `mdc-appheader` per page; multiple `banner` landmarks confuse assistive technologies.\n- Provide accessible names for any interactive controls slotted into the header (logos, buttons, search field) — the header itself does not label its contents.",
    "sections": {
      "Overview": "The app header provides a structured layout for the top of an application with three sections: leading, center, and trailing. Each section automatically handles flex alignment — leading aligns left, center aligns center, trailing aligns right.\n\n### When to use\n\n- Use `mdc-appheader` as the top-level bar of an application to host branding, primary navigation/search, and account or action controls in a consistent layout.\n\n### When not to use\n\n- Use a plain `<header>` element when you only need a simple top bar without the leading/center/trailing three-section structure.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/appheader/index.js\"; // custom element registration\n// or via React wrapper\nimport { Appheader } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-appheader>\n  <mdc-brandvisual slot=\"leading\" name=\"logo\"></mdc-brandvisual>\n  <mdc-searchfield slot=\"center\"></mdc-searchfield>\n  <mdc-avatarbutton slot=\"trailing\" initials=\"AB\"></mdc-avatarbutton>\n</mdc-appheader>\n```\n\n### Content guidance\n\n- **Leading** — typically the brand logo, brand name, or a menu/back icon.\n- **Center** — search bar, primary navigation links, or quick action controls.\n- **Trailing** — profile avatar, settings, notifications, or other account-level controls.",
      "Accessibility": "### Built-in features\n\nThe component renders its container as a `<header>` landmark element, exposing the app header as a `banner` landmark to assistive technologies by default when used at the top of the document.\n\n#### Internal ARIA managed by the component\n\n| Element              | Attribute | Value                                       |\n| -------------------- | --------- | ------------------------------------------- |\n| Container (`header`) | `role`    | Implicit `banner` (from the `<header>` tag) |\n\n### Implementation requirements\n\n#### General\n\n- Use only one `mdc-appheader` per page; multiple `banner` landmarks confuse assistive technologies.\n- Provide accessible names for any interactive controls slotted into the header (logos, buttons, search field) — the header itself does not label its contents."
    },
    "dependencies": [],
    "api": {
      "properties": [],
      "slots": [
        {
          "name": "leading",
          "description": "Content for the leading section (left-aligned)."
        },
        {
          "name": "center",
          "description": "Content for the center section (center-aligned)."
        },
        {
          "name": "trailing",
          "description": "Content for the trailing section (right-aligned)."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "container",
          "description": "The main header container."
        },
        {
          "name": "leading-section",
          "description": "The leading section wrapper (left side)."
        },
        {
          "name": "center-section",
          "description": "The center section wrapper (middle)."
        },
        {
          "name": "trailing-section",
          "description": "The trailing section wrapper (right side)."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-appheader-height",
          "description": "Height of the app header. Default is 4rem (64px)."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/appheader/index.js\"; // custom element registration\n// or via React wrapper\nimport { Appheader } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-appheader>\n  <mdc-brandvisual slot=\"leading\" name=\"logo\"></mdc-brandvisual>\n  <mdc-searchfield slot=\"center\"></mdc-searchfield>\n  <mdc-avatarbutton slot=\"trailing\" initials=\"AB\"></mdc-avatarbutton>\n</mdc-appheader>"
      }
    ],
    "reactImports": [
      "Appheader"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "app",
      "appheader",
      "center",
      "component",
      "for",
      "guidelines",
      "header",
      "layout",
      "leading",
      "mdc",
      "overview",
      "sections",
      "structured",
      "the",
      "trailing",
      "usage",
      "with"
    ]
  },
  {
    "name": "avatar",
    "title": "Avatar",
    "tagName": "mdc-avatar",
    "summary": "Usage, guidelines, and accessibility for the mdc-avatar component — a non-interactive avatar that displays a person or group as a photo, initials, icon, or counter.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/avatar",
      "knowledgeBasePath": "components/src/components/avatar/knowledge-base/avatar.component.md"
    },
    "docs": "## Overview\n\nThe avatar represents a person or a group. It can be rendered as a photo, initials, icon, or counter, and is non-interactive and non-focusable by default. An optional presence indicator and a typing loading state are also supported.\n\n### When to use\n\n- Use `mdc-avatar` to visually represent a person or group inline within content (e.g. lists, cards, message rows).\n\n### When not to use\n\n- Use `mdc-avatarbutton` when the avatar must be clickable or focusable (profile menu, account switcher, etc.).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/avatar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Avatar } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-avatar initials=\"AB\" presence=\"active\"></mdc-avatar>\n```\n\n### Content guidance\n\n- **Initials** are uppercased and truncated to the first two characters automatically.\n- **Counter** values are clamped: negatives display as `0`, values above 99 display as `99+`.\n\n### Property/Attribute details\n\nWhen multiple display attributes are provided, the avatar picks what to render in this priority order:\n\n1. **Photo** (`src`) — highest priority. While loading, `initials` show as an instant placeholder when provided, otherwise the icon is shown (after the icon library loads). On load error, the placeholder remains visible.\n2. **Icon** (`icon-name`) — used when `src` is not provided. If both `icon-name` and `initials` are provided (without `src`), the icon wins and `initials` are ignored, which means users may briefly see nothing while the icon library loads even though initials would have rendered instantly. Defaults to `user-regular` when nothing else is available.\n3. **Initials** (`initials`) — shown when neither `src` nor `icon-name` is provided. Renders instantly.\n4. **Counter** (`counter`) — shown only when none of the above are provided.\n\nOther behaviour worth knowing:\n\n- When `is-typing` is `true`, a typing loading indicator overlays the existing content.\n- The presence indicator is hidden when the avatar is rendering a counter, or when it is in the typing state.\n\n## Accessibility\n\n### Built-in features\n\nThe avatar is treated as decorative by default — it is hidden from assistive technologies so screen readers do not announce it when it is purely visual context.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute     | Value                                                                              |\n| -------------- | ------------- | ---------------------------------------------------------------------------------- |\n| Host           | `aria-hidden` | `true` by default; consumers may set `aria-hidden=\"false\"` when the avatar conveys meaning |\n| Photo (`img`)  | `aria-hidden` | `true` (the host carries the accessible exposure)                                   |\n\n### Implementation requirements\n\n#### General\n\n- Override `aria-hidden=\"false\"` only when the avatar is the sole source of identity information for the user (e.g. when no name is shown next to it). Provide an `aria-label` in that case.\n- Prefer pairing the avatar with adjacent visible text (name, group name) so the avatar can stay decorative and the text carries the accessible name.",
    "sections": {
      "Overview": "The avatar represents a person or a group. It can be rendered as a photo, initials, icon, or counter, and is non-interactive and non-focusable by default. An optional presence indicator and a typing loading state are also supported.\n\n### When to use\n\n- Use `mdc-avatar` to visually represent a person or group inline within content (e.g. lists, cards, message rows).\n\n### When not to use\n\n- Use `mdc-avatarbutton` when the avatar must be clickable or focusable (profile menu, account switcher, etc.).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/avatar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Avatar } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-avatar initials=\"AB\" presence=\"active\"></mdc-avatar>\n```\n\n### Content guidance\n\n- **Initials** are uppercased and truncated to the first two characters automatically.\n- **Counter** values are clamped: negatives display as `0`, values above 99 display as `99+`.\n\n### Property/Attribute details\n\nWhen multiple display attributes are provided, the avatar picks what to render in this priority order:\n\n1. **Photo** (`src`) — highest priority. While loading, `initials` show as an instant placeholder when provided, otherwise the icon is shown (after the icon library loads). On load error, the placeholder remains visible.\n2. **Icon** (`icon-name`) — used when `src` is not provided. If both `icon-name` and `initials` are provided (without `src`), the icon wins and `initials` are ignored, which means users may briefly see nothing while the icon library loads even though initials would have rendered instantly. Defaults to `user-regular` when nothing else is available.\n3. **Initials** (`initials`) — shown when neither `src` nor `icon-name` is provided. Renders instantly.\n4. **Counter** (`counter`) — shown only when none of the above are provided.\n\nOther behaviour worth knowing:\n\n- When `is-typing` is `true`, a typing loading indicator overlays the existing content.\n- The presence indicator is hidden when the avatar is rendering a counter, or when it is in the typing state.",
      "Accessibility": "### Built-in features\n\nThe avatar is treated as decorative by default — it is hidden from assistive technologies so screen readers do not announce it when it is purely visual context.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute     | Value                                                                              |\n| -------------- | ------------- | ---------------------------------------------------------------------------------- |\n| Host           | `aria-hidden` | `true` by default; consumers may set `aria-hidden=\"false\"` when the avatar conveys meaning |\n| Photo (`img`)  | `aria-hidden` | `true` (the host carries the accessible exposure)                                   |\n\n### Implementation requirements\n\n#### General\n\n- Override `aria-hidden=\"false\"` only when the avatar is the sole source of identity information for the user (e.g. when no name is shown next to it). Provide an `aria-label` in that case.\n- Prefer pairing the avatar with adjacent visible text (name, group name) so the avatar can stay decorative and the text carries the accessible name."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-presence",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "counter",
          "attribute": "counter",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "initials",
          "attribute": "initials",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "isTyping",
          "attribute": "is-typing",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "presence",
          "attribute": "presence",
          "type": "select",
          "values": [
            "none"
          ],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "values": [
            24,
            32,
            48,
            64,
            72,
            88,
            124
          ],
          "source": "storybook"
        },
        {
          "name": "src",
          "attribute": "src",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "photo",
          "description": "The photo of the avatar."
        },
        {
          "name": "presence",
          "description": "The presence indicator of the avatar."
        },
        {
          "name": "loading-wrapper",
          "description": "The wrapper for the loading indicator."
        },
        {
          "name": "loader",
          "description": "The loading indicator of the avatar."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-avatar-default-background-color",
          "description": "Allows customization of the default background color."
        },
        {
          "name": "--mdc-avatar-default-foreground-color",
          "description": "Allows customization of the default foreground color."
        },
        {
          "name": "--mdc-avatar-loading-indicator-background-color -",
          "description": ""
        },
        {
          "name": "--mdc-avatar-loading-indicator-foreground-color -",
          "description": ""
        },
        {
          "name": "--mdc-avatar-loading-overlay-background-color -",
          "description": ""
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/avatar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Avatar } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-avatar initials=\"AB\" presence=\"active\"></mdc-avatar>"
      }
    ],
    "reactImports": [
      "Avatar"
    ],
    "constants": {
      "AVATAR_TYPE": {
        "COUNTER": "counter",
        "ICON": "icon",
        "PHOTO": "photo",
        "TEXT": "text"
      },
      "AVATAR_SIZE": {
        "24": 24,
        "32": 32,
        "48": 48,
        "64": 64,
        "72": 72,
        "88": 88,
        "124": 124
      },
      "DEFAULTS": {
        "TYPE": "photo",
        "SIZE": 32
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "as",
      "avatar",
      "component",
      "counter",
      "displays",
      "for",
      "group",
      "guidelines",
      "icon",
      "initials",
      "interactive",
      "mdc",
      "non",
      "or",
      "overview",
      "person",
      "photo",
      "presence",
      "text",
      "that",
      "the",
      "usage"
    ]
  },
  {
    "name": "avatarbutton",
    "title": "Avatar Button",
    "tagName": "mdc-avatarbutton",
    "summary": "Usage, guidelines, and accessibility for the mdc-avatarbutton component — an interactive, focusable avatar wrapped in a button.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/avatarbutton",
      "knowledgeBasePath": "components/src/components/avatarbutton/knowledge-base/avatarbutton.component.md"
    },
    "docs": "## Overview\n\nThe avatar button is an interactive, clickable version of the avatar. It wraps the avatar in a button so it becomes focusable and actionable, while still supporting all avatar display modes (photo, initials, icon, counter) and the presence indicator. Use CSS parts to customise the avatar appearance inside the button.\n\n### When to use\n\n- Use `mdc-avatarbutton` when the avatar must be clickable or keyboard-focusable — for example a profile menu trigger, account switcher, or a member chip that opens a details panel.\n\n### When not to use\n\n- Use `mdc-avatar` when the avatar is purely decorative or informational and should not be interactive.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/avatarbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { AvatarButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-avatarbutton initials=\"AB\" aria-label=\"Open profile menu\"></mdc-avatarbutton>\n```\n\n### Property/Attribute details\n\nWhen multiple display attributes are provided, the avatar button picks what to render in this priority order:\n\n1. **Photo** (`src`) — highest priority. While loading, `initials` show as an instant placeholder when provided, otherwise the icon is shown (after the icon library loads). On load error, the placeholder remains visible.\n2. **Icon** (`icon-name`) — used when `src` is not provided. If both `icon-name` and `initials` are provided (without `src`), the icon wins and `initials` are ignored. Defaults to `user-regular` when nothing else is available.\n3. **Initials** (`initials`) — shown when neither `src` nor `icon-name` is provided. Uppercased and truncated to the first two characters.\n4. **Counter** (`counter`) — shown only when none of the above are provided. Negatives display as `0`, values above 99 display as `99+`.\n\nOther supported attributes:\n\n- `size` — avatar size in px: `24`, `32` (default), `48`, `64`, `72`, `88`, `124`. Invalid values fall back to the default.\n- `presence` — activity status (`active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`). Hidden when a counter is rendered or while typing.\n- `is-typing` — when `true`, overlays a typing loading indicator on top of the current content.\n- `aria-label` — required accessible name for the button (see Accessibility).\n\n## Accessibility\n\n### Built-in features\n\nThe component behaves as a button: it is keyboard-focusable, activates on Enter/Space, and exposes button semantics to assistive technologies.\n\n#### Internal ARIA managed by the component\n\n| Element            | Attribute     | Value                                                          |\n| ------------------ | ------------- | -------------------------------------------------------------- |\n| Host               | `role`        | `button`                                                       |\n| Host               | `aria-label`  | Reflected from the `aria-label` attribute set by the consumer  |\n| Overlay (`div`)    | `aria-hidden` | `true` (decorative styling layer)                              |\n| Inner `mdc-avatar` | `aria-hidden` | `true` (the host carries the accessible name and button role)  |\n\n### Implementation requirements\n\n#### Labelling\n\n- **Always** provide an `aria-label` describing the button's purpose (e.g. `aria-label=\"Open profile menu\"`). Without it the button has no accessible name, since the inner avatar is hidden from assistive technologies.",
    "sections": {
      "Overview": "The avatar button is an interactive, clickable version of the avatar. It wraps the avatar in a button so it becomes focusable and actionable, while still supporting all avatar display modes (photo, initials, icon, counter) and the presence indicator. Use CSS parts to customise the avatar appearance inside the button.\n\n### When to use\n\n- Use `mdc-avatarbutton` when the avatar must be clickable or keyboard-focusable — for example a profile menu trigger, account switcher, or a member chip that opens a details panel.\n\n### When not to use\n\n- Use `mdc-avatar` when the avatar is purely decorative or informational and should not be interactive.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/avatarbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { AvatarButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-avatarbutton initials=\"AB\" aria-label=\"Open profile menu\"></mdc-avatarbutton>\n```\n\n### Property/Attribute details\n\nWhen multiple display attributes are provided, the avatar button picks what to render in this priority order:\n\n1. **Photo** (`src`) — highest priority. While loading, `initials` show as an instant placeholder when provided, otherwise the icon is shown (after the icon library loads). On load error, the placeholder remains visible.\n2. **Icon** (`icon-name`) — used when `src` is not provided. If both `icon-name` and `initials` are provided (without `src`), the icon wins and `initials` are ignored. Defaults to `user-regular` when nothing else is available.\n3. **Initials** (`initials`) — shown when neither `src` nor `icon-name` is provided. Uppercased and truncated to the first two characters.\n4. **Counter** (`counter`) — shown only when none of the above are provided. Negatives display as `0`, values above 99 display as `99+`.\n\nOther supported attributes:\n\n- `size` — avatar size in px: `24`, `32` (default), `48`, `64`, `72`, `88`, `124`. Invalid values fall back to the default.\n- `presence` — activity status (`active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`). Hidden when a counter is rendered or while typing.\n- `is-typing` — when `true`, overlays a typing loading indicator on top of the current content.\n- `aria-label` — required accessible name for the button (see Accessibility).",
      "Accessibility": "### Built-in features\n\nThe component behaves as a button: it is keyboard-focusable, activates on Enter/Space, and exposes button semantics to assistive technologies.\n\n#### Internal ARIA managed by the component\n\n| Element            | Attribute     | Value                                                          |\n| ------------------ | ------------- | -------------------------------------------------------------- |\n| Host               | `role`        | `button`                                                       |\n| Host               | `aria-label`  | Reflected from the `aria-label` attribute set by the consumer  |\n| Overlay (`div`)    | `aria-hidden` | `true` (decorative styling layer)                              |\n| Inner `mdc-avatar` | `aria-hidden` | `true` (the host carries the accessible name and button role)  |\n\n### Implementation requirements\n\n#### Labelling\n\n- **Always** provide an `aria-label` describing the button's purpose (e.g. `aria-label=\"Open profile menu\"`). Without it the button has no accessible name, since the inner avatar is hidden from assistive technologies."
    },
    "dependencies": [
      "mdc-avatar"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "required accessible name for the button (see Accessibility).",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "counter",
          "attribute": "counter",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "initials",
          "attribute": "initials",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "isTyping",
          "attribute": "is-typing",
          "type": "boolean",
          "values": [],
          "description": "when `true`, overlays a typing loading indicator on top of the current content.",
          "source": "storybook"
        },
        {
          "name": "presence",
          "attribute": "presence",
          "type": "select",
          "values": [
            "none"
          ],
          "description": "activity status (`active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`). Hidden when a counter is rendered or while typing.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "values": [],
          "description": "avatar size in px: `24`, `32` (default), `48`, `64`, `72`, `88`, `124`. Invalid values fall back to the default.",
          "source": "storybook"
        },
        {
          "name": "src",
          "attribute": "src",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Dispatched when the avatar button is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) Dispatched when a key is pressed on the avatar button."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) Dispatched when a key is released on the avatar button."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Dispatched when the avatar button receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "overlay",
          "description": "The overlay part of the avatar button."
        },
        {
          "name": "content",
          "description": "The main content of the avatar."
        },
        {
          "name": "photo",
          "description": "The photo part of the avatar."
        },
        {
          "name": "presence",
          "description": "The presence indicator part of the avatar."
        },
        {
          "name": "loading-wrapper",
          "description": "The wrapper for the loading indicator of the avatar."
        },
        {
          "name": "loader",
          "description": "The loading indicator part of the avatar."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-avatarbutton-overlay-background-color-rest",
          "description": "Background color of the overlay in rest state."
        },
        {
          "name": "--mdc-avatarbutton-overlay-background-color-hover",
          "description": "Background color of the overlay in hover state."
        },
        {
          "name": "--mdc-avatarbutton-overlay-background-color-active",
          "description": "Background color of the overlay in active state."
        },
        {
          "name": "--mdc-avatarbutton-default-background-color",
          "description": "Default background color of the avatar."
        },
        {
          "name": "--mdc-avatarbutton-default-foreground-color",
          "description": "Default foreground color of the avatar."
        },
        {
          "name": "--mdc-avatarbutton-loading-indicator-background-color",
          "description": "Background color of the loading indicator."
        },
        {
          "name": "--mdc-avatarbutton-loading-indicator-foreground-color",
          "description": "Foreground color of the loading indicator."
        },
        {
          "name": "--mdc-avatarbutton-loading-overlay-background-color",
          "description": "Background color of the loading overlay."
        }
      ],
      "rules": [
        "avatar size in px: `24`, `32` (default), `48`, `64`, `72`, `88`, `124`. Invalid values fall back to the default.",
        "activity status (`active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`). Hidden when a counter is rendered or while typing.",
        "when `true`, overlays a typing loading indicator on top of the current content.",
        "required accessible name for the button (see Accessibility)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/avatarbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { AvatarButton } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-avatarbutton initials=\"AB\" aria-label=\"Open profile menu\"></mdc-avatarbutton>"
      }
    ],
    "reactImports": [
      "AvatarButton"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "an",
      "and",
      "avatar",
      "avatarbutton",
      "button",
      "component",
      "focusable",
      "for",
      "guidelines",
      "in",
      "interactive",
      "mdc",
      "overview",
      "the",
      "usage",
      "wrapped"
    ]
  },
  {
    "name": "badge",
    "title": "Badge",
    "tagName": "mdc-badge",
    "summary": "Usage, guidelines, and accessibility for the mdc-badge component — a small non-interactive indicator used to display a dot, icon, counter, or status (success, warning, error).",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/badge",
      "knowledgeBasePath": "components/src/components/badge/knowledge-base/badge.component.md"
    },
    "docs": "## Overview\n\nThe badge is a small, non-interactive indicator used to surface a notification, status, or count. It supports six types — `dot`, `icon`, `counter`, `success`, `warning`, and `error` — and can be layered on top of other UI (for example a button) with an optional outline overlay.\n\n### When to use\n\n- Use `mdc-badge` to show an unread/notification dot, a numeric counter, or a status indicator next to or on top of another element.\n\n### When not to use\n\n- Use `mdc-alertchip` when the indicator must be interactive (focusable, clickable) and behave as a button.\n- Use `mdc-staticchip` or `mdc-text` when the status needs richer label content rather than a single icon, dot, or count.\n- Use `mdc-presence` when you specifically need to indicate a user's activity/availability state (e.g. active, away, busy, dnd) rather than a generic notification, count, or status.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/badge/index.js\"; // custom element registration\n// or via React wrapper\nimport { Badge } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-badge type=\"counter\" counter=\"5\" aria-label=\"5 unread messages\"></mdc-badge>\n```\n\n### Property/Attribute details\n\n- `type` — selects the badge shape and content:\n  - `dot` (default) — a small notification dot.\n  - `icon` — renders the icon named by `icon-name`.\n  - `counter` — renders the numeric `counter` value as text.\n  - `success` — renders a check-circle icon with success styling.\n  - `warning` — renders a warning icon with warning styling.\n  - `error` — renders an error icon with error styling.\n- `variant` — `primary` (default) or `secondary`; controls the badge foreground/background colors.\n- `counter` — numeric value rendered when `type=\"counter\"`. Non-numeric values render as empty.\n- `max-counter` — `9`, `99`, or `999` (default `99`). When `counter` exceeds this value, the badge renders `<maxCounter>+`. Values above the hard ceiling of 999 always render as `999+`.\n- `overlay` — when `true`, adds a thin outline so the badge reads clearly when stacked on top of another element (e.g. a button). Defaults to `false`.\n- `icon-name` — required when `type=\"icon\"`; ignored for the preset variants which use their own icons.\n- `aria-label` — accessible name for the badge; see Accessibility.\n\n## Accessibility\n\n### Built-in features\n\nThe badge is decorative by default and is not exposed to assistive technologies unless the consumer provides an `aria-label`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute    | Value                                                                                  |\n| ------- | ------------ | -------------------------------------------------------------------------------------- |\n| Host    | `role`       | `img` when `aria-label` is set; otherwise no role is applied                           |\n| Host    | `aria-label` | Reflected from the `aria-label` attribute set by the consumer                          |\n\n### Implementation requirements\n\n#### Labelling\n\n- When the badge carries meaning (counter value, status), provide an `aria-label` describing it (e.g. `aria-label=\"5 unread\"`, `aria-label=\"Error\"`). The component then exposes itself as a labelled `img`.\n- Leave `aria-label` unset when the badge is purely decorative or its meaning is already conveyed by adjacent text — the component stays out of the accessibility tree.",
    "sections": {
      "Overview": "The badge is a small, non-interactive indicator used to surface a notification, status, or count. It supports six types — `dot`, `icon`, `counter`, `success`, `warning`, and `error` — and can be layered on top of other UI (for example a button) with an optional outline overlay.\n\n### When to use\n\n- Use `mdc-badge` to show an unread/notification dot, a numeric counter, or a status indicator next to or on top of another element.\n\n### When not to use\n\n- Use `mdc-alertchip` when the indicator must be interactive (focusable, clickable) and behave as a button.\n- Use `mdc-staticchip` or `mdc-text` when the status needs richer label content rather than a single icon, dot, or count.\n- Use `mdc-presence` when you specifically need to indicate a user's activity/availability state (e.g. active, away, busy, dnd) rather than a generic notification, count, or status.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/badge/index.js\"; // custom element registration\n// or via React wrapper\nimport { Badge } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-badge type=\"counter\" counter=\"5\" aria-label=\"5 unread messages\"></mdc-badge>\n```\n\n### Property/Attribute details\n\n- `type` — selects the badge shape and content:\n  - `dot` (default) — a small notification dot.\n  - `icon` — renders the icon named by `icon-name`.\n  - `counter` — renders the numeric `counter` value as text.\n  - `success` — renders a check-circle icon with success styling.\n  - `warning` — renders a warning icon with warning styling.\n  - `error` — renders an error icon with error styling.\n- `variant` — `primary` (default) or `secondary`; controls the badge foreground/background colors.\n- `counter` — numeric value rendered when `type=\"counter\"`. Non-numeric values render as empty.\n- `max-counter` — `9`, `99`, or `999` (default `99`). When `counter` exceeds this value, the badge renders `<maxCounter>+`. Values above the hard ceiling of 999 always render as `999+`.\n- `overlay` — when `true`, adds a thin outline so the badge reads clearly when stacked on top of another element (e.g. a button). Defaults to `false`.\n- `icon-name` — required when `type=\"icon\"`; ignored for the preset variants which use their own icons.\n- `aria-label` — accessible name for the badge; see Accessibility.",
      "Accessibility": "### Built-in features\n\nThe badge is decorative by default and is not exposed to assistive technologies unless the consumer provides an `aria-label`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute    | Value                                                                                  |\n| ------- | ------------ | -------------------------------------------------------------------------------------- |\n| Host    | `role`       | `img` when `aria-label` is set; otherwise no role is applied                           |\n| Host    | `aria-label` | Reflected from the `aria-label` attribute set by the consumer                          |\n\n### Implementation requirements\n\n#### Labelling\n\n- When the badge carries meaning (counter value, status), provide an `aria-label` describing it (e.g. `aria-label=\"5 unread\"`, `aria-label=\"Error\"`). The component then exposes itself as a labelled `img`.\n- Leave `aria-label` unset when the badge is purely decorative or its meaning is already conveyed by adjacent text — the component stays out of the accessibility tree."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "accessible name for the badge; see Accessibility.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "counter",
          "attribute": "counter",
          "type": "number",
          "values": [],
          "description": "numeric value rendered when `type=\"counter\"`. Non-numeric values render as empty.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "required when `type=\"icon\"`; ignored for the preset variants which use their own icons.",
          "source": "storybook"
        },
        {
          "name": "maxCounter",
          "attribute": "max-counter",
          "type": "select",
          "default": "DEFAULTS.MAX_COUNTER",
          "values": [],
          "description": "`9`, `99`, or `999` (default `99`). When `counter` exceeds this value, the badge renders `<maxCounter>+`. Values above the hard ceiling of 999 always render as `999+`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "overlay",
          "attribute": "overlay",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, adds a thin outline so the badge reads clearly when stacked on top of another element (e.g. a button). Defaults to `false`.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "select",
          "values": [
            "dot",
            "icon",
            "counter",
            "success",
            "warning",
            "error"
          ],
          "description": "selects the badge shape and content:",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "DEFAULTS.VARIANT",
          "values": [
            "primary",
            "secondary"
          ],
          "description": "`primary` (default) or `secondary`; controls the badge foreground/background colors.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "badge-dot",
          "description": "The dot notification badge."
        },
        {
          "name": "badge-icon",
          "description": "The icon badge."
        },
        {
          "name": "badge-overlay",
          "description": "The overlay badge."
        },
        {
          "name": "badge-text",
          "description": "The text badge."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-badge-primary-foreground-color",
          "description": "The foreground color of the primary badge."
        },
        {
          "name": "--mdc-badge-primary-background-color",
          "description": "The background color of the primary badge."
        },
        {
          "name": "--mdc-badge-secondary-foreground-color",
          "description": "The foreground color of the secondary badge."
        },
        {
          "name": "--mdc-badge-secondary-background-color",
          "description": "The background color of the secondary badge."
        },
        {
          "name": "--mdc-badge-success-foreground-color",
          "description": "The foreground color of the success badge."
        },
        {
          "name": "--mdc-badge-success-background-color",
          "description": "The background color of the success badge."
        },
        {
          "name": "--mdc-badge-warning-foreground-color",
          "description": "The foreground color of the warning badge."
        },
        {
          "name": "--mdc-badge-warning-background-color",
          "description": "The background color of the warning badge."
        },
        {
          "name": "--mdc-badge-error-foreground-color",
          "description": "The foreground color of the error badge."
        },
        {
          "name": "--mdc-badge-error-background-color",
          "description": "The background color of the error badge."
        },
        {
          "name": "--mdc-badge-overlay-background-color",
          "description": "The background color of the badge overlay."
        },
        {
          "name": "--mdc-badge-dot-width",
          "description": "The width of the dot badge. Default is 0.75rem (12px)."
        },
        {
          "name": "--mdc-badge-dot-height",
          "description": "The height of the dot badge. Default is 0.75rem (12px)."
        }
      ],
      "rules": [
        "selects the badge shape and content:",
        "`primary` (default) or `secondary`; controls the badge foreground/background colors.",
        "numeric value rendered when `type=\"counter\"`. Non-numeric values render as empty.",
        "`9`, `99`, or `999` (default `99`). When `counter` exceeds this value, the badge renders `<maxCounter>+`. Values above the hard ceiling of 999 always render as `999+`.",
        "when `true`, adds a thin outline so the badge reads clearly when stacked on top of another element (e.g. a button). Defaults to `false`.",
        "required when `type=\"icon\"`; ignored for the preset variants which use their own icons.",
        "accessible name for the badge; see Accessibility."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/badge/index.js\"; // custom element registration\n// or via React wrapper\nimport { Badge } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-badge type=\"counter\" counter=\"5\" aria-label=\"5 unread messages\"></mdc-badge>"
      }
    ],
    "reactImports": [
      "Badge"
    ],
    "constants": {
      "TYPE": {
        "DOT": "dot",
        "ICON": "icon",
        "COUNTER": "counter",
        "SUCCESS": "success",
        "WARNING": "warning",
        "ERROR": "error"
      },
      "ICON_NAMES_LIST": {
        "SUCCESS_ICON_NAME": "'check-circle-badge-filled' as Extract<IconNames, 'check-circle-badge-filled'>",
        "WARNING_ICON_NAME": "'warning-badge-filled' as Extract<IconNames, 'warning-badge-filled'>",
        "ERROR_ICON_NAME": "'error-legacy-badge-filled' as Extract<IconNames, 'error-legacy-badge-filled'>"
      },
      "ICON_VARIANT": {
        "PRIMARY": "primary",
        "SECONDARY": "secondary"
      },
      "DEFAULTS": {
        "TYPE": "dot",
        "MAX_COUNTER": 99,
        "MAX_COUNTER_LIMIT": 999,
        "VARIANT": "primary",
        "ICON_SIZE": 1,
        "OVERLAY": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "badge",
      "component",
      "counter",
      "display",
      "dot",
      "error",
      "for",
      "guidelines",
      "icon",
      "indicator",
      "interactive",
      "mdc",
      "non",
      "or",
      "overview",
      "small",
      "status",
      "success",
      "text",
      "the",
      "to",
      "usage",
      "used",
      "warning"
    ]
  },
  {
    "name": "banner",
    "title": "Banner",
    "tagName": "mdc-banner",
    "summary": "Usage, guidelines, and accessibility for the mdc-banner component — an in-page message used to communicate important information that requires user awareness or action to dismiss.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/banner",
      "knowledgeBasePath": "components/src/components/banner/knowledge-base/banner.component.md"
    },
    "docs": "## Overview\n\nThe banner is an in-page message used to communicate important information to users. It supports several message variants with appropriate icons and styling, and can be customised through label, secondary label, leading icon, and trailing action slots. Banners are designed to be noticeable yet non-intrusive — visible enough to inform without interrupting the user's workflow.\n\n### When to use\n\n- Use `mdc-banner` to surface important contextual information at the top of a page, section, or panel — for example service status, validation summaries, or persistent notices that should remain visible until the user acts on them.\n\n### When not to use\n\n- Use `mdc-toast` for short, transient feedback that should auto-dismiss and not occupy layout space.\n- Use `mdc-announcementdialog` or `mdc-dialog` when the message must block the rest of the UI until the user responds.\n- Use `mdc-alertchip` for a compact, interactive inline status indicator rather than a full-width message bar.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/banner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Banner } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-banner\n  variant=\"warning\"\n  label=\"Connection unstable\"\n  secondary-label=\"We will retry automatically in the background.\"\n>\n  <mdc-button slot=\"trailing-actions\" variant=\"tertiary\">Dismiss</mdc-button>\n</mdc-banner>\n```\n\n### Content guidance\n\n- Keep `label` short and action-oriented; use `secondary-label` for the supporting detail.\n- `secondary-label` is only rendered when `label` is also provided.\n- Provide dismiss or follow-up controls through the `trailing-actions` slot rather than inventing custom layouts.\n\n### Property/Attribute details\n\n- `variant` — selects the message tone and the auto-injected leading icon. Supported values: `custom` (default, no icon), `informational`, `warning`, `error`, `success`. When `variant=\"custom\"`, supply your own icon via the `leading-icon` slot, otherwise no icon is shown.\n- `label` — primary message text shown in the leading area.\n- `secondary-label` — supporting text rendered below `label`. Ignored when no `label` is provided.\n\nThe component also exposes slots for full layout control:\n\n- `content` — completely replaces the default layout (leading icon, text, trailing actions).\n- `leading-icon` — overrides the default variant-based icon.\n- `leading-text` — overrides the label/secondary-label text rendering.\n- `trailing-actions` — slot for action controls such as dismiss or \"Learn more\" buttons.\n\n## Accessibility\n\n### Built-in features\n\nThe banner is rendered as a generic container — it does not set any landmark or live-region role on its own.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value                                  |\n| ------- | --------- | -------------------------------------- |\n| Host    | `role`    | None set by the component (default)    |\n\n### Implementation requirements\n\n#### General\n\n- Decide on the right live-region behaviour for the surrounding context: when a banner appears dynamically and conveys important state (e.g. a warning or error), wrap or set `role=\"status\"` / `role=\"alert\"` (or `aria-live`) on the host so assistive technologies announce it.\n- Ensure interactive controls slotted into `trailing-actions` (e.g. dismiss buttons) have accessible names — the banner does not label them.\n\n#### Labelling\n\n- The visible `label` (and `secondary-label`) act as the accessible message. Make sure the wording can stand alone without surrounding visual context.\n- When using the `content` slot to fully replace the layout, retain a clear text label inside the slot so the message remains accessible.",
    "sections": {
      "Overview": "The banner is an in-page message used to communicate important information to users. It supports several message variants with appropriate icons and styling, and can be customised through label, secondary label, leading icon, and trailing action slots. Banners are designed to be noticeable yet non-intrusive — visible enough to inform without interrupting the user's workflow.\n\n### When to use\n\n- Use `mdc-banner` to surface important contextual information at the top of a page, section, or panel — for example service status, validation summaries, or persistent notices that should remain visible until the user acts on them.\n\n### When not to use\n\n- Use `mdc-toast` for short, transient feedback that should auto-dismiss and not occupy layout space.\n- Use `mdc-announcementdialog` or `mdc-dialog` when the message must block the rest of the UI until the user responds.\n- Use `mdc-alertchip` for a compact, interactive inline status indicator rather than a full-width message bar.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/banner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Banner } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-banner\n  variant=\"warning\"\n  label=\"Connection unstable\"\n  secondary-label=\"We will retry automatically in the background.\"\n>\n  <mdc-button slot=\"trailing-actions\" variant=\"tertiary\">Dismiss</mdc-button>\n</mdc-banner>\n```\n\n### Content guidance\n\n- Keep `label` short and action-oriented; use `secondary-label` for the supporting detail.\n- `secondary-label` is only rendered when `label` is also provided.\n- Provide dismiss or follow-up controls through the `trailing-actions` slot rather than inventing custom layouts.\n\n### Property/Attribute details\n\n- `variant` — selects the message tone and the auto-injected leading icon. Supported values: `custom` (default, no icon), `informational`, `warning`, `error`, `success`. When `variant=\"custom\"`, supply your own icon via the `leading-icon` slot, otherwise no icon is shown.\n- `label` — primary message text shown in the leading area.\n- `secondary-label` — supporting text rendered below `label`. Ignored when no `label` is provided.\n\nThe component also exposes slots for full layout control:\n\n- `content` — completely replaces the default layout (leading icon, text, trailing actions).\n- `leading-icon` — overrides the default variant-based icon.\n- `leading-text` — overrides the label/secondary-label text rendering.\n- `trailing-actions` — slot for action controls such as dismiss or \"Learn more\" buttons.",
      "Accessibility": "### Built-in features\n\nThe banner is rendered as a generic container — it does not set any landmark or live-region role on its own.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value                                  |\n| ------- | --------- | -------------------------------------- |\n| Host    | `role`    | None set by the component (default)    |\n\n### Implementation requirements\n\n#### General\n\n- Decide on the right live-region behaviour for the surrounding context: when a banner appears dynamically and conveys important state (e.g. a warning or error), wrap or set `role=\"status\"` / `role=\"alert\"` (or `aria-live`) on the host so assistive technologies announce it.\n- Ensure interactive controls slotted into `trailing-actions` (e.g. dismiss buttons) have accessible names — the banner does not label them.\n\n#### Labelling\n\n- The visible `label` (and `secondary-label`) act as the accessible message. Make sure the wording can stand alone without surrounding visual context.\n- When using the `content` slot to fully replace the layout, retain a clear text label inside the slot so the message remains accessible."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "primary message text shown in the leading area.",
          "source": "storybook"
        },
        {
          "name": "secondaryLabel",
          "attribute": "secondary-label",
          "type": "text",
          "values": [],
          "description": "supporting text rendered below `label`. Ignored when no `label` is provided.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'custom'",
          "values": [
            "custom",
            "informational",
            "warning",
            "error",
            "success"
          ],
          "description": "selects the message tone and the auto-injected leading icon. Supported values: `custom` (default, no icon), `informational`, `warning`, `error`, `success`. When `variant=\"custom\"`, supply your own icon via the `leading-icon` slot, otherwise no icon is shown.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "content",
          "description": "Complete content override. When used, it replaces all default banner content including icon, label, secondary label, and actions."
        },
        {
          "name": "leading-icon",
          "description": "Custom icon content. Overrides the default variant-based icon."
        },
        {
          "name": "leading-text",
          "description": "Custom text content. Overrides the label and secondaryLabel properties."
        },
        {
          "name": "trailing-actions",
          "description": "Custom action buttons and controls. Use this for dismiss buttons, reset buttons, or any other actions."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "leading",
          "description": "The leading section containing the icon and text."
        },
        {
          "name": "leading-icon",
          "description": "The icon displayed for variants or custom icon slot."
        },
        {
          "name": "leading-text",
          "description": "The leading section containing label and secondary label text."
        },
        {
          "name": "trailing",
          "description": "The trailing section containing action buttons and controls."
        },
        {
          "name": "leading-label",
          "description": "The label text of the banner."
        },
        {
          "name": "leading-secondary-label",
          "description": "The secondary label text of the banner."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-banner-background-color",
          "description": "Background color of the banner."
        },
        {
          "name": "--mdc-banner-border-color",
          "description": "Border color of the banner."
        },
        {
          "name": "--mdc-banner-border-width",
          "description": "Border width of the banner."
        },
        {
          "name": "--mdc-banner-icon-color",
          "description": "Color of the icon in the banner."
        },
        {
          "name": "--mdc-banner-elevation",
          "description": "Elevation/shadow of the banner."
        },
        {
          "name": "--mdc-banner-padding",
          "description": "Padding inside the banner."
        },
        {
          "name": "--mdc-banner-gap",
          "description": "Gap between banner elements."
        }
      ],
      "rules": [
        "selects the message tone and the auto-injected leading icon. Supported values: `custom` (default, no icon), `informational`, `warning`, `error`, `success`. When `variant=\"custom\"`, supply your own icon via the `leading-icon` slot, otherwise no icon is shown.",
        "primary message text shown in the leading area.",
        "supporting text rendered below `label`. Ignored when no `label` is provided.",
        "completely replaces the default layout (leading icon, text, trailing actions).",
        "overrides the default variant-based icon.",
        "overrides the label/secondary-label text rendering.",
        "slot for action controls such as dismiss or \"Learn more\" buttons."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/banner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Banner } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-banner\n  variant=\"warning\"\n  label=\"Connection unstable\"\n  secondary-label=\"We will retry automatically in the background.\"\n>\n  <mdc-button slot=\"trailing-actions\" variant=\"tertiary\">Dismiss</mdc-button>\n</mdc-banner>"
      }
    ],
    "reactImports": [
      "Banner"
    ],
    "constants": {
      "BANNER_VARIANT": {
        "CUSTOM": "custom",
        "INFORMATIONAL": "informational",
        "WARNING": "warning",
        "ERROR": "error",
        "SUCCESS": "success"
      },
      "VARIANT_ICON_NAMES": {
        "INFORMATIONAL_ICON_NAME": "'info-circle-bold' as Extract<IconNames, 'info-circle-bold'>",
        "SUCCESS_ICON_NAME": "'check-circle-bold' as Extract<IconNames, 'check-circle-bold'>",
        "WARNING_ICON_NAME": "'warning-bold' as Extract<IconNames, 'warning-bold'>",
        "ERROR_ICON_NAME": "'error-legacy-bold' as Extract<IconNames, 'error-legacy-bold'>"
      },
      "DEFAULTS": {
        "VARIANT": "custom",
        "PREFIX_ICON_SIZE": 1.25
      }
    },
    "keywords": [
      "accessibility",
      "action",
      "an",
      "and",
      "awareness",
      "banner",
      "communicate",
      "component",
      "dismiss",
      "for",
      "guidelines",
      "icon",
      "important",
      "in",
      "information",
      "mdc",
      "message",
      "or",
      "overview",
      "page",
      "requires",
      "text",
      "that",
      "the",
      "to",
      "usage",
      "used",
      "user"
    ]
  },
  {
    "name": "brandvisual",
    "title": "Brandvisual",
    "tagName": "mdc-brandvisual",
    "summary": "Usage, guidelines, and accessibility for the mdc-brandvisual component — a dynamic loader that renders branded logos and wordmarks from the Momentum brand-visuals package.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/brandvisual",
      "knowledgeBasePath": "components/src/components/brandvisual/knowledge-base/brandvisual.component.md"
    },
    "docs": "## Overview\n\nThe brandvisual renders brand artwork — logos and wordmarks — from the Momentum `brand-visuals` package. Assets are loaded dynamically by name, so the consumer only references the visual they need and the component handles fetching and inserting the SVG or image at runtime. Successful loads emit a `load` event; failed lookups emit an `error` event so the host application can react.\n\n### When to use\n\n- Use `mdc-brandvisual` to render product, company, or partner logos that ship with the Momentum brand library.\n- Use it when the logo asset is published in `@momentum-design/brand-visuals` and you want the design system to keep the artwork in sync rather than hard-coding inline SVG.\n\n### When not to use\n\n- Use `mdc-icon` for UI iconography (controls, status, decorative glyphs) rather than brand artwork.\n- Use `mdc-illustration` for larger illustrative imagery that is not a brand mark.\n- Use a plain `<img>` tag when the asset lives outside the Momentum brand library and you do not need the dynamic-loading or event semantics.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/brandvisual/index.js\"; // custom element registration\n// or via React wrapper\nimport { Brandvisual } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-brandvisual name=\"webex-logo\" alt-text=\"Webex\"></mdc-brandvisual>\n```\n\n### Property/Attribute details\n\n- `name` — the filename of the brand visual to load from `@momentum-design/brand-visuals`. Changing the value triggers a new dynamic import. On success the component dispatches a `load` event; on failure it dispatches an `error` event whose `event.detail.error` carries the original error (for example, an unknown `name` or a missing asset).\n- `alt-text` — accessible alternative text. Applied to the underlying `<img>` element when the loaded asset is an image (e.g. PNG). For inline SVG artwork, provide the accessible name via the surrounding context (see Accessibility).\n\n## Accessibility\n\n### Built-in features\n\nThe brandvisual renders the fetched artwork as-is and does not set any ARIA role on the host element. When the loaded asset is an image (PNG), the component forwards `alt-text` onto the `<img>`'s `alt` attribute, giving screen readers an accessible name for that image.\n\n#### Internal ARIA managed by the component\n\n| Element                   | Attribute | Value                                                                |\n| ------------------------- | --------- | -------------------------------------------------------------------- |\n| Host                      | `role`    | None set by the component                                            |\n| Loaded `<img>` (PNG only) | `alt`     | Mirrors the `alt-text` attribute set by the consumer                 |\n\n### Implementation requirements\n\n#### Labelling\n\n- For image (PNG) assets, always set `alt-text` to a short description of what the logo represents (e.g. `alt-text=\"Webex\"`). Leave `alt-text` empty only when the same brand name is already conveyed by adjacent visible text.\n- For SVG assets (which do not receive the `alt` attribute), provide the accessible name through the surrounding context — for example, wrap the brandvisual in an element with `aria-label`, or place visually hidden text alongside it.\n- When the brand visual is purely decorative and a non-decorative label exists nearby, mark the host with `aria-hidden=\"true\"` so assistive technologies do not announce it twice.",
    "sections": {
      "Overview": "The brandvisual renders brand artwork — logos and wordmarks — from the Momentum `brand-visuals` package. Assets are loaded dynamically by name, so the consumer only references the visual they need and the component handles fetching and inserting the SVG or image at runtime. Successful loads emit a `load` event; failed lookups emit an `error` event so the host application can react.\n\n### When to use\n\n- Use `mdc-brandvisual` to render product, company, or partner logos that ship with the Momentum brand library.\n- Use it when the logo asset is published in `@momentum-design/brand-visuals` and you want the design system to keep the artwork in sync rather than hard-coding inline SVG.\n\n### When not to use\n\n- Use `mdc-icon` for UI iconography (controls, status, decorative glyphs) rather than brand artwork.\n- Use `mdc-illustration` for larger illustrative imagery that is not a brand mark.\n- Use a plain `<img>` tag when the asset lives outside the Momentum brand library and you do not need the dynamic-loading or event semantics.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/brandvisual/index.js\"; // custom element registration\n// or via React wrapper\nimport { Brandvisual } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-brandvisual name=\"webex-logo\" alt-text=\"Webex\"></mdc-brandvisual>\n```\n\n### Property/Attribute details\n\n- `name` — the filename of the brand visual to load from `@momentum-design/brand-visuals`. Changing the value triggers a new dynamic import. On success the component dispatches a `load` event; on failure it dispatches an `error` event whose `event.detail.error` carries the original error (for example, an unknown `name` or a missing asset).\n- `alt-text` — accessible alternative text. Applied to the underlying `<img>` element when the loaded asset is an image (e.g. PNG). For inline SVG artwork, provide the accessible name via the surrounding context (see Accessibility).",
      "Accessibility": "### Built-in features\n\nThe brandvisual renders the fetched artwork as-is and does not set any ARIA role on the host element. When the loaded asset is an image (PNG), the component forwards `alt-text` onto the `<img>`'s `alt` attribute, giving screen readers an accessible name for that image.\n\n#### Internal ARIA managed by the component\n\n| Element                   | Attribute | Value                                                                |\n| ------------------------- | --------- | -------------------------------------------------------------------- |\n| Host                      | `role`    | None set by the component                                            |\n| Loaded `<img>` (PNG only) | `alt`     | Mirrors the `alt-text` attribute set by the consumer                 |\n\n### Implementation requirements\n\n#### Labelling\n\n- For image (PNG) assets, always set `alt-text` to a short description of what the logo represents (e.g. `alt-text=\"Webex\"`). Leave `alt-text` empty only when the same brand name is already conveyed by adjacent visible text.\n- For SVG assets (which do not receive the `alt` attribute), provide the accessible name through the surrounding context — for example, wrap the brandvisual in an element with `aria-label`, or place visually hidden text alongside it.\n- When the brand visual is purely decorative and a non-decorative label exists nearby, mark the host with `aria-hidden=\"true\"` so assistive technologies do not announce it twice."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "altText",
          "attribute": "alt-text",
          "type": "text",
          "values": [],
          "description": "accessible alternative text. Applied to the underlying `<img>` element when the loaded asset is an image (e.g. PNG). For inline SVG artwork, provide the accessible name via the surrounding context (see Accessibility).",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "select",
          "values": [],
          "description": "the filename of the brand visual to load from `@momentum-design/brand-visuals`. Changing the value triggers a new dynamic import. On success the component dispatches a `load` event; on failure it dispatches an `error` event whose `event.detail.error` carries the original error (for example, an unknown `name` or a missing asset).",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "load",
          "description": "(React: onLoad) This event is dispatched when the brandvisual has been successfully loaded."
        },
        {
          "name": "error",
          "description": "(React: onError) This event is dispatched when the brandvisual fetching has failed."
        }
      ],
      "cssParts": [
        {
          "name": "brandvisual",
          "description": "Set styles for the brandvisual svg container"
        },
        {
          "name": "brandvisualImage",
          "description": "Set styles for the brandvisual image container"
        }
      ],
      "cssProperties": [],
      "rules": [
        "the filename of the brand visual to load from `@momentum-design/brand-visuals`. Changing the value triggers a new dynamic import. On success the component dispatches a `load` event; on failure it dispatches an `error` event whose `event.detail.error` carries the original error (for example, an unknown `name` or a missing asset).",
        "accessible alternative text. Applied to the underlying `<img>` element when the loaded asset is an image (e.g. PNG). For inline SVG artwork, provide the accessible name via the surrounding context (see Accessibility)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/brandvisual/index.js\"; // custom element registration\n// or via React wrapper\nimport { Brandvisual } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-brandvisual name=\"webex-logo\" alt-text=\"Webex\"></mdc-brandvisual>"
      }
    ],
    "reactImports": [
      "Brandvisual"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "brand",
      "branded",
      "brandvisual",
      "component",
      "dynamic",
      "for",
      "from",
      "guidelines",
      "loader",
      "logos",
      "mdc",
      "momentum",
      "overview",
      "package",
      "renders",
      "that",
      "the",
      "usage",
      "visuals",
      "wordmarks"
    ]
  },
  {
    "name": "bullet",
    "title": "Bullet",
    "tagName": "mdc-bullet",
    "summary": "Usage, guidelines, and accessibility for the mdc-bullet component — a small circular visual indicator used to mark or separate list-style content.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/bullet",
      "knowledgeBasePath": "components/src/components/bullet/knowledge-base/bullet.component.md"
    },
    "docs": "## Overview\n\nThe bullet is a small, non-interactive circular indicator used to mark or separate items in a list-style layout. It provides a consistent visual mark in three sizes (small, medium, large) so consumers can match the bullet to the surrounding content density.\n\n### When to use\n\n- Use `mdc-bullet` as a visual marker in custom unordered lists, navigation indicators, or as a separator dot between inline text fragments.\n- Pick the size based on layout density and visual emphasis: `small` for compact rows, `medium` for typical body content, `large` when the marker should stand out.\n\n### When not to use\n\n- Use a native `<ul>`/`<li>` list when the platform's default bullet styling is sufficient — it carries list semantics for free.\n- Use `mdc-badge` (`dot` type) when the indicator should communicate a notification or status rather than mark a list item.\n- Use `mdc-presence` when the dot conveys a user's availability state (active, away, busy, dnd).\n- Use `mdc-divider` when you need a visible separator between sections rather than a marker for an individual item.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/bullet/index.js\"; // custom element registration\n// or via React wrapper\nimport { Bullet } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-bullet size=\"medium\"></mdc-bullet>\n```\n\n### Property/Attribute details\n\n- `size` — controls the diameter of the bullet:\n  - `small` (default) — 0.25rem (4px), for compact lists.\n  - `medium` — 0.5rem (8px), for typical body content.\n  - `large` — 1rem (16px), for emphasised or hero layouts.\n\n## Accessibility\n\n### Built-in features\n\nThe bullet is purely decorative. It renders as a generic element with no role, label, or interactive behaviour, so assistive technologies skip over it by default.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value                       |\n| ------- | --------- | --------------------------- |\n| Host    | `role`    | None set by the component   |\n\n### Implementation requirements\n\n#### General\n\n- Always pair the bullet with meaningful adjacent text — never let the bullet itself carry information.\n- Do not rely on bullet size or colour alone to convey state or hierarchy; use accompanying text or an appropriate semantic component (e.g. `mdc-badge`, `mdc-presence`) when the dot must communicate meaning.\n- If the bullet is rendered inside a list of items, prefer marking up the surrounding container with the appropriate list semantics (`<ul>`/`<li>` or `role=\"list\"`/`role=\"listitem\"`) so assistive technologies announce the list structure.",
    "sections": {
      "Overview": "The bullet is a small, non-interactive circular indicator used to mark or separate items in a list-style layout. It provides a consistent visual mark in three sizes (small, medium, large) so consumers can match the bullet to the surrounding content density.\n\n### When to use\n\n- Use `mdc-bullet` as a visual marker in custom unordered lists, navigation indicators, or as a separator dot between inline text fragments.\n- Pick the size based on layout density and visual emphasis: `small` for compact rows, `medium` for typical body content, `large` when the marker should stand out.\n\n### When not to use\n\n- Use a native `<ul>`/`<li>` list when the platform's default bullet styling is sufficient — it carries list semantics for free.\n- Use `mdc-badge` (`dot` type) when the indicator should communicate a notification or status rather than mark a list item.\n- Use `mdc-presence` when the dot conveys a user's availability state (active, away, busy, dnd).\n- Use `mdc-divider` when you need a visible separator between sections rather than a marker for an individual item.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/bullet/index.js\"; // custom element registration\n// or via React wrapper\nimport { Bullet } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-bullet size=\"medium\"></mdc-bullet>\n```\n\n### Property/Attribute details\n\n- `size` — controls the diameter of the bullet:\n  - `small` (default) — 0.25rem (4px), for compact lists.\n  - `medium` — 0.5rem (8px), for typical body content.\n  - `large` — 1rem (16px), for emphasised or hero layouts.",
      "Accessibility": "### Built-in features\n\nThe bullet is purely decorative. It renders as a generic element with no role, label, or interactive behaviour, so assistive technologies skip over it by default.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value                       |\n| ------- | --------- | --------------------------- |\n| Host    | `role`    | None set by the component   |\n\n### Implementation requirements\n\n#### General\n\n- Always pair the bullet with meaningful adjacent text — never let the bullet itself carry information.\n- Do not rely on bullet size or colour alone to convey state or hierarchy; use accompanying text or an appropriate semantic component (e.g. `mdc-badge`, `mdc-presence`) when the dot must communicate meaning.\n- If the bullet is rendered inside a list of items, prefer marking up the surrounding container with the appropriate list semantics (`<ul>`/`<li>` or `role=\"list\"`/`role=\"listitem\"`) so assistive technologies announce the list structure."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "small",
          "values": [
            "small",
            "medium",
            "large"
          ],
          "description": "controls the diameter of the bullet:",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-bullet-background-color",
          "description": "Background color of the bullet."
        },
        {
          "name": "--mdc-bullet-size",
          "description": "Height of the bullet."
        }
      ],
      "rules": [
        "controls the diameter of the bullet:"
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/bullet/index.js\"; // custom element registration\n// or via React wrapper\nimport { Bullet } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-bullet size=\"medium\"></mdc-bullet>"
      }
    ],
    "reactImports": [
      "Bullet"
    ],
    "constants": {
      "SIZE": {
        "SMALL": "small",
        "MEDIUM": "medium",
        "LARGE": "large"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "bullet",
      "circular",
      "component",
      "content",
      "for",
      "guidelines",
      "indicator",
      "list",
      "mark",
      "mdc",
      "or",
      "overview",
      "separate",
      "small",
      "style",
      "the",
      "to",
      "usage",
      "used",
      "visual"
    ]
  },
  {
    "name": "button",
    "title": "Button",
    "tagName": "mdc-button",
    "summary": "Usage, guidelines, and accessibility for the mdc-button component — a configurable button that supports pill, pill-with-icon, and icon-only layouts in multiple variants, colors, and sizes.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/button",
      "knowledgeBasePath": "components/src/components/button/knowledge-base/button.component.md"
    },
    "docs": "## Overview\n\nThe button triggers user actions such as submitting a form, opening a dialog, or running a command. It supports three layouts — pill (text only), pill with leading/trailing icons, and icon-only — and offers variant, color, and size options so it can scale from a primary call-to-action down to a compact toolbar control.\n\n### When to use\n\n- Use `mdc-button` for the primary, secondary, or tertiary actions inside forms, dialogs, toolbars, and content surfaces.\n- Use the icon-only layout for compact action targets such as toolbars or table-row controls where space is constrained and the icon's meaning is unambiguous.\n- Use the `inverted` flag when placing a primary default button on a dark or inverted surface so the color scheme keeps the required contrast.\n\n### When not to use\n\n- Use `mdc-link` when the control navigates to a different URL or in-app route — links should look and behave like links, not buttons.\n- Use `mdc-linkbutton` when you need the visual styling of a button but the underlying action is a navigation (renders an `<a>` instead of a `<button>`, so it carries link semantics and browser affordances like open-in-new-tab).\n- Use `mdc-toggle` or `mdc-checkbox` for binary on/off state inside forms — they expose the right form-control semantics rather than mimicking state with a toggled button.\n- Use `mdc-tab` inside `mdc-tablist` for tabbed navigation rather than a styled button.\n- Use `mdc-menuitem` (or a related menu item) when the control lives inside a menu surface.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/button/index.js\"; // custom element registration\n// or via React wrapper\nimport { Button } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button variant=\"primary\" color=\"default\" size=\"32\">Save changes</mdc-button>\n\n<mdc-button variant=\"secondary\" prefix-icon=\"download-bold\" size=\"32\">Download</mdc-button>\n\n<mdc-button variant=\"tertiary\" prefix-icon=\"more-bold\" size=\"32\" aria-label=\"More options\"></mdc-button>\n```\n\n### Content guidance\n\n- Keep labels short and action-oriented; prefer a verb or verb phrase (\"Save changes\", \"Send invite\") over generic words like \"OK\" or \"Submit\".\n- Use sentence case for button labels.\n- For icon-only buttons, always set `aria-label` to describe the action (e.g. `aria-label=\"Delete\"`); the icon alone is not announced.\n\n### Property/Attribute details\n\n- `variant` — visual style. `primary` (default, solid background), `secondary` (transparent background with border), `tertiary` (text-only). Tertiary supports a reduced color palette (see `color`).\n- `color` — semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttons only support `default`, `accent`, and `negative`; other values fall back to `default`.\n- `size` — pixel-derived sizing. The valid set depends on the inferred button type:\n  - Pill / pill-with-icon: `40`, `32` (default), `28`, `24`.\n  - Icon: `64`, `52`, `40`, `32` (default), `28`, `24`. The `20` size is only valid when `variant=\"tertiary\"`.\n  - Invalid combinations are coerced back to the default (`32`).\n- `prefix-icon` / `postfix-icon` — icon name rendered before/after the label. Supplying an icon without a default slot label produces an icon-only button. When the button is `active`, prefix/postfix icons automatically switch to their `-filled` variant if one exists.\n- `inverted` — flips the color scheme for use on dark backgrounds. Only valid with `variant=\"primary\"`, `color=\"default\"`, and when the button is not `active`.\n- `active` — toggles the button between pressed/unpressed. While set, the component reflects the state via `aria-pressed` (or whichever ARIA attribute(s) are listed in `aria-state-key`) and swaps prefix/postfix icons to their filled variants.\n- `aria-state-key` — ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; pass a comma-separated list (e.g. `aria-pressed,aria-expanded`) to toggle several at once.\n- `disabled` — fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`), and click/keyboard handlers are blocked.\n- `soft-disabled` — visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the button from the tab order; consumers must suppress side-effects themselves.\n- `type` — native button behaviour: `button` (default, no implicit action), `submit` (calls `form.requestSubmit()`), `reset` (calls `form.reset()`).\n- `name` / `value` — submitted with the form data when the button submits the surrounding form.\n- `role` — defaults to `button`. Override only when a different role is genuinely required for the surrounding context.\n\n### Limitations\n\n- The button does not own its accessible name when used as icon-only — the consumer must supply `aria-label`.\n- `inverted` is ignored unless the variant/color/active combination above is met.\n- `size=\"20\"` is only honoured for tertiary icon buttons; other variants fall back to the default size.\n\n## Accessibility\n\n### Built-in features\n\nThe button is rendered with `role=\"button\"` and is part of the page's tab order by default. It dispatches `click` on `Enter` keyup-equivalent behaviour and on `Space` (press on keydown, fire on keyup), matching native `<button>` keyboard semantics. While focused, pressing and releasing the key updates an internal `pressed` class so the styling tracks the interaction; losing focus mid-press clears it.\n\nWhen `active` is set, the component writes the boolean state into the configured ARIA state attribute (default `aria-pressed`) so screen readers announce the toggled state. Setting `disabled` removes the button from the tab order and marks it `aria-disabled=\"true\"`; `soft-disabled` keeps the button focusable while still announcing it as disabled, so the consumer can convey *why* the action is unavailable.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute                  | Value                                                                                                                       |\n| ------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------- |\n| Host    | `role`                     | `button` by default; reflects the `role` property when overridden                                                           |\n| Host    | `tabindex`                 | `0` when enabled; `-1` while `disabled`                                                                                     |\n| Host    | `aria-disabled`            | `true` while `disabled` or `soft-disabled`; removed otherwise                                                               |\n| Host    | `aria-pressed` (default)   | `true`/`false` mirroring the `active` property when `aria-state-key` is left at its default                                 |\n| Host    | configured `aria-state-key` attribute(s) | `true`/`false` mirroring `active` when `aria-state-key` is set (e.g. `aria-expanded`); removed when `active` is `undefined` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Text buttons get their accessible name from the slotted label — keep the label concise and self-describing.\n- Icon-only buttons (no default slot content, only `prefix-icon` or `postfix-icon`) MUST have an `aria-label` describing the action; without it, the button is announced without context.\n- When using `aria-state-key` with a non-default ARIA attribute (e.g. `aria-expanded`), make sure the visual treatment for `active` matches the semantic meaning of that attribute — `aria-pressed` is for toggle buttons, `aria-expanded` is for disclosure controls.\n- If a button triggers form submission or reset, prefer `type=\"submit\"` / `type=\"reset\"` over wiring custom click handlers so the native form behaviour is preserved.",
    "sections": {
      "Overview": "The button triggers user actions such as submitting a form, opening a dialog, or running a command. It supports three layouts — pill (text only), pill with leading/trailing icons, and icon-only — and offers variant, color, and size options so it can scale from a primary call-to-action down to a compact toolbar control.\n\n### When to use\n\n- Use `mdc-button` for the primary, secondary, or tertiary actions inside forms, dialogs, toolbars, and content surfaces.\n- Use the icon-only layout for compact action targets such as toolbars or table-row controls where space is constrained and the icon's meaning is unambiguous.\n- Use the `inverted` flag when placing a primary default button on a dark or inverted surface so the color scheme keeps the required contrast.\n\n### When not to use\n\n- Use `mdc-link` when the control navigates to a different URL or in-app route — links should look and behave like links, not buttons.\n- Use `mdc-linkbutton` when you need the visual styling of a button but the underlying action is a navigation (renders an `<a>` instead of a `<button>`, so it carries link semantics and browser affordances like open-in-new-tab).\n- Use `mdc-toggle` or `mdc-checkbox` for binary on/off state inside forms — they expose the right form-control semantics rather than mimicking state with a toggled button.\n- Use `mdc-tab` inside `mdc-tablist` for tabbed navigation rather than a styled button.\n- Use `mdc-menuitem` (or a related menu item) when the control lives inside a menu surface.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/button/index.js\"; // custom element registration\n// or via React wrapper\nimport { Button } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button variant=\"primary\" color=\"default\" size=\"32\">Save changes</mdc-button>\n\n<mdc-button variant=\"secondary\" prefix-icon=\"download-bold\" size=\"32\">Download</mdc-button>\n\n<mdc-button variant=\"tertiary\" prefix-icon=\"more-bold\" size=\"32\" aria-label=\"More options\"></mdc-button>\n```\n\n### Content guidance\n\n- Keep labels short and action-oriented; prefer a verb or verb phrase (\"Save changes\", \"Send invite\") over generic words like \"OK\" or \"Submit\".\n- Use sentence case for button labels.\n- For icon-only buttons, always set `aria-label` to describe the action (e.g. `aria-label=\"Delete\"`); the icon alone is not announced.\n\n### Property/Attribute details\n\n- `variant` — visual style. `primary` (default, solid background), `secondary` (transparent background with border), `tertiary` (text-only). Tertiary supports a reduced color palette (see `color`).\n- `color` — semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttons only support `default`, `accent`, and `negative`; other values fall back to `default`.\n- `size` — pixel-derived sizing. The valid set depends on the inferred button type:\n  - Pill / pill-with-icon: `40`, `32` (default), `28`, `24`.\n  - Icon: `64`, `52`, `40`, `32` (default), `28`, `24`. The `20` size is only valid when `variant=\"tertiary\"`.\n  - Invalid combinations are coerced back to the default (`32`).\n- `prefix-icon` / `postfix-icon` — icon name rendered before/after the label. Supplying an icon without a default slot label produces an icon-only button. When the button is `active`, prefix/postfix icons automatically switch to their `-filled` variant if one exists.\n- `inverted` — flips the color scheme for use on dark backgrounds. Only valid with `variant=\"primary\"`, `color=\"default\"`, and when the button is not `active`.\n- `active` — toggles the button between pressed/unpressed. While set, the component reflects the state via `aria-pressed` (or whichever ARIA attribute(s) are listed in `aria-state-key`) and swaps prefix/postfix icons to their filled variants.\n- `aria-state-key` — ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; pass a comma-separated list (e.g. `aria-pressed,aria-expanded`) to toggle several at once.\n- `disabled` — fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`), and click/keyboard handlers are blocked.\n- `soft-disabled` — visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the button from the tab order; consumers must suppress side-effects themselves.\n- `type` — native button behaviour: `button` (default, no implicit action), `submit` (calls `form.requestSubmit()`), `reset` (calls `form.reset()`).\n- `name` / `value` — submitted with the form data when the button submits the surrounding form.\n- `role` — defaults to `button`. Override only when a different role is genuinely required for the surrounding context.\n\n### Limitations\n\n- The button does not own its accessible name when used as icon-only — the consumer must supply `aria-label`.\n- `inverted` is ignored unless the variant/color/active combination above is met.\n- `size=\"20\"` is only honoured for tertiary icon buttons; other variants fall back to the default size.",
      "Accessibility": "### Built-in features\n\nThe button is rendered with `role=\"button\"` and is part of the page's tab order by default. It dispatches `click` on `Enter` keyup-equivalent behaviour and on `Space` (press on keydown, fire on keyup), matching native `<button>` keyboard semantics. While focused, pressing and releasing the key updates an internal `pressed` class so the styling tracks the interaction; losing focus mid-press clears it.\n\nWhen `active` is set, the component writes the boolean state into the configured ARIA state attribute (default `aria-pressed`) so screen readers announce the toggled state. Setting `disabled` removes the button from the tab order and marks it `aria-disabled=\"true\"`; `soft-disabled` keeps the button focusable while still announcing it as disabled, so the consumer can convey *why* the action is unavailable.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute                  | Value                                                                                                                       |\n| ------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------- |\n| Host    | `role`                     | `button` by default; reflects the `role` property when overridden                                                           |\n| Host    | `tabindex`                 | `0` when enabled; `-1` while `disabled`                                                                                     |\n| Host    | `aria-disabled`            | `true` while `disabled` or `soft-disabled`; removed otherwise                                                               |\n| Host    | `aria-pressed` (default)   | `true`/`false` mirroring the `active` property when `aria-state-key` is left at its default                                 |\n| Host    | configured `aria-state-key` attribute(s) | `true`/`false` mirroring `active` when `aria-state-key` is set (e.g. `aria-expanded`); removed when `active` is `undefined` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Text buttons get their accessible name from the slotted label — keep the label concise and self-describing.\n- Icon-only buttons (no default slot content, only `prefix-icon` or `postfix-icon`) MUST have an `aria-label` describing the action; without it, the button is announced without context.\n- When using `aria-state-key` with a non-default ARIA attribute (e.g. `aria-expanded`), make sure the visual treatment for `active` matches the semantic meaning of that attribute — `aria-pressed` is for toggle buttons, `aria-expanded` is for disclosure controls.\n- If a button triggers form submission or reset, prefer `type=\"submit\"` / `type=\"reset\"` over wiring custom click handlers so the native form behaviour is preserved."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "active",
          "attribute": "active",
          "type": "boolean",
          "values": [],
          "description": "toggles the button between pressed/unpressed. While set, the component reflects the state via `aria-pressed` (or whichever ARIA attribute(s) are listed in `aria-state-key`) and swaps prefix/postfix icons to their filled variants.",
          "source": "storybook"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "description": "Defines an accessible label for screen readers. Required when using icon-only buttons.",
          "source": "storybook"
        },
        {
          "name": "ariaStateKey",
          "attribute": "aria-state-key",
          "type": "text",
          "values": [],
          "description": "ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; pass a comma-separated list (e.g. `aria-pressed,aria-expanded`) to toggle several at once.",
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Text label for the button.",
          "source": "storybook"
        },
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "values": [
            "positive",
            "negative",
            "accent",
            "promotional",
            "default"
          ],
          "description": "semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttons only support `default`, `accent`, and `negative`; other values fall back to `default`.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`), and click/keyboard handlers are blocked.",
          "source": "storybook"
        },
        {
          "name": "inverted",
          "attribute": "inverted",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "flips the color scheme for use on dark backgrounds. Only valid with `variant=\"primary\"`, `color=\"default\"`, and when the button is not `active`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "submitted with the form data when the button submits the surrounding form.",
          "source": "storybook"
        },
        {
          "name": "postfixIcon",
          "attribute": "postfix-icon",
          "type": "select",
          "values": [],
          "description": "icon name rendered before/after the label. Supplying an icon without a default slot label produces an icon-only button. When the button is `active`, prefix/postfix icons automatically switch to their `-filled` variant if one exists.",
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "select",
          "values": [],
          "description": "icon name rendered before/after the label. Supplying an icon without a default slot label produces an icon-only button. When the button is `active`, prefix/postfix icons automatically switch to their `-filled` variant if one exists.",
          "source": "storybook"
        },
        {
          "name": "role",
          "attribute": "role",
          "type": "RoleType",
          "default": "button",
          "values": [],
          "description": "defaults to `button`. Override only when a different role is genuinely required for the surrounding context.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "32",
          "values": [
            24,
            28,
            32,
            40,
            20,
            52,
            64
          ],
          "description": "pixel-derived sizing. The valid set depends on the inferred button type:",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the button from the tab order; consumers must suppress side-effects themselves.",
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "select",
          "values": [],
          "description": "native button behaviour: `button` (default, no implicit action), `submit` (calls `form.requestSubmit()`), `reset` (calls `form.reset()`).",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "submitted with the form data when the button submits the surrounding form.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [
            "primary",
            "secondary",
            "tertiary"
          ],
          "description": "visual style. `primary` (default, solid background), `secondary` (transparent background with border), `tertiary` (text-only). Tertiary supports a reduced color palette (see `color`).",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Text label of the button."
        },
        {
          "name": "prefix",
          "description": "Content to be displayed before the text label."
        },
        {
          "name": "postfix",
          "description": "Content to be displayed after the text label."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "button-text",
          "description": "Text label of the button, passed in default slot"
        },
        {
          "name": "prefix",
          "description": "Content before the text label, passed in `prefix` slot"
        },
        {
          "name": "postfix",
          "description": "Content after the text label, passed in `postfix` slot"
        },
        {
          "name": "prefix-icon",
          "description": "Icon element displayed before the text label when `prefix-icon` attribute is set"
        },
        {
          "name": "postfix-icon",
          "description": "Icon element displayed after the text label when `postfix-icon` attribute is set"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-button-height",
          "description": "Height for button size"
        },
        {
          "name": "--mdc-button-background",
          "description": "Background of the button"
        },
        {
          "name": "--mdc-button-border-color",
          "description": "Border color of the button"
        },
        {
          "name": "--mdc-button-text-color",
          "description": "Text color of the button"
        },
        {
          "name": "--mdc-button-prefix-icon-size",
          "description": "Size of the prefix icon"
        },
        {
          "name": "--mdc-button-postfix-icon-size",
          "description": "Size of the postfix icon"
        },
        {
          "name": "--mdc-button-line-height",
          "description": "Line height of the button text"
        }
      ],
      "rules": [
        "visual style. `primary` (default, solid background), `secondary` (transparent background with border), `tertiary` (text-only). Tertiary supports a reduced color palette (see `color`).",
        "semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttons only support `default`, `accent`, and `negative`; other values fall back to `default`.",
        "pixel-derived sizing. The valid set depends on the inferred button type:",
        "icon name rendered before/after the label. Supplying an icon without a default slot label produces an icon-only button. When the button is `active`, prefix/postfix icons automatically switch to their `-filled` variant if one exists.",
        "icon name rendered before/after the label. Supplying an icon without a default slot label produces an icon-only button. When the button is `active`, prefix/postfix icons automatically switch to their `-filled` variant if one exists.",
        "flips the color scheme for use on dark backgrounds. Only valid with `variant=\"primary\"`, `color=\"default\"`, and when the button is not `active`.",
        "toggles the button between pressed/unpressed. While set, the component reflects the state via `aria-pressed` (or whichever ARIA attribute(s) are listed in `aria-state-key`) and swaps prefix/postfix icons to their filled variants.",
        "ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; pass a comma-separated list (e.g. `aria-pressed,aria-expanded`) to toggle several at once.",
        "fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`), and click/keyboard handlers are blocked.",
        "visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the button from the tab order; consumers must suppress side-effects themselves.",
        "native button behaviour: `button` (default, no implicit action), `submit` (calls `form.requestSubmit()`), `reset` (calls `form.reset()`).",
        "submitted with the form data when the button submits the surrounding form.",
        "submitted with the form data when the button submits the surrounding form.",
        "defaults to `button`. Override only when a different role is genuinely required for the surrounding context."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/button/index.js\"; // custom element registration\n// or via React wrapper\nimport { Button } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button variant=\"primary\" color=\"default\" size=\"32\">Save changes</mdc-button>\n\n<mdc-button variant=\"secondary\" prefix-icon=\"download-bold\" size=\"32\">Download</mdc-button>\n\n<mdc-button variant=\"tertiary\" prefix-icon=\"more-bold\" size=\"32\" aria-label=\"More options\"></mdc-button>"
      }
    ],
    "reactImports": [
      "Button"
    ],
    "constants": {
      "BUTTON_VARIANTS": {
        "PRIMARY": "primary",
        "SECONDARY": "secondary",
        "TERTIARY": "tertiary"
      },
      "PILL_BUTTON_SIZES": {
        "24": 24,
        "28": 28,
        "32": 32,
        "40": 40
      },
      "ICON_BUTTON_SIZES": {
        "20": 20,
        "24": 24,
        "28": 28,
        "32": 32,
        "40": 40,
        "52": 52,
        "64": 64
      },
      "BUTTON_COLORS": {
        "POSITIVE": "positive",
        "NEGATIVE": "negative",
        "ACCENT": "accent",
        "PROMOTIONAL": "promotional",
        "DEFAULT": "default"
      },
      "BUTTON_TYPE_INTERNAL": {
        "PILL": "pill",
        "ICON": "icon",
        "PILL_WITH_ICON": "pill-with-icon"
      },
      "TERTIARY_BUTTON_COLORS": {
        "DEFAULT": "default",
        "ACCENT": "accent",
        "NEGATIVE": "negative"
      },
      "DEFAULTS": {
        "VARIANT": "primary",
        "SIZE": 32,
        "COLOR": "default",
        "TYPE_INTERNAL": "icon",
        "INVERTED": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "button",
      "colors",
      "component",
      "configurable",
      "for",
      "guidelines",
      "icon",
      "in",
      "layouts",
      "mdc",
      "multiple",
      "only",
      "overview",
      "pill",
      "sizes",
      "supports",
      "that",
      "the",
      "usage",
      "variants",
      "with"
    ]
  },
  {
    "name": "buttongroup",
    "title": "ButtonGroup",
    "tagName": "mdc-buttongroup",
    "summary": "Usage, guidelines, and accessibility for the mdc-buttongroup component — a container that groups related buttons into a visually unified, segmented control with shared size and variant.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/buttongroup",
      "knowledgeBasePath": "components/src/components/buttongroup/knowledge-base/buttongroup.component.md"
    },
    "docs": "## Overview\n\nThe buttongroup arranges related `mdc-button` instances into a single segmented control. It applies a shared `size` and `variant` to each slotted button so the group reads as one unit, supports horizontal or vertical layouts, and offers a compact mode for space-constrained surfaces.\n\n### When to use\n\n- Use `mdc-buttongroup` to cluster mutually related actions that operate on the same target (for example a toolbar of formatting actions, a zoom in/out pair, or a \"select / clear\" duo).\n- Use it when the visual unification of multiple buttons communicates that they belong together and share styling.\n\n### When not to use\n\n- Use a single `mdc-button` when there is only one action to present.\n- Use `mdc-tablist` with `mdc-tab` when the controls switch between panels of content — tabs carry the right semantics for that pattern.\n- Use `mdc-toggle` or `mdc-radiogroup` when the buttons represent a mutually exclusive selection that should be announced as a single form value.\n- Use `mdc-menubar` or `mdc-menupopover` when the actions belong inside a menu surface rather than a persistent toolbar.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/buttongroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { ButtonGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-buttongroup orientation=\"horizontal\" variant=\"secondary\" size=\"32\">\n  <mdc-button prefix-icon=\"bold-bold\" aria-label=\"Bold\"></mdc-button>\n  <mdc-button prefix-icon=\"italic-bold\" aria-label=\"Italic\"></mdc-button>\n  <mdc-button prefix-icon=\"underline-bold\" aria-label=\"Underline\"></mdc-button>\n</mdc-buttongroup>\n```\n\n### Content guidance\n\n- Slot only `mdc-button` elements as direct children. Other content is not styled by the group and breaks the segmented appearance.\n- Keep the buttons inside a group conceptually related. If the actions are unrelated, render them as standalone buttons.\n- Mix icon-only and pill buttons sparingly — when mixed, ensure every icon-only button has an `aria-label` so the group remains accessible.\n\n### Property/Attribute details\n\n- `orientation` — layout direction of the slotted buttons.\n  - `horizontal` (default) — buttons are arranged in a row.\n  - `vertical` — buttons are stacked top-to-bottom.\n- `variant` — visual style applied to every child button, overriding their individual `variant`.\n  - `primary` (default) — solid background.\n  - `secondary` — transparent background with border.\n  - `tertiary` — text/icon only, no background or border.\n- `size` — pixel-derived height applied to every child button, overriding their individual `size`. Supported values: `24`, `28` (default), `32`, `40`.\n- `compact` — when `true`, forces every button to a fixed 24px height regardless of `size`. Useful for dense layouts. Defaults to `false`.\n\n### Limitations\n\n- The group only manages direct `mdc-button` children. Any other slotted element keeps its own styling and may visually break the segmented look.\n- `size` and `variant` on the group always override matching attributes on the slotted buttons — set those on the group, not on the individual button.\n- The component does not manage roving tabindex or selection state; arrow-key navigation between the grouped buttons is up to the consumer when that pattern is desired (see Accessibility).\n\n## Accessibility\n\n### Built-in features\n\nThe buttongroup is a presentational wrapper: it renders a generic container and does not set a role, label, or keyboard behaviour on the host. Each slotted `mdc-button` retains its own `role=\"button\"`, tab stop, and ARIA contract.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value                       |\n| ------- | --------- | --------------------------- |\n| Host    | `role`    | None set by the component   |\n\n### Implementation requirements\n\n#### General\n\n- When the group represents a labelled toolbar of actions, wrap or annotate it with `role=\"toolbar\"` and an `aria-label` (or `aria-labelledby`) describing what the toolbar controls. Apply this on the host, not on the inner buttons.\n- If the buttons in the group represent a mutually exclusive selection (radio-style segmented control), use `mdc-radiogroup` or implement `role=\"radiogroup\"` semantics with `aria-checked` on each option — `mdc-buttongroup` alone does not convey selection state.\n- When using a `role=\"toolbar\"` pattern, implement arrow-key navigation between the buttons (`ArrowLeft`/`ArrowRight` for horizontal, `ArrowUp`/`ArrowDown` for vertical) so users do not have to tab through each item.\n\n#### Labelling\n\n- Always label icon-only buttons inside the group with `aria-label` (e.g. `aria-label=\"Bold\"`) — the icon glyph alone is not announced.\n- When the group as a whole represents a single function (e.g. \"Text formatting\"), expose that name via `aria-label` on the host so the surrounding toolbar context is clear to assistive technology users.",
    "sections": {
      "Overview": "The buttongroup arranges related `mdc-button` instances into a single segmented control. It applies a shared `size` and `variant` to each slotted button so the group reads as one unit, supports horizontal or vertical layouts, and offers a compact mode for space-constrained surfaces.\n\n### When to use\n\n- Use `mdc-buttongroup` to cluster mutually related actions that operate on the same target (for example a toolbar of formatting actions, a zoom in/out pair, or a \"select / clear\" duo).\n- Use it when the visual unification of multiple buttons communicates that they belong together and share styling.\n\n### When not to use\n\n- Use a single `mdc-button` when there is only one action to present.\n- Use `mdc-tablist` with `mdc-tab` when the controls switch between panels of content — tabs carry the right semantics for that pattern.\n- Use `mdc-toggle` or `mdc-radiogroup` when the buttons represent a mutually exclusive selection that should be announced as a single form value.\n- Use `mdc-menubar` or `mdc-menupopover` when the actions belong inside a menu surface rather than a persistent toolbar.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/buttongroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { ButtonGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-buttongroup orientation=\"horizontal\" variant=\"secondary\" size=\"32\">\n  <mdc-button prefix-icon=\"bold-bold\" aria-label=\"Bold\"></mdc-button>\n  <mdc-button prefix-icon=\"italic-bold\" aria-label=\"Italic\"></mdc-button>\n  <mdc-button prefix-icon=\"underline-bold\" aria-label=\"Underline\"></mdc-button>\n</mdc-buttongroup>\n```\n\n### Content guidance\n\n- Slot only `mdc-button` elements as direct children. Other content is not styled by the group and breaks the segmented appearance.\n- Keep the buttons inside a group conceptually related. If the actions are unrelated, render them as standalone buttons.\n- Mix icon-only and pill buttons sparingly — when mixed, ensure every icon-only button has an `aria-label` so the group remains accessible.\n\n### Property/Attribute details\n\n- `orientation` — layout direction of the slotted buttons.\n  - `horizontal` (default) — buttons are arranged in a row.\n  - `vertical` — buttons are stacked top-to-bottom.\n- `variant` — visual style applied to every child button, overriding their individual `variant`.\n  - `primary` (default) — solid background.\n  - `secondary` — transparent background with border.\n  - `tertiary` — text/icon only, no background or border.\n- `size` — pixel-derived height applied to every child button, overriding their individual `size`. Supported values: `24`, `28` (default), `32`, `40`.\n- `compact` — when `true`, forces every button to a fixed 24px height regardless of `size`. Useful for dense layouts. Defaults to `false`.\n\n### Limitations\n\n- The group only manages direct `mdc-button` children. Any other slotted element keeps its own styling and may visually break the segmented look.\n- `size` and `variant` on the group always override matching attributes on the slotted buttons — set those on the group, not on the individual button.\n- The component does not manage roving tabindex or selection state; arrow-key navigation between the grouped buttons is up to the consumer when that pattern is desired (see Accessibility).",
      "Accessibility": "### Built-in features\n\nThe buttongroup is a presentational wrapper: it renders a generic container and does not set a role, label, or keyboard behaviour on the host. Each slotted `mdc-button` retains its own `role=\"button\"`, tab stop, and ARIA contract.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value                       |\n| ------- | --------- | --------------------------- |\n| Host    | `role`    | None set by the component   |\n\n### Implementation requirements\n\n#### General\n\n- When the group represents a labelled toolbar of actions, wrap or annotate it with `role=\"toolbar\"` and an `aria-label` (or `aria-labelledby`) describing what the toolbar controls. Apply this on the host, not on the inner buttons.\n- If the buttons in the group represent a mutually exclusive selection (radio-style segmented control), use `mdc-radiogroup` or implement `role=\"radiogroup\"` semantics with `aria-checked` on each option — `mdc-buttongroup` alone does not convey selection state.\n- When using a `role=\"toolbar\"` pattern, implement arrow-key navigation between the buttons (`ArrowLeft`/`ArrowRight` for horizontal, `ArrowUp`/`ArrowDown` for vertical) so users do not have to tab through each item.\n\n#### Labelling\n\n- Always label icon-only buttons inside the group with `aria-label` (e.g. `aria-label=\"Bold\"`) — the icon glyph alone is not announced.\n- When the group as a whole represents a single function (e.g. \"Text formatting\"), expose that name via `aria-label` on the host so the surrounding toolbar context is clear to assistive technology users."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "buttons",
          "attribute": "buttons",
          "type": "Array<HTMLButtonElement>",
          "default": "false",
          "values": [],
          "description": "Enables compact mode with reduced height.\nWhen true, all buttons have a fixed height of 24px regardless of their size setting.\nUseful for space-constrained layouts.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "compact",
          "attribute": "compact",
          "type": "boolean",
          "values": [],
          "description": "when `true`, forces every button to a fixed 24px height regardless of `size`. Useful for dense layouts. Defaults to `false`.",
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "select",
          "default": "'horizontal'",
          "values": [
            "horizontal",
            "vertical"
          ],
          "description": "layout direction of the slotted buttons.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "28",
          "values": [
            24,
            28,
            32,
            40
          ],
          "description": "pixel-derived height applied to every child button, overriding their individual `size`. Supported values: `24`, `28` (default), `32`, `40`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'primary'",
          "values": [
            "primary",
            "secondary"
          ],
          "description": "visual style applied to every child button, overriding their individual `variant`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot, which contains the buttons"
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "container",
          "description": "The container of the buttongroup."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-buttongroup-border-radius",
          "description": "The border radius of the buttongroup"
        },
        {
          "name": "--mdc-buttongroup-border-color",
          "description": "The border color of the buttongroup"
        },
        {
          "name": "--mdc-buttongroup-divider-color",
          "description": "The color of the divider between buttons within the buttongroup"
        }
      ],
      "rules": [
        "layout direction of the slotted buttons.",
        "visual style applied to every child button, overriding their individual `variant`.",
        "pixel-derived height applied to every child button, overriding their individual `size`. Supported values: `24`, `28` (default), `32`, `40`.",
        "when `true`, forces every button to a fixed 24px height regardless of `size`. Useful for dense layouts. Defaults to `false`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/buttongroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { ButtonGroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-buttongroup orientation=\"horizontal\" variant=\"secondary\" size=\"32\">\n  <mdc-button prefix-icon=\"bold-bold\" aria-label=\"Bold\"></mdc-button>\n  <mdc-button prefix-icon=\"italic-bold\" aria-label=\"Italic\"></mdc-button>\n  <mdc-button prefix-icon=\"underline-bold\" aria-label=\"Underline\"></mdc-button>\n</mdc-buttongroup>"
      }
    ],
    "reactImports": [
      "ButtonGroup"
    ],
    "constants": {
      "BUTTON_GROUP_SIZE": {
        "24": 24,
        "28": 28,
        "32": 32,
        "40": 40
      },
      "BUTTON_GROUP_ORIENTATION": {
        "HORIZONTAL": "horizontal",
        "VERTICAL": "vertical"
      },
      "BUTTON_GROUP_VARIANT": {
        "PRIMARY": "primary",
        "SECONDARY": "secondary"
      },
      "DEFAULTS": {
        "SIZE": 28,
        "VARIANT": "primary",
        "ORIENTATION": "horizontal"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "buttongroup",
      "buttons",
      "component",
      "container",
      "control",
      "for",
      "groups",
      "guidelines",
      "into",
      "mdc",
      "overview",
      "related",
      "segmented",
      "shared",
      "size",
      "that",
      "the",
      "unified",
      "usage",
      "variant",
      "visually",
      "with"
    ]
  },
  {
    "name": "buttonlink",
    "title": "ButtonLink",
    "tagName": "mdc-buttonlink",
    "summary": "Usage, guidelines, and accessibility for the mdc-buttonlink component — a link that looks like a button, supporting variant, color, size, and prefix/postfix icons while rendering as an anchor.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/buttonlink",
      "knowledgeBasePath": "components/src/components/buttonlink/knowledge-base/buttonlink.component.md"
    },
    "docs": "## Overview\n\nThe buttonlink renders a navigational anchor (`<a>`) styled as a button. It is the right choice when the visual treatment should match a button but the action is navigation — the underlying element carries link semantics, so right-click, middle-click, and \"open in new tab\" all work as users expect.\n\n### When to use\n\n- Use `mdc-buttonlink` when the control navigates to a different URL or in-app route but the design calls for a button's visual weight (primary call-to-action, prominent toolbar entry, etc.).\n- Use it when you want native browser link affordances such as `target=\"_blank\"`, `download`, `ping`, or `rel` while keeping the button appearance.\n\n### When not to use\n\n- Use `mdc-button` when the control performs an action (submitting a form, opening a dialog, running a command) and does not change the URL.\n- Use `mdc-link` when the control is a textual inline or standalone link and does not need to look like a button.\n- Use `mdc-linksimple` when you need a minimal, unstyled link primitive without button styling or icon slots.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/buttonlink/index.js\"; // custom element registration\n// or via React wrapper\nimport { ButtonLink } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-buttonlink href=\"/docs\" variant=\"primary\" size=\"32\">View documentation</mdc-buttonlink>\n\n<mdc-buttonlink\n  href=\"https://example.com/report.pdf\"\n  download\n  variant=\"secondary\"\n  prefix-icon=\"download-bold\"\n  size=\"32\"\n>\n  Download report\n</mdc-buttonlink>\n\n<mdc-buttonlink\n  href=\"https://example.com\"\n  target=\"_blank\"\n  rel=\"noopener\"\n  variant=\"tertiary\"\n  prefix-icon=\"pop-out-bold\"\n  size=\"32\"\n  data-aria-label=\"Open example.com in a new tab\"\n></mdc-buttonlink>\n```\n\n### Content guidance\n\n- Use destination-oriented labels (e.g. \"View documentation\", \"Open release notes\") rather than verbs like \"Click here\".\n- For icon-only buttonlinks, set `data-aria-label` to describe the destination — the icon alone is not announced.\n- When `target=\"_blank\"`, indicate that the link opens in a new tab in the visible label or accessible name, and include `rel=\"noopener\"` (or `noreferrer`) for safety.\n\n### Property/Attribute details\n\n- `href` — destination URL. Required for the link to be activatable.\n- `target` — anchor target window: `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.\n- `rel` — space-separated relationship tokens (e.g. `noopener`, `noreferrer`); pair with `target=\"_blank\"`.\n- `download` — when set, instructs the browser to download the linked resource. An empty string lets the browser pick the filename; any other string is used as the suggested filename.\n- `ping` — space-separated list of URLs notified when the link is followed.\n- `hreflang` — language hint for the linked resource.\n- `type` — MIME type hint for the linked resource.\n- `referrerpolicy` — controls the Referer header sent when following the link.\n- `variant` — visual style applied through `ButtonComponentMixin`.\n  - `primary` (default) — solid background.\n  - `secondary` — transparent background with border.\n  - `tertiary` — text/icon only, no background or border.\n- `color` — semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttonlinks only support `default`, `accent`, and `negative`; other values fall back to `default`.\n- `size` — pixel-derived sizing matched against the inferred button type:\n  - Pill / pill-with-icon: `40`, `32` (default), `28`, `24`.\n  - Icon: `64`, `52`, `40`, `32` (default), `28`, `24`. `20` is only valid when `variant=\"tertiary\"`.\n  - Invalid combinations fall back to the default (`32`).\n- `prefix-icon` / `postfix-icon` — icon name rendered before/after the slotted label. Supplying an icon without a default slot label produces an icon-only buttonlink.\n- `inline` — when `true`, the buttonlink flows inline with surrounding text rather than as a standalone block. Defaults to `false`.\n- `inverted` — flips the color scheme for use on dark/inverted surfaces.\n- `disabled` — disables navigation. Sets `aria-disabled=\"true\"`, makes the anchor unfocusable (`tabindex=\"-1\"`), and suppresses click/keydown navigation.\n- `soft-disabled` — appears disabled but stays focusable and activatable. Sets `aria-disabled=\"true\"` without changing `tabindex`; consumers must suppress side-effects themselves.\n- `data-aria-label` — accessible name applied to the underlying `<a>` element via `aria-label`. Required for icon-only buttonlinks.\n\n### Limitations\n\n- Unlike `mdc-button`, the buttonlink does not expose `active`/`aria-pressed` semantics — anchors do not have a toggled state. Use `mdc-button` when the control must communicate an on/off state.\n- `size=\"20\"` is only honoured for tertiary icon buttonlinks; other variant/size combinations fall back to the default size.\n- `inverted` is purely visual; pair it with a surface that actually requires the inverted palette.\n\n## Accessibility\n\n### Built-in features\n\nThe buttonlink renders a real `<a>` element inside its shadow DOM, so screen readers and the browser treat it as a link: it appears in the page's link list, supports the browser's native context menu, and respects `target`, `rel`, and `download` semantics out of the box. Focus is delegated into the shadow anchor (`delegatesFocus: true`), so consumer focus styles and keyboard navigation work on the host element.\n\nActivation works via `Enter` (the anchor's native behaviour) and on click. When `disabled` is set, the component intercepts both `click` and `keydown` to prevent navigation, marks the host `aria-disabled=\"true\"`, and gives the anchor `tabindex=\"-1\"` so it is removed from the tab order. `soft-disabled` only sets `aria-disabled=\"true\"`; the anchor remains focusable and activatable so the consumer can suppress the side-effect while keeping the control discoverable.\n\nThe accessible name on the inner anchor is taken from the host's `data-aria-label` attribute and mirrored onto the anchor's `aria-label`.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute       | Value                                                                                              |\n| -------------- | --------------- | -------------------------------------------------------------------------------------------------- |\n| Host           | `aria-disabled` | `true` while `disabled` or `soft-disabled`; removed otherwise                                      |\n| Shadow `<a>`   | `href`          | Mirrors the `href` property                                                                        |\n| Shadow `<a>`   | `target`        | Mirrors the `target` property (default `_self`)                                                    |\n| Shadow `<a>`   | `rel`           | Mirrors the `rel` property when set                                                                |\n| Shadow `<a>`   | `download`      | Mirrors the `download` property when set                                                           |\n| Shadow `<a>`   | `aria-label`    | Mirrors `data-aria-label` from the host                                                            |\n| Shadow `<a>`   | `tabindex`      | `0` when enabled; `-1` while `disabled`                                                            |\n\n### Implementation requirements\n\n#### Labelling\n\n- Text buttonlinks get their accessible name from the slotted label — keep it destination-oriented and self-describing.\n- Icon-only buttonlinks (no default slot content, only `prefix-icon` or `postfix-icon`) MUST set `data-aria-label` so the inner anchor has an accessible name.\n- When `target=\"_blank\"`, surface the \"opens in a new tab\" hint in the label or `data-aria-label`, and add `rel=\"noopener\"` (and `noreferrer` when appropriate) to avoid window-opener leaks.\n- For downloads, prefer a label that names the file or format (e.g. \"Download PDF report\") so the action is obvious before the user activates it.",
    "sections": {
      "Overview": "The buttonlink renders a navigational anchor (`<a>`) styled as a button. It is the right choice when the visual treatment should match a button but the action is navigation — the underlying element carries link semantics, so right-click, middle-click, and \"open in new tab\" all work as users expect.\n\n### When to use\n\n- Use `mdc-buttonlink` when the control navigates to a different URL or in-app route but the design calls for a button's visual weight (primary call-to-action, prominent toolbar entry, etc.).\n- Use it when you want native browser link affordances such as `target=\"_blank\"`, `download`, `ping`, or `rel` while keeping the button appearance.\n\n### When not to use\n\n- Use `mdc-button` when the control performs an action (submitting a form, opening a dialog, running a command) and does not change the URL.\n- Use `mdc-link` when the control is a textual inline or standalone link and does not need to look like a button.\n- Use `mdc-linksimple` when you need a minimal, unstyled link primitive without button styling or icon slots.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/buttonlink/index.js\"; // custom element registration\n// or via React wrapper\nimport { ButtonLink } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-buttonlink href=\"/docs\" variant=\"primary\" size=\"32\">View documentation</mdc-buttonlink>\n\n<mdc-buttonlink\n  href=\"https://example.com/report.pdf\"\n  download\n  variant=\"secondary\"\n  prefix-icon=\"download-bold\"\n  size=\"32\"\n>\n  Download report\n</mdc-buttonlink>\n\n<mdc-buttonlink\n  href=\"https://example.com\"\n  target=\"_blank\"\n  rel=\"noopener\"\n  variant=\"tertiary\"\n  prefix-icon=\"pop-out-bold\"\n  size=\"32\"\n  data-aria-label=\"Open example.com in a new tab\"\n></mdc-buttonlink>\n```\n\n### Content guidance\n\n- Use destination-oriented labels (e.g. \"View documentation\", \"Open release notes\") rather than verbs like \"Click here\".\n- For icon-only buttonlinks, set `data-aria-label` to describe the destination — the icon alone is not announced.\n- When `target=\"_blank\"`, indicate that the link opens in a new tab in the visible label or accessible name, and include `rel=\"noopener\"` (or `noreferrer`) for safety.\n\n### Property/Attribute details\n\n- `href` — destination URL. Required for the link to be activatable.\n- `target` — anchor target window: `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.\n- `rel` — space-separated relationship tokens (e.g. `noopener`, `noreferrer`); pair with `target=\"_blank\"`.\n- `download` — when set, instructs the browser to download the linked resource. An empty string lets the browser pick the filename; any other string is used as the suggested filename.\n- `ping` — space-separated list of URLs notified when the link is followed.\n- `hreflang` — language hint for the linked resource.\n- `type` — MIME type hint for the linked resource.\n- `referrerpolicy` — controls the Referer header sent when following the link.\n- `variant` — visual style applied through `ButtonComponentMixin`.\n  - `primary` (default) — solid background.\n  - `secondary` — transparent background with border.\n  - `tertiary` — text/icon only, no background or border.\n- `color` — semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttonlinks only support `default`, `accent`, and `negative`; other values fall back to `default`.\n- `size` — pixel-derived sizing matched against the inferred button type:\n  - Pill / pill-with-icon: `40`, `32` (default), `28`, `24`.\n  - Icon: `64`, `52`, `40`, `32` (default), `28`, `24`. `20` is only valid when `variant=\"tertiary\"`.\n  - Invalid combinations fall back to the default (`32`).\n- `prefix-icon` / `postfix-icon` — icon name rendered before/after the slotted label. Supplying an icon without a default slot label produces an icon-only buttonlink.\n- `inline` — when `true`, the buttonlink flows inline with surrounding text rather than as a standalone block. Defaults to `false`.\n- `inverted` — flips the color scheme for use on dark/inverted surfaces.\n- `disabled` — disables navigation. Sets `aria-disabled=\"true\"`, makes the anchor unfocusable (`tabindex=\"-1\"`), and suppresses click/keydown navigation.\n- `soft-disabled` — appears disabled but stays focusable and activatable. Sets `aria-disabled=\"true\"` without changing `tabindex`; consumers must suppress side-effects themselves.\n- `data-aria-label` — accessible name applied to the underlying `<a>` element via `aria-label`. Required for icon-only buttonlinks.\n\n### Limitations\n\n- Unlike `mdc-button`, the buttonlink does not expose `active`/`aria-pressed` semantics — anchors do not have a toggled state. Use `mdc-button` when the control must communicate an on/off state.\n- `size=\"20\"` is only honoured for tertiary icon buttonlinks; other variant/size combinations fall back to the default size.\n- `inverted` is purely visual; pair it with a surface that actually requires the inverted palette.",
      "Accessibility": "### Built-in features\n\nThe buttonlink renders a real `<a>` element inside its shadow DOM, so screen readers and the browser treat it as a link: it appears in the page's link list, supports the browser's native context menu, and respects `target`, `rel`, and `download` semantics out of the box. Focus is delegated into the shadow anchor (`delegatesFocus: true`), so consumer focus styles and keyboard navigation work on the host element.\n\nActivation works via `Enter` (the anchor's native behaviour) and on click. When `disabled` is set, the component intercepts both `click` and `keydown` to prevent navigation, marks the host `aria-disabled=\"true\"`, and gives the anchor `tabindex=\"-1\"` so it is removed from the tab order. `soft-disabled` only sets `aria-disabled=\"true\"`; the anchor remains focusable and activatable so the consumer can suppress the side-effect while keeping the control discoverable.\n\nThe accessible name on the inner anchor is taken from the host's `data-aria-label` attribute and mirrored onto the anchor's `aria-label`.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute       | Value                                                                                              |\n| -------------- | --------------- | -------------------------------------------------------------------------------------------------- |\n| Host           | `aria-disabled` | `true` while `disabled` or `soft-disabled`; removed otherwise                                      |\n| Shadow `<a>`   | `href`          | Mirrors the `href` property                                                                        |\n| Shadow `<a>`   | `target`        | Mirrors the `target` property (default `_self`)                                                    |\n| Shadow `<a>`   | `rel`           | Mirrors the `rel` property when set                                                                |\n| Shadow `<a>`   | `download`      | Mirrors the `download` property when set                                                           |\n| Shadow `<a>`   | `aria-label`    | Mirrors `data-aria-label` from the host                                                            |\n| Shadow `<a>`   | `tabindex`      | `0` when enabled; `-1` while `disabled`                                                            |\n\n### Implementation requirements\n\n#### Labelling\n\n- Text buttonlinks get their accessible name from the slotted label — keep it destination-oriented and self-describing.\n- Icon-only buttonlinks (no default slot content, only `prefix-icon` or `postfix-icon`) MUST set `data-aria-label` so the inner anchor has an accessible name.\n- When `target=\"_blank\"`, surface the \"opens in a new tab\" hint in the label or `data-aria-label`, and add `rel=\"noopener\"` (and `noreferrer` when appropriate) to avoid window-opener leaks.\n- For downloads, prefer a label that names the file or format (e.g. \"Download PDF report\") so the action is obvious before the user activates it."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Text label for the buttonLink.",
          "source": "storybook"
        },
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "values": [],
          "description": "semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttonlinks only support `default`, `accent`, and `negative`; other values fall back to `default`.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the underlying `<a>` element via `aria-label`. Required for icon-only buttonlinks.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "disables navigation. Sets `aria-disabled=\"true\"`, makes the anchor unfocusable (`tabindex=\"-1\"`), and suppresses click/keydown navigation.",
          "source": "storybook"
        },
        {
          "name": "download",
          "attribute": "download",
          "type": "text",
          "values": [],
          "description": "when set, instructs the browser to download the linked resource. An empty string lets the browser pick the filename; any other string is used as the suggested filename.",
          "source": "storybook"
        },
        {
          "name": "href",
          "attribute": "href",
          "type": "text",
          "values": [],
          "description": "destination URL. Required for the link to be activatable.",
          "source": "storybook"
        },
        {
          "name": "hreflang",
          "attribute": "hreflang",
          "type": "text",
          "values": [],
          "description": "language hint for the linked resource.",
          "source": "storybook"
        },
        {
          "name": "ping",
          "attribute": "ping",
          "type": "text",
          "values": [],
          "description": "space-separated list of URLs notified when the link is followed.",
          "source": "storybook"
        },
        {
          "name": "postfixIcon",
          "attribute": "postfix-icon",
          "type": "select",
          "values": [],
          "description": "icon name rendered before/after the slotted label. Supplying an icon without a default slot label produces an icon-only buttonlink.",
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "select",
          "values": [],
          "description": "icon name rendered before/after the slotted label. Supplying an icon without a default slot label produces an icon-only buttonlink.",
          "source": "storybook"
        },
        {
          "name": "referrerpolicy",
          "attribute": "referrerpolicy",
          "type": "text",
          "values": [],
          "description": "controls the Referer header sent when following the link.",
          "source": "storybook"
        },
        {
          "name": "rel",
          "attribute": "rel",
          "type": "text",
          "values": [],
          "description": "space-separated relationship tokens (e.g. `noopener`, `noreferrer`); pair with `target=\"_blank\"`.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "32",
          "values": [],
          "description": "pixel-derived sizing matched against the inferred button type:",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "appears disabled but stays focusable and activatable. Sets `aria-disabled=\"true\"` without changing `tabindex`; consumers must suppress side-effects themselves.",
          "source": "storybook"
        },
        {
          "name": "target",
          "attribute": "target",
          "type": "text",
          "values": [],
          "description": "anchor target window: `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.",
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "text",
          "values": [],
          "description": "MIME type hint for the linked resource.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [],
          "description": "visual style applied through `ButtonComponentMixin`.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "The default slot for buttonlink text content."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Fired when the user activates the buttonLink using a mouse or assistive technology."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) Fired when the user presses a key while the buttonLink has focus."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Fired when the buttonLink receives keyboard or mouse focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) Fired when the buttonLink loses keyboard or mouse focus."
        }
      ],
      "cssParts": [
        {
          "name": "anchor",
          "description": "The anchor element that wraps the buttonlink content."
        },
        {
          "name": "prefix-icon",
          "description": "The prefix icon element."
        },
        {
          "name": "button-text",
          "description": "The slot containing the buttonlink text."
        },
        {
          "name": "postfix-icon",
          "description": "The postfix icon element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-button-height",
          "description": "Height of the buttonlink"
        },
        {
          "name": "--mdc-button-background",
          "description": "Background color of the buttonlink"
        },
        {
          "name": "--mdc-button-border-color",
          "description": "Border color of the buttonlink"
        },
        {
          "name": "--mdc-button-text-color",
          "description": "Text color of the buttonlink"
        },
        {
          "name": "--mdc-button-line-height",
          "description": "Line height of the buttonlink text"
        },
        {
          "name": "--mdc-button-prefix-icon-size",
          "description": "Size of the prefix icon"
        },
        {
          "name": "--mdc-button-postfix-icon-size",
          "description": "Size of the postfix icon"
        }
      ],
      "rules": [
        "destination URL. Required for the link to be activatable.",
        "anchor target window: `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.",
        "space-separated relationship tokens (e.g. `noopener`, `noreferrer`); pair with `target=\"_blank\"`.",
        "when set, instructs the browser to download the linked resource. An empty string lets the browser pick the filename; any other string is used as the suggested filename.",
        "space-separated list of URLs notified when the link is followed.",
        "language hint for the linked resource.",
        "MIME type hint for the linked resource.",
        "controls the Referer header sent when following the link.",
        "visual style applied through `ButtonComponentMixin`.",
        "semantic color. `default` (default), `positive`, `negative`, `accent`, `promotional`. Tertiary buttonlinks only support `default`, `accent`, and `negative`; other values fall back to `default`.",
        "pixel-derived sizing matched against the inferred button type:",
        "icon name rendered before/after the slotted label. Supplying an icon without a default slot label produces an icon-only buttonlink.",
        "icon name rendered before/after the slotted label. Supplying an icon without a default slot label produces an icon-only buttonlink.",
        "when `true`, the buttonlink flows inline with surrounding text rather than as a standalone block. Defaults to `false`.",
        "flips the color scheme for use on dark/inverted surfaces.",
        "disables navigation. Sets `aria-disabled=\"true\"`, makes the anchor unfocusable (`tabindex=\"-1\"`), and suppresses click/keydown navigation.",
        "appears disabled but stays focusable and activatable. Sets `aria-disabled=\"true\"` without changing `tabindex`; consumers must suppress side-effects themselves.",
        "accessible name applied to the underlying `<a>` element via `aria-label`. Required for icon-only buttonlinks."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/buttonlink/index.js\"; // custom element registration\n// or via React wrapper\nimport { ButtonLink } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-buttonlink href=\"/docs\" variant=\"primary\" size=\"32\">View documentation</mdc-buttonlink>\n\n<mdc-buttonlink\n  href=\"https://example.com/report.pdf\"\n  download\n  variant=\"secondary\"\n  prefix-icon=\"download-bold\"\n  size=\"32\"\n>\n  Download report\n</mdc-buttonlink>\n\n<mdc-buttonlink\n  href=\"https://example.com\"\n  target=\"_blank\"\n  rel=\"noopener\"\n  variant=\"tertiary\"\n  prefix-icon=\"pop-out-bold\"\n  size=\"32\"\n  data-aria-label=\"Open example.com in a new tab\"\n></mdc-buttonlink>"
      }
    ],
    "reactImports": [
      "ButtonLink"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "an",
      "anchor",
      "and",
      "as",
      "button",
      "buttonlink",
      "color",
      "component",
      "for",
      "guidelines",
      "icon",
      "icons",
      "like",
      "link",
      "looks",
      "mdc",
      "overview",
      "postfix",
      "prefix",
      "rendering",
      "size",
      "supporting",
      "that",
      "the",
      "usage",
      "variant",
      "while"
    ]
  },
  {
    "name": "buttonsimple",
    "title": "ButtonSimple",
    "tagName": "mdc-buttonsimple",
    "summary": "Usage, guidelines, and accessibility for the mdc-buttonsimple component — an unstyled, form-associated button primitive supplying keyboard, focus, disabled, and toggle semantics.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/buttonsimple",
      "knowledgeBasePath": "components/src/components/buttonsimple/knowledge-base/buttonsimple.component.md"
    },
    "docs": "## Overview\n\nThe buttonsimple is a low-level button primitive that handles keyboard activation, focus management, disabled/soft-disabled states, form association, and optional toggled state. It does not provide visual styling on its own — consumers wrap or compose it when they need button semantics without the opinionated appearance of `mdc-button`.\n\n### When to use\n\n- Use `mdc-buttonsimple` when you are building a custom button surface (for example a card, list item, or composite control) and need correct keyboard/ARIA semantics without inheriting the visual styling of `mdc-button`.\n- Use it inside design-system components that need to behave like buttons but render their own layout (e.g. `mdc-button`, `mdc-cardbutton`, `mdc-listitem` actions).\n\n### When not to use\n\n- Use `mdc-button` when the design calls for the standard visual treatment (variant, color, size, prefix/postfix icons).\n- Use `mdc-buttonlink` when the control is a navigation that should render as an anchor.\n- Use `mdc-toggle` or `mdc-checkbox` for binary state in forms — they expose the correct form-control semantics and labelling.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/buttonsimple/index.js\"; // custom element registration\n// or via React wrapper\nimport { Buttonsimple } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-buttonsimple aria-label=\"Save\">\n  <!-- consumer-provided content / styling -->\n  <span class=\"custom-surface\">Save</span>\n</mdc-buttonsimple>\n\n<!-- Toggle button: aria-pressed reflects the active state automatically -->\n<mdc-buttonsimple active aria-label=\"Mute microphone\">…</mdc-buttonsimple>\n\n<!-- Form submit -->\n<form>\n  <mdc-buttonsimple type=\"submit\" name=\"action\" value=\"save\">Save</mdc-buttonsimple>\n</form>\n```\n\n### Content guidance\n\n- Always supply an accessible name (visible text in the default slot or `aria-label`) — the primitive has no built-in label.\n- Keep the slotted content focused on the button's action; do not nest focusable controls inside it.\n\n### Property/Attribute details\n\n- `type` — native button behaviour: `button` (default, no implicit action), `submit` (calls the associated form's `requestSubmit()`), `reset` (calls `reset()`). Form association is automatic when the element is placed inside a `<form>`.\n- `name` / `value` — submitted as a form-data pair when this button is used to submit the form.\n- `size` — numeric size token. Defaults to `32`. Higher-level buttons (e.g. `mdc-button`) validate sizes against their own supported sets; `mdc-buttonsimple` accepts the superset and leaves visual interpretation to the consumer.\n- `active` — toggles the button between pressed and unpressed states. While set, the component writes `true`/`false` into the configured ARIA state attribute (default `aria-pressed`). Leave it `undefined` to remove the attribute entirely (i.e. not a toggle button).\n- `aria-state-key` — the ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; supply a comma-separated list (e.g. `aria-pressed,aria-expanded`) to mirror the active state across multiple attributes.\n- `role` — defaults to `button`. Override only when a different role is required for the surrounding context.\n- `disabled` — fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`, the previous value is restored when re-enabled), and click/keyboard handlers are blocked.\n- `soft-disabled` — visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the tab stop; consumers must suppress side-effects themselves so the button behaves as disabled.\n- `tabindex` — standard tabindex; the primitive saves and restores the consumer-provided value across `disabled` toggles.\n\n### Limitations\n\n- The component ships with no visual styling — consumers (or wrapping components) are responsible for the appearance, hover/focus indicators, and active-state visuals.\n- The form-association integration relies on `ElementInternals`; the surrounding page must run in a browser that supports it.\n- `aria-state-key` only writes attributes that start with `aria-`; non-ARIA attribute names in the list are ignored.\n\n## Accessibility\n\n### Built-in features\n\nThe host carries `role=\"button\"` (overridable) and participates in the page's tab order via the `TabIndexMixin`. The primitive replicates native `<button>` keyboard behaviour: `Enter` activates immediately on keydown (firing `click`) and `Space` activates on keyup after a keydown — the component temporarily adds a `pressed` class during the press so consumers can style the down-state, and clears it on blur if focus is lost mid-press. The default browser action for both keys is prevented so the surrounding page does not scroll on `Space`.\n\n`active` is mirrored into the configured ARIA state attribute(s); by default that is `aria-pressed`, but consumers can broaden it (e.g. `aria-pressed,aria-expanded`) for disclosure-style buttons. Setting `disabled` removes the element from the tab order and marks it `aria-disabled=\"true\"`; setting `soft-disabled` keeps the tab stop but still announces the button as disabled, which is useful when you want to explain *why* the action is unavailable without hiding the control.\n\nThe element is form-associated (`formAssociated = true`): with `type=\"submit\"`/`\"reset\"` it triggers `requestSubmit()` / `reset()` on the surrounding form, and `name`/`value` are sent with the form data.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute                                | Value                                                                                                                                  |\n| ------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| Host    | `role`                                   | `button` by default; reflects the `role` property when overridden                                                                      |\n| Host    | `tabindex`                               | Consumer-provided value (default `0`); forced to `-1` while `disabled` and restored on re-enable                                       |\n| Host    | `aria-disabled`                          | `true` while `disabled` or `soft-disabled`; removed otherwise                                                                          |\n| Host    | `aria-pressed` (default)                 | `true`/`false` mirroring the `active` property when `aria-state-key` is left at its default; attribute removed when `active` is `undefined` |\n| Host    | configured `aria-state-key` attribute(s) | `true`/`false` mirroring `active` when `aria-state-key` is set to one or more `aria-*` names                                           |\n\n### Implementation requirements\n\n#### General\n\n- Wrap or compose `mdc-buttonsimple` with your own focus ring and pressed-state styles — the primitive only toggles a `pressed` class while a key is held.\n- When using `soft-disabled`, also convey *why* the button is unavailable (helper text, tooltip) so the announcement is actionable rather than confusing.\n- If you change `role` away from `button`, audit the resulting keyboard contract: `Enter`/`Space` handling and `aria-pressed` semantics are tuned for the button role and may not match the new role's expectations.\n\n#### Labelling\n\n- Supply an accessible name via the default slot text or `aria-label` — the primitive does not generate one.\n- For icon-only or visually empty buttons, `aria-label` is required.\n- When `aria-state-key` uses a non-default ARIA attribute (e.g. `aria-expanded`), make sure the visible state and the chosen attribute carry compatible meaning (`aria-pressed` for toggle, `aria-expanded` for disclosure, etc.).",
    "sections": {
      "Overview": "The buttonsimple is a low-level button primitive that handles keyboard activation, focus management, disabled/soft-disabled states, form association, and optional toggled state. It does not provide visual styling on its own — consumers wrap or compose it when they need button semantics without the opinionated appearance of `mdc-button`.\n\n### When to use\n\n- Use `mdc-buttonsimple` when you are building a custom button surface (for example a card, list item, or composite control) and need correct keyboard/ARIA semantics without inheriting the visual styling of `mdc-button`.\n- Use it inside design-system components that need to behave like buttons but render their own layout (e.g. `mdc-button`, `mdc-cardbutton`, `mdc-listitem` actions).\n\n### When not to use\n\n- Use `mdc-button` when the design calls for the standard visual treatment (variant, color, size, prefix/postfix icons).\n- Use `mdc-buttonlink` when the control is a navigation that should render as an anchor.\n- Use `mdc-toggle` or `mdc-checkbox` for binary state in forms — they expose the correct form-control semantics and labelling.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/buttonsimple/index.js\"; // custom element registration\n// or via React wrapper\nimport { Buttonsimple } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-buttonsimple aria-label=\"Save\">\n  <!-- consumer-provided content / styling -->\n  <span class=\"custom-surface\">Save</span>\n</mdc-buttonsimple>\n\n<!-- Toggle button: aria-pressed reflects the active state automatically -->\n<mdc-buttonsimple active aria-label=\"Mute microphone\">…</mdc-buttonsimple>\n\n<!-- Form submit -->\n<form>\n  <mdc-buttonsimple type=\"submit\" name=\"action\" value=\"save\">Save</mdc-buttonsimple>\n</form>\n```\n\n### Content guidance\n\n- Always supply an accessible name (visible text in the default slot or `aria-label`) — the primitive has no built-in label.\n- Keep the slotted content focused on the button's action; do not nest focusable controls inside it.\n\n### Property/Attribute details\n\n- `type` — native button behaviour: `button` (default, no implicit action), `submit` (calls the associated form's `requestSubmit()`), `reset` (calls `reset()`). Form association is automatic when the element is placed inside a `<form>`.\n- `name` / `value` — submitted as a form-data pair when this button is used to submit the form.\n- `size` — numeric size token. Defaults to `32`. Higher-level buttons (e.g. `mdc-button`) validate sizes against their own supported sets; `mdc-buttonsimple` accepts the superset and leaves visual interpretation to the consumer.\n- `active` — toggles the button between pressed and unpressed states. While set, the component writes `true`/`false` into the configured ARIA state attribute (default `aria-pressed`). Leave it `undefined` to remove the attribute entirely (i.e. not a toggle button).\n- `aria-state-key` — the ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; supply a comma-separated list (e.g. `aria-pressed,aria-expanded`) to mirror the active state across multiple attributes.\n- `role` — defaults to `button`. Override only when a different role is required for the surrounding context.\n- `disabled` — fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`, the previous value is restored when re-enabled), and click/keyboard handlers are blocked.\n- `soft-disabled` — visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the tab stop; consumers must suppress side-effects themselves so the button behaves as disabled.\n- `tabindex` — standard tabindex; the primitive saves and restores the consumer-provided value across `disabled` toggles.\n\n### Limitations\n\n- The component ships with no visual styling — consumers (or wrapping components) are responsible for the appearance, hover/focus indicators, and active-state visuals.\n- The form-association integration relies on `ElementInternals`; the surrounding page must run in a browser that supports it.\n- `aria-state-key` only writes attributes that start with `aria-`; non-ARIA attribute names in the list are ignored.",
      "Accessibility": "### Built-in features\n\nThe host carries `role=\"button\"` (overridable) and participates in the page's tab order via the `TabIndexMixin`. The primitive replicates native `<button>` keyboard behaviour: `Enter` activates immediately on keydown (firing `click`) and `Space` activates on keyup after a keydown — the component temporarily adds a `pressed` class during the press so consumers can style the down-state, and clears it on blur if focus is lost mid-press. The default browser action for both keys is prevented so the surrounding page does not scroll on `Space`.\n\n`active` is mirrored into the configured ARIA state attribute(s); by default that is `aria-pressed`, but consumers can broaden it (e.g. `aria-pressed,aria-expanded`) for disclosure-style buttons. Setting `disabled` removes the element from the tab order and marks it `aria-disabled=\"true\"`; setting `soft-disabled` keeps the tab stop but still announces the button as disabled, which is useful when you want to explain *why* the action is unavailable without hiding the control.\n\nThe element is form-associated (`formAssociated = true`): with `type=\"submit\"`/`\"reset\"` it triggers `requestSubmit()` / `reset()` on the surrounding form, and `name`/`value` are sent with the form data.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute                                | Value                                                                                                                                  |\n| ------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| Host    | `role`                                   | `button` by default; reflects the `role` property when overridden                                                                      |\n| Host    | `tabindex`                               | Consumer-provided value (default `0`); forced to `-1` while `disabled` and restored on re-enable                                       |\n| Host    | `aria-disabled`                          | `true` while `disabled` or `soft-disabled`; removed otherwise                                                                          |\n| Host    | `aria-pressed` (default)                 | `true`/`false` mirroring the `active` property when `aria-state-key` is left at its default; attribute removed when `active` is `undefined` |\n| Host    | configured `aria-state-key` attribute(s) | `true`/`false` mirroring `active` when `aria-state-key` is set to one or more `aria-*` names                                           |\n\n### Implementation requirements\n\n#### General\n\n- Wrap or compose `mdc-buttonsimple` with your own focus ring and pressed-state styles — the primitive only toggles a `pressed` class while a key is held.\n- When using `soft-disabled`, also convey *why* the button is unavailable (helper text, tooltip) so the announcement is actionable rather than confusing.\n- If you change `role` away from `button`, audit the resulting keyboard contract: `Enter`/`Space` handling and `aria-pressed` semantics are tuned for the button role and may not match the new role's expectations.\n\n#### Labelling\n\n- Supply an accessible name via the default slot text or `aria-label` — the primitive does not generate one.\n- For icon-only or visually empty buttons, `aria-label` is required.\n- When `aria-state-key` uses a non-default ARIA attribute (e.g. `aria-expanded`), make sure the visible state and the chosen attribute carry compatible meaning (`aria-pressed` for toggle, `aria-expanded` for disclosure, etc.)."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "active",
          "attribute": "active",
          "type": "boolean",
          "values": [],
          "description": "toggles the button between pressed and unpressed states. While set, the component writes `true`/`false` into the configured ARIA state attribute (default `aria-pressed`). Leave it `undefined` to remove the attribute entirely (i.e. not a toggle button).",
          "source": "storybook"
        },
        {
          "name": "ariaStateKey",
          "attribute": "aria-state-key",
          "type": "text",
          "values": [],
          "description": "the ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; supply a comma-separated list (e.g. `aria-pressed,aria-expanded`) to mirror the active state across multiple attributes.",
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Text label for the button.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`, the previous value is restored when re-enabled), and click/keyboard handlers are blocked.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "submitted as a form-data pair when this button is used to submit the form.",
          "source": "storybook"
        },
        {
          "name": "role",
          "attribute": "role",
          "type": "RoleType",
          "default": "button",
          "values": [],
          "description": "defaults to `button`. Override only when a different role is required for the surrounding context.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "undefined",
          "values": [
            12,
            14,
            16,
            20,
            24,
            28,
            32,
            40,
            48,
            52,
            64,
            72,
            88,
            124
          ],
          "description": "numeric size token. Defaults to `32`. Higher-level buttons (e.g. `mdc-button`) validate sizes against their own supported sets; `mdc-buttonsimple` accepts the superset and leaves visual interpretation to the consumer.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the tab stop; consumers must suppress side-effects themselves so the button behaves as disabled.",
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "select",
          "default": "'aria-pressed'",
          "values": [
            "button",
            "submit",
            "reset"
          ],
          "description": "native button behaviour: `button` (default, no implicit action), `submit` (calls the associated form's `requestSubmit()`), `reset` (calls `reset()`). Form association is automatic when the element is placed inside a `<form>`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "submitted as a form-data pair when this button is used to submit the form.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Text label of the button."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the button is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the button."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the button."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the button receives focus."
        }
      ],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-button-height",
          "description": "Height of the button"
        },
        {
          "name": "--mdc-button-background",
          "description": "Background color of the button"
        },
        {
          "name": "--mdc-button-border-color",
          "description": "Border color of the button"
        },
        {
          "name": "--mdc-button-text-color",
          "description": "Text color of the button"
        }
      ],
      "rules": [
        "native button behaviour: `button` (default, no implicit action), `submit` (calls the associated form's `requestSubmit()`), `reset` (calls `reset()`). Form association is automatic when the element is placed inside a `<form>`.",
        "submitted as a form-data pair when this button is used to submit the form.",
        "submitted as a form-data pair when this button is used to submit the form.",
        "numeric size token. Defaults to `32`. Higher-level buttons (e.g. `mdc-button`) validate sizes against their own supported sets; `mdc-buttonsimple` accepts the superset and leaves visual interpretation to the consumer.",
        "toggles the button between pressed and unpressed states. While set, the component writes `true`/`false` into the configured ARIA state attribute (default `aria-pressed`). Leave it `undefined` to remove the attribute entirely (i.e. not a toggle button).",
        "the ARIA attribute(s) toggled by `active`. Defaults to `aria-pressed`; supply a comma-separated list (e.g. `aria-pressed,aria-expanded`) to mirror the active state across multiple attributes.",
        "defaults to `button`. Override only when a different role is required for the surrounding context.",
        "fully disables the button. The host receives `aria-disabled=\"true\"`, tab order is removed (`tabindex=\"-1\"`, the previous value is restored when re-enabled), and click/keyboard handlers are blocked.",
        "visually disabled but still focusable and clickable. Sets `aria-disabled=\"true\"` without removing the tab stop; consumers must suppress side-effects themselves so the button behaves as disabled.",
        "standard tabindex; the primitive saves and restores the consumer-provided value across `disabled` toggles."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/buttonsimple/index.js\"; // custom element registration\n// or via React wrapper\nimport { Buttonsimple } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-buttonsimple aria-label=\"Save\">\n  <!-- consumer-provided content / styling -->\n  <span class=\"custom-surface\">Save</span>\n</mdc-buttonsimple>\n\n<!-- Toggle button: aria-pressed reflects the active state automatically -->\n<mdc-buttonsimple active aria-label=\"Mute microphone\">…</mdc-buttonsimple>\n\n<!-- Form submit -->\n<form>\n  <mdc-buttonsimple type=\"submit\" name=\"action\" value=\"save\">Save</mdc-buttonsimple>\n</form>"
      }
    ],
    "reactImports": [
      "Buttonsimple"
    ],
    "constants": {
      "BUTTON_SIZES": {
        "12": 12,
        "14": 14,
        "16": 16,
        "20": 20,
        "24": 24,
        "28": 28,
        "32": 32,
        "40": 40,
        "48": 48,
        "52": 52,
        "64": 64,
        "72": 72,
        "88": 88,
        "124": 124
      },
      "BUTTON_TYPE": {
        "BUTTON": "button",
        "SUBMIT": "submit",
        "RESET": "reset"
      },
      "DEFAULTS": {
        "SIZE": 32,
        "TYPE": "button",
        "ARIA_STATE_KEY": "aria-pressed"
      }
    },
    "keywords": [
      "accessibility",
      "an",
      "and",
      "associated",
      "button",
      "buttonsimple",
      "component",
      "disabled",
      "focus",
      "for",
      "form",
      "guidelines",
      "keyboard",
      "mdc",
      "overview",
      "primitive",
      "semantics",
      "supplying",
      "the",
      "toggle",
      "unstyled",
      "usage"
    ]
  },
  {
    "name": "calendar",
    "title": "Calendar",
    "tagName": "mdc-calendar",
    "summary": "Usage, guidelines, and accessibility for the mdc-calendar component — a monthly grid for selecting a single date, a week, or a date range with keyboard navigation and locale-aware formatting.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/calendar",
      "knowledgeBasePath": "components/src/components/calendar/knowledge-base/calendar.component.md"
    },
    "docs": "## Overview\n\nThe calendar renders a month-by-month grid that lets users select a single date, an entire week, or a date range. It is keyboard-navigable, respects locale-defined week start and date formatting, and constrains selection to an optional `min`/`max` window.\n\n### When to use\n\n- Use `mdc-calendar` when the surface needs to display a full month grid for date selection — for example a standalone scheduler view or the calendar panel of a custom date picker.\n- Use the `week` selection mode to pick an entire week (Mon–Sun, locale dependent).\n- Use the `range` selection mode for booking-style flows where the user picks a start and an end date.\n\n### When not to use\n\n- Use `mdc-datepicker` when you need an input field paired with a popover calendar; `mdc-calendar` is the grid only, not the input/popover wrapper.\n- Use `mdc-timepicker` for time-of-day selection without a calendar surface.\n- Use a plain `mdc-select`, `mdc-input`, or `mdc-combobox` when the user only needs to type or pick a textual value rather than navigate dates.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/calendar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Calendar } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<!-- Single date selection -->\n<mdc-calendar\n  selection-mode=\"single\"\n  value=\"2026-06-15\"\n  min=\"2026-01-01\"\n  max=\"2026-12-31\"\n  locale=\"en-US\"\n  show-today-button\n  locale-today-label=\"Today\"\n  locale-prev-month-label=\"Previous month\"\n  locale-next-month-label=\"Next month\"\n></mdc-calendar>\n\n<!-- Date range selection -->\n<mdc-calendar\n  selection-mode=\"range\"\n  value=\"2026-06-10\"\n  end-value=\"2026-06-14\"\n></mdc-calendar>\n```\n\nListen for `date-selected` to receive the picked value(s) and `month-changed` to react to grid navigation.\n\n### Content guidance\n\n- Always supply localized labels for the navigation and Today button (`locale-today-label`, `locale-prev-month-label`, `locale-next-month-label`) — the component does not provide built-in translations.\n- Set `locale` to a BCP 47 string that matches the user's interface language so the week start day, weekday names, and month/year heading are formatted correctly.\n- When using `range`, communicate the two-step flow to users (first click selects the start, second selects the end). Clicks made before the current start automatically swap the two values.\n\n### Property/Attribute details\n\n- `value` — selected date as an ISO string (`yyyy-mm-dd`). In `range` and `week` modes this represents the start date.\n- `end-value` — end of the selected range/week as an ISO string. Only meaningful in `range` and `week` modes.\n- `selection-mode` — `single` (default), `week`, or `range`. Switching mode resets the internal range-selection phase to \"start\".\n- `locale` — BCP 47 locale string used for week start day, weekday names, month/year heading, and the `aria-label` on each day cell. Defaults to `en-US`.\n- `min` / `max` — inclusive lower/upper bounds for selectable dates as ISO strings. Days outside the range render as disabled and cannot be focused via keyboard navigation; month navigation is clipped to the same window.\n- `show-today-button` — when `true`, renders a \"Today\" button below the grid that jumps focus and selection to the current date. Disabled automatically when today falls outside `min`/`max`. Defaults to `false`.\n- `locale-today-label` — visible label and accessible name for the Today button.\n- `locale-prev-month-label` / `locale-next-month-label` — accessible names for the icon-only previous/next month navigation buttons.\n\n### Limitations\n\n- Translations are not bundled. Without the `locale-*-label` props the navigation and Today button have empty accessible names.\n- Range selection always commits on the second click; there is no built-in \"clear\" affordance other than re-clicking to start a new range.\n- The grid renders a single month at a time — multi-month or year-picker views are not part of this component.\n- Day cells reflect `aria-disabled` only when `min`/`max` exclude them; consumers cannot disable individual dates via attribute today.\n\n## Accessibility\n\n### Built-in features\n\nThe day cells are wrapped in `role=\"grid\"` with weekday headers marked as `role=\"columnheader\"` and each week as `role=\"row\"`. Day cells use `role=\"gridcell\"` and follow the standard date-grid keyboard model:\n\n- `ArrowLeft` / `ArrowRight` move focus by one day.\n- `ArrowUp` / `ArrowDown` move focus by one week.\n- `Home` / `End` jump to the start/end of the focused week (respecting the locale's week start day).\n- `PageUp` / `PageDown` move focus to the same date in the previous/next month; with `Shift` they move by one year.\n- `Enter` / `Space` selects the focused date.\n\nThe component manages a single roving `tabindex=\"0\"` on the currently focused day cell (every other cell is `tabindex=\"-1\"`), so the grid takes a single tab stop. Navigating across a month boundary updates `displayMonth`/`displayYear` automatically and restores focus on the new cell after the re-render. Disabled cells (those outside `min`/`max`) are skipped by keyboard navigation and cannot be selected.\n\nEach cell exposes its full localized date through `aria-label` (e.g. \"Monday, June 15, 2026\"), and the grid's own `aria-label` is updated to `Calendar, <Month Year>` — or for week mode with a selection, `Calendar, <formatted week range>`. The current day is marked with `aria-current=\"date\"`, selected cells (including range members) with `aria-selected=\"true\"`, and out-of-bounds cells with `aria-disabled=\"true\"`. Navigation buttons render as `mdc-button` with `aria-label` taken from the `locale-prev-month-label` / `locale-next-month-label` props and become `disabled` once the grid reaches the configured `min`/`max`.\n\n#### Internal ARIA managed by the component\n\n| Element                           | Attribute       | Value                                                                                                                  |\n| --------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| Grid container                    | `role`          | `grid`                                                                                                                 |\n| Grid container                    | `aria-label`    | `Calendar, <Month Year>` in single/range mode; `Calendar, <formatted range>` when a week selection exists              |\n| Weekday header row                | `role`          | `row`                                                                                                                  |\n| Weekday cell                      | `role`          | `columnheader`                                                                                                         |\n| Week row                          | `role`          | `row`                                                                                                                  |\n| Day cell `<button>`               | `role`          | `gridcell`                                                                                                             |\n| Day cell `<button>`               | `tabindex`      | `0` for the currently focused date; `-1` for every other cell                                                          |\n| Day cell `<button>`               | `aria-label`    | Localized full date (e.g. `Monday, June 15, 2026`)                                                                     |\n| Day cell `<button>`               | `aria-selected` | `true` when the cell is part of the current selection or range; `false` otherwise                                      |\n| Day cell `<button>`               | `aria-disabled` | `true` when the date falls outside `min`/`max`; attribute removed otherwise                                            |\n| Day cell `<button>`               | `aria-current`  | `date` when the cell represents today; attribute removed otherwise                                                     |\n| Previous/next month `mdc-button`  | `aria-label`    | Mirrors `locale-prev-month-label` / `locale-next-month-label`                                                          |\n| Previous/next month `mdc-button`  | `disabled`      | `true` when navigation in that direction would cross `min` / `max`                                                     |\n| Today `mdc-button`                | `aria-label`    | Mirrors `locale-today-label`                                                                                           |\n| Today `mdc-button`                | `disabled`      | `true` when today falls outside `min`/`max`                                                                            |\n\n### Implementation requirements\n\n#### General\n\n- Always provide the localized labels (`locale-today-label`, `locale-prev-month-label`, `locale-next-month-label`) so the icon-only navigation and Today buttons have accessible names.\n- Match `locale` to the surrounding UI language; the value drives weekday order, header text, and day-cell announcements.\n- When using `range`, surface the current selection in the surrounding UI (for example as visible text \"Start date: …, End date: …\") so screen reader users get reinforcement beyond `aria-selected` on individual cells.\n- If the consumer needs to disable arbitrary dates beyond `min`/`max`, that is not currently supported — narrow the range with `min`/`max` instead.\n\n#### Labelling\n\n- The grid is announced as `Calendar, <Month Year>` by default. If the surrounding UI already conveys the calendar's purpose (e.g. \"Departure date\"), wrap the calendar in an element with a more specific `aria-label`/`aria-labelledby` and rely on the grid's own label for the month/year context.\n- Day cells are labelled with the localized full date — do not override these via custom DOM; the localized form is the most accessible representation.",
    "sections": {
      "Overview": "The calendar renders a month-by-month grid that lets users select a single date, an entire week, or a date range. It is keyboard-navigable, respects locale-defined week start and date formatting, and constrains selection to an optional `min`/`max` window.\n\n### When to use\n\n- Use `mdc-calendar` when the surface needs to display a full month grid for date selection — for example a standalone scheduler view or the calendar panel of a custom date picker.\n- Use the `week` selection mode to pick an entire week (Mon–Sun, locale dependent).\n- Use the `range` selection mode for booking-style flows where the user picks a start and an end date.\n\n### When not to use\n\n- Use `mdc-datepicker` when you need an input field paired with a popover calendar; `mdc-calendar` is the grid only, not the input/popover wrapper.\n- Use `mdc-timepicker` for time-of-day selection without a calendar surface.\n- Use a plain `mdc-select`, `mdc-input`, or `mdc-combobox` when the user only needs to type or pick a textual value rather than navigate dates.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/calendar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Calendar } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<!-- Single date selection -->\n<mdc-calendar\n  selection-mode=\"single\"\n  value=\"2026-06-15\"\n  min=\"2026-01-01\"\n  max=\"2026-12-31\"\n  locale=\"en-US\"\n  show-today-button\n  locale-today-label=\"Today\"\n  locale-prev-month-label=\"Previous month\"\n  locale-next-month-label=\"Next month\"\n></mdc-calendar>\n\n<!-- Date range selection -->\n<mdc-calendar\n  selection-mode=\"range\"\n  value=\"2026-06-10\"\n  end-value=\"2026-06-14\"\n></mdc-calendar>\n```\n\nListen for `date-selected` to receive the picked value(s) and `month-changed` to react to grid navigation.\n\n### Content guidance\n\n- Always supply localized labels for the navigation and Today button (`locale-today-label`, `locale-prev-month-label`, `locale-next-month-label`) — the component does not provide built-in translations.\n- Set `locale` to a BCP 47 string that matches the user's interface language so the week start day, weekday names, and month/year heading are formatted correctly.\n- When using `range`, communicate the two-step flow to users (first click selects the start, second selects the end). Clicks made before the current start automatically swap the two values.\n\n### Property/Attribute details\n\n- `value` — selected date as an ISO string (`yyyy-mm-dd`). In `range` and `week` modes this represents the start date.\n- `end-value` — end of the selected range/week as an ISO string. Only meaningful in `range` and `week` modes.\n- `selection-mode` — `single` (default), `week`, or `range`. Switching mode resets the internal range-selection phase to \"start\".\n- `locale` — BCP 47 locale string used for week start day, weekday names, month/year heading, and the `aria-label` on each day cell. Defaults to `en-US`.\n- `min` / `max` — inclusive lower/upper bounds for selectable dates as ISO strings. Days outside the range render as disabled and cannot be focused via keyboard navigation; month navigation is clipped to the same window.\n- `show-today-button` — when `true`, renders a \"Today\" button below the grid that jumps focus and selection to the current date. Disabled automatically when today falls outside `min`/`max`. Defaults to `false`.\n- `locale-today-label` — visible label and accessible name for the Today button.\n- `locale-prev-month-label` / `locale-next-month-label` — accessible names for the icon-only previous/next month navigation buttons.\n\n### Limitations\n\n- Translations are not bundled. Without the `locale-*-label` props the navigation and Today button have empty accessible names.\n- Range selection always commits on the second click; there is no built-in \"clear\" affordance other than re-clicking to start a new range.\n- The grid renders a single month at a time — multi-month or year-picker views are not part of this component.\n- Day cells reflect `aria-disabled` only when `min`/`max` exclude them; consumers cannot disable individual dates via attribute today.",
      "Accessibility": "### Built-in features\n\nThe day cells are wrapped in `role=\"grid\"` with weekday headers marked as `role=\"columnheader\"` and each week as `role=\"row\"`. Day cells use `role=\"gridcell\"` and follow the standard date-grid keyboard model:\n\n- `ArrowLeft` / `ArrowRight` move focus by one day.\n- `ArrowUp` / `ArrowDown` move focus by one week.\n- `Home` / `End` jump to the start/end of the focused week (respecting the locale's week start day).\n- `PageUp` / `PageDown` move focus to the same date in the previous/next month; with `Shift` they move by one year.\n- `Enter` / `Space` selects the focused date.\n\nThe component manages a single roving `tabindex=\"0\"` on the currently focused day cell (every other cell is `tabindex=\"-1\"`), so the grid takes a single tab stop. Navigating across a month boundary updates `displayMonth`/`displayYear` automatically and restores focus on the new cell after the re-render. Disabled cells (those outside `min`/`max`) are skipped by keyboard navigation and cannot be selected.\n\nEach cell exposes its full localized date through `aria-label` (e.g. \"Monday, June 15, 2026\"), and the grid's own `aria-label` is updated to `Calendar, <Month Year>` — or for week mode with a selection, `Calendar, <formatted week range>`. The current day is marked with `aria-current=\"date\"`, selected cells (including range members) with `aria-selected=\"true\"`, and out-of-bounds cells with `aria-disabled=\"true\"`. Navigation buttons render as `mdc-button` with `aria-label` taken from the `locale-prev-month-label` / `locale-next-month-label` props and become `disabled` once the grid reaches the configured `min`/`max`.\n\n#### Internal ARIA managed by the component\n\n| Element                           | Attribute       | Value                                                                                                                  |\n| --------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| Grid container                    | `role`          | `grid`                                                                                                                 |\n| Grid container                    | `aria-label`    | `Calendar, <Month Year>` in single/range mode; `Calendar, <formatted range>` when a week selection exists              |\n| Weekday header row                | `role`          | `row`                                                                                                                  |\n| Weekday cell                      | `role`          | `columnheader`                                                                                                         |\n| Week row                          | `role`          | `row`                                                                                                                  |\n| Day cell `<button>`               | `role`          | `gridcell`                                                                                                             |\n| Day cell `<button>`               | `tabindex`      | `0` for the currently focused date; `-1` for every other cell                                                          |\n| Day cell `<button>`               | `aria-label`    | Localized full date (e.g. `Monday, June 15, 2026`)                                                                     |\n| Day cell `<button>`               | `aria-selected` | `true` when the cell is part of the current selection or range; `false` otherwise                                      |\n| Day cell `<button>`               | `aria-disabled` | `true` when the date falls outside `min`/`max`; attribute removed otherwise                                            |\n| Day cell `<button>`               | `aria-current`  | `date` when the cell represents today; attribute removed otherwise                                                     |\n| Previous/next month `mdc-button`  | `aria-label`    | Mirrors `locale-prev-month-label` / `locale-next-month-label`                                                          |\n| Previous/next month `mdc-button`  | `disabled`      | `true` when navigation in that direction would cross `min` / `max`                                                     |\n| Today `mdc-button`                | `aria-label`    | Mirrors `locale-today-label`                                                                                           |\n| Today `mdc-button`                | `disabled`      | `true` when today falls outside `min`/`max`                                                                            |\n\n### Implementation requirements\n\n#### General\n\n- Always provide the localized labels (`locale-today-label`, `locale-prev-month-label`, `locale-next-month-label`) so the icon-only navigation and Today buttons have accessible names.\n- Match `locale` to the surrounding UI language; the value drives weekday order, header text, and day-cell announcements.\n- When using `range`, surface the current selection in the surrounding UI (for example as visible text \"Start date: …, End date: …\") so screen reader users get reinforcement beyond `aria-selected` on individual cells.\n- If the consumer needs to disable arbitrary dates beyond `min`/`max`, that is not currently supported — narrow the range with `min`/`max` instead.\n\n#### Labelling\n\n- The grid is announced as `Calendar, <Month Year>` by default. If the surrounding UI already conveys the calendar's purpose (e.g. \"Departure date\"), wrap the calendar in an element with a more specific `aria-label`/`aria-labelledby` and rely on the grid's own label for the month/year context.\n- Day cells are labelled with the localized full date — do not override these via custom DOM; the localized form is the most accessible representation."
    },
    "dependencies": [
      "mdc-button"
    ],
    "api": {
      "properties": [
        {
          "name": "displayMonth",
          "attribute": "show-today-button",
          "type": "number",
          "default": "new Date().getMonth() + 1",
          "values": [],
          "description": "when `true`, renders a \"Today\" button below the grid that jumps focus and selection to the current date. Disabled automatically when today falls outside `min`/`max`. Defaults to `false`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "endValue",
          "attribute": "end-value",
          "type": "text",
          "values": [],
          "description": "end of the selected range/week as an ISO string. Only meaningful in `range` and `week` modes.",
          "source": "storybook"
        },
        {
          "name": "locale",
          "attribute": "locale",
          "type": "text",
          "default": "'en-US'",
          "values": [],
          "description": "BCP 47 locale string used for week start day, weekday names, month/year heading, and the `aria-label` on each day cell. Defaults to `en-US`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "localeNextMonthLabel",
          "attribute": "locale-next-month-label",
          "type": "text",
          "values": [],
          "description": "accessible names for the icon-only previous/next month navigation buttons.",
          "source": "storybook"
        },
        {
          "name": "localePrevMonthLabel",
          "attribute": "locale-prev-month-label",
          "type": "text",
          "values": [],
          "description": "accessible names for the icon-only previous/next month navigation buttons.",
          "source": "storybook"
        },
        {
          "name": "localeTodayLabel",
          "attribute": "locale-today-label",
          "type": "text",
          "values": [],
          "description": "visible label and accessible name for the Today button.",
          "source": "storybook"
        },
        {
          "name": "max",
          "attribute": "max",
          "type": "text",
          "values": [],
          "description": "inclusive lower/upper bounds for selectable dates as ISO strings. Days outside the range render as disabled and cannot be focused via keyboard navigation; month navigation is clipped to the same window.",
          "source": "storybook"
        },
        {
          "name": "min",
          "attribute": "min",
          "type": "text",
          "values": [],
          "description": "inclusive lower/upper bounds for selectable dates as ISO strings. Days outside the range render as disabled and cannot be focused via keyboard navigation; month navigation is clipped to the same window.",
          "source": "storybook"
        },
        {
          "name": "selectionMode",
          "attribute": "selection-mode",
          "type": "select",
          "default": "DEFAULTS.SELECTION_MODE",
          "values": [
            "single",
            "week",
            "range"
          ],
          "description": "`single` (default), `week`, or `range`. Switching mode resets the internal range-selection phase to \"start\".",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "showTodayButton",
          "attribute": "show-today-button",
          "type": "boolean",
          "values": [],
          "description": "when `true`, renders a \"Today\" button below the grid that jumps focus and selection to the current date. Disabled automatically when today falls outside `min`/`max`. Defaults to `false`.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "selected date as an ISO string (`yyyy-mm-dd`). In `range` and `week` modes this represents the start date.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "date-selected",
          "description": "Fires when a date or date range is selected."
        },
        {
          "name": "month-changed",
          "description": "Fires when the displayed month changes."
        }
      ],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-calendar-background-color",
          "description": "Background color of the calendar."
        },
        {
          "name": "--mdc-calendar-text-color",
          "description": "Text color of the calendar."
        },
        {
          "name": "--mdc-calendar-day-selected-bg",
          "description": "Background color of selected days."
        },
        {
          "name": "--mdc-calendar-day-selected-text-color",
          "description": "Text color of selected days."
        },
        {
          "name": "--mdc-calendar-day-today-border-color",
          "description": "Border color for today's date."
        },
        {
          "name": "--mdc-calendar-day-hover-bg",
          "description": "Background color on hover."
        },
        {
          "name": "--mdc-calendar-range-bg",
          "description": "Background color for days within a range."
        },
        {
          "name": "--mdc-calendar-day-outside-month-text-color",
          "description": "Text color for days outside the current month."
        },
        {
          "name": "--mdc-calendar-day-disabled-text-color",
          "description": "Text color for disabled days."
        }
      ],
      "rules": [
        "selected date as an ISO string (`yyyy-mm-dd`). In `range` and `week` modes this represents the start date.",
        "end of the selected range/week as an ISO string. Only meaningful in `range` and `week` modes.",
        "`single` (default), `week`, or `range`. Switching mode resets the internal range-selection phase to \"start\".",
        "BCP 47 locale string used for week start day, weekday names, month/year heading, and the `aria-label` on each day cell. Defaults to `en-US`.",
        "inclusive lower/upper bounds for selectable dates as ISO strings. Days outside the range render as disabled and cannot be focused via keyboard navigation; month navigation is clipped to the same window.",
        "inclusive lower/upper bounds for selectable dates as ISO strings. Days outside the range render as disabled and cannot be focused via keyboard navigation; month navigation is clipped to the same window.",
        "when `true`, renders a \"Today\" button below the grid that jumps focus and selection to the current date. Disabled automatically when today falls outside `min`/`max`. Defaults to `false`.",
        "visible label and accessible name for the Today button.",
        "accessible names for the icon-only previous/next month navigation buttons.",
        "accessible names for the icon-only previous/next month navigation buttons."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/calendar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Calendar } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<!-- Single date selection -->\n<mdc-calendar\n  selection-mode=\"single\"\n  value=\"2026-06-15\"\n  min=\"2026-01-01\"\n  max=\"2026-12-31\"\n  locale=\"en-US\"\n  show-today-button\n  locale-today-label=\"Today\"\n  locale-prev-month-label=\"Previous month\"\n  locale-next-month-label=\"Next month\"\n></mdc-calendar>\n\n<!-- Date range selection -->\n<mdc-calendar\n  selection-mode=\"range\"\n  value=\"2026-06-10\"\n  end-value=\"2026-06-14\"\n></mdc-calendar>"
      }
    ],
    "reactImports": [
      "Calendar"
    ],
    "constants": {
      "SELECTION_MODE": {
        "SINGLE": "single",
        "WEEK": "week",
        "RANGE": "range"
      },
      "CALENDAR_ICONS": {
        "CHEVRON_LEFT": "'arrow-left-bold' as Extract<IconNames, 'arrow-left-bold'>",
        "CHEVRON_RIGHT": "'arrow-right-bold' as Extract<IconNames, 'arrow-right-bold'>"
      },
      "DEFAULTS": {
        "SELECTION_MODE": "single",
        "LOCALE": "en-US"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "aware",
      "button",
      "calendar",
      "component",
      "date",
      "for",
      "formatting",
      "grid",
      "guidelines",
      "keyboard",
      "locale",
      "mdc",
      "monthly",
      "navigation",
      "or",
      "overview",
      "range",
      "selecting",
      "single",
      "the",
      "usage",
      "week",
      "with"
    ]
  },
  {
    "name": "card",
    "title": "Card",
    "tagName": "mdc-card",
    "summary": "Usage, guidelines, and accessibility for the mdc-card component — a static container grouping related information into image, header, body, and footer with link and button slots.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/card",
      "knowledgeBasePath": "components/src/components/card/knowledge-base/card.component.md"
    },
    "docs": "## Overview\n\nThe card groups related content — image, title and subtitle, body text, header action icons, and footer link/buttons — into a single visually contained surface. It is a static container: it has no built-in click handler or selection state, and is laid out either vertically (default, narrow) or horizontally (wide).\n\n### When to use\n\n- Use `mdc-card` to present a self-contained piece of content (a record, a tile in a gallery, an information panel) with consistent padding, header, body, and footer regions.\n- Use the `promotional` variant for marketing, upsell, or feature-highlight surfaces; use `ghost` when the surrounding layout already provides the visual container; otherwise keep the default `border` variant.\n\n### When not to use\n\n- Use `mdc-cardbutton` when the entire card should act as a single click target.\n- Use `mdc-cardcheckbox` or `mdc-cardradio` when the card represents a selectable option inside a form (multi-select / single-select).\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone tile.\n- Use `mdc-banner` or `mdc-toast` when the content is a transient or status message rather than a persistent record.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/card/index.js\"; // custom element registration\n// or via React wrapper\nimport { Card } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-card\n  card-title=\"Weekly sync\"\n  subtitle=\"Wednesdays · 10:00 AM\"\n  icon-name=\"calendar-bold\"\n  image-src=\"/img/team.png\"\n  image-alt=\"Photo of the project team\"\n  variant=\"border\"\n  orientation=\"vertical\"\n>\n  <mdc-button slot=\"icon-button\" prefix-icon=\"more-bold\" aria-label=\"More options\"></mdc-button>\n\n  <mdc-text slot=\"body\">Quick alignment on roadmap, blockers, and demos.</mdc-text>\n\n  <mdc-link slot=\"footer-link\" href=\"/events/weekly-sync\">Open event</mdc-link>\n  <mdc-button slot=\"footer-button-secondary\">Decline</mdc-button>\n  <mdc-button slot=\"footer-button-primary\">Join</mdc-button>\n</mdc-card>\n```\n\n### Content guidance\n\n- Keep the title short and scannable; let `subtitle` carry supporting metadata (time, owner, status).\n- Prefer the `title`/`subtitle` slots over `card-title`/`subtitle` attributes when the content needs custom markup (links, icons, badges) inside the header.\n- Limit `icon-button` slot entries to at most three; additional buttons cause the header to feel crowded.\n- Use the `footer-link` slot for navigation cues and `footer-button-primary` / `footer-button-secondary` for actions — only use the generic `footer` slot when the standard layout cannot express what you need.\n\n### Property/Attribute details\n\n- `card-title` — primary header text. Rendered through `mdc-text`; ignored if the `title` slot has content.\n- `subtitle` — secondary header text below the title. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text`. Accepts any tag supported by `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered in the `image` slot region. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost` (no border), `promotional` (promotional styling that also re-themes the footer buttons).\n- `orientation` — layout direction. `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n\nThe header `icon-button` slot accepts up to three `mdc-button` entries. Each slotted button is automatically coerced to `variant=\"tertiary\"` and `size=\"32\"` so they integrate visually with the header.\n\n### Limitations\n\n- `mdc-card` is non-interactive — it has no role, focus, or activation. Use `mdc-cardbutton`, `mdc-cardcheckbox`, or `mdc-cardradio` when the whole card needs to be actionable or selectable.\n- The header `icon-button` slot enforces tertiary 32px buttons; other variants/sizes are overridden.\n- The `footer` slot bypasses the layout for `footer-link`, `footer-button-primary`, and `footer-button-secondary` — mixing them produces an inconsistent footer.\n\n## Accessibility\n\n### Built-in features\n\nThe card renders as a generic container — no role, label, or focus management is applied to the host. Interactive children (header `icon-button`, footer link, footer buttons) keep their own focus, keyboard behaviour, and ARIA contract. The card's own title is rendered through `mdc-text` with a consumer-chosen tag, so you can promote it to a real heading (`h2`/`h3`/…) when the surrounding page needs heading-level navigation.\n\n#### Internal ARIA managed by the component\n\n| Element                       | Attribute | Value                                                        |\n| ----------------------------- | --------- | ------------------------------------------------------------ |\n| Host                          | `role`    | None set by the component                                    |\n| Rendered `<img>` (image slot) | `alt`     | Mirrors the `image-alt` attribute set by the consumer        |\n\n### Implementation requirements\n\n#### General\n\n- The card itself is not focusable. If the whole tile must be activatable, use `mdc-cardbutton`/`mdc-cardcheckbox`/`mdc-cardradio` instead of wiring click handlers onto `mdc-card`.\n- Each interactive child must be independently labelled. Icon-only buttons in the `icon-button`, `footer-button-primary`, or `footer-button-secondary` slots require an `aria-label` describing the action.\n- When the card sits inside a list of similar tiles, make the surrounding container an actual list (`<ul>`/`<li>` or `role=\"list\"`/`role=\"listitem\"`) so assistive technologies announce the count and position.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is what users skim when navigating between cards.\n- Always set `image-alt` for informative images. For purely decorative imagery, set `image-alt=\"\"` so screen readers skip the image.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline (e.g. `h3` if the cards live in an `h2` section). Leaving the default `span` is fine when the card's title is not a section heading in the page outline.",
    "sections": {
      "Overview": "The card groups related content — image, title and subtitle, body text, header action icons, and footer link/buttons — into a single visually contained surface. It is a static container: it has no built-in click handler or selection state, and is laid out either vertically (default, narrow) or horizontally (wide).\n\n### When to use\n\n- Use `mdc-card` to present a self-contained piece of content (a record, a tile in a gallery, an information panel) with consistent padding, header, body, and footer regions.\n- Use the `promotional` variant for marketing, upsell, or feature-highlight surfaces; use `ghost` when the surrounding layout already provides the visual container; otherwise keep the default `border` variant.\n\n### When not to use\n\n- Use `mdc-cardbutton` when the entire card should act as a single click target.\n- Use `mdc-cardcheckbox` or `mdc-cardradio` when the card represents a selectable option inside a form (multi-select / single-select).\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone tile.\n- Use `mdc-banner` or `mdc-toast` when the content is a transient or status message rather than a persistent record.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/card/index.js\"; // custom element registration\n// or via React wrapper\nimport { Card } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-card\n  card-title=\"Weekly sync\"\n  subtitle=\"Wednesdays · 10:00 AM\"\n  icon-name=\"calendar-bold\"\n  image-src=\"/img/team.png\"\n  image-alt=\"Photo of the project team\"\n  variant=\"border\"\n  orientation=\"vertical\"\n>\n  <mdc-button slot=\"icon-button\" prefix-icon=\"more-bold\" aria-label=\"More options\"></mdc-button>\n\n  <mdc-text slot=\"body\">Quick alignment on roadmap, blockers, and demos.</mdc-text>\n\n  <mdc-link slot=\"footer-link\" href=\"/events/weekly-sync\">Open event</mdc-link>\n  <mdc-button slot=\"footer-button-secondary\">Decline</mdc-button>\n  <mdc-button slot=\"footer-button-primary\">Join</mdc-button>\n</mdc-card>\n```\n\n### Content guidance\n\n- Keep the title short and scannable; let `subtitle` carry supporting metadata (time, owner, status).\n- Prefer the `title`/`subtitle` slots over `card-title`/`subtitle` attributes when the content needs custom markup (links, icons, badges) inside the header.\n- Limit `icon-button` slot entries to at most three; additional buttons cause the header to feel crowded.\n- Use the `footer-link` slot for navigation cues and `footer-button-primary` / `footer-button-secondary` for actions — only use the generic `footer` slot when the standard layout cannot express what you need.\n\n### Property/Attribute details\n\n- `card-title` — primary header text. Rendered through `mdc-text`; ignored if the `title` slot has content.\n- `subtitle` — secondary header text below the title. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text`. Accepts any tag supported by `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered in the `image` slot region. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost` (no border), `promotional` (promotional styling that also re-themes the footer buttons).\n- `orientation` — layout direction. `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n\nThe header `icon-button` slot accepts up to three `mdc-button` entries. Each slotted button is automatically coerced to `variant=\"tertiary\"` and `size=\"32\"` so they integrate visually with the header.\n\n### Limitations\n\n- `mdc-card` is non-interactive — it has no role, focus, or activation. Use `mdc-cardbutton`, `mdc-cardcheckbox`, or `mdc-cardradio` when the whole card needs to be actionable or selectable.\n- The header `icon-button` slot enforces tertiary 32px buttons; other variants/sizes are overridden.\n- The `footer` slot bypasses the layout for `footer-link`, `footer-button-primary`, and `footer-button-secondary` — mixing them produces an inconsistent footer.",
      "Accessibility": "### Built-in features\n\nThe card renders as a generic container — no role, label, or focus management is applied to the host. Interactive children (header `icon-button`, footer link, footer buttons) keep their own focus, keyboard behaviour, and ARIA contract. The card's own title is rendered through `mdc-text` with a consumer-chosen tag, so you can promote it to a real heading (`h2`/`h3`/…) when the surrounding page needs heading-level navigation.\n\n#### Internal ARIA managed by the component\n\n| Element                       | Attribute | Value                                                        |\n| ----------------------------- | --------- | ------------------------------------------------------------ |\n| Host                          | `role`    | None set by the component                                    |\n| Rendered `<img>` (image slot) | `alt`     | Mirrors the `image-alt` attribute set by the consumer        |\n\n### Implementation requirements\n\n#### General\n\n- The card itself is not focusable. If the whole tile must be activatable, use `mdc-cardbutton`/`mdc-cardcheckbox`/`mdc-cardradio` instead of wiring click handlers onto `mdc-card`.\n- Each interactive child must be independently labelled. Icon-only buttons in the `icon-button`, `footer-button-primary`, or `footer-button-secondary` slots require an `aria-label` describing the action.\n- When the card sits inside a list of similar tiles, make the surrounding container an actual list (`<ul>`/`<li>` or `role=\"list\"`/`role=\"listitem\"`) so assistive technologies announce the count and position.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is what users skim when navigating between cards.\n- Always set `image-alt` for informative images. For purely decorative imagery, set `image-alt=\"\"` so screen readers skip the image.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline (e.g. `h3` if the cards live in an `h2` section). Leaving the default `span` is fine when the card's title is not a section heading in the page outline."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "cardTitle",
          "attribute": "card-title",
          "type": "text",
          "values": [],
          "description": "primary header text. Rendered through `mdc-text`; ignored if the `title` slot has content.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "name of the leading icon shown in the header. When empty, no icon is rendered.",
          "source": "storybook"
        },
        {
          "name": "imageAlt",
          "attribute": "image-alt",
          "type": "text",
          "values": [],
          "description": "alt text for the image. Required for non-decorative images.",
          "source": "storybook"
        },
        {
          "name": "imageSrc",
          "attribute": "image-src",
          "type": "text",
          "values": [],
          "description": "URL of the image rendered in the `image` slot region. When empty, no image is rendered.",
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "select",
          "values": [
            "horizontal",
            "vertical"
          ],
          "description": "layout direction. `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).",
          "source": "storybook"
        },
        {
          "name": "subtitle",
          "attribute": "subtitle",
          "type": "text",
          "values": [],
          "description": "secondary header text below the title. Ignored if the `subtitle` slot has content.",
          "source": "storybook"
        },
        {
          "name": "subtitleTagName",
          "attribute": "subtitle-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text`. Accepts any tag supported by `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "titleTagName",
          "attribute": "title-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text`. Accepts any tag supported by `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [
            "border",
            "ghost",
            "promotional"
          ],
          "description": "border treatment. `border` (default), `ghost` (no border), `promotional` (promotional styling that also re-themes the footer buttons).",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "image",
          "description": "This slot is for overriding the image content of the card"
        },
        {
          "name": "before-body",
          "description": "This slot is for passing the content before the body"
        },
        {
          "name": "body",
          "description": "This slot is for passing the text content for the card"
        },
        {
          "name": "after-body",
          "description": "This slot is for passing the content after the body"
        },
        {
          "name": "title",
          "description": "This slot is for passing the title of the card in the header section"
        },
        {
          "name": "subtitle",
          "description": "This slot is for passing the subtitle of the card in the header section"
        },
        {
          "name": "icon-button",
          "description": "This slot supports action icon buttons in the header section (maximum of 3 buttons)"
        },
        {
          "name": "footer-link",
          "description": "This slot is for passing `mdc-link` component within the footer section."
        },
        {
          "name": "footer-button-primary",
          "description": "This slot is for passing primary variant of"
        },
        {
          "name": "footer-button-secondary",
          "description": "This slot is for passing secondary variant of `mdc-button` component"
        },
        {
          "name": "footer",
          "description": "This slot is for passing custom footer content. Only use this if really needed,"
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "header",
          "description": "The header part of the card"
        },
        {
          "name": "icon",
          "description": "The icon part of the card header"
        },
        {
          "name": "body",
          "description": "The body part of the card"
        },
        {
          "name": "image",
          "description": "The image part of the card"
        },
        {
          "name": "footer",
          "description": "The footer part of the card"
        },
        {
          "name": "footer-link",
          "description": "The link part of the card footer"
        },
        {
          "name": "footer-button-primary",
          "description": "The primary button part of the card footer"
        },
        {
          "name": "footer-button-secondary",
          "description": "The secondary button part of the card footer"
        },
        {
          "name": "icon-button",
          "description": "The icon button part of the card header"
        },
        {
          "name": "text",
          "description": "The text part of the card"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-card-width",
          "description": "The width of the card"
        }
      ],
      "rules": [
        "primary header text. Rendered through `mdc-text`; ignored if the `title` slot has content.",
        "secondary header text below the title. Ignored if the `subtitle` slot has content.",
        "DOM tag used for the rendered title/subtitle `mdc-text`. Accepts any tag supported by `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "DOM tag used for the rendered title/subtitle `mdc-text`. Accepts any tag supported by `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "URL of the image rendered in the `image` slot region. When empty, no image is rendered.",
        "alt text for the image. Required for non-decorative images.",
        "name of the leading icon shown in the header. When empty, no icon is rendered.",
        "border treatment. `border` (default), `ghost` (no border), `promotional` (promotional styling that also re-themes the footer buttons).",
        "layout direction. `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/card/index.js\"; // custom element registration\n// or via React wrapper\nimport { Card } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-card\n  card-title=\"Weekly sync\"\n  subtitle=\"Wednesdays · 10:00 AM\"\n  icon-name=\"calendar-bold\"\n  image-src=\"/img/team.png\"\n  image-alt=\"Photo of the project team\"\n  variant=\"border\"\n  orientation=\"vertical\"\n>\n  <mdc-button slot=\"icon-button\" prefix-icon=\"more-bold\" aria-label=\"More options\"></mdc-button>\n\n  <mdc-text slot=\"body\">Quick alignment on roadmap, blockers, and demos.</mdc-text>\n\n  <mdc-link slot=\"footer-link\" href=\"/events/weekly-sync\">Open event</mdc-link>\n  <mdc-button slot=\"footer-button-secondary\">Decline</mdc-button>\n  <mdc-button slot=\"footer-button-primary\">Join</mdc-button>\n</mdc-card>"
      }
    ],
    "reactImports": [
      "Card"
    ],
    "constants": {
      "VARIANTS": {
        "BORDER": "border",
        "GHOST": "ghost",
        "PROMOTIONAL": "promotional"
      },
      "ORIENTATIONS": {
        "HORIZONTAL": "horizontal",
        "VERTICAL": "vertical"
      },
      "DEFAULTS": {
        "VARIANT": "border",
        "ORIENTATION": "vertical",
        "ICON_SIZE": 1.5,
        "ICON_LENGTH_UNIT": "rem",
        "LINK": "LINK_TAG",
        "BUTTON": "BUTTON_TAG"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "body",
      "button",
      "card",
      "component",
      "container",
      "footer",
      "for",
      "grouping",
      "guidelines",
      "header",
      "icon",
      "image",
      "information",
      "into",
      "link",
      "mdc",
      "overview",
      "related",
      "slots",
      "static",
      "text",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "cardbutton",
    "title": "Card button",
    "tagName": "mdc-cardbutton",
    "summary": "Usage, guidelines, and accessibility for the mdc-cardbutton component — a card-shaped surface that behaves as a single button, so clicking or activating anywhere on the card fires the click event.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/cardbutton",
      "knowledgeBasePath": "components/src/components/cardbutton/knowledge-base/cardbutton.component.md"
    },
    "docs": "## Overview\n\nThe card button looks like a card (image, header with icon/title/subtitle, body) but acts as one large button. Clicking, pressing `Enter`, or pressing `Space` anywhere on the card surface dispatches a `click` event. Use it when the entire tile should be a single activation target.\n\n### When to use\n\n- Use `mdc-cardbutton` when the whole tile is a clickable target — a navigation tile, a \"create new\" surface, or an action card in a grid where the user expects to click anywhere on the card.\n- Use the `name` and `value` attributes when the card button needs to participate in a form submission.\n\n### When not to use\n\n- Use `mdc-card` when the surface is a static container and only specific children are interactive.\n- Use `mdc-cardcheckbox` when the card represents a multi-select option inside a form.\n- Use `mdc-cardradio` when the card represents a single-select option inside a group.\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone tile.\n- Use `mdc-button` when the action does not warrant a card-sized surface (no image, no header, no body content).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/cardbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-cardbutton\n  card-title=\"Create new project\"\n  subtitle=\"Start from a blank template\"\n  icon-name=\"plus-bold\"\n  variant=\"border\"\n  orientation=\"vertical\"\n  name=\"action\"\n  value=\"create-project\"\n>\n  <mdc-text slot=\"body\">Spin up a fresh project with the default settings.</mdc-text>\n</mdc-cardbutton>\n```\n\n### Content guidance\n\n- Keep slot content non-interactive. Nested buttons, links, or form controls inside the card body create overlapping click targets and break assistive technology behaviour.\n- Use the `card-title` and body to describe the single action the card performs — the entire surface activates one thing.\n\n### Property/Attribute details\n\n- `card-title` — primary header text rendered through `mdc-text`. Ignored if the `title` slot has content.\n- `subtitle` — secondary header text. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered in the `image` slot region. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost`, or `promotional`.\n- `orientation` — `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n- `type` — inherited from `mdc-buttonsimple`; `button` (default), `submit`, or `reset` when used inside a form.\n- `name` / `value` — form name and value submitted when the card button is inside a form. Required for form participation.\n- `disabled` — when `true`, the card is not interactive and is removed from the tab order.\n- `soft-disabled` — when `true`, the card looks disabled and announces as disabled to assistive tech but stays focusable so screen-reader users can still discover it.\n\n### Limitations\n\n- The `active` and `size` properties from `mdc-buttonsimple` are reset on connect; the card button is not a toggle button and does not respond to button sizing.\n- Slot content must remain non-interactive. The card itself is the click target, so nested buttons/links/inputs create nested interactives and are not supported — use `mdc-card` when you need actionable children inside the surface.\n\n## Accessibility\n\n### Built-in features\n\nThe host renders with `role=\"button\"` and is focusable via the keyboard (single tab stop). It follows the standard button keyboard contract: `Enter` activates on `keydown`, `Space` activates on `keyup`, and both dispatch the `click` event. Focus is managed by `mdc-buttonsimple`, including the visible focus ring.\n\nWhen `disabled` is `true`, the card is removed from the tab order and pointer/keyboard activation is suppressed. When `soft-disabled` is `true`, the card stays focusable but is announced as disabled and does not activate — use this when you need screen-reader users to discover the control and learn why it is unavailable.\n\nThe card title rendered through `mdc-text` provides the accessible name of the button by default. When the title alone is not descriptive enough (for example when the image carries meaning), provide an explicit `aria-label` on the host so the announcement matches the intent of the surface.\n\nWhen the card participates in a form, `mdc-buttonsimple` registers it as a form-associated element via `ElementInternals`, so the `name`/`value` are submitted like a native `<button>`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                                                 |\n| ------- | --------------- | ------------------------------------------------------------------------------------- |\n| Host    | `role`          | `button`                                                                              |\n| Host    | `tabindex`      | `0` when enabled and not soft-disabled; removed/`-1` otherwise                        |\n| Host    | `aria-disabled` | `true` when `disabled` or `soft-disabled` is set; attribute removed otherwise         |\n\n### Implementation requirements\n\n#### General\n\n- Make sure every slotted element is presentational. If you need actionable children alongside card content, switch to `mdc-card` (multiple actions) or keep the card button and move the secondary action out of the card.\n- Use `disabled` to prevent activation when the action is genuinely unavailable; use `soft-disabled` only when the user still needs to be able to discover the control with the keyboard or screen reader.\n- When the card sits in a list of similar tiles, wrap the collection in a real list (`<ul>`/`<li>` or `role=\"list\"`/`role=\"listitem\"`) so assistive technologies announce the count and position.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is the default accessible name of the button.\n- When the title is not enough on its own (e.g. icon-driven cards or repeating titles disambiguated by context), set `aria-label` (or `aria-labelledby`) on the host so the announcement describes the action precisely.\n- Set `image-alt` for informative images and `image-alt=\"\"` for decorative ones so screen readers do not double-announce the visual.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline only when the title functions as a heading; otherwise leave the default `span`.",
    "sections": {
      "Overview": "The card button looks like a card (image, header with icon/title/subtitle, body) but acts as one large button. Clicking, pressing `Enter`, or pressing `Space` anywhere on the card surface dispatches a `click` event. Use it when the entire tile should be a single activation target.\n\n### When to use\n\n- Use `mdc-cardbutton` when the whole tile is a clickable target — a navigation tile, a \"create new\" surface, or an action card in a grid where the user expects to click anywhere on the card.\n- Use the `name` and `value` attributes when the card button needs to participate in a form submission.\n\n### When not to use\n\n- Use `mdc-card` when the surface is a static container and only specific children are interactive.\n- Use `mdc-cardcheckbox` when the card represents a multi-select option inside a form.\n- Use `mdc-cardradio` when the card represents a single-select option inside a group.\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone tile.\n- Use `mdc-button` when the action does not warrant a card-sized surface (no image, no header, no body content).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/cardbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-cardbutton\n  card-title=\"Create new project\"\n  subtitle=\"Start from a blank template\"\n  icon-name=\"plus-bold\"\n  variant=\"border\"\n  orientation=\"vertical\"\n  name=\"action\"\n  value=\"create-project\"\n>\n  <mdc-text slot=\"body\">Spin up a fresh project with the default settings.</mdc-text>\n</mdc-cardbutton>\n```\n\n### Content guidance\n\n- Keep slot content non-interactive. Nested buttons, links, or form controls inside the card body create overlapping click targets and break assistive technology behaviour.\n- Use the `card-title` and body to describe the single action the card performs — the entire surface activates one thing.\n\n### Property/Attribute details\n\n- `card-title` — primary header text rendered through `mdc-text`. Ignored if the `title` slot has content.\n- `subtitle` — secondary header text. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered in the `image` slot region. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost`, or `promotional`.\n- `orientation` — `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n- `type` — inherited from `mdc-buttonsimple`; `button` (default), `submit`, or `reset` when used inside a form.\n- `name` / `value` — form name and value submitted when the card button is inside a form. Required for form participation.\n- `disabled` — when `true`, the card is not interactive and is removed from the tab order.\n- `soft-disabled` — when `true`, the card looks disabled and announces as disabled to assistive tech but stays focusable so screen-reader users can still discover it.\n\n### Limitations\n\n- The `active` and `size` properties from `mdc-buttonsimple` are reset on connect; the card button is not a toggle button and does not respond to button sizing.\n- Slot content must remain non-interactive. The card itself is the click target, so nested buttons/links/inputs create nested interactives and are not supported — use `mdc-card` when you need actionable children inside the surface.",
      "Accessibility": "### Built-in features\n\nThe host renders with `role=\"button\"` and is focusable via the keyboard (single tab stop). It follows the standard button keyboard contract: `Enter` activates on `keydown`, `Space` activates on `keyup`, and both dispatch the `click` event. Focus is managed by `mdc-buttonsimple`, including the visible focus ring.\n\nWhen `disabled` is `true`, the card is removed from the tab order and pointer/keyboard activation is suppressed. When `soft-disabled` is `true`, the card stays focusable but is announced as disabled and does not activate — use this when you need screen-reader users to discover the control and learn why it is unavailable.\n\nThe card title rendered through `mdc-text` provides the accessible name of the button by default. When the title alone is not descriptive enough (for example when the image carries meaning), provide an explicit `aria-label` on the host so the announcement matches the intent of the surface.\n\nWhen the card participates in a form, `mdc-buttonsimple` registers it as a form-associated element via `ElementInternals`, so the `name`/`value` are submitted like a native `<button>`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                                                 |\n| ------- | --------------- | ------------------------------------------------------------------------------------- |\n| Host    | `role`          | `button`                                                                              |\n| Host    | `tabindex`      | `0` when enabled and not soft-disabled; removed/`-1` otherwise                        |\n| Host    | `aria-disabled` | `true` when `disabled` or `soft-disabled` is set; attribute removed otherwise         |\n\n### Implementation requirements\n\n#### General\n\n- Make sure every slotted element is presentational. If you need actionable children alongside card content, switch to `mdc-card` (multiple actions) or keep the card button and move the secondary action out of the card.\n- Use `disabled` to prevent activation when the action is genuinely unavailable; use `soft-disabled` only when the user still needs to be able to discover the control with the keyboard or screen reader.\n- When the card sits in a list of similar tiles, wrap the collection in a real list (`<ul>`/`<li>` or `role=\"list\"`/`role=\"listitem\"`) so assistive technologies announce the count and position.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is the default accessible name of the button.\n- When the title is not enough on its own (e.g. icon-driven cards or repeating titles disambiguated by context), set `aria-label` (or `aria-labelledby`) on the host so the announcement describes the action precisely.\n- Set `image-alt` for informative images and `image-alt=\"\"` for decorative ones so screen readers do not double-announce the visual.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline only when the title functions as a heading; otherwise leave the default `span`."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaStateKey",
          "attribute": "aria-state-key",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "cardTitle",
          "attribute": "card-title",
          "type": "text",
          "values": [],
          "description": "primary header text rendered through `mdc-text`. Ignored if the `title` slot has content.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the card is not interactive and is removed from the tab order.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "name of the leading icon shown in the header. When empty, no icon is rendered.",
          "source": "storybook"
        },
        {
          "name": "imageAlt",
          "attribute": "image-alt",
          "type": "text",
          "values": [],
          "description": "alt text for the image. Required for non-decorative images.",
          "source": "storybook"
        },
        {
          "name": "imageSrc",
          "attribute": "image-src",
          "type": "text",
          "values": [],
          "description": "URL of the image rendered in the `image` slot region. When empty, no image is rendered.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "form name and value submitted when the card button is inside a form. Required for form participation.",
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "select",
          "values": [],
          "description": "`vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).",
          "source": "storybook"
        },
        {
          "name": "role",
          "attribute": "role",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the card looks disabled and announces as disabled to assistive tech but stays focusable so screen-reader users can still discover it.",
          "source": "storybook"
        },
        {
          "name": "subtitle",
          "attribute": "subtitle",
          "type": "text",
          "values": [],
          "description": "secondary header text. Ignored if the `subtitle` slot has content.",
          "source": "storybook"
        },
        {
          "name": "subtitleTagName",
          "attribute": "subtitle-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "tabIndex",
          "attribute": "tab-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "titleTagName",
          "attribute": "title-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "select",
          "values": [],
          "description": "inherited from `mdc-buttonsimple`; `button` (default), `submit`, or `reset` when used inside a form.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "form name and value submitted when the card button is inside a form. Required for form participation.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [],
          "description": "border treatment. `border` (default), `ghost`, or `promotional`.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "image",
          "description": "This slot is for overriding the image content of the card"
        },
        {
          "name": "before-body",
          "description": "This slot is for passing the content before the body"
        },
        {
          "name": "body",
          "description": "This slot is for passing the text content for the card"
        },
        {
          "name": "after-body",
          "description": "This slot is for passing the content after the body"
        },
        {
          "name": "title",
          "description": "This slot is for passing the title of the card in the header section"
        },
        {
          "name": "subtitle",
          "description": "This slot is for passing the subtitle of the card in the header section"
        },
        {
          "name": "footer-link",
          "description": "This slot is for passing `mdc-link` component within the footer section."
        },
        {
          "name": "footer-button-primary",
          "description": "This slot is for passing primary variant of `mdc-button` component within the footer section."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Event that gets dispatched when the card is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the card."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the card."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Event that gets dispatched when the card receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "header",
          "description": "The header part of the card"
        },
        {
          "name": "icon",
          "description": "The icon part of the card header"
        },
        {
          "name": "body",
          "description": "The body part of the card"
        },
        {
          "name": "image",
          "description": "The image part of the card"
        },
        {
          "name": "footer",
          "description": "The footer part of the card"
        },
        {
          "name": "footer-link",
          "description": "The link part of the card footer"
        },
        {
          "name": "footer-button-primary",
          "description": "The primary button part of the card footer"
        },
        {
          "name": "footer-button-secondary",
          "description": "The secondary button part of the card footer"
        },
        {
          "name": "icon-button",
          "description": "The icon button part of the card header"
        },
        {
          "name": "text",
          "description": "The text part of the card"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-card-width",
          "description": "The width of the card"
        }
      ],
      "rules": [
        "primary header text rendered through `mdc-text`. Ignored if the `title` slot has content.",
        "secondary header text. Ignored if the `subtitle` slot has content.",
        "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "URL of the image rendered in the `image` slot region. When empty, no image is rendered.",
        "alt text for the image. Required for non-decorative images.",
        "name of the leading icon shown in the header. When empty, no icon is rendered.",
        "border treatment. `border` (default), `ghost`, or `promotional`.",
        "`vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).",
        "inherited from `mdc-buttonsimple`; `button` (default), `submit`, or `reset` when used inside a form.",
        "form name and value submitted when the card button is inside a form. Required for form participation.",
        "form name and value submitted when the card button is inside a form. Required for form participation.",
        "when `true`, the card is not interactive and is removed from the tab order.",
        "when `true`, the card looks disabled and announces as disabled to assistive tech but stays focusable so screen-reader users can still discover it."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/cardbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardButton } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-cardbutton\n  card-title=\"Create new project\"\n  subtitle=\"Start from a blank template\"\n  icon-name=\"plus-bold\"\n  variant=\"border\"\n  orientation=\"vertical\"\n  name=\"action\"\n  value=\"create-project\"\n>\n  <mdc-text slot=\"body\">Spin up a fresh project with the default settings.</mdc-text>\n</mdc-cardbutton>"
      }
    ],
    "reactImports": [
      "CardButton"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "activating",
      "and",
      "anywhere",
      "as",
      "behaves",
      "button",
      "card",
      "cardbutton",
      "click",
      "clicking",
      "component",
      "event",
      "fires",
      "for",
      "guidelines",
      "icon",
      "mdc",
      "on",
      "or",
      "overview",
      "shaped",
      "single",
      "so",
      "surface",
      "text",
      "that",
      "the",
      "usage"
    ]
  },
  {
    "name": "cardcheckbox",
    "title": "Card checkbox",
    "tagName": "mdc-cardcheckbox",
    "summary": "Usage, guidelines, and accessibility for the mdc-cardcheckbox component — a card surface that behaves as a single checkbox, toggling on click or Enter/Space.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/cardcheckbox",
      "knowledgeBasePath": "components/src/components/cardcheckbox/knowledge-base/cardcheckbox.component.md"
    },
    "docs": "## Overview\n\nThe card checkbox looks like a card (image, header with icon/title/subtitle, body) but behaves as a single checkbox. Clicking anywhere on the card, pressing `Enter`, or pressing `Space` toggles the `checked` state and dispatches a `change` event. Multiple card checkboxes in a group can be checked at the same time.\n\n### When to use\n\n- Use `mdc-cardcheckbox` when each option in a multi-select group needs the visual weight of a card (image, title, supporting copy) rather than a small inline control.\n- Use it inside a labelled group container (`role=\"group\"` + `aria-label` / `aria-labelledby`) when several card checkboxes belong to the same selection set.\n\n### When not to use\n\n- Use `mdc-card` when the surface is a static container and should not toggle a selection state.\n- Use `mdc-cardbutton` when activating the card should trigger an action rather than toggle a selection.\n- Use `mdc-cardradio` when only one card in the group can be selected at a time.\n- Use `mdc-checkbox` (or `mdc-staticcheckbox`) when a plain control is enough and the card surface is unnecessary.\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone selectable tile.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/cardcheckbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardCheckbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<div role=\"group\" aria-label=\"Notification channels\">\n  <mdc-cardcheckbox\n    card-title=\"Email\"\n    subtitle=\"Daily digest at 8:00 AM\"\n    icon-name=\"email-bold\"\n    selection-type=\"checkbox\"\n    checked\n  >\n    <mdc-text slot=\"body\">Receive a summary of activity in your inbox.</mdc-text>\n  </mdc-cardcheckbox>\n\n  <mdc-cardcheckbox\n    card-title=\"Push\"\n    subtitle=\"On all signed-in devices\"\n    icon-name=\"bell-bold\"\n    selection-type=\"checkbox\"\n  ></mdc-cardcheckbox>\n</div>\n```\n\nListen for the `change` event to react to toggles; the new state is available on `event.target.checked`.\n\n### Content guidance\n\n- Always provide a `card-title` — it is the accessible name of the checkbox.\n- Use the body to describe the consequence of selecting the option, not to repeat the title.\n- When grouping multiple card checkboxes, wrap them in a container with `role=\"group\"` and a label so assistive technologies announce the set.\n\n### Property/Attribute details\n\n- `checked` — selection state. Defaults to `false`. Reflected to the host as `aria-checked`.\n- `selection-type` — visual indicator. `check` (default) renders an `mdc-icon` that appears/changes when selected; `checkbox` renders an `mdc-staticcheckbox` that is always visible and reflects both states.\n- `disabled` — when `true`, the card cannot be toggled and is removed from the tab order. Also reflected as `aria-disabled=\"true\"`.\n- `card-title` — primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.\n- `subtitle` — secondary header text. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered above the header. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost`, or `promotional`.\n- `orientation` — `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n\n### Limitations\n\n- Slot content must remain non-interactive. The card itself is the toggle target, so nested buttons, links, or form controls create overlapping interactive regions and break keyboard/assistive-technology behaviour — use `mdc-card` if the surface needs actionable children.\n- The card checkbox is not form-associated. To submit its state in a native form, mirror `checked` into a `<input type=\"hidden\">` (or use `mdc-checkbox` directly) and update it from the `change` event.\n- `card-title` is required for an accessible name; there is no fallback if both `card-title` and the `title` slot are empty.\n\n## Accessibility\n\n### Built-in features\n\nThe host renders with `role=\"checkbox\"` and is focusable (single tab stop per card; `tabindex` is set to `0` when enabled, `-1` when disabled). The component manages `aria-checked` to mirror the `checked` property and `aria-disabled` to mirror `disabled`.\n\nKeyboard interaction follows the checkbox pattern:\n\n- `Enter` toggles the checked state on `keydown` and dispatches a `change` event.\n- `Space` toggles the checked state on `keyup` and dispatches a `change` event. `Space` is suppressed on `keydown` so the page does not scroll.\n- Click toggles the checked state and dispatches a `change` event.\n\nWhen `disabled` is `true`, click/keyboard activation is suppressed, the host is removed from the tab order, and `aria-disabled=\"true\"` is set.\n\nThe `card-title` (or `title` slot) provides the accessible name. The visual selection indicator (`mdc-icon` for `selection-type=\"check\"`, `mdc-staticcheckbox` for `selection-type=\"checkbox\"`) is decorative — state is conveyed via `aria-checked` on the host.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                                       |\n| ------- | --------------- | --------------------------------------------------------------------------- |\n| Host    | `role`          | `checkbox`                                                                  |\n| Host    | `tabindex`      | `0` when enabled; `-1` when `disabled`                                      |\n| Host    | `aria-checked`  | `true` / `false`, mirrors the `checked` property                            |\n| Host    | `aria-disabled` | `true` / `false`, mirrors the `disabled` property                           |\n\n### Implementation requirements\n\n#### General\n\n- Wrap related card checkboxes in a container with `role=\"group\"` and a label (`aria-label` or `aria-labelledby`) so assistive technologies announce the option set.\n- Keep every slotted child presentational. If you need actionable content alongside the option, switch to `mdc-card` and use `mdc-checkbox` for the selection.\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be selectable, so the state is exposed to assistive technology.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is the default accessible name of the checkbox.\n- When the title alone is ambiguous (e.g. icon-driven cards, repeated titles disambiguated by context), set `aria-label` (or `aria-labelledby`) on the host so the announcement matches the option.\n- Set `image-alt` for informative images and `image-alt=\"\"` for decorative ones to avoid double-announcing the visual.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline only when the title functions as a heading; otherwise leave the default `span`.",
    "sections": {
      "Overview": "The card checkbox looks like a card (image, header with icon/title/subtitle, body) but behaves as a single checkbox. Clicking anywhere on the card, pressing `Enter`, or pressing `Space` toggles the `checked` state and dispatches a `change` event. Multiple card checkboxes in a group can be checked at the same time.\n\n### When to use\n\n- Use `mdc-cardcheckbox` when each option in a multi-select group needs the visual weight of a card (image, title, supporting copy) rather than a small inline control.\n- Use it inside a labelled group container (`role=\"group\"` + `aria-label` / `aria-labelledby`) when several card checkboxes belong to the same selection set.\n\n### When not to use\n\n- Use `mdc-card` when the surface is a static container and should not toggle a selection state.\n- Use `mdc-cardbutton` when activating the card should trigger an action rather than toggle a selection.\n- Use `mdc-cardradio` when only one card in the group can be selected at a time.\n- Use `mdc-checkbox` (or `mdc-staticcheckbox`) when a plain control is enough and the card surface is unnecessary.\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone selectable tile.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/cardcheckbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardCheckbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<div role=\"group\" aria-label=\"Notification channels\">\n  <mdc-cardcheckbox\n    card-title=\"Email\"\n    subtitle=\"Daily digest at 8:00 AM\"\n    icon-name=\"email-bold\"\n    selection-type=\"checkbox\"\n    checked\n  >\n    <mdc-text slot=\"body\">Receive a summary of activity in your inbox.</mdc-text>\n  </mdc-cardcheckbox>\n\n  <mdc-cardcheckbox\n    card-title=\"Push\"\n    subtitle=\"On all signed-in devices\"\n    icon-name=\"bell-bold\"\n    selection-type=\"checkbox\"\n  ></mdc-cardcheckbox>\n</div>\n```\n\nListen for the `change` event to react to toggles; the new state is available on `event.target.checked`.\n\n### Content guidance\n\n- Always provide a `card-title` — it is the accessible name of the checkbox.\n- Use the body to describe the consequence of selecting the option, not to repeat the title.\n- When grouping multiple card checkboxes, wrap them in a container with `role=\"group\"` and a label so assistive technologies announce the set.\n\n### Property/Attribute details\n\n- `checked` — selection state. Defaults to `false`. Reflected to the host as `aria-checked`.\n- `selection-type` — visual indicator. `check` (default) renders an `mdc-icon` that appears/changes when selected; `checkbox` renders an `mdc-staticcheckbox` that is always visible and reflects both states.\n- `disabled` — when `true`, the card cannot be toggled and is removed from the tab order. Also reflected as `aria-disabled=\"true\"`.\n- `card-title` — primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.\n- `subtitle` — secondary header text. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered above the header. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost`, or `promotional`.\n- `orientation` — `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n\n### Limitations\n\n- Slot content must remain non-interactive. The card itself is the toggle target, so nested buttons, links, or form controls create overlapping interactive regions and break keyboard/assistive-technology behaviour — use `mdc-card` if the surface needs actionable children.\n- The card checkbox is not form-associated. To submit its state in a native form, mirror `checked` into a `<input type=\"hidden\">` (or use `mdc-checkbox` directly) and update it from the `change` event.\n- `card-title` is required for an accessible name; there is no fallback if both `card-title` and the `title` slot are empty.",
      "Accessibility": "### Built-in features\n\nThe host renders with `role=\"checkbox\"` and is focusable (single tab stop per card; `tabindex` is set to `0` when enabled, `-1` when disabled). The component manages `aria-checked` to mirror the `checked` property and `aria-disabled` to mirror `disabled`.\n\nKeyboard interaction follows the checkbox pattern:\n\n- `Enter` toggles the checked state on `keydown` and dispatches a `change` event.\n- `Space` toggles the checked state on `keyup` and dispatches a `change` event. `Space` is suppressed on `keydown` so the page does not scroll.\n- Click toggles the checked state and dispatches a `change` event.\n\nWhen `disabled` is `true`, click/keyboard activation is suppressed, the host is removed from the tab order, and `aria-disabled=\"true\"` is set.\n\nThe `card-title` (or `title` slot) provides the accessible name. The visual selection indicator (`mdc-icon` for `selection-type=\"check\"`, `mdc-staticcheckbox` for `selection-type=\"checkbox\"`) is decorative — state is conveyed via `aria-checked` on the host.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                                       |\n| ------- | --------------- | --------------------------------------------------------------------------- |\n| Host    | `role`          | `checkbox`                                                                  |\n| Host    | `tabindex`      | `0` when enabled; `-1` when `disabled`                                      |\n| Host    | `aria-checked`  | `true` / `false`, mirrors the `checked` property                            |\n| Host    | `aria-disabled` | `true` / `false`, mirrors the `disabled` property                           |\n\n### Implementation requirements\n\n#### General\n\n- Wrap related card checkboxes in a container with `role=\"group\"` and a label (`aria-label` or `aria-labelledby`) so assistive technologies announce the option set.\n- Keep every slotted child presentational. If you need actionable content alongside the option, switch to `mdc-card` and use `mdc-checkbox` for the selection.\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be selectable, so the state is exposed to assistive technology.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is the default accessible name of the checkbox.\n- When the title alone is ambiguous (e.g. icon-driven cards, repeated titles disambiguated by context), set `aria-label` (or `aria-labelledby`) on the host so the announcement matches the option.\n- Set `image-alt` for informative images and `image-alt=\"\"` for decorative ones to avoid double-announcing the visual.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline only when the title functions as a heading; otherwise leave the default `span`."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-staticcheckbox",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "description": "Defines an accessible label for screen readers. Required when using icon-only cards.",
          "source": "storybook"
        },
        {
          "name": "cardTitle",
          "attribute": "card-title",
          "type": "text",
          "values": [],
          "description": "primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.",
          "source": "storybook"
        },
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "selection state. Defaults to `false`. Reflected to the host as `aria-checked`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the card cannot be toggled and is removed from the tab order. Also reflected as `aria-disabled=\"true\"`.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "name of the leading icon shown in the header. When empty, no icon is rendered.",
          "source": "storybook"
        },
        {
          "name": "imageAlt",
          "attribute": "image-alt",
          "type": "text",
          "values": [],
          "description": "alt text for the image. Required for non-decorative images.",
          "source": "storybook"
        },
        {
          "name": "imageSrc",
          "attribute": "image-src",
          "type": "text",
          "values": [],
          "description": "URL of the image rendered above the header. When empty, no image is rendered.",
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "select",
          "values": [],
          "description": "`vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).",
          "source": "storybook"
        },
        {
          "name": "selectionType",
          "attribute": "selection-type",
          "type": "select",
          "default": "'check'",
          "values": [
            "check",
            "checkbox"
          ],
          "description": "visual indicator. `check` (default) renders an `mdc-icon` that appears/changes when selected; `checkbox` renders an `mdc-staticcheckbox` that is always visible and reflects both states.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "subtitle",
          "attribute": "subtitle",
          "type": "text",
          "values": [],
          "description": "secondary header text. Ignored if the `subtitle` slot has content.",
          "source": "storybook"
        },
        {
          "name": "subtitleTagName",
          "attribute": "subtitle-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "tabIndex",
          "attribute": "tab-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "titleTagName",
          "attribute": "title-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [],
          "description": "border treatment. `border` (default), `ghost`, or `promotional`.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "before-body",
          "description": "This slot is for passing the content before the body"
        },
        {
          "name": "body",
          "description": "This slot is for passing the text content for the card"
        },
        {
          "name": "after-body",
          "description": "This slot is for passing the content after the body"
        },
        {
          "name": "title",
          "description": "This slot is for passing the title of the card in the header section"
        },
        {
          "name": "subtitle",
          "description": "This slot is for passing the subtitle of the card in the header section"
        },
        {
          "name": "footer-link",
          "description": "This slot is for passing `mdc-link` component within the footer section."
        },
        {
          "name": "footer-button-primary",
          "description": "This slot is for passing primary variant of `mdc-button` component within the footer section."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Event that gets dispatched when the card is clicked. It toggles the checked state."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the card."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the card."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Event that gets dispatched when the card receives focus."
        },
        {
          "name": "change",
          "description": "(React: onChange) Event that gets dispatched when the card's checked state changes."
        }
      ],
      "cssParts": [
        {
          "name": "header",
          "description": "The header part of the card"
        },
        {
          "name": "icon",
          "description": "The icon part of the card header"
        },
        {
          "name": "body",
          "description": "The body part of the card"
        },
        {
          "name": "image",
          "description": "The image part of the card"
        },
        {
          "name": "footer",
          "description": "The footer part of the card"
        },
        {
          "name": "footer-link",
          "description": "The link part of the card footer"
        },
        {
          "name": "footer-button-primary",
          "description": "The primary button part of the card footer"
        },
        {
          "name": "footer-button-secondary",
          "description": "The secondary button part of the card footer"
        },
        {
          "name": "icon-button",
          "description": "The icon button part of the card header"
        },
        {
          "name": "text",
          "description": "The text part of the card"
        },
        {
          "name": "check",
          "description": "The check part of the card"
        },
        {
          "name": "check-icon",
          "description": "The check icon part of the card"
        },
        {
          "name": "check-icon-button",
          "description": "The check icon button part of the card"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-card-width",
          "description": "The width of the card"
        }
      ],
      "rules": [
        "selection state. Defaults to `false`. Reflected to the host as `aria-checked`.",
        "visual indicator. `check` (default) renders an `mdc-icon` that appears/changes when selected; `checkbox` renders an `mdc-staticcheckbox` that is always visible and reflects both states.",
        "when `true`, the card cannot be toggled and is removed from the tab order. Also reflected as `aria-disabled=\"true\"`.",
        "primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.",
        "secondary header text. Ignored if the `subtitle` slot has content.",
        "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "URL of the image rendered above the header. When empty, no image is rendered.",
        "alt text for the image. Required for non-decorative images.",
        "name of the leading icon shown in the header. When empty, no icon is rendered.",
        "border treatment. `border` (default), `ghost`, or `promotional`.",
        "`vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/cardcheckbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardCheckbox } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<div role=\"group\" aria-label=\"Notification channels\">\n  <mdc-cardcheckbox\n    card-title=\"Email\"\n    subtitle=\"Daily digest at 8:00 AM\"\n    icon-name=\"email-bold\"\n    selection-type=\"checkbox\"\n    checked\n  >\n    <mdc-text slot=\"body\">Receive a summary of activity in your inbox.</mdc-text>\n  </mdc-cardcheckbox>\n\n  <mdc-cardcheckbox\n    card-title=\"Push\"\n    subtitle=\"On all signed-in devices\"\n    icon-name=\"bell-bold\"\n    selection-type=\"checkbox\"\n  ></mdc-cardcheckbox>\n</div>"
      }
    ],
    "reactImports": [
      "CardCheckbox"
    ],
    "constants": {
      "CHECK_MARK": {
        "DEFAULT": "'check-circle-bold' as Extract<IconNames, 'check-circle-bold'>",
        "CHECKED": "'check-circle-filled' as Extract<IconNames, 'check-circle-filled'>"
      },
      "SELECTION_TYPE": {
        "CHECK": "check",
        "CHECKBOX": "checkbox"
      },
      "DEFAULTS": {
        "SELECTION_TYPE": "check",
        "ICON_SIZE": 1.5,
        "ICON_LENGTH_UNIT": "rem"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "as",
      "behaves",
      "card",
      "cardcheckbox",
      "checkbox",
      "click",
      "component",
      "enter",
      "for",
      "guidelines",
      "icon",
      "mdc",
      "on",
      "or",
      "overview",
      "single",
      "space",
      "staticcheckbox",
      "surface",
      "text",
      "that",
      "the",
      "toggling",
      "usage"
    ]
  },
  {
    "name": "cardradio",
    "title": "Card radio",
    "tagName": "mdc-cardradio",
    "summary": "Usage, guidelines, and accessibility for the mdc-cardradio component — a card surface behaving as a radio option; only one card per name group can be selected at a time.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/cardradio",
      "knowledgeBasePath": "components/src/components/cardradio/knowledge-base/cardradio.component.md"
    },
    "docs": "## Overview\n\nThe card radio looks like a card (image, header with icon/title/subtitle, body) but behaves as a single radio option. Cards sharing the same `name` form a group: clicking, pressing `Enter`, or pressing `Space` on one card selects it and clears the others. Arrow keys move focus and selection between enabled cards in the group.\n\n### When to use\n\n- Use `mdc-cardradio` when each option in a single-select group needs the visual weight of a card (image, title, supporting copy) rather than a small inline control.\n- Use it inside a labelled radio-group container (`role=\"radiogroup\"` + `aria-label` / `aria-labelledby`) when several card radios belong to the same selection set.\n\n### When not to use\n\n- Use `mdc-card` when the surface is a static container and should not represent a selection.\n- Use `mdc-cardbutton` when activating the card should trigger an action rather than select an option.\n- Use `mdc-cardcheckbox` when more than one card in the group can be selected at the same time.\n- Use `mdc-radio` (or `mdc-radiogroup`) when a plain control is enough and the card surface is unnecessary.\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone selectable tile.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/cardradio/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardRadio } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<div role=\"radiogroup\" aria-label=\"Delivery speed\">\n  <mdc-cardradio\n    name=\"delivery\"\n    card-title=\"Standard\"\n    subtitle=\"3–5 business days\"\n    icon-name=\"truck-bold\"\n    checked\n  >\n    <mdc-text slot=\"body\">Free for all orders.</mdc-text>\n  </mdc-cardradio>\n\n  <mdc-cardradio\n    name=\"delivery\"\n    card-title=\"Express\"\n    subtitle=\"Next business day\"\n    icon-name=\"bolt-bold\"\n  >\n    <mdc-text slot=\"body\">$12 flat fee.</mdc-text>\n  </mdc-cardradio>\n</div>\n```\n\nListen for the `change` event to react to selection changes; the newly selected card is the event target.\n\n### Content guidance\n\n- Always provide a `card-title` — it is the accessible name of the radio.\n- Use the body to differentiate the option from its siblings (price, speed, capacity), not to repeat the title.\n- Group related card radios visually inside one container with `role=\"radiogroup\"` and a label so assistive technologies announce the option set.\n\n### Property/Attribute details\n\n- `checked` — selection state. Defaults to `false`. Reflected on the host as `aria-checked`.\n- `name` — radio-group name. All `mdc-cardradio` elements in the document that share the same `name` form one mutually-exclusive group. **Required**.\n- `disabled` — when `true`, the card cannot be selected and is removed from both the tab order and the arrow-key navigation. Reflected as `aria-disabled=\"true\"`.\n- `card-title` — primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.\n- `subtitle` — secondary header text. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered above the header. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost`, or `promotional`.\n- `orientation` — `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n\n### Limitations\n\n- Slot content must remain non-interactive. The card itself is the selection target, so nested buttons, links, or form controls create overlapping interactive regions and break keyboard/assistive-technology behaviour — use `mdc-card` if the surface needs actionable children.\n- Grouping is global to the document: every `mdc-cardradio[name=\"…\"]` is considered part of the same group regardless of where it lives in the DOM. Use distinct `name` values when multiple radio groups appear on the same page.\n- The card radio is not form-associated. To submit the selected value in a native form, mirror the selected card's identifier into a `<input type=\"hidden\">` (or use `mdc-radiogroup` directly) and update it from the `change` event.\n- `card-title` is required for an accessible name; there is no fallback if both `card-title` and the `title` slot are empty.\n\n## Accessibility\n\n### Built-in features\n\nThe host renders with `role=\"radio\"` and is focusable (single tab stop per card; `tabindex` is set to `0` when enabled, `-1` when disabled). The component manages `aria-checked` to mirror the `checked` property and `aria-disabled` to mirror `disabled`.\n\nKeyboard interaction follows the radio pattern:\n\n- `ArrowDown` / `ArrowRight` moves focus to the next enabled card in the same group and selects it (wrapping at the end).\n- `ArrowUp` / `ArrowLeft` moves focus to the previous enabled card in the same group and selects it (wrapping at the start).\n- `Enter` selects the focused card and dispatches a `change` event.\n- `Space` selects the focused card on `keyup` and dispatches a `change` event. `Space` is suppressed on `keydown` so the page does not scroll.\n- Click selects the card and dispatches a `change` event.\n\nSelecting a card automatically clears `checked` on every other card sharing the same `name`, so only one card per group is selected at any time. When `disabled` is `true`, the card is removed from the tab order and from arrow-key navigation, and `aria-disabled=\"true\"` is set.\n\nThe `card-title` (or `title` slot) provides the accessible name. The `mdc-staticradio` rendered in the header is decorative — state is conveyed via `aria-checked` on the host.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                                       |\n| ------- | --------------- | --------------------------------------------------------------------------- |\n| Host    | `role`          | `radio`                                                                     |\n| Host    | `tabindex`      | `0` when enabled; `-1` when `disabled`                                      |\n| Host    | `aria-checked`  | `true` / `false`, mirrors the `checked` property                            |\n| Host    | `aria-disabled` | `true` / `false`, mirrors the `disabled` property                           |\n\n### Implementation requirements\n\n#### General\n\n- Wrap related card radios in a container with `role=\"radiogroup\"` and a label (`aria-label` or `aria-labelledby`) so assistive technologies announce the option set and its current selection.\n- Give every group of card radios a unique `name`. Avoid placing two unrelated groups in the document with the same `name` — they will merge into a single mutually-exclusive group.\n- Keep every slotted child presentational. If you need actionable content alongside the option, switch to `mdc-card` and use `mdc-radio` / `mdc-radiogroup` for the selection.\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be selectable, so the state is exposed to assistive technology and arrow navigation skips it correctly.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is the default accessible name of the radio.\n- When the title alone is ambiguous (e.g. icon-driven cards, repeated titles disambiguated by context), set `aria-label` (or `aria-labelledby`) on the host so the announcement matches the option.\n- Set `image-alt` for informative images and `image-alt=\"\"` for decorative ones to avoid double-announcing the visual.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline only when the title functions as a heading; otherwise leave the default `span`.",
    "sections": {
      "Overview": "The card radio looks like a card (image, header with icon/title/subtitle, body) but behaves as a single radio option. Cards sharing the same `name` form a group: clicking, pressing `Enter`, or pressing `Space` on one card selects it and clears the others. Arrow keys move focus and selection between enabled cards in the group.\n\n### When to use\n\n- Use `mdc-cardradio` when each option in a single-select group needs the visual weight of a card (image, title, supporting copy) rather than a small inline control.\n- Use it inside a labelled radio-group container (`role=\"radiogroup\"` + `aria-label` / `aria-labelledby`) when several card radios belong to the same selection set.\n\n### When not to use\n\n- Use `mdc-card` when the surface is a static container and should not represent a selection.\n- Use `mdc-cardbutton` when activating the card should trigger an action rather than select an option.\n- Use `mdc-cardcheckbox` when more than one card in the group can be selected at the same time.\n- Use `mdc-radio` (or `mdc-radiogroup`) when a plain control is enough and the card surface is unnecessary.\n- Use `mdc-listitem` inside `mdc-list` when the content is a row of a scrolling list rather than a standalone selectable tile.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/cardradio/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardRadio } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<div role=\"radiogroup\" aria-label=\"Delivery speed\">\n  <mdc-cardradio\n    name=\"delivery\"\n    card-title=\"Standard\"\n    subtitle=\"3–5 business days\"\n    icon-name=\"truck-bold\"\n    checked\n  >\n    <mdc-text slot=\"body\">Free for all orders.</mdc-text>\n  </mdc-cardradio>\n\n  <mdc-cardradio\n    name=\"delivery\"\n    card-title=\"Express\"\n    subtitle=\"Next business day\"\n    icon-name=\"bolt-bold\"\n  >\n    <mdc-text slot=\"body\">$12 flat fee.</mdc-text>\n  </mdc-cardradio>\n</div>\n```\n\nListen for the `change` event to react to selection changes; the newly selected card is the event target.\n\n### Content guidance\n\n- Always provide a `card-title` — it is the accessible name of the radio.\n- Use the body to differentiate the option from its siblings (price, speed, capacity), not to repeat the title.\n- Group related card radios visually inside one container with `role=\"radiogroup\"` and a label so assistive technologies announce the option set.\n\n### Property/Attribute details\n\n- `checked` — selection state. Defaults to `false`. Reflected on the host as `aria-checked`.\n- `name` — radio-group name. All `mdc-cardradio` elements in the document that share the same `name` form one mutually-exclusive group. **Required**.\n- `disabled` — when `true`, the card cannot be selected and is removed from both the tab order and the arrow-key navigation. Reflected as `aria-disabled=\"true\"`.\n- `card-title` — primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.\n- `subtitle` — secondary header text. Ignored if the `subtitle` slot has content.\n- `title-tag-name` / `subtitle-tag-name` — DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.\n- `image-src` — URL of the image rendered above the header. When empty, no image is rendered.\n- `image-alt` — alt text for the image. Required for non-decorative images.\n- `icon-name` — name of the leading icon shown in the header. When empty, no icon is rendered.\n- `variant` — border treatment. `border` (default), `ghost`, or `promotional`.\n- `orientation` — `vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).\n\n### Limitations\n\n- Slot content must remain non-interactive. The card itself is the selection target, so nested buttons, links, or form controls create overlapping interactive regions and break keyboard/assistive-technology behaviour — use `mdc-card` if the surface needs actionable children.\n- Grouping is global to the document: every `mdc-cardradio[name=\"…\"]` is considered part of the same group regardless of where it lives in the DOM. Use distinct `name` values when multiple radio groups appear on the same page.\n- The card radio is not form-associated. To submit the selected value in a native form, mirror the selected card's identifier into a `<input type=\"hidden\">` (or use `mdc-radiogroup` directly) and update it from the `change` event.\n- `card-title` is required for an accessible name; there is no fallback if both `card-title` and the `title` slot are empty.",
      "Accessibility": "### Built-in features\n\nThe host renders with `role=\"radio\"` and is focusable (single tab stop per card; `tabindex` is set to `0` when enabled, `-1` when disabled). The component manages `aria-checked` to mirror the `checked` property and `aria-disabled` to mirror `disabled`.\n\nKeyboard interaction follows the radio pattern:\n\n- `ArrowDown` / `ArrowRight` moves focus to the next enabled card in the same group and selects it (wrapping at the end).\n- `ArrowUp` / `ArrowLeft` moves focus to the previous enabled card in the same group and selects it (wrapping at the start).\n- `Enter` selects the focused card and dispatches a `change` event.\n- `Space` selects the focused card on `keyup` and dispatches a `change` event. `Space` is suppressed on `keydown` so the page does not scroll.\n- Click selects the card and dispatches a `change` event.\n\nSelecting a card automatically clears `checked` on every other card sharing the same `name`, so only one card per group is selected at any time. When `disabled` is `true`, the card is removed from the tab order and from arrow-key navigation, and `aria-disabled=\"true\"` is set.\n\nThe `card-title` (or `title` slot) provides the accessible name. The `mdc-staticradio` rendered in the header is decorative — state is conveyed via `aria-checked` on the host.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                                       |\n| ------- | --------------- | --------------------------------------------------------------------------- |\n| Host    | `role`          | `radio`                                                                     |\n| Host    | `tabindex`      | `0` when enabled; `-1` when `disabled`                                      |\n| Host    | `aria-checked`  | `true` / `false`, mirrors the `checked` property                            |\n| Host    | `aria-disabled` | `true` / `false`, mirrors the `disabled` property                           |\n\n### Implementation requirements\n\n#### General\n\n- Wrap related card radios in a container with `role=\"radiogroup\"` and a label (`aria-label` or `aria-labelledby`) so assistive technologies announce the option set and its current selection.\n- Give every group of card radios a unique `name`. Avoid placing two unrelated groups in the document with the same `name` — they will merge into a single mutually-exclusive group.\n- Keep every slotted child presentational. If you need actionable content alongside the option, switch to `mdc-card` and use `mdc-radio` / `mdc-radiogroup` for the selection.\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be selectable, so the state is exposed to assistive technology and arrow navigation skips it correctly.\n\n#### Labelling\n\n- Provide a meaningful `card-title` (or `title` slot) — it is the default accessible name of the radio.\n- When the title alone is ambiguous (e.g. icon-driven cards, repeated titles disambiguated by context), set `aria-label` (or `aria-labelledby`) on the host so the announcement matches the option.\n- Set `image-alt` for informative images and `image-alt=\"\"` for decorative ones to avoid double-announcing the visual.\n- Pick `title-tag-name` / `subtitle-tag-name` to match the surrounding heading outline only when the title functions as a heading; otherwise leave the default `span`."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-staticradio",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "description": "Defines an accessible label for screen readers. Required when using icon-only cards.",
          "source": "storybook"
        },
        {
          "name": "cardTitle",
          "attribute": "card-title",
          "type": "text",
          "values": [],
          "description": "primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.",
          "source": "storybook"
        },
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "description": "selection state. Defaults to `false`. Reflected on the host as `aria-checked`.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the card cannot be selected and is removed from both the tab order and the arrow-key navigation. Reflected as `aria-disabled=\"true\"`.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "name of the leading icon shown in the header. When empty, no icon is rendered.",
          "source": "storybook"
        },
        {
          "name": "imageAlt",
          "attribute": "image-alt",
          "type": "text",
          "values": [],
          "description": "alt text for the image. Required for non-decorative images.",
          "source": "storybook"
        },
        {
          "name": "imageSrc",
          "attribute": "image-src",
          "type": "text",
          "values": [],
          "description": "URL of the image rendered above the header. When empty, no image is rendered.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "radio-group name. All `mdc-cardradio` elements in the document that share the same `name` form one mutually-exclusive group. **Required**.",
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "select",
          "values": [],
          "description": "`vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem).",
          "source": "storybook"
        },
        {
          "name": "subtitle",
          "attribute": "subtitle",
          "type": "text",
          "values": [],
          "description": "secondary header text. Ignored if the `subtitle` slot has content.",
          "source": "storybook"
        },
        {
          "name": "subtitleTagName",
          "attribute": "subtitle-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "tabIndex",
          "attribute": "tab-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "titleTagName",
          "attribute": "title-tag-name",
          "type": "select",
          "values": [],
          "description": "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "values": [],
          "description": "border treatment. `border` (default), `ghost`, or `promotional`.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "before-body",
          "description": "This slot is for passing the content before the body"
        },
        {
          "name": "body",
          "description": "This slot is for passing the text content for the card"
        },
        {
          "name": "after-body",
          "description": "This slot is for passing the content after the body"
        },
        {
          "name": "title",
          "description": "This slot is for passing the title of the card in the header section"
        },
        {
          "name": "subtitle",
          "description": "This slot is for passing the subtitle of the card in the header section"
        },
        {
          "name": "footer-link",
          "description": "This slot is for passing `mdc-link` component within the footer section."
        },
        {
          "name": "footer-button-primary",
          "description": "This slot is for passing primary variant of `mdc-button` component within the footer section."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Event that gets dispatched when the card is clicked. It selects the card and unselects others in the same group."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the card."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the card."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Event that gets dispatched when the card receives focus."
        },
        {
          "name": "change",
          "description": "(React: onChange) Event that gets dispatched when the card's checked state changes."
        }
      ],
      "cssParts": [
        {
          "name": "header",
          "description": "The header part of the card"
        },
        {
          "name": "icon",
          "description": "The icon part of the card header"
        },
        {
          "name": "body",
          "description": "The body part of the card"
        },
        {
          "name": "image",
          "description": "The image part of the card"
        },
        {
          "name": "footer",
          "description": "The footer part of the card"
        },
        {
          "name": "footer-link",
          "description": "The link part of the card footer"
        },
        {
          "name": "footer-button-primary",
          "description": "The primary button part of the card footer"
        },
        {
          "name": "footer-button-secondary",
          "description": "The secondary button part of the card footer"
        },
        {
          "name": "icon-button",
          "description": "The icon button part of the card header"
        },
        {
          "name": "text",
          "description": "The text part of the card"
        },
        {
          "name": "check",
          "description": "The check part of the card"
        },
        {
          "name": "check-icon",
          "description": "The check icon part of the card"
        },
        {
          "name": "check-icon-button",
          "description": "The check icon button part of the card"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-card-width",
          "description": "The width of the card"
        }
      ],
      "rules": [
        "selection state. Defaults to `false`. Reflected on the host as `aria-checked`.",
        "radio-group name. All `mdc-cardradio` elements in the document that share the same `name` form one mutually-exclusive group. **Required**.",
        "when `true`, the card cannot be selected and is removed from both the tab order and the arrow-key navigation. Reflected as `aria-disabled=\"true\"`.",
        "primary header text rendered through `mdc-text`. Ignored if the `title` slot has content. **Required** for accessibility.",
        "secondary header text. Ignored if the `subtitle` slot has content.",
        "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "DOM tag used for the rendered title/subtitle `mdc-text` (e.g. `h2`, `h3`, `span`). Default: `span`.",
        "URL of the image rendered above the header. When empty, no image is rendered.",
        "alt text for the image. Required for non-decorative images.",
        "name of the leading icon shown in the header. When empty, no icon is rendered.",
        "border treatment. `border` (default), `ghost`, or `promotional`.",
        "`vertical` (default, min-width 20rem) or `horizontal` (min-width 40rem)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/cardradio/index.js\"; // custom element registration\n// or via React wrapper\nimport { CardRadio } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<div role=\"radiogroup\" aria-label=\"Delivery speed\">\n  <mdc-cardradio\n    name=\"delivery\"\n    card-title=\"Standard\"\n    subtitle=\"3–5 business days\"\n    icon-name=\"truck-bold\"\n    checked\n  >\n    <mdc-text slot=\"body\">Free for all orders.</mdc-text>\n  </mdc-cardradio>\n\n  <mdc-cardradio\n    name=\"delivery\"\n    card-title=\"Express\"\n    subtitle=\"Next business day\"\n    icon-name=\"bolt-bold\"\n  >\n    <mdc-text slot=\"body\">$12 flat fee.</mdc-text>\n  </mdc-cardradio>\n</div>"
      }
    ],
    "reactImports": [
      "CardRadio"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "as",
      "at",
      "be",
      "behaving",
      "can",
      "card",
      "cardradio",
      "component",
      "for",
      "group",
      "guidelines",
      "icon",
      "mdc",
      "name",
      "one",
      "only",
      "option",
      "overview",
      "per",
      "radio",
      "selected",
      "staticradio",
      "surface",
      "text",
      "the",
      "time",
      "usage"
    ]
  },
  {
    "name": "checkbox",
    "title": "Checkbox",
    "tagName": "mdc-checkbox",
    "summary": "Usage, guidelines, and accessibility for the mdc-checkbox component — a form control that lets users select one or multiple options, toggle a feature on/off, or indicate agreement.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/checkbox",
      "knowledgeBasePath": "components/src/components/checkbox/knowledge-base/checkbox.component.md"
    },
    "docs": "## Overview\n\nThe checkbox is a form control that represents a binary choice (\"checked\"/\"unchecked\") or, when used in a group, allows multiple selections from a list. It wraps a native `<input type=\"checkbox\">` and renders a custom visual through `mdc-staticcheckbox`, while preserving full form participation (value, name, validation, reset).\n\nA checkbox can also be in an `indeterminate` (mixed) state — typically used when a parent checkbox summarises a nested group whose children are not all in the same state.\n\n### When to use\n\n- Use `mdc-checkbox` when the user can select **any number** of options from a list (zero, one, or many).\n- Use a single `mdc-checkbox` for a binary choice that does not need to take effect immediately (e.g. agreeing to terms before submitting a form).\n- Use it inside `mdc-formfieldgroup` when several checkboxes belong to the same labelled group.\n\n### When not to use\n\n- Use `mdc-radio` (or `mdc-radiogroup`) when **only one** option in a set can be selected.\n- Use `mdc-toggle` for a binary on/off setting that takes effect immediately.\n- Use `mdc-cardcheckbox` when each option needs the visual weight of a card surface (image, title, supporting copy).\n- Use `mdc-staticcheckbox` when the visual is purely decorative and not user-interactive (e.g. inside a list item that owns the click target).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/checkbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { Checkbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-checkbox\n  label=\"Send me product updates\"\n  name=\"newsletter\"\n  value=\"weekly\"\n></mdc-checkbox>\n```\n\nListen for the `change` event to react to toggles; the new state is on `event.target.checked`.\n\n### Content guidance\n\n- Provide a `label` that clearly describes what the checkbox controls. If the visual label cannot be set (e.g. compact tables), supply `data-aria-label` instead.\n- Use `help-text` to explain consequences, requirements, or validation errors — not to repeat the label.\n- When several checkboxes belong together, group them with `mdc-formfieldgroup` and give the group a label so assistive technologies announce the set.\n\n### Property/Attribute details\n\n- `checked` — selection state. Defaults to `false`. Reflected to the host.\n- `indeterminate` — mixed state. Defaults to `false`. Reflected to the host. Setting `checked` clears `indeterminate`.\n- `name` — form field name submitted with the form.\n- `value` — form field value. When the checkbox is checked and no `value` is set, the form receives `\"on\"` (matches native `<input type=\"checkbox\">`).\n- `label` — visible label rendered through `mdc-text`. Used as the accessible name.\n- `data-aria-label` — accessible name fallback when no visual label is rendered.\n- `help-text` — helper or validation message shown below the label.\n- `help-text-type` — `default` (helper) or `error` (validation). When set to `error`, the validation icon is shown next to the help text.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip` with extra context.\n- `required` — when `true`, an asterisk is appended to the label and the form is invalid unless the checkbox is checked.\n- `validation-message` — custom message reported through `setCustomValidity` when the checkbox is required but not checked.\n- `disabled` — fully disabled; the host is removed from the tab order and the form value is not submitted.\n- `soft-disabled` — visually disabled but still focusable; suppresses `Space` activation while keeping the control discoverable by assistive technology.\n- `readonly` — non-interactive but focusable; the current value is still submitted with the form.\n- `autoFocusOnMount` — when `true`, focuses the internal input element on first render.\n\n### Limitations\n\n- The form value is always either the `value` attribute (defaulting to `\"on\"`) when checked or `null` when unchecked. The `indeterminate` state is not submitted — choose a deterministic value before submission if it matters.\n- `indeterminate` is visual-only and is cleared automatically when the user toggles the checkbox.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders a native `<input type=\"checkbox\">` inside the shadow root, so the platform owns the role, focus ring, and form association. The visual `mdc-staticcheckbox` is decorative — state is conveyed via `aria-checked` on the native input (`true`, `false`, or `mixed` when `indeterminate` is set).\n\nKeyboard interaction follows the native checkbox pattern:\n\n- `Space` toggles the checked state and dispatches a `change` event.\n- `Enter` submits the surrounding form (default browser behaviour).\n- `Tab` / `Shift+Tab` moves focus to/from the checkbox.\n\nWhen `disabled` is `true`, the input is removed from the tab order and the form value is not submitted. When `soft-disabled` is `true`, the input remains focusable but `Space` is suppressed, so assistive technologies can still discover the option. When `readonly` is `true`, toggling is suppressed but the current value is still submitted.\n\nThe `label` (or `data-aria-label`) provides the accessible name. `help-text` is linked via `aria-describedby` so it is announced after the name.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute          | Value                                                                  |\n| ------------- | ------------------ | ---------------------------------------------------------------------- |\n| Native input  | `type`             | `checkbox`                                                             |\n| Native input  | `aria-checked`     | `true` / `false` / `mixed` (when `indeterminate`)                      |\n| Native input  | `aria-label`       | mirrors `data-aria-label`                                              |\n| Native input  | `aria-describedby` | id of the help text element when `help-text` is set                    |\n| Native input  | `tabindex`         | `0` when enabled; `-1` when `disabled`                                 |\n| Host          | `:state(checked)`  | added when `checked` is `true`, for CSS targeting via `:state(checked)`|\n\n### Implementation requirements\n\n#### General\n\n- Group related checkboxes inside `mdc-formfieldgroup` (or a container with `role=\"group\"` and a label) so assistive technologies announce the option set.\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be selectable; the state is then exposed to assistive technology and removed from the tab order.\n- Reach for `soft-disabled` instead of `disabled` only when the option must remain discoverable by assistive technology (e.g. so a screen-reader user can still hear why it is unavailable).\n\n#### Labelling\n\n- Provide a meaningful `label` — it is the default accessible name.\n- When a visible label is not appropriate, set `data-aria-label` so the control still has an accessible name.\n- Use `help-text` with `help-text-type=\"error\"` to communicate validation errors; the error icon and `aria-describedby` association are wired automatically.",
    "sections": {
      "Overview": "The checkbox is a form control that represents a binary choice (\"checked\"/\"unchecked\") or, when used in a group, allows multiple selections from a list. It wraps a native `<input type=\"checkbox\">` and renders a custom visual through `mdc-staticcheckbox`, while preserving full form participation (value, name, validation, reset).\n\nA checkbox can also be in an `indeterminate` (mixed) state — typically used when a parent checkbox summarises a nested group whose children are not all in the same state.\n\n### When to use\n\n- Use `mdc-checkbox` when the user can select **any number** of options from a list (zero, one, or many).\n- Use a single `mdc-checkbox` for a binary choice that does not need to take effect immediately (e.g. agreeing to terms before submitting a form).\n- Use it inside `mdc-formfieldgroup` when several checkboxes belong to the same labelled group.\n\n### When not to use\n\n- Use `mdc-radio` (or `mdc-radiogroup`) when **only one** option in a set can be selected.\n- Use `mdc-toggle` for a binary on/off setting that takes effect immediately.\n- Use `mdc-cardcheckbox` when each option needs the visual weight of a card surface (image, title, supporting copy).\n- Use `mdc-staticcheckbox` when the visual is purely decorative and not user-interactive (e.g. inside a list item that owns the click target).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/checkbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { Checkbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-checkbox\n  label=\"Send me product updates\"\n  name=\"newsletter\"\n  value=\"weekly\"\n></mdc-checkbox>\n```\n\nListen for the `change` event to react to toggles; the new state is on `event.target.checked`.\n\n### Content guidance\n\n- Provide a `label` that clearly describes what the checkbox controls. If the visual label cannot be set (e.g. compact tables), supply `data-aria-label` instead.\n- Use `help-text` to explain consequences, requirements, or validation errors — not to repeat the label.\n- When several checkboxes belong together, group them with `mdc-formfieldgroup` and give the group a label so assistive technologies announce the set.\n\n### Property/Attribute details\n\n- `checked` — selection state. Defaults to `false`. Reflected to the host.\n- `indeterminate` — mixed state. Defaults to `false`. Reflected to the host. Setting `checked` clears `indeterminate`.\n- `name` — form field name submitted with the form.\n- `value` — form field value. When the checkbox is checked and no `value` is set, the form receives `\"on\"` (matches native `<input type=\"checkbox\">`).\n- `label` — visible label rendered through `mdc-text`. Used as the accessible name.\n- `data-aria-label` — accessible name fallback when no visual label is rendered.\n- `help-text` — helper or validation message shown below the label.\n- `help-text-type` — `default` (helper) or `error` (validation). When set to `error`, the validation icon is shown next to the help text.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip` with extra context.\n- `required` — when `true`, an asterisk is appended to the label and the form is invalid unless the checkbox is checked.\n- `validation-message` — custom message reported through `setCustomValidity` when the checkbox is required but not checked.\n- `disabled` — fully disabled; the host is removed from the tab order and the form value is not submitted.\n- `soft-disabled` — visually disabled but still focusable; suppresses `Space` activation while keeping the control discoverable by assistive technology.\n- `readonly` — non-interactive but focusable; the current value is still submitted with the form.\n- `autoFocusOnMount` — when `true`, focuses the internal input element on first render.\n\n### Limitations\n\n- The form value is always either the `value` attribute (defaulting to `\"on\"`) when checked or `null` when unchecked. The `indeterminate` state is not submitted — choose a deterministic value before submission if it matters.\n- `indeterminate` is visual-only and is cleared automatically when the user toggles the checkbox.",
      "Accessibility": "### Built-in features\n\nThe component renders a native `<input type=\"checkbox\">` inside the shadow root, so the platform owns the role, focus ring, and form association. The visual `mdc-staticcheckbox` is decorative — state is conveyed via `aria-checked` on the native input (`true`, `false`, or `mixed` when `indeterminate` is set).\n\nKeyboard interaction follows the native checkbox pattern:\n\n- `Space` toggles the checked state and dispatches a `change` event.\n- `Enter` submits the surrounding form (default browser behaviour).\n- `Tab` / `Shift+Tab` moves focus to/from the checkbox.\n\nWhen `disabled` is `true`, the input is removed from the tab order and the form value is not submitted. When `soft-disabled` is `true`, the input remains focusable but `Space` is suppressed, so assistive technologies can still discover the option. When `readonly` is `true`, toggling is suppressed but the current value is still submitted.\n\nThe `label` (or `data-aria-label`) provides the accessible name. `help-text` is linked via `aria-describedby` so it is announced after the name.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute          | Value                                                                  |\n| ------------- | ------------------ | ---------------------------------------------------------------------- |\n| Native input  | `type`             | `checkbox`                                                             |\n| Native input  | `aria-checked`     | `true` / `false` / `mixed` (when `indeterminate`)                      |\n| Native input  | `aria-label`       | mirrors `data-aria-label`                                              |\n| Native input  | `aria-describedby` | id of the help text element when `help-text` is set                    |\n| Native input  | `tabindex`         | `0` when enabled; `-1` when `disabled`                                 |\n| Host          | `:state(checked)`  | added when `checked` is `true`, for CSS targeting via `:state(checked)`|\n\n### Implementation requirements\n\n#### General\n\n- Group related checkboxes inside `mdc-formfieldgroup` (or a container with `role=\"group\"` and a label) so assistive technologies announce the option set.\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be selectable; the state is then exposed to assistive technology and removed from the tab order.\n- Reach for `soft-disabled` instead of `disabled` only when the option must remain discoverable by assistive technology (e.g. so a screen-reader user can still hear why it is unavailable).\n\n#### Labelling\n\n- Provide a meaningful `label` — it is the default accessible name.\n- When a visible label is not appropriate, set `data-aria-label` so the control still has an accessible name.\n- Use `help-text` with `help-text-type=\"error\"` to communicate validation errors; the error icon and `aria-describedby` association are wired automatically."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-staticcheckbox",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, focuses the internal input element on first render.",
          "source": "storybook"
        },
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "description": "selection state. Defaults to `false`. Reflected to the host.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name fallback when no visual label is rendered.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disabled; the host is removed from the tab order and the form value is not submitted.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper or validation message shown below the label.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "radio",
          "default": "false",
          "values": [
            null
          ],
          "description": "`default` (helper) or `error` (validation). When set to `error`, the validation icon is shown next to the help text.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "indeterminate",
          "attribute": "indeterminate",
          "type": "boolean",
          "values": [],
          "description": "mixed state. Defaults to `false`. Reflected to the host. Setting `checked` clears `indeterminate`.",
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label rendered through `mdc-text`. Used as the accessible name.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "form field name submitted with the form.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "non-interactive but focusable; the current value is still submitted with the form.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, an asterisk is appended to the label and the form is invalid unless the checkbox is checked.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "visually disabled but still focusable; suppresses `Space` activation while keeping the control discoverable by assistive technology.",
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "form field value. When the checkbox is checked and no `value` is set, the form receives `\"on\"` (matches native `<input type=\"checkbox\">`).",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) Event that gets dispatched when the checkbox state changes."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Event that gets dispatched when the checkbox receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "checkbox-input",
          "description": "The native checkbox input element that provides the interactive functionality."
        },
        {
          "name": "text-container",
          "description": "The container for the label and helper text elements."
        },
        {
          "name": "static-checkbox",
          "description": "The staticcheckbox that provides the visual checkbox appearance."
        }
      ],
      "cssProperties": [],
      "rules": [
        "selection state. Defaults to `false`. Reflected to the host.",
        "mixed state. Defaults to `false`. Reflected to the host. Setting `checked` clears `indeterminate`.",
        "form field name submitted with the form.",
        "form field value. When the checkbox is checked and no `value` is set, the form receives `\"on\"` (matches native `<input type=\"checkbox\">`).",
        "visible label rendered through `mdc-text`. Used as the accessible name.",
        "accessible name fallback when no visual label is rendered.",
        "helper or validation message shown below the label.",
        "`default` (helper) or `error` (validation). When set to `error`, the validation icon is shown next to the help text.",
        "when `true`, an asterisk is appended to the label and the form is invalid unless the checkbox is checked.",
        "custom message reported through `setCustomValidity` when the checkbox is required but not checked.",
        "fully disabled; the host is removed from the tab order and the form value is not submitted.",
        "visually disabled but still focusable; suppresses `Space` activation while keeping the control discoverable by assistive technology.",
        "non-interactive but focusable; the current value is still submitted with the form.",
        "when `true`, focuses the internal input element on first render."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/checkbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { Checkbox } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-checkbox\n  label=\"Send me product updates\"\n  name=\"newsletter\"\n  value=\"weekly\"\n></mdc-checkbox>"
      }
    ],
    "reactImports": [
      "Checkbox"
    ],
    "constants": {
      "ICON_NAME": {
        "CHECKED": "'check-bold' as Extract<IconNames, 'check-bold'>",
        "INDETERMINATE": "'minus-bold' as Extract<IconNames, 'minus-bold'>"
      },
      "CHECKBOX_VALIDATION": {}
    },
    "keywords": [
      "a",
      "accessibility",
      "agreement",
      "and",
      "button",
      "checkbox",
      "component",
      "control",
      "feature",
      "for",
      "form",
      "guidelines",
      "icon",
      "indicate",
      "lets",
      "mdc",
      "multiple",
      "off",
      "on",
      "one",
      "options",
      "or",
      "overview",
      "select",
      "staticcheckbox",
      "text",
      "that",
      "the",
      "toggle",
      "toggletip",
      "usage",
      "users"
    ]
  },
  {
    "name": "chip",
    "title": "Chip",
    "tagName": "mdc-chip",
    "summary": "Usage, guidelines, and accessibility for the mdc-chip component — a small interactive button-shaped element representing compact info, an attribute, or a quick action with optional icon and label.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/chip",
      "knowledgeBasePath": "components/src/components/chip/knowledge-base/chip.component.md"
    },
    "docs": "## Overview\n\nThe chip is a compact interactive surface that renders an optional leading icon and a short label. It behaves like a button (single tap or `Enter`/`Space` activation, focus, hover, and disabled states) and is commonly used to represent a tag, attribute, filter trigger, or quick action.\n\nKeep the label short — we recommend a maximum of **20 characters** (including spaces). Chips can be connected to an `mdc-tooltip` (via the tooltip's `triggerid` pointing at the chip's `id`) to surface additional context when the label is truncated or needs explanation.\n\n### When to use\n\n- Use `mdc-chip` to represent an attribute, tag, or quick action that fits in a single, short word or phrase.\n- Use it inside a horizontal row or wrap of chips when several short actions or attributes share the same visual weight.\n\n### When not to use\n\n- Use `mdc-button` when the action needs more visual weight, more text, or icon-only treatment beyond a compact tag.\n- Use `mdc-filterchip` when the chip should reflect an applied filter that can be toggled and removed.\n- Use `mdc-alertchip` when the chip should communicate a status (information, warning, error) rather than trigger an action.\n- Use `mdc-staticchip` when the chip is purely decorative and should not be interactive (e.g. inside a list item that owns the click target).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/chip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Chip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-chip label=\"Design\" icon-name=\"brush-bold\"></mdc-chip>\n```\n\nListen for the `click` event (or `keydown`/`keyup` for keyboard activation) to react to chip activations.\n\n### Content guidance\n\n- Keep the `label` short — we recommend up to 20 characters including spaces.\n- Use `icon-name` only when the icon clarifies the chip; an icon-only chip is not supported (a label is required for an accessible name).\n- Pick a `color` token that matches the chip's semantic meaning in your surface; do not rely on color alone to convey meaning.\n\n### Property/Attribute details\n\n- `label` — visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.\n- `icon-name` — optional leading icon, rendered through `mdc-icon`. When omitted, no icon is rendered.\n- `color` — visual treatment. One of `default` (default), `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`.\n- `disabled` — when `true`, the chip is non-interactive and removed from the tab order.\n- `autoFocusOnMount` — when `true`, focuses the chip on first render.\n\n### Limitations\n\n- The chip is not a toggle: `active` and `soft-disabled` are explicitly reset on connect and should not be set. Use `mdc-filterchip` if a toggle is needed.\n- The chip has a fixed size — the underlying button size is locked to 24, so chips share the same compact height regardless of label length.\n\n## Accessibility\n\n### Built-in features\n\nThe host renders with `role=\"button\"` and a single tab stop. The component owns keyboard activation:\n\n- `Enter` activates the chip on `keydown` (matches native button behaviour).\n- `Space` activates the chip on `keyup` (matches native button behaviour; `keydown` is suppressed so the page does not scroll).\n- Click activates the chip.\n\nWhen `disabled` is `true`, click and keyboard activation are suppressed and the host is removed from the tab order.\n\nThe `label` provides the accessible name. The leading icon is decorative and is not announced.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute   | Value                                  |\n| ------- | ----------- | -------------------------------------- |\n| Host    | `role`      | `button`                               |\n| Host    | `tabindex`  | `0` when enabled; `-1` when `disabled` |\n\n### Implementation requirements\n\n#### General\n\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be interactive, so the state is exposed to assistive technology and removed from the tab order.\n- Do not rely on `color` alone to convey meaning — pair color with a label that already states the semantic.\n\n#### Labelling\n\n- Provide a meaningful `label` — it is the accessible name of the chip and is mandatory for screen-reader users.\n- When the chip is truncated, connect an `mdc-tooltip` (via the tooltip's `triggerid` pointing at the chip's `id`) so the full label remains discoverable.",
    "sections": {
      "Overview": "The chip is a compact interactive surface that renders an optional leading icon and a short label. It behaves like a button (single tap or `Enter`/`Space` activation, focus, hover, and disabled states) and is commonly used to represent a tag, attribute, filter trigger, or quick action.\n\nKeep the label short — we recommend a maximum of **20 characters** (including spaces). Chips can be connected to an `mdc-tooltip` (via the tooltip's `triggerid` pointing at the chip's `id`) to surface additional context when the label is truncated or needs explanation.\n\n### When to use\n\n- Use `mdc-chip` to represent an attribute, tag, or quick action that fits in a single, short word or phrase.\n- Use it inside a horizontal row or wrap of chips when several short actions or attributes share the same visual weight.\n\n### When not to use\n\n- Use `mdc-button` when the action needs more visual weight, more text, or icon-only treatment beyond a compact tag.\n- Use `mdc-filterchip` when the chip should reflect an applied filter that can be toggled and removed.\n- Use `mdc-alertchip` when the chip should communicate a status (information, warning, error) rather than trigger an action.\n- Use `mdc-staticchip` when the chip is purely decorative and should not be interactive (e.g. inside a list item that owns the click target).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/chip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Chip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-chip label=\"Design\" icon-name=\"brush-bold\"></mdc-chip>\n```\n\nListen for the `click` event (or `keydown`/`keyup` for keyboard activation) to react to chip activations.\n\n### Content guidance\n\n- Keep the `label` short — we recommend up to 20 characters including spaces.\n- Use `icon-name` only when the icon clarifies the chip; an icon-only chip is not supported (a label is required for an accessible name).\n- Pick a `color` token that matches the chip's semantic meaning in your surface; do not rely on color alone to convey meaning.\n\n### Property/Attribute details\n\n- `label` — visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.\n- `icon-name` — optional leading icon, rendered through `mdc-icon`. When omitted, no icon is rendered.\n- `color` — visual treatment. One of `default` (default), `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`.\n- `disabled` — when `true`, the chip is non-interactive and removed from the tab order.\n- `autoFocusOnMount` — when `true`, focuses the chip on first render.\n\n### Limitations\n\n- The chip is not a toggle: `active` and `soft-disabled` are explicitly reset on connect and should not be set. Use `mdc-filterchip` if a toggle is needed.\n- The chip has a fixed size — the underlying button size is locked to 24, so chips share the same compact height regardless of label length.",
      "Accessibility": "### Built-in features\n\nThe host renders with `role=\"button\"` and a single tab stop. The component owns keyboard activation:\n\n- `Enter` activates the chip on `keydown` (matches native button behaviour).\n- `Space` activates the chip on `keyup` (matches native button behaviour; `keydown` is suppressed so the page does not scroll).\n- Click activates the chip.\n\nWhen `disabled` is `true`, click and keyboard activation are suppressed and the host is removed from the tab order.\n\nThe `label` provides the accessible name. The leading icon is decorative and is not announced.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute   | Value                                  |\n| ------- | ----------- | -------------------------------------- |\n| Host    | `role`      | `button`                               |\n| Host    | `tabindex`  | `0` when enabled; `-1` when `disabled` |\n\n### Implementation requirements\n\n#### General\n\n- Use `disabled` (not `aria-hidden` or visual cues alone) when an option must not be interactive, so the state is exposed to assistive technology and removed from the tab order.\n- Do not rely on `color` alone to convey meaning — pair color with a label that already states the semantic.\n\n#### Labelling\n\n- Provide a meaningful `label` — it is the accessible name of the chip and is mandatory for screen-reader users.\n- When the chip is truncated, connect an `mdc-tooltip` (via the tooltip's `triggerid` pointing at the chip's `id`) so the full label remains discoverable."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaStateKey",
          "attribute": "aria-state-key",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, focuses the chip on first render.",
          "source": "storybook"
        },
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "default": "default",
          "values": [],
          "description": "visual treatment. One of `default` (default), `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the chip is non-interactive and removed from the tab order.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "optional leading icon, rendered through `mdc-icon`. When omitted, no icon is rendered.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the chip is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the chip."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the chip."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the chip receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "icon",
          "description": "The icon part of the chip."
        },
        {
          "name": "label",
          "description": "The label part of the chip."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-chip-color",
          "description": "The color of the chip."
        },
        {
          "name": "--mdc-chip-border-color",
          "description": "The border color of the chip."
        },
        {
          "name": "--mdc-chip-background-color",
          "description": "The background color of the chip."
        }
      ],
      "rules": [
        "visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.",
        "optional leading icon, rendered through `mdc-icon`. When omitted, no icon is rendered.",
        "visual treatment. One of `default` (default), `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`.",
        "when `true`, the chip is non-interactive and removed from the tab order.",
        "when `true`, focuses the chip on first render."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/chip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Chip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-chip label=\"Design\" icon-name=\"brush-bold\"></mdc-chip>"
      }
    ],
    "reactImports": [
      "Chip"
    ],
    "constants": {
      "DEFAULTS": {}
    },
    "keywords": [
      "a",
      "accessibility",
      "action",
      "an",
      "and",
      "attribute",
      "button",
      "chip",
      "compact",
      "component",
      "element",
      "for",
      "guidelines",
      "icon",
      "info",
      "interactive",
      "label",
      "mdc",
      "optional",
      "or",
      "overview",
      "quick",
      "representing",
      "shaped",
      "small",
      "text",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "coachmark",
    "title": "Coachmark",
    "tagName": "mdc-coachmark",
    "summary": "Usage, guidelines, and accessibility for the mdc-coachmark component — a contextual onboarding overlay that highlights a UI element with an arrow-anchored bubble to explain its purpose.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/coachmark",
      "knowledgeBasePath": "components/src/components/coachmark/knowledge-base/coachmark.component.md"
    },
    "docs": "## Overview\n\nA coachmark is a contextual guidance overlay used to highlight a specific UI element and explain its purpose. It is typically shown as part of onboarding, feature discovery, or step-by-step walkthroughs.\n\nIt renders as a high-contrast bubble anchored to a trigger element with a visible arrow and a close button. Unlike a tooltip, the coachmark stays open until the user dismisses it (or the consumer toggles it programmatically), making it suitable for explanatory copy rather than a quick hover hint.\n\n### When to use\n\n- Introducing a new or complex feature when users land on a screen for the first time.\n- Drawing attention to a UI element that might otherwise be overlooked (a secondary action, a settings entry point).\n- Providing step-by-step guidance in an onboarding flow or guided tour.\n\n### When not to use\n\n- Use `mdc-tooltip` for a short, hover-triggered hint about a control that does not need a close affordance.\n- Use `mdc-toggletip` when a small piece of contextual help should appear on click and dismiss on outside interaction without a persistent step-flow.\n- Use `mdc-popover` directly when a generic dismissible overlay is needed and the onboarding-styled contrast bubble + arrow + close button defaults are not desired.\n- Use `mdc-dialog` (or `mdc-announcementdialog`) when the message is modal, must block the rest of the UI, or is not anchored to a specific element.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/coachmark/index.js\"; // custom element registration\n// or via React wrapper\nimport { Coachmark } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button id=\"filters-trigger\">Filters</mdc-button>\n\n<mdc-coachmark triggerid=\"filters-trigger\" placement=\"bottom\" visible>\n  <p>Filter results to find what you need faster.</p>\n</mdc-coachmark>\n```\n\nSet `visible` to show the coachmark and clear it to hide it. Listen for `shown`, `hidden`, `created`, and `destroyed` to drive your walkthrough state machine.\n\nReact-only quirk: when a coachmark uses `append-to`, the component moves itself out of the React-managed DOM. Wrap the coachmark in a non-Fragment element (e.g. a `<div>`) when conditionally rendering it, otherwise React may throw `NotFoundError` on unmount.\n\n### Content guidance\n\n- Keep the body short — coachmarks are scanned, not read; a single sentence explaining the highlighted element is usually enough.\n- Always anchor a coachmark to the element it is describing via `triggerid`; an unanchored coachmark is confusing for sighted and screen-reader users alike.\n- If the coachmark is part of a sequence, provide the user a clear way to advance (a primary button inside the slot) and a way to dismiss the whole sequence.\n\n### Property/Attribute details\n\nThe coachmark exposes the same overlay surface as `mdc-popover` with onboarding-appropriate defaults. Key properties:\n\n- `trigger` — events that open the coachmark. `manual` (default — open via the `visible` property), `click`, `mouseenter`, `focusin`, or any space-separated combination.\n- `show-arrow` — when `true` (default), renders the anchor arrow.\n- `close-button` — when `true` (default), renders a close button inside the bubble.\n- `disable-aria-expanded` — when `true` (default), the trigger does not toggle `aria-expanded` / `aria-haspopup` when the coachmark opens.\n- `visible` — controls visibility imperatively when `trigger` is `manual`.\n- `triggerid` — id of the element the coachmark is anchored to.\n- `placement` — preferred placement around the trigger (e.g. `top`, `bottom`, `left`, `right`, and `*-start` / `*-end` variants). The component auto-flips when there is not enough room.\n- `offset`, `strategy`, `boundary` — fine-grained positioning, identical to `mdc-popover`.\n- `role` — defaults to `dialog`; override when a different role better describes the coachmark's purpose.\n- `aria-label` / `aria-labelledby` — accessible name for the coachmark surface.\n\n### Limitations\n\n- The color is always `contrast` and the arrow is always visible — coachmarks are deliberately consistent so users learn to recognise them across the product.\n- The trigger does not toggle `aria-expanded` / `aria-haspopup` by default. If multiple overlays share the same trigger and one of them should expose those attributes, leave `disable-aria-expanded` enabled on every coachmark and let the other overlay own the announcement.\n- Onboarding sequences need to be coordinated by the consumer — the component does not own state for \"step 2 of 5\" navigation.\n\n## Accessibility\n\n### Built-in features\n\nThe coachmark renders with `role=\"dialog\"` and is positioned with [Floating UI](https://floating-ui.com/) relative to the trigger. While visible:\n\n- The close button is focusable and dismisses the coachmark on activation.\n- `Escape` closes the coachmark when `hide-on-escape` is enabled (inherited default from popover).\n- Outside click closes the coachmark when `hide-on-outside-click` is enabled (inherited default from popover).\n- An optional `focus-trap` can be enabled to keep keyboard focus inside the coachmark while open; when disabled, focus stays where the user left it.\n- The `shown`, `hidden`, `created`, and `destroyed` events allow the consumer to drive a walkthrough or restore focus after dismissal.\n\nThe bubble is high-contrast and arrow-anchored to the trigger, making the spatial relationship explicit for sighted users; screen-reader users get the role and accessible name announcement when the coachmark opens.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute          | Value                                                              |\n| -------------- | ------------------ | ------------------------------------------------------------------ |\n| Host (overlay) | `role`             | `dialog` (default; override via `role`)                            |\n| Host (overlay) | `aria-modal`       | `true` when the focus trap is active                               |\n| Host (overlay) | `aria-labelledby`  | id of the labelling element, when `aria-labelledby` is set         |\n| Host (overlay) | `aria-label`       | mirrors `aria-label` when set                                      |\n| Trigger        | `aria-expanded`    | not set (suppressed by `disable-aria-expanded`)                    |\n| Trigger        | `aria-haspopup`    | not set (suppressed by `disable-aria-expanded`)                    |\n\n### Implementation requirements\n\n#### General\n\n- Anchor every coachmark to a specific trigger via `triggerid` and place the coachmark close to the element it describes.\n- Coordinate sequences from the consumer side: show one coachmark at a time, restore focus to a sensible element after dismissal, and provide a way to skip the whole flow.\n- If multiple popovers share the same trigger, keep `disable-aria-expanded` enabled on every coachmark and let exactly one overlay drive `aria-expanded` / `aria-haspopup` on the trigger.\n\n#### Labelling\n\n- Provide an `aria-label` or `aria-labelledby` so the coachmark announces a meaningful name when it opens; the default `role=\"dialog\"` requires it.\n- If the coachmark's title is rendered inside its content, use `aria-labelledby` pointing at that element rather than repeating the text in `aria-label`.",
    "sections": {
      "Overview": "A coachmark is a contextual guidance overlay used to highlight a specific UI element and explain its purpose. It is typically shown as part of onboarding, feature discovery, or step-by-step walkthroughs.\n\nIt renders as a high-contrast bubble anchored to a trigger element with a visible arrow and a close button. Unlike a tooltip, the coachmark stays open until the user dismisses it (or the consumer toggles it programmatically), making it suitable for explanatory copy rather than a quick hover hint.\n\n### When to use\n\n- Introducing a new or complex feature when users land on a screen for the first time.\n- Drawing attention to a UI element that might otherwise be overlooked (a secondary action, a settings entry point).\n- Providing step-by-step guidance in an onboarding flow or guided tour.\n\n### When not to use\n\n- Use `mdc-tooltip` for a short, hover-triggered hint about a control that does not need a close affordance.\n- Use `mdc-toggletip` when a small piece of contextual help should appear on click and dismiss on outside interaction without a persistent step-flow.\n- Use `mdc-popover` directly when a generic dismissible overlay is needed and the onboarding-styled contrast bubble + arrow + close button defaults are not desired.\n- Use `mdc-dialog` (or `mdc-announcementdialog`) when the message is modal, must block the rest of the UI, or is not anchored to a specific element.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/coachmark/index.js\"; // custom element registration\n// or via React wrapper\nimport { Coachmark } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button id=\"filters-trigger\">Filters</mdc-button>\n\n<mdc-coachmark triggerid=\"filters-trigger\" placement=\"bottom\" visible>\n  <p>Filter results to find what you need faster.</p>\n</mdc-coachmark>\n```\n\nSet `visible` to show the coachmark and clear it to hide it. Listen for `shown`, `hidden`, `created`, and `destroyed` to drive your walkthrough state machine.\n\nReact-only quirk: when a coachmark uses `append-to`, the component moves itself out of the React-managed DOM. Wrap the coachmark in a non-Fragment element (e.g. a `<div>`) when conditionally rendering it, otherwise React may throw `NotFoundError` on unmount.\n\n### Content guidance\n\n- Keep the body short — coachmarks are scanned, not read; a single sentence explaining the highlighted element is usually enough.\n- Always anchor a coachmark to the element it is describing via `triggerid`; an unanchored coachmark is confusing for sighted and screen-reader users alike.\n- If the coachmark is part of a sequence, provide the user a clear way to advance (a primary button inside the slot) and a way to dismiss the whole sequence.\n\n### Property/Attribute details\n\nThe coachmark exposes the same overlay surface as `mdc-popover` with onboarding-appropriate defaults. Key properties:\n\n- `trigger` — events that open the coachmark. `manual` (default — open via the `visible` property), `click`, `mouseenter`, `focusin`, or any space-separated combination.\n- `show-arrow` — when `true` (default), renders the anchor arrow.\n- `close-button` — when `true` (default), renders a close button inside the bubble.\n- `disable-aria-expanded` — when `true` (default), the trigger does not toggle `aria-expanded` / `aria-haspopup` when the coachmark opens.\n- `visible` — controls visibility imperatively when `trigger` is `manual`.\n- `triggerid` — id of the element the coachmark is anchored to.\n- `placement` — preferred placement around the trigger (e.g. `top`, `bottom`, `left`, `right`, and `*-start` / `*-end` variants). The component auto-flips when there is not enough room.\n- `offset`, `strategy`, `boundary` — fine-grained positioning, identical to `mdc-popover`.\n- `role` — defaults to `dialog`; override when a different role better describes the coachmark's purpose.\n- `aria-label` / `aria-labelledby` — accessible name for the coachmark surface.\n\n### Limitations\n\n- The color is always `contrast` and the arrow is always visible — coachmarks are deliberately consistent so users learn to recognise them across the product.\n- The trigger does not toggle `aria-expanded` / `aria-haspopup` by default. If multiple overlays share the same trigger and one of them should expose those attributes, leave `disable-aria-expanded` enabled on every coachmark and let the other overlay own the announcement.\n- Onboarding sequences need to be coordinated by the consumer — the component does not own state for \"step 2 of 5\" navigation.",
      "Accessibility": "### Built-in features\n\nThe coachmark renders with `role=\"dialog\"` and is positioned with [Floating UI](https://floating-ui.com/) relative to the trigger. While visible:\n\n- The close button is focusable and dismisses the coachmark on activation.\n- `Escape` closes the coachmark when `hide-on-escape` is enabled (inherited default from popover).\n- Outside click closes the coachmark when `hide-on-outside-click` is enabled (inherited default from popover).\n- An optional `focus-trap` can be enabled to keep keyboard focus inside the coachmark while open; when disabled, focus stays where the user left it.\n- The `shown`, `hidden`, `created`, and `destroyed` events allow the consumer to drive a walkthrough or restore focus after dismissal.\n\nThe bubble is high-contrast and arrow-anchored to the trigger, making the spatial relationship explicit for sighted users; screen-reader users get the role and accessible name announcement when the coachmark opens.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute          | Value                                                              |\n| -------------- | ------------------ | ------------------------------------------------------------------ |\n| Host (overlay) | `role`             | `dialog` (default; override via `role`)                            |\n| Host (overlay) | `aria-modal`       | `true` when the focus trap is active                               |\n| Host (overlay) | `aria-labelledby`  | id of the labelling element, when `aria-labelledby` is set         |\n| Host (overlay) | `aria-label`       | mirrors `aria-label` when set                                      |\n| Trigger        | `aria-expanded`    | not set (suppressed by `disable-aria-expanded`)                    |\n| Trigger        | `aria-haspopup`    | not set (suppressed by `disable-aria-expanded`)                    |\n\n### Implementation requirements\n\n#### General\n\n- Anchor every coachmark to a specific trigger via `triggerid` and place the coachmark close to the element it describes.\n- Coordinate sequences from the consumer side: show one coachmark at a time, restore focus to a sensible element after dismissal, and provide a way to skip the whole flow.\n- If multiple popovers share the same trigger, keep `disable-aria-expanded` enabled on every coachmark and let exactly one overlay drive `aria-expanded` / `aria-haspopup` on the trigger.\n\n#### Labelling\n\n- Provide an `aria-label` or `aria-labelledby` so the coachmark announces a meaningful name when it opens; the default `role=\"dialog\"` requires it.\n- If the coachmark's title is rendered inside its content, use `aria-labelledby` pointing at that element rather than repeating the text in `aria-label`."
    },
    "dependencies": [
      "mdc-popover"
    ],
    "api": {
      "properties": [
        {
          "name": "closeButton",
          "attribute": "close-button",
          "type": "boolean",
          "default": "true",
          "values": [],
          "description": "when `true` (default), renders a close button inside the bubble.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "disableAriaExpanded",
          "attribute": "disable-aria-expanded",
          "type": "boolean",
          "default": "true",
          "values": [],
          "description": "when `true` (default), the trigger does not toggle `aria-expanded` / `aria-haspopup` when the coachmark opens.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "showArrow",
          "attribute": "show-arrow",
          "type": "boolean",
          "default": "true",
          "values": [],
          "description": "when `true` (default), renders the anchor arrow.",
          "reflected": false,
          "source": "component"
        },
        {
          "name": "trigger",
          "attribute": "trigger",
          "type": "PopoverTrigger",
          "default": "manual",
          "values": [],
          "description": "events that open the coachmark. `manual` (default — open via the `visible` property), `click`, `mouseenter`, `focusin`, or any space-separated combination.",
          "reflected": true,
          "source": "component"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for modal container"
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) This event is dispatched when the coachmark is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) This event is dispatched when the coachmark is hidden"
        },
        {
          "name": "created",
          "description": "(React: onCreated) This event is dispatched when the coachmark is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) This event is dispatched when the coachmark is"
        }
      ],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-popover-arrow-border-radius",
          "description": "radius of the arrow border"
        },
        {
          "name": "--mdc-popover-arrow-border",
          "description": "border of the arrow"
        },
        {
          "name": "--mdc-popover-primary-background-color",
          "description": "primary background color of the popover"
        },
        {
          "name": "--mdc-popover-border-color",
          "description": "border color of the popover"
        },
        {
          "name": "--mdc-popover-inverted-background-color",
          "description": "inverted background color of the popover"
        },
        {
          "name": "--mdc-popover-inverted-border-color",
          "description": "inverted border color of the popover"
        },
        {
          "name": "--mdc-popover-inverted-text-color",
          "description": "inverted text color of the popover"
        },
        {
          "name": "--mdc-popover-elevation-3",
          "description": "elevation of the popover"
        }
      ],
      "rules": [
        "events that open the coachmark. `manual` (default — open via the `visible` property), `click`, `mouseenter`, `focusin`, or any space-separated combination.",
        "when `true` (default), renders the anchor arrow.",
        "when `true` (default), renders a close button inside the bubble.",
        "when `true` (default), the trigger does not toggle `aria-expanded` / `aria-haspopup` when the coachmark opens.",
        "controls visibility imperatively when `trigger` is `manual`.",
        "id of the element the coachmark is anchored to.",
        "preferred placement around the trigger (e.g. `top`, `bottom`, `left`, `right`, and `*-start` / `*-end` variants). The component auto-flips when there is not enough room.",
        "defaults to `dialog`; override when a different role better describes the coachmark's purpose.",
        "accessible name for the coachmark surface.",
        "accessible name for the coachmark surface."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/coachmark/index.js\"; // custom element registration\n// or via React wrapper\nimport { Coachmark } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"filters-trigger\">Filters</mdc-button>\n\n<mdc-coachmark triggerid=\"filters-trigger\" placement=\"bottom\" visible>\n  <p>Filter results to find what you need faster.</p>\n</mdc-coachmark>"
      }
    ],
    "reactImports": [
      "Coachmark"
    ],
    "constants": {
      "DEFAULTS": {
        "ARROW": true,
        "CLOSE_BUTTON": true,
        "DISABLE_ARIA_EXPANDED": true
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "an",
      "anchored",
      "and",
      "arrow",
      "bubble",
      "coachmark",
      "component",
      "contextual",
      "element",
      "explain",
      "for",
      "guidelines",
      "highlights",
      "its",
      "mdc",
      "onboarding",
      "overlay",
      "overview",
      "popover",
      "purpose",
      "that",
      "the",
      "to",
      "ui",
      "usage",
      "with"
    ]
  },
  {
    "name": "combobox",
    "title": "Combobox",
    "tagName": "mdc-combobox",
    "summary": "Usage, guidelines, and accessibility for the mdc-combobox component — a text-based dropdown that lets users either type to filter or pick from a predefined list of options.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/combobox",
      "knowledgeBasePath": "components/src/components/combobox/knowledge-base/combobox.component.md"
    },
    "docs": "## Overview\n\nThe combobox is a text-based dropdown control that lets the user type to filter a predefined list of options, then pick one. Typing narrows the visible options based on the configured `filter` strategy; clicking the trigger button or pressing arrow keys opens the dropdown without filtering.\n\nThe component is form-associated and participates in native form submission, validation, and reset.\n\nIt is designed to be used with `mdc-option` (individual options) and `mdc-optgroup` (grouped options) placed inside an `mdc-selectlistbox` slotted child — `mdc-selectlistbox` owns the `role=\"listbox\"` and is required for correct screen-reader behaviour.\n\nWhen the consumer needs to drive the value externally (e.g. server-side filtering), set `control-type=\"controlled\"` on a wrapping `mdc-controltypeprovider` (or use `filter=\"none\"`): the combobox will emit `input` and `change` events but will not mutate its own `value` — the parent component is expected to update `value` to reflect the selection.\n\n### When to use\n\n- Use `mdc-combobox` when the user must pick a single value from a long list and typing to filter is faster than scanning.\n- Use it when a predictive \"starts with\" filter (or a custom filter function) genuinely helps the user — short lists rarely justify a combobox.\n- Use it when the option list is fetched dynamically (set `filter=\"none\"` and update slotted options as data arrives).\n\n### When not to use\n\n- Use `mdc-select` when the user should only pick from a fixed list without typing.\n- Use `mdc-searchfield` when the user is searching for content rather than picking a single value from a known set.\n- Use `mdc-input` when free-form text is the goal and no constrained option list applies.\n- Use `mdc-radiogroup` when there are only a handful of options and they should all be visible at once.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/combobox/index.js\"; // custom element registration\n// or via React wrapper\nimport { Combobox, SelectListBox, Option, OptGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-combobox label=\"Country\" name=\"country\" placeholder=\"Start typing…\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"ch\" label=\"Switzerland\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-combobox>\n```\n\nListen for `change` to react to a committed selection and `input` to react to every keystroke or filter change.\n\nStructural requirements:\n\n- Slot every `mdc-option` (and `mdc-optgroup`) inside `mdc-selectlistbox`. `mdc-selectlistbox` owns the `role=\"listbox\"` and is required for correct screen-reader behaviour; placing `mdc-option` directly inside the combobox breaks listbox semantics.\n- Give every `mdc-option` a unique `value` — form submission, programmatic `value` updates, and \"starts with\" filtering all depend on it.\n- Place per-option `mdc-tooltip` siblings **outside** `mdc-selectlistbox` and link them via `triggerid` pointing at the option's `id`. Tooltips slotted inside the listbox break announcement and focus order.\n\nFiltering modes:\n\n- Default (`filter=\"match-starts-with\"`) — options whose label starts with the typed text are shown.\n- Custom function (`filter={(option, inputValue) => boolean}`) — full control over the match logic, useful for multi-token or fuzzy matching.\n- External (`filter=\"none\"`) — the combobox does not filter; update the slotted options as data arrives (e.g. from an API).\n\n### Content guidance\n\n- Provide a `label` describing what the user is selecting; reach for `data-aria-label` only when a visible label is not possible.\n- Set `placeholder` to a short hint about the expected input (\"Start typing a country…\"), not as a replacement for the label.\n- Set `no-result-text` to a short message shown when filtering excludes every option (\"No matches\"). When omitted, the dropdown closes silently on empty results.\n- Set `invalid-custom-value-text` to a short validation message used when the user types a value that does not match any option, leaves the field, and has never committed a value (e.g. \"Pick a value from the list\").\n\n### Property/Attribute details\n\n- `value` — the value of the selected option. Setting this attribute updates the visible selection without firing `change`.\n- `name` — form field name submitted with the form.\n- `placeholder` — placeholder text shown when no value is selected.\n- `placement` — popover placement relative to the input. `bottom-start` (default) or `top-start`.\n- `filter` — filter strategy: `match-starts-with` (default), `none` (no filtering), or a custom `(option, inputValue) => boolean` function. Only string values are reflected as the `filter` attribute.\n- `no-result-text` — text shown inside the dropdown when no options match the filter; omit to hide the dropdown silently.\n- `invalid-custom-value-text` — error message used when the user enters a value that does not match any option and the field has no previously committed value.\n- `boundary` — clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Used together with `strategy` to keep the popover visible inside scrollable parents.\n- `strategy` — popover positioning strategy. `absolute` (default) or `fixed`. Use `fixed` together with a non-default `boundary` to avoid clipping inside scroll containers.\n- `popover-z-index` — z-index override for the popover.\n- `backdrop-append-to` — id of the element the popover backdrop is appended to (defaults to the combobox's parent).\n- `label` — visible label rendered above the field. Used as the accessible name when set.\n- `data-aria-label` — accessible name fallback when no visual label is rendered.\n- `help-text` — helper or validation text below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n- `required` — when `true`, the form is invalid unless a value is selected.\n- `validation-message` — custom validation message reported through `setCustomValidity` when the combobox is required but empty.\n- `disabled` — fully disabled; the host is removed from the tab order and the popover cannot open.\n- `readonly` — non-interactive but focusable; the popover cannot open and typing is suppressed, but the current value is still submitted.\n- `control-type` — when set to `controlled` (via `mdc-controltypeprovider`), the combobox emits `input` / `change` but does not update its own `value`; the parent must drive `value` in response.\n- `autoFocusOnMount` — when `true`, focuses the visible combobox input on first render.\n\n## Accessibility\n\n### Built-in features\n\nThe component implements the [WAI-ARIA combobox autocomplete-list pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-autocomplete-list/). The visible input has `role=\"combobox\"`, the slotted `mdc-selectlistbox` provides `role=\"listbox\"`, and the combobox wires `aria-controls`, `aria-expanded`, `aria-haspopup`, and `aria-activedescendant`-style focus highlighting (via `data-focused` on the option) between them.\n\nKeyboard interaction:\n\n- `ArrowDown` / `ArrowUp` opens the dropdown (if closed) and moves the visual highlight to the next/previous enabled option (wraps at the ends).\n- `Enter` selects the highlighted option, closes the dropdown, and dispatches `change`. In spatial navigation mode, `Enter` first opens the dropdown.\n- `Escape` clears the highlighted option. If no options are visible (and no `no-result-text`), it also clears the typed input.\n- `Tab` closes the dropdown and moves focus to the next focusable element.\n- `Home` / `End` clear the highlighted option (the native input then handles caret movement).\n- Typing filters the visible options according to `filter`, opens the dropdown if closed, and dispatches `input`.\n\nBlur behaviour: on blur, the combobox commits the currently highlighted option if any, otherwise the typed text if it exactly matches an option label, otherwise reverts to the last committed value, otherwise (if `invalid-custom-value-text` is set) shows the error message.\n\nWhen `disabled` is `true`, the input is removed from the tab order and the dropdown cannot open. When `readonly` is `true`, the input remains focusable, the dropdown cannot open, and the form value is still submitted.\n\nThe `label` (or `data-aria-label`) provides the accessible name. `help-text` is linked via `aria-describedby`.\n\n#### Internal ARIA managed by the component\n\n| Element                | Attribute              | Value                                                                                  |\n| ---------------------- | ---------------------- | -------------------------------------------------------------------------------------- |\n| Visible input          | `role`                 | `combobox`                                                                             |\n| Visible input          | `aria-haspopup`        | `listbox`                                                                              |\n| Visible input          | `aria-autocomplete`    | `list`                                                                                 |\n| Visible input          | `aria-expanded`        | `true` when the dropdown is open, otherwise `false`                                    |\n| Visible input          | `aria-controls`        | id of the slotted listbox                                                              |\n| Visible input          | `aria-invalid`         | `true` when `help-text-type=\"error\"`, otherwise `false`                                |\n| Visible input          | `aria-required`        | mirrors `required`                                                                     |\n| Visible input          | `aria-readonly`        | mirrors `readonly`                                                                     |\n| Visible input          | `aria-disabled`        | mirrors `disabled`                                                                     |\n| Visible input          | `aria-label`           | mirrors `data-aria-label`                                                              |\n| Visible input          | `aria-labelledby`      | id of the label when `label` is set                                                    |\n| Visible input          | `aria-describedby`     | id of the help text when `help-text` is set                                            |\n| Visible input          | `tabindex`             | `0` when enabled; `-1` when `disabled`                                                 |\n| Trigger button         | `aria-expanded`        | mirrors the dropdown open state                                                        |\n| Trigger button         | `aria-label`           | mirrors `data-aria-label`                                                              |\n| Trigger button         | `tabindex`             | `-1` (not in the tab order; the input is the single tab stop)                          |\n| Slotted listbox        | `id`                   | generated id linked from the input's `aria-controls`                                   |\n| Slotted listbox        | `aria-label`           | mirrors `label` or `data-aria-label`                                                   |\n| Focused option         | `data-focused`         | present while the option is the visual active descendant                               |\n| Focused option         | `aria-selected`        | `true` while highlighted, `false` otherwise                                            |\n| Native hidden input    | `aria-hidden`          | `true` (only used to participate in form submission)                                   |\n\n### Implementation requirements\n\n#### General\n\n- Wrap options in `mdc-selectlistbox`. Without it, the listbox role and option focus semantics are missing.\n- Place per-option `mdc-tooltip` siblings outside `mdc-selectlistbox` and link them via `triggerid` so they do not break listbox navigation.\n- Set a unique `value` on every `mdc-option`; form submission and value-based selection rely on it.\n- When filtering is driven externally (API, server), set `filter=\"none\"` and update slotted options as data arrives — the combobox will not filter on its own.\n- When using `control-type=\"controlled\"`, listen to `input` and `change` and drive the `value` attribute from the parent; the combobox will not mutate its own value.\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`) — `role=\"combobox\"` requires an accessible name.\n- Use `help-text` with `help-text-type=\"error\"` for validation feedback; the icon and `aria-describedby` association are wired automatically.\n- Pair `no-result-text` and `invalid-custom-value-text` thoughtfully: the first describes \"nothing matches your filter\", the second describes \"what you typed is not a valid option\". Pick wording that helps the user recover.",
    "sections": {
      "Overview": "The combobox is a text-based dropdown control that lets the user type to filter a predefined list of options, then pick one. Typing narrows the visible options based on the configured `filter` strategy; clicking the trigger button or pressing arrow keys opens the dropdown without filtering.\n\nThe component is form-associated and participates in native form submission, validation, and reset.\n\nIt is designed to be used with `mdc-option` (individual options) and `mdc-optgroup` (grouped options) placed inside an `mdc-selectlistbox` slotted child — `mdc-selectlistbox` owns the `role=\"listbox\"` and is required for correct screen-reader behaviour.\n\nWhen the consumer needs to drive the value externally (e.g. server-side filtering), set `control-type=\"controlled\"` on a wrapping `mdc-controltypeprovider` (or use `filter=\"none\"`): the combobox will emit `input` and `change` events but will not mutate its own `value` — the parent component is expected to update `value` to reflect the selection.\n\n### When to use\n\n- Use `mdc-combobox` when the user must pick a single value from a long list and typing to filter is faster than scanning.\n- Use it when a predictive \"starts with\" filter (or a custom filter function) genuinely helps the user — short lists rarely justify a combobox.\n- Use it when the option list is fetched dynamically (set `filter=\"none\"` and update slotted options as data arrives).\n\n### When not to use\n\n- Use `mdc-select` when the user should only pick from a fixed list without typing.\n- Use `mdc-searchfield` when the user is searching for content rather than picking a single value from a known set.\n- Use `mdc-input` when free-form text is the goal and no constrained option list applies.\n- Use `mdc-radiogroup` when there are only a handful of options and they should all be visible at once.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/combobox/index.js\"; // custom element registration\n// or via React wrapper\nimport { Combobox, SelectListBox, Option, OptGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-combobox label=\"Country\" name=\"country\" placeholder=\"Start typing…\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"ch\" label=\"Switzerland\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-combobox>\n```\n\nListen for `change` to react to a committed selection and `input` to react to every keystroke or filter change.\n\nStructural requirements:\n\n- Slot every `mdc-option` (and `mdc-optgroup`) inside `mdc-selectlistbox`. `mdc-selectlistbox` owns the `role=\"listbox\"` and is required for correct screen-reader behaviour; placing `mdc-option` directly inside the combobox breaks listbox semantics.\n- Give every `mdc-option` a unique `value` — form submission, programmatic `value` updates, and \"starts with\" filtering all depend on it.\n- Place per-option `mdc-tooltip` siblings **outside** `mdc-selectlistbox` and link them via `triggerid` pointing at the option's `id`. Tooltips slotted inside the listbox break announcement and focus order.\n\nFiltering modes:\n\n- Default (`filter=\"match-starts-with\"`) — options whose label starts with the typed text are shown.\n- Custom function (`filter={(option, inputValue) => boolean}`) — full control over the match logic, useful for multi-token or fuzzy matching.\n- External (`filter=\"none\"`) — the combobox does not filter; update the slotted options as data arrives (e.g. from an API).\n\n### Content guidance\n\n- Provide a `label` describing what the user is selecting; reach for `data-aria-label` only when a visible label is not possible.\n- Set `placeholder` to a short hint about the expected input (\"Start typing a country…\"), not as a replacement for the label.\n- Set `no-result-text` to a short message shown when filtering excludes every option (\"No matches\"). When omitted, the dropdown closes silently on empty results.\n- Set `invalid-custom-value-text` to a short validation message used when the user types a value that does not match any option, leaves the field, and has never committed a value (e.g. \"Pick a value from the list\").\n\n### Property/Attribute details\n\n- `value` — the value of the selected option. Setting this attribute updates the visible selection without firing `change`.\n- `name` — form field name submitted with the form.\n- `placeholder` — placeholder text shown when no value is selected.\n- `placement` — popover placement relative to the input. `bottom-start` (default) or `top-start`.\n- `filter` — filter strategy: `match-starts-with` (default), `none` (no filtering), or a custom `(option, inputValue) => boolean` function. Only string values are reflected as the `filter` attribute.\n- `no-result-text` — text shown inside the dropdown when no options match the filter; omit to hide the dropdown silently.\n- `invalid-custom-value-text` — error message used when the user enters a value that does not match any option and the field has no previously committed value.\n- `boundary` — clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Used together with `strategy` to keep the popover visible inside scrollable parents.\n- `strategy` — popover positioning strategy. `absolute` (default) or `fixed`. Use `fixed` together with a non-default `boundary` to avoid clipping inside scroll containers.\n- `popover-z-index` — z-index override for the popover.\n- `backdrop-append-to` — id of the element the popover backdrop is appended to (defaults to the combobox's parent).\n- `label` — visible label rendered above the field. Used as the accessible name when set.\n- `data-aria-label` — accessible name fallback when no visual label is rendered.\n- `help-text` — helper or validation text below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n- `required` — when `true`, the form is invalid unless a value is selected.\n- `validation-message` — custom validation message reported through `setCustomValidity` when the combobox is required but empty.\n- `disabled` — fully disabled; the host is removed from the tab order and the popover cannot open.\n- `readonly` — non-interactive but focusable; the popover cannot open and typing is suppressed, but the current value is still submitted.\n- `control-type` — when set to `controlled` (via `mdc-controltypeprovider`), the combobox emits `input` / `change` but does not update its own `value`; the parent must drive `value` in response.\n- `autoFocusOnMount` — when `true`, focuses the visible combobox input on first render.",
      "Accessibility": "### Built-in features\n\nThe component implements the [WAI-ARIA combobox autocomplete-list pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-autocomplete-list/). The visible input has `role=\"combobox\"`, the slotted `mdc-selectlistbox` provides `role=\"listbox\"`, and the combobox wires `aria-controls`, `aria-expanded`, `aria-haspopup`, and `aria-activedescendant`-style focus highlighting (via `data-focused` on the option) between them.\n\nKeyboard interaction:\n\n- `ArrowDown` / `ArrowUp` opens the dropdown (if closed) and moves the visual highlight to the next/previous enabled option (wraps at the ends).\n- `Enter` selects the highlighted option, closes the dropdown, and dispatches `change`. In spatial navigation mode, `Enter` first opens the dropdown.\n- `Escape` clears the highlighted option. If no options are visible (and no `no-result-text`), it also clears the typed input.\n- `Tab` closes the dropdown and moves focus to the next focusable element.\n- `Home` / `End` clear the highlighted option (the native input then handles caret movement).\n- Typing filters the visible options according to `filter`, opens the dropdown if closed, and dispatches `input`.\n\nBlur behaviour: on blur, the combobox commits the currently highlighted option if any, otherwise the typed text if it exactly matches an option label, otherwise reverts to the last committed value, otherwise (if `invalid-custom-value-text` is set) shows the error message.\n\nWhen `disabled` is `true`, the input is removed from the tab order and the dropdown cannot open. When `readonly` is `true`, the input remains focusable, the dropdown cannot open, and the form value is still submitted.\n\nThe `label` (or `data-aria-label`) provides the accessible name. `help-text` is linked via `aria-describedby`.\n\n#### Internal ARIA managed by the component\n\n| Element                | Attribute              | Value                                                                                  |\n| ---------------------- | ---------------------- | -------------------------------------------------------------------------------------- |\n| Visible input          | `role`                 | `combobox`                                                                             |\n| Visible input          | `aria-haspopup`        | `listbox`                                                                              |\n| Visible input          | `aria-autocomplete`    | `list`                                                                                 |\n| Visible input          | `aria-expanded`        | `true` when the dropdown is open, otherwise `false`                                    |\n| Visible input          | `aria-controls`        | id of the slotted listbox                                                              |\n| Visible input          | `aria-invalid`         | `true` when `help-text-type=\"error\"`, otherwise `false`                                |\n| Visible input          | `aria-required`        | mirrors `required`                                                                     |\n| Visible input          | `aria-readonly`        | mirrors `readonly`                                                                     |\n| Visible input          | `aria-disabled`        | mirrors `disabled`                                                                     |\n| Visible input          | `aria-label`           | mirrors `data-aria-label`                                                              |\n| Visible input          | `aria-labelledby`      | id of the label when `label` is set                                                    |\n| Visible input          | `aria-describedby`     | id of the help text when `help-text` is set                                            |\n| Visible input          | `tabindex`             | `0` when enabled; `-1` when `disabled`                                                 |\n| Trigger button         | `aria-expanded`        | mirrors the dropdown open state                                                        |\n| Trigger button         | `aria-label`           | mirrors `data-aria-label`                                                              |\n| Trigger button         | `tabindex`             | `-1` (not in the tab order; the input is the single tab stop)                          |\n| Slotted listbox        | `id`                   | generated id linked from the input's `aria-controls`                                   |\n| Slotted listbox        | `aria-label`           | mirrors `label` or `data-aria-label`                                                   |\n| Focused option         | `data-focused`         | present while the option is the visual active descendant                               |\n| Focused option         | `aria-selected`        | `true` while highlighted, `false` otherwise                                            |\n| Native hidden input    | `aria-hidden`          | `true` (only used to participate in form submission)                                   |\n\n### Implementation requirements\n\n#### General\n\n- Wrap options in `mdc-selectlistbox`. Without it, the listbox role and option focus semantics are missing.\n- Place per-option `mdc-tooltip` siblings outside `mdc-selectlistbox` and link them via `triggerid` so they do not break listbox navigation.\n- Set a unique `value` on every `mdc-option`; form submission and value-based selection rely on it.\n- When filtering is driven externally (API, server), set `filter=\"none\"` and update slotted options as data arrives — the combobox will not filter on its own.\n- When using `control-type=\"controlled\"`, listen to `input` and `change` and drive the `value` attribute from the parent; the combobox will not mutate its own value.\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`) — `role=\"combobox\"` requires an accessible name.\n- Use `help-text` with `help-text-type=\"error\"` for validation feedback; the icon and `aria-describedby` association are wired automatically.\n- Pair `no-result-text` and `invalid-custom-value-text` thoughtfully: the first describes \"nothing matches your filter\", the second describes \"what you typed is not a valid option\". Pick wording that helps the user recover."
    },
    "dependencies": [
      "mdc-buttonsimple",
      "mdc-icon",
      "mdc-input",
      "mdc-listitem",
      "mdc-popover"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, focuses the visible combobox input on first render.",
          "source": "storybook"
        },
        {
          "name": "backdropAppendTo",
          "attribute": "backdrop-append-to",
          "type": "text",
          "values": [],
          "description": "id of the element the popover backdrop is appended to (defaults to the combobox's parent).",
          "source": "storybook"
        },
        {
          "name": "boundary",
          "attribute": "boundary",
          "type": "text",
          "default": "undefined",
          "values": [
            "clippingAncestors"
          ],
          "description": "clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Used together with `strategy` to keep the popover visible inside scrollable parents.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "controlType",
          "attribute": "control-type",
          "type": "select",
          "values": [],
          "description": "when set to `controlled` (via `mdc-controltypeprovider`), the combobox emits `input` / `change` but does not update its own `value`; the parent must drive `value` in response.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name fallback when no visual label is rendered.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disabled; the host is removed from the tab order and the popover cannot open.",
          "source": "storybook"
        },
        {
          "name": "filter",
          "attribute": "filter",
          "type": "select",
          "default": "undefined",
          "values": [
            "match-starts-with",
            "none"
          ],
          "description": "filter strategy: `match-starts-with` (default), `none` (no filtering), or a custom `(option, inputValue) => boolean` function. Only string values are reflected as the `filter` attribute.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper or validation text below the field.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.",
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "invalidCustomValueText",
          "attribute": "invalid-custom-value-text",
          "type": "text",
          "values": [],
          "description": "error message used when the user enters a value that does not match any option and the field has no previously committed value.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label rendered above the field. Used as the accessible name when set.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "form field name submitted with the form.",
          "source": "storybook"
        },
        {
          "name": "noResultText",
          "attribute": "no-result-text",
          "type": "text",
          "values": [],
          "description": "text shown inside the dropdown when no options match the filter; omit to hide the dropdown silently.",
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "description": "placeholder text shown when no value is selected.",
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "select",
          "default": "undefined",
          "values": [
            "bottom-start",
            "top-start"
          ],
          "description": "popover placement relative to the input. `bottom-start` (default) or `top-start`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "popoverZIndex",
          "attribute": "popover-z-index",
          "type": "number",
          "values": [],
          "description": "z-index override for the popover.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "non-interactive but focusable; the popover cannot open and typing is suppressed, but the current value is still submitted.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the form is invalid unless a value is selected.",
          "source": "storybook"
        },
        {
          "name": "strategy",
          "attribute": "strategy",
          "type": "select",
          "default": "absolute",
          "values": [],
          "description": "popover positioning strategy. `absolute` (default) or `fixed`. Use `fixed` together with a non-default `boundary` to avoid clipping inside scroll containers.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "validationMessage",
          "attribute": "validation-message",
          "type": "text",
          "values": [],
          "description": "custom validation message reported through `setCustomValidity` when the combobox is required but empty.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "the value of the selected option. Setting this attribute updates the visible selection without firing `change`.",
          "source": "storybook"
        },
        {
          "name": "visualCombobox",
          "attribute": "backdrop-append-to",
          "type": "HTMLInputElement",
          "values": [],
          "description": "id of the element the popover backdrop is appended to (defaults to the combobox's parent).",
          "reflected": true,
          "source": "component"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot for Selectlistbox including options and/or option group."
        },
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the combobox is clicked."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the combobox is changed."
        },
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the combobox is changed."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the combobox."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the combobox receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "internal-native-input",
          "description": "The internal native input element of the combobox."
        },
        {
          "name": "input-text",
          "description": "The input element of the combobox."
        },
        {
          "name": "no-result-text",
          "description": "The no result text element of the combobox."
        },
        {
          "name": "combobox-base",
          "description": "The base container element of the combobox."
        },
        {
          "name": "combobox-button",
          "description": "The button element of the combobox."
        },
        {
          "name": "combobox-button-icon",
          "description": "The icon element of the button of the combobox."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-combobox-border-color",
          "description": "The border color of the combobox"
        },
        {
          "name": "--mdc-combobox-height",
          "description": "The height of the combobox trigger control"
        },
        {
          "name": "--mdc-combobox-icon-color",
          "description": "The icon color of the combobox"
        },
        {
          "name": "--mdc-combobox-listbox-height",
          "description": "The height of the listbox inside the combobox"
        },
        {
          "name": "--mdc-combobox-listbox-width",
          "description": "The width of the listbox inside the combobox"
        },
        {
          "name": "--mdc-combobox-width",
          "description": "The width of the combobox"
        },
        {
          "name": "--mdc-combobox-hover-background-color",
          "description": "The background color of the combobox when hovered"
        },
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        }
      ],
      "rules": [
        "the value of the selected option. Setting this attribute updates the visible selection without firing `change`.",
        "form field name submitted with the form.",
        "placeholder text shown when no value is selected.",
        "popover placement relative to the input. `bottom-start` (default) or `top-start`.",
        "filter strategy: `match-starts-with` (default), `none` (no filtering), or a custom `(option, inputValue) => boolean` function. Only string values are reflected as the `filter` attribute.",
        "text shown inside the dropdown when no options match the filter; omit to hide the dropdown silently.",
        "error message used when the user enters a value that does not match any option and the field has no previously committed value.",
        "clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Used together with `strategy` to keep the popover visible inside scrollable parents.",
        "popover positioning strategy. `absolute` (default) or `fixed`. Use `fixed` together with a non-default `boundary` to avoid clipping inside scroll containers.",
        "z-index override for the popover.",
        "id of the element the popover backdrop is appended to (defaults to the combobox's parent).",
        "visible label rendered above the field. Used as the accessible name when set.",
        "accessible name fallback when no visual label is rendered.",
        "helper or validation text below the field.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.",
        "when `true`, the form is invalid unless a value is selected.",
        "custom validation message reported through `setCustomValidity` when the combobox is required but empty.",
        "fully disabled; the host is removed from the tab order and the popover cannot open.",
        "non-interactive but focusable; the popover cannot open and typing is suppressed, but the current value is still submitted.",
        "when set to `controlled` (via `mdc-controltypeprovider`), the combobox emits `input` / `change` but does not update its own `value`; the parent must drive `value` in response.",
        "when `true`, focuses the visible combobox input on first render."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/combobox/index.js\"; // custom element registration\n// or via React wrapper\nimport { Combobox, SelectListBox, Option, OptGroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-combobox label=\"Country\" name=\"country\" placeholder=\"Start typing…\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"ch\" label=\"Switzerland\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-combobox>"
      }
    ],
    "reactImports": [
      "Combobox",
      "SelectListBox",
      "Option",
      "OptGroup"
    ],
    "constants": {
      "ICON_NAME": {
        "ARROW_UP": "'arrow-up-bold' as Extract<IconNames, 'arrow-up-bold'>",
        "ARROW_DOWN": "'arrow-down-bold' as Extract<IconNames, 'arrow-down-bold'>"
      },
      "FILTER": {
        "MATCH_STARTS_WITH": "match-starts-with",
        "NONE": "none"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "based",
      "buttonsimple",
      "combobox",
      "component",
      "dropdown",
      "either",
      "filter",
      "for",
      "from",
      "guidelines",
      "icon",
      "input",
      "lets",
      "list",
      "listitem",
      "mdc",
      "of",
      "options",
      "or",
      "overview",
      "pick",
      "popover",
      "predefined",
      "text",
      "that",
      "the",
      "to",
      "type",
      "usage",
      "users"
    ]
  },
  {
    "name": "controltypeprovider",
    "title": "Control type provider",
    "tagName": "mdc-controltypeprovider",
    "summary": "Usage and guidelines for the mdc-controltypeprovider component — a context provider setting whether descendant form controls behave as controlled or uncontrolled.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/controltypeprovider",
      "knowledgeBasePath": "components/src/components/controltypeprovider/knowledge-base/controltypeprovider.component.md"
    },
    "docs": "## Overview\n\nThe control type provider is a context provider used to set whether descendant form controls operate as **controlled** or **uncontrolled** components.\n\n- `uncontrolled` (default) — each control manages its own state. Toggling a checkbox, picking a combobox option, or moving a slider updates the control's internal value and dispatches a `change` event.\n- `controlled` — each control delegates state to its parent. The control emits `input` and `change` with the requested new value but does not update its own value; the parent component is expected to update the control's `value` attribute in response.\n\nThis mirrors the React controlled/uncontrolled pattern at the web-component level so the same component can be embedded in either consumer style without forking the API.\n\nIt is rendered as a transparent wrapper — the provider does not produce any visible UI.\n\n### When to use\n\n- Wrap a tree of form controls (combobox, select, checkbox, etc.) in `mdc-controltypeprovider control-type=\"controlled\"` when the parent owns the canonical state and the controls must always reflect what the parent passes in.\n- Use it when integrating with state-management libraries (React, Redux, signals, etc.) that need every user interaction to flow through the consumer's update cycle.\n\n### When not to use\n\n- Leave the provider out (or rely on the default `uncontrolled`) when each control should own its own state and the consumer just listens to `change`.\n- Do not nest providers to switch modes mid-tree unless absolutely necessary — descendant controls subscribe to the context once and do not re-subscribe when the value changes.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/controltypeprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { ControlTypeProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-controltypeprovider control-type=\"controlled\">\n  <mdc-combobox label=\"Country\" name=\"country\"> … </mdc-combobox>\n  <mdc-checkbox label=\"Subscribe\"></mdc-checkbox>\n</mdc-controltypeprovider>\n```\n\nIn controlled mode, listen for `input` / `change` on each form control and drive its `value` (or `checked`) attribute from the parent state.\n\n### Content guidance\n\nThe provider has no visible content of its own. Place form controls (or higher-level layouts) inside it. Avoid using it as a generic layout wrapper — its only purpose is to broadcast `control-type` to the descendants that consume it.\n\n### Property/Attribute details\n\n- `control-type` — `controlled` or `uncontrolled` (default). Determines how descendant controls handle user interaction.\n\n### Limitations\n\n- The provider broadcasts `control-type` once at mount; descendants do **not** react to runtime changes of the attribute. If you must switch modes after mount, remount the descendant tree (e.g. by toggling its `key` in React) so it reads the new value.\n- Only components that use the `ControlTypeMixin` consume this context — currently the combobox and other form controls that opt in. Components that have no need for the controlled/uncontrolled distinction ignore the provider.\n\n## Accessibility\n\n### Built-in features\n\nThe provider does not render any visible content or accept focus. It does not set ARIA attributes on itself or its descendants and therefore has no direct accessibility surface.\n\nThe provider does affect the **behaviour** of consuming form controls — in controlled mode, a control will not update its own `value` in response to user interaction, so the consumer must drive the value through the parent. If the parent forgets to update the value, the control will appear unresponsive to keyboard and pointer input, which is an accessibility regression. Always wire the `input` / `change` handlers when using `control-type=\"controlled\"`.\n\n#### Internal ARIA managed by the component\n\nThe provider does not manage any ARIA attributes.\n\n### Implementation requirements\n\n#### General\n\n- Place the provider as high in the tree as the controlled/uncontrolled decision applies; nesting two providers with different `control-type` values is supported but rarely needed.\n- In controlled mode, always handle `input` and `change` events from descendant form controls and update their `value` (or `checked`) attribute — otherwise the controls will look broken to assistive technology users.",
    "sections": {
      "Overview": "The control type provider is a context provider used to set whether descendant form controls operate as **controlled** or **uncontrolled** components.\n\n- `uncontrolled` (default) — each control manages its own state. Toggling a checkbox, picking a combobox option, or moving a slider updates the control's internal value and dispatches a `change` event.\n- `controlled` — each control delegates state to its parent. The control emits `input` and `change` with the requested new value but does not update its own value; the parent component is expected to update the control's `value` attribute in response.\n\nThis mirrors the React controlled/uncontrolled pattern at the web-component level so the same component can be embedded in either consumer style without forking the API.\n\nIt is rendered as a transparent wrapper — the provider does not produce any visible UI.\n\n### When to use\n\n- Wrap a tree of form controls (combobox, select, checkbox, etc.) in `mdc-controltypeprovider control-type=\"controlled\"` when the parent owns the canonical state and the controls must always reflect what the parent passes in.\n- Use it when integrating with state-management libraries (React, Redux, signals, etc.) that need every user interaction to flow through the consumer's update cycle.\n\n### When not to use\n\n- Leave the provider out (or rely on the default `uncontrolled`) when each control should own its own state and the consumer just listens to `change`.\n- Do not nest providers to switch modes mid-tree unless absolutely necessary — descendant controls subscribe to the context once and do not re-subscribe when the value changes.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/controltypeprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { ControlTypeProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-controltypeprovider control-type=\"controlled\">\n  <mdc-combobox label=\"Country\" name=\"country\"> … </mdc-combobox>\n  <mdc-checkbox label=\"Subscribe\"></mdc-checkbox>\n</mdc-controltypeprovider>\n```\n\nIn controlled mode, listen for `input` / `change` on each form control and drive its `value` (or `checked`) attribute from the parent state.\n\n### Content guidance\n\nThe provider has no visible content of its own. Place form controls (or higher-level layouts) inside it. Avoid using it as a generic layout wrapper — its only purpose is to broadcast `control-type` to the descendants that consume it.\n\n### Property/Attribute details\n\n- `control-type` — `controlled` or `uncontrolled` (default). Determines how descendant controls handle user interaction.\n\n### Limitations\n\n- The provider broadcasts `control-type` once at mount; descendants do **not** react to runtime changes of the attribute. If you must switch modes after mount, remount the descendant tree (e.g. by toggling its `key` in React) so it reads the new value.\n- Only components that use the `ControlTypeMixin` consume this context — currently the combobox and other form controls that opt in. Components that have no need for the controlled/uncontrolled distinction ignore the provider.",
      "Accessibility": "### Built-in features\n\nThe provider does not render any visible content or accept focus. It does not set ARIA attributes on itself or its descendants and therefore has no direct accessibility surface.\n\nThe provider does affect the **behaviour** of consuming form controls — in controlled mode, a control will not update its own `value` in response to user interaction, so the consumer must drive the value through the parent. If the parent forgets to update the value, the control will appear unresponsive to keyboard and pointer input, which is an accessibility regression. Always wire the `input` / `change` handlers when using `control-type=\"controlled\"`.\n\n#### Internal ARIA managed by the component\n\nThe provider does not manage any ARIA attributes.\n\n### Implementation requirements\n\n#### General\n\n- Place the provider as high in the tree as the controlled/uncontrolled decision applies; nesting two providers with different `control-type` values is supported but rarely needed.\n- In controlled mode, always handle `input` and `change` events from descendant form controls and update their `value` (or `checked`) attribute — otherwise the controls will look broken to assistive technology users."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "controlType",
          "attribute": "control-type",
          "type": "select",
          "values": [
            "controlled",
            "uncontrolled"
          ],
          "description": "`controlled` or `uncontrolled` (default). Determines how descendant controls handle user interaction.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "`controlled` or `uncontrolled` (default). Determines how descendant controls handle user interaction."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/controltypeprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { ControlTypeProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-controltypeprovider control-type=\"controlled\">\n  <mdc-combobox label=\"Country\" name=\"country\"> … </mdc-combobox>\n  <mdc-checkbox label=\"Subscribe\"></mdc-checkbox>\n</mdc-controltypeprovider>"
      }
    ],
    "reactImports": [
      "ControlTypeProvider"
    ],
    "constants": {
      "DEFAULTS": {
        "CONTROL_TYPE": "uncontrolled"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "as",
      "behave",
      "component",
      "context",
      "control",
      "controlled",
      "controls",
      "controltypeprovider",
      "descendant",
      "for",
      "form",
      "guidelines",
      "mdc",
      "or",
      "overview",
      "provider",
      "setting",
      "the",
      "type",
      "uncontrolled",
      "usage",
      "whether"
    ]
  },
  {
    "name": "datepicker",
    "title": "Datepicker",
    "tagName": "mdc-datepicker",
    "summary": "Usage, guidelines, and accessibility for the mdc-datepicker component — a date input offering manual entry or a calendar popover for single, range, or week selection with locale support.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/datepicker",
      "knowledgeBasePath": "components/src/components/datepicker/knowledge-base/datepicker.component.md"
    },
    "docs": "## Overview\n\nThe datepicker lets users pick a date either by typing it directly into month/day/year spinbutton fields or by opening an `mdc-calendar` inside a popover. It supports BCP 47 locale strings to drive field order, separators, placeholders, and localised accessible labels.\n\nThe component is form-associated and participates in native form submission, validation, and reset. When a range or week is selected, both the start (`value`) and end (`end-value`) dates are submitted together using the configured `name`.\n\n### When to use\n\n- Use `mdc-datepicker` with `variant=\"input\"` when the user typically knows the date and entering it digit-by-digit is fastest.\n- Use `mdc-datepicker` with `variant=\"default\"` when picking from a calendar (range or week) is the primary interaction.\n- Use it when the value needs to be locale-aware (field order, separator, formatting) and accessible to spinbutton-aware screen readers.\n\n### When not to use\n\n- Use `mdc-input` when free-form text is the goal and date semantics are not required.\n- Use `mdc-calendar` directly when the calendar should always be visible (e.g. embedded in a sidebar) without a popover trigger.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/datepicker/index.js\"; // custom element registration\n// or via React wrapper\nimport { Datepicker } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (input variant, single date):\n\n```html\n<mdc-datepicker\n  label=\"Date of birth\"\n  name=\"dob\"\n  variant=\"input\"\n  locale=\"en-US\"\n  locale-month-label=\"Month\"\n  locale-day-label=\"Day\"\n  locale-year-label=\"Year\"\n  locale-calendar-label=\"Open calendar\"\n></mdc-datepicker>\n```\n\nDefault variant (range selection in a calendar):\n\n```html\n<mdc-datepicker\n  label=\"Stay\"\n  name=\"stay\"\n  variant=\"default\"\n  selection-mode=\"range\"\n  locale=\"en-US\"\n  locale-calendar-label=\"Choose a date range\"\n></mdc-datepicker>\n```\n\nListen for `change` to react to a committed date and `input` to react to interim updates. Both events carry `{ value, endValue }` in `event.detail`.\n\nThe `input` variant offers spinbutton fields (`role=\"spinbutton\"`) with `ArrowUp`/`ArrowDown` to increment/decrement, `ArrowLeft`/`ArrowRight` to jump between fields, and digit typing with auto-advance to the next field when a complete value is entered. Pressing the calendar icon button opens the popover so the user can pick the date visually.\n\nThe `default` variant renders a select-style trigger (`role=\"combobox\"`, `aria-haspopup=\"dialog\"`) that opens the calendar popover on click, `Enter`, `Space`, or `ArrowDown`.\n\n### Content guidance\n\n- Provide a `label` describing what date is being collected; reach for `data-aria-label` only when a visible label is not possible.\n- Always set every `locale-*` label property the variant uses — the spinbutton fields and calendar toggle button rely on them for their accessible names; there is no fallback prose.\n- Use `selection-mode` deliberately: `single` for one date (input variant default), `range` for two dates, `week` for a full week range derived from the locale.\n- Set `min` / `max` (ISO `YYYY-MM-DD`) to constrain the selectable dates; out-of-range values entered in spinbuttons are silently rejected without committing.\n\n### Property/Attribute details\n\n- `variant` — `input` (default) renders spinbutton fields; `default` renders a select-style trigger.\n- `value` — selected date as an ISO `YYYY-MM-DD` string. For range/week selections, this is the start date.\n- `end-value` — end date as an ISO `YYYY-MM-DD` string for range/week selections. When set together with `value` and `name`, both are submitted as `name` and `name-end`.\n- `selection-mode` — `single` (default), `range`, or `week`. The default variant promotes `single` to `range` (single dates inside the calendar are still allowed).\n- `locale` — BCP 47 locale string (e.g. `en-US`, `de-DE`). Drives field order, separator, placeholders, and weekday/month names in the calendar. Default `en-US`.\n- `min` / `max` — ISO date strings bounding the selectable range. Values outside the range are rejected.\n- `placement` — preferred popover placement around the trigger. Default `bottom-start`.\n- `strategy` — popover positioning strategy. `absolute` (default) or `fixed`.\n- `disable-flip` — when `true`, the popover does not flip to the opposite side when the preferred placement does not fit. Default `false`.\n- `append-to` — id of the element the popover is appended to.\n- `backdrop-append-to` — id of the element the popover backdrop is appended to.\n- `locale-month-label` / `locale-day-label` / `locale-year-label` — accessible labels for the month/day/year spinbutton inputs (input variant). Required for screen-reader users.\n- `locale-calendar-label` — accessible label for the calendar toggle button (input variant) and the combobox trigger (default variant).\n- `locale-spinbutton-description` — `aria-description` applied to every spinbutton input. Use it to explain the increment/decrement behaviour (\"Use up and down arrows to change\").\n- `locale-today-label` — label for the \"today\" button inside the calendar.\n- `locale-prev-month-label` / `locale-next-month-label` — accessible labels for the calendar's month-navigation arrows.\n- `label` — visible label rendered above the field.\n- `data-aria-label` — accessible name fallback when no visible label is rendered.\n- `help-text` — helper or validation text below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n- `required` — when `true`, the form is invalid unless a value is selected.\n- `disabled` — fully disabled; the field is removed from the tab order and the popover cannot open.\n- `readonly` — non-interactive but focusable; typing is suppressed and the popover cannot open. The current value is still submitted.\n\n## Accessibility\n\n### Built-in features\n\nIn the `input` variant, each of the three fields renders as a `role=\"spinbutton\"` input with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, `aria-label`, and `aria-description`. The field order is derived from the configured `locale`. Keyboard interaction:\n\n- `ArrowUp` / `ArrowDown` increment/decrement the current field within its valid range (wrapping at the ends).\n- `ArrowLeft` / `ArrowRight` move focus to the previous/next field.\n- Typing digits fills the current field; after the maximum number of digits (or an unambiguous single digit that cannot fit a second), focus auto-advances to the next field.\n- `Tab` moves focus to the calendar toggle button, then out of the component.\n- `Escape` closes the calendar popover when it is open.\n\nThe calendar toggle button exposes `aria-expanded` and `aria-haspopup=\"dialog\"` and opens the popover.\n\nIn the `default` variant, the trigger renders with `role=\"combobox\"`, `aria-haspopup=\"dialog\"`, and `aria-expanded` reflecting the popover state. `Enter`, `Space`, and `ArrowDown` open the popover; the calendar receives focus on the currently focused day cell.\n\nThe popover wraps the calendar with focus trap, escape-to-close, and outside-click-to-close enabled. On close, focus returns to the trigger (or the calendar toggle button in the input variant).\n\nWhen `disabled` is `true`, all inputs and the trigger are removed from the tab order. When `readonly` is `true`, focus is preserved, typing is suppressed, the popover cannot open, and the form value is still submitted.\n\nThe `label` (or `data-aria-label`) provides the accessible name for the field. `help-text` is linked via `aria-describedby`.\n\n#### Internal ARIA managed by the component\n\n| Element                  | Attribute          | Value                                                                                  |\n| ------------------------ | ------------------ | -------------------------------------------------------------------------------------- |\n| Spinbutton (each field)  | `role`             | `spinbutton`                                                                           |\n| Spinbutton (each field)  | `aria-label`       | mirrors the matching `locale-{month\\|day\\|year}-label`                                 |\n| Spinbutton (each field)  | `aria-valuemin`    | minimum for the field (e.g. `1` for month, computed for day from current month/year)   |\n| Spinbutton (each field)  | `aria-valuemax`    | maximum for the field (e.g. `12` for month, `28`–`31` for day)                         |\n| Spinbutton (each field)  | `aria-valuenow`    | current numeric value of the field, omitted when empty                                 |\n| Spinbutton (each field)  | `aria-description` | mirrors `locale-spinbutton-description`                                                |\n| Spinbutton (each field)  | `tabindex`         | `0` when enabled; `-1` when `disabled`                                                 |\n| Calendar toggle (input)  | `aria-label`       | mirrors `locale-calendar-label`                                                        |\n| Calendar toggle (input)  | `aria-expanded`    | `true` when the popover is open, otherwise `false`                                     |\n| Calendar toggle (input)  | `aria-haspopup`    | `dialog`                                                                               |\n| Combobox trigger (default) | `role`           | `combobox`                                                                             |\n| Combobox trigger (default) | `aria-haspopup` | `dialog`                                                                               |\n| Combobox trigger (default) | `aria-expanded` | `true` when the popover is open, otherwise `false`                                     |\n| Combobox trigger (default) | `aria-label`    | mirrors `locale-calendar-label`                                                        |\n| Combobox trigger (default) | `tabindex`      | `0` when enabled; `-1` when `disabled`                                                 |\n| Native hidden input      | `aria-hidden`      | `true` (only used to participate in form submission)                                   |\n\n### Implementation requirements\n\n#### General\n\n- Provide a `locale` matching the user's expected formatting; this drives field order, separator, placeholders, and weekday/month names.\n- Set `min` and `max` when only a subset of dates is valid — the spinbutton fields clamp typed input and the calendar disables out-of-range cells.\n- When using the popover, set `append-to` if the datepicker is rendered inside an element with `overflow: hidden` so the calendar can escape the clip region.\n- For range or week selection, listen for `change` and read both `event.detail.value` (start) and `event.detail.endDate` (end) — or, when reading attributes back, both `value` and `end-value`.\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`); without it, the spinbuttons and trigger have no accessible name.\n- Provide every `locale-*` label the variant uses — there is no English fallback for these.\n- Use `help-text` with `help-text-type=\"error\"` for validation feedback; the icon and `aria-describedby` association are wired automatically.",
    "sections": {
      "Overview": "The datepicker lets users pick a date either by typing it directly into month/day/year spinbutton fields or by opening an `mdc-calendar` inside a popover. It supports BCP 47 locale strings to drive field order, separators, placeholders, and localised accessible labels.\n\nThe component is form-associated and participates in native form submission, validation, and reset. When a range or week is selected, both the start (`value`) and end (`end-value`) dates are submitted together using the configured `name`.\n\n### When to use\n\n- Use `mdc-datepicker` with `variant=\"input\"` when the user typically knows the date and entering it digit-by-digit is fastest.\n- Use `mdc-datepicker` with `variant=\"default\"` when picking from a calendar (range or week) is the primary interaction.\n- Use it when the value needs to be locale-aware (field order, separator, formatting) and accessible to spinbutton-aware screen readers.\n\n### When not to use\n\n- Use `mdc-input` when free-form text is the goal and date semantics are not required.\n- Use `mdc-calendar` directly when the calendar should always be visible (e.g. embedded in a sidebar) without a popover trigger.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/datepicker/index.js\"; // custom element registration\n// or via React wrapper\nimport { Datepicker } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (input variant, single date):\n\n```html\n<mdc-datepicker\n  label=\"Date of birth\"\n  name=\"dob\"\n  variant=\"input\"\n  locale=\"en-US\"\n  locale-month-label=\"Month\"\n  locale-day-label=\"Day\"\n  locale-year-label=\"Year\"\n  locale-calendar-label=\"Open calendar\"\n></mdc-datepicker>\n```\n\nDefault variant (range selection in a calendar):\n\n```html\n<mdc-datepicker\n  label=\"Stay\"\n  name=\"stay\"\n  variant=\"default\"\n  selection-mode=\"range\"\n  locale=\"en-US\"\n  locale-calendar-label=\"Choose a date range\"\n></mdc-datepicker>\n```\n\nListen for `change` to react to a committed date and `input` to react to interim updates. Both events carry `{ value, endValue }` in `event.detail`.\n\nThe `input` variant offers spinbutton fields (`role=\"spinbutton\"`) with `ArrowUp`/`ArrowDown` to increment/decrement, `ArrowLeft`/`ArrowRight` to jump between fields, and digit typing with auto-advance to the next field when a complete value is entered. Pressing the calendar icon button opens the popover so the user can pick the date visually.\n\nThe `default` variant renders a select-style trigger (`role=\"combobox\"`, `aria-haspopup=\"dialog\"`) that opens the calendar popover on click, `Enter`, `Space`, or `ArrowDown`.\n\n### Content guidance\n\n- Provide a `label` describing what date is being collected; reach for `data-aria-label` only when a visible label is not possible.\n- Always set every `locale-*` label property the variant uses — the spinbutton fields and calendar toggle button rely on them for their accessible names; there is no fallback prose.\n- Use `selection-mode` deliberately: `single` for one date (input variant default), `range` for two dates, `week` for a full week range derived from the locale.\n- Set `min` / `max` (ISO `YYYY-MM-DD`) to constrain the selectable dates; out-of-range values entered in spinbuttons are silently rejected without committing.\n\n### Property/Attribute details\n\n- `variant` — `input` (default) renders spinbutton fields; `default` renders a select-style trigger.\n- `value` — selected date as an ISO `YYYY-MM-DD` string. For range/week selections, this is the start date.\n- `end-value` — end date as an ISO `YYYY-MM-DD` string for range/week selections. When set together with `value` and `name`, both are submitted as `name` and `name-end`.\n- `selection-mode` — `single` (default), `range`, or `week`. The default variant promotes `single` to `range` (single dates inside the calendar are still allowed).\n- `locale` — BCP 47 locale string (e.g. `en-US`, `de-DE`). Drives field order, separator, placeholders, and weekday/month names in the calendar. Default `en-US`.\n- `min` / `max` — ISO date strings bounding the selectable range. Values outside the range are rejected.\n- `placement` — preferred popover placement around the trigger. Default `bottom-start`.\n- `strategy` — popover positioning strategy. `absolute` (default) or `fixed`.\n- `disable-flip` — when `true`, the popover does not flip to the opposite side when the preferred placement does not fit. Default `false`.\n- `append-to` — id of the element the popover is appended to.\n- `backdrop-append-to` — id of the element the popover backdrop is appended to.\n- `locale-month-label` / `locale-day-label` / `locale-year-label` — accessible labels for the month/day/year spinbutton inputs (input variant). Required for screen-reader users.\n- `locale-calendar-label` — accessible label for the calendar toggle button (input variant) and the combobox trigger (default variant).\n- `locale-spinbutton-description` — `aria-description` applied to every spinbutton input. Use it to explain the increment/decrement behaviour (\"Use up and down arrows to change\").\n- `locale-today-label` — label for the \"today\" button inside the calendar.\n- `locale-prev-month-label` / `locale-next-month-label` — accessible labels for the calendar's month-navigation arrows.\n- `label` — visible label rendered above the field.\n- `data-aria-label` — accessible name fallback when no visible label is rendered.\n- `help-text` — helper or validation text below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n- `required` — when `true`, the form is invalid unless a value is selected.\n- `disabled` — fully disabled; the field is removed from the tab order and the popover cannot open.\n- `readonly` — non-interactive but focusable; typing is suppressed and the popover cannot open. The current value is still submitted.",
      "Accessibility": "### Built-in features\n\nIn the `input` variant, each of the three fields renders as a `role=\"spinbutton\"` input with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, `aria-label`, and `aria-description`. The field order is derived from the configured `locale`. Keyboard interaction:\n\n- `ArrowUp` / `ArrowDown` increment/decrement the current field within its valid range (wrapping at the ends).\n- `ArrowLeft` / `ArrowRight` move focus to the previous/next field.\n- Typing digits fills the current field; after the maximum number of digits (or an unambiguous single digit that cannot fit a second), focus auto-advances to the next field.\n- `Tab` moves focus to the calendar toggle button, then out of the component.\n- `Escape` closes the calendar popover when it is open.\n\nThe calendar toggle button exposes `aria-expanded` and `aria-haspopup=\"dialog\"` and opens the popover.\n\nIn the `default` variant, the trigger renders with `role=\"combobox\"`, `aria-haspopup=\"dialog\"`, and `aria-expanded` reflecting the popover state. `Enter`, `Space`, and `ArrowDown` open the popover; the calendar receives focus on the currently focused day cell.\n\nThe popover wraps the calendar with focus trap, escape-to-close, and outside-click-to-close enabled. On close, focus returns to the trigger (or the calendar toggle button in the input variant).\n\nWhen `disabled` is `true`, all inputs and the trigger are removed from the tab order. When `readonly` is `true`, focus is preserved, typing is suppressed, the popover cannot open, and the form value is still submitted.\n\nThe `label` (or `data-aria-label`) provides the accessible name for the field. `help-text` is linked via `aria-describedby`.\n\n#### Internal ARIA managed by the component\n\n| Element                  | Attribute          | Value                                                                                  |\n| ------------------------ | ------------------ | -------------------------------------------------------------------------------------- |\n| Spinbutton (each field)  | `role`             | `spinbutton`                                                                           |\n| Spinbutton (each field)  | `aria-label`       | mirrors the matching `locale-{month\\|day\\|year}-label`                                 |\n| Spinbutton (each field)  | `aria-valuemin`    | minimum for the field (e.g. `1` for month, computed for day from current month/year)   |\n| Spinbutton (each field)  | `aria-valuemax`    | maximum for the field (e.g. `12` for month, `28`–`31` for day)                         |\n| Spinbutton (each field)  | `aria-valuenow`    | current numeric value of the field, omitted when empty                                 |\n| Spinbutton (each field)  | `aria-description` | mirrors `locale-spinbutton-description`                                                |\n| Spinbutton (each field)  | `tabindex`         | `0` when enabled; `-1` when `disabled`                                                 |\n| Calendar toggle (input)  | `aria-label`       | mirrors `locale-calendar-label`                                                        |\n| Calendar toggle (input)  | `aria-expanded`    | `true` when the popover is open, otherwise `false`                                     |\n| Calendar toggle (input)  | `aria-haspopup`    | `dialog`                                                                               |\n| Combobox trigger (default) | `role`           | `combobox`                                                                             |\n| Combobox trigger (default) | `aria-haspopup` | `dialog`                                                                               |\n| Combobox trigger (default) | `aria-expanded` | `true` when the popover is open, otherwise `false`                                     |\n| Combobox trigger (default) | `aria-label`    | mirrors `locale-calendar-label`                                                        |\n| Combobox trigger (default) | `tabindex`      | `0` when enabled; `-1` when `disabled`                                                 |\n| Native hidden input      | `aria-hidden`      | `true` (only used to participate in form submission)                                   |\n\n### Implementation requirements\n\n#### General\n\n- Provide a `locale` matching the user's expected formatting; this drives field order, separator, placeholders, and weekday/month names.\n- Set `min` and `max` when only a subset of dates is valid — the spinbutton fields clamp typed input and the calendar disables out-of-range cells.\n- When using the popover, set `append-to` if the datepicker is rendered inside an element with `overflow: hidden` so the calendar can escape the clip region.\n- For range or week selection, listen for `change` and read both `event.detail.value` (start) and `event.detail.endDate` (end) — or, when reading attributes back, both `value` and `end-value`.\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`); without it, the spinbuttons and trigger have no accessible name.\n- Provide every `locale-*` label the variant uses — there is no English fallback for these.\n- Use `help-text` with `help-text-type=\"error\"` for validation feedback; the icon and `aria-describedby` association are wired automatically."
    },
    "dependencies": [
      "mdc-button",
      "mdc-calendar",
      "mdc-icon",
      "mdc-popover",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "appendTo",
          "attribute": "append-to",
          "type": "text",
          "values": [],
          "description": "id of the element the popover is appended to.",
          "source": "storybook"
        },
        {
          "name": "backdropAppendTo",
          "attribute": "backdrop-append-to",
          "type": "text",
          "values": [],
          "description": "id of the element the popover backdrop is appended to.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disabled; the field is removed from the tab order and the popover cannot open.",
          "source": "storybook"
        },
        {
          "name": "disableFlip",
          "attribute": "disable-flip",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the popover does not flip to the opposite side when the preferred placement does not fit. Default `false`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "endValue",
          "attribute": "end-value",
          "type": "text",
          "values": [],
          "description": "end date as an ISO `YYYY-MM-DD` string for range/week selections. When set together with `value` and `name`, both are submitted as `name` and `name-end`.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper or validation text below the field.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label rendered above the field.",
          "source": "storybook"
        },
        {
          "name": "locale",
          "attribute": "locale",
          "type": "text",
          "default": "'en-US'",
          "values": [],
          "description": "BCP 47 locale string (e.g. `en-US`, `de-DE`). Drives field order, separator, placeholders, and weekday/month names in the calendar. Default `en-US`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "localeCalendarLabel",
          "attribute": "locale-calendar-label",
          "type": "text",
          "values": [],
          "description": "accessible label for the calendar toggle button (input variant) and the combobox trigger (default variant).",
          "source": "storybook"
        },
        {
          "name": "localeDayLabel",
          "attribute": "locale-day-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeMonthLabel",
          "attribute": "locale-month-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeNextMonthLabel",
          "attribute": "locale-next-month-label",
          "type": "text",
          "values": [],
          "description": "accessible labels for the calendar's month-navigation arrows.",
          "source": "storybook"
        },
        {
          "name": "localePrevMonthLabel",
          "attribute": "locale-prev-month-label",
          "type": "text",
          "values": [],
          "description": "accessible labels for the calendar's month-navigation arrows.",
          "source": "storybook"
        },
        {
          "name": "localeSpinbuttonDescription",
          "attribute": "locale-spinbutton-description",
          "type": "text",
          "values": [],
          "description": "`aria-description` applied to every spinbutton input. Use it to explain the increment/decrement behaviour (\"Use up and down arrows to change\").",
          "source": "storybook"
        },
        {
          "name": "localeTodayLabel",
          "attribute": "locale-today-label",
          "type": "text",
          "values": [],
          "description": "label for the \"today\" button inside the calendar.",
          "source": "storybook"
        },
        {
          "name": "localeYearLabel",
          "attribute": "locale-year-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "max",
          "attribute": "max",
          "type": "text",
          "values": [],
          "description": "ISO date strings bounding the selectable range. Values outside the range are rejected.",
          "source": "storybook"
        },
        {
          "name": "min",
          "attribute": "min",
          "type": "text",
          "values": [],
          "description": "ISO date strings bounding the selectable range. Values outside the range are rejected.",
          "source": "storybook"
        },
        {
          "name": "monthInput",
          "attribute": "append-to",
          "type": "HTMLInputElement",
          "values": [],
          "description": "id of the element the popover is appended to.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "Placement",
          "default": "POPOVER_PLACEMENT.BOTTOM_START",
          "values": [
            "bottom-start",
            "top-start"
          ],
          "description": "preferred popover placement around the trigger. Default `bottom-start`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "non-interactive but focusable; typing is suppressed and the popover cannot open. The current value is still submitted.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the form is invalid unless a value is selected.",
          "source": "storybook"
        },
        {
          "name": "selectionMode",
          "attribute": "selection-mode",
          "type": "select",
          "default": "SELECTION_MODE.SINGLE",
          "values": [],
          "description": "`single` (default), `range`, or `week`. The default variant promotes `single` to `range` (single dates inside the calendar are still allowed).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "strategy",
          "attribute": "strategy",
          "type": "PopoverStrategy",
          "default": "'absolute'",
          "values": [],
          "description": "popover positioning strategy. `absolute` (default) or `fixed`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "selected date as an ISO `YYYY-MM-DD` string. For range/week selections, this is the start date.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'input'",
          "values": [
            "default",
            "input"
          ],
          "description": "`input` (default) renders spinbutton fields; `default` renders a select-style trigger.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the date value changes."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the date value is committed."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the datepicker receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the datepicker loses focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element."
        },
        {
          "name": "help-text-container",
          "description": "The container for helper/validation elements."
        },
        {
          "name": "container",
          "description": "The outer container for the input and popover."
        },
        {
          "name": "base-container",
          "description": "The input container with border and background."
        },
        {
          "name": "spinbutton-group",
          "description": "The container for spinbutton elements."
        },
        {
          "name": "spinbutton",
          "description": "A spinbutton input element."
        },
        {
          "name": "separator",
          "description": "The separator between spinbuttons."
        },
        {
          "name": "icon-container",
          "description": "The calendar/dropdown button container."
        },
        {
          "name": "native-input",
          "description": "The hidden native input for form participation."
        },
        {
          "name": "select-text",
          "description": "The selected value display text (default variant)."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-datepicker-background-color",
          "description": "Background color of the datepicker input."
        },
        {
          "name": "--mdc-datepicker-border-color",
          "description": "Border color of the datepicker input."
        },
        {
          "name": "--mdc-datepicker-text-color",
          "description": "Text color of the datepicker input."
        },
        {
          "name": "--mdc-datepicker-height",
          "description": "The height of the datepicker trigger control."
        },
        {
          "name": "--mdc-datepicker-width",
          "description": "Width of the datepicker component."
        }
      ],
      "rules": [
        "`input` (default) renders spinbutton fields; `default` renders a select-style trigger.",
        "selected date as an ISO `YYYY-MM-DD` string. For range/week selections, this is the start date.",
        "end date as an ISO `YYYY-MM-DD` string for range/week selections. When set together with `value` and `name`, both are submitted as `name` and `name-end`.",
        "`single` (default), `range`, or `week`. The default variant promotes `single` to `range` (single dates inside the calendar are still allowed).",
        "BCP 47 locale string (e.g. `en-US`, `de-DE`). Drives field order, separator, placeholders, and weekday/month names in the calendar. Default `en-US`.",
        "ISO date strings bounding the selectable range. Values outside the range are rejected.",
        "ISO date strings bounding the selectable range. Values outside the range are rejected.",
        "preferred popover placement around the trigger. Default `bottom-start`.",
        "popover positioning strategy. `absolute` (default) or `fixed`.",
        "when `true`, the popover does not flip to the opposite side when the preferred placement does not fit. Default `false`.",
        "id of the element the popover is appended to.",
        "id of the element the popover backdrop is appended to.",
        "accessible label for the calendar toggle button (input variant) and the combobox trigger (default variant).",
        "`aria-description` applied to every spinbutton input. Use it to explain the increment/decrement behaviour (\"Use up and down arrows to change\").",
        "label for the \"today\" button inside the calendar.",
        "accessible labels for the calendar's month-navigation arrows.",
        "accessible labels for the calendar's month-navigation arrows.",
        "visible label rendered above the field.",
        "accessible name fallback when no visible label is rendered.",
        "helper or validation text below the field.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.",
        "when `true`, the form is invalid unless a value is selected.",
        "fully disabled; the field is removed from the tab order and the popover cannot open.",
        "non-interactive but focusable; typing is suppressed and the popover cannot open. The current value is still submitted."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/datepicker/index.js\"; // custom element registration\n// or via React wrapper\nimport { Datepicker } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-datepicker\n  label=\"Date of birth\"\n  name=\"dob\"\n  variant=\"input\"\n  locale=\"en-US\"\n  locale-month-label=\"Month\"\n  locale-day-label=\"Day\"\n  locale-year-label=\"Year\"\n  locale-calendar-label=\"Open calendar\"\n></mdc-datepicker>"
      },
      {
        "language": "html",
        "code": "<mdc-datepicker\n  label=\"Stay\"\n  name=\"stay\"\n  variant=\"default\"\n  selection-mode=\"range\"\n  locale=\"en-US\"\n  locale-calendar-label=\"Choose a date range\"\n></mdc-datepicker>"
      }
    ],
    "reactImports": [
      "Datepicker"
    ],
    "constants": {
      "VARIANT": {
        "DEFAULT": "default",
        "INPUT": "input"
      },
      "DEFAULTS": {
        "VARIANT": "input",
        "LOCALE": "en-US",
        "DISABLE_FLIP": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "button",
      "calendar",
      "component",
      "date",
      "datepicker",
      "entry",
      "for",
      "guidelines",
      "icon",
      "input",
      "locale",
      "manual",
      "mdc",
      "offering",
      "or",
      "overview",
      "popover",
      "range",
      "selection",
      "single",
      "support",
      "text",
      "the",
      "toggletip",
      "usage",
      "week",
      "with"
    ]
  },
  {
    "name": "dialog",
    "title": "Dialog",
    "tagName": "mdc-dialog",
    "summary": "Usage, guidelines, and accessibility for the mdc-dialog component — a modal overlay with a titled, optionally described surface containing custom body content and footer actions.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/dialog",
      "knowledgeBasePath": "components/src/components/dialog/knowledge-base/dialog.component.md"
    },
    "docs": "## Overview\n\nA dialog is a modal surface that interrupts the user to display information, confirm a destructive action, or collect input. It blocks interaction with the rest of the application until it is closed.\n\nThe dialog is a controlled component: the consumer drives visibility through the `visible` property and listens to the `close` event to react to the user dismissing the dialog. Visibility is not toggled internally.\n\nFive sizes are available: `small` (432px), `medium` (656px), `large` (992px), `xlarge` (90% viewport), and `fullscreen` (100% viewport). Two visual variants are available: `default` and `promotional`. When the surrounding `mdc-responsivesettingsprovider` has `forceFullscreenDialog` enabled, every dialog is rendered as `fullscreen` regardless of its `size`.\n\n### When to use\n\n- Use `mdc-dialog` to confirm a destructive action (e.g. deleting an item) before it happens.\n- Use it to collect a short input that should block the underlying flow until completed.\n- Use it for messages that demand the user's attention and cannot be deferred to a banner or toast.\n\n### When not to use\n\n- Use `mdc-announcementdialog` when the surface is a curated marketing/announcement layout with a fixed structure.\n- Use `mdc-popover` or `mdc-coachmark` for non-modal overlays anchored to a trigger.\n- Use `mdc-toast` for transient, dismissable notifications that should not block interaction.\n- Use `mdc-banner` for persistent in-page messages.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/dialog/index.js\"; // custom element registration\n// or via React wrapper\nimport { Dialog } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button id=\"open-dialog\">Open dialog</mdc-button>\n\n<mdc-dialog\n  id=\"confirm-delete\"\n  visible\n  triggerID=\"open-dialog\"\n  header-text=\"Delete item?\"\n  description-text=\"This action cannot be undone.\"\n  close-button-aria-label=\"Close\"\n>\n  <p slot=\"dialog-body\">\n    The selected item will be permanently removed from your account.\n  </p>\n  <mdc-button slot=\"footer-button-secondary\" variant=\"secondary\">Cancel</mdc-button>\n  <mdc-button slot=\"footer-button-primary\">Delete</mdc-button>\n</mdc-dialog>\n```\n\nThe dialog renders nothing in the DOM until `visible` becomes `true`. Listen to `close` to detect the user pressing the close button or `Escape` (the consumer must set `visible=\"false\"` in response). Listen to `shown`, `hidden`, `created`, and `destroyed` to drive surrounding state.\n\nProvide a `triggerID` pointing at the element that opens the dialog so the component can return focus there on close and toggle `aria-expanded` / `aria-haspopup` on it. Without `triggerID`, the dialog returns focus to whichever element was active when it opened.\n\nAvailable slots:\n\n- `header-prefix` — content rendered before the `header-text` (e.g. an icon).\n- `dialog-body` — the main body content.\n- `footer-link` — slot for an `mdc-link` in the footer.\n- `footer-button-secondary` — slot for the secondary `mdc-button` in the footer.\n- `footer-button-primary` — slot for the primary `mdc-button` in the footer.\n- `footer` — fully custom footer content; prefer the named footer slots when possible.\n\nWhen multiple dialogs are open at once, the component uses an internal depth manager to compute z-index so that the most recently opened dialog stacks on top. Pressing `Escape` closes only the top-most dialog.\n\n### Content guidance\n\n- Keep `header-text` short and action-oriented — it doubles as the accessible name when no explicit `aria-label` is provided.\n- Use `description-text` for one or two sentences of context; longer content belongs in the body slot.\n- Use `close-button-aria-label` to describe what closing the dialog does (e.g. \"Close delete confirmation\"); a generic \"Close\" is acceptable when the header already conveys context.\n- Pair primary and secondary footer buttons clearly: the destructive or confirming action is the primary, the cancel/back action is the secondary.\n\n### Property/Attribute details\n\n- `visible` — controls the dialog's visibility. Default `false`. The component does not manage this internally.\n- `triggerID` — id of the element that opened the dialog. Focus returns here on close, and `aria-expanded` / `aria-haspopup=\"dialog\"` are managed on it while the dialog is open.\n- `size` — `small` (default), `medium`, `large`, `xlarge`, or `fullscreen`. Overridden to `fullscreen` when the responsive settings provider has `forceFullscreenDialog` enabled.\n- `variant` — `default` or `promotional`. Drives the visual treatment and the footer button colours.\n- `header-text` — title rendered inside the header section. Used as the accessible name when `aria-label`/`aria-labelledby` are not set.\n- `description-text` — descriptive text rendered below the header.\n- `header-tag-name` / `description-tag-name` — HTML tag (`h1`–`h6`, `p`, etc.) used to render the header/description text. Default `header-tag-name=\"h2\"`.\n- `close-button-aria-label` — accessible label for the built-in close button. Provide a meaningful value; the close button has no visible text.\n- `aria-label` — accessible name for the dialog when `header-text` is not used (or when a different name is needed).\n- `aria-labelledby` — id of an element that labels the dialog. Use this when the title is rendered inside the body slot rather than via `header-text`.\n- `aria-describedby` / `aria-description` — accessible description, set automatically from `description-text` and `triggerID` when not provided.\n- `role` — defaults to `dialog`. Override (e.g. to `alertdialog`) when a different role better describes the surface.\n- `disable-aria-haspopup` — when `true`, suppresses setting `aria-haspopup=\"dialog\"` on the trigger. Default `false`. Set to `true` when the trigger already exposes a different `aria-haspopup`.\n- `focus-trap` — when `true` (default), focus is restricted to elements inside the dialog while it is open. Disable only in very rare cases — a modal dialog should normally trap focus.\n- `hide-backdrop` — when `true`, no backdrop is rendered. Default `false`.\n- `stack-group-name` — when set, the dialog joins the named depth-manager group; only one dialog per group is rendered on top at a time.\n- `z-index` — explicit z-index override. When not set, the depth manager computes it based on the number of stacked overlays.\n\n### Limitations\n\n- The dialog is not form-associated and does not participate in native form submission. To submit a form from within a dialog, render an `mdc-button` with `type=\"submit\"` whose `form` attribute points at a `<form>` rendered outside the dialog (or inside the body slot).\n- `Escape` closes only the top-most dialog in the depth manager. If multiple dialogs are visible at once, `Escape` is consumed by the top dialog and does not bubble to surrounding handlers.\n\n## Accessibility\n\n### Built-in features\n\nThe dialog renders with `role=\"dialog\"` and `aria-modal=\"true\"`. While visible:\n\n- A focus trap keeps keyboard focus inside the dialog (`focus-trap` is `true` by default).\n- `Escape` closes the dialog by firing the `close` event (the consumer is expected to set `visible=\"false\"`).\n- The close button is focusable and also fires `close` on activation.\n- Page scrolling is prevented while the dialog is open (`preventScroll`).\n- A backdrop is rendered behind the dialog (unless `hide-backdrop` is `true`) and prevents interaction with the rest of the page.\n- On close, focus returns to the element identified by `triggerID`, or to the element that was focused when the dialog opened.\n- The trigger element receives `aria-expanded=\"true\"` while the dialog is open and `aria-expanded=\"false\"` when closed. Unless `disable-aria-haspopup` is `true`, the trigger also receives `aria-haspopup=\"dialog\"`.\n\n`header-text` is automatically wired as the dialog's `aria-label` when no `aria-label`/`aria-labelledby` is provided. `description-text` is wired as `aria-description` when no `aria-describedby`/`aria-description` is provided. When neither header nor description text is provided but `triggerID` is set, `aria-labelledby` and `aria-describedby` fall back to the trigger's id.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute          | Value                                                                       |\n| -------------- | ------------------ | --------------------------------------------------------------------------- |\n| Host (dialog)  | `role`             | `dialog` (default; override via `role`)                                     |\n| Host (dialog)  | `aria-modal`       | `true`                                                                      |\n| Host (dialog)  | `aria-label`       | set from `header-text` when no `aria-label`/`aria-labelledby` provided      |\n| Host (dialog)  | `aria-labelledby`  | set from `triggerID` when no header text and no explicit labelling provided |\n| Host (dialog)  | `aria-description` | set from `description-text` when no `aria-describedby` provided             |\n| Host (dialog)  | `aria-describedby` | set from `triggerID` when no description text provided                      |\n| Close button   | `aria-label`       | mirrors `close-button-aria-label`                                           |\n| Trigger        | `aria-expanded`    | `true` while the dialog is visible, `false` after close                     |\n| Trigger        | `aria-haspopup`    | `dialog` (suppressed when `disable-aria-haspopup` is `true`)                |\n\n### Implementation requirements\n\n#### General\n\n- Drive `visible` from consumer state; the dialog does not manage it. Listen for `close` and set `visible` to `false` to dismiss.\n- Provide `triggerID` so focus returns to the opener element and the trigger's `aria-expanded` is updated.\n- When multiple dialogs may open at once, set `stack-group-name` to coordinate stacking and avoid duplicate top-most dialogs.\n- When the dialog hosts a form, place the `<form>` outside the dialog (or inside the body slot) and connect submit buttons via the `form` attribute.\n\n#### Labelling\n\n- Provide `header-text` whenever possible — it doubles as the accessible name.\n- If the title is rendered inside the body slot instead of `header-text`, set `aria-labelledby` pointing at that element.\n- Always set `close-button-aria-label` — the close button has no visible text and otherwise has no accessible name.",
    "sections": {
      "Overview": "A dialog is a modal surface that interrupts the user to display information, confirm a destructive action, or collect input. It blocks interaction with the rest of the application until it is closed.\n\nThe dialog is a controlled component: the consumer drives visibility through the `visible` property and listens to the `close` event to react to the user dismissing the dialog. Visibility is not toggled internally.\n\nFive sizes are available: `small` (432px), `medium` (656px), `large` (992px), `xlarge` (90% viewport), and `fullscreen` (100% viewport). Two visual variants are available: `default` and `promotional`. When the surrounding `mdc-responsivesettingsprovider` has `forceFullscreenDialog` enabled, every dialog is rendered as `fullscreen` regardless of its `size`.\n\n### When to use\n\n- Use `mdc-dialog` to confirm a destructive action (e.g. deleting an item) before it happens.\n- Use it to collect a short input that should block the underlying flow until completed.\n- Use it for messages that demand the user's attention and cannot be deferred to a banner or toast.\n\n### When not to use\n\n- Use `mdc-announcementdialog` when the surface is a curated marketing/announcement layout with a fixed structure.\n- Use `mdc-popover` or `mdc-coachmark` for non-modal overlays anchored to a trigger.\n- Use `mdc-toast` for transient, dismissable notifications that should not block interaction.\n- Use `mdc-banner` for persistent in-page messages.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/dialog/index.js\"; // custom element registration\n// or via React wrapper\nimport { Dialog } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button id=\"open-dialog\">Open dialog</mdc-button>\n\n<mdc-dialog\n  id=\"confirm-delete\"\n  visible\n  triggerID=\"open-dialog\"\n  header-text=\"Delete item?\"\n  description-text=\"This action cannot be undone.\"\n  close-button-aria-label=\"Close\"\n>\n  <p slot=\"dialog-body\">\n    The selected item will be permanently removed from your account.\n  </p>\n  <mdc-button slot=\"footer-button-secondary\" variant=\"secondary\">Cancel</mdc-button>\n  <mdc-button slot=\"footer-button-primary\">Delete</mdc-button>\n</mdc-dialog>\n```\n\nThe dialog renders nothing in the DOM until `visible` becomes `true`. Listen to `close` to detect the user pressing the close button or `Escape` (the consumer must set `visible=\"false\"` in response). Listen to `shown`, `hidden`, `created`, and `destroyed` to drive surrounding state.\n\nProvide a `triggerID` pointing at the element that opens the dialog so the component can return focus there on close and toggle `aria-expanded` / `aria-haspopup` on it. Without `triggerID`, the dialog returns focus to whichever element was active when it opened.\n\nAvailable slots:\n\n- `header-prefix` — content rendered before the `header-text` (e.g. an icon).\n- `dialog-body` — the main body content.\n- `footer-link` — slot for an `mdc-link` in the footer.\n- `footer-button-secondary` — slot for the secondary `mdc-button` in the footer.\n- `footer-button-primary` — slot for the primary `mdc-button` in the footer.\n- `footer` — fully custom footer content; prefer the named footer slots when possible.\n\nWhen multiple dialogs are open at once, the component uses an internal depth manager to compute z-index so that the most recently opened dialog stacks on top. Pressing `Escape` closes only the top-most dialog.\n\n### Content guidance\n\n- Keep `header-text` short and action-oriented — it doubles as the accessible name when no explicit `aria-label` is provided.\n- Use `description-text` for one or two sentences of context; longer content belongs in the body slot.\n- Use `close-button-aria-label` to describe what closing the dialog does (e.g. \"Close delete confirmation\"); a generic \"Close\" is acceptable when the header already conveys context.\n- Pair primary and secondary footer buttons clearly: the destructive or confirming action is the primary, the cancel/back action is the secondary.\n\n### Property/Attribute details\n\n- `visible` — controls the dialog's visibility. Default `false`. The component does not manage this internally.\n- `triggerID` — id of the element that opened the dialog. Focus returns here on close, and `aria-expanded` / `aria-haspopup=\"dialog\"` are managed on it while the dialog is open.\n- `size` — `small` (default), `medium`, `large`, `xlarge`, or `fullscreen`. Overridden to `fullscreen` when the responsive settings provider has `forceFullscreenDialog` enabled.\n- `variant` — `default` or `promotional`. Drives the visual treatment and the footer button colours.\n- `header-text` — title rendered inside the header section. Used as the accessible name when `aria-label`/`aria-labelledby` are not set.\n- `description-text` — descriptive text rendered below the header.\n- `header-tag-name` / `description-tag-name` — HTML tag (`h1`–`h6`, `p`, etc.) used to render the header/description text. Default `header-tag-name=\"h2\"`.\n- `close-button-aria-label` — accessible label for the built-in close button. Provide a meaningful value; the close button has no visible text.\n- `aria-label` — accessible name for the dialog when `header-text` is not used (or when a different name is needed).\n- `aria-labelledby` — id of an element that labels the dialog. Use this when the title is rendered inside the body slot rather than via `header-text`.\n- `aria-describedby` / `aria-description` — accessible description, set automatically from `description-text` and `triggerID` when not provided.\n- `role` — defaults to `dialog`. Override (e.g. to `alertdialog`) when a different role better describes the surface.\n- `disable-aria-haspopup` — when `true`, suppresses setting `aria-haspopup=\"dialog\"` on the trigger. Default `false`. Set to `true` when the trigger already exposes a different `aria-haspopup`.\n- `focus-trap` — when `true` (default), focus is restricted to elements inside the dialog while it is open. Disable only in very rare cases — a modal dialog should normally trap focus.\n- `hide-backdrop` — when `true`, no backdrop is rendered. Default `false`.\n- `stack-group-name` — when set, the dialog joins the named depth-manager group; only one dialog per group is rendered on top at a time.\n- `z-index` — explicit z-index override. When not set, the depth manager computes it based on the number of stacked overlays.\n\n### Limitations\n\n- The dialog is not form-associated and does not participate in native form submission. To submit a form from within a dialog, render an `mdc-button` with `type=\"submit\"` whose `form` attribute points at a `<form>` rendered outside the dialog (or inside the body slot).\n- `Escape` closes only the top-most dialog in the depth manager. If multiple dialogs are visible at once, `Escape` is consumed by the top dialog and does not bubble to surrounding handlers.",
      "Accessibility": "### Built-in features\n\nThe dialog renders with `role=\"dialog\"` and `aria-modal=\"true\"`. While visible:\n\n- A focus trap keeps keyboard focus inside the dialog (`focus-trap` is `true` by default).\n- `Escape` closes the dialog by firing the `close` event (the consumer is expected to set `visible=\"false\"`).\n- The close button is focusable and also fires `close` on activation.\n- Page scrolling is prevented while the dialog is open (`preventScroll`).\n- A backdrop is rendered behind the dialog (unless `hide-backdrop` is `true`) and prevents interaction with the rest of the page.\n- On close, focus returns to the element identified by `triggerID`, or to the element that was focused when the dialog opened.\n- The trigger element receives `aria-expanded=\"true\"` while the dialog is open and `aria-expanded=\"false\"` when closed. Unless `disable-aria-haspopup` is `true`, the trigger also receives `aria-haspopup=\"dialog\"`.\n\n`header-text` is automatically wired as the dialog's `aria-label` when no `aria-label`/`aria-labelledby` is provided. `description-text` is wired as `aria-description` when no `aria-describedby`/`aria-description` is provided. When neither header nor description text is provided but `triggerID` is set, `aria-labelledby` and `aria-describedby` fall back to the trigger's id.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute          | Value                                                                       |\n| -------------- | ------------------ | --------------------------------------------------------------------------- |\n| Host (dialog)  | `role`             | `dialog` (default; override via `role`)                                     |\n| Host (dialog)  | `aria-modal`       | `true`                                                                      |\n| Host (dialog)  | `aria-label`       | set from `header-text` when no `aria-label`/`aria-labelledby` provided      |\n| Host (dialog)  | `aria-labelledby`  | set from `triggerID` when no header text and no explicit labelling provided |\n| Host (dialog)  | `aria-description` | set from `description-text` when no `aria-describedby` provided             |\n| Host (dialog)  | `aria-describedby` | set from `triggerID` when no description text provided                      |\n| Close button   | `aria-label`       | mirrors `close-button-aria-label`                                           |\n| Trigger        | `aria-expanded`    | `true` while the dialog is visible, `false` after close                     |\n| Trigger        | `aria-haspopup`    | `dialog` (suppressed when `disable-aria-haspopup` is `true`)                |\n\n### Implementation requirements\n\n#### General\n\n- Drive `visible` from consumer state; the dialog does not manage it. Listen for `close` and set `visible` to `false` to dismiss.\n- Provide `triggerID` so focus returns to the opener element and the trigger's `aria-expanded` is updated.\n- When multiple dialogs may open at once, set `stack-group-name` to coordinate stacking and avoid duplicate top-most dialogs.\n- When the dialog hosts a form, place the `<form>` outside the dialog (or inside the body slot) and connect submit buttons via the `form` attribute.\n\n#### Labelling\n\n- Provide `header-text` whenever possible — it doubles as the accessible name.\n- If the title is rendered inside the body slot instead of `header-text`, set `aria-labelledby` pointing at that element.\n- Always set `close-button-aria-label` — the close button has no visible text and otherwise has no accessible name."
    },
    "dependencies": [
      "mdc-button",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaDescribedby",
          "attribute": "aria-describedby",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "accessible description, set automatically from `description-text` and `triggerID` when not provided.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "ariaDescription",
          "attribute": "aria-description",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "accessible description, set automatically from `description-text` and `triggerID` when not provided.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "accessible name for the dialog when `header-text` is not used (or when a different name is needed).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "id of an element that labels the dialog. Use this when the title is rendered inside the body slot rather than via `header-text`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "closeButtonAriaLabel",
          "attribute": "close-button-aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "accessible label for the built-in close button. Provide a meaningful value; the close button has no visible text.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "descriptionTagName",
          "attribute": "description-tag-name",
          "type": "text",
          "default": "DEFAULTS.DESCRIPTION_TAG_NAME",
          "values": [],
          "description": "HTML tag (`h1`–`h6`, `p`, etc.) used to render the header/description text. Default `header-tag-name=\"h2\"`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "descriptionText",
          "attribute": "description-text",
          "type": "text",
          "values": [],
          "description": "descriptive text rendered below the header.",
          "source": "storybook"
        },
        {
          "name": "disableAriaHasPopup",
          "attribute": "disable-aria-haspopup",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, suppresses setting `aria-haspopup=\"dialog\"` on the trigger. Default `false`. Set to `true` when the trigger already exposes a different `aria-haspopup`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "focusTrap",
          "attribute": "focus-trap",
          "type": "boolean",
          "default": "true",
          "values": [],
          "description": "when `true` (default), focus is restricted to elements inside the dialog while it is open. Disable only in very rare cases — a modal dialog should normally trap focus.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "headerTagName",
          "attribute": "header-tag-name",
          "type": "text",
          "default": "DEFAULTS.HEADER_TAG_NAME",
          "values": [],
          "description": "HTML tag (`h1`–`h6`, `p`, etc.) used to render the header/description text. Default `header-tag-name=\"h2\"`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "values": [],
          "description": "title rendered inside the header section. Used as the accessible name when `aria-label`/`aria-labelledby` are not set.",
          "source": "storybook"
        },
        {
          "name": "hideBackdrop",
          "attribute": "hide-backdrop",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, no backdrop is rendered. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "default": "''",
          "values": [],
          "description": "(this does not hide the dialog)\nwithin the footer section.\n`mdc-button` component within the footer section.\nusing the footer-link and footer-button slots is preferred\n/\nclass Dialog\nextends KeyDownHandledMixin(\nKeyToActionMixin(BackdropMixin(PreventScrollMixin(FocusTrapMixin(FooterMixin(Component))))),\n)\nimplements StackedOverlayComponent\n{\n/** @internal */\nprotected readonly responsiveSettingsContext = providerUtils.consume<typeof ResponsiveSettingsContext.context>({\nhost: this,\ncontext: ResponsiveSettingsProvider.Context,\nsubscribe: true,\nsyncProperties: ['size'],\n});\n/** track the depth of the popover for z-index calculation\n/\nprivate depthManager = new DepthManager(this);\n/**\nThe unique ID of the dialog",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "role",
          "attribute": "role",
          "type": "select",
          "default": "dialog",
          "values": [],
          "description": "defaults to `dialog`. Override (e.g. to `alertdialog`) when a different role better describes the surface.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "shouldFocusTrapWrap",
          "attribute": "should-focus-trap-wrap",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "values": [],
          "description": "`small` (default), `medium`, `large`, `xlarge`, or `fullscreen`. Overridden to `fullscreen` when the responsive settings provider has `forceFullscreenDialog` enabled.",
          "source": "storybook"
        },
        {
          "name": "stackGroupName",
          "attribute": "stack-group-name",
          "type": "string",
          "default": "''",
          "values": [],
          "description": "when set, the dialog joins the named depth-manager group; only one dialog per group is rendered on top at a time.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "triggerId",
          "attribute": "trigger-id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "DEFAULTS.VARIANT",
          "values": [
            "default",
            "promotional"
          ],
          "description": "`default` or `promotional`. Drives the visual treatment and the footer button colours.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "visible",
          "attribute": "visible",
          "type": "boolean",
          "default": "undefined",
          "values": [],
          "description": "controls the dialog's visibility. Default `false`. The component does not manage this internally.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "zIndex",
          "attribute": "z-index",
          "type": "number",
          "values": [],
          "description": "explicit z-index override. When not set, the depth manager computes it based on the number of stacked overlays.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "header-prefix",
          "description": "Slot for the dialog header content. This can be used to pass custom header content."
        },
        {
          "name": "dialog-body",
          "description": "Slot for the dialog body content"
        },
        {
          "name": "footer-link",
          "description": "This slot is for passing `mdc-link` component within the footer section."
        },
        {
          "name": "footer-button-secondary",
          "description": "This slot is for passing secondary variant of `mdc-button` component"
        },
        {
          "name": "footer-button-primary",
          "description": "This slot is for passing primary variant of"
        },
        {
          "name": "footer",
          "description": "This slot is for passing custom footer content. Only use this if really needed,"
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) Dispatched when the dialog is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) Dispatched when the dialog is hidden"
        },
        {
          "name": "created",
          "description": "(React: onCreated) Dispatched when the dialog is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) Dispatched when the dialog is destroyed (removed from the DOM)"
        },
        {
          "name": "close",
          "description": "(React: onClose) Dispatched when the Close Button is clicked or Escape key is pressed"
        }
      ],
      "cssParts": [
        {
          "name": "body",
          "description": "The body section of the dialog."
        },
        {
          "name": "description-text",
          "description": "The description text of the dialog."
        },
        {
          "name": "dialog-close-btn",
          "description": "The close button of the dialog."
        },
        {
          "name": "header",
          "description": "The header of the dialog."
        },
        {
          "name": "header-section",
          "description": "The header section of the dialog."
        },
        {
          "name": "header-text",
          "description": "The header text of the dialog."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-dialog-primary-background-color",
          "description": "primary background color of the dialog"
        },
        {
          "name": "--mdc-dialog-border-color",
          "description": "border color of the dialog"
        },
        {
          "name": "--mdc-dialog-header-text-color",
          "description": "text color of the header/title of the dialog"
        },
        {
          "name": "--mdc-dialog-description-text-color",
          "description": "text color of the below header description of the dialog"
        },
        {
          "name": "--mdc-dialog-elevation-3",
          "description": "elevation of the dialog"
        },
        {
          "name": "--mdc-dialog-width",
          "description": "width of the dialog"
        },
        {
          "name": "--mdc-dialog-height",
          "description": "height of the dialog"
        },
        {
          "name": "--mdc-dialog-backdrop-color",
          "description": "background color of the backdrop (if backdrop is enabled)"
        }
      ],
      "rules": [
        "controls the dialog's visibility. Default `false`. The component does not manage this internally.",
        "id of the element that opened the dialog. Focus returns here on close, and `aria-expanded` / `aria-haspopup=\"dialog\"` are managed on it while the dialog is open.",
        "`small` (default), `medium`, `large`, `xlarge`, or `fullscreen`. Overridden to `fullscreen` when the responsive settings provider has `forceFullscreenDialog` enabled.",
        "`default` or `promotional`. Drives the visual treatment and the footer button colours.",
        "title rendered inside the header section. Used as the accessible name when `aria-label`/`aria-labelledby` are not set.",
        "descriptive text rendered below the header.",
        "HTML tag (`h1`–`h6`, `p`, etc.) used to render the header/description text. Default `header-tag-name=\"h2\"`.",
        "HTML tag (`h1`–`h6`, `p`, etc.) used to render the header/description text. Default `header-tag-name=\"h2\"`.",
        "accessible label for the built-in close button. Provide a meaningful value; the close button has no visible text.",
        "accessible name for the dialog when `header-text` is not used (or when a different name is needed).",
        "id of an element that labels the dialog. Use this when the title is rendered inside the body slot rather than via `header-text`.",
        "accessible description, set automatically from `description-text` and `triggerID` when not provided.",
        "accessible description, set automatically from `description-text` and `triggerID` when not provided.",
        "defaults to `dialog`. Override (e.g. to `alertdialog`) when a different role better describes the surface.",
        "when `true`, suppresses setting `aria-haspopup=\"dialog\"` on the trigger. Default `false`. Set to `true` when the trigger already exposes a different `aria-haspopup`.",
        "when `true` (default), focus is restricted to elements inside the dialog while it is open. Disable only in very rare cases — a modal dialog should normally trap focus.",
        "when `true`, no backdrop is rendered. Default `false`.",
        "when set, the dialog joins the named depth-manager group; only one dialog per group is rendered on top at a time.",
        "explicit z-index override. When not set, the depth manager computes it based on the number of stacked overlays."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/dialog/index.js\"; // custom element registration\n// or via React wrapper\nimport { Dialog } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"open-dialog\">Open dialog</mdc-button>\n\n<mdc-dialog\n  id=\"confirm-delete\"\n  visible\n  triggerID=\"open-dialog\"\n  header-text=\"Delete item?\"\n  description-text=\"This action cannot be undone.\"\n  close-button-aria-label=\"Close\"\n>\n  <p slot=\"dialog-body\">\n    The selected item will be permanently removed from your account.\n  </p>\n  <mdc-button slot=\"footer-button-secondary\" variant=\"secondary\">Cancel</mdc-button>\n  <mdc-button slot=\"footer-button-primary\">Delete</mdc-button>\n</mdc-dialog>"
      }
    ],
    "reactImports": [
      "Dialog"
    ],
    "constants": {
      "DIALOG_VARIANT": {
        "DEFAULT": "default",
        "PROMOTIONAL": "promotional"
      },
      "DEFAULTS": {
        "VISIBLE": false,
        "Z_INDEX": 1000,
        "SIZE": "small",
        "CANCEL_ICON": "'cancel-bold' as Extract<IconNames, 'cancel-bold'>",
        "VARIANT": "default",
        "DISABLE_ARIA_HAS_POPUP": false,
        "FOCUS_TRAP": true,
        "HIDE_BACKDROP": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "actions",
      "and",
      "body",
      "button",
      "component",
      "containing",
      "content",
      "custom",
      "described",
      "dialog",
      "footer",
      "for",
      "guidelines",
      "mdc",
      "modal",
      "optionally",
      "overlay",
      "overview",
      "surface",
      "text",
      "the",
      "titled",
      "usage",
      "with"
    ]
  },
  {
    "name": "divider",
    "title": "Divider",
    "tagName": "mdc-divider",
    "summary": "Usage, guidelines, and accessibility for the mdc-divider component — a thin horizontal or vertical separator that can optionally host a centered text label or an interactive grabber button.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/divider",
      "knowledgeBasePath": "components/src/components/divider/knowledge-base/divider.component.md"
    },
    "docs": "## Overview\n\nThe divider draws a thin separator line between sections of a layout. It supports both horizontal and vertical orientations and two visual variants — `solid` (uniform colour) and `gradient` (fades on both ends).\n\nThe component infers its type from the slotted content:\n\n- **Primary** — no slotted content; renders just the line.\n- **Text** — a single `mdc-text` child; renders the line with a centred label.\n- **Grabber Button** — a single `mdc-button` child; renders the line with a centred interactive button (typically used to collapse/expand a resizable pane).\n\nIf the slot contains multiple elements or unrecognised tag names, the divider falls back to the primary type.\n\n### When to use\n\n- Use `mdc-divider` to separate sections of content within a layout, list, or table.\n- Use a text divider to add a short caption between two visually similar sections.\n- Use a grabber-button divider to create a resizable boundary between two panes.\n\n### When not to use\n\n- Use whitespace, padding, or background-colour groupings when a visible line is not needed — a divider should add meaning, not decoration.\n- Use `mdc-list` separators rather than nesting dividers inside list items, since lists already render their own dividers.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/divider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Divider } from '@momentum-design/components/dist/react';\n```\n\nPrimary divider:\n\n```html\n<mdc-divider></mdc-divider>\n```\n\nText divider:\n\n```html\n<mdc-divider>\n  <mdc-text>OR</mdc-text>\n</mdc-divider>\n```\n\nGrabber-button divider:\n\n```html\n<mdc-divider arrow-direction=\"negative\" button-position=\"negative\">\n  <mdc-button aria-label=\"Collapse panel\"></mdc-button>\n</mdc-divider>\n```\n\nThe component sets `data-type=\"mdc-primary-divider\"` on connect and updates it to `mdc-text-divider` or `mdc-grabber-divider` whenever the slot contents change.\n\nFor a grabber-button divider the component automatically applies `variant=\"secondary\"` and the correct `prefix-icon` to the slotted `mdc-button`, based on `orientation` and `arrow-direction`. The button is owned by the consumer for interactivity (e.g. `click` listeners that toggle a pane's collapsed state).\n\n### Content guidance\n\n- Keep text labels short — a single word (\"OR\") or a brief phrase fits the centred layout best.\n- Use the grabber-button divider only when the divider itself is the resize/expand affordance; otherwise prefer a primary divider next to a separate control.\n- Pick `variant=\"gradient\"` when the divider sits over a coloured surface and a hard solid line would feel heavy; otherwise prefer `solid`.\n\n### Property/Attribute details\n\n- `orientation` — `horizontal` (default) or `vertical`. Vertical text dividers are not currently supported.\n- `variant` — `solid` (default) or `gradient`. Drives the line's fill.\n- `arrow-direction` — direction of the arrow icon on the grabber button. `positive` (up for horizontal, right for vertical) or `negative` (down for horizontal, left for vertical). Default `negative`. Only relevant for the grabber-button divider.\n- `button-position` — position of the grabber button along the divider. `positive` (right for horizontal, top for vertical) or `negative` (left for horizontal, bottom for vertical). Default `negative`. Only relevant for the grabber-button divider.\n\n### Limitations\n\n- Vertical text dividers are not supported — combine `orientation=\"vertical\"` with a text child and the layout will not render the label centred. Use the horizontal variant for text dividers.\n- The divider type is inferred from a single slotted child. If the slot contains more than one element or unrecognised tag names, the divider falls back to the primary type and any extra content is ignored visually.\n\n## Accessibility\n\n### Built-in features\n\nThe divider is presentational and exposes no implicit ARIA role. When a grabber button is slotted, the button is fully focusable and keyboard-operable on its own (it is a standard `mdc-button`), and the consumer is responsible for wiring its accessibility (`aria-label`, `aria-expanded`, click handler) to the resize behaviour it controls.\n\nWhen a text label is slotted, the label is rendered inline through `mdc-text` and is read by screen readers in document order.\n\n#### Internal ARIA managed by the component\n\n| Element     | Attribute   | Value                                                                                       |\n| ----------- | ----------- | ------------------------------------------------------------------------------------------- |\n| Host        | `data-type` | `mdc-primary-divider` (default), `mdc-text-divider`, or `mdc-grabber-divider` (inferred)    |\n| Slot button | `variant`   | `secondary` (auto-applied to a slotted `mdc-button`)                                        |\n| Slot button | `prefix-icon` | arrow icon derived from `orientation` and `arrow-direction`                               |\n\n### Implementation requirements\n\n#### General\n\n- Place exactly one element in the slot when using a text or grabber-button divider; multiple elements fall back to the primary type.\n- Ensure sufficient contrast between the divider line and the background — the component does not enforce a contrast ratio.\n\n#### Labelling\n\n- When using a grabber-button divider, provide an `aria-label` on the slotted `mdc-button` describing what activating it does (\"Collapse panel\", \"Show details\").\n- When the grabber button controls an expandable region, also set `aria-expanded` on the button and update it from the consumer's state machine.",
    "sections": {
      "Overview": "The divider draws a thin separator line between sections of a layout. It supports both horizontal and vertical orientations and two visual variants — `solid` (uniform colour) and `gradient` (fades on both ends).\n\nThe component infers its type from the slotted content:\n\n- **Primary** — no slotted content; renders just the line.\n- **Text** — a single `mdc-text` child; renders the line with a centred label.\n- **Grabber Button** — a single `mdc-button` child; renders the line with a centred interactive button (typically used to collapse/expand a resizable pane).\n\nIf the slot contains multiple elements or unrecognised tag names, the divider falls back to the primary type.\n\n### When to use\n\n- Use `mdc-divider` to separate sections of content within a layout, list, or table.\n- Use a text divider to add a short caption between two visually similar sections.\n- Use a grabber-button divider to create a resizable boundary between two panes.\n\n### When not to use\n\n- Use whitespace, padding, or background-colour groupings when a visible line is not needed — a divider should add meaning, not decoration.\n- Use `mdc-list` separators rather than nesting dividers inside list items, since lists already render their own dividers.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/divider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Divider } from '@momentum-design/components/dist/react';\n```\n\nPrimary divider:\n\n```html\n<mdc-divider></mdc-divider>\n```\n\nText divider:\n\n```html\n<mdc-divider>\n  <mdc-text>OR</mdc-text>\n</mdc-divider>\n```\n\nGrabber-button divider:\n\n```html\n<mdc-divider arrow-direction=\"negative\" button-position=\"negative\">\n  <mdc-button aria-label=\"Collapse panel\"></mdc-button>\n</mdc-divider>\n```\n\nThe component sets `data-type=\"mdc-primary-divider\"` on connect and updates it to `mdc-text-divider` or `mdc-grabber-divider` whenever the slot contents change.\n\nFor a grabber-button divider the component automatically applies `variant=\"secondary\"` and the correct `prefix-icon` to the slotted `mdc-button`, based on `orientation` and `arrow-direction`. The button is owned by the consumer for interactivity (e.g. `click` listeners that toggle a pane's collapsed state).\n\n### Content guidance\n\n- Keep text labels short — a single word (\"OR\") or a brief phrase fits the centred layout best.\n- Use the grabber-button divider only when the divider itself is the resize/expand affordance; otherwise prefer a primary divider next to a separate control.\n- Pick `variant=\"gradient\"` when the divider sits over a coloured surface and a hard solid line would feel heavy; otherwise prefer `solid`.\n\n### Property/Attribute details\n\n- `orientation` — `horizontal` (default) or `vertical`. Vertical text dividers are not currently supported.\n- `variant` — `solid` (default) or `gradient`. Drives the line's fill.\n- `arrow-direction` — direction of the arrow icon on the grabber button. `positive` (up for horizontal, right for vertical) or `negative` (down for horizontal, left for vertical). Default `negative`. Only relevant for the grabber-button divider.\n- `button-position` — position of the grabber button along the divider. `positive` (right for horizontal, top for vertical) or `negative` (left for horizontal, bottom for vertical). Default `negative`. Only relevant for the grabber-button divider.\n\n### Limitations\n\n- Vertical text dividers are not supported — combine `orientation=\"vertical\"` with a text child and the layout will not render the label centred. Use the horizontal variant for text dividers.\n- The divider type is inferred from a single slotted child. If the slot contains more than one element or unrecognised tag names, the divider falls back to the primary type and any extra content is ignored visually.",
      "Accessibility": "### Built-in features\n\nThe divider is presentational and exposes no implicit ARIA role. When a grabber button is slotted, the button is fully focusable and keyboard-operable on its own (it is a standard `mdc-button`), and the consumer is responsible for wiring its accessibility (`aria-label`, `aria-expanded`, click handler) to the resize behaviour it controls.\n\nWhen a text label is slotted, the label is rendered inline through `mdc-text` and is read by screen readers in document order.\n\n#### Internal ARIA managed by the component\n\n| Element     | Attribute   | Value                                                                                       |\n| ----------- | ----------- | ------------------------------------------------------------------------------------------- |\n| Host        | `data-type` | `mdc-primary-divider` (default), `mdc-text-divider`, or `mdc-grabber-divider` (inferred)    |\n| Slot button | `variant`   | `secondary` (auto-applied to a slotted `mdc-button`)                                        |\n| Slot button | `prefix-icon` | arrow icon derived from `orientation` and `arrow-direction`                               |\n\n### Implementation requirements\n\n#### General\n\n- Place exactly one element in the slot when using a text or grabber-button divider; multiple elements fall back to the primary type.\n- Ensure sufficient contrast between the divider line and the background — the component does not enforce a contrast ratio.\n\n#### Labelling\n\n- When using a grabber-button divider, provide an `aria-label` on the slotted `mdc-button` describing what activating it does (\"Collapse panel\", \"Show details\").\n- When the grabber button controls an expandable region, also set `aria-expanded` on the button and update it from the consumer's state machine."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "arrowDirection",
          "attribute": "arrow-direction",
          "type": "select",
          "default": "negative",
          "values": [
            "positive",
            "negative"
          ],
          "description": "direction of the arrow icon on the grabber button. `positive` (up for horizontal, right for vertical) or `negative` (down for horizontal, left for vertical). Default `negative`. Only relevant for the grabber-button divider.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "buttonPosition",
          "attribute": "button-position",
          "type": "select",
          "default": "negative",
          "values": [
            "positive",
            "negative"
          ],
          "description": "position of the grabber button along the divider. `positive` (right for horizontal, top for vertical) or `negative` (left for horizontal, bottom for vertical). Default `negative`. Only relevant for the grabber-button divider.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "childrenText",
          "attribute": "children-text",
          "type": "text",
          "values": [],
          "description": "Text content for the text divider",
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "radio",
          "default": "horizontal",
          "values": [
            "horizontal",
            "vertical"
          ],
          "description": "`horizontal` (default) or `vertical`. Vertical text dividers are not currently supported.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "typeOfChildren",
          "attribute": "type-of-children",
          "type": "radio",
          "values": [
            "noChildren",
            "text",
            "grabber"
          ],
          "description": "Choose the type of content to render inside the divider",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "radio",
          "default": "solid",
          "values": [
            "solid",
            "gradient"
          ],
          "description": "`solid` (default) or `gradient`. Drives the line's fill.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Content for the divider. Use `mdc-text` for text labels or `mdc-button` for grabber buttons."
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-divider-background-color",
          "description": "Background color of the divider line."
        },
        {
          "name": "--mdc-divider-width",
          "description": "Width (thickness) of the divider line."
        },
        {
          "name": "--mdc-divider-horizontal-gradient",
          "description": "Gradient for horizontal dividers."
        },
        {
          "name": "--mdc-divider-vertical-gradient",
          "description": "Gradient for vertical dividers."
        },
        {
          "name": "--mdc-divider-text-size",
          "description": "Font size of the text label in text dividers."
        },
        {
          "name": "--mdc-divider-text-color",
          "description": "Font color of the text label in text dividers."
        },
        {
          "name": "--mdc-divider-text-margin",
          "description": "Left and right margin of the text label."
        },
        {
          "name": "--mdc-divider-text-line-height",
          "description": "Line height of the text label."
        },
        {
          "name": "--mdc-divider-grabber-button-background-color-normal",
          "description": "Background color of the grabber button in rest state."
        },
        {
          "name": "--mdc-divider-grabber-button-background-color-hover",
          "description": "Background color of the grabber button in hover state."
        },
        {
          "name": "--mdc-divider-grabber-button-background-color-pressed",
          "description": "Background color of the grabber button in pressed state."
        },
        {
          "name": "--mdc-divider-grabber-button-border-color",
          "description": "Border color of the grabber button."
        },
        {
          "name": "--mdc-divider-grabber-button-border-radius",
          "description": "Border radius of the grabber button."
        }
      ],
      "rules": [
        "`horizontal` (default) or `vertical`. Vertical text dividers are not currently supported.",
        "`solid` (default) or `gradient`. Drives the line's fill.",
        "direction of the arrow icon on the grabber button. `positive` (up for horizontal, right for vertical) or `negative` (down for horizontal, left for vertical). Default `negative`. Only relevant for the grabber-button divider.",
        "position of the grabber button along the divider. `positive` (right for horizontal, top for vertical) or `negative` (left for horizontal, bottom for vertical). Default `negative`. Only relevant for the grabber-button divider."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/divider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Divider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-divider></mdc-divider>"
      },
      {
        "language": "html",
        "code": "<mdc-divider>\n  <mdc-text>OR</mdc-text>\n</mdc-divider>"
      },
      {
        "language": "html",
        "code": "<mdc-divider arrow-direction=\"negative\" button-position=\"negative\">\n  <mdc-button aria-label=\"Collapse panel\"></mdc-button>\n</mdc-divider>"
      }
    ],
    "reactImports": [
      "Divider"
    ],
    "constants": {
      "DIVIDER_ORIENTATION": {
        "HORIZONTAL": "horizontal",
        "VERTICAL": "vertical"
      },
      "DIVIDER_VARIANT": {
        "SOLID": "solid",
        "GRADIENT": "gradient"
      },
      "DIRECTIONS": {
        "POSITIVE": "positive",
        "NEGATIVE": "negative"
      },
      "ARROW_ICONS": {
        "UP": "arrow-up-regular",
        "DOWN": "arrow-down-regular",
        "LEFT": "arrow-left-regular",
        "RIGHT": "arrow-right-regular"
      },
      "DEFAULTS": {
        "ORIENTATION": "horizontal",
        "VARIANT": "solid",
        "ARROW_DIRECTION": "negative",
        "BUTTON_DIRECTION": "negative"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "an",
      "and",
      "button",
      "can",
      "centered",
      "component",
      "divider",
      "for",
      "grabber",
      "guidelines",
      "horizontal",
      "host",
      "interactive",
      "label",
      "mdc",
      "optionally",
      "or",
      "overview",
      "separator",
      "text",
      "that",
      "the",
      "thin",
      "usage",
      "vertical"
    ]
  },
  {
    "name": "filterchip",
    "title": "Filterchip",
    "tagName": "mdc-filterchip",
    "summary": "Usage, guidelines, and accessibility for the mdc-filterchip component — a togglable chip used to apply or remove a single filter, with a built-in checkmark indicator when selected.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/filterchip",
      "knowledgeBasePath": "components/src/components/filterchip/knowledge-base/filterchip.component.md"
    },
    "docs": "## Overview\n\nThe filterchip is a compact, togglable chip used to represent a single filter in a list, table, or search context. Activating the chip flips its `selected` state and the component swaps its leading icon to a checkmark to indicate the active filter.\n\nFilterchips are typically rendered in a horizontal row next to a result set; users toggle them on and off to narrow the set.\n\n### When to use\n\n- Use `mdc-filterchip` when each chip toggles a single filter that can be applied or removed independently.\n- Use it inside a row of filters where the selected state should be visually obvious at a glance.\n\n### When not to use\n\n- Use `mdc-chip` when the chip triggers an action rather than a toggle.\n- Use `mdc-alertchip` when the chip communicates a status (information, warning, error) rather than a togglable filter.\n- Use `mdc-staticchip` when the chip is purely decorative and should not be interactive.\n- Use `mdc-checkbox` or `mdc-radio` when the control should appear inside a form field group rather than as a compact filter row.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/filterchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Filterchip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-filterchip label=\"In stock\"></mdc-filterchip>\n<mdc-filterchip label=\"On sale\" selected></mdc-filterchip>\n```\n\nListen for the `click` event (or `keydown`/`keyup` for keyboard activation) to react to filter toggles. The component flips `selected` automatically on activation; read the updated `selected` attribute to drive the surrounding filter state.\n\n### Content guidance\n\n- Keep the `label` short — we recommend up to 20 characters including spaces, matching the underlying chip.\n- Phrase the label as the filter that gets applied when the chip is selected (\"In stock\", not \"Toggle in stock\"); the checkmark indicates state.\n- Group related filterchips together and place the row close to the result set they affect.\n\n### Property/Attribute details\n\n- `selected` — when `true`, the chip is in the active state, displays the checkmark icon, and sets `aria-pressed=\"true\"`. Default `false`.\n- `label` — visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.\n- `color` — fixed to the filterchip-specific colour token on connect; setting a different `color` has no effect after connect.\n- `disabled` — when `true`, the chip is non-interactive and removed from the tab order.\n- `autoFocusOnMount` — when `true`, focuses the chip on first render.\n\n## Accessibility\n\n### Built-in features\n\nThe host renders with `role=\"button\"` and a single tab stop. Activating the chip flips `selected` and updates `aria-pressed` accordingly, exposing the toggle state to assistive technology.\n\n- `Enter` activates the chip on `keydown` (matches native button behaviour).\n- `Space` activates the chip on `keyup` (matches native button behaviour; `keydown` is suppressed so the page does not scroll).\n- Click activates the chip.\n\nWhen `disabled` is `true`, click and keyboard activation are suppressed and the host is removed from the tab order.\n\nThe `label` provides the accessible name. When `selected` is `true`, the leading icon is swapped to a checkmark; the icon itself is decorative and the selected state is communicated through `aria-pressed`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute      | Value                                                  |\n| ------- | -------------- | ------------------------------------------------------ |\n| Host    | `role`         | `button`                                               |\n| Host    | `aria-pressed` | `true` when `selected`, `false` otherwise              |\n| Host    | `tabindex`     | `0` when enabled; `-1` when `disabled`                 |\n\n### Implementation requirements\n\n#### General\n\n- Use `disabled` (not `aria-hidden` or visual cues alone) when a filter must not be interactive — the state is then exposed to assistive technology and removed from the tab order.\n- Drive the surrounding filter state from `selected` after the user activates the chip; the component does not own the underlying filter logic.\n\n#### Labelling\n\n- Provide a meaningful `label` — it is the accessible name and is mandatory for screen-reader users.\n- The toggle state is announced through `aria-pressed`; do not encode the selected state in the `label` itself (e.g. avoid \"In stock ✓\").",
    "sections": {
      "Overview": "The filterchip is a compact, togglable chip used to represent a single filter in a list, table, or search context. Activating the chip flips its `selected` state and the component swaps its leading icon to a checkmark to indicate the active filter.\n\nFilterchips are typically rendered in a horizontal row next to a result set; users toggle them on and off to narrow the set.\n\n### When to use\n\n- Use `mdc-filterchip` when each chip toggles a single filter that can be applied or removed independently.\n- Use it inside a row of filters where the selected state should be visually obvious at a glance.\n\n### When not to use\n\n- Use `mdc-chip` when the chip triggers an action rather than a toggle.\n- Use `mdc-alertchip` when the chip communicates a status (information, warning, error) rather than a togglable filter.\n- Use `mdc-staticchip` when the chip is purely decorative and should not be interactive.\n- Use `mdc-checkbox` or `mdc-radio` when the control should appear inside a form field group rather than as a compact filter row.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/filterchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Filterchip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-filterchip label=\"In stock\"></mdc-filterchip>\n<mdc-filterchip label=\"On sale\" selected></mdc-filterchip>\n```\n\nListen for the `click` event (or `keydown`/`keyup` for keyboard activation) to react to filter toggles. The component flips `selected` automatically on activation; read the updated `selected` attribute to drive the surrounding filter state.\n\n### Content guidance\n\n- Keep the `label` short — we recommend up to 20 characters including spaces, matching the underlying chip.\n- Phrase the label as the filter that gets applied when the chip is selected (\"In stock\", not \"Toggle in stock\"); the checkmark indicates state.\n- Group related filterchips together and place the row close to the result set they affect.\n\n### Property/Attribute details\n\n- `selected` — when `true`, the chip is in the active state, displays the checkmark icon, and sets `aria-pressed=\"true\"`. Default `false`.\n- `label` — visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.\n- `color` — fixed to the filterchip-specific colour token on connect; setting a different `color` has no effect after connect.\n- `disabled` — when `true`, the chip is non-interactive and removed from the tab order.\n- `autoFocusOnMount` — when `true`, focuses the chip on first render.",
      "Accessibility": "### Built-in features\n\nThe host renders with `role=\"button\"` and a single tab stop. Activating the chip flips `selected` and updates `aria-pressed` accordingly, exposing the toggle state to assistive technology.\n\n- `Enter` activates the chip on `keydown` (matches native button behaviour).\n- `Space` activates the chip on `keyup` (matches native button behaviour; `keydown` is suppressed so the page does not scroll).\n- Click activates the chip.\n\nWhen `disabled` is `true`, click and keyboard activation are suppressed and the host is removed from the tab order.\n\nThe `label` provides the accessible name. When `selected` is `true`, the leading icon is swapped to a checkmark; the icon itself is decorative and the selected state is communicated through `aria-pressed`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute      | Value                                                  |\n| ------- | -------------- | ------------------------------------------------------ |\n| Host    | `role`         | `button`                                               |\n| Host    | `aria-pressed` | `true` when `selected`, `false` otherwise              |\n| Host    | `tabindex`     | `0` when enabled; `-1` when `disabled`                 |\n\n### Implementation requirements\n\n#### General\n\n- Use `disabled` (not `aria-hidden` or visual cues alone) when a filter must not be interactive — the state is then exposed to assistive technology and removed from the tab order.\n- Drive the surrounding filter state from `selected` after the user activates the chip; the component does not own the underlying filter logic.\n\n#### Labelling\n\n- Provide a meaningful `label` — it is the accessible name and is mandatory for screen-reader users.\n- The toggle state is announced through `aria-pressed`; do not encode the selected state in the `label` itself (e.g. avoid \"In stock ✓\")."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the chip is non-interactive and removed from the tab order.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.",
          "source": "storybook"
        },
        {
          "name": "selected",
          "attribute": "selected",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the chip is in the active state, displays the checkmark icon, and sets `aria-pressed=\"true\"`. Default `false`.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the chip is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the chip."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the chip."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the chip receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label part of the chip."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-chip-color",
          "description": "The color of the chip."
        },
        {
          "name": "--mdc-chip-border-color",
          "description": "The border color of the chip."
        },
        {
          "name": "--mdc-chip-background-color",
          "description": "The background color of the chip."
        }
      ],
      "rules": [
        "when `true`, the chip is in the active state, displays the checkmark icon, and sets `aria-pressed=\"true\"`. Default `false`.",
        "visible label text rendered through `mdc-text`. Used as the accessible name. Recommended max length 20 characters.",
        "fixed to the filterchip-specific colour token on connect; setting a different `color` has no effect after connect.",
        "when `true`, the chip is non-interactive and removed from the tab order.",
        "when `true`, focuses the chip on first render."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/filterchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Filterchip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-filterchip label=\"In stock\"></mdc-filterchip>\n<mdc-filterchip label=\"On sale\" selected></mdc-filterchip>"
      }
    ],
    "reactImports": [
      "Filterchip"
    ],
    "constants": {
      "DEFAULTS": {
        "ICON_NAME": "'check-bold' as Extract<IconNames, 'check-bold'>",
        "COLOR": "'default' as Extract<ColorType, 'default'>"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "apply",
      "built",
      "checkmark",
      "chip",
      "component",
      "filter",
      "filterchip",
      "for",
      "guidelines",
      "icon",
      "in",
      "indicator",
      "mdc",
      "or",
      "overview",
      "remove",
      "selected",
      "single",
      "text",
      "the",
      "to",
      "togglable",
      "usage",
      "used",
      "when",
      "with"
    ]
  },
  {
    "name": "focustrap",
    "title": "FocusTrap",
    "tagName": "mdc-focustrap",
    "summary": "Traps keyboard focus within a container, preventing users from tabbing outside its bounds and ensuring focus is restored when the trap is deactivated.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/focustrap",
      "knowledgeBasePath": "components/src/components/focustrap/knowledge-base/focustrap.component.md"
    },
    "docs": "## Overview\n\nFocusTrap is a container component that manages keyboard focus within a specified region. It prevents focus from moving outside the container via Tab/Shift+Tab and optionally restores focus to the previously focused element when the trap is deactivated. Commonly used in modals, dialogs, popovers, and other overlay patterns to improve accessibility and user experience.\n\n### When to use\n\n- **Custom Modals and dialogs** – Trap focus so users interact only with the dialog content.\n- **Custom Popovers and menus** – Keep focus within an overlay component.\n- **Custom Dropdown menus** – Ensure focus stays within menu options while open.\n- **Multi-step forms** – Control focus flow between steps or sub-sections.\n- **Accessible disclosure widgets** – Any collapsible or hidden content that should capture focus when revealed.\n\n### When not to use\n\n- **Full-page navigation** – Do not trap focus on the main page; only use on transient overlays.\n- **Optional or advisory overlays** – If users can dismiss the overlay and continue with the page, consider whether focus trapping is necessary.\n- **Nested focus traps** – Avoid nesting multiple FocusTrap instances; the innermost trap will dominate focus behavior. MDC Dialog, popover and menu components already implement FocusTrap internally, so wrapping them in another FocusTrap is unnecessary and may cause unexpected behavior.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the `<mdc-focustrap>` web component to wrap any focusable content:\n\n```html\n<mdc-focustrap trap-disabled=\"false\" auto-focus=\"false\">\n  <input type=\"text\" placeholder=\"First input\" />\n  <input type=\"text\" placeholder=\"Second input\" />\n  <button>Submit</button>\n</mdc-focustrap>\n```\n\n**React / TypeScript integration:**\n\n```tsx\nimport { FocusTrap } from '@momentum-design/components/react';\n\nexport function MyModal() {\n  const [isOpen, setIsOpen] = useState(true);\n\n  return (\n    isOpen && (\n      <FocusTrap\n        trapDisabled={false}\n        restoreFocusDisabled={false}\n        autoFocus={true}\n      >\n        <h2>Modal Title</h2>\n        <input type=\"text\" placeholder=\"Name\" />\n        <button onClick={() => setIsOpen(false)}>Close</button>\n      </FocusTrap>\n    )\n  );\n}\n```\n\n**Package import:** `@momentum-design/components`  \n**Custom element tag:** `mdc-focustrap`\n\n### Property/Attribute details\n\n- **`trap-disabled` / `trapDisabled`** (boolean, default: `false`)  \n  When `true`, focus trapping is disabled. When `false`, focus is trapped within the component. This is the inverse of the `contain` prop in `@react-aria/focus`.\n\n- **`restore-focus-disabled` / `restoreFocusDisabled`** (boolean, default: `false`)  \n  When `true`, focus is not restored to the previously focused element when the trap is deactivated or the component is removed from the DOM. When `false`, focus returns to the element that had focus before the trap was activated.\n\n- **`auto-focus` / `autoFocus`** (boolean, default: `false`)  \n  When `true`, the first focusable element inside the container automatically receives focus when focus trapping is enabled. When `false`, no automatic focus is set.\n\n- **`should-focus-trap-wrap` / `shouldFocusTrapWrap`** (boolean, default: `true`)  \n  When `true` (default), Tab from the last focusable element wraps to the first, and Shift+Tab from the first wraps to the last. When `false`, Tab/Shift+Tab does not wrap; focus exits the trap.\n\n### Limitations\n\n- **Single trap per overlay** – Multiple nested FocusTrap instances may produce unexpected behavior. Only the innermost trap will fully control focus.\n- **Required focusable content** – The FocusTrap requires at least one focusable element (button, input, link, etc.) inside it. An empty or content-only container has no effect.\n- **Focus management outside content** – The trap manages focus within its slot content only. External DOM elements or adjacent overlays are not affected.\n- **Slotted content only** – The FocusTrap does not trap focus on direct child elements added programmatically after render; slot-based content is required.\n\n### Notes\n\nFocusTrap is built on the `FocusTrapMixin` utility and is designed to complement accessible overlay patterns. For complex focus scenarios (such as multiple overlays or programmatic focus control), consider pairing with higher-level patterns or frameworks. Focus restoration respects the `restoreFocusDisabled` flag and is automatically triggered on component disconnect.\n\n## Accessibility\n\n### Built-in features\n\n- **Focus containment** – Prevents keyboard navigation from leaving the trap boundaries, ensuring users remain focused on the intended content.\n- **Focus wrapping** – By default, Tab from the last focusable element returns to the first, and vice versa, creating a continuous focus cycle.\n- **Focus restoration** – Restores focus to the previously focused element when the trap is deactivated (unless `restoreFocusDisabled` is `true`), maintaining context for screen-reader users and sighted users alike.\n\n#### Internal ARIA managed by the component\n\nThe FocusTrap component does not apply ARIA roles or properties itself. It relies on semantic HTML and standard focus behavior. The consumer must ensure focusable elements inside the trap have appropriate roles and labels.\n\n### Implementation requirements\n\n#### Labelling\n\nFocusTrap itself does not require a label. However, the container and its content must follow standard labelling practices:\n\n- **Modal dialogs** – Use `role=\"dialog\"` on a wrapper element and include `aria-labelledby` pointing to the dialog's title.\n- **Popover or menu** – Use `role=\"menu\"` or similar, with `aria-label` or `aria-labelledby` as appropriate.\n- **Focusable children** – Ensure all interactive children (buttons, inputs, links) have visible or accessible labels via `<label>`, `aria-label`, or `aria-labelledby`.\n\n**Example with proper semantics:**\n\n```html\n<div role=\"dialog\" aria-labelledby=\"dialog-title\" aria-modal=\"true\">\n  <h2 id=\"dialog-title\">Confirm Action</h2>\n  <mdc-focustrap trap-disabled=\"false\" auto-focus=\"true\">\n    <p>Are you sure?</p>\n    <button>Cancel</button>\n    <button class=\"primary\">Confirm</button>\n  </mdc-focustrap>\n</div>\n```\n\n#### General\n\n- Ensure the context in which FocusTrap is used (e.g., a modal overlay) is semantically marked with `role=\"dialog\"`, `role=\"menu\"`, or another appropriate role.\n- Provide a way for users to dismiss or deactivate the focus trap (e.g., a Close button or Escape key handler), and restore focus to the triggering element on dismissal.\n- Test focus behavior with keyboard navigation and screen readers to verify that focus enters, cycles, and exits the trap as expected.\n\n### Notes\n\nFocus trapping is a critical accessibility pattern for overlays and modals. Always test with actual assistive technology (NVDA, JAWS, VoiceOver) to ensure focus announcements and navigation feel natural. Pair FocusTrap with semantic markup and appropriate ARIA roles to provide the full accessibility contract.",
    "sections": {
      "Overview": "FocusTrap is a container component that manages keyboard focus within a specified region. It prevents focus from moving outside the container via Tab/Shift+Tab and optionally restores focus to the previously focused element when the trap is deactivated. Commonly used in modals, dialogs, popovers, and other overlay patterns to improve accessibility and user experience.\n\n### When to use\n\n- **Custom Modals and dialogs** – Trap focus so users interact only with the dialog content.\n- **Custom Popovers and menus** – Keep focus within an overlay component.\n- **Custom Dropdown menus** – Ensure focus stays within menu options while open.\n- **Multi-step forms** – Control focus flow between steps or sub-sections.\n- **Accessible disclosure widgets** – Any collapsible or hidden content that should capture focus when revealed.\n\n### When not to use\n\n- **Full-page navigation** – Do not trap focus on the main page; only use on transient overlays.\n- **Optional or advisory overlays** – If users can dismiss the overlay and continue with the page, consider whether focus trapping is necessary.\n- **Nested focus traps** – Avoid nesting multiple FocusTrap instances; the innermost trap will dominate focus behavior. MDC Dialog, popover and menu components already implement FocusTrap internally, so wrapping them in another FocusTrap is unnecessary and may cause unexpected behavior.",
      "Guidelines": "### Developer usage\n\nImport and use the `<mdc-focustrap>` web component to wrap any focusable content:\n\n```html\n<mdc-focustrap trap-disabled=\"false\" auto-focus=\"false\">\n  <input type=\"text\" placeholder=\"First input\" />\n  <input type=\"text\" placeholder=\"Second input\" />\n  <button>Submit</button>\n</mdc-focustrap>\n```\n\n**React / TypeScript integration:**\n\n```tsx\nimport { FocusTrap } from '@momentum-design/components/react';\n\nexport function MyModal() {\n  const [isOpen, setIsOpen] = useState(true);\n\n  return (\n    isOpen && (\n      <FocusTrap\n        trapDisabled={false}\n        restoreFocusDisabled={false}\n        autoFocus={true}\n      >\n        <h2>Modal Title</h2>\n        <input type=\"text\" placeholder=\"Name\" />\n        <button onClick={() => setIsOpen(false)}>Close</button>\n      </FocusTrap>\n    )\n  );\n}\n```\n\n**Package import:** `@momentum-design/components`  \n**Custom element tag:** `mdc-focustrap`\n\n### Property/Attribute details\n\n- **`trap-disabled` / `trapDisabled`** (boolean, default: `false`)  \n  When `true`, focus trapping is disabled. When `false`, focus is trapped within the component. This is the inverse of the `contain` prop in `@react-aria/focus`.\n\n- **`restore-focus-disabled` / `restoreFocusDisabled`** (boolean, default: `false`)  \n  When `true`, focus is not restored to the previously focused element when the trap is deactivated or the component is removed from the DOM. When `false`, focus returns to the element that had focus before the trap was activated.\n\n- **`auto-focus` / `autoFocus`** (boolean, default: `false`)  \n  When `true`, the first focusable element inside the container automatically receives focus when focus trapping is enabled. When `false`, no automatic focus is set.\n\n- **`should-focus-trap-wrap` / `shouldFocusTrapWrap`** (boolean, default: `true`)  \n  When `true` (default), Tab from the last focusable element wraps to the first, and Shift+Tab from the first wraps to the last. When `false`, Tab/Shift+Tab does not wrap; focus exits the trap.\n\n### Limitations\n\n- **Single trap per overlay** – Multiple nested FocusTrap instances may produce unexpected behavior. Only the innermost trap will fully control focus.\n- **Required focusable content** – The FocusTrap requires at least one focusable element (button, input, link, etc.) inside it. An empty or content-only container has no effect.\n- **Focus management outside content** – The trap manages focus within its slot content only. External DOM elements or adjacent overlays are not affected.\n- **Slotted content only** – The FocusTrap does not trap focus on direct child elements added programmatically after render; slot-based content is required.\n\n### Notes\n\nFocusTrap is built on the `FocusTrapMixin` utility and is designed to complement accessible overlay patterns. For complex focus scenarios (such as multiple overlays or programmatic focus control), consider pairing with higher-level patterns or frameworks. Focus restoration respects the `restoreFocusDisabled` flag and is automatically triggered on component disconnect.",
      "Accessibility": "### Built-in features\n\n- **Focus containment** – Prevents keyboard navigation from leaving the trap boundaries, ensuring users remain focused on the intended content.\n- **Focus wrapping** – By default, Tab from the last focusable element returns to the first, and vice versa, creating a continuous focus cycle.\n- **Focus restoration** – Restores focus to the previously focused element when the trap is deactivated (unless `restoreFocusDisabled` is `true`), maintaining context for screen-reader users and sighted users alike.\n\n#### Internal ARIA managed by the component\n\nThe FocusTrap component does not apply ARIA roles or properties itself. It relies on semantic HTML and standard focus behavior. The consumer must ensure focusable elements inside the trap have appropriate roles and labels.\n\n### Implementation requirements\n\n#### Labelling\n\nFocusTrap itself does not require a label. However, the container and its content must follow standard labelling practices:\n\n- **Modal dialogs** – Use `role=\"dialog\"` on a wrapper element and include `aria-labelledby` pointing to the dialog's title.\n- **Popover or menu** – Use `role=\"menu\"` or similar, with `aria-label` or `aria-labelledby` as appropriate.\n- **Focusable children** – Ensure all interactive children (buttons, inputs, links) have visible or accessible labels via `<label>`, `aria-label`, or `aria-labelledby`.\n\n**Example with proper semantics:**\n\n```html\n<div role=\"dialog\" aria-labelledby=\"dialog-title\" aria-modal=\"true\">\n  <h2 id=\"dialog-title\">Confirm Action</h2>\n  <mdc-focustrap trap-disabled=\"false\" auto-focus=\"true\">\n    <p>Are you sure?</p>\n    <button>Cancel</button>\n    <button class=\"primary\">Confirm</button>\n  </mdc-focustrap>\n</div>\n```\n\n#### General\n\n- Ensure the context in which FocusTrap is used (e.g., a modal overlay) is semantically marked with `role=\"dialog\"`, `role=\"menu\"`, or another appropriate role.\n- Provide a way for users to dismiss or deactivate the focus trap (e.g., a Close button or Escape key handler), and restore focus to the triggering element on dismissal.\n- Test focus behavior with keyboard navigation and screen readers to verify that focus enters, cycles, and exits the trap as expected.\n\n### Notes\n\nFocus trapping is a critical accessibility pattern for overlays and modals. Always test with actual assistive technology (NVDA, JAWS, VoiceOver) to ensure focus announcements and navigation feel natural. Pair FocusTrap with semantic markup and appropriate ARIA roles to provide the full accessibility contract."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "autoFocus",
          "attribute": "auto-focus",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "When true, the first focusable element receives focus automatically when the trap is activated.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "restoreFocusDisabled",
          "attribute": "restore-focus-disabled",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "When true, focus is NOT restored to the previously focused element when the trap is deactivated. When false (default), focus is restored.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "shouldFocusTrapWrap",
          "attribute": "should-focus-trap-wrap",
          "type": "boolean",
          "values": [],
          "description": "When true (default), Tab wraps from the last focusable element back to the first and vice versa.",
          "source": "storybook"
        },
        {
          "name": "trapDisabled",
          "attribute": "trap-disabled",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "When true, focus trapping is disabled. When false (default), keyboard focus is trapped.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot – place any focusable content here."
        }
      ],
      "events": [
        {
          "name": "focus-trap-activated",
          "description": "(React: onFocusTrapActivated) Fired when the focus trap is activated."
        },
        {
          "name": "focus-trap-deactivated",
          "description": "(React: onFocusTrapDeactivated) Fired when the focus trap is deactivated."
        }
      ],
      "cssParts": [],
      "cssProperties": [],
      "rules": []
    },
    "examples": [
      {
        "language": "html",
        "code": "<mdc-focustrap trap-disabled=\"false\" auto-focus=\"false\">\n  <input type=\"text\" placeholder=\"First input\" />\n  <input type=\"text\" placeholder=\"Second input\" />\n  <button>Submit</button>\n</mdc-focustrap>"
      },
      {
        "language": "tsx",
        "code": "import { FocusTrap } from '@momentum-design/components/react';\n\nexport function MyModal() {\n  const [isOpen, setIsOpen] = useState(true);\n\n  return (\n    isOpen && (\n      <FocusTrap\n        trapDisabled={false}\n        restoreFocusDisabled={false}\n        autoFocus={true}\n      >\n        <h2>Modal Title</h2>\n        <input type=\"text\" placeholder=\"Name\" />\n        <button onClick={() => setIsOpen(false)}>Close</button>\n      </FocusTrap>\n    )\n  );\n}"
      },
      {
        "language": "html",
        "code": "<div role=\"dialog\" aria-labelledby=\"dialog-title\" aria-modal=\"true\">\n  <h2 id=\"dialog-title\">Confirm Action</h2>\n  <mdc-focustrap trap-disabled=\"false\" auto-focus=\"true\">\n    <p>Are you sure?</p>\n    <button>Cancel</button>\n    <button class=\"primary\">Confirm</button>\n  </mdc-focustrap>\n</div>"
      }
    ],
    "reactImports": [
      "FocusTrap"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "bounds",
      "container",
      "deactivated",
      "ensuring",
      "focus",
      "focustrap",
      "from",
      "guidelines",
      "is",
      "its",
      "keyboard",
      "mdc",
      "outside",
      "overview",
      "preventing",
      "restored",
      "tabbing",
      "the",
      "trap",
      "traps",
      "users",
      "when",
      "within"
    ]
  },
  {
    "name": "formfieldgroup",
    "title": "Formfieldgroup",
    "tagName": "mdc-formfieldgroup",
    "summary": "Usage, guidelines, and accessibility for the mdc-formfieldgroup component — a labelled container grouping checkbox/toggle form fields with shared header, helper text, and group semantics.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/formfieldgroup",
      "knowledgeBasePath": "components/src/components/formfieldgroup/knowledge-base/formfieldgroup.component.md"
    },
    "docs": "## Overview\n\nThe formfieldgroup groups several related form fields under a single shared label, helper text, and group role. It is intended for checkbox groups and toggle groups; each child is rendered with a 12px (0.75rem) gap between siblings.\n\nThe group is announced to screen readers as a single semantic unit via `role=\"group\"`, with the header text and helper text wired as the accessible name and description.\n\n### When to use\n\n- Use `mdc-formfieldgroup` to wrap a set of related `mdc-checkbox` controls so they share a single label and helper text.\n- Use it to wrap a set of related `mdc-toggle` controls in the same way.\n\n### When not to use\n\n- Use `mdc-radiogroup` for radio buttons — radios require the `radiogroup` role and arrow-key navigation, which `mdc-formfieldgroup` does not provide.\n- Use `mdc-formfieldwrapper` for a single labelled form field rather than a group.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/formfieldgroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { Formfieldgroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (checkbox group):\n\n```html\n<mdc-formfieldgroup\n  label=\"Notification preferences\"\n  help-text=\"Choose how you want to be notified.\"\n>\n  <mdc-checkbox name=\"prefs\" value=\"email\" label=\"Email\"></mdc-checkbox>\n  <mdc-checkbox name=\"prefs\" value=\"sms\" label=\"SMS\"></mdc-checkbox>\n  <mdc-checkbox name=\"prefs\" value=\"push\" label=\"Push\"></mdc-checkbox>\n</mdc-formfieldgroup>\n```\n\nEach child checkbox/toggle continues to manage its own form value through its own `name`/`value` attributes — the group is purely a semantic and visual container.\n\n### Content guidance\n\n- Provide a meaningful `label` (or `data-aria-label`) — without it the group has no accessible name and assistive technology will not announce the grouping.\n- Use `help-text` for short instructions or constraints that apply to the whole group (\"Select at least one channel\"); per-field validation belongs on the child fields.\n- Keep the number of children small enough to scan in one visual block; for long lists, prefer a different control such as a `mdc-select` with multi-select.\n\n### Property/Attribute details\n\n- `label` — visible header text rendered above the group. Used as the accessible name when no `aria-label` is provided.\n- `data-aria-label` — accessible name fallback when no visible label is rendered.\n- `help-text` — helper or validation text rendered above the children. Wired as `aria-description` on the group.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.\n- `required` — when `true`, appends a required indicator (`*`) to the label. The group does not enforce required state on its own; the consumer must validate the children.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n\n### Limitations\n\n- The group does not manage form submission, validation, or selection state for its children — each child is responsible for its own value and validity. The group only provides the visible header, helper text, and ARIA grouping.\n- The group does not enforce the type of its children; placing radios inside will not produce a `radiogroup`. Use `mdc-radiogroup` for radio buttons.\n\n## Accessibility\n\n### Built-in features\n\nThe host renders with `role=\"group\"`. The header text (`label`) is wired as the accessible name via `aria-label`, and the helper text (`help-text`) is wired as the accessible description via `aria-description`. When the consumer provides `aria-label` directly, the component does not overwrite it.\n\nChildren are rendered in the default slot in DOM order and announced to screen readers in that order.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute          | Value                                                                       |\n| ------- | ------------------ | --------------------------------------------------------------------------- |\n| Host    | `role`             | `group`                                                                     |\n| Host    | `aria-label`       | mirrors `label` when no explicit `aria-label` is set                        |\n| Host    | `aria-description` | mirrors `help-text` when no explicit `aria-description` is set              |\n\n### Implementation requirements\n\n#### General\n\n- Place only checkbox or toggle children in the default slot; radio buttons need `mdc-radiogroup` instead.\n- Validate the children's combined state from the consumer when `required` is `true` — the group does not enforce that at least one child is selected.\n\n#### Labelling\n\n- Always provide a `label` (or `data-aria-label`) — without it the group has no accessible name.\n- Use `help-text` for constraints that apply to the whole group; per-field error messages belong on each child's own `help-text`.",
    "sections": {
      "Overview": "The formfieldgroup groups several related form fields under a single shared label, helper text, and group role. It is intended for checkbox groups and toggle groups; each child is rendered with a 12px (0.75rem) gap between siblings.\n\nThe group is announced to screen readers as a single semantic unit via `role=\"group\"`, with the header text and helper text wired as the accessible name and description.\n\n### When to use\n\n- Use `mdc-formfieldgroup` to wrap a set of related `mdc-checkbox` controls so they share a single label and helper text.\n- Use it to wrap a set of related `mdc-toggle` controls in the same way.\n\n### When not to use\n\n- Use `mdc-radiogroup` for radio buttons — radios require the `radiogroup` role and arrow-key navigation, which `mdc-formfieldgroup` does not provide.\n- Use `mdc-formfieldwrapper` for a single labelled form field rather than a group.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/formfieldgroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { Formfieldgroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (checkbox group):\n\n```html\n<mdc-formfieldgroup\n  label=\"Notification preferences\"\n  help-text=\"Choose how you want to be notified.\"\n>\n  <mdc-checkbox name=\"prefs\" value=\"email\" label=\"Email\"></mdc-checkbox>\n  <mdc-checkbox name=\"prefs\" value=\"sms\" label=\"SMS\"></mdc-checkbox>\n  <mdc-checkbox name=\"prefs\" value=\"push\" label=\"Push\"></mdc-checkbox>\n</mdc-formfieldgroup>\n```\n\nEach child checkbox/toggle continues to manage its own form value through its own `name`/`value` attributes — the group is purely a semantic and visual container.\n\n### Content guidance\n\n- Provide a meaningful `label` (or `data-aria-label`) — without it the group has no accessible name and assistive technology will not announce the grouping.\n- Use `help-text` for short instructions or constraints that apply to the whole group (\"Select at least one channel\"); per-field validation belongs on the child fields.\n- Keep the number of children small enough to scan in one visual block; for long lists, prefer a different control such as a `mdc-select` with multi-select.\n\n### Property/Attribute details\n\n- `label` — visible header text rendered above the group. Used as the accessible name when no `aria-label` is provided.\n- `data-aria-label` — accessible name fallback when no visible label is rendered.\n- `help-text` — helper or validation text rendered above the children. Wired as `aria-description` on the group.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.\n- `required` — when `true`, appends a required indicator (`*`) to the label. The group does not enforce required state on its own; the consumer must validate the children.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n\n### Limitations\n\n- The group does not manage form submission, validation, or selection state for its children — each child is responsible for its own value and validity. The group only provides the visible header, helper text, and ARIA grouping.\n- The group does not enforce the type of its children; placing radios inside will not produce a `radiogroup`. Use `mdc-radiogroup` for radio buttons.",
      "Accessibility": "### Built-in features\n\nThe host renders with `role=\"group\"`. The header text (`label`) is wired as the accessible name via `aria-label`, and the helper text (`help-text`) is wired as the accessible description via `aria-description`. When the consumer provides `aria-label` directly, the component does not overwrite it.\n\nChildren are rendered in the default slot in DOM order and announced to screen readers in that order.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute          | Value                                                                       |\n| ------- | ------------------ | --------------------------------------------------------------------------- |\n| Host    | `role`             | `group`                                                                     |\n| Host    | `aria-label`       | mirrors `label` when no explicit `aria-label` is set                        |\n| Host    | `aria-description` | mirrors `help-text` when no explicit `aria-description` is set              |\n\n### Implementation requirements\n\n#### General\n\n- Place only checkbox or toggle children in the default slot; radio buttons need `mdc-radiogroup` instead.\n- Validate the children's combined state from the consumer when `required` is `true` — the group does not enforce that at least one child is selected.\n\n#### Labelling\n\n- Always provide a `label` (or `data-aria-label`) — without it the group has no accessible name.\n- Use `help-text` for constraints that apply to the whole group; per-field error messages belong on each child's own `help-text`."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "object",
          "values": [],
          "description": "The html content which can be placed inside the formfieldgroup component.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name fallback when no visible label is rendered.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper or validation text rendered above the children. Wired as `aria-description` on the group.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.",
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible header text rendered above the group. Used as the accessible name when no `aria-label` is provided.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, appends a required indicator (`*`) to the label. The group does not enforce required state on its own; the consumer must validate the children.",
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default slot for checkbox or toggle components."
        },
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "group-header",
          "description": "This contains the label and help text for the group"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        }
      ],
      "rules": [
        "visible header text rendered above the group. Used as the accessible name when no `aria-label` is provided.",
        "accessible name fallback when no visible label is rendered.",
        "helper or validation text rendered above the children. Wired as `aria-description` on the group.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.",
        "when `true`, appends a required indicator (`*`) to the label. The group does not enforce required state on its own; the consumer must validate the children."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/formfieldgroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { Formfieldgroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-formfieldgroup\n  label=\"Notification preferences\"\n  help-text=\"Choose how you want to be notified.\"\n>\n  <mdc-checkbox name=\"prefs\" value=\"email\" label=\"Email\"></mdc-checkbox>\n  <mdc-checkbox name=\"prefs\" value=\"sms\" label=\"SMS\"></mdc-checkbox>\n  <mdc-checkbox name=\"prefs\" value=\"push\" label=\"Push\"></mdc-checkbox>\n</mdc-formfieldgroup>"
      }
    ],
    "reactImports": [
      "Formfieldgroup"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "button",
      "checkbox",
      "component",
      "container",
      "fields",
      "for",
      "form",
      "formfieldgroup",
      "group",
      "grouping",
      "guidelines",
      "header",
      "helper",
      "icon",
      "labelled",
      "mdc",
      "overview",
      "semantics",
      "shared",
      "text",
      "the",
      "toggle",
      "toggletip",
      "usage",
      "with"
    ]
  },
  {
    "name": "formfieldwrapper",
    "title": "Formfieldwrapper",
    "tagName": "mdc-formfieldwrapper",
    "summary": "Internal base component that renders the label, helper/validation text, optional toggletip info icon, and required indicator shared by every form field component.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/formfieldwrapper",
      "knowledgeBasePath": "components/src/components/formfieldwrapper/knowledge-base/formfieldwrapper.component.md"
    },
    "docs": "## Overview\n\nThe formfieldwrapper is an internal base component that renders the label row (label, required indicator, optional info-icon toggletip) and the helper/validation row (icon + text) used by every form field. It is **not** intended to be instantiated directly by consumers — components such as `mdc-input`, `mdc-textarea`, `mdc-checkbox`, `mdc-radio`, `mdc-select`, `mdc-combobox`, `mdc-datepicker`, `mdc-formfieldgroup`, and others extend it to inherit a consistent label and helper-text contract.\n\nThis entry documents the properties, slots, and accessibility hooks that every extending component exposes to consumers. Consumers will interact with these via the concrete form-field component (e.g. set `label` on an `mdc-input`), not on `mdc-formfieldwrapper` itself.\n\n### When to use\n\n- Use the formfieldwrapper indirectly by extending it from a new form-field component that needs the standard label, required indicator, helper text, and toggletip layout.\n\n### When not to use\n\n- Do not render `mdc-formfieldwrapper` directly in application code — pick the concrete form-field component (`mdc-input`, `mdc-textarea`, `mdc-checkbox`, etc.) instead.\n\n## Guidelines\n\n### Developer usage\n\nThis component is internal. Extend it from a new form-field component when building one:\n\n```ts\nimport FormfieldWrapper from '../formfieldwrapper/formfieldwrapper.component';\n\nclass MyField extends FormfieldWrapper {\n  // ...field-specific implementation\n  static override styles = [...FormfieldWrapper.styles, ...myStyles];\n}\n```\n\nThe wrapper provides `renderLabel()` and `renderHelperText()` helpers; subclasses call them from their `render()` method to compose the final markup. Subclasses also inherit the `inputId` protected field, used to wire the label's `for` attribute to the native input via `id`.\n\n### Content guidance\n\n- Provide a meaningful `label` on every field that extends the wrapper; the label doubles as the form control's accessible name through the rendered `<label for>`.\n- Use `help-text` for short helper or validation messages; pair it with `help-text-type` for visual treatment.\n- Use `toggletip-text` (or the `toggletip` slot) to attach an info-icon button next to the label that opens an `mdc-toggletip` with additional context.\n\n### Property/Attribute details\n\n- `label` — visible label text. Rendered either as a native `<label for>` (linked to the field's `inputId`) or as a `mdc-text` heading depending on the extending subclass's `shouldRenderLabel` flag.\n- `required` — when `true`, appends a `*` required indicator next to the label. Default `false`.\n- `help-text` — helper or validation text rendered below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon rendered next to the helper text (no icon for `default`).\n- `toggletip-text` — when set, renders an info-icon button next to the label that opens an `mdc-toggletip` with this text.\n- `toggletip-placement` — placement of the toggletip popover relative to the info-icon button. Default `top`.\n- `toggletip-strategy` — popover positioning strategy for the toggletip. `absolute` (default) or `fixed`.\n- `info-icon-aria-label` — accessible label for the info-icon button. Required when `toggletip-text` is set (the button has no visible text).\n- `readonly` — when `true`, the field is non-interactive but focusable. Subclasses honour this in their own rendering.\n- `disabled` — when `true`, the field is non-interactive and removed from the tab order. The info-icon button and helper-text icon are also disabled.\n- `soft-disabled` — when `true`, the field is visually disabled but remains focusable so assistive technology can read it. Mainly used for fields where disabled state must still be discoverable.\n\nSlots exposed to the consumer:\n\n- `label` — replace the default label rendering with custom markup.\n- `toggletip` — replace the default info-icon button + toggletip with custom markup.\n- `help-icon` — replace the default helper-text icon (driven by `help-text-type`).\n- `help-text` — replace the default helper-text rendering.\n\n## Accessibility\n\n### Built-in features\n\nThe wrapper renders the label as a native `<label for>` element linked to the form field's hidden native input via `inputId`. This produces a click-on-label-focuses-field behaviour for free, with no additional ARIA wiring required from the subclass.\n\nThe helper text is rendered through `mdc-text` with a stable id (`DEFAULTS.HELPER_TEXT_ID`); extending components are expected to link this id from their input element via `aria-describedby` so screen readers announce the helper text on focus.\n\nThe required indicator (`*`) is purely visual — accessible-name announcement of the required state is the responsibility of the extending component (typically via `aria-required` on the input).\n\nThe info-icon button + toggletip uses a fresh `triggerid` per render to anchor the `mdc-toggletip` to the trigger button; the toggletip opens on click or keyboard activation.\n\nThe wrapper itself does not own any role or ARIA state — the role and ARIA contract are the responsibility of the extending component (e.g. the field's input element).\n\n#### Internal ARIA managed by the component\n\n| Element              | Attribute       | Value                                                              |\n| -------------------- | --------------- | ------------------------------------------------------------------ |\n| Native label         | `for`           | the `inputId` of the field's input element                         |\n| Helper-text `mdc-text` | `id`          | stable id used by the extending component for `aria-describedby`   |\n| Info-icon button     | `aria-label`    | mirrors `info-icon-aria-label`                                     |\n| Info-icon toggletip  | `triggerid`     | id of the info-icon button (auto-generated)                        |\n\n### Implementation requirements\n\n#### General\n\n- Extending components must call `renderLabel()` and `renderHelperText()` from their own `render()` method to surface the standard label and helper-text layout.\n- Extending components must wire `aria-describedby` on their input element to the helper-text id so the helper text is announced on focus.\n- Extending components must set `aria-required` on their input when `required` is `true`; the wrapper only renders the visual indicator.\n- Set `shouldRenderLabel` to `false` on extending components that render the label as a heading rather than a native `<label for>` (e.g. groups like `mdc-formfieldgroup` and `mdc-radiogroup`).\n\n#### Labelling\n\n- Always provide a `label` on the extending component — without it the field has no accessible name from the wrapper, and the extending component must supply one through `data-aria-label` or `aria-labelledby`.\n- When `toggletip-text` is set, always provide `info-icon-aria-label` — the info-icon button has no visible text and otherwise has no accessible name.",
    "sections": {
      "Overview": "The formfieldwrapper is an internal base component that renders the label row (label, required indicator, optional info-icon toggletip) and the helper/validation row (icon + text) used by every form field. It is **not** intended to be instantiated directly by consumers — components such as `mdc-input`, `mdc-textarea`, `mdc-checkbox`, `mdc-radio`, `mdc-select`, `mdc-combobox`, `mdc-datepicker`, `mdc-formfieldgroup`, and others extend it to inherit a consistent label and helper-text contract.\n\nThis entry documents the properties, slots, and accessibility hooks that every extending component exposes to consumers. Consumers will interact with these via the concrete form-field component (e.g. set `label` on an `mdc-input`), not on `mdc-formfieldwrapper` itself.\n\n### When to use\n\n- Use the formfieldwrapper indirectly by extending it from a new form-field component that needs the standard label, required indicator, helper text, and toggletip layout.\n\n### When not to use\n\n- Do not render `mdc-formfieldwrapper` directly in application code — pick the concrete form-field component (`mdc-input`, `mdc-textarea`, `mdc-checkbox`, etc.) instead.",
      "Guidelines": "### Developer usage\n\nThis component is internal. Extend it from a new form-field component when building one:\n\n```ts\nimport FormfieldWrapper from '../formfieldwrapper/formfieldwrapper.component';\n\nclass MyField extends FormfieldWrapper {\n  // ...field-specific implementation\n  static override styles = [...FormfieldWrapper.styles, ...myStyles];\n}\n```\n\nThe wrapper provides `renderLabel()` and `renderHelperText()` helpers; subclasses call them from their `render()` method to compose the final markup. Subclasses also inherit the `inputId` protected field, used to wire the label's `for` attribute to the native input via `id`.\n\n### Content guidance\n\n- Provide a meaningful `label` on every field that extends the wrapper; the label doubles as the form control's accessible name through the rendered `<label for>`.\n- Use `help-text` for short helper or validation messages; pair it with `help-text-type` for visual treatment.\n- Use `toggletip-text` (or the `toggletip` slot) to attach an info-icon button next to the label that opens an `mdc-toggletip` with additional context.\n\n### Property/Attribute details\n\n- `label` — visible label text. Rendered either as a native `<label for>` (linked to the field's `inputId`) or as a `mdc-text` heading depending on the extending subclass's `shouldRenderLabel` flag.\n- `required` — when `true`, appends a `*` required indicator next to the label. Default `false`.\n- `help-text` — helper or validation text rendered below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon rendered next to the helper text (no icon for `default`).\n- `toggletip-text` — when set, renders an info-icon button next to the label that opens an `mdc-toggletip` with this text.\n- `toggletip-placement` — placement of the toggletip popover relative to the info-icon button. Default `top`.\n- `toggletip-strategy` — popover positioning strategy for the toggletip. `absolute` (default) or `fixed`.\n- `info-icon-aria-label` — accessible label for the info-icon button. Required when `toggletip-text` is set (the button has no visible text).\n- `readonly` — when `true`, the field is non-interactive but focusable. Subclasses honour this in their own rendering.\n- `disabled` — when `true`, the field is non-interactive and removed from the tab order. The info-icon button and helper-text icon are also disabled.\n- `soft-disabled` — when `true`, the field is visually disabled but remains focusable so assistive technology can read it. Mainly used for fields where disabled state must still be discoverable.\n\nSlots exposed to the consumer:\n\n- `label` — replace the default label rendering with custom markup.\n- `toggletip` — replace the default info-icon button + toggletip with custom markup.\n- `help-icon` — replace the default helper-text icon (driven by `help-text-type`).\n- `help-text` — replace the default helper-text rendering.",
      "Accessibility": "### Built-in features\n\nThe wrapper renders the label as a native `<label for>` element linked to the form field's hidden native input via `inputId`. This produces a click-on-label-focuses-field behaviour for free, with no additional ARIA wiring required from the subclass.\n\nThe helper text is rendered through `mdc-text` with a stable id (`DEFAULTS.HELPER_TEXT_ID`); extending components are expected to link this id from their input element via `aria-describedby` so screen readers announce the helper text on focus.\n\nThe required indicator (`*`) is purely visual — accessible-name announcement of the required state is the responsibility of the extending component (typically via `aria-required` on the input).\n\nThe info-icon button + toggletip uses a fresh `triggerid` per render to anchor the `mdc-toggletip` to the trigger button; the toggletip opens on click or keyboard activation.\n\nThe wrapper itself does not own any role or ARIA state — the role and ARIA contract are the responsibility of the extending component (e.g. the field's input element).\n\n#### Internal ARIA managed by the component\n\n| Element              | Attribute       | Value                                                              |\n| -------------------- | --------------- | ------------------------------------------------------------------ |\n| Native label         | `for`           | the `inputId` of the field's input element                         |\n| Helper-text `mdc-text` | `id`          | stable id used by the extending component for `aria-describedby`   |\n| Info-icon button     | `aria-label`    | mirrors `info-icon-aria-label`                                     |\n| Info-icon toggletip  | `triggerid`     | id of the info-icon button (auto-generated)                        |\n\n### Implementation requirements\n\n#### General\n\n- Extending components must call `renderLabel()` and `renderHelperText()` from their own `render()` method to surface the standard label and helper-text layout.\n- Extending components must wire `aria-describedby` on their input element to the helper-text id so the helper text is announced on focus.\n- Extending components must set `aria-required` on their input when `required` is `true`; the wrapper only renders the visual indicator.\n- Set `shouldRenderLabel` to `false` on extending components that render the label as a heading rather than a native `<label for>` (e.g. groups like `mdc-formfieldgroup` and `mdc-radiogroup`).\n\n#### Labelling\n\n- Always provide a `label` on the extending component — without it the field has no accessible name from the wrapper, and the extending component must supply one through `data-aria-label` or `aria-labelledby`.\n- When `toggletip-text` is set, always provide `info-icon-aria-label` — the info-icon button has no visible text and otherwise has no accessible name."
    },
    "dependencies": [
      "mdc-text",
      "mdc-icon",
      "mdc-button",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the field is non-interactive and removed from the tab order. The info-icon button and helper-text icon are also disabled.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "replace the default helper-text rendering.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "default": "DEFAULTS.VALIDATION",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon rendered next to the helper text (no icon for `default`).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible label for the info-icon button. Required when `toggletip-text` is set (the button has no visible text).",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "replace the default label rendering with custom markup.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the field is non-interactive but focusable. Subclasses honour this in their own rendering.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, appends a `*` required indicator next to the label. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the field is visually disabled but remains focusable so assistive technology can read it. Mainly used for fields where disabled state must still be discoverable.",
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "default": "DEFAULTS.TOGGLETIP_PLACEMENT",
          "values": [],
          "description": "placement of the toggletip popover relative to the info-icon button. Default `top`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "default": "'absolute'",
          "values": [],
          "description": "popover positioning strategy for the toggletip. `absolute` (default) or `fixed`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "description": "when set, renders an info-icon button next to the label that opens an `mdc-toggletip` with this text.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        }
      ],
      "rules": [
        "replace the default label rendering with custom markup.",
        "when `true`, appends a `*` required indicator next to the label. Default `false`.",
        "replace the default helper-text rendering.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon rendered next to the helper text (no icon for `default`).",
        "when set, renders an info-icon button next to the label that opens an `mdc-toggletip` with this text.",
        "placement of the toggletip popover relative to the info-icon button. Default `top`.",
        "popover positioning strategy for the toggletip. `absolute` (default) or `fixed`.",
        "accessible label for the info-icon button. Required when `toggletip-text` is set (the button has no visible text).",
        "when `true`, the field is non-interactive but focusable. Subclasses honour this in their own rendering.",
        "when `true`, the field is non-interactive and removed from the tab order. The info-icon button and helper-text icon are also disabled.",
        "when `true`, the field is visually disabled but remains focusable so assistive technology can read it. Mainly used for fields where disabled state must still be discoverable.",
        "replace the default info-icon button + toggletip with custom markup.",
        "replace the default helper-text icon (driven by `help-text-type`)."
      ]
    },
    "examples": [
      {
        "language": "ts",
        "code": "import FormfieldWrapper from '../formfieldwrapper/formfieldwrapper.component';\n\nclass MyField extends FormfieldWrapper {\n  // ...field-specific implementation\n  static override styles = [...FormfieldWrapper.styles, ...myStyles];\n}"
      }
    ],
    "reactImports": [
      "Formfieldwrapper"
    ],
    "constants": {
      "DEFAULTS": {
        "HELPER_TEXT_ID": "helper-text-id",
        "HEADING_ID": "heading-id",
        "ICON_SIZE": 20,
        "INFO_ICON": "'info-badge-filled' as Extract<IconNames, 'info-badge-filled'>"
      },
      "MDC_TEXT_OPTIONS": {}
    },
    "keywords": [
      "accessibility",
      "and",
      "base",
      "button",
      "by",
      "component",
      "every",
      "field",
      "form",
      "formfieldwrapper",
      "guidelines",
      "helper",
      "icon",
      "indicator",
      "info",
      "internal",
      "label",
      "mdc",
      "optional",
      "overview",
      "renders",
      "required",
      "shared",
      "text",
      "that",
      "the",
      "toggletip",
      "validation"
    ]
  },
  {
    "name": "icon",
    "title": "Icon",
    "tagName": "mdc-icon",
    "summary": "Usage, guidelines, and accessibility for the mdc-icon component — a dynamically loaded SVG icon reading its URL from a parent mdc-iconprovider, supporting decorative and informative roles.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/icon",
      "knowledgeBasePath": "components/src/components/icon/knowledge-base/icon.component.md"
    },
    "docs": "## Overview\n\nThe icon renders an SVG by name. It must be mounted inside an `mdc-iconprovider`, which supplies the source URL (or selects the `momentum-icons` package), file extension, default size, and length unit. Setting `name` triggers a fetch (or dynamic import) for that icon; on success the SVG is inlined into the component, and on failure the component renders nothing.\n\nThe size is computed from the `size` and `length-unit` attributes, with fallbacks to the values broadcast by `mdc-iconprovider`. The accessibility contract depends on whether the icon is decorative, informative, or informative-standalone — see the Accessibility section.\n\n### When to use\n\n- Use `mdc-icon` inside any component or layout that needs a Momentum design-system icon.\n- Use it when the icon source is shared across the app — the surrounding `mdc-iconprovider` configures it once.\n\n### When not to use\n\n- Use `mdc-illustration` when the asset is a larger illustrative graphic rather than a simple, single-colour icon.\n- Use an inline `<svg>` when the icon is a one-off graphic that does not need to be loaded through the icon provider system.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/icon/index.js\"; // custom element registration\n// or via React wrapper\nimport { Icon, IconProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (decorative icon inside a provider):\n\n```html\n<mdc-iconprovider icon-set=\"momentum-icons\">\n  <mdc-icon name=\"check-bold\" size=\"1.5\" length-unit=\"rem\"></mdc-icon>\n</mdc-iconprovider>\n```\n\nInformative icon (announced by screen readers):\n\n```html\n<mdc-icon name=\"warning-bold\" aria-label=\"Warning\"></mdc-icon>\n```\n\nInformative standalone icon (informative and not inside an interactive element — needs an accompanying tooltip):\n\n```html\n<mdc-icon\n  id=\"info-icon\"\n  name=\"info-circle-bold\"\n  aria-label=\"More information\"\n  tabindex=\"0\"\n></mdc-icon>\n<mdc-tooltip triggerid=\"info-icon\">More information about this row.</mdc-tooltip>\n```\n\nListen for the `load` event to react to a successful icon fetch and `error` to handle a failed fetch.\n\n### Content guidance\n\n- Pick an icon `name` that exists in the configured icon set (`momentum-icons` package, or a file in the provider's `url` directory). When the icon cannot be fetched, nothing is rendered.\n- Set `size` (with `length-unit`) to match the surrounding type scale. When omitted, the size falls back to the value supplied by the provider, then to `1` (in the provider's `length-unit`).\n- Decide deliberately whether the icon is decorative, informative, or informative-standalone — the right answer drives the ARIA contract (see below).\n\n### Property/Attribute details\n\n- `name` — the filename of the icon to load (without extension). When unset or fetch fails, nothing is rendered.\n- `size` — numeric size; combined with `length-unit` it produces the rendered dimensions (e.g. `size=\"1.5\"` + `length-unit=\"rem\"` → `1.5rem` square). Falls back to the provider's `size`, then to the provider's `DEFAULTS.SIZE`.\n- `length-unit` — CSS length unit (`em`, `rem`, `px`, etc.) used together with `size`. Falls back to the provider's `length-unit`.\n- `aria-label` — accessible name for informative icons. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.\n- `aria-labelledby` — id of an element that labels the icon. Same effect on `role` as `aria-label`.\n\n## Accessibility\n\n### Built-in features\n\nThe component handles three accessibility modes, selected by what the consumer sets on it:\n\n- **Decorative**: no `aria-label` and no `aria-labelledby`. The host has no `role`, and the inlined `<svg>` is set to `aria-hidden=\"true\"` so screen readers skip it entirely.\n- **Informative**: `aria-label` (or `aria-labelledby`) is set. The host receives `role=\"img\"` and the configured accessible name is announced. The inlined `<svg>` remains `aria-hidden=\"true\"` so the SVG's internal contents do not duplicate the announcement.\n- **Informative standalone**: informative as above, plus the consumer must set `tabindex=\"0\"` and connect an `mdc-tooltip` via `triggerid` pointing at the icon's `id`. This case applies only when an informative icon is **not** inside an interactive element (button, link) that already provides hover/focus context.\n\nThe component does not own keyboard handling. Standalone icons rely on the consumer's `tabindex=\"0\"` and the connected tooltip's own keyboard handling.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute     | Value                                                                       |\n| ------------- | ------------- | --------------------------------------------------------------------------- |\n| Host          | `role`        | `img` when `aria-label` or `aria-labelledby` is set, otherwise removed      |\n| Host          | `aria-label`  | mirrors `aria-label` when set                                               |\n| Host          | `aria-labelledby` | mirrors `aria-labelledby` when set                                      |\n| Inlined SVG   | `aria-hidden` | `true` (the SVG content is never announced; the host's `aria-label` is)     |\n| Inlined SVG   | `data-name`   | mirrors the icon `name`, used for styling/automation                        |\n| Inlined SVG   | `part`        | `icon`                                                                      |\n\n### Implementation requirements\n\n#### General\n\n- Mount every `mdc-icon` inside an `mdc-iconprovider`; outside of one the fetch fails and the component renders nothing.\n- Decide the accessibility mode based on the surrounding context: an icon inside an `mdc-button` is decorative (the button has the accessible name); an icon-only button uses an `aria-label` on the **button**, not on the icon.\n\n#### Labelling\n\n- Leave `aria-label`/`aria-labelledby` unset for decorative icons — the SVG is already hidden from screen readers.\n- For informative icons, provide a meaningful `aria-label` (or `aria-labelledby`) that describes what the icon conveys.\n- For informative standalone icons, also set `tabindex=\"0\"` and anchor an `mdc-tooltip` via the tooltip's `triggerid` pointing at the icon's `id`, so the meaning is also visible to sighted users.",
    "sections": {
      "Overview": "The icon renders an SVG by name. It must be mounted inside an `mdc-iconprovider`, which supplies the source URL (or selects the `momentum-icons` package), file extension, default size, and length unit. Setting `name` triggers a fetch (or dynamic import) for that icon; on success the SVG is inlined into the component, and on failure the component renders nothing.\n\nThe size is computed from the `size` and `length-unit` attributes, with fallbacks to the values broadcast by `mdc-iconprovider`. The accessibility contract depends on whether the icon is decorative, informative, or informative-standalone — see the Accessibility section.\n\n### When to use\n\n- Use `mdc-icon` inside any component or layout that needs a Momentum design-system icon.\n- Use it when the icon source is shared across the app — the surrounding `mdc-iconprovider` configures it once.\n\n### When not to use\n\n- Use `mdc-illustration` when the asset is a larger illustrative graphic rather than a simple, single-colour icon.\n- Use an inline `<svg>` when the icon is a one-off graphic that does not need to be loaded through the icon provider system.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/icon/index.js\"; // custom element registration\n// or via React wrapper\nimport { Icon, IconProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (decorative icon inside a provider):\n\n```html\n<mdc-iconprovider icon-set=\"momentum-icons\">\n  <mdc-icon name=\"check-bold\" size=\"1.5\" length-unit=\"rem\"></mdc-icon>\n</mdc-iconprovider>\n```\n\nInformative icon (announced by screen readers):\n\n```html\n<mdc-icon name=\"warning-bold\" aria-label=\"Warning\"></mdc-icon>\n```\n\nInformative standalone icon (informative and not inside an interactive element — needs an accompanying tooltip):\n\n```html\n<mdc-icon\n  id=\"info-icon\"\n  name=\"info-circle-bold\"\n  aria-label=\"More information\"\n  tabindex=\"0\"\n></mdc-icon>\n<mdc-tooltip triggerid=\"info-icon\">More information about this row.</mdc-tooltip>\n```\n\nListen for the `load` event to react to a successful icon fetch and `error` to handle a failed fetch.\n\n### Content guidance\n\n- Pick an icon `name` that exists in the configured icon set (`momentum-icons` package, or a file in the provider's `url` directory). When the icon cannot be fetched, nothing is rendered.\n- Set `size` (with `length-unit`) to match the surrounding type scale. When omitted, the size falls back to the value supplied by the provider, then to `1` (in the provider's `length-unit`).\n- Decide deliberately whether the icon is decorative, informative, or informative-standalone — the right answer drives the ARIA contract (see below).\n\n### Property/Attribute details\n\n- `name` — the filename of the icon to load (without extension). When unset or fetch fails, nothing is rendered.\n- `size` — numeric size; combined with `length-unit` it produces the rendered dimensions (e.g. `size=\"1.5\"` + `length-unit=\"rem\"` → `1.5rem` square). Falls back to the provider's `size`, then to the provider's `DEFAULTS.SIZE`.\n- `length-unit` — CSS length unit (`em`, `rem`, `px`, etc.) used together with `size`. Falls back to the provider's `length-unit`.\n- `aria-label` — accessible name for informative icons. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.\n- `aria-labelledby` — id of an element that labels the icon. Same effect on `role` as `aria-label`.",
      "Accessibility": "### Built-in features\n\nThe component handles three accessibility modes, selected by what the consumer sets on it:\n\n- **Decorative**: no `aria-label` and no `aria-labelledby`. The host has no `role`, and the inlined `<svg>` is set to `aria-hidden=\"true\"` so screen readers skip it entirely.\n- **Informative**: `aria-label` (or `aria-labelledby`) is set. The host receives `role=\"img\"` and the configured accessible name is announced. The inlined `<svg>` remains `aria-hidden=\"true\"` so the SVG's internal contents do not duplicate the announcement.\n- **Informative standalone**: informative as above, plus the consumer must set `tabindex=\"0\"` and connect an `mdc-tooltip` via `triggerid` pointing at the icon's `id`. This case applies only when an informative icon is **not** inside an interactive element (button, link) that already provides hover/focus context.\n\nThe component does not own keyboard handling. Standalone icons rely on the consumer's `tabindex=\"0\"` and the connected tooltip's own keyboard handling.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute     | Value                                                                       |\n| ------------- | ------------- | --------------------------------------------------------------------------- |\n| Host          | `role`        | `img` when `aria-label` or `aria-labelledby` is set, otherwise removed      |\n| Host          | `aria-label`  | mirrors `aria-label` when set                                               |\n| Host          | `aria-labelledby` | mirrors `aria-labelledby` when set                                      |\n| Inlined SVG   | `aria-hidden` | `true` (the SVG content is never announced; the host's `aria-label` is)     |\n| Inlined SVG   | `data-name`   | mirrors the icon `name`, used for styling/automation                        |\n| Inlined SVG   | `part`        | `icon`                                                                      |\n\n### Implementation requirements\n\n#### General\n\n- Mount every `mdc-icon` inside an `mdc-iconprovider`; outside of one the fetch fails and the component renders nothing.\n- Decide the accessibility mode based on the surrounding context: an icon inside an `mdc-button` is decorative (the button has the accessible name); an icon-only button uses an `aria-label` on the **button**, not on the icon.\n\n#### Labelling\n\n- Leave `aria-label`/`aria-labelledby` unset for decorative icons — the SVG is already hidden from screen readers.\n- For informative icons, provide a meaningful `aria-label` (or `aria-labelledby`) that describes what the icon conveys.\n- For informative standalone icons, also set `tabindex=\"0\"` and anchor an `mdc-tooltip` via the tooltip's `triggerid` pointing at the icon's `id`, so the meaning is also visible to sighted users."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "length-unit",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "accessible name for informative icons. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "id of an element that labels the icon. Same effect on `role` as `aria-label`.",
          "reflected": false,
          "source": "component"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "icon",
          "description": "The svg inside the icon element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-icon-fill-color",
          "description": "Allows customization of the default fill color."
        },
        {
          "name": "--mdc-icon-size",
          "description": "Allows customization of the icon size."
        },
        {
          "name": "--mdc-icon-border-radius",
          "description": "Allows customization of the icon border radius."
        }
      ],
      "rules": [
        "the filename of the icon to load (without extension). When unset or fetch fails, nothing is rendered.",
        "numeric size; combined with `length-unit` it produces the rendered dimensions (e.g. `size=\"1.5\"` + `length-unit=\"rem\"` → `1.5rem` square). Falls back to the provider's `size`, then to the provider's `DEFAULTS.SIZE`.",
        "CSS length unit (`em`, `rem`, `px`, etc.) used together with `size`. Falls back to the provider's `length-unit`.",
        "accessible name for informative icons. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.",
        "id of an element that labels the icon. Same effect on `role` as `aria-label`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/icon/index.js\"; // custom element registration\n// or via React wrapper\nimport { Icon, IconProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-iconprovider icon-set=\"momentum-icons\">\n  <mdc-icon name=\"check-bold\" size=\"1.5\" length-unit=\"rem\"></mdc-icon>\n</mdc-iconprovider>"
      },
      {
        "language": "html",
        "code": "<mdc-icon name=\"warning-bold\" aria-label=\"Warning\"></mdc-icon>"
      },
      {
        "language": "html",
        "code": "<mdc-icon\n  id=\"info-icon\"\n  name=\"info-circle-bold\"\n  aria-label=\"More information\"\n  tabindex=\"0\"\n></mdc-icon>\n<mdc-tooltip triggerid=\"info-icon\">More information about this row.</mdc-tooltip>"
      }
    ],
    "reactImports": [
      "Icon",
      "IconProvider"
    ],
    "constants": {
      "DEFAULTS": {
        "NAME": "undefined",
        "SIZE": 1
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "decorative",
      "dynamically",
      "for",
      "from",
      "guidelines",
      "icon",
      "iconprovider",
      "informative",
      "its",
      "loaded",
      "mdc",
      "overview",
      "parent",
      "reading",
      "roles",
      "supporting",
      "svg",
      "the",
      "url",
      "usage"
    ]
  },
  {
    "name": "iconprovider",
    "title": "Iconprovider",
    "tagName": "mdc-iconprovider",
    "summary": "Usage and guidelines for the mdc-iconprovider component — a context provider supplying icon source, file extension, default sizing, and optional caching to every nested mdc-icon.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/iconprovider",
      "knowledgeBasePath": "components/src/components/iconprovider/knowledge-base/iconprovider.component.md"
    },
    "docs": "## Overview\n\nThe iconprovider is a context provider for `mdc-icon`. It broadcasts the configuration every nested icon needs to resolve its `name` into a rendered SVG: which icon set to use, where to fetch from (for custom icons), what file extension to apply, the default size and length unit, and an optional cache strategy.\n\nWrap the application (or a sub-tree) with an `mdc-iconprovider` once; every `mdc-icon` inside it reads the same configuration via context. Without a surrounding iconprovider, an `mdc-icon` cannot resolve its source and renders nothing.\n\n### When to use\n\n- Wrap the root of the application (or any sub-tree that uses icons) with `mdc-iconprovider` to configure the icon source for all nested `mdc-icon` components in one place.\n- Use it when icons should be cached across re-renders or page navigations to avoid repeated network fetches.\n\n### When not to use\n\n- Do not nest multiple `mdc-iconprovider`s when one provider is enough — the nearest provider wins per icon, so nesting only makes sense when different sub-trees genuinely need different sources.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/iconprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { IconProvider } from '@momentum-design/components/dist/react';\n```\n\nMomentum icons (dynamic import from the `@momentum-design/icons` package):\n\n```html\n<mdc-iconprovider icon-set=\"momentum-icons\" length-unit=\"rem\" size=\"1\">\n  <!-- application root with mdc-icon usages -->\n</mdc-iconprovider>\n```\n\nCustom icons (fetched over HTTP from a URL the consumer hosts):\n\n```html\n<mdc-iconprovider\n  icon-set=\"custom-icons\"\n  url=\"/assets/icons\"\n  file-extension=\"svg\"\n  length-unit=\"px\"\n  size=\"16\"\n  cache-strategy=\"web-cache-api\"\n  cache-name=\"my-app-icons\"\n>\n  <!-- application root -->\n</mdc-iconprovider>\n```\n\nThe iconprovider does not render any visible UI of its own — it only acts as a context-broadcasting wrapper around its slotted children.\n\n### Content guidance\n\n- Place the iconprovider once near the root of the application; every nested `mdc-icon` will inherit its configuration.\n- Pick `icon-set=\"momentum-icons\"` for the standard Momentum icon library (the build tooling must support dynamic imports). Pick `icon-set=\"custom-icons\"` when icons live in your own asset bundle.\n- When using `cache-strategy`, always set `cache-name` to a value that is unique across the app's asset caches.\n\n### Property/Attribute details\n\n- `icon-set` — `momentum-icons` (default) for dynamic import from the `@momentum-design/icons` package, or `custom-icons` for fetch from `url`.\n- `url` — base URL from which icons are fetched. Required when `icon-set=\"custom-icons\"`.\n- `file-extension` — extension appended to the icon `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.\n- `length-unit` — CSS length unit (`em`, `rem`, `px`, etc.) used together with `size` on every nested `mdc-icon`. Default `em`. Restricted to an allow-list; invalid values fall back to the default.\n- `size` — numeric default size for every nested `mdc-icon` when the icon does not set its own `size`. Default `1`.\n- `cache-strategy` — `in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `icon-set=\"custom-icons\"`. Default unset (no caching).\n- `cache-name` — name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches.\n\n### Limitations\n\n- The iconprovider has no visible UI — its only effect is broadcasting configuration to nested `mdc-icon` components. Nesting outside this contract has no effect.\n- `cache-strategy=\"web-cache-api\"` only works in HTTPS environments (browser Cache API restriction).\n- `cache-strategy` and `cache-name` only apply when `icon-set=\"custom-icons\"`; with `momentum-icons` the icons are loaded via dynamic JS import and the bundler handles caching.\n\n## Accessibility\n\n### Built-in features\n\nThe iconprovider renders nothing of its own; it has no role, no focusable surface, and no keyboard interaction. Accessibility for icons is entirely owned by the nested `mdc-icon` components (decorative vs informative vs informative-standalone) — see the `mdc-icon` documentation for the ARIA contract.\n\n### Implementation requirements\n\n#### General\n\n- Mount the provider once near the application root; every `mdc-icon` inside the sub-tree inherits its configuration through context.\n- When using `cache-strategy`, always pair it with a unique `cache-name`.\n- For HTTPS-only deployments using custom icons, prefer `cache-strategy=\"web-cache-api\"` so cached icons survive page reloads; use `in-memory-cache` only when persistence across reloads is not needed.",
    "sections": {
      "Overview": "The iconprovider is a context provider for `mdc-icon`. It broadcasts the configuration every nested icon needs to resolve its `name` into a rendered SVG: which icon set to use, where to fetch from (for custom icons), what file extension to apply, the default size and length unit, and an optional cache strategy.\n\nWrap the application (or a sub-tree) with an `mdc-iconprovider` once; every `mdc-icon` inside it reads the same configuration via context. Without a surrounding iconprovider, an `mdc-icon` cannot resolve its source and renders nothing.\n\n### When to use\n\n- Wrap the root of the application (or any sub-tree that uses icons) with `mdc-iconprovider` to configure the icon source for all nested `mdc-icon` components in one place.\n- Use it when icons should be cached across re-renders or page navigations to avoid repeated network fetches.\n\n### When not to use\n\n- Do not nest multiple `mdc-iconprovider`s when one provider is enough — the nearest provider wins per icon, so nesting only makes sense when different sub-trees genuinely need different sources.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/iconprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { IconProvider } from '@momentum-design/components/dist/react';\n```\n\nMomentum icons (dynamic import from the `@momentum-design/icons` package):\n\n```html\n<mdc-iconprovider icon-set=\"momentum-icons\" length-unit=\"rem\" size=\"1\">\n  <!-- application root with mdc-icon usages -->\n</mdc-iconprovider>\n```\n\nCustom icons (fetched over HTTP from a URL the consumer hosts):\n\n```html\n<mdc-iconprovider\n  icon-set=\"custom-icons\"\n  url=\"/assets/icons\"\n  file-extension=\"svg\"\n  length-unit=\"px\"\n  size=\"16\"\n  cache-strategy=\"web-cache-api\"\n  cache-name=\"my-app-icons\"\n>\n  <!-- application root -->\n</mdc-iconprovider>\n```\n\nThe iconprovider does not render any visible UI of its own — it only acts as a context-broadcasting wrapper around its slotted children.\n\n### Content guidance\n\n- Place the iconprovider once near the root of the application; every nested `mdc-icon` will inherit its configuration.\n- Pick `icon-set=\"momentum-icons\"` for the standard Momentum icon library (the build tooling must support dynamic imports). Pick `icon-set=\"custom-icons\"` when icons live in your own asset bundle.\n- When using `cache-strategy`, always set `cache-name` to a value that is unique across the app's asset caches.\n\n### Property/Attribute details\n\n- `icon-set` — `momentum-icons` (default) for dynamic import from the `@momentum-design/icons` package, or `custom-icons` for fetch from `url`.\n- `url` — base URL from which icons are fetched. Required when `icon-set=\"custom-icons\"`.\n- `file-extension` — extension appended to the icon `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.\n- `length-unit` — CSS length unit (`em`, `rem`, `px`, etc.) used together with `size` on every nested `mdc-icon`. Default `em`. Restricted to an allow-list; invalid values fall back to the default.\n- `size` — numeric default size for every nested `mdc-icon` when the icon does not set its own `size`. Default `1`.\n- `cache-strategy` — `in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `icon-set=\"custom-icons\"`. Default unset (no caching).\n- `cache-name` — name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches.\n\n### Limitations\n\n- The iconprovider has no visible UI — its only effect is broadcasting configuration to nested `mdc-icon` components. Nesting outside this contract has no effect.\n- `cache-strategy=\"web-cache-api\"` only works in HTTPS environments (browser Cache API restriction).\n- `cache-strategy` and `cache-name` only apply when `icon-set=\"custom-icons\"`; with `momentum-icons` the icons are loaded via dynamic JS import and the bundler handles caching.",
      "Accessibility": "### Built-in features\n\nThe iconprovider renders nothing of its own; it has no role, no focusable surface, and no keyboard interaction. Accessibility for icons is entirely owned by the nested `mdc-icon` components (decorative vs informative vs informative-standalone) — see the `mdc-icon` documentation for the ARIA contract.\n\n### Implementation requirements\n\n#### General\n\n- Mount the provider once near the application root; every `mdc-icon` inside the sub-tree inherits its configuration through context.\n- When using `cache-strategy`, always pair it with a unique `cache-name`.\n- For HTTPS-only deployments using custom icons, prefer `cache-strategy=\"web-cache-api\"` so cached icons survive page reloads; use `in-memory-cache` only when persistence across reloads is not needed."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "cacheName",
          "attribute": "cache-name",
          "type": "unknown",
          "values": [],
          "description": "name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches.",
          "source": "storybook"
        },
        {
          "name": "cacheStrategy",
          "attribute": "cache-strategy",
          "type": "select",
          "values": [
            "in-memory-cache",
            "web-cache-api"
          ],
          "description": "`in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `icon-set=\"custom-icons\"`. Default unset (no caching).",
          "source": "storybook"
        },
        {
          "name": "fileExtension",
          "attribute": "file-extension",
          "type": "unknown",
          "values": [],
          "description": "extension appended to the icon `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "unknown",
          "values": [],
          "description": "Name of the icon to be rendered underneath IconProvider (not part of IconProvider component)",
          "source": "storybook"
        },
        {
          "name": "iconSet",
          "attribute": "icon-set",
          "type": "select",
          "values": [
            "momentum-icons",
            "custom-icons"
          ],
          "description": "`momentum-icons` (default) for dynamic import from the `@momentum-design/icons` package, or `custom-icons` for fetch from `url`.",
          "source": "storybook"
        },
        {
          "name": "lengthUnit",
          "attribute": "length-unit",
          "type": "unknown",
          "default": "momentum-icons",
          "values": [],
          "description": "CSS length unit (`em`, `rem`, `px`, etc.) used together with `size` on every nested `mdc-icon`. Default `em`. Restricted to an allow-list; invalid values fall back to the default.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "children"
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "`momentum-icons` (default) for dynamic import from the `@momentum-design/icons` package, or `custom-icons` for fetch from `url`.",
        "base URL from which icons are fetched. Required when `icon-set=\"custom-icons\"`.",
        "extension appended to the icon `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.",
        "CSS length unit (`em`, `rem`, `px`, etc.) used together with `size` on every nested `mdc-icon`. Default `em`. Restricted to an allow-list; invalid values fall back to the default.",
        "numeric default size for every nested `mdc-icon` when the icon does not set its own `size`. Default `1`.",
        "`in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `icon-set=\"custom-icons\"`. Default unset (no caching).",
        "name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/iconprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { IconProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-iconprovider icon-set=\"momentum-icons\" length-unit=\"rem\" size=\"1\">\n  <!-- application root with mdc-icon usages -->\n</mdc-iconprovider>"
      },
      {
        "language": "html",
        "code": "<mdc-iconprovider\n  icon-set=\"custom-icons\"\n  url=\"/assets/icons\"\n  file-extension=\"svg\"\n  length-unit=\"px\"\n  size=\"16\"\n  cache-strategy=\"web-cache-api\"\n  cache-name=\"my-app-icons\"\n>\n  <!-- application root -->\n</mdc-iconprovider>"
      }
    ],
    "reactImports": [
      "IconProvider"
    ],
    "constants": {
      "LENGTH_UNIT_SIZE": {
        "px": 16,
        "em": 1,
        "rem": 1,
        "FILE_EXTENSION": "svg",
        "LENGTH_UNIT": "em",
        "SIZE": "LENGTH_UNIT_SIZE.em",
        "ICON_SET": "momentum-icons"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "caching",
      "component",
      "context",
      "default",
      "every",
      "extension",
      "file",
      "for",
      "guidelines",
      "icon",
      "iconprovider",
      "mdc",
      "nested",
      "optional",
      "overview",
      "provider",
      "sizing",
      "source",
      "supplying",
      "the",
      "to",
      "usage"
    ]
  },
  {
    "name": "illustration",
    "title": "Illustration",
    "tagName": "mdc-illustration",
    "summary": "Usage, guidelines, and accessibility for the mdc-illustration component — a dynamically loaded SVG illustration reading its URL from a parent mdc-illustrationprovider.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/illustration",
      "knowledgeBasePath": "components/src/components/illustration/knowledge-base/illustration.component.md"
    },
    "docs": "## Overview\n\nThe illustration renders an SVG illustration by name. It must be mounted inside an `mdc-illustrationprovider`, which supplies the source URL (or selects the `momentum-illustrations` package), file extension, and optional cache strategy. Setting `name` triggers a fetch (or dynamic import); on success the SVG is inlined into the component, and on failure the component renders nothing.\n\nDefault sizing is baked into each illustration name. Override the rendered size with the `--mdc-illustration-size` CSS property. Colouring is also baked into the SVG, so the choice of `name` determines the palette.\n\n### When to use\n\n- Use `mdc-illustration` for larger illustrative graphics in empty states, onboarding screens, success/error confirmations, and feature highlights.\n- Use it when the asset should be loaded from the shared illustration source rather than bundled inline.\n\n### When not to use\n\n- Use `mdc-icon` for small, single-colour interface icons rather than full illustrations.\n- Use an inline `<svg>` for one-off illustrations that do not need to be loaded through the illustration provider system.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/illustration/index.js\"; // custom element registration\n// or via React wrapper\nimport { Illustration, IllustrationProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (decorative illustration inside a provider):\n\n```html\n<mdc-illustrationprovider illustration-set=\"momentum-illustrations\">\n  <mdc-illustration name=\"empty-state-inbox\"></mdc-illustration>\n</mdc-illustrationprovider>\n```\n\nInformative illustration (announced by screen readers):\n\n```html\n<mdc-illustration\n  name=\"empty-state-inbox\"\n  aria-label=\"Your inbox is empty\"\n></mdc-illustration>\n```\n\nOverride the size:\n\n```html\n<mdc-illustration\n  name=\"success-confetti\"\n  style=\"--mdc-illustration-size: 240px;\"\n></mdc-illustration>\n```\n\nListen for the `load` event to react to a successful fetch and `error` to handle a failed fetch.\n\n### Content guidance\n\n- Pick a `name` that exists in the configured illustration set. When the illustration cannot be fetched, nothing is rendered.\n- Override `--mdc-illustration-size` only when the surrounding layout demands a non-default footprint; the per-illustration defaults are designed to feel consistent across surfaces.\n- Decide deliberately whether the illustration is decorative or informative — the right answer drives the ARIA contract.\n\n### Property/Attribute details\n\n- `name` — the filename of the illustration to load (without extension). When unset or fetch fails, nothing is rendered.\n- `aria-label` — accessible name for informative illustrations. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.\n- `aria-labelledby` — id of an element that labels the illustration. Same effect on `role` as `aria-label`.\n\n### Limitations\n\n- Colouring is baked into each illustration name — the component does not expose a way to recolour the SVG at runtime. Pick a different `name` for a different palette.\n- Default sizing is also baked into each illustration name. Use the `--mdc-illustration-size` CSS property to override the rendered size when needed.\n\n## Accessibility\n\n### Built-in features\n\nThe component handles two accessibility modes, selected by what the consumer sets on it:\n\n- **Decorative**: no `aria-label` and no `aria-labelledby`. The host has no `role`, and the inlined `<svg>` is set to `aria-hidden=\"true\"` so screen readers skip it entirely.\n- **Informative**: `aria-label` (or `aria-labelledby`) is set. The host receives `role=\"img\"` and the configured accessible name is announced. The inlined `<svg>` remains `aria-hidden=\"true\"` so the SVG's internal contents do not duplicate the announcement.\n\nThe component does not own keyboard handling or focus.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute        | Value                                                                       |\n| ------------- | ---------------- | --------------------------------------------------------------------------- |\n| Host          | `role`           | `img` when `aria-label` or `aria-labelledby` is set, otherwise removed      |\n| Host          | `aria-label`     | mirrors `aria-label` when set                                               |\n| Host          | `aria-labelledby` | mirrors `aria-labelledby` when set                                         |\n| Inlined SVG   | `aria-hidden`    | `true` (the SVG content is never announced)                                 |\n| Inlined SVG   | `data-name`      | mirrors the illustration `name`                                             |\n| Inlined SVG   | `part`           | `illustration`                                                              |\n\n### Implementation requirements\n\n#### General\n\n- Mount every `mdc-illustration` inside an `mdc-illustrationprovider`; outside of one the fetch fails and the component renders nothing.\n- Use a single illustration per empty state / confirmation surface — pairing two illustrations on the same screen dilutes their visual weight.\n\n#### Labelling\n\n- Leave `aria-label`/`aria-labelledby` unset for decorative illustrations — the SVG is already hidden from screen readers.\n- For informative illustrations, provide a meaningful `aria-label` (or `aria-labelledby`) that describes what the illustration conveys to a sighted user.",
    "sections": {
      "Overview": "The illustration renders an SVG illustration by name. It must be mounted inside an `mdc-illustrationprovider`, which supplies the source URL (or selects the `momentum-illustrations` package), file extension, and optional cache strategy. Setting `name` triggers a fetch (or dynamic import); on success the SVG is inlined into the component, and on failure the component renders nothing.\n\nDefault sizing is baked into each illustration name. Override the rendered size with the `--mdc-illustration-size` CSS property. Colouring is also baked into the SVG, so the choice of `name` determines the palette.\n\n### When to use\n\n- Use `mdc-illustration` for larger illustrative graphics in empty states, onboarding screens, success/error confirmations, and feature highlights.\n- Use it when the asset should be loaded from the shared illustration source rather than bundled inline.\n\n### When not to use\n\n- Use `mdc-icon` for small, single-colour interface icons rather than full illustrations.\n- Use an inline `<svg>` for one-off illustrations that do not need to be loaded through the illustration provider system.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/illustration/index.js\"; // custom element registration\n// or via React wrapper\nimport { Illustration, IllustrationProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (decorative illustration inside a provider):\n\n```html\n<mdc-illustrationprovider illustration-set=\"momentum-illustrations\">\n  <mdc-illustration name=\"empty-state-inbox\"></mdc-illustration>\n</mdc-illustrationprovider>\n```\n\nInformative illustration (announced by screen readers):\n\n```html\n<mdc-illustration\n  name=\"empty-state-inbox\"\n  aria-label=\"Your inbox is empty\"\n></mdc-illustration>\n```\n\nOverride the size:\n\n```html\n<mdc-illustration\n  name=\"success-confetti\"\n  style=\"--mdc-illustration-size: 240px;\"\n></mdc-illustration>\n```\n\nListen for the `load` event to react to a successful fetch and `error` to handle a failed fetch.\n\n### Content guidance\n\n- Pick a `name` that exists in the configured illustration set. When the illustration cannot be fetched, nothing is rendered.\n- Override `--mdc-illustration-size` only when the surrounding layout demands a non-default footprint; the per-illustration defaults are designed to feel consistent across surfaces.\n- Decide deliberately whether the illustration is decorative or informative — the right answer drives the ARIA contract.\n\n### Property/Attribute details\n\n- `name` — the filename of the illustration to load (without extension). When unset or fetch fails, nothing is rendered.\n- `aria-label` — accessible name for informative illustrations. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.\n- `aria-labelledby` — id of an element that labels the illustration. Same effect on `role` as `aria-label`.\n\n### Limitations\n\n- Colouring is baked into each illustration name — the component does not expose a way to recolour the SVG at runtime. Pick a different `name` for a different palette.\n- Default sizing is also baked into each illustration name. Use the `--mdc-illustration-size` CSS property to override the rendered size when needed.",
      "Accessibility": "### Built-in features\n\nThe component handles two accessibility modes, selected by what the consumer sets on it:\n\n- **Decorative**: no `aria-label` and no `aria-labelledby`. The host has no `role`, and the inlined `<svg>` is set to `aria-hidden=\"true\"` so screen readers skip it entirely.\n- **Informative**: `aria-label` (or `aria-labelledby`) is set. The host receives `role=\"img\"` and the configured accessible name is announced. The inlined `<svg>` remains `aria-hidden=\"true\"` so the SVG's internal contents do not duplicate the announcement.\n\nThe component does not own keyboard handling or focus.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute        | Value                                                                       |\n| ------------- | ---------------- | --------------------------------------------------------------------------- |\n| Host          | `role`           | `img` when `aria-label` or `aria-labelledby` is set, otherwise removed      |\n| Host          | `aria-label`     | mirrors `aria-label` when set                                               |\n| Host          | `aria-labelledby` | mirrors `aria-labelledby` when set                                         |\n| Inlined SVG   | `aria-hidden`    | `true` (the SVG content is never announced)                                 |\n| Inlined SVG   | `data-name`      | mirrors the illustration `name`                                             |\n| Inlined SVG   | `part`           | `illustration`                                                              |\n\n### Implementation requirements\n\n#### General\n\n- Mount every `mdc-illustration` inside an `mdc-illustrationprovider`; outside of one the fetch fails and the component renders nothing.\n- Use a single illustration per empty state / confirmation surface — pairing two illustrations on the same screen dilutes their visual weight.\n\n#### Labelling\n\n- Leave `aria-label`/`aria-labelledby` unset for decorative illustrations — the SVG is already hidden from screen readers.\n- For informative illustrations, provide a meaningful `aria-label` (or `aria-labelledby`) that describes what the illustration conveys to a sighted user."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "accessible name for informative illustrations. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "id of an element that labels the illustration. Same effect on `role` as `aria-label`.",
          "reflected": false,
          "source": "component"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "select",
          "values": [],
          "description": "the filename of the illustration to load (without extension). When unset or fetch fails, nothing is rendered.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "illustration",
          "description": "The svg inside the illustration element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-illustration-size",
          "description": "Allows customization of the illustration size."
        }
      ],
      "rules": [
        "the filename of the illustration to load (without extension). When unset or fetch fails, nothing is rendered.",
        "accessible name for informative illustrations. When set (or `aria-labelledby` is set), the host receives `role=\"img\"`. When unset, `role` is removed.",
        "id of an element that labels the illustration. Same effect on `role` as `aria-label`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/illustration/index.js\"; // custom element registration\n// or via React wrapper\nimport { Illustration, IllustrationProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-illustrationprovider illustration-set=\"momentum-illustrations\">\n  <mdc-illustration name=\"empty-state-inbox\"></mdc-illustration>\n</mdc-illustrationprovider>"
      },
      {
        "language": "html",
        "code": "<mdc-illustration\n  name=\"empty-state-inbox\"\n  aria-label=\"Your inbox is empty\"\n></mdc-illustration>"
      },
      {
        "language": "html",
        "code": "<mdc-illustration\n  name=\"success-confetti\"\n  style=\"--mdc-illustration-size: 240px;\"\n></mdc-illustration>"
      }
    ],
    "reactImports": [
      "Illustration",
      "IllustrationProvider"
    ],
    "constants": {
      "DEFAULTS": {
        "NAME": "undefined"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "dynamically",
      "for",
      "from",
      "guidelines",
      "illustration",
      "illustrationprovider",
      "its",
      "loaded",
      "mdc",
      "overview",
      "parent",
      "reading",
      "svg",
      "the",
      "url",
      "usage"
    ]
  },
  {
    "name": "illustrationprovider",
    "title": "Illustrationprovider",
    "tagName": "mdc-illustrationprovider",
    "summary": "Usage and guidelines for the mdc-illustrationprovider component — a context provider supplying illustration source, file extension, and optional caching to every nested mdc-illustration.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/illustrationprovider",
      "knowledgeBasePath": "components/src/components/illustrationprovider/knowledge-base/illustrationprovider.component.md"
    },
    "docs": "## Overview\n\nThe illustrationprovider is a context provider for `mdc-illustration`. It broadcasts the configuration every nested illustration needs to resolve its `name` into a rendered SVG: which illustration set to use, where to fetch from (for custom illustrations), what file extension to apply, and an optional cache strategy.\n\nWrap the application (or a sub-tree) with an `mdc-illustrationprovider` once; every `mdc-illustration` inside it reads the same configuration via context. Without a surrounding illustrationprovider, an `mdc-illustration` cannot resolve its source and renders nothing.\n\n### When to use\n\n- Wrap the root of the application (or any sub-tree that uses illustrations) with `mdc-illustrationprovider` to configure the source once for every nested `mdc-illustration`.\n- Use it when illustrations should be cached across re-renders or page navigations to avoid repeated network fetches.\n\n### When not to use\n\n- Do not nest multiple `mdc-illustrationprovider`s when one provider is enough — the nearest provider wins per illustration, so nesting only makes sense when different sub-trees genuinely need different sources.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/illustrationprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { IllustrationProvider } from '@momentum-design/components/dist/react';\n```\n\nMomentum illustrations (dynamic import from the `@momentum-design/illustrations` package):\n\n```html\n<mdc-illustrationprovider illustration-set=\"momentum-illustrations\">\n  <!-- application root with mdc-illustration usages -->\n</mdc-illustrationprovider>\n```\n\nCustom illustrations (fetched over HTTP from a URL the consumer hosts):\n\n```html\n<mdc-illustrationprovider\n  illustration-set=\"custom-illustrations\"\n  url=\"/assets/illustrations\"\n  file-extension=\"svg\"\n  cache-strategy=\"web-cache-api\"\n  cache-name=\"my-app-illustrations\"\n>\n  <!-- application root -->\n</mdc-illustrationprovider>\n```\n\nThe illustrationprovider does not render any visible UI of its own — it only acts as a context-broadcasting wrapper around its slotted children.\n\n### Content guidance\n\n- Place the illustrationprovider once near the root of the application; every nested `mdc-illustration` will inherit its configuration.\n- Pick `illustration-set=\"momentum-illustrations\"` for the standard Momentum illustration library (the build tooling must support dynamic imports). Pick `illustration-set=\"custom-illustrations\"` when illustrations live in your own asset bundle.\n- When using `cache-strategy`, always set `cache-name` to a value that is unique across the app's asset caches.\n\n### Property/Attribute details\n\n- `illustration-set` — `momentum-illustrations` (default) for dynamic import from the `@momentum-design/illustrations` package, or `custom-illustrations` for fetch from `url`.\n- `url` — base URL from which illustrations are fetched. Required when `illustration-set=\"custom-illustrations\"`.\n- `file-extension` — extension appended to the illustration `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.\n- `cache-strategy` — `in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `illustration-set=\"custom-illustrations\"`. Default unset (no caching).\n- `cache-name` — name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches.\n\n### Limitations\n\n- The illustrationprovider has no visible UI — its only effect is broadcasting configuration to nested `mdc-illustration` components. Nesting outside this contract has no effect.\n- `cache-strategy=\"web-cache-api\"` only works in HTTPS environments (browser Cache API restriction).\n- `cache-strategy` and `cache-name` only apply when `illustration-set=\"custom-illustrations\"`; with `momentum-illustrations` the illustrations are loaded via dynamic JS import and the bundler handles caching.\n\n## Accessibility\n\n### Built-in features\n\nThe illustrationprovider renders nothing of its own; it has no role, no focusable surface, and no keyboard interaction. Accessibility for illustrations is entirely owned by the nested `mdc-illustration` components (decorative vs informative) — see the `mdc-illustration` documentation for the ARIA contract.\n\n### Implementation requirements\n\n#### General\n\n- Mount the provider once near the application root; every `mdc-illustration` inside the sub-tree inherits its configuration through context.\n- When using `cache-strategy`, always pair it with a unique `cache-name`.\n- For HTTPS-only deployments using custom illustrations, prefer `cache-strategy=\"web-cache-api\"` so cached illustrations survive page reloads; use `in-memory-cache` only when persistence across reloads is not needed.",
    "sections": {
      "Overview": "The illustrationprovider is a context provider for `mdc-illustration`. It broadcasts the configuration every nested illustration needs to resolve its `name` into a rendered SVG: which illustration set to use, where to fetch from (for custom illustrations), what file extension to apply, and an optional cache strategy.\n\nWrap the application (or a sub-tree) with an `mdc-illustrationprovider` once; every `mdc-illustration` inside it reads the same configuration via context. Without a surrounding illustrationprovider, an `mdc-illustration` cannot resolve its source and renders nothing.\n\n### When to use\n\n- Wrap the root of the application (or any sub-tree that uses illustrations) with `mdc-illustrationprovider` to configure the source once for every nested `mdc-illustration`.\n- Use it when illustrations should be cached across re-renders or page navigations to avoid repeated network fetches.\n\n### When not to use\n\n- Do not nest multiple `mdc-illustrationprovider`s when one provider is enough — the nearest provider wins per illustration, so nesting only makes sense when different sub-trees genuinely need different sources.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/illustrationprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { IllustrationProvider } from '@momentum-design/components/dist/react';\n```\n\nMomentum illustrations (dynamic import from the `@momentum-design/illustrations` package):\n\n```html\n<mdc-illustrationprovider illustration-set=\"momentum-illustrations\">\n  <!-- application root with mdc-illustration usages -->\n</mdc-illustrationprovider>\n```\n\nCustom illustrations (fetched over HTTP from a URL the consumer hosts):\n\n```html\n<mdc-illustrationprovider\n  illustration-set=\"custom-illustrations\"\n  url=\"/assets/illustrations\"\n  file-extension=\"svg\"\n  cache-strategy=\"web-cache-api\"\n  cache-name=\"my-app-illustrations\"\n>\n  <!-- application root -->\n</mdc-illustrationprovider>\n```\n\nThe illustrationprovider does not render any visible UI of its own — it only acts as a context-broadcasting wrapper around its slotted children.\n\n### Content guidance\n\n- Place the illustrationprovider once near the root of the application; every nested `mdc-illustration` will inherit its configuration.\n- Pick `illustration-set=\"momentum-illustrations\"` for the standard Momentum illustration library (the build tooling must support dynamic imports). Pick `illustration-set=\"custom-illustrations\"` when illustrations live in your own asset bundle.\n- When using `cache-strategy`, always set `cache-name` to a value that is unique across the app's asset caches.\n\n### Property/Attribute details\n\n- `illustration-set` — `momentum-illustrations` (default) for dynamic import from the `@momentum-design/illustrations` package, or `custom-illustrations` for fetch from `url`.\n- `url` — base URL from which illustrations are fetched. Required when `illustration-set=\"custom-illustrations\"`.\n- `file-extension` — extension appended to the illustration `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.\n- `cache-strategy` — `in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `illustration-set=\"custom-illustrations\"`. Default unset (no caching).\n- `cache-name` — name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches.\n\n### Limitations\n\n- The illustrationprovider has no visible UI — its only effect is broadcasting configuration to nested `mdc-illustration` components. Nesting outside this contract has no effect.\n- `cache-strategy=\"web-cache-api\"` only works in HTTPS environments (browser Cache API restriction).\n- `cache-strategy` and `cache-name` only apply when `illustration-set=\"custom-illustrations\"`; with `momentum-illustrations` the illustrations are loaded via dynamic JS import and the bundler handles caching.",
      "Accessibility": "### Built-in features\n\nThe illustrationprovider renders nothing of its own; it has no role, no focusable surface, and no keyboard interaction. Accessibility for illustrations is entirely owned by the nested `mdc-illustration` components (decorative vs informative) — see the `mdc-illustration` documentation for the ARIA contract.\n\n### Implementation requirements\n\n#### General\n\n- Mount the provider once near the application root; every `mdc-illustration` inside the sub-tree inherits its configuration through context.\n- When using `cache-strategy`, always pair it with a unique `cache-name`.\n- For HTTPS-only deployments using custom illustrations, prefer `cache-strategy=\"web-cache-api\"` so cached illustrations survive page reloads; use `in-memory-cache` only when persistence across reloads is not needed."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "cacheName",
          "attribute": "cache-name",
          "type": "unknown",
          "values": [],
          "description": "name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches.",
          "source": "storybook"
        },
        {
          "name": "cacheStrategy",
          "attribute": "cache-strategy",
          "type": "select",
          "values": [
            "in-memory-cache",
            "web-cache-api"
          ],
          "description": "`in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `illustration-set=\"custom-illustrations\"`. Default unset (no caching).",
          "source": "storybook"
        },
        {
          "name": "fileExtension",
          "attribute": "file-extension",
          "type": "unknown",
          "values": [],
          "description": "extension appended to the illustration `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.",
          "source": "storybook"
        },
        {
          "name": "illustrationName",
          "attribute": "illustration-name",
          "type": "unknown",
          "values": [],
          "description": "Name of the illustration to be rendered underneath IllustrationProvider (not part of IllustrationProvider component)",
          "source": "storybook"
        },
        {
          "name": "illustrationSet",
          "attribute": "illustration-set",
          "type": "select",
          "values": [
            "momentum-illustrations",
            "custom-illustrations"
          ],
          "description": "`momentum-illustrations` (default) for dynamic import from the `@momentum-design/illustrations` package, or `custom-illustrations` for fetch from `url`.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "children"
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "`momentum-illustrations` (default) for dynamic import from the `@momentum-design/illustrations` package, or `custom-illustrations` for fetch from `url`.",
        "base URL from which illustrations are fetched. Required when `illustration-set=\"custom-illustrations\"`.",
        "extension appended to the illustration `name` when fetching. Default `svg`. Restricted to an allow-list; invalid values fall back to the default.",
        "`in-memory-cache` (JS in-memory) or `web-cache-api` (browser Cache API; HTTPS only). Only applies when `illustration-set=\"custom-illustrations\"`. Default unset (no caching).",
        "name of the cache used by `cache-strategy`. Required when `cache-strategy` is set; must be unique across the app's caches."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/illustrationprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { IllustrationProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-illustrationprovider illustration-set=\"momentum-illustrations\">\n  <!-- application root with mdc-illustration usages -->\n</mdc-illustrationprovider>"
      },
      {
        "language": "html",
        "code": "<mdc-illustrationprovider\n  illustration-set=\"custom-illustrations\"\n  url=\"/assets/illustrations\"\n  file-extension=\"svg\"\n  cache-strategy=\"web-cache-api\"\n  cache-name=\"my-app-illustrations\"\n>\n  <!-- application root -->\n</mdc-illustrationprovider>"
      }
    ],
    "reactImports": [
      "IllustrationProvider"
    ],
    "constants": {
      "DEFAULTS": {
        "FILE_EXTENSION": "svg",
        "ILLUSTRATION_SET": "momentum-illustrations"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "caching",
      "component",
      "context",
      "every",
      "extension",
      "file",
      "for",
      "guidelines",
      "illustration",
      "illustrationprovider",
      "mdc",
      "nested",
      "optional",
      "overview",
      "provider",
      "source",
      "supplying",
      "the",
      "to",
      "usage"
    ]
  },
  {
    "name": "input",
    "title": "Input",
    "tagName": "mdc-input",
    "summary": "Usage, guidelines, and accessibility for the mdc-input component — a single-line text input with label, helper/validation text, optional leading icon, prefix text, and clear button.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/input",
      "knowledgeBasePath": "components/src/components/input/knowledge-base/input.component.md"
    },
    "docs": "## Overview\n\nThe input is a single-line text field with a label, optional helper or validation text, and an optional trailing clear button. It supports a leading icon, a static prefix text, character counting, and full HTML form integration (the host is form-associated and submits its `value` under `name`).\n\nThe label, required indicator, and helper text rendering follow the standard form-field layout; an optional info-icon toggletip can be attached next to the label for additional context.\n\n### When to use\n\n- Use `mdc-input` for any short, free-form text entry: names, search terms, single-line addresses, codes, or any value that fits on one line.\n- Use it when the field needs label, helper/validation text, and form integration without writing the wiring by hand.\n\n### When not to use\n\n- Use `mdc-textarea` for multi-line input.\n- Use `mdc-password` for password entry that needs show/hide toggling.\n- Use `mdc-searchfield` for search-specific input with built-in search styling.\n- Use `mdc-select` or `mdc-combobox` when the value must be chosen from a fixed list.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/input/index.js\"; // custom element registration\n// or via React wrapper\nimport { Input } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-input\n  name=\"username\"\n  label=\"Username\"\n  placeholder=\"Enter your username\"\n  required\n></mdc-input>\n```\n\nInput with leading icon, prefix text, clear button, and helper text:\n\n```html\n<mdc-input\n  name=\"amount\"\n  label=\"Amount\"\n  leading-icon=\"dollar-bold\"\n  prefix-text=\"USD\"\n  trailing-button\n  clear-aria-label=\"Clear amount\"\n  help-text=\"Enter a value between 1 and 1000\"\n  help-text-type=\"default\"\n></mdc-input>\n```\n\nListen for `input`/`change` to read the value as the user types and on blur; listen for `clear` to react when the trailing clear button empties the field; listen for `limitexceeded` to react when the character counter crosses the configured `max-character-limit`.\n\n### Content guidance\n\n- Provide a meaningful `label` on every input; without it the field has no accessible name unless a `data-aria-label` is supplied.\n- Use `placeholder` for example values or format hints (e.g. `\"name@example.com\"`); never use it as a substitute for the label.\n- Use `help-text` for short instructions or validation messages; pair with `help-text-type` (`default`, `error`, `warning`, `success`, `priority`) so the leading icon matches the message.\n- When `prefix-text` is set, also set `data-aria-label` on the input — the prefix is rendered `aria-hidden=\"true\"` so screen readers do not announce it.\n\n### Property/Attribute details\n\n- `placeholder` — text shown when the value is empty. Default `''`.\n- `prefix-text` — short static text rendered before the input (max 10 characters; longer values trigger a validation message and the prefix is not rendered). Default unset.\n- `leading-icon` — name of an icon rendered before the input (`mdc-icon` name from the configured icon set). Default unset.\n- `trailing-button` — when `true`, shows a clear button at the trailing edge once the value is non-empty. Default `false`.\n- `clear-aria-label` — accessible name for the trailing clear button. Required when `trailing-button` is `true`.\n- `maxlength` / `minlength` — native `maxlength` / `minlength` validation passed through to the underlying `<input>`.\n- `pattern` — regex the value must match for native form validation.\n- `list` — id of a `<datalist>` providing autocomplete suggestions.\n- `size` — native `size` attribute (CSS character width). Default unset.\n- `autocapitalize` — `off` (default), `on`, `sentences`, `words`, `characters`, `none`.\n- `autocomplete` — standard HTML `autocomplete` token. Default `off`.\n- `dirname` — submits the text directionality alongside the value.\n- `data-aria-label` — accessible name applied to the underlying `<input>`. Required when no visible label is rendered (e.g. when `prefix-text` is set or when the field is used without a label).\n- `data-aria-describedby` — id of an element that describes the input. Used as the input's `aria-describedby` when no helper text is rendered; when helper text is rendered, the helper text's id wins.\n\nProperties inherited from the form-field wrapper:\n\n- `label` — visible label text. Rendered as a `<label for>` linked to the input.\n- `required` — when `true`, appends a `*` required indicator and sets `required` on the underlying `<input>`.\n- `help-text` — helper or validation text rendered below the input.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and the input's `aria-invalid` (`true` only for `error`).\n- `toggletip-text` — when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.\n- `toggletip-placement`, `toggletip-strategy` — placement / positioning strategy for the toggletip popover.\n- `info-icon-aria-label` — accessible name for the info-icon button. Required when `toggletip-text` is set.\n- `readonly` — when `true`, the input is non-interactive but focusable.\n- `disabled` — when `true`, the input is non-interactive and removed from the tab order.\n- `soft-disabled` — when `true`, the input looks disabled but remains focusable so assistive technology can read it.\n- `name`, `value`, `validation-message` — standard form-control hooks; the host participates in form submission and validation through ElementInternals.\n- `max-character-limit` — when set, renders a character counter and dispatches `limitexceeded` when the value crosses or returns under the limit.\n- `auto-focus-on-mount` — when `true`, the underlying `<input>` is focused after first render.\n\n## Accessibility\n\n### Built-in features\n\nThe wrapper renders the label as a native `<label for>` element linked to the underlying `<input>` by `id`, so clicking the label focuses the input with no extra wiring. When helper text is rendered, the input's `aria-describedby` points at the helper-text id so the message is announced when the input receives focus. When `help-text-type=\"error\"`, `aria-invalid=\"true\"` is set on the input. The `required` flag drives the native `required` attribute on the input (the visual `*` is decorative). The trailing clear button is rendered as an `mdc-button` with its own `aria-label` (`clear-aria-label`). Prefix text is rendered with `aria-hidden=\"true\"` to avoid double announcement — the consumer must supply `data-aria-label` so the input still has an accessible name. Pressing `Enter` in the field submits the surrounding form via `form.requestSubmit()`.\n\nThe character-limit announcer is rendered as an `mdc-screenreaderannouncer` with `data-aria-live=\"polite\"`, so warnings are announced without stealing focus.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute         | Value                                                                                     |\n| ------------- | ----------------- | ----------------------------------------------------------------------------------------- |\n| Native label  | `for`             | the `id` of the underlying `<input>`                                                      |\n| `<input>`     | `aria-label`      | mirrors `data-aria-label`                                                                 |\n| `<input>`     | `aria-describedby`| helper-text id when `help-text` is set, otherwise mirrors `data-aria-describedby`         |\n| `<input>`     | `aria-invalid`    | `true` when `help-text-type=\"error\"`, otherwise `false`                                   |\n| `<input>`     | `required`        | reflects `required`                                                                       |\n| Prefix text   | `aria-hidden`     | `true`                                                                                    |\n| Clear button  | `aria-label`      | mirrors `clear-aria-label`                                                                |\n| Helper text   | `id`              | stable id used by the input's `aria-describedby`                                          |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide either `label` or `data-aria-label`; otherwise the input has no accessible name.\n- When `prefix-text` is set, provide `data-aria-label` so the announced name includes the prefix's meaning (e.g. `data-aria-label=\"Amount in US dollars\"`).\n- When `trailing-button` is `true`, provide `clear-aria-label` so the clear button has an accessible name.\n- When `toggletip-text` is set, provide `info-icon-aria-label` for the info-icon button.",
    "sections": {
      "Overview": "The input is a single-line text field with a label, optional helper or validation text, and an optional trailing clear button. It supports a leading icon, a static prefix text, character counting, and full HTML form integration (the host is form-associated and submits its `value` under `name`).\n\nThe label, required indicator, and helper text rendering follow the standard form-field layout; an optional info-icon toggletip can be attached next to the label for additional context.\n\n### When to use\n\n- Use `mdc-input` for any short, free-form text entry: names, search terms, single-line addresses, codes, or any value that fits on one line.\n- Use it when the field needs label, helper/validation text, and form integration without writing the wiring by hand.\n\n### When not to use\n\n- Use `mdc-textarea` for multi-line input.\n- Use `mdc-password` for password entry that needs show/hide toggling.\n- Use `mdc-searchfield` for search-specific input with built-in search styling.\n- Use `mdc-select` or `mdc-combobox` when the value must be chosen from a fixed list.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/input/index.js\"; // custom element registration\n// or via React wrapper\nimport { Input } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-input\n  name=\"username\"\n  label=\"Username\"\n  placeholder=\"Enter your username\"\n  required\n></mdc-input>\n```\n\nInput with leading icon, prefix text, clear button, and helper text:\n\n```html\n<mdc-input\n  name=\"amount\"\n  label=\"Amount\"\n  leading-icon=\"dollar-bold\"\n  prefix-text=\"USD\"\n  trailing-button\n  clear-aria-label=\"Clear amount\"\n  help-text=\"Enter a value between 1 and 1000\"\n  help-text-type=\"default\"\n></mdc-input>\n```\n\nListen for `input`/`change` to read the value as the user types and on blur; listen for `clear` to react when the trailing clear button empties the field; listen for `limitexceeded` to react when the character counter crosses the configured `max-character-limit`.\n\n### Content guidance\n\n- Provide a meaningful `label` on every input; without it the field has no accessible name unless a `data-aria-label` is supplied.\n- Use `placeholder` for example values or format hints (e.g. `\"name@example.com\"`); never use it as a substitute for the label.\n- Use `help-text` for short instructions or validation messages; pair with `help-text-type` (`default`, `error`, `warning`, `success`, `priority`) so the leading icon matches the message.\n- When `prefix-text` is set, also set `data-aria-label` on the input — the prefix is rendered `aria-hidden=\"true\"` so screen readers do not announce it.\n\n### Property/Attribute details\n\n- `placeholder` — text shown when the value is empty. Default `''`.\n- `prefix-text` — short static text rendered before the input (max 10 characters; longer values trigger a validation message and the prefix is not rendered). Default unset.\n- `leading-icon` — name of an icon rendered before the input (`mdc-icon` name from the configured icon set). Default unset.\n- `trailing-button` — when `true`, shows a clear button at the trailing edge once the value is non-empty. Default `false`.\n- `clear-aria-label` — accessible name for the trailing clear button. Required when `trailing-button` is `true`.\n- `maxlength` / `minlength` — native `maxlength` / `minlength` validation passed through to the underlying `<input>`.\n- `pattern` — regex the value must match for native form validation.\n- `list` — id of a `<datalist>` providing autocomplete suggestions.\n- `size` — native `size` attribute (CSS character width). Default unset.\n- `autocapitalize` — `off` (default), `on`, `sentences`, `words`, `characters`, `none`.\n- `autocomplete` — standard HTML `autocomplete` token. Default `off`.\n- `dirname` — submits the text directionality alongside the value.\n- `data-aria-label` — accessible name applied to the underlying `<input>`. Required when no visible label is rendered (e.g. when `prefix-text` is set or when the field is used without a label).\n- `data-aria-describedby` — id of an element that describes the input. Used as the input's `aria-describedby` when no helper text is rendered; when helper text is rendered, the helper text's id wins.\n\nProperties inherited from the form-field wrapper:\n\n- `label` — visible label text. Rendered as a `<label for>` linked to the input.\n- `required` — when `true`, appends a `*` required indicator and sets `required` on the underlying `<input>`.\n- `help-text` — helper or validation text rendered below the input.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and the input's `aria-invalid` (`true` only for `error`).\n- `toggletip-text` — when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.\n- `toggletip-placement`, `toggletip-strategy` — placement / positioning strategy for the toggletip popover.\n- `info-icon-aria-label` — accessible name for the info-icon button. Required when `toggletip-text` is set.\n- `readonly` — when `true`, the input is non-interactive but focusable.\n- `disabled` — when `true`, the input is non-interactive and removed from the tab order.\n- `soft-disabled` — when `true`, the input looks disabled but remains focusable so assistive technology can read it.\n- `name`, `value`, `validation-message` — standard form-control hooks; the host participates in form submission and validation through ElementInternals.\n- `max-character-limit` — when set, renders a character counter and dispatches `limitexceeded` when the value crosses or returns under the limit.\n- `auto-focus-on-mount` — when `true`, the underlying `<input>` is focused after first render.",
      "Accessibility": "### Built-in features\n\nThe wrapper renders the label as a native `<label for>` element linked to the underlying `<input>` by `id`, so clicking the label focuses the input with no extra wiring. When helper text is rendered, the input's `aria-describedby` points at the helper-text id so the message is announced when the input receives focus. When `help-text-type=\"error\"`, `aria-invalid=\"true\"` is set on the input. The `required` flag drives the native `required` attribute on the input (the visual `*` is decorative). The trailing clear button is rendered as an `mdc-button` with its own `aria-label` (`clear-aria-label`). Prefix text is rendered with `aria-hidden=\"true\"` to avoid double announcement — the consumer must supply `data-aria-label` so the input still has an accessible name. Pressing `Enter` in the field submits the surrounding form via `form.requestSubmit()`.\n\nThe character-limit announcer is rendered as an `mdc-screenreaderannouncer` with `data-aria-live=\"polite\"`, so warnings are announced without stealing focus.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute         | Value                                                                                     |\n| ------------- | ----------------- | ----------------------------------------------------------------------------------------- |\n| Native label  | `for`             | the `id` of the underlying `<input>`                                                      |\n| `<input>`     | `aria-label`      | mirrors `data-aria-label`                                                                 |\n| `<input>`     | `aria-describedby`| helper-text id when `help-text` is set, otherwise mirrors `data-aria-describedby`         |\n| `<input>`     | `aria-invalid`    | `true` when `help-text-type=\"error\"`, otherwise `false`                                   |\n| `<input>`     | `required`        | reflects `required`                                                                       |\n| Prefix text   | `aria-hidden`     | `true`                                                                                    |\n| Clear button  | `aria-label`      | mirrors `clear-aria-label`                                                                |\n| Helper text   | `id`              | stable id used by the input's `aria-describedby`                                          |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide either `label` or `data-aria-label`; otherwise the input has no accessible name.\n- When `prefix-text` is set, provide `data-aria-label` so the announced name includes the prefix's meaning (e.g. `data-aria-label=\"Amount in US dollars\"`).\n- When `trailing-button` is `true`, provide `clear-aria-label` so the clear button has an accessible name.\n- When `toggletip-text` is set, provide `info-icon-aria-label` for the info-icon button."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text",
      "mdc-button",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autocapitalize",
          "attribute": "autocapitalize",
          "type": "select",
          "default": "AUTO_CAPITALIZE.OFF",
          "values": [
            "off",
            "none",
            "on",
            "sentences",
            "words",
            "characters"
          ],
          "description": "`off` (default), `on`, `sentences`, `words`, `characters`, `none`.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "autocomplete",
          "attribute": "autocomplete",
          "type": "text",
          "default": "'off'",
          "values": [
            "off",
            "on"
          ],
          "description": "standard HTML `autocomplete` token. Default `off`.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the underlying `<input>` is focused after first render.",
          "source": "storybook"
        },
        {
          "name": "characterLimitAnnouncement",
          "attribute": "character-limit-announcement",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "clearAriaLabel",
          "attribute": "clear-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name for the trailing clear button. Required when `trailing-button` is `true`.",
          "source": "storybook"
        },
        {
          "name": "dataAriaDescribedby",
          "attribute": "data-aria-describedby",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "id of an element that describes the input. Used as the input's `aria-describedby` when no helper text is rendered; when helper text is rendered, the helper text's id wins.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the underlying `<input>`. Required when no visible label is rendered (e.g. when `prefix-text` is set or when the field is used without a label).",
          "source": "storybook"
        },
        {
          "name": "dirname",
          "attribute": "dirname",
          "type": "text",
          "values": [],
          "description": "submits the text directionality alongside the value.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the input is non-interactive and removed from the tab order.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper or validation text rendered below the input.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and the input's `aria-invalid` (`true` only for `error`).",
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name for the info-icon button. Required when `toggletip-text` is set.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label text. Rendered as a `<label for>` linked to the input.",
          "source": "storybook"
        },
        {
          "name": "leadingIcon",
          "attribute": "leading-icon",
          "type": "text",
          "values": [],
          "description": "name of an icon rendered before the input (`mdc-icon` name from the configured icon set). Default unset.",
          "source": "storybook"
        },
        {
          "name": "list",
          "attribute": "list",
          "type": "text",
          "values": [],
          "description": "id of a `<datalist>` providing autocomplete suggestions.",
          "source": "storybook"
        },
        {
          "name": "maxCharacterLimit",
          "attribute": "max-character-limit",
          "type": "number",
          "values": [],
          "description": "when set, renders a character counter and dispatches `limitexceeded` when the value crosses or returns under the limit.",
          "source": "storybook"
        },
        {
          "name": "maxlength",
          "attribute": "maxlength",
          "type": "number",
          "values": [],
          "description": "native `maxlength` / `minlength` validation passed through to the underlying `<input>`.",
          "source": "storybook"
        },
        {
          "name": "minlength",
          "attribute": "minlength",
          "type": "number",
          "values": [],
          "description": "native `maxlength` / `minlength` validation passed through to the underlying `<input>`.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "pattern",
          "attribute": "pattern",
          "type": "text",
          "values": [],
          "description": "regex the value must match for native form validation.",
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "description": "text shown when the value is empty. Default `''`.",
          "source": "storybook"
        },
        {
          "name": "prefixText",
          "attribute": "prefix-text",
          "type": "text",
          "values": [],
          "description": "short static text rendered before the input (max 10 characters; longer values trigger a validation message and the prefix is not rendered). Default unset.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the input is non-interactive but focusable.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, appends a `*` required indicator and sets `required` on the underlying `<input>`.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "number",
          "values": [],
          "description": "native `size` attribute (CSS character width). Default unset.",
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "description": "when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.",
          "source": "storybook"
        },
        {
          "name": "trailingButton",
          "attribute": "trailing-button",
          "type": "boolean",
          "values": [],
          "description": "when `true`, shows a clear button at the trailing edge once the value is non-empty. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "validationMessage",
          "attribute": "validation-message",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        },
        {
          "name": "input",
          "description": "Slot for the input element. If not provided, the input field will be rendered."
        },
        {
          "name": "input-leading-icon",
          "description": "Slot for the leading icon before the input field. If not provided, the `leadingIcon` property will be used to render the leading icon."
        },
        {
          "name": "input-prefix-text",
          "description": "Slot for the prefix text before the input field. If not provided, the `prefixText` property will be used to render the prefix text."
        },
        {
          "name": "trailing-button",
          "description": "Slot for the trailing button to clear the input field. If not provided, the clear button will be rendered when `trailingButton` property is set to true."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the value of the input field changes (every press)."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the value of the input field changes (on blur)."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the input receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the input loses focus."
        },
        {
          "name": "clear",
          "description": "(React: onClear) This event is dispatched when the input text is cleared."
        },
        {
          "name": "limitexceeded",
          "description": "(React: onLimitExceeded) This event is dispatched once when the character limit"
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "leading-icon",
          "description": "The leading icon element that is displayed before the input field."
        },
        {
          "name": "prefix-text",
          "description": "The prefix text element that is displayed before the input field."
        },
        {
          "name": "input-container",
          "description": "The container for the input field, leading icon, prefix text, and trailing button elements."
        },
        {
          "name": "input-section",
          "description": "The container for the input field, leading icon, and prefix text elements."
        },
        {
          "name": "input-text",
          "description": "The input field element."
        },
        {
          "name": "trailing-button",
          "description": "The trailing button element that is displayed to clear the input field when the `trailingButton` property is set to true."
        },
        {
          "name": "input-footer",
          "description": "The footer element that contains the helper text and character counter."
        },
        {
          "name": "character-counter",
          "description": "The character counter element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-input-text-color",
          "description": "Text color for the input field"
        },
        {
          "name": "--mdc-input-border-color",
          "description": "Border color for the input container"
        },
        {
          "name": "--mdc-input-background-color",
          "description": "Background color for the input field"
        },
        {
          "name": "--mdc-input-height",
          "description": "Height for the input container"
        },
        {
          "name": "--mdc-input-support-text-color",
          "description": "Text color for the help text"
        },
        {
          "name": "--mdc-input-selection-text-color",
          "description": "Text color for the selected text"
        },
        {
          "name": "--mdc-input-selection-background-color",
          "description": "Background color for the selected text"
        }
      ],
      "rules": [
        "text shown when the value is empty. Default `''`.",
        "short static text rendered before the input (max 10 characters; longer values trigger a validation message and the prefix is not rendered). Default unset.",
        "name of an icon rendered before the input (`mdc-icon` name from the configured icon set). Default unset.",
        "when `true`, shows a clear button at the trailing edge once the value is non-empty. Default `false`.",
        "accessible name for the trailing clear button. Required when `trailing-button` is `true`.",
        "native `maxlength` / `minlength` validation passed through to the underlying `<input>`.",
        "native `maxlength` / `minlength` validation passed through to the underlying `<input>`.",
        "regex the value must match for native form validation.",
        "id of a `<datalist>` providing autocomplete suggestions.",
        "native `size` attribute (CSS character width). Default unset.",
        "`off` (default), `on`, `sentences`, `words`, `characters`, `none`.",
        "standard HTML `autocomplete` token. Default `off`.",
        "submits the text directionality alongside the value.",
        "accessible name applied to the underlying `<input>`. Required when no visible label is rendered (e.g. when `prefix-text` is set or when the field is used without a label).",
        "id of an element that describes the input. Used as the input's `aria-describedby` when no helper text is rendered; when helper text is rendered, the helper text's id wins.",
        "visible label text. Rendered as a `<label for>` linked to the input.",
        "when `true`, appends a `*` required indicator and sets `required` on the underlying `<input>`.",
        "helper or validation text rendered below the input.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and the input's `aria-invalid` (`true` only for `error`).",
        "when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.",
        "accessible name for the info-icon button. Required when `toggletip-text` is set.",
        "when `true`, the input is non-interactive but focusable.",
        "when `true`, the input is non-interactive and removed from the tab order.",
        "when `true`, the input looks disabled but remains focusable so assistive technology can read it.",
        "when set, renders a character counter and dispatches `limitexceeded` when the value crosses or returns under the limit.",
        "when `true`, the underlying `<input>` is focused after first render."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/input/index.js\"; // custom element registration\n// or via React wrapper\nimport { Input } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-input\n  name=\"username\"\n  label=\"Username\"\n  placeholder=\"Enter your username\"\n  required\n></mdc-input>"
      },
      {
        "language": "html",
        "code": "<mdc-input\n  name=\"amount\"\n  label=\"Amount\"\n  leading-icon=\"dollar-bold\"\n  prefix-text=\"USD\"\n  trailing-button\n  clear-aria-label=\"Clear amount\"\n  help-text=\"Enter a value between 1 and 1000\"\n  help-text-type=\"default\"\n></mdc-input>"
      }
    ],
    "reactImports": [
      "Input"
    ],
    "constants": {
      "AUTO_CAPITALIZE": {
        "OFF": "off",
        "NONE": "none",
        "ON": "on",
        "SENTENCES": "sentences",
        "WORDS": "words",
        "CHARACTERS": "characters"
      },
      "AUTO_COMPLETE": {
        "OFF": "off",
        "ON": "on"
      },
      "INPUT_TYPE": {
        "PASSWORD": "password",
        "TEXT": "text",
        "SEARCH": "search"
      },
      "PREFIX_TEXT_OPTIONS": {
        "MAX_LENGTH": 10,
        "ICON_SIZE_VALUE": 1,
        "ICON_SIZE_UNIT": "rem",
        "CLEAR_BUTTON_ICON": "'cancel-bold' as Extract<IconNames, 'cancel-bold'>",
        "AUTO_COMPLETE": "off",
        "INPUT_TYPE": "text"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "button",
      "clear",
      "component",
      "for",
      "guidelines",
      "helper",
      "icon",
      "input",
      "label",
      "leading",
      "line",
      "mdc",
      "optional",
      "overview",
      "prefix",
      "single",
      "text",
      "the",
      "toggletip",
      "usage",
      "validation",
      "with"
    ]
  },
  {
    "name": "inputchip",
    "title": "Inputchip",
    "tagName": "mdc-inputchip",
    "summary": "Usage, guidelines, and accessibility for the mdc-inputchip component — an interactive chip representing a tokenised input value with a leading prefix, a label, and a close button.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/inputchip",
      "knowledgeBasePath": "components/src/components/inputchip/knowledge-base/inputchip.component.md"
    },
    "docs": "## Overview\n\nThe inputchip represents a single tokenised value: a short label with an optional leading prefix (icon name or arbitrary slotted content such as an avatar) and a close button that fires a `remove` event when activated. It is used in patterns where multiple values are collected into a list of chips (recipients, filters, tags).\n\nThe chip supports an `error` visual state for invalid values, a `disabled` state that also disables the close button, and a `prefix` slot that takes precedence over `icon-name` when both are provided.\n\n### When to use\n\n- Use `mdc-inputchip` to represent individual values inside a multi-value input pattern: token-style inputs for recipients, tags, filters, or any list of removable items.\n- Use it when the consumer needs full control over chip removal — the chip emits `remove` and the consumer decides what to do with that signal.\n\n### When not to use\n\n- Use `mdc-filterchip` for chips that toggle a selected/unselected state to filter a list, rather than representing a removable input value.\n- Use `mdc-chip` for static, non-removable display chips.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/inputchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { InputChip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-inputchip\n  label=\"alex@example.com\"\n  icon-name=\"email-bold\"\n  clear-aria-label=\"Remove alex@example.com\"\n></mdc-inputchip>\n```\n\nWith an avatar slotted as the prefix (takes precedence over `icon-name`):\n\n```html\n<mdc-inputchip label=\"Alex Kim\" clear-aria-label=\"Remove Alex Kim\">\n  <mdc-avatar slot=\"prefix\" initials=\"AK\" size=\"24\"></mdc-avatar>\n</mdc-inputchip>\n```\n\nListen for the `remove` event to react to the close button being clicked.\n\n### Content guidance\n\n- Keep the `label` short — we recommend a maximum length of 20 characters (including spaces). Longer values are visually truncated.\n- Use `icon-name` for simple iconographic prefixes; use the `prefix` slot for richer content such as avatars or presence indicators (slot content always wins over `icon-name`).\n- Set `error=\"true\"` when the represented value fails validation; pair the chip with surrounding helper text that explains the failure.\n\n### Property/Attribute details\n\n- `label` — the visible text. Default `''`. Aim for ≤20 characters.\n- `icon-name` — name of the icon rendered in the leading prefix when no `prefix` slot content is provided. Default unset.\n- `error` — when `true`, switches the chip to its error visual treatment. Default `false`.\n- `disabled` — when `true`, dims the chip and disables the close button so the value cannot be removed by user interaction.\n- `clear-aria-label` — accessible name for the close button. Required (the close button has no visible text).\n\n### Limitations\n\n- The chip is not form-associated — it does not submit a value of its own. The owning input pattern is responsible for tracking which values are currently represented as chips and serialising them when the form submits.\n\n## Accessibility\n\n### Built-in features\n\nThe close button is rendered as a tertiary `mdc-button` with its own `aria-label` (sourced from `clear-aria-label`), so screen readers announce the remove action. Activating the button (click, Enter, or Space) dispatches a composed, bubbling `remove` event the consumer listens for. When `disabled` is `true`, the close button is also disabled, so the chip cannot be removed by keyboard or pointer.\n\nThe label and prefix icon are presentational — the consumer chooses how to expose the chip's meaning through `clear-aria-label` (which typically includes the value, e.g. `\"Remove alex@example.com\"`).\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute     | Value                              |\n| -------------- | ------------- | ---------------------------------- |\n| Close button   | `aria-label`  | mirrors `clear-aria-label`         |\n| Close button   | `disabled`    | reflects `disabled`                |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always set `clear-aria-label` to a value that identifies which chip will be removed (e.g. `\"Remove alex@example.com\"`); the close button otherwise has no accessible name.\n- When using slotted prefix content such as an avatar, do not rely on the avatar's accessible name to identify the chip — the `clear-aria-label` is the only name announced for the remove action.",
    "sections": {
      "Overview": "The inputchip represents a single tokenised value: a short label with an optional leading prefix (icon name or arbitrary slotted content such as an avatar) and a close button that fires a `remove` event when activated. It is used in patterns where multiple values are collected into a list of chips (recipients, filters, tags).\n\nThe chip supports an `error` visual state for invalid values, a `disabled` state that also disables the close button, and a `prefix` slot that takes precedence over `icon-name` when both are provided.\n\n### When to use\n\n- Use `mdc-inputchip` to represent individual values inside a multi-value input pattern: token-style inputs for recipients, tags, filters, or any list of removable items.\n- Use it when the consumer needs full control over chip removal — the chip emits `remove` and the consumer decides what to do with that signal.\n\n### When not to use\n\n- Use `mdc-filterchip` for chips that toggle a selected/unselected state to filter a list, rather than representing a removable input value.\n- Use `mdc-chip` for static, non-removable display chips.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/inputchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { InputChip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-inputchip\n  label=\"alex@example.com\"\n  icon-name=\"email-bold\"\n  clear-aria-label=\"Remove alex@example.com\"\n></mdc-inputchip>\n```\n\nWith an avatar slotted as the prefix (takes precedence over `icon-name`):\n\n```html\n<mdc-inputchip label=\"Alex Kim\" clear-aria-label=\"Remove Alex Kim\">\n  <mdc-avatar slot=\"prefix\" initials=\"AK\" size=\"24\"></mdc-avatar>\n</mdc-inputchip>\n```\n\nListen for the `remove` event to react to the close button being clicked.\n\n### Content guidance\n\n- Keep the `label` short — we recommend a maximum length of 20 characters (including spaces). Longer values are visually truncated.\n- Use `icon-name` for simple iconographic prefixes; use the `prefix` slot for richer content such as avatars or presence indicators (slot content always wins over `icon-name`).\n- Set `error=\"true\"` when the represented value fails validation; pair the chip with surrounding helper text that explains the failure.\n\n### Property/Attribute details\n\n- `label` — the visible text. Default `''`. Aim for ≤20 characters.\n- `icon-name` — name of the icon rendered in the leading prefix when no `prefix` slot content is provided. Default unset.\n- `error` — when `true`, switches the chip to its error visual treatment. Default `false`.\n- `disabled` — when `true`, dims the chip and disables the close button so the value cannot be removed by user interaction.\n- `clear-aria-label` — accessible name for the close button. Required (the close button has no visible text).\n\n### Limitations\n\n- The chip is not form-associated — it does not submit a value of its own. The owning input pattern is responsible for tracking which values are currently represented as chips and serialising them when the form submits.",
      "Accessibility": "### Built-in features\n\nThe close button is rendered as a tertiary `mdc-button` with its own `aria-label` (sourced from `clear-aria-label`), so screen readers announce the remove action. Activating the button (click, Enter, or Space) dispatches a composed, bubbling `remove` event the consumer listens for. When `disabled` is `true`, the close button is also disabled, so the chip cannot be removed by keyboard or pointer.\n\nThe label and prefix icon are presentational — the consumer chooses how to expose the chip's meaning through `clear-aria-label` (which typically includes the value, e.g. `\"Remove alex@example.com\"`).\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute     | Value                              |\n| -------------- | ------------- | ---------------------------------- |\n| Close button   | `aria-label`  | mirrors `clear-aria-label`         |\n| Close button   | `disabled`    | reflects `disabled`                |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always set `clear-aria-label` to a value that identifies which chip will be removed (e.g. `\"Remove alex@example.com\"`); the close button otherwise has no accessible name.\n- When using slotted prefix content such as an avatar, do not rely on the avatar's accessible name to identify the chip — the `clear-aria-label` is the only name announced for the remove action."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "clearAriaLabel",
          "attribute": "clear-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name for the close button. Required (the close button has no visible text).",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, dims the chip and disables the close button so the value cannot be removed by user interaction.",
          "source": "storybook"
        },
        {
          "name": "error",
          "attribute": "error",
          "type": "boolean",
          "values": [],
          "description": "when `true`, switches the chip to its error visual treatment. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "text",
          "values": [],
          "description": "name of the icon rendered in the leading prefix when no `prefix` slot content is provided. Default unset.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "the visible text. Default `''`. Aim for ≤20 characters.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "prefix",
          "description": "A slot for prefix content such as avatars."
        }
      ],
      "events": [
        {
          "name": "remove",
          "description": "This event is dispatched when the close button is activated. It bubbles and is composed."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label part of the chip."
        },
        {
          "name": "icon",
          "description": "The icon part of the chip."
        },
        {
          "name": "close-icon",
          "description": "The close icon part of the chip."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-chip-color",
          "description": "The color of the chip."
        },
        {
          "name": "--mdc-chip-border-color",
          "description": "The border color of the chip."
        },
        {
          "name": "--mdc-chip-background-color",
          "description": "The background color of the chip."
        }
      ],
      "rules": [
        "the visible text. Default `''`. Aim for ≤20 characters.",
        "name of the icon rendered in the leading prefix when no `prefix` slot content is provided. Default unset.",
        "when `true`, switches the chip to its error visual treatment. Default `false`.",
        "when `true`, dims the chip and disables the close button so the value cannot be removed by user interaction.",
        "accessible name for the close button. Required (the close button has no visible text)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/inputchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { InputChip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-inputchip\n  label=\"alex@example.com\"\n  icon-name=\"email-bold\"\n  clear-aria-label=\"Remove alex@example.com\"\n></mdc-inputchip>"
      },
      {
        "language": "html",
        "code": "<mdc-inputchip label=\"Alex Kim\" clear-aria-label=\"Remove Alex Kim\">\n  <mdc-avatar slot=\"prefix\" initials=\"AK\" size=\"24\"></mdc-avatar>\n</mdc-inputchip>"
      }
    ],
    "reactImports": [
      "InputChip"
    ],
    "constants": {
      "DEFAULTS": {
        "CLOSE_ICON": "'cancel-bold' as Extract<IconNames, 'cancel-bold'>"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "an",
      "and",
      "button",
      "chip",
      "close",
      "component",
      "for",
      "guidelines",
      "icon",
      "input",
      "inputchip",
      "interactive",
      "label",
      "leading",
      "mdc",
      "overview",
      "prefix",
      "representing",
      "text",
      "the",
      "tokenised",
      "usage",
      "value",
      "with"
    ]
  },
  {
    "name": "link",
    "title": "Link",
    "tagName": "mdc-link",
    "summary": "Usage, guidelines, and accessibility for the mdc-link component — a navigational hyperlink for in-app routing, external sites, or email/phone handlers, with optional trailing icon.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/link",
      "knowledgeBasePath": "components/src/components/link/knowledge-base/link.component.md"
    },
    "docs": "## Overview\n\nThe link renders a navigational hyperlink. The slotted children supply the link text; setting `icon-name` adds a trailing icon (commonly used for \"opens external site\" or \"download\" affordances). Three sizes are available (`small`, `midsize`, `large`), and the link supports inline / standalone display and an inverted colour scheme for dark backgrounds.\n\nLike a native `<a>`, the link supports `href`, `target`, `rel`, `download`, `ping`, `hreflang`, `type`, and `referrerpolicy`. When `disabled`, navigation is suppressed and the link is removed from the tab order.\n\n### When to use\n\n- Use `mdc-link` for navigation: routing within the application, opening an external URL, launching an email client (`mailto:`), or a phone dialler (`tel:`).\n- Use it when the destination is a URL — that is, when the user expectation is \"go somewhere\", not \"perform an action\".\n\n### When not to use\n\n- Use `mdc-button` (or `mdc-linkbutton` when a link's visual treatment is required) for in-page actions that do not change the URL.\n- Use `mdc-linksimple` when no default size, no trailing icon, or a custom child layout (e.g. an icon as the entire content) is required.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/link/index.js\"; // custom element registration\n// or via React wrapper\nimport { Link } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-link href=\"/dashboard\">Open dashboard</mdc-link>\n```\n\nExternal link with trailing icon and a new-tab target:\n\n```html\n<mdc-link\n  href=\"https://example.com\"\n  target=\"_blank\"\n  rel=\"noopener noreferrer\"\n  icon-name=\"pop-out-bold\"\n  size=\"midsize\"\n>\n  Open example.com\n</mdc-link>\n```\n\nInline link inside body text, on an inverted (dark) background:\n\n```html\n<p>\n  Read the\n  <mdc-link href=\"/changelog\" inline inverted>full changelog</mdc-link>\n  for details.\n</p>\n```\n\nListen for `click`, `keydown`, `focus`, and `blur` to react to user interaction (these are forwarded from the underlying anchor element).\n\n### Content guidance\n\n- Write link text that describes the destination (`\"View invoice #1234\"`), not the mechanic (`\"Click here\"`); the link text is the screen-reader-announced name.\n- When the target is external or opens in a new tab, add the trailing icon (`icon-name=\"pop-out-bold\"` or similar) so sighted users see the affordance, and include `rel=\"noopener noreferrer\"` with `target=\"_blank\"`.\n- Use `inline` when the link sits inside running prose; leave it unset when the link stands alone (a card action, a navigation item) so the standalone styling applies.\n\n### Property/Attribute details\n\n- `href` — the URL the link points to. When unset, the rendered `<a>` has no `href` (acts as a placeholder).\n- `target` — `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.\n- `rel` — space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.). Pair with `target=\"_blank\"`.\n- `download` — when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.\n- `ping` — space-separated list of URLs notified when the link is followed.\n- `hreflang`, `type`, `referrerpolicy` — standard anchor attributes passed through.\n- `size` — `small` (`0.75rem`), `midsize` (`0.875rem`), or `large` (`1rem`, default). Also drives the trailing icon's size.\n- `icon-name` — name of an icon rendered after the link text (`mdc-icon` name from the configured icon set). Default unset.\n- `inline` — when `true`, the link uses inline styling suitable for flow inside body text. Default `false`.\n- `inverted` — when `true`, swaps the colour palette for use on dark backgrounds. Default `false`.\n- `disabled` — when `true`, sets `aria-disabled=\"true\"` on the anchor, removes it from the tab order, and suppresses navigation on click or keyboard activation.\n- `data-aria-label` — accessible name applied to the rendered anchor. Use when the visible text alone does not describe the destination.\n\n## Accessibility\n\n### Built-in features\n\nThe link renders a native `<a>` element, so it inherits the platform's link semantics: announced as a link by screen readers, focusable by keyboard, activated with `Enter`, and routed through the browser's navigation flow. When `disabled` is `true`, the host sets `aria-disabled=\"true\"` on itself, sets `tabindex=\"-1\"` on the anchor (removing it from the tab order), and the click / keydown handler calls `preventDefault()` so activation does not navigate.\n\nThe anchor's accessible name comes from the slotted text content by default; when `data-aria-label` is set it overrides the slot text. The trailing icon (when `icon-name` is set) is rendered as a decorative `mdc-icon` so it does not add extra announcements.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute        | Value                                                       |\n| ------------- | ---------------- | ----------------------------------------------------------- |\n| Host          | `aria-disabled`  | `true` when `disabled`, otherwise removed                   |\n| Anchor `<a>`  | `aria-label`     | mirrors `data-aria-label`                                   |\n| Anchor `<a>`  | `tabindex`       | `-1` when `disabled`, otherwise `0`                         |\n| Anchor `<a>`  | `href`, `target`, `rel`, `download`, `ping`, `hreflang`, `type`, `referrerpolicy` | reflect the matching host attributes |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide descriptive link text so the link's accessible name conveys where it leads; supply `data-aria-label` only when the visible text would be ambiguous out of context.\n- For `target=\"_blank\"` links, the icon affordance is visual — the consumer can supply a `data-aria-label` such as `\"Open example.com (opens in new tab)\"` if the new-tab behaviour must be conveyed to screen readers.",
    "sections": {
      "Overview": "The link renders a navigational hyperlink. The slotted children supply the link text; setting `icon-name` adds a trailing icon (commonly used for \"opens external site\" or \"download\" affordances). Three sizes are available (`small`, `midsize`, `large`), and the link supports inline / standalone display and an inverted colour scheme for dark backgrounds.\n\nLike a native `<a>`, the link supports `href`, `target`, `rel`, `download`, `ping`, `hreflang`, `type`, and `referrerpolicy`. When `disabled`, navigation is suppressed and the link is removed from the tab order.\n\n### When to use\n\n- Use `mdc-link` for navigation: routing within the application, opening an external URL, launching an email client (`mailto:`), or a phone dialler (`tel:`).\n- Use it when the destination is a URL — that is, when the user expectation is \"go somewhere\", not \"perform an action\".\n\n### When not to use\n\n- Use `mdc-button` (or `mdc-linkbutton` when a link's visual treatment is required) for in-page actions that do not change the URL.\n- Use `mdc-linksimple` when no default size, no trailing icon, or a custom child layout (e.g. an icon as the entire content) is required.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/link/index.js\"; // custom element registration\n// or via React wrapper\nimport { Link } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-link href=\"/dashboard\">Open dashboard</mdc-link>\n```\n\nExternal link with trailing icon and a new-tab target:\n\n```html\n<mdc-link\n  href=\"https://example.com\"\n  target=\"_blank\"\n  rel=\"noopener noreferrer\"\n  icon-name=\"pop-out-bold\"\n  size=\"midsize\"\n>\n  Open example.com\n</mdc-link>\n```\n\nInline link inside body text, on an inverted (dark) background:\n\n```html\n<p>\n  Read the\n  <mdc-link href=\"/changelog\" inline inverted>full changelog</mdc-link>\n  for details.\n</p>\n```\n\nListen for `click`, `keydown`, `focus`, and `blur` to react to user interaction (these are forwarded from the underlying anchor element).\n\n### Content guidance\n\n- Write link text that describes the destination (`\"View invoice #1234\"`), not the mechanic (`\"Click here\"`); the link text is the screen-reader-announced name.\n- When the target is external or opens in a new tab, add the trailing icon (`icon-name=\"pop-out-bold\"` or similar) so sighted users see the affordance, and include `rel=\"noopener noreferrer\"` with `target=\"_blank\"`.\n- Use `inline` when the link sits inside running prose; leave it unset when the link stands alone (a card action, a navigation item) so the standalone styling applies.\n\n### Property/Attribute details\n\n- `href` — the URL the link points to. When unset, the rendered `<a>` has no `href` (acts as a placeholder).\n- `target` — `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.\n- `rel` — space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.). Pair with `target=\"_blank\"`.\n- `download` — when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.\n- `ping` — space-separated list of URLs notified when the link is followed.\n- `hreflang`, `type`, `referrerpolicy` — standard anchor attributes passed through.\n- `size` — `small` (`0.75rem`), `midsize` (`0.875rem`), or `large` (`1rem`, default). Also drives the trailing icon's size.\n- `icon-name` — name of an icon rendered after the link text (`mdc-icon` name from the configured icon set). Default unset.\n- `inline` — when `true`, the link uses inline styling suitable for flow inside body text. Default `false`.\n- `inverted` — when `true`, swaps the colour palette for use on dark backgrounds. Default `false`.\n- `disabled` — when `true`, sets `aria-disabled=\"true\"` on the anchor, removes it from the tab order, and suppresses navigation on click or keyboard activation.\n- `data-aria-label` — accessible name applied to the rendered anchor. Use when the visible text alone does not describe the destination.",
      "Accessibility": "### Built-in features\n\nThe link renders a native `<a>` element, so it inherits the platform's link semantics: announced as a link by screen readers, focusable by keyboard, activated with `Enter`, and routed through the browser's navigation flow. When `disabled` is `true`, the host sets `aria-disabled=\"true\"` on itself, sets `tabindex=\"-1\"` on the anchor (removing it from the tab order), and the click / keydown handler calls `preventDefault()` so activation does not navigate.\n\nThe anchor's accessible name comes from the slotted text content by default; when `data-aria-label` is set it overrides the slot text. The trailing icon (when `icon-name` is set) is rendered as a decorative `mdc-icon` so it does not add extra announcements.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute        | Value                                                       |\n| ------------- | ---------------- | ----------------------------------------------------------- |\n| Host          | `aria-disabled`  | `true` when `disabled`, otherwise removed                   |\n| Anchor `<a>`  | `aria-label`     | mirrors `data-aria-label`                                   |\n| Anchor `<a>`  | `tabindex`       | `-1` when `disabled`, otherwise `0`                         |\n| Anchor `<a>`  | `href`, `target`, `rel`, `download`, `ping`, `hreflang`, `type`, `referrerpolicy` | reflect the matching host attributes |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide descriptive link text so the link's accessible name conveys where it leads; supply `data-aria-label` only when the visible text would be ambiguous out of context.\n- For `target=\"_blank\"` links, the icon affordance is visual — the consumer can supply a `data-aria-label` such as `\"Open example.com (opens in new tab)\"` if the new-tab behaviour must be conveyed to screen readers."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Text content to be displayed.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the rendered anchor. Use when the visible text alone does not describe the destination.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, sets `aria-disabled=\"true\"` on the anchor, removes it from the tab order, and suppresses navigation on click or keyboard activation.",
          "source": "storybook"
        },
        {
          "name": "href",
          "attribute": "href",
          "type": "text",
          "values": [],
          "description": "the URL the link points to. When unset, the rendered `<a>` has no `href` (acts as a placeholder).",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "text",
          "values": [],
          "description": "name of an icon rendered after the link text (`mdc-icon` name from the configured icon set). Default unset.",
          "source": "storybook"
        },
        {
          "name": "inline",
          "attribute": "inline",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the link uses inline styling suitable for flow inside body text. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "inverted",
          "attribute": "inverted",
          "type": "boolean",
          "values": [],
          "description": "when `true`, swaps the colour palette for use on dark backgrounds. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "rel",
          "attribute": "rel",
          "type": "text",
          "values": [],
          "description": "space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.). Pair with `target=\"_blank\"`.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "large",
          "values": [
            "large",
            "midsize",
            "small"
          ],
          "description": "`small` (`0.75rem`), `midsize` (`0.875rem`), or `large` (`1rem`, default). Also drives the trailing icon's size.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "target",
          "attribute": "target",
          "type": "text",
          "values": [],
          "description": "`_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Fired when the user activates the Link using a mouse or assistive technology."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) Fired when the user presses a key while the Link has focus."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Fired when the Link receives keyboard or mouse focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) Fired when the Link loses keyboard or mouse focus."
        }
      ],
      "cssParts": [
        {
          "name": "anchor",
          "description": "The anchor element that wraps the link content."
        },
        {
          "name": "text",
          "description": "The element that wraps the link text."
        },
        {
          "name": "icon",
          "description": "The icon element."
        }
      ],
      "cssProperties": [],
      "rules": [
        "the URL the link points to. When unset, the rendered `<a>` has no `href` (acts as a placeholder).",
        "`_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.",
        "space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.). Pair with `target=\"_blank\"`.",
        "when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.",
        "space-separated list of URLs notified when the link is followed.",
        "`small` (`0.75rem`), `midsize` (`0.875rem`), or `large` (`1rem`, default). Also drives the trailing icon's size.",
        "name of an icon rendered after the link text (`mdc-icon` name from the configured icon set). Default unset.",
        "when `true`, the link uses inline styling suitable for flow inside body text. Default `false`.",
        "when `true`, swaps the colour palette for use on dark backgrounds. Default `false`.",
        "when `true`, sets `aria-disabled=\"true\"` on the anchor, removes it from the tab order, and suppresses navigation on click or keyboard activation.",
        "accessible name applied to the rendered anchor. Use when the visible text alone does not describe the destination."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/link/index.js\"; // custom element registration\n// or via React wrapper\nimport { Link } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-link href=\"/dashboard\">Open dashboard</mdc-link>"
      },
      {
        "language": "html",
        "code": "<mdc-link\n  href=\"https://example.com\"\n  target=\"_blank\"\n  rel=\"noopener noreferrer\"\n  icon-name=\"pop-out-bold\"\n  size=\"midsize\"\n>\n  Open example.com\n</mdc-link>"
      },
      {
        "language": "html",
        "code": "<p>\n  Read the\n  <mdc-link href=\"/changelog\" inline inverted>full changelog</mdc-link>\n  for details.\n</p>"
      }
    ],
    "reactImports": [
      "Link"
    ],
    "constants": {
      "LINK_SIZES": {
        "LARGE": "large",
        "MIDSIZE": "midsize",
        "SMALL": "small"
      },
      "LINK_ICON_SIZES": {
        "LARGE": 1,
        "MIDSIZE": 0.875,
        "SMALL": 0.75
      },
      "DEFAULTS": {
        "LINK_SIZE": "large"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "app",
      "component",
      "email",
      "external",
      "for",
      "guidelines",
      "handlers",
      "hyperlink",
      "icon",
      "in",
      "link",
      "mdc",
      "navigational",
      "optional",
      "or",
      "overview",
      "phone",
      "routing",
      "sites",
      "the",
      "trailing",
      "usage",
      "with"
    ]
  },
  {
    "name": "linkbutton",
    "title": "Linkbutton",
    "tagName": "mdc-linkbutton",
    "summary": "Usage, guidelines, and accessibility for the mdc-linkbutton component — a control that looks like a hyperlink but behaves as a button, used for in-page actions that should visually read as a link.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/linkbutton",
      "knowledgeBasePath": "components/src/components/linkbutton/knowledge-base/linkbutton.component.md"
    },
    "docs": "## Overview\n\nThe linkbutton renders as a link visually but behaves as a button: activating it fires a `click` event rather than navigating to a URL. Use it for in-page actions (open a panel, copy a value, trigger a workflow) that should sit in body text or alongside other links without standing out as a button.\n\nThe slotted children supply the label; an optional trailing icon can be added via `icon-name`. Three text sizes are supported, plus inline / standalone display and an inverted colour scheme for dark backgrounds.\n\n### When to use\n\n- Use `mdc-linkbutton` for in-page actions that should visually mimic a hyperlink — for example a \"Show more\" toggle inside a paragraph, an undo prompt after a toast, or an action embedded in a sentence.\n- Use it when the surrounding design treats the affordance as link-styled text but the underlying behaviour is a button (no URL navigation).\n\n### When not to use\n\n- Use `mdc-link` when the affordance actually navigates to a URL — even if the URL is in-app, the user expectation is \"go somewhere\".\n- Use `mdc-button` when the surrounding design expects a button-shaped control.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/linkbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { LinkButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (inline action inside a sentence):\n\n```html\n<p>\n  Forgot your password?\n  <mdc-linkbutton inline @click=${openResetFlow}>Reset it</mdc-linkbutton>.\n</p>\n```\n\nStandalone link-button with a trailing icon:\n\n```html\n<mdc-linkbutton size=\"14\" icon-name=\"arrow-right-bold\" @click=${showMore}>\n  Show more details\n</mdc-linkbutton>\n```\n\nListen for `click` to react to user activation; `keydown` / `keyup` / `focus` / `blur` are also dispatched for fine-grained interaction handling.\n\n### Content guidance\n\n- Write text that describes the action the linkbutton performs (`\"Show more details\"`), not the visual mechanic (`\"Click here\"`).\n- Use `inline` when the linkbutton sits inside running prose so it picks up the inline-link styling; leave it unset when the linkbutton stands alone.\n- Pair the trailing icon (`icon-name`) with a directional or action-confirming glyph (arrow, external pop-out) only when the icon adds meaning — gratuitous icons add visual noise without value.\n\n### Property/Attribute details\n\n- `size` — text and icon size: `12` (`0.75rem`), `14` (`0.875rem`), or `16` (`1rem`, default).\n- `inline` — when `true`, applies inline-link styling suitable for body text. Default `false`.\n- `inverted` — when `true`, swaps the colour palette for dark backgrounds. Default `false`.\n- `icon-name` — name of an icon rendered after the label (`mdc-icon` name from the configured icon set). Default unset.\n\nProperties inherited from the underlying button:\n\n- `disabled` — when `true`, removes the linkbutton from the tab order and prevents activation.\n- `soft-disabled` — when `true`, the linkbutton looks disabled but remains focusable so assistive technology can read it; consumers must still suppress action handling.\n- `auto-focus-on-mount` — when `true`, focuses the linkbutton on first render.\n- `data-aria-label` — accessible name override; use when the visible text alone does not describe the action.\n\n## Accessibility\n\n### Built-in features\n\nThe linkbutton exposes itself with `role=\"link\"` (overriding the default button role) so screen readers announce it as a link, matching its visual treatment. It is focusable by keyboard and activates on `Enter` and `Space` (the standard button activation pattern); activation dispatches a `click` event that consumers handle to perform the in-page action. When `disabled` is `true`, the linkbutton is removed from the tab order and activation is suppressed.\n\nThe label is taken from the slotted children; the trailing icon (when `icon-name` is set) is rendered as a decorative `mdc-icon` and is not announced separately.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                              |\n| ------- | --------------- | ------------------------------------------------------------------ |\n| Host    | `role`          | `link`                                                             |\n| Host    | `aria-label`    | mirrors `data-aria-label` when set                                 |\n| Host    | `tabindex`      | `-1` when `disabled`, otherwise `0`                                |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide descriptive label text so the accessible name conveys the action being taken; use `data-aria-label` only when the visible text would be ambiguous on its own.\n\n### Notes\n\n- Because the linkbutton announces as `role=\"link\"` but does not navigate to a URL, the destination announced to screen readers is implicit. Make sure the label text alone makes the resulting action clear (e.g. `\"Show more details\"`, not `\"More\"`).",
    "sections": {
      "Overview": "The linkbutton renders as a link visually but behaves as a button: activating it fires a `click` event rather than navigating to a URL. Use it for in-page actions (open a panel, copy a value, trigger a workflow) that should sit in body text or alongside other links without standing out as a button.\n\nThe slotted children supply the label; an optional trailing icon can be added via `icon-name`. Three text sizes are supported, plus inline / standalone display and an inverted colour scheme for dark backgrounds.\n\n### When to use\n\n- Use `mdc-linkbutton` for in-page actions that should visually mimic a hyperlink — for example a \"Show more\" toggle inside a paragraph, an undo prompt after a toast, or an action embedded in a sentence.\n- Use it when the surrounding design treats the affordance as link-styled text but the underlying behaviour is a button (no URL navigation).\n\n### When not to use\n\n- Use `mdc-link` when the affordance actually navigates to a URL — even if the URL is in-app, the user expectation is \"go somewhere\".\n- Use `mdc-button` when the surrounding design expects a button-shaped control.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/linkbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { LinkButton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (inline action inside a sentence):\n\n```html\n<p>\n  Forgot your password?\n  <mdc-linkbutton inline @click=${openResetFlow}>Reset it</mdc-linkbutton>.\n</p>\n```\n\nStandalone link-button with a trailing icon:\n\n```html\n<mdc-linkbutton size=\"14\" icon-name=\"arrow-right-bold\" @click=${showMore}>\n  Show more details\n</mdc-linkbutton>\n```\n\nListen for `click` to react to user activation; `keydown` / `keyup` / `focus` / `blur` are also dispatched for fine-grained interaction handling.\n\n### Content guidance\n\n- Write text that describes the action the linkbutton performs (`\"Show more details\"`), not the visual mechanic (`\"Click here\"`).\n- Use `inline` when the linkbutton sits inside running prose so it picks up the inline-link styling; leave it unset when the linkbutton stands alone.\n- Pair the trailing icon (`icon-name`) with a directional or action-confirming glyph (arrow, external pop-out) only when the icon adds meaning — gratuitous icons add visual noise without value.\n\n### Property/Attribute details\n\n- `size` — text and icon size: `12` (`0.75rem`), `14` (`0.875rem`), or `16` (`1rem`, default).\n- `inline` — when `true`, applies inline-link styling suitable for body text. Default `false`.\n- `inverted` — when `true`, swaps the colour palette for dark backgrounds. Default `false`.\n- `icon-name` — name of an icon rendered after the label (`mdc-icon` name from the configured icon set). Default unset.\n\nProperties inherited from the underlying button:\n\n- `disabled` — when `true`, removes the linkbutton from the tab order and prevents activation.\n- `soft-disabled` — when `true`, the linkbutton looks disabled but remains focusable so assistive technology can read it; consumers must still suppress action handling.\n- `auto-focus-on-mount` — when `true`, focuses the linkbutton on first render.\n- `data-aria-label` — accessible name override; use when the visible text alone does not describe the action.",
      "Accessibility": "### Built-in features\n\nThe linkbutton exposes itself with `role=\"link\"` (overriding the default button role) so screen readers announce it as a link, matching its visual treatment. It is focusable by keyboard and activates on `Enter` and `Space` (the standard button activation pattern); activation dispatches a `click` event that consumers handle to perform the in-page action. When `disabled` is `true`, the linkbutton is removed from the tab order and activation is suppressed.\n\nThe label is taken from the slotted children; the trailing icon (when `icon-name` is set) is rendered as a decorative `mdc-icon` and is not announced separately.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute       | Value                                                              |\n| ------- | --------------- | ------------------------------------------------------------------ |\n| Host    | `role`          | `link`                                                             |\n| Host    | `aria-label`    | mirrors `data-aria-label` when set                                 |\n| Host    | `tabindex`      | `-1` when `disabled`, otherwise `0`                                |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide descriptive label text so the accessible name conveys the action being taken; use `data-aria-label` only when the visible text would be ambiguous on its own.\n\n### Notes\n\n- Because the linkbutton announces as `role=\"link\"` but does not navigate to a URL, the destination announced to screen readers is implicit. Make sure the label text alone makes the resulting action clear (e.g. `\"Show more details\"`, not `\"More\"`)."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "description": "Defines an accessible label for the linkbutton for screen readers.",
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, focuses the linkbutton on first render.",
          "source": "storybook"
        },
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, removes the linkbutton from the tab order and prevents activation.",
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "name of an icon rendered after the label (`mdc-icon` name from the configured icon set). Default unset.",
          "source": "storybook"
        },
        {
          "name": "inline",
          "attribute": "inline",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, applies inline-link styling suitable for body text. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "inverted",
          "attribute": "inverted",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, swaps the colour palette for dark backgrounds. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "16",
          "values": [
            12,
            14,
            16
          ],
          "description": "text and icon size: `12` (`0.75rem`), `14` (`0.875rem`), or `16` (`1rem`, default).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the linkbutton looks disabled but remains focusable so assistive technology can read it; consumers must still suppress action handling.",
          "source": "storybook"
        },
        {
          "name": "tabIndex",
          "attribute": "tab-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Text label of the linkbutton."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the linkbutton is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the linkbutton."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the linkbutton."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the linkbutton receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the linkbutton loses focus."
        }
      ],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-link-border-radius",
          "description": "Border radius of the linkbutton."
        },
        {
          "name": "--mdc-link-color-active",
          "description": "Color of the linkbutton’s child content in the active state."
        },
        {
          "name": "--mdc-link-color-disabled",
          "description": "Color of the linkbutton’s child content in the disabled state."
        },
        {
          "name": "--mdc-link-color-hover",
          "description": "Color of the linkbutton’s child content in the hover state."
        },
        {
          "name": "--mdc-link-color-normal",
          "description": "Color of the linkbutton’s child content in the normal state."
        },
        {
          "name": "--mdc-link-inverted-color-active",
          "description": "Color of the inverted linkbutton’s child content in the active state."
        },
        {
          "name": "--mdc-link-inverted-color-disabled",
          "description": "Color of the inverted linkbutton’s child content in the disabled state."
        },
        {
          "name": "--mdc-link-inverted-color-hover",
          "description": "Color of the inverted linkbutton’s child content in the hover state."
        },
        {
          "name": "--mdc-link-inverted-color-normal",
          "description": "Color of the inverted linkbutton’s child content in the normal state."
        },
        {
          "name": "--mdc-button-height",
          "description": "Height for button size"
        }
      ],
      "rules": [
        "text and icon size: `12` (`0.75rem`), `14` (`0.875rem`), or `16` (`1rem`, default).",
        "when `true`, applies inline-link styling suitable for body text. Default `false`.",
        "when `true`, swaps the colour palette for dark backgrounds. Default `false`.",
        "name of an icon rendered after the label (`mdc-icon` name from the configured icon set). Default unset.",
        "when `true`, removes the linkbutton from the tab order and prevents activation.",
        "when `true`, the linkbutton looks disabled but remains focusable so assistive technology can read it; consumers must still suppress action handling.",
        "when `true`, focuses the linkbutton on first render.",
        "accessible name override; use when the visible text alone does not describe the action."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/linkbutton/index.js\"; // custom element registration\n// or via React wrapper\nimport { LinkButton } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<p>\n  Forgot your password?\n  <mdc-linkbutton inline @click=${openResetFlow}>Reset it</mdc-linkbutton>.\n</p>"
      },
      {
        "language": "html",
        "code": "<mdc-linkbutton size=\"14\" icon-name=\"arrow-right-bold\" @click=${showMore}>\n  Show more details\n</mdc-linkbutton>"
      }
    ],
    "reactImports": [
      "LinkButton"
    ],
    "constants": {
      "LINKBUTTON_SIZES": {
        "12": 12,
        "14": 14,
        "16": 16
      },
      "DEFAULTS": {
        "SIZE": 16
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "actions",
      "and",
      "as",
      "behaves",
      "but",
      "button",
      "component",
      "control",
      "for",
      "guidelines",
      "hyperlink",
      "icon",
      "in",
      "like",
      "link",
      "linkbutton",
      "looks",
      "mdc",
      "overview",
      "page",
      "read",
      "should",
      "that",
      "the",
      "usage",
      "used",
      "visually"
    ]
  },
  {
    "name": "linksimple",
    "title": "Linksimple",
    "tagName": "mdc-linksimple",
    "summary": "Usage, guidelines, and accessibility for the mdc-linksimple component — a lightweight, unstyled-by-default hyperlink with no built-in size or icon, suitable for custom child content.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/linksimple",
      "knowledgeBasePath": "components/src/components/linksimple/knowledge-base/linksimple.component.md"
    },
    "docs": "## Overview\n\nThe linksimple is a minimal hyperlink: it renders an `<a>`, forwards the standard anchor attributes, and lets the slotted children supply whatever content the consumer needs (text, an icon, a mix of inline elements). It does not impose a default size or a built-in trailing icon — those are the job of `mdc-link`.\n\nUse linksimple when the visual treatment of `mdc-link` does not fit (icon-only navigation, custom layouts inside the link, embedding in a tight design) but the link's accessibility and interaction contract is still needed.\n\n### When to use\n\n- Use `mdc-linksimple` when you need a hyperlink but the slotted content is not plain text (an icon-only link, a link wrapping a small layout) or the default sizing of `mdc-link` would interfere with the surrounding design.\n- Use it when the link should sit unstyled in a tight context and the consumer's own CSS will handle the typography.\n\n### When not to use\n\n- Use `mdc-link` when the link is plain text with a clearly defined size and an optional trailing icon — `mdc-link` already wraps `mdc-linksimple` with the standard text + icon treatment.\n- Use `mdc-button` or `mdc-linkbutton` for in-page actions that do not navigate to a URL.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/linksimple/index.js\"; // custom element registration\n```\n\nMinimal markup example (icon-only link):\n\n```html\n<mdc-linksimple href=\"/profile\" data-aria-label=\"Open profile\">\n  <mdc-icon name=\"user-bold\" size=\"1.25\" length-unit=\"rem\"></mdc-icon>\n</mdc-linksimple>\n```\n\nPlain-text link with no default size applied:\n\n```html\n<mdc-linksimple href=\"https://example.com\" target=\"_blank\" rel=\"noopener noreferrer\">\n  example.com\n</mdc-linksimple>\n```\n\nListen for `click`, `keydown`, `focus`, and `blur` to react to user interaction.\n\n### Content guidance\n\n- When the slotted content is non-textual (an icon, an image), always provide `data-aria-label` so the link has an accessible name.\n- The component intentionally has no `size` — let the parent context's typography apply, or wrap with `mdc-link` if a defined size is needed.\n\n### Property/Attribute details\n\n- `href` — the URL the link points to. When unset, the rendered anchor has no `href`.\n- `target` — `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.\n- `rel` — space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.).\n- `download` — when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.\n- `ping` — space-separated list of URLs notified when the link is followed.\n- `hreflang`, `type`, `referrerpolicy` — standard anchor attributes passed through.\n- `inline` — when `true`, applies inline-link styling. Default `false`.\n- `inverted` — when `true`, swaps the colour palette for dark backgrounds. Default `false`.\n- `disabled` — when `true`, sets `aria-disabled=\"true\"` on the host, removes the anchor from the tab order, and suppresses navigation on click or keyboard activation.\n- `data-aria-label` — accessible name applied to the rendered anchor. Required when the slotted content does not include readable text.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders a native `<a>` element, so it inherits the platform's link semantics: announced as a link by screen readers, focusable by keyboard, and activated by `Enter`. When `disabled` is `true`, the host sets `aria-disabled=\"true\"`, sets `tabindex=\"-1\"` on the anchor, and suppresses click and keydown propagation so activation does not navigate.\n\nThe anchor's accessible name comes from the slotted text by default; `data-aria-label`, when set, overrides it.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute        | Value                                                       |\n| ------------- | ---------------- | ----------------------------------------------------------- |\n| Host          | `aria-disabled`  | `true` when `disabled`, otherwise removed                   |\n| Anchor `<a>`  | `aria-label`     | mirrors `data-aria-label`                                   |\n| Anchor `<a>`  | `tabindex`       | `-1` when `disabled`, otherwise `0`                         |\n| Anchor `<a>`  | `href`, `target`, `rel`, `download`, `ping`, `hreflang`, `type`, `referrerpolicy` | reflect the matching host attributes |\n\n### Implementation requirements\n\n#### Labelling\n\n- When the slotted content is non-textual (an icon, an image), always provide `data-aria-label` — otherwise the link has no accessible name.\n- For `target=\"_blank\"` links, supply a `data-aria-label` (e.g. `\"Open example.com (opens in new tab)\"`) when the new-tab behaviour must be conveyed to screen readers.",
    "sections": {
      "Overview": "The linksimple is a minimal hyperlink: it renders an `<a>`, forwards the standard anchor attributes, and lets the slotted children supply whatever content the consumer needs (text, an icon, a mix of inline elements). It does not impose a default size or a built-in trailing icon — those are the job of `mdc-link`.\n\nUse linksimple when the visual treatment of `mdc-link` does not fit (icon-only navigation, custom layouts inside the link, embedding in a tight design) but the link's accessibility and interaction contract is still needed.\n\n### When to use\n\n- Use `mdc-linksimple` when you need a hyperlink but the slotted content is not plain text (an icon-only link, a link wrapping a small layout) or the default sizing of `mdc-link` would interfere with the surrounding design.\n- Use it when the link should sit unstyled in a tight context and the consumer's own CSS will handle the typography.\n\n### When not to use\n\n- Use `mdc-link` when the link is plain text with a clearly defined size and an optional trailing icon — `mdc-link` already wraps `mdc-linksimple` with the standard text + icon treatment.\n- Use `mdc-button` or `mdc-linkbutton` for in-page actions that do not navigate to a URL.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/linksimple/index.js\"; // custom element registration\n```\n\nMinimal markup example (icon-only link):\n\n```html\n<mdc-linksimple href=\"/profile\" data-aria-label=\"Open profile\">\n  <mdc-icon name=\"user-bold\" size=\"1.25\" length-unit=\"rem\"></mdc-icon>\n</mdc-linksimple>\n```\n\nPlain-text link with no default size applied:\n\n```html\n<mdc-linksimple href=\"https://example.com\" target=\"_blank\" rel=\"noopener noreferrer\">\n  example.com\n</mdc-linksimple>\n```\n\nListen for `click`, `keydown`, `focus`, and `blur` to react to user interaction.\n\n### Content guidance\n\n- When the slotted content is non-textual (an icon, an image), always provide `data-aria-label` so the link has an accessible name.\n- The component intentionally has no `size` — let the parent context's typography apply, or wrap with `mdc-link` if a defined size is needed.\n\n### Property/Attribute details\n\n- `href` — the URL the link points to. When unset, the rendered anchor has no `href`.\n- `target` — `_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.\n- `rel` — space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.).\n- `download` — when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.\n- `ping` — space-separated list of URLs notified when the link is followed.\n- `hreflang`, `type`, `referrerpolicy` — standard anchor attributes passed through.\n- `inline` — when `true`, applies inline-link styling. Default `false`.\n- `inverted` — when `true`, swaps the colour palette for dark backgrounds. Default `false`.\n- `disabled` — when `true`, sets `aria-disabled=\"true\"` on the host, removes the anchor from the tab order, and suppresses navigation on click or keyboard activation.\n- `data-aria-label` — accessible name applied to the rendered anchor. Required when the slotted content does not include readable text.",
      "Accessibility": "### Built-in features\n\nThe component renders a native `<a>` element, so it inherits the platform's link semantics: announced as a link by screen readers, focusable by keyboard, and activated by `Enter`. When `disabled` is `true`, the host sets `aria-disabled=\"true\"`, sets `tabindex=\"-1\"` on the anchor, and suppresses click and keydown propagation so activation does not navigate.\n\nThe anchor's accessible name comes from the slotted text by default; `data-aria-label`, when set, overrides it.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute        | Value                                                       |\n| ------------- | ---------------- | ----------------------------------------------------------- |\n| Host          | `aria-disabled`  | `true` when `disabled`, otherwise removed                   |\n| Anchor `<a>`  | `aria-label`     | mirrors `data-aria-label`                                   |\n| Anchor `<a>`  | `tabindex`       | `-1` when `disabled`, otherwise `0`                         |\n| Anchor `<a>`  | `href`, `target`, `rel`, `download`, `ping`, `hreflang`, `type`, `referrerpolicy` | reflect the matching host attributes |\n\n### Implementation requirements\n\n#### Labelling\n\n- When the slotted content is non-textual (an icon, an image), always provide `data-aria-label` — otherwise the link has no accessible name.\n- For `target=\"_blank\"` links, supply a `data-aria-label` (e.g. `\"Open example.com (opens in new tab)\"`) when the new-tab behaviour must be conveyed to screen readers."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Text content to be displayed.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the rendered anchor. Required when the slotted content does not include readable text.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, sets `aria-disabled=\"true\"` on the host, removes the anchor from the tab order, and suppresses navigation on click or keyboard activation.",
          "source": "storybook"
        },
        {
          "name": "download",
          "attribute": "download",
          "type": "text",
          "values": [],
          "description": "when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.",
          "source": "storybook"
        },
        {
          "name": "href",
          "attribute": "href",
          "type": "text",
          "values": [],
          "description": "the URL the link points to. When unset, the rendered anchor has no `href`.",
          "source": "storybook"
        },
        {
          "name": "hreflang",
          "attribute": "hreflang",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "inline",
          "attribute": "inline",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, applies inline-link styling. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "inverted",
          "attribute": "inverted",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, swaps the colour palette for dark backgrounds. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "ping",
          "attribute": "ping",
          "type": "text",
          "values": [],
          "description": "space-separated list of URLs notified when the link is followed.",
          "source": "storybook"
        },
        {
          "name": "referrerpolicy",
          "attribute": "referrerpolicy",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "rel",
          "attribute": "rel",
          "type": "text",
          "values": [],
          "description": "space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.).",
          "source": "storybook"
        },
        {
          "name": "target",
          "attribute": "target",
          "type": "text",
          "values": [],
          "description": "`_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.",
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) Fired when the user activates the buttonLink using a mouse or assistive technology."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) Fired when the user presses a key while the buttonLink has focus."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Fired when the buttonLink receives keyboard or mouse focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) Fired when the buttonLink loses keyboard or mouse focus."
        }
      ],
      "cssParts": [
        {
          "name": "anchor",
          "description": "The anchor element that wraps the linksimple content."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-link-border-radius",
          "description": "Border radius of the link."
        },
        {
          "name": "--mdc-link-color-active",
          "description": "Color of the link’s child content in the active state."
        },
        {
          "name": "--mdc-link-color-disabled",
          "description": "Color of the link’s child content in the disabled state."
        },
        {
          "name": "--mdc-link-color-hover",
          "description": "Color of the link’s child content in the hover state."
        },
        {
          "name": "--mdc-link-color-normal",
          "description": "Color of the link’s child content in the normal state."
        },
        {
          "name": "--mdc-link-inverted-color-active",
          "description": "Color of the inverted link’s child content in the active state."
        },
        {
          "name": "--mdc-link-inverted-color-disabled",
          "description": "Color of the inverted link’s child content in the disabled state."
        },
        {
          "name": "--mdc-link-inverted-color-hover",
          "description": "Color of the inverted link’s child content in the hover state."
        },
        {
          "name": "--mdc-link-inverted-color-normal",
          "description": "Color of the inverted link’s child content in the normal state."
        }
      ],
      "rules": [
        "the URL the link points to. When unset, the rendered anchor has no `href`.",
        "`_self` (default), `_blank`, `_parent`, `_top`, or `_unfencedTop`.",
        "space-separated link types (`noopener`, `noreferrer`, `nofollow`, etc.).",
        "when set, the browser downloads the resource. Empty string lets the browser pick the filename; a string overrides it.",
        "space-separated list of URLs notified when the link is followed.",
        "when `true`, applies inline-link styling. Default `false`.",
        "when `true`, swaps the colour palette for dark backgrounds. Default `false`.",
        "when `true`, sets `aria-disabled=\"true\"` on the host, removes the anchor from the tab order, and suppresses navigation on click or keyboard activation.",
        "accessible name applied to the rendered anchor. Required when the slotted content does not include readable text."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/linksimple/index.js\"; // custom element registration"
      },
      {
        "language": "html",
        "code": "<mdc-linksimple href=\"/profile\" data-aria-label=\"Open profile\">\n  <mdc-icon name=\"user-bold\" size=\"1.25\" length-unit=\"rem\"></mdc-icon>\n</mdc-linksimple>"
      },
      {
        "language": "html",
        "code": "<mdc-linksimple href=\"https://example.com\" target=\"_blank\" rel=\"noopener noreferrer\">\n  example.com\n</mdc-linksimple>"
      }
    ],
    "reactImports": [
      "Linksimple"
    ],
    "constants": {
      "DEFAULTS": {
        "INLINE": false,
        "INVERTED": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "built",
      "by",
      "child",
      "component",
      "content",
      "custom",
      "default",
      "for",
      "guidelines",
      "hyperlink",
      "icon",
      "in",
      "lightweight",
      "linksimple",
      "mdc",
      "no",
      "or",
      "overview",
      "size",
      "suitable",
      "the",
      "unstyled",
      "usage",
      "with"
    ]
  },
  {
    "name": "list",
    "title": "List",
    "tagName": "mdc-list",
    "summary": "Usage, guidelines, and accessibility for the mdc-list component — a keyboard-navigable container grouping mdc-listitem elements with an optional header slot and configurable orientation.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/list",
      "knowledgeBasePath": "components/src/components/list/knowledge-base/list.component.md"
    },
    "docs": "## Overview\n\nThe list is a container that groups `mdc-listitem` children and provides shared keyboard navigation across them. Listitems are placed in the default slot; an optional `mdc-listheader` can be placed in the `list-header` slot. The list manages roving tabindex across items so the keyboard user only enters the list once, then moves between items with arrow keys.\n\nThe list's orientation (`vertical` or `horizontal`) selects whether arrow Up/Down or Left/Right navigate; looping (`loop=\"true\"` / `loop=\"false\"`) chooses whether navigation wraps at the ends; `initial-focus` selects which item receives focus when the list is entered for the first time.\n\n### When to use\n\n- Use `mdc-list` to display a related group of `mdc-listitem` elements (selection lists, navigation lists, settings lists) that should share keyboard navigation.\n- Use it whenever a list of two or more listitems is rendered — even a single listitem should be wrapped in `mdc-list` for screen-reader semantics.\n\n### When not to use\n\n- Use `mdc-listbox` for a list of selectable options that participate in single- or multi-select selection semantics (the listbox uses `role=\"listbox\"` and `mdc-option` children rather than `mdc-listitem`).\n- Use `mdc-menubar` / `mdc-menupopover` for application menus.\n- Use `mdc-virtualizedlist` for very large lists where DOM virtualisation is needed.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/list/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, ListItem, Listheader } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-list>\n  <mdc-listheader slot=\"list-header\" header-text=\"Recent files\"></mdc-listheader>\n  <mdc-listitem label=\"report.pdf\"></mdc-listitem>\n  <mdc-listitem label=\"notes.md\"></mdc-listitem>\n  <mdc-listitem label=\"design.fig\"></mdc-listitem>\n</mdc-list>\n```\n\nHorizontal list without wrap-around navigation:\n\n```html\n<mdc-list orientation=\"horizontal\" loop=\"false\">\n  <mdc-listitem label=\"Tab 1\"></mdc-listitem>\n  <mdc-listitem label=\"Tab 2\"></mdc-listitem>\n  <mdc-listitem label=\"Tab 3\"></mdc-listitem>\n</mdc-list>\n```\n\n### Content guidance\n\n- Place `mdc-listitem` children in the default slot; use the `list-header` slot for `mdc-listheader`.\n- Use the `vertical` orientation for column-style lists (the default) and `horizontal` only when listitems flow side-by-side and the surrounding layout truly reads left-to-right.\n- Set `loop=\"false\"` when the ends of the list matter to the user (e.g. when item position implies progress); leave the default `loop=\"true\"` for menus / navigation lists where wrapping feels natural.\n\n### Property/Attribute details\n\n- `loop` — `\"true\"` (default) wraps navigation at both ends; `\"false\"` stops at the first / last item.\n- `initial-focus` — zero-based index of the item that receives focus the first time the list is entered. Out-of-bounds values clamp to the nearest valid index. Default `0`.\n- `orientation` — `vertical` (default; Up/Down arrows navigate) or `horizontal` (Left/Right arrows navigate). Also drives the Flexbox direction.\n\n### Limitations\n\n- The list only manages listitems that match `mdc-listitem:not([disabled])`. Items with the `disabled` attribute are skipped by keyboard navigation but remain in the DOM.\n- The list does not own selection state — selection is a property of the individual listitem (or of the surrounding pattern such as a checkbox group). The list's job is grouping and navigation, not single-/multi-select semantics.\n\n## Accessibility\n\n### Built-in features\n\nThe host exposes itself with `role=\"list\"` so screen readers announce the group. Keyboard navigation is managed by the list: pressing the arrow key matching the orientation moves focus between listitems with a roving tabindex (only the focused item has `tabindex=\"0\"`, the others have `tabindex=\"-1\"`). `Home` and `End` jump to the first / last item; `Space` is intercepted at the list level when it targets a listitem so the page does not scroll. When `loop=\"true\"`, navigation wraps at both ends; when `loop=\"false\"`, it stops at the first / last item.\n\nWhen a listitem is added or removed (via the element-store life-cycle hooks), the list re-computes the focusable item — the first item gets `tabindex=\"0\"` on initial population, and removing the currently focused item moves focus to the next valid sibling.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute     | Value                                                       |\n| ------------- | ------------- | ----------------------------------------------------------- |\n| Host          | `role`        | `list`                                                      |\n| Container slot| `role`        | `presentation` (so the wrapper does not break the a11y tree)|\n| Each listitem | `tabindex`    | `0` for the currently focused item, `-1` for the rest       |\n\n### Implementation requirements\n\n#### General\n\n- Always wrap `mdc-listitem` children in `mdc-list`, even when there is only one item, so screen readers announce the list semantics.\n- Use `mdc-listheader` (in the `list-header` slot) for any visible heading that introduces the list, instead of rendering plain text above it.\n- Match `orientation` to the visual flow of the list; mismatched orientation makes arrow-key navigation feel broken.",
    "sections": {
      "Overview": "The list is a container that groups `mdc-listitem` children and provides shared keyboard navigation across them. Listitems are placed in the default slot; an optional `mdc-listheader` can be placed in the `list-header` slot. The list manages roving tabindex across items so the keyboard user only enters the list once, then moves between items with arrow keys.\n\nThe list's orientation (`vertical` or `horizontal`) selects whether arrow Up/Down or Left/Right navigate; looping (`loop=\"true\"` / `loop=\"false\"`) chooses whether navigation wraps at the ends; `initial-focus` selects which item receives focus when the list is entered for the first time.\n\n### When to use\n\n- Use `mdc-list` to display a related group of `mdc-listitem` elements (selection lists, navigation lists, settings lists) that should share keyboard navigation.\n- Use it whenever a list of two or more listitems is rendered — even a single listitem should be wrapped in `mdc-list` for screen-reader semantics.\n\n### When not to use\n\n- Use `mdc-listbox` for a list of selectable options that participate in single- or multi-select selection semantics (the listbox uses `role=\"listbox\"` and `mdc-option` children rather than `mdc-listitem`).\n- Use `mdc-menubar` / `mdc-menupopover` for application menus.\n- Use `mdc-virtualizedlist` for very large lists where DOM virtualisation is needed.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/list/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, ListItem, Listheader } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-list>\n  <mdc-listheader slot=\"list-header\" header-text=\"Recent files\"></mdc-listheader>\n  <mdc-listitem label=\"report.pdf\"></mdc-listitem>\n  <mdc-listitem label=\"notes.md\"></mdc-listitem>\n  <mdc-listitem label=\"design.fig\"></mdc-listitem>\n</mdc-list>\n```\n\nHorizontal list without wrap-around navigation:\n\n```html\n<mdc-list orientation=\"horizontal\" loop=\"false\">\n  <mdc-listitem label=\"Tab 1\"></mdc-listitem>\n  <mdc-listitem label=\"Tab 2\"></mdc-listitem>\n  <mdc-listitem label=\"Tab 3\"></mdc-listitem>\n</mdc-list>\n```\n\n### Content guidance\n\n- Place `mdc-listitem` children in the default slot; use the `list-header` slot for `mdc-listheader`.\n- Use the `vertical` orientation for column-style lists (the default) and `horizontal` only when listitems flow side-by-side and the surrounding layout truly reads left-to-right.\n- Set `loop=\"false\"` when the ends of the list matter to the user (e.g. when item position implies progress); leave the default `loop=\"true\"` for menus / navigation lists where wrapping feels natural.\n\n### Property/Attribute details\n\n- `loop` — `\"true\"` (default) wraps navigation at both ends; `\"false\"` stops at the first / last item.\n- `initial-focus` — zero-based index of the item that receives focus the first time the list is entered. Out-of-bounds values clamp to the nearest valid index. Default `0`.\n- `orientation` — `vertical` (default; Up/Down arrows navigate) or `horizontal` (Left/Right arrows navigate). Also drives the Flexbox direction.\n\n### Limitations\n\n- The list only manages listitems that match `mdc-listitem:not([disabled])`. Items with the `disabled` attribute are skipped by keyboard navigation but remain in the DOM.\n- The list does not own selection state — selection is a property of the individual listitem (or of the surrounding pattern such as a checkbox group). The list's job is grouping and navigation, not single-/multi-select semantics.",
      "Accessibility": "### Built-in features\n\nThe host exposes itself with `role=\"list\"` so screen readers announce the group. Keyboard navigation is managed by the list: pressing the arrow key matching the orientation moves focus between listitems with a roving tabindex (only the focused item has `tabindex=\"0\"`, the others have `tabindex=\"-1\"`). `Home` and `End` jump to the first / last item; `Space` is intercepted at the list level when it targets a listitem so the page does not scroll. When `loop=\"true\"`, navigation wraps at both ends; when `loop=\"false\"`, it stops at the first / last item.\n\nWhen a listitem is added or removed (via the element-store life-cycle hooks), the list re-computes the focusable item — the first item gets `tabindex=\"0\"` on initial population, and removing the currently focused item moves focus to the next valid sibling.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute     | Value                                                       |\n| ------------- | ------------- | ----------------------------------------------------------- |\n| Host          | `role`        | `list`                                                      |\n| Container slot| `role`        | `presentation` (so the wrapper does not break the a11y tree)|\n| Each listitem | `tabindex`    | `0` for the currently focused item, `-1` for the rest       |\n\n### Implementation requirements\n\n#### General\n\n- Always wrap `mdc-listitem` children in `mdc-list`, even when there is only one item, so screen readers announce the list semantics.\n- Use `mdc-listheader` (in the `list-header` slot) for any visible heading that introduces the list, instead of rendering plain text above it.\n- Match `orientation` to the visual flow of the list; mismatched orientation makes arrow-key navigation feel broken."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "initialFocus",
          "attribute": "initial-focus",
          "type": "number",
          "default": "0",
          "values": [],
          "description": "zero-based index of the item that receives focus the first time the list is entered. Out-of-bounds values clamp to the nearest valid index. Default `0`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "loop",
          "attribute": "loop",
          "type": "select",
          "default": "'true'",
          "values": [
            "true",
            "false"
          ],
          "description": "`\"true\"` (default) wraps navigation at both ends; `\"false\"` stops at the first / last item.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "select",
          "default": "'vertical'",
          "values": [
            "vertical",
            "horizontal"
          ],
          "description": "`vertical` (default; Up/Down arrows navigate) or `horizontal` (Left/Right arrows navigate). Also drives the Flexbox direction.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "textPassedToListHeader",
          "attribute": "text-passed-to-list-header",
          "type": "text",
          "values": [],
          "description": "Text to be rendered in the list header component. This is a control in storybook only.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot, where listitems can be placed."
        },
        {
          "name": "list-header",
          "description": "This slot is used to pass a header for the list, which can be a `mdc-listheader` component."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "container",
          "description": "The container slot around the list items"
        }
      ],
      "cssProperties": [],
      "rules": [
        "`\"true\"` (default) wraps navigation at both ends; `\"false\"` stops at the first / last item.",
        "zero-based index of the item that receives focus the first time the list is entered. Out-of-bounds values clamp to the nearest valid index. Default `0`.",
        "`vertical` (default; Up/Down arrows navigate) or `horizontal` (Left/Right arrows navigate). Also drives the Flexbox direction."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/list/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, ListItem, Listheader } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-list>\n  <mdc-listheader slot=\"list-header\" header-text=\"Recent files\"></mdc-listheader>\n  <mdc-listitem label=\"report.pdf\"></mdc-listitem>\n  <mdc-listitem label=\"notes.md\"></mdc-listitem>\n  <mdc-listitem label=\"design.fig\"></mdc-listitem>\n</mdc-list>"
      },
      {
        "language": "html",
        "code": "<mdc-list orientation=\"horizontal\" loop=\"false\">\n  <mdc-listitem label=\"Tab 1\"></mdc-listitem>\n  <mdc-listitem label=\"Tab 2\"></mdc-listitem>\n  <mdc-listitem label=\"Tab 3\"></mdc-listitem>\n</mdc-list>"
      }
    ],
    "reactImports": [
      "List",
      "ListItem",
      "Listheader"
    ],
    "constants": {
      "ORIENTATION": {
        "VERTICAL": "vertical",
        "HORIZONTAL": "horizontal"
      },
      "DEFAULTS": {
        "LOOP": "true",
        "INITIAL_FOCUS": 0,
        "ORIENTATION": "vertical"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "an",
      "and",
      "component",
      "configurable",
      "container",
      "elements",
      "for",
      "grouping",
      "guidelines",
      "header",
      "keyboard",
      "list",
      "listitem",
      "mdc",
      "navigable",
      "optional",
      "orientation",
      "overview",
      "slot",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "listbox",
    "title": "Listbox",
    "tagName": "mdc-listbox",
    "summary": "Usage, guidelines, and accessibility for the mdc-listbox component — a single- or multi-select list of mdc-option (optionally grouped by mdc-optgroup) that follows the WAI-ARIA listbox pattern.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/listbox",
      "knowledgeBasePath": "components/src/components/listbox/knowledge-base/listbox.component.md"
    },
    "docs": "## Overview\n\nThe listbox presents a list of `mdc-option` elements (optionally grouped by `mdc-optgroup`) and lets the user pick one option (default) or multiple options (`multiple`). Clicking an option, or pressing `Enter` / `Space` while it is focused, toggles selection in multi mode and replaces the selection in single mode.\n\nThe listbox emits a `change` event whenever the selection changes, with `value` (the most recently selected option's `value`) and `selectedValues` (all currently selected `value`s) in the event detail. It is not yet a form control — wire its `value` into a form manually if form submission is needed.\n\n### When to use\n\n- Use `mdc-listbox` to present a list of options where the user must pick one (single select) or several (`multiple`) — for example a side-panel filter list, a settings picker, or a stand-alone option group.\n- Use it when the WAI-ARIA listbox pattern (focus on selected option on entry, no looping, `aria-multiselectable` for multi) is the right semantic fit.\n\n### When not to use\n\n- Use `mdc-select` (with `mdc-selectlistbox` inside) for a form-control select that opens a popover — the standalone `mdc-listbox` is the visible-options-only variant.\n- Use `mdc-menupopover` for application menus (commands), not for selection.\n- Use `mdc-list` with `mdc-listitem` for a navigable list that does not have selection semantics.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/listbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { ListBox, Option, Optgroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (single select):\n\n```html\n<mdc-listbox name=\"theme\" value=\"light\" @change=${onChange}>\n  <mdc-option value=\"light\" label=\"Light\"></mdc-option>\n  <mdc-option value=\"dark\" label=\"Dark\"></mdc-option>\n  <mdc-option value=\"system\" label=\"System\"></mdc-option>\n</mdc-listbox>\n```\n\nMulti-select listbox with grouped options:\n\n```html\n<mdc-listbox name=\"cuisines\" multiple @change=${onChange}>\n  <mdc-optgroup label=\"European\">\n    <mdc-option value=\"italian\" label=\"Italian\"></mdc-option>\n    <mdc-option value=\"french\" label=\"French\"></mdc-option>\n  </mdc-optgroup>\n  <mdc-optgroup label=\"Asian\">\n    <mdc-option value=\"japanese\" label=\"Japanese\"></mdc-option>\n    <mdc-option value=\"thai\" label=\"Thai\"></mdc-option>\n  </mdc-optgroup>\n</mdc-listbox>\n```\n\nListen for `change` to react to selection updates. `event.detail.value` is the last-selected option's value; `event.detail.selectedValues` is the full array (useful in multi mode).\n\n### Content guidance\n\n- Give every `mdc-option` a unique `value`; the listbox uses `value` to sync its own `value` property with the currently selected option.\n- Use `mdc-optgroup` to label related options when the list is long enough that scanning would benefit from section headers.\n- Keep `multiple` false unless multi-select is genuinely needed; multi-select listboxes are easy to use incorrectly (consumers expect single select by default).\n\n### Property/Attribute details\n\n- `name` — identifier for the listbox. Currently informational (the listbox is not yet form-associated); useful when serialising the value manually.\n- `value` — the value of the currently selected option (single mode) or the first selected option (multi mode). Setting `value` programmatically updates the selected option to match.\n- `multiple` — when `true`, multiple options can be selected; clicking / Enter / Space toggles each option independently. Default `false`.\n\n### Limitations\n\n- The listbox is not yet form-associated. Its `value` does not participate in form submission; consumers must read `value` (or `event.detail.selectedValues`) and serialise it themselves.\n- Navigation does not loop — pressing the down arrow on the last option stays on the last option (per the WAI-ARIA listbox pattern).\n\n## Accessibility\n\n### Built-in features\n\nThe host exposes itself with `role=\"listbox\"` and sets `aria-multiselectable=\"true\"` when `multiple` is `true`. Options manage their own `role=\"option\"` and `aria-selected` state.\n\nKeyboard navigation follows the WAI-ARIA listbox pattern: arrow keys move focus between options (no wrap), `Home` / `End` jump to the first / last option, and `Enter` / `Space` activates the focused option (replacing selection in single mode, toggling in multi mode). The first time the listbox receives focus, the focus lands on the first currently-selected option (or the first option if none is selected) so screen-reader users start at a meaningful position.\n\nWhenever the selection changes — by click, keyboard, or programmatic `value` update — a composed, bubbling `change` event is dispatched with both `value` and `selectedValues` so consumers can sync their own state.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute               | Value                                                      |\n| -------------- | ----------------------- | ---------------------------------------------------------- |\n| Host           | `role`                  | `listbox`                                                  |\n| Host           | `aria-multiselectable`  | `true` when `multiple`, otherwise removed                  |\n| Each option    | `tabindex`              | `0` for the currently focused option, `-1` for the rest    |\n| Each option    | `aria-selected`         | reflects the option's `selected` attribute                 |\n\n### Implementation requirements\n\n#### General\n\n- Place options in the default slot (optionally wrapped in `mdc-optgroup`); only `mdc-option:not([disabled])` participates in keyboard navigation and selection.\n- Wire the `change` event to your own state and re-render — the listbox tracks selection internally but the consumer typically owns the canonical value.\n- When `multiple` is `true`, communicate the multi-select affordance in the surrounding UI (header text, helper text) so users know to pick more than one option.",
    "sections": {
      "Overview": "The listbox presents a list of `mdc-option` elements (optionally grouped by `mdc-optgroup`) and lets the user pick one option (default) or multiple options (`multiple`). Clicking an option, or pressing `Enter` / `Space` while it is focused, toggles selection in multi mode and replaces the selection in single mode.\n\nThe listbox emits a `change` event whenever the selection changes, with `value` (the most recently selected option's `value`) and `selectedValues` (all currently selected `value`s) in the event detail. It is not yet a form control — wire its `value` into a form manually if form submission is needed.\n\n### When to use\n\n- Use `mdc-listbox` to present a list of options where the user must pick one (single select) or several (`multiple`) — for example a side-panel filter list, a settings picker, or a stand-alone option group.\n- Use it when the WAI-ARIA listbox pattern (focus on selected option on entry, no looping, `aria-multiselectable` for multi) is the right semantic fit.\n\n### When not to use\n\n- Use `mdc-select` (with `mdc-selectlistbox` inside) for a form-control select that opens a popover — the standalone `mdc-listbox` is the visible-options-only variant.\n- Use `mdc-menupopover` for application menus (commands), not for selection.\n- Use `mdc-list` with `mdc-listitem` for a navigable list that does not have selection semantics.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/listbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { ListBox, Option, Optgroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (single select):\n\n```html\n<mdc-listbox name=\"theme\" value=\"light\" @change=${onChange}>\n  <mdc-option value=\"light\" label=\"Light\"></mdc-option>\n  <mdc-option value=\"dark\" label=\"Dark\"></mdc-option>\n  <mdc-option value=\"system\" label=\"System\"></mdc-option>\n</mdc-listbox>\n```\n\nMulti-select listbox with grouped options:\n\n```html\n<mdc-listbox name=\"cuisines\" multiple @change=${onChange}>\n  <mdc-optgroup label=\"European\">\n    <mdc-option value=\"italian\" label=\"Italian\"></mdc-option>\n    <mdc-option value=\"french\" label=\"French\"></mdc-option>\n  </mdc-optgroup>\n  <mdc-optgroup label=\"Asian\">\n    <mdc-option value=\"japanese\" label=\"Japanese\"></mdc-option>\n    <mdc-option value=\"thai\" label=\"Thai\"></mdc-option>\n  </mdc-optgroup>\n</mdc-listbox>\n```\n\nListen for `change` to react to selection updates. `event.detail.value` is the last-selected option's value; `event.detail.selectedValues` is the full array (useful in multi mode).\n\n### Content guidance\n\n- Give every `mdc-option` a unique `value`; the listbox uses `value` to sync its own `value` property with the currently selected option.\n- Use `mdc-optgroup` to label related options when the list is long enough that scanning would benefit from section headers.\n- Keep `multiple` false unless multi-select is genuinely needed; multi-select listboxes are easy to use incorrectly (consumers expect single select by default).\n\n### Property/Attribute details\n\n- `name` — identifier for the listbox. Currently informational (the listbox is not yet form-associated); useful when serialising the value manually.\n- `value` — the value of the currently selected option (single mode) or the first selected option (multi mode). Setting `value` programmatically updates the selected option to match.\n- `multiple` — when `true`, multiple options can be selected; clicking / Enter / Space toggles each option independently. Default `false`.\n\n### Limitations\n\n- The listbox is not yet form-associated. Its `value` does not participate in form submission; consumers must read `value` (or `event.detail.selectedValues`) and serialise it themselves.\n- Navigation does not loop — pressing the down arrow on the last option stays on the last option (per the WAI-ARIA listbox pattern).",
      "Accessibility": "### Built-in features\n\nThe host exposes itself with `role=\"listbox\"` and sets `aria-multiselectable=\"true\"` when `multiple` is `true`. Options manage their own `role=\"option\"` and `aria-selected` state.\n\nKeyboard navigation follows the WAI-ARIA listbox pattern: arrow keys move focus between options (no wrap), `Home` / `End` jump to the first / last option, and `Enter` / `Space` activates the focused option (replacing selection in single mode, toggling in multi mode). The first time the listbox receives focus, the focus lands on the first currently-selected option (or the first option if none is selected) so screen-reader users start at a meaningful position.\n\nWhenever the selection changes — by click, keyboard, or programmatic `value` update — a composed, bubbling `change` event is dispatched with both `value` and `selectedValues` so consumers can sync their own state.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute               | Value                                                      |\n| -------------- | ----------------------- | ---------------------------------------------------------- |\n| Host           | `role`                  | `listbox`                                                  |\n| Host           | `aria-multiselectable`  | `true` when `multiple`, otherwise removed                  |\n| Each option    | `tabindex`              | `0` for the currently focused option, `-1` for the rest    |\n| Each option    | `aria-selected`         | reflects the option's `selected` attribute                 |\n\n### Implementation requirements\n\n#### General\n\n- Place options in the default slot (optionally wrapped in `mdc-optgroup`); only `mdc-option:not([disabled])` participates in keyboard navigation and selection.\n- Wire the `change` event to your own state and re-render — the listbox tracks selection internally but the consumer typically owns the canonical value.\n- When `multiple` is `true`, communicate the multi-select affordance in the surrounding UI (header text, helper text) so users know to pick more than one option."
    },
    "dependencies": [
      "mdc-list",
      "mdc-icon",
      "mdc-text",
      "mdc-option",
      "mdc-optgroup"
    ],
    "api": {
      "properties": [
        {
          "name": "multiple",
          "attribute": "multiple",
          "type": "boolean",
          "values": [],
          "description": "when `true`, multiple options can be selected; clicking / Enter / Space toggles each option independently. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "identifier for the listbox. Currently informational (the listbox is not yet form-associated); useful when serialising the value manually.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "the value of the currently selected option (single mode) or the first selected option (multi mode). Setting `value` programmatically updates the selected option to match.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot, where options and optgroups are placed"
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is emitted when the selected item changed"
        }
      ],
      "cssParts": [
        {
          "name": "container",
          "description": "The container of the listbox"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-listbox-max-height",
          "description": "max height of the listbox"
        }
      ],
      "rules": [
        "identifier for the listbox. Currently informational (the listbox is not yet form-associated); useful when serialising the value manually.",
        "the value of the currently selected option (single mode) or the first selected option (multi mode). Setting `value` programmatically updates the selected option to match.",
        "when `true`, multiple options can be selected; clicking / Enter / Space toggles each option independently. Default `false`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/listbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { ListBox, Option, Optgroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-listbox name=\"theme\" value=\"light\" @change=${onChange}>\n  <mdc-option value=\"light\" label=\"Light\"></mdc-option>\n  <mdc-option value=\"dark\" label=\"Dark\"></mdc-option>\n  <mdc-option value=\"system\" label=\"System\"></mdc-option>\n</mdc-listbox>"
      },
      {
        "language": "html",
        "code": "<mdc-listbox name=\"cuisines\" multiple @change=${onChange}>\n  <mdc-optgroup label=\"European\">\n    <mdc-option value=\"italian\" label=\"Italian\"></mdc-option>\n    <mdc-option value=\"french\" label=\"French\"></mdc-option>\n  </mdc-optgroup>\n  <mdc-optgroup label=\"Asian\">\n    <mdc-option value=\"japanese\" label=\"Japanese\"></mdc-option>\n    <mdc-option value=\"thai\" label=\"Thai\"></mdc-option>\n  </mdc-optgroup>\n</mdc-listbox>"
      }
    ],
    "reactImports": [
      "ListBox",
      "Option",
      "Optgroup"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "aria",
      "by",
      "component",
      "follows",
      "for",
      "grouped",
      "guidelines",
      "icon",
      "list",
      "listbox",
      "mdc",
      "multi",
      "of",
      "optgroup",
      "option",
      "optionally",
      "or",
      "overview",
      "pattern",
      "select",
      "single",
      "text",
      "that",
      "the",
      "usage",
      "wai"
    ]
  },
  {
    "name": "listheader",
    "title": "Listheader",
    "tagName": "mdc-listheader",
    "summary": "Usage, guidelines, and accessibility for the mdc-listheader component — a non-interactive header row for an mdc-list, with optional prefix and postfix icons and a default slot for actionable elements.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/listheader",
      "knowledgeBasePath": "components/src/components/listheader/knowledge-base/listheader.component.md"
    },
    "docs": "## Overview\n\nThe listheader is a header row rendered above the items of an `mdc-list` (via the list's `list-header` slot). It displays optional prefix and postfix icons and a header text, plus a default slot for actionable content (buttons, links) aligned to the trailing end. The header itself is not interactive — interactivity comes only from elements placed in the slot.\n\n### When to use\n\n- Use `mdc-listheader` (in the `list-header` slot of an `mdc-list`) to label a list with a heading, an optional leading icon, and optional trailing actions.\n\n### When not to use\n\n- Do not use `mdc-listheader` as an item inside the list's default slot — it is designed for the `list-header` slot only.\n- Use a plain section title outside the list when no leading icon, trailing icon, or trailing action is needed.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/listheader/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, Listheader, ListItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (text-only header):\n\n```html\n<mdc-list>\n  <mdc-listheader slot=\"list-header\" header-text=\"Recent files\"></mdc-listheader>\n  <mdc-listitem label=\"report.pdf\"></mdc-listitem>\n  <mdc-listitem label=\"notes.md\"></mdc-listitem>\n</mdc-list>\n```\n\nHeader with a leading folder icon and a trailing \"see all\" link:\n\n```html\n<mdc-listheader\n  slot=\"list-header\"\n  prefix-icon=\"folder-bold\"\n  header-text=\"Shared with me\"\n>\n  <mdc-link href=\"/files/shared\">See all</mdc-link>\n</mdc-listheader>\n```\n\n### Content guidance\n\n- Keep `header-text` short and noun-style — it labels the list section, it is not an action.\n- Use the default slot for trailing actionable elements (e.g. an `mdc-link`, `mdc-button`, or `mdc-icon` rendered as a control); plain decorative text in the slot is not announced as part of the header.\n- Use `prefix-icon` to reinforce the section's meaning visually (folder icon for a folder section, star icon for favourites); leave it unset when text alone is clearer.\n\n### Property/Attribute details\n\n- `header-text` — the text displayed in the header. Default unset (header text is not rendered).\n- `prefix-icon` — name of an icon rendered before the header text. Default unset; when unset, the text is aligned to the start.\n- `postfix-icon` — name of an icon rendered at the trailing end after the slot. Default unset; when unset, slot content is aligned to the end.\n- `disabled` — when `true`, the header is visually styled as disabled. Default `false`.\n\n### Limitations\n\n- The `disabled` flag only changes the visual style of the header itself — it does not propagate to elements inside the default slot. Disable any actionable slotted elements explicitly (e.g. set `disabled` on the slotted `mdc-button` / `mdc-link`).\n\n## Accessibility\n\n### Built-in features\n\nThe header text and its accompanying icons are rendered with `aria-hidden=\"true\"` on the inner `mdc-text` and `mdc-icon` elements so the listheader does not produce duplicate announcements when the list is read by a screen reader. The header is a presentational decoration on the list — accessibility for the list itself (`role=\"list\"`, item count, item navigation) is owned by `mdc-list`.\n\nSlotted actionable elements (links, buttons) keep their own accessibility semantics — they are focusable and announced like any other interactive element in the page.\n\n#### Internal ARIA managed by the component\n\n| Element                | Attribute     | Value      |\n| ---------------------- | ------------- | ---------- |\n| Prefix icon            | `aria-hidden` | `true`     |\n| Postfix icon           | `aria-hidden` | `true`     |\n| Header text `mdc-text` | `aria-hidden` | `true`     |\n\n### Implementation requirements\n\n#### Labelling\n\n- Use the `header-text` attribute (rather than slotting plain text) so the header picks up the right typography and `aria-hidden` treatment.\n- For trailing actions, slot a control with its own accessible name (e.g. `<mdc-link>See all</mdc-link>`) so the action is announced when focused.",
    "sections": {
      "Overview": "The listheader is a header row rendered above the items of an `mdc-list` (via the list's `list-header` slot). It displays optional prefix and postfix icons and a header text, plus a default slot for actionable content (buttons, links) aligned to the trailing end. The header itself is not interactive — interactivity comes only from elements placed in the slot.\n\n### When to use\n\n- Use `mdc-listheader` (in the `list-header` slot of an `mdc-list`) to label a list with a heading, an optional leading icon, and optional trailing actions.\n\n### When not to use\n\n- Do not use `mdc-listheader` as an item inside the list's default slot — it is designed for the `list-header` slot only.\n- Use a plain section title outside the list when no leading icon, trailing icon, or trailing action is needed.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/listheader/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, Listheader, ListItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (text-only header):\n\n```html\n<mdc-list>\n  <mdc-listheader slot=\"list-header\" header-text=\"Recent files\"></mdc-listheader>\n  <mdc-listitem label=\"report.pdf\"></mdc-listitem>\n  <mdc-listitem label=\"notes.md\"></mdc-listitem>\n</mdc-list>\n```\n\nHeader with a leading folder icon and a trailing \"see all\" link:\n\n```html\n<mdc-listheader\n  slot=\"list-header\"\n  prefix-icon=\"folder-bold\"\n  header-text=\"Shared with me\"\n>\n  <mdc-link href=\"/files/shared\">See all</mdc-link>\n</mdc-listheader>\n```\n\n### Content guidance\n\n- Keep `header-text` short and noun-style — it labels the list section, it is not an action.\n- Use the default slot for trailing actionable elements (e.g. an `mdc-link`, `mdc-button`, or `mdc-icon` rendered as a control); plain decorative text in the slot is not announced as part of the header.\n- Use `prefix-icon` to reinforce the section's meaning visually (folder icon for a folder section, star icon for favourites); leave it unset when text alone is clearer.\n\n### Property/Attribute details\n\n- `header-text` — the text displayed in the header. Default unset (header text is not rendered).\n- `prefix-icon` — name of an icon rendered before the header text. Default unset; when unset, the text is aligned to the start.\n- `postfix-icon` — name of an icon rendered at the trailing end after the slot. Default unset; when unset, slot content is aligned to the end.\n- `disabled` — when `true`, the header is visually styled as disabled. Default `false`.\n\n### Limitations\n\n- The `disabled` flag only changes the visual style of the header itself — it does not propagate to elements inside the default slot. Disable any actionable slotted elements explicitly (e.g. set `disabled` on the slotted `mdc-button` / `mdc-link`).",
      "Accessibility": "### Built-in features\n\nThe header text and its accompanying icons are rendered with `aria-hidden=\"true\"` on the inner `mdc-text` and `mdc-icon` elements so the listheader does not produce duplicate announcements when the list is read by a screen reader. The header is a presentational decoration on the list — accessibility for the list itself (`role=\"list\"`, item count, item navigation) is owned by `mdc-list`.\n\nSlotted actionable elements (links, buttons) keep their own accessibility semantics — they are focusable and announced like any other interactive element in the page.\n\n#### Internal ARIA managed by the component\n\n| Element                | Attribute     | Value      |\n| ---------------------- | ------------- | ---------- |\n| Prefix icon            | `aria-hidden` | `true`     |\n| Postfix icon           | `aria-hidden` | `true`     |\n| Header text `mdc-text` | `aria-hidden` | `true`     |\n\n### Implementation requirements\n\n#### Labelling\n\n- Use the `header-text` attribute (rather than slotting plain text) so the header picks up the right typography and `aria-hidden` treatment.\n- For trailing actions, slot a control with its own accessible name (e.g. `<mdc-link>See all</mdc-link>`) so the action is announced when focused."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "unknown",
          "values": [
            "Button",
            "None"
          ],
          "description": "Content for the default slot (e.g., button)",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the header is visually styled as disabled. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "values": [],
          "description": "the text displayed in the header. Default unset (header text is not rendered).",
          "source": "storybook"
        },
        {
          "name": "postfixIcon",
          "attribute": "postfix-icon",
          "type": "text",
          "values": [],
          "description": "name of an icon rendered at the trailing end after the slot. Default unset; when unset, slot content is aligned to the end.",
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "text",
          "values": [],
          "description": "name of an icon rendered before the header text. Default unset; when unset, the text is aligned to the start.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "to pass in actionable content like buttons or links"
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "header-text",
          "description": "The header text of list header"
        },
        {
          "name": "postfix-icon",
          "description": "The postfix icon of list header"
        },
        {
          "name": "prefix-icon",
          "description": "The prefix icon of list header"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-listheader-height",
          "description": "height of the header"
        },
        {
          "name": "--mdc-listheader-padding",
          "description": "padding around the header content"
        },
        {
          "name": "--mdc-listheader-gap",
          "description": "gap between content"
        }
      ],
      "rules": [
        "the text displayed in the header. Default unset (header text is not rendered).",
        "name of an icon rendered before the header text. Default unset; when unset, the text is aligned to the start.",
        "name of an icon rendered at the trailing end after the slot. Default unset; when unset, slot content is aligned to the end.",
        "when `true`, the header is visually styled as disabled. Default `false`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/listheader/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, Listheader, ListItem } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-list>\n  <mdc-listheader slot=\"list-header\" header-text=\"Recent files\"></mdc-listheader>\n  <mdc-listitem label=\"report.pdf\"></mdc-listitem>\n  <mdc-listitem label=\"notes.md\"></mdc-listitem>\n</mdc-list>"
      },
      {
        "language": "html",
        "code": "<mdc-listheader\n  slot=\"list-header\"\n  prefix-icon=\"folder-bold\"\n  header-text=\"Shared with me\"\n>\n  <mdc-link href=\"/files/shared\">See all</mdc-link>\n</mdc-listheader>"
      }
    ],
    "reactImports": [
      "List",
      "Listheader",
      "ListItem"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "actionable",
      "an",
      "and",
      "component",
      "default",
      "elements",
      "for",
      "guidelines",
      "header",
      "icons",
      "interactive",
      "list",
      "listheader",
      "mdc",
      "non",
      "optional",
      "overview",
      "postfix",
      "prefix",
      "row",
      "slot",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "listitem",
    "title": "Listitem",
    "tagName": "mdc-listitem",
    "summary": "Usage, guidelines, and accessibility for the mdc-listitem component — a flexible row inside an mdc-list with optional leading/trailing controls, three label slots, and keyboard activation.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/listitem",
      "knowledgeBasePath": "components/src/components/listitem/knowledge-base/listitem.component.md"
    },
    "docs": "## Overview\n\nThe listitem is a single row inside an `mdc-list`. It supports a leading control (radio, checkbox, avatar, icon), a trailing control (toggle, badge, icon), three leading text slots (primary / secondary / tertiary label), and two trailing text slots (side header, subline). The listitem is focusable, activates on click, `Enter`, or `Space` (dispatching a `click` event), and exposes life-cycle events (`enabled`, `disabled`, `created`, `destroyed`) that the surrounding list uses to track its items.\n\nThree visual variants are available: `full-width` (default), `rectangle`, and `pill`. The `active` flag is a visual-only state that bolds the primary label; the canonical active / selected state belongs to the parent list pattern.\n\n### When to use\n\n- Use `mdc-listitem` as a row inside an `mdc-list`. Even a single listitem should be wrapped in `mdc-list` for screen-reader semantics.\n- Use it whenever a row needs to combine an icon / avatar / control with a label and optional trailing content.\n\n### When not to use\n\n- Use `mdc-option` inside `mdc-listbox` / `mdc-selectlistbox` when the row represents a selectable option in a listbox pattern.\n- Use `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` inside menu patterns rather than `mdc-listitem`.\n- Use `mdc-navmenuitem` when the row is a navigation entry inside `mdc-sidenavigation`.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/listitem/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, ListItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-list>\n  <mdc-listitem label=\"Inbox\" secondary-label=\"42 unread\"></mdc-listitem>\n  <mdc-listitem label=\"Drafts\" secondary-label=\"3\"></mdc-listitem>\n</mdc-list>\n```\n\nListitem with a leading avatar, primary/secondary labels, side-header text, and a trailing toggle:\n\n```html\n<mdc-listitem label=\"Alex Kim\" secondary-label=\"alex@example.com\" side-header-text=\"Online\">\n  <mdc-avatar slot=\"leading-controls\" initials=\"AK\" size=\"32\"></mdc-avatar>\n  <mdc-toggle slot=\"trailing-controls\" checked></mdc-toggle>\n</mdc-listitem>\n```\n\nLong-text listitem with an attached tooltip (rendered as a sibling, not a wrapper):\n\n```html\n<mdc-listitem id=\"long-row\" label=\"A very long file name that may not fit\"></mdc-listitem>\n<mdc-tooltip triggerid=\"long-row\">A very long file name that may not fit</mdc-tooltip>\n```\n\nListen for `click`, `keydown`, `keyup`, and `focus` to react to user interaction; listen for `enabled` / `disabled` / `created` / `destroyed` if your code needs to mirror the list's life-cycle (e.g. when synchronising with a virtualised store).\n\n### Content guidance\n\n- Use `label` for the primary, scannable text; `secondary-label` for supporting context (e.g. count, status); `tertiary-label` for low-priority metadata.\n- Use `side-header-text` and `subline-text` for trailing-side text that belongs to the row (e.g. a timestamp and a status line).\n- Use `leading-controls` for radios, checkboxes, avatars, or icons that identify the row; use `trailing-controls` for toggles, badges, or actions that affect the row.\n- When the primary label may be too long to fit, connect an `mdc-tooltip` to the listitem via the tooltip's `triggerid` attribute pointing at the listitem's `id`, and let the tooltip render the full text on hover / focus.\n\n### Property/Attribute details\n\n- `variant` — `full-width` (default), `rectangle`, or `pill`. Controls the row's visual treatment.\n- `label`, `secondary-label`, `tertiary-label` — the three leading-side text slots. Each is also exposed as a slot (`leading-text-primary-label`, `leading-text-secondary-label`, `leading-text-tertiary-label`) for custom markup.\n- `side-header-text`, `subline-text` — the two trailing-side text slots. Slot equivalents are `trailing-text-side-header` and `trailing-text-subline`.\n- `disabled` — when `true`, the row is removed from the tab order, its `aria-disabled` is set to `true`, click is suppressed, and slotted controls are disabled.\n- `soft-disabled` — when `true`, the row looks disabled but remains focusable so assistive technology can read it; click and keyboard actions still pass through (consumers must suppress unintended actions themselves).\n- `data-index` — zero-based index used by virtualised lists. When set, the row's `aria-posinset` is updated so screen readers announce the correct position even when items are not sequentially rendered.\n- `active` — visual-only state that bolds the primary label. It does not affect behaviour or accessibility; use it to highlight the current row (e.g. the selected route) when the canonical state lives elsewhere.\n\nSlots:\n\n- `leading-controls` — radio, checkbox, avatar, or icon at the leading edge.\n- `leading-text-primary-label`, `leading-text-secondary-label`, `leading-text-tertiary-label` — override the default text rendering for each leading label.\n- `trailing-text-side-header`, `trailing-text-subline` — override the default trailing text rendering.\n- `trailing-controls` — toggle, badge, or action at the trailing edge.\n- `content` — replaces the entire built-in layout with custom markup (use sparingly — overriding `content` discards the structured leading / trailing layout).\n\n### Limitations\n\n- The `active` attribute is purely visual. It does not set `aria-current` or any selected state; the surrounding pattern (e.g. a navigation list) must own the canonical \"current item\" semantics.\n- The listitem is not form-associated. Form fields placed inside the row (checkbox, toggle, radio) handle their own form values; the row itself does not submit anything.\n\n## Accessibility\n\n### Built-in features\n\nThe host exposes itself with `role=\"listitem\"` (overrideable when a parent pattern needs a different role, e.g. `role=\"option\"` inside a listbox). The row is focusable: when the parent `mdc-list` is entered the row receives `tabindex=\"0\"`, and the standard arrow-key navigation in the list moves focus between rows.\n\nActivation follows the button pattern: `Enter` fires `click` on keydown; `Space` fires `click` on keyup (so holding Space does not repeatedly trigger). Activation events from inside slotted controls (a slotted toggle, a slotted button) are stopped from re-triggering the row's own click handler.\n\nWhen `disabled` is set, the host's `aria-disabled` is set to `true`, the row's `tabindex` becomes `-1`, click is prevented, and any slotted controls in the leading / trailing slots are disabled too. The component dispatches a `disabled` (or `enabled`) life-cycle event so the parent list can update its item store.\n\nWhen `data-index` is set, the row's `aria-posinset` is set to `data-index + 1` so virtualised lists report the correct position to screen readers.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute       | Value                                                              |\n| ------------- | --------------- | ------------------------------------------------------------------ |\n| Host          | `role`          | `listitem` (defaults; overrideable by the parent pattern)          |\n| Host          | `aria-disabled` | `true` when `disabled`, otherwise `false`                          |\n| Host          | `aria-posinset` | `data-index + 1` when `data-index` is set, otherwise removed       |\n| Host          | `tabindex`      | `-1` when `disabled`, otherwise managed by the parent list         |\n\n### Implementation requirements\n\n#### General\n\n- Always wrap listitems in `mdc-list` so screen readers announce the list semantics, even when there is only one row.\n- For virtualised lists, set `data-index` on every row so `aria-posinset` reflects the row's true position, not its position in the rendered window.\n- When the row's primary label may truncate, connect an `mdc-tooltip` to the row via the tooltip's `triggerid` pointing at the row's `id`. Do not rely on the truncated label alone — the tooltip is the only way to surface the full text to screen-reader users.\n- When using `soft-disabled`, suppress unintended click / keyboard actions in your own handlers; the row only changes visuals, it does not block activation.\n\n#### Labelling\n\n- Provide `label` on every listitem; without it the row has no announced name unless the consumer slots their own text into `leading-text-primary-label`.",
    "sections": {
      "Overview": "The listitem is a single row inside an `mdc-list`. It supports a leading control (radio, checkbox, avatar, icon), a trailing control (toggle, badge, icon), three leading text slots (primary / secondary / tertiary label), and two trailing text slots (side header, subline). The listitem is focusable, activates on click, `Enter`, or `Space` (dispatching a `click` event), and exposes life-cycle events (`enabled`, `disabled`, `created`, `destroyed`) that the surrounding list uses to track its items.\n\nThree visual variants are available: `full-width` (default), `rectangle`, and `pill`. The `active` flag is a visual-only state that bolds the primary label; the canonical active / selected state belongs to the parent list pattern.\n\n### When to use\n\n- Use `mdc-listitem` as a row inside an `mdc-list`. Even a single listitem should be wrapped in `mdc-list` for screen-reader semantics.\n- Use it whenever a row needs to combine an icon / avatar / control with a label and optional trailing content.\n\n### When not to use\n\n- Use `mdc-option` inside `mdc-listbox` / `mdc-selectlistbox` when the row represents a selectable option in a listbox pattern.\n- Use `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` inside menu patterns rather than `mdc-listitem`.\n- Use `mdc-navmenuitem` when the row is a navigation entry inside `mdc-sidenavigation`.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/listitem/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, ListItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-list>\n  <mdc-listitem label=\"Inbox\" secondary-label=\"42 unread\"></mdc-listitem>\n  <mdc-listitem label=\"Drafts\" secondary-label=\"3\"></mdc-listitem>\n</mdc-list>\n```\n\nListitem with a leading avatar, primary/secondary labels, side-header text, and a trailing toggle:\n\n```html\n<mdc-listitem label=\"Alex Kim\" secondary-label=\"alex@example.com\" side-header-text=\"Online\">\n  <mdc-avatar slot=\"leading-controls\" initials=\"AK\" size=\"32\"></mdc-avatar>\n  <mdc-toggle slot=\"trailing-controls\" checked></mdc-toggle>\n</mdc-listitem>\n```\n\nLong-text listitem with an attached tooltip (rendered as a sibling, not a wrapper):\n\n```html\n<mdc-listitem id=\"long-row\" label=\"A very long file name that may not fit\"></mdc-listitem>\n<mdc-tooltip triggerid=\"long-row\">A very long file name that may not fit</mdc-tooltip>\n```\n\nListen for `click`, `keydown`, `keyup`, and `focus` to react to user interaction; listen for `enabled` / `disabled` / `created` / `destroyed` if your code needs to mirror the list's life-cycle (e.g. when synchronising with a virtualised store).\n\n### Content guidance\n\n- Use `label` for the primary, scannable text; `secondary-label` for supporting context (e.g. count, status); `tertiary-label` for low-priority metadata.\n- Use `side-header-text` and `subline-text` for trailing-side text that belongs to the row (e.g. a timestamp and a status line).\n- Use `leading-controls` for radios, checkboxes, avatars, or icons that identify the row; use `trailing-controls` for toggles, badges, or actions that affect the row.\n- When the primary label may be too long to fit, connect an `mdc-tooltip` to the listitem via the tooltip's `triggerid` attribute pointing at the listitem's `id`, and let the tooltip render the full text on hover / focus.\n\n### Property/Attribute details\n\n- `variant` — `full-width` (default), `rectangle`, or `pill`. Controls the row's visual treatment.\n- `label`, `secondary-label`, `tertiary-label` — the three leading-side text slots. Each is also exposed as a slot (`leading-text-primary-label`, `leading-text-secondary-label`, `leading-text-tertiary-label`) for custom markup.\n- `side-header-text`, `subline-text` — the two trailing-side text slots. Slot equivalents are `trailing-text-side-header` and `trailing-text-subline`.\n- `disabled` — when `true`, the row is removed from the tab order, its `aria-disabled` is set to `true`, click is suppressed, and slotted controls are disabled.\n- `soft-disabled` — when `true`, the row looks disabled but remains focusable so assistive technology can read it; click and keyboard actions still pass through (consumers must suppress unintended actions themselves).\n- `data-index` — zero-based index used by virtualised lists. When set, the row's `aria-posinset` is updated so screen readers announce the correct position even when items are not sequentially rendered.\n- `active` — visual-only state that bolds the primary label. It does not affect behaviour or accessibility; use it to highlight the current row (e.g. the selected route) when the canonical state lives elsewhere.\n\nSlots:\n\n- `leading-controls` — radio, checkbox, avatar, or icon at the leading edge.\n- `leading-text-primary-label`, `leading-text-secondary-label`, `leading-text-tertiary-label` — override the default text rendering for each leading label.\n- `trailing-text-side-header`, `trailing-text-subline` — override the default trailing text rendering.\n- `trailing-controls` — toggle, badge, or action at the trailing edge.\n- `content` — replaces the entire built-in layout with custom markup (use sparingly — overriding `content` discards the structured leading / trailing layout).\n\n### Limitations\n\n- The `active` attribute is purely visual. It does not set `aria-current` or any selected state; the surrounding pattern (e.g. a navigation list) must own the canonical \"current item\" semantics.\n- The listitem is not form-associated. Form fields placed inside the row (checkbox, toggle, radio) handle their own form values; the row itself does not submit anything.",
      "Accessibility": "### Built-in features\n\nThe host exposes itself with `role=\"listitem\"` (overrideable when a parent pattern needs a different role, e.g. `role=\"option\"` inside a listbox). The row is focusable: when the parent `mdc-list` is entered the row receives `tabindex=\"0\"`, and the standard arrow-key navigation in the list moves focus between rows.\n\nActivation follows the button pattern: `Enter` fires `click` on keydown; `Space` fires `click` on keyup (so holding Space does not repeatedly trigger). Activation events from inside slotted controls (a slotted toggle, a slotted button) are stopped from re-triggering the row's own click handler.\n\nWhen `disabled` is set, the host's `aria-disabled` is set to `true`, the row's `tabindex` becomes `-1`, click is prevented, and any slotted controls in the leading / trailing slots are disabled too. The component dispatches a `disabled` (or `enabled`) life-cycle event so the parent list can update its item store.\n\nWhen `data-index` is set, the row's `aria-posinset` is set to `data-index + 1` so virtualised lists report the correct position to screen readers.\n\n#### Internal ARIA managed by the component\n\n| Element       | Attribute       | Value                                                              |\n| ------------- | --------------- | ------------------------------------------------------------------ |\n| Host          | `role`          | `listitem` (defaults; overrideable by the parent pattern)          |\n| Host          | `aria-disabled` | `true` when `disabled`, otherwise `false`                          |\n| Host          | `aria-posinset` | `data-index + 1` when `data-index` is set, otherwise removed       |\n| Host          | `tabindex`      | `-1` when `disabled`, otherwise managed by the parent list         |\n\n### Implementation requirements\n\n#### General\n\n- Always wrap listitems in `mdc-list` so screen readers announce the list semantics, even when there is only one row.\n- For virtualised lists, set `data-index` on every row so `aria-posinset` reflects the row's true position, not its position in the rendered window.\n- When the row's primary label may truncate, connect an `mdc-tooltip` to the row via the tooltip's `triggerid` pointing at the row's `id`. Do not rely on the truncated label alone — the tooltip is the only way to surface the full text to screen-reader users.\n- When using `soft-disabled`, suppress unintended click / keyboard actions in your own handlers; the row only changes visuals, it does not block activation.\n\n#### Labelling\n\n- Provide `label` on every listitem; without it the row has no announced name unless the consumer slots their own text into `leading-text-primary-label`."
    },
    "dependencies": [
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "active",
          "attribute": "active",
          "type": "boolean",
          "values": [],
          "description": "visual-only state that bolds the primary label. It does not affect behaviour or accessibility; use it to highlight the current row (e.g. the selected route) when the canonical state lives elsewhere.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the row is removed from the tab order, its `aria-disabled` is set to `true`, click is suppressed, and slotted controls are disabled.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "secondaryLabel",
          "attribute": "secondary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "sideHeaderText",
          "attribute": "side-header-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the row looks disabled but remains focusable so assistive technology can read it; click and keyboard actions still pass through (consumers must suppress unintended actions themselves).",
          "source": "storybook"
        },
        {
          "name": "sublineText",
          "attribute": "subline-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "tertiaryLabel",
          "attribute": "tertiary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'full-width'",
          "values": [
            "full-width",
            "inset-pill",
            "inset-rectangle"
          ],
          "description": "`full-width` (default), `rectangle`, or `pill`. Controls the row's visual treatment.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "leading-controls",
          "description": "slot for list item controls to appear of leading end."
        },
        {
          "name": "leading-text-primary-label",
          "description": "slot for list item primary label."
        },
        {
          "name": "leading-text-secondary-label",
          "description": "slot for list item secondary label."
        },
        {
          "name": "leading-text-tertiary-label",
          "description": "slot for list item tertiary label."
        },
        {
          "name": "trailing-text-side-header",
          "description": "slot for list item side header text."
        },
        {
          "name": "trailing-text-subline",
          "description": "slot for list item subline text."
        },
        {
          "name": "trailing-controls",
          "description": "slot for list item controls to appear of trailing end."
        },
        {
          "name": "content",
          "description": "content slot can be used to override the content completely. Be aware that"
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the listitem is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the listitem."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the listitem."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the listitem receives focus."
        },
        {
          "name": "enabled",
          "description": "(React: onEnabled) This event is dispatched after the listitem is enabled"
        },
        {
          "name": "disabled",
          "description": "(React: onDisabled) This event is dispatched after the listitem is disabled"
        },
        {
          "name": "created",
          "description": "(React: onCreated) This event is dispatched after the listitem is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) This event is dispatched after the listitem is destroyed (removed from the DOM)"
        }
      ],
      "cssParts": [
        {
          "name": "leading",
          "description": "Allows customization of the leading part."
        },
        {
          "name": "leading-text",
          "description": "Allows customization of the leading text part."
        },
        {
          "name": "trailing",
          "description": "Allows customization of the trailing part."
        },
        {
          "name": "trailing-text",
          "description": "Allows customization of the trailing text part."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-listitem-default-background-color",
          "description": "Allows customization of the default background color."
        },
        {
          "name": "--mdc-listitem-background-color-hover",
          "description": "Allows customization of the background color on hover."
        },
        {
          "name": "--mdc-listitem-background-color-active",
          "description": "Allows customization of the background color when pressed."
        },
        {
          "name": "--mdc-listitem-primary-label-color",
          "description": ""
        },
        {
          "name": "--mdc-listitem-secondary-label-color",
          "description": ""
        },
        {
          "name": "--mdc-listitem-disabled-color",
          "description": "Allows customization of the disabled color."
        },
        {
          "name": "--mdc-listitem-column-gap",
          "description": "Allows customization of column gap."
        },
        {
          "name": "--mdc-listitem-padding-left-right",
          "description": "Allows customization of padding left and right."
        },
        {
          "name": "--mdc-listitem-padding-top-bottom",
          "description": "Allows customization of padding top and bottom."
        },
        {
          "name": "--mdc-listitem-cursor",
          "description": "Allows customization of the cursor."
        },
        {
          "name": "--mdc-listitem-width",
          "description": "Allows customization of the width of the list item."
        },
        {
          "name": "--mdc-listitem-height",
          "description": "Allows customization of the height of the list item."
        }
      ],
      "rules": [
        "`full-width` (default), `rectangle`, or `pill`. Controls the row's visual treatment.",
        "when `true`, the row is removed from the tab order, its `aria-disabled` is set to `true`, click is suppressed, and slotted controls are disabled.",
        "when `true`, the row looks disabled but remains focusable so assistive technology can read it; click and keyboard actions still pass through (consumers must suppress unintended actions themselves).",
        "zero-based index used by virtualised lists. When set, the row's `aria-posinset` is updated so screen readers announce the correct position even when items are not sequentially rendered.",
        "visual-only state that bolds the primary label. It does not affect behaviour or accessibility; use it to highlight the current row (e.g. the selected route) when the canonical state lives elsewhere.",
        "radio, checkbox, avatar, or icon at the leading edge.",
        "toggle, badge, or action at the trailing edge.",
        "replaces the entire built-in layout with custom markup (use sparingly — overriding `content` discards the structured leading / trailing layout)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/listitem/index.js\"; // custom element registration\n// or via React wrapper\nimport { List, ListItem } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-list>\n  <mdc-listitem label=\"Inbox\" secondary-label=\"42 unread\"></mdc-listitem>\n  <mdc-listitem label=\"Drafts\" secondary-label=\"3\"></mdc-listitem>\n</mdc-list>"
      },
      {
        "language": "html",
        "code": "<mdc-listitem label=\"Alex Kim\" secondary-label=\"alex@example.com\" side-header-text=\"Online\">\n  <mdc-avatar slot=\"leading-controls\" initials=\"AK\" size=\"32\"></mdc-avatar>\n  <mdc-toggle slot=\"trailing-controls\" checked></mdc-toggle>\n</mdc-listitem>"
      },
      {
        "language": "html",
        "code": "<mdc-listitem id=\"long-row\" label=\"A very long file name that may not fit\"></mdc-listitem>\n<mdc-tooltip triggerid=\"long-row\">A very long file name that may not fit</mdc-tooltip>"
      }
    ],
    "reactImports": [
      "List",
      "ListItem"
    ],
    "constants": {
      "LISTITEM_VARIANTS": {
        "FULL_WIDTH": "full-width",
        "INSET_PILL": "inset-pill",
        "INSET_RECTANGLE": "inset-rectangle"
      },
      "DEFAULTS": {
        "VARIANT": "full-width"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "activation",
      "an",
      "and",
      "component",
      "controls",
      "flexible",
      "for",
      "guidelines",
      "inside",
      "keyboard",
      "label",
      "leading",
      "list",
      "listitem",
      "mdc",
      "optional",
      "overview",
      "row",
      "slots",
      "text",
      "the",
      "three",
      "trailing",
      "usage",
      "with"
    ]
  },
  {
    "name": "marker",
    "title": "Marker",
    "tagName": "mdc-marker",
    "summary": "Usage, guidelines, and accessibility for the mdc-marker component — a thin vertical visual indicator (solid or striped) used alongside list items, cards, or content blocks to convey status.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/marker",
      "knowledgeBasePath": "components/src/components/marker/knowledge-base/marker.component.md"
    },
    "docs": "## Overview\n\nThe marker is a thin vertical line (0.25rem wide by default) used to draw the eye to a piece of content — typically rendered alongside a list item, card, or content block to indicate status, priority, or categorisation. Two visual variants are available: `solid` (a single coloured line) and `striped` (a line with diagonal stripes for a high-attention variant).\n\nThe marker is purely presentational; its meaning must be conveyed elsewhere (label, status text, icon) because it has no accessible name.\n\n### When to use\n\n- Use `mdc-marker` to draw attention to important content, signal priority, or visually categorise items inside a layout.\n- Use it as a decorative companion to a list item, card, or content block — never as the sole signal for the information.\n\n### When not to use\n\n- Use a label, status text, or an icon when the meaning must be announced to screen readers — the marker alone is invisible to assistive technology.\n- Use `mdc-divider` for separating content sections rather than highlighting a single block.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/marker/index.js\"; // custom element registration\n// or via React wrapper\nimport { Marker } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (solid marker next to a card title):\n\n```html\n<div class=\"row\">\n  <mdc-marker variant=\"solid\"></mdc-marker>\n  <mdc-text type=\"body-midsize-bold\">Priority item</mdc-text>\n</div>\n```\n\nStriped marker for higher-attention treatment:\n\n```html\n<mdc-marker variant=\"striped\"></mdc-marker>\n```\n\nCustomise the colour and width with CSS custom properties:\n\n```html\n<mdc-marker\n  variant=\"solid\"\n  style=\"--mdc-marker-solid-background-color: var(--mds-color-theme-text-error); --mdc-marker-width: 0.5rem;\"\n></mdc-marker>\n```\n\n### Content guidance\n\n- Always pair the marker with text or an icon that conveys the same meaning — the colour and pattern alone are not accessible.\n- Reserve the `striped` variant for the highest-priority or most urgent treatment so it retains its visual weight.\n- Use the CSS custom properties (`--mdc-marker-solid-background-color`, `--mdc-marker-striped-color`, `--mdc-marker-striped-background-color`, `--mdc-marker-width`) to map the marker into a theme's colour tokens.\n\n### Property/Attribute details\n\n- `variant` — `solid` (default; single coloured line) or `striped` (line with diagonal stripes for higher-attention treatment).\n\n### Limitations\n\n- The marker is decorative and has no accessible name. It is invisible to screen readers and must always be accompanied by a text or icon affordance that carries the same meaning.\n\n## Accessibility\n\n### Built-in features\n\nThe marker has no role, no accessible name, and no keyboard or focus behaviour — it is a purely decorative bar. Screen readers skip it.\n\n### Implementation requirements\n\n#### General\n\n- Always pair the marker with a text label, status string, or icon that conveys the same meaning. Do not rely on colour or pattern alone.\n- Ensure sufficient colour contrast between the marker and the surrounding background so sighted users with low vision can perceive it.",
    "sections": {
      "Overview": "The marker is a thin vertical line (0.25rem wide by default) used to draw the eye to a piece of content — typically rendered alongside a list item, card, or content block to indicate status, priority, or categorisation. Two visual variants are available: `solid` (a single coloured line) and `striped` (a line with diagonal stripes for a high-attention variant).\n\nThe marker is purely presentational; its meaning must be conveyed elsewhere (label, status text, icon) because it has no accessible name.\n\n### When to use\n\n- Use `mdc-marker` to draw attention to important content, signal priority, or visually categorise items inside a layout.\n- Use it as a decorative companion to a list item, card, or content block — never as the sole signal for the information.\n\n### When not to use\n\n- Use a label, status text, or an icon when the meaning must be announced to screen readers — the marker alone is invisible to assistive technology.\n- Use `mdc-divider` for separating content sections rather than highlighting a single block.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/marker/index.js\"; // custom element registration\n// or via React wrapper\nimport { Marker } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (solid marker next to a card title):\n\n```html\n<div class=\"row\">\n  <mdc-marker variant=\"solid\"></mdc-marker>\n  <mdc-text type=\"body-midsize-bold\">Priority item</mdc-text>\n</div>\n```\n\nStriped marker for higher-attention treatment:\n\n```html\n<mdc-marker variant=\"striped\"></mdc-marker>\n```\n\nCustomise the colour and width with CSS custom properties:\n\n```html\n<mdc-marker\n  variant=\"solid\"\n  style=\"--mdc-marker-solid-background-color: var(--mds-color-theme-text-error); --mdc-marker-width: 0.5rem;\"\n></mdc-marker>\n```\n\n### Content guidance\n\n- Always pair the marker with text or an icon that conveys the same meaning — the colour and pattern alone are not accessible.\n- Reserve the `striped` variant for the highest-priority or most urgent treatment so it retains its visual weight.\n- Use the CSS custom properties (`--mdc-marker-solid-background-color`, `--mdc-marker-striped-color`, `--mdc-marker-striped-background-color`, `--mdc-marker-width`) to map the marker into a theme's colour tokens.\n\n### Property/Attribute details\n\n- `variant` — `solid` (default; single coloured line) or `striped` (line with diagonal stripes for higher-attention treatment).\n\n### Limitations\n\n- The marker is decorative and has no accessible name. It is invisible to screen readers and must always be accompanied by a text or icon affordance that carries the same meaning.",
      "Accessibility": "### Built-in features\n\nThe marker has no role, no accessible name, and no keyboard or focus behaviour — it is a purely decorative bar. Screen readers skip it.\n\n### Implementation requirements\n\n#### General\n\n- Always pair the marker with a text label, status string, or icon that conveys the same meaning. Do not rely on colour or pattern alone.\n- Ensure sufficient colour contrast between the marker and the surrounding background so sighted users with low vision can perceive it."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "variant",
          "attribute": "variant",
          "type": "radio",
          "default": "solid",
          "values": [
            "solid",
            "striped"
          ],
          "description": "`solid` (default; single coloured line) or `striped` (line with diagonal stripes for higher-attention treatment).",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-marker-solid-background-color",
          "description": "Background color for solid variant markers."
        },
        {
          "name": "--mdc-marker-striped-color",
          "description": "Stripe color for striped variant markers."
        },
        {
          "name": "--mdc-marker-striped-background-color",
          "description": "Background color for striped variant markers."
        },
        {
          "name": "--mdc-marker-width",
          "description": "Width (thickness) of the marker line."
        }
      ],
      "rules": [
        "`solid` (default; single coloured line) or `striped` (line with diagonal stripes for higher-attention treatment)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/marker/index.js\"; // custom element registration\n// or via React wrapper\nimport { Marker } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<div class=\"row\">\n  <mdc-marker variant=\"solid\"></mdc-marker>\n  <mdc-text type=\"body-midsize-bold\">Priority item</mdc-text>\n</div>"
      },
      {
        "language": "html",
        "code": "<mdc-marker variant=\"striped\"></mdc-marker>"
      },
      {
        "language": "html",
        "code": "<mdc-marker\n  variant=\"solid\"\n  style=\"--mdc-marker-solid-background-color: var(--mds-color-theme-text-error); --mdc-marker-width: 0.5rem;\"\n></mdc-marker>"
      }
    ],
    "reactImports": [
      "Marker"
    ],
    "constants": {
      "MARKER_VARIANTS": {
        "SOLID": "solid",
        "STRIPED": "striped"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "alongside",
      "and",
      "blocks",
      "cards",
      "component",
      "content",
      "convey",
      "for",
      "guidelines",
      "indicator",
      "items",
      "list",
      "marker",
      "mdc",
      "or",
      "overview",
      "solid",
      "status",
      "striped",
      "the",
      "thin",
      "to",
      "usage",
      "used",
      "vertical",
      "visual"
    ]
  },
  {
    "name": "menubar",
    "title": "Menubar",
    "tagName": "mdc-menubar",
    "summary": "Usage, guidelines, and accessibility for the mdc-menubar component — a vertical container for menu items with keyboard navigation and nested submenu support.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/menubar",
      "knowledgeBasePath": "components/src/components/menubar/knowledge-base/menubar.component.md"
    },
    "docs": "## Overview\n\n`mdc-menubar` is a navigational menu container that holds a fixed list of menu items and manages keyboard navigation, focus, and the opening of nested submenus. It wraps its children in an internal group so screen readers announce the menu structure correctly.\n\n### When to use\n\n- Use `mdc-menubar` when grouping a set of `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` entries that may also expose nested submenus via `mdc-menupopover`.\n- Use it as the root for a persistent menu region (for example, the navigation tree inside `mdc-sidenavigation`).\n\n### When not to use\n\n- Do not use `mdc-menubar` for a single overlay menu opened from a trigger button — use `mdc-menupopover` directly.\n- Do not use it for selection lists where users pick a value to submit with a form — use `mdc-listbox` with `mdc-option` (typically inside `mdc-select`).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menubar/index.js\";\nimport \"@momentum-design/components/dist/components/menuitem/index.js\";\nimport \"@momentum-design/components/dist/components/menupopover/index.js\";\nimport { MenuBar, MenuItem, MenuPopover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menubar>\n  <mdc-menuitem id=\"file-menu\" label=\"File\" arrow-position=\"trailing\"></mdc-menuitem>\n  <mdc-menupopover triggerid=\"file-menu\">\n    <mdc-menuitem label=\"New\"></mdc-menuitem>\n    <mdc-menuitem label=\"Open\"></mdc-menuitem>\n  </mdc-menupopover>\n\n  <mdc-menuitem id=\"edit-menu\" label=\"Edit\"></mdc-menuitem>\n</mdc-menubar>\n```\n\n- Each `mdc-menuitem` that opens a submenu must have a unique `id` matching the sibling `mdc-menupopover`'s `triggerid`.\n- Items may optionally be grouped inside `mdc-menusection`; sections inside a menubar receive a gradient divider variant automatically.\n\n### Property/Attribute details\n\n- The component reads no public attributes of its own. Navigation behaviour is driven by the keyboard handling described below and configured through the surrounding `mdc-spatialnavigationprovider` keyboard navigation mode.\n- Each direct or grouped `mdc-menuitem` is tracked as a navigable entry; items with `disabled` are skipped, items with `soft-disabled` remain focusable but do not open their submenu.\n\n### Notes\n\n- The component automatically applies `divider-variant=\"gradient\"` to any direct child `mdc-menusection` after mount.\n- All submenu popovers attached via `triggerid` are forced to `placement=\"right-start\"` so nested menus open consistently.\n\n## Accessibility\n\n### Built-in features\n\n- Manages `role=\"menubar\"` on itself and `aria-orientation=\"vertical\"`.\n- Implements a roving tabindex: the first enabled menu item is tabbable (`tabindex=\"0\"`) and all others are `tabindex=\"-1\"`. Arrow keys move focus and update the tab stop.\n- Closes any other sibling submenu when a different top-level menu item is activated, so only one branch is open at a time.\n- Supports the following keys on focused items:\n  - `Home` — focus the first item.\n  - `End` — focus the last item.\n  - `ArrowUp` / `ArrowDown` — move focus between items. In the default keyboard navigation mode focus loops; with arrow-mode navigation it clamps at the ends.\n  - `ArrowRight` — on a top-level item with a submenu, open the submenu; from inside a nested submenu, close back to the parent and open the next top-level submenu.\n  - `ArrowLeft` — from inside a submenu, close it and focus the previous top-level item (opening its submenu if it has one).\n- Items marked `soft-disabled` are focusable but their submenu will not open via keyboard or click.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menubar` |\n| host | aria-orientation | `vertical` |\n| each menu item | tabindex | `0` for the active item, `-1` for the rest |\n| nested `mdc-menupopover` | placement | `right-start` |\n\n### Implementation requirements\n\n#### General\n\n- Place only `mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`, or `mdc-menusection` (containing those items) as direct children.\n- Pair each item that opens a submenu with an `mdc-menupopover` whose `triggerid` matches the item's `id`.\n\n#### Labelling\n\n- The menubar itself does not render visible text; provide an `aria-label` on the host element if the surrounding context does not already describe the menu.\n- Each menu item is responsible for its own accessible name via `label`, slotted text, or `aria-label`.",
    "sections": {
      "Overview": "`mdc-menubar` is a navigational menu container that holds a fixed list of menu items and manages keyboard navigation, focus, and the opening of nested submenus. It wraps its children in an internal group so screen readers announce the menu structure correctly.\n\n### When to use\n\n- Use `mdc-menubar` when grouping a set of `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` entries that may also expose nested submenus via `mdc-menupopover`.\n- Use it as the root for a persistent menu region (for example, the navigation tree inside `mdc-sidenavigation`).\n\n### When not to use\n\n- Do not use `mdc-menubar` for a single overlay menu opened from a trigger button — use `mdc-menupopover` directly.\n- Do not use it for selection lists where users pick a value to submit with a form — use `mdc-listbox` with `mdc-option` (typically inside `mdc-select`).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menubar/index.js\";\nimport \"@momentum-design/components/dist/components/menuitem/index.js\";\nimport \"@momentum-design/components/dist/components/menupopover/index.js\";\nimport { MenuBar, MenuItem, MenuPopover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menubar>\n  <mdc-menuitem id=\"file-menu\" label=\"File\" arrow-position=\"trailing\"></mdc-menuitem>\n  <mdc-menupopover triggerid=\"file-menu\">\n    <mdc-menuitem label=\"New\"></mdc-menuitem>\n    <mdc-menuitem label=\"Open\"></mdc-menuitem>\n  </mdc-menupopover>\n\n  <mdc-menuitem id=\"edit-menu\" label=\"Edit\"></mdc-menuitem>\n</mdc-menubar>\n```\n\n- Each `mdc-menuitem` that opens a submenu must have a unique `id` matching the sibling `mdc-menupopover`'s `triggerid`.\n- Items may optionally be grouped inside `mdc-menusection`; sections inside a menubar receive a gradient divider variant automatically.\n\n### Property/Attribute details\n\n- The component reads no public attributes of its own. Navigation behaviour is driven by the keyboard handling described below and configured through the surrounding `mdc-spatialnavigationprovider` keyboard navigation mode.\n- Each direct or grouped `mdc-menuitem` is tracked as a navigable entry; items with `disabled` are skipped, items with `soft-disabled` remain focusable but do not open their submenu.\n\n### Notes\n\n- The component automatically applies `divider-variant=\"gradient\"` to any direct child `mdc-menusection` after mount.\n- All submenu popovers attached via `triggerid` are forced to `placement=\"right-start\"` so nested menus open consistently.",
      "Accessibility": "### Built-in features\n\n- Manages `role=\"menubar\"` on itself and `aria-orientation=\"vertical\"`.\n- Implements a roving tabindex: the first enabled menu item is tabbable (`tabindex=\"0\"`) and all others are `tabindex=\"-1\"`. Arrow keys move focus and update the tab stop.\n- Closes any other sibling submenu when a different top-level menu item is activated, so only one branch is open at a time.\n- Supports the following keys on focused items:\n  - `Home` — focus the first item.\n  - `End` — focus the last item.\n  - `ArrowUp` / `ArrowDown` — move focus between items. In the default keyboard navigation mode focus loops; with arrow-mode navigation it clamps at the ends.\n  - `ArrowRight` — on a top-level item with a submenu, open the submenu; from inside a nested submenu, close back to the parent and open the next top-level submenu.\n  - `ArrowLeft` — from inside a submenu, close it and focus the previous top-level item (opening its submenu if it has one).\n- Items marked `soft-disabled` are focusable but their submenu will not open via keyboard or click.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menubar` |\n| host | aria-orientation | `vertical` |\n| each menu item | tabindex | `0` for the active item, `-1` for the rest |\n| nested `mdc-menupopover` | placement | `right-start` |\n\n### Implementation requirements\n\n#### General\n\n- Place only `mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`, or `mdc-menusection` (containing those items) as direct children.\n- Pair each item that opens a submenu with an `mdc-menupopover` whose `triggerid` matches the item's `id`.\n\n#### Labelling\n\n- The menubar itself does not render visible text; provide an `aria-label` on the host element if the surrounding context does not already describe the menu.\n- Each menu item is responsible for its own accessible name via `label`, slotted text, or `aria-label`."
    },
    "dependencies": [],
    "api": {
      "properties": [],
      "slots": [
        {
          "name": "default",
          "description": "Contains the menu items and their associated popovers"
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/menubar/index.js\";\nimport \"@momentum-design/components/dist/components/menuitem/index.js\";\nimport \"@momentum-design/components/dist/components/menupopover/index.js\";\nimport { MenuBar, MenuItem, MenuPopover } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-menubar>\n  <mdc-menuitem id=\"file-menu\" label=\"File\" arrow-position=\"trailing\"></mdc-menuitem>\n  <mdc-menupopover triggerid=\"file-menu\">\n    <mdc-menuitem label=\"New\"></mdc-menuitem>\n    <mdc-menuitem label=\"Open\"></mdc-menuitem>\n  </mdc-menupopover>\n\n  <mdc-menuitem id=\"edit-menu\" label=\"Edit\"></mdc-menuitem>\n</mdc-menubar>"
      }
    ],
    "reactImports": [
      "MenuBar",
      "MenuItem",
      "MenuPopover"
    ],
    "constants": {
      "DEFAULTS": {
        "ORIENTATION": "vertical"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "container",
      "for",
      "guidelines",
      "items",
      "keyboard",
      "mdc",
      "menu",
      "menubar",
      "navigation",
      "nested",
      "overview",
      "submenu",
      "support",
      "the",
      "usage",
      "vertical",
      "with"
    ]
  },
  {
    "name": "menuitem",
    "title": "Menuitem",
    "tagName": "mdc-menuitem",
    "summary": "Usage, guidelines, and accessibility for the mdc-menuitem component — an actionable item inside a menubar or menu popover with leading/trailing controls and an optional arrow indicator.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/menuitem",
      "knowledgeBasePath": "components/src/components/menuitem/knowledge-base/menuitem.component.md"
    },
    "docs": "## Overview\n\n`mdc-menuitem` is an actionable entry placed inside a `mdc-menubar` or `mdc-menupopover`. It exposes leading and trailing slots for controls and text, supports an optional arrow indicator for items that open submenus, and emits a click event when activated by mouse, `Enter`, or `Space`.\n\n### When to use\n\n- Use `mdc-menuitem` for the individual entries inside a menubar or menu popover that perform an action or open a submenu.\n- Use it as the trigger element for a nested `mdc-menupopover` by giving it a unique `id` and pointing the popover's `triggerid` at it.\n\n### When not to use\n\n- Do not use `mdc-menuitem` for toggleable state — use `mdc-menuitemcheckbox` or `mdc-menuitemradio`.\n- Do not use it inside `mdc-listbox`/`mdc-select` (selection lists) — use `mdc-option` there.\n- Do not use it as a stand-alone clickable row outside of a `menu`/`menubar`/`menupopover` context; use `mdc-listitem` for generic lists.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menuitem/index.js\";\nimport { MenuItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menuitem\n  label=\"Open\"\n  name=\"file-action\"\n  value=\"open\"\n></mdc-menuitem>\n\n<mdc-menuitem id=\"share-menu\" label=\"Share\" arrow-position=\"trailing\"></mdc-menuitem>\n<mdc-menupopover triggerid=\"share-menu\">...</mdc-menupopover>\n```\n\nAlways place `mdc-menuitem` inside an ancestor element with `role=\"menu\"` or `role=\"menubar\"` (typically `mdc-menupopover` or `mdc-menubar`), even when there is only a single item, so assistive technology announces it correctly.\n\n### Content guidance\n\n- Use the `label` attribute (or `leading-text-primary-label` slot) for the primary text and `secondary-label`/`tertiary-label` for supporting detail.\n- Use the trailing slots (`trailing-text-side-header`, `trailing-text-subline`, `trailing-controls`) for metadata such as shortcuts, status, or badges.\n- If the item text is long enough to truncate, connect an `mdc-tooltip` via its `triggerid` to the menu item's `id` to expose the full text on hover and focus. The component does not create the tooltip automatically.\n\n### Property/Attribute details\n\n- `label`, `secondary-label`, `tertiary-label` — primary, secondary, and tertiary text rendered on the leading side. May be replaced by slotting custom text into `leading-text-primary-label`, `leading-text-secondary-label`, `leading-text-tertiary-label`.\n- `side-header-text`, `subline-text` — trailing text. May be replaced by slotting custom text into `trailing-text-side-header`, `trailing-text-subline`.\n- `name` — identifier used to identify the menu item when it is selected.\n- `value` — value associated with the menu item when it is selected. Especially useful for checkbox or radio variants but available on every menu item.\n- `arrow-position` — `'leading' | 'trailing'`. When set, an arrow icon is rendered on the chosen side. Defaults to no arrow.\n- `arrow-direction` — `'positive' | 'negative'`. Controls which way the arrow points. With `positive`, leading arrows point left and trailing arrows point right; with `negative`, the directions invert.\n- `disabled` — disables interaction entirely and removes the item from keyboard navigation; clicks and key presses are stopped before they bubble.\n- `soft-disabled` — appears disabled but remains focusable and reachable. Consumers must ensure that the item does not trigger unintended actions while soft-disabled.\n- `active` — visual-only flag that styles the item as active without changing focus or ARIA state.\n- `data-index` — zero-based index used by virtualized lists; mirrored to `aria-posinset`.\n- Events emitted: `click`, `keydown`, `keyup`, `focus`, `enabled`, `disabled`, `created`, `destroyed`.\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"menuitem\"` on the host and adopts the inset-rectangle list-item visual variant.\n- `Enter` triggers a synthetic click on `keydown` (matching native behaviour); `Space` triggers the click on `keyup` and prevents page scroll.\n- When `disabled` is set, the host becomes `tabindex=\"-1\"`, `aria-disabled=\"true\"`, and click/keyboard events are stopped before reaching consumers.\n- Any controls slotted into `leading-controls`/`trailing-controls` have their `disabled` attribute mirrored from the host while the menu item is `disabled` or `soft-disabled`.\n- `data-index` is reflected to `aria-posinset` so virtualized usages announce correct position.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitem` |\n| host | tabindex | `0` when active, `-1` when disabled (parent menu controls focus) |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled`, otherwise `false` |\n| host | aria-posinset | reflects `data-index + 1` when `data-index` is set |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menuitem` inside an `mdc-menubar`, `mdc-menupopover`, or other element with `role=\"menu\"` / `role=\"menubar\"` so the surrounding navigation, focus management, and roving tabindex are applied.\n- For items that open submenus, give the item a unique `id`, set `arrow-position=\"trailing\"`, and create a sibling `mdc-menupopover` whose `triggerid` matches the `id`.\n\n#### Labelling\n\n- Always provide an accessible name through `label`, slotted text in `leading-text-primary-label`, or `aria-label` on the host.\n- When the item contains only icons or non-text content, set `aria-label` explicitly.",
    "sections": {
      "Overview": "`mdc-menuitem` is an actionable entry placed inside a `mdc-menubar` or `mdc-menupopover`. It exposes leading and trailing slots for controls and text, supports an optional arrow indicator for items that open submenus, and emits a click event when activated by mouse, `Enter`, or `Space`.\n\n### When to use\n\n- Use `mdc-menuitem` for the individual entries inside a menubar or menu popover that perform an action or open a submenu.\n- Use it as the trigger element for a nested `mdc-menupopover` by giving it a unique `id` and pointing the popover's `triggerid` at it.\n\n### When not to use\n\n- Do not use `mdc-menuitem` for toggleable state — use `mdc-menuitemcheckbox` or `mdc-menuitemradio`.\n- Do not use it inside `mdc-listbox`/`mdc-select` (selection lists) — use `mdc-option` there.\n- Do not use it as a stand-alone clickable row outside of a `menu`/`menubar`/`menupopover` context; use `mdc-listitem` for generic lists.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menuitem/index.js\";\nimport { MenuItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menuitem\n  label=\"Open\"\n  name=\"file-action\"\n  value=\"open\"\n></mdc-menuitem>\n\n<mdc-menuitem id=\"share-menu\" label=\"Share\" arrow-position=\"trailing\"></mdc-menuitem>\n<mdc-menupopover triggerid=\"share-menu\">...</mdc-menupopover>\n```\n\nAlways place `mdc-menuitem` inside an ancestor element with `role=\"menu\"` or `role=\"menubar\"` (typically `mdc-menupopover` or `mdc-menubar`), even when there is only a single item, so assistive technology announces it correctly.\n\n### Content guidance\n\n- Use the `label` attribute (or `leading-text-primary-label` slot) for the primary text and `secondary-label`/`tertiary-label` for supporting detail.\n- Use the trailing slots (`trailing-text-side-header`, `trailing-text-subline`, `trailing-controls`) for metadata such as shortcuts, status, or badges.\n- If the item text is long enough to truncate, connect an `mdc-tooltip` via its `triggerid` to the menu item's `id` to expose the full text on hover and focus. The component does not create the tooltip automatically.\n\n### Property/Attribute details\n\n- `label`, `secondary-label`, `tertiary-label` — primary, secondary, and tertiary text rendered on the leading side. May be replaced by slotting custom text into `leading-text-primary-label`, `leading-text-secondary-label`, `leading-text-tertiary-label`.\n- `side-header-text`, `subline-text` — trailing text. May be replaced by slotting custom text into `trailing-text-side-header`, `trailing-text-subline`.\n- `name` — identifier used to identify the menu item when it is selected.\n- `value` — value associated with the menu item when it is selected. Especially useful for checkbox or radio variants but available on every menu item.\n- `arrow-position` — `'leading' | 'trailing'`. When set, an arrow icon is rendered on the chosen side. Defaults to no arrow.\n- `arrow-direction` — `'positive' | 'negative'`. Controls which way the arrow points. With `positive`, leading arrows point left and trailing arrows point right; with `negative`, the directions invert.\n- `disabled` — disables interaction entirely and removes the item from keyboard navigation; clicks and key presses are stopped before they bubble.\n- `soft-disabled` — appears disabled but remains focusable and reachable. Consumers must ensure that the item does not trigger unintended actions while soft-disabled.\n- `active` — visual-only flag that styles the item as active without changing focus or ARIA state.\n- `data-index` — zero-based index used by virtualized lists; mirrored to `aria-posinset`.\n- Events emitted: `click`, `keydown`, `keyup`, `focus`, `enabled`, `disabled`, `created`, `destroyed`.",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"menuitem\"` on the host and adopts the inset-rectangle list-item visual variant.\n- `Enter` triggers a synthetic click on `keydown` (matching native behaviour); `Space` triggers the click on `keyup` and prevents page scroll.\n- When `disabled` is set, the host becomes `tabindex=\"-1\"`, `aria-disabled=\"true\"`, and click/keyboard events are stopped before reaching consumers.\n- Any controls slotted into `leading-controls`/`trailing-controls` have their `disabled` attribute mirrored from the host while the menu item is `disabled` or `soft-disabled`.\n- `data-index` is reflected to `aria-posinset` so virtualized usages announce correct position.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitem` |\n| host | tabindex | `0` when active, `-1` when disabled (parent menu controls focus) |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled`, otherwise `false` |\n| host | aria-posinset | reflects `data-index + 1` when `data-index` is set |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menuitem` inside an `mdc-menubar`, `mdc-menupopover`, or other element with `role=\"menu\"` / `role=\"menubar\"` so the surrounding navigation, focus management, and roving tabindex are applied.\n- For items that open submenus, give the item a unique `id`, set `arrow-position=\"trailing\"`, and create a sibling `mdc-menupopover` whose `triggerid` matches the `id`.\n\n#### Labelling\n\n- Always provide an accessible name through `label`, slotted text in `leading-text-primary-label`, or `aria-label` on the host.\n- When the item contains only icons or non-text content, set `aria-label` explicitly."
    },
    "dependencies": [
      "mdc-text",
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "arrowDirection",
          "attribute": "arrow-direction",
          "type": "select",
          "values": [
            "positive",
            "negative"
          ],
          "description": "`'positive' | 'negative'`. Controls which way the arrow points. With `positive`, leading arrows point left and trailing arrows point right; with `negative`, the directions invert.",
          "source": "storybook"
        },
        {
          "name": "arrowPosition",
          "attribute": "arrow-position",
          "type": "select",
          "values": [
            "leading",
            "trailing"
          ],
          "description": "`'leading' | 'trailing'`. When set, an arrow icon is rendered on the chosen side. Defaults to no arrow.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "disables interaction entirely and removes the item from keyboard navigation; clicks and key presses are stopped before they bubble.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "secondaryLabel",
          "attribute": "secondary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "sideHeaderText",
          "attribute": "side-header-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "appears disabled but remains focusable and reachable. Consumers must ensure that the item does not trigger unintended actions while soft-disabled.",
          "source": "storybook"
        },
        {
          "name": "sublineText",
          "attribute": "subline-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "tertiaryLabel",
          "attribute": "tertiary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "leading-controls",
          "description": "slot for menu item controls to appear of leading end."
        },
        {
          "name": "leading-text-primary-label",
          "description": "slot for menu item primary label."
        },
        {
          "name": "leading-text-secondary-label",
          "description": "slot for menu item secondary label."
        },
        {
          "name": "leading-text-tertiary-label",
          "description": "slot for menu item tertiary label."
        },
        {
          "name": "trailing-text-side-header",
          "description": "slot for menu item side header text."
        },
        {
          "name": "trailing-text-subline",
          "description": "slot for menu item subline text."
        },
        {
          "name": "trailing-controls",
          "description": "slot for menu item controls to appear of trailing end."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the menuitem is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the menuitem."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the menuitem."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the menuitem receives focus."
        },
        {
          "name": "enabled",
          "description": "(React: onEnabled) This event is dispatched after the menuitem is enabled"
        },
        {
          "name": "disabled",
          "description": "(React: onDisabled) This event is dispatched after the menuitem is disabled"
        },
        {
          "name": "created",
          "description": "(React: onCreated) This event is dispatched after the menuitem is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) This event is dispatched after the menuitem is destroyed (removed from the DOM)"
        }
      ],
      "cssParts": [
        {
          "name": "leading",
          "description": "Allows customization of the leading part."
        },
        {
          "name": "leading-arrow",
          "description": "Allows customization of leading arrow icon."
        },
        {
          "name": "leading-text",
          "description": "Allows customization of the leading text part."
        },
        {
          "name": "trailing",
          "description": "Allows customization of the trailing part."
        },
        {
          "name": "trailing-arrow",
          "description": "Allows customization of trailing arrow icon."
        },
        {
          "name": "trailing-text",
          "description": "Allows customization of the trailing text part."
        }
      ],
      "cssProperties": [],
      "rules": [
        "identifier used to identify the menu item when it is selected.",
        "value associated with the menu item when it is selected. Especially useful for checkbox or radio variants but available on every menu item.",
        "`'leading' | 'trailing'`. When set, an arrow icon is rendered on the chosen side. Defaults to no arrow.",
        "`'positive' | 'negative'`. Controls which way the arrow points. With `positive`, leading arrows point left and trailing arrows point right; with `negative`, the directions invert.",
        "disables interaction entirely and removes the item from keyboard navigation; clicks and key presses are stopped before they bubble.",
        "appears disabled but remains focusable and reachable. Consumers must ensure that the item does not trigger unintended actions while soft-disabled.",
        "visual-only flag that styles the item as active without changing focus or ARIA state.",
        "zero-based index used by virtualized lists; mirrored to `aria-posinset`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/menuitem/index.js\";\nimport { MenuItem } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-menuitem\n  label=\"Open\"\n  name=\"file-action\"\n  value=\"open\"\n></mdc-menuitem>\n\n<mdc-menuitem id=\"share-menu\" label=\"Share\" arrow-position=\"trailing\"></mdc-menuitem>\n<mdc-menupopover triggerid=\"share-menu\">...</mdc-menupopover>"
      }
    ],
    "reactImports": [
      "MenuItem"
    ],
    "constants": {
      "ARROW_ICONS": {
        "LEFT": "'arrow-left-bold' as Extract<IconNames, 'arrow-left-bold'>",
        "RIGHT": "'arrow-right-bold' as Extract<IconNames, 'arrow-right-bold'>"
      },
      "ARROW_DIRECTIONS": {
        "POSITIVE": "positive",
        "NEGATIVE": "negative"
      },
      "ARROW_POSITIONS": {
        "LEADING": "leading",
        "TRAILING": "trailing"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "actionable",
      "an",
      "and",
      "arrow",
      "component",
      "controls",
      "for",
      "guidelines",
      "icon",
      "indicator",
      "inside",
      "item",
      "leading",
      "mdc",
      "menu",
      "menubar",
      "menuitem",
      "optional",
      "or",
      "overview",
      "popover",
      "text",
      "the",
      "trailing",
      "usage",
      "with"
    ]
  },
  {
    "name": "menuitemcheckbox",
    "title": "Menuitemcheckbox",
    "tagName": "mdc-menuitemcheckbox",
    "summary": "Usage, guidelines, and accessibility for the mdc-menuitemcheckbox component — a checkable menu item with checkbox, checkmark, or toggle indicators.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/menuitemcheckbox",
      "knowledgeBasePath": "components/src/components/menuitemcheckbox/knowledge-base/menuitemcheckbox.component.md"
    },
    "docs": "## Overview\n\n`mdc-menuitemcheckbox` is a checkable menu item used inside a `mdc-menupopover`, `mdc-menubar`, or `mdc-menusection`. Its checked state can be toggled independently of other items in the same group and is reflected to assistive technology through `aria-checked`. Visual feedback is provided by a checkbox (leading), toggle (trailing), checkmark (trailing), or no indicator at all.\n\n### When to use\n\n- Use `mdc-menuitemcheckbox` for menu entries that toggle an independent on/off setting.\n- Use it when several items in the same menu can be checked at the same time.\n\n### When not to use\n\n- Use `mdc-menuitemradio` when only one item in a group may be selected.\n- Use `mdc-menuitem` for actions that do not have a checked state.\n- Do not use `mdc-checkbox` directly inside a menu; the dedicated `mdc-menuitemcheckbox` provides the correct role and keyboard behaviour.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menuitemcheckbox/index.js\";\nimport { MenuItemCheckbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menupopover triggerid=\"view-trigger\">\n  <mdc-menuitemcheckbox\n    label=\"Show sidebar\"\n    name=\"view\"\n    value=\"sidebar\"\n    indicator=\"checkbox\"\n    checked\n  ></mdc-menuitemcheckbox>\n  <mdc-menuitemcheckbox\n    label=\"Show notifications\"\n    indicator=\"toggle\"\n  ></mdc-menuitemcheckbox>\n</mdc-menupopover>\n```\n\nThe component should not contain its own focusable descendants — interaction must happen on the menu item itself.\n\n### Property/Attribute details\n\n- `checked` — boolean. Defaults to `false`. Toggled on click unless `control-type=\"controlled\"`. Reflected to `aria-checked`.\n- `indicator` — `'checkbox' | 'checkmark' | 'toggle' | 'none'`. Defaults to `'checkbox'`.\n  - `checkbox` renders an `mdc-staticcheckbox` on the leading side.\n  - `toggle` renders a compact `mdc-statictoggle` on the trailing side.\n  - `checkmark` renders a checkmark icon on the trailing side, visible only when `checked` is `true`.\n  - `none` renders no indicator; consumers are responsible for surfacing the state in another accessible way.\n- `control-type` — `'controlled' | 'uncontrolled'` (from the shared control-type provider). In `controlled` mode the component does not flip `checked` on click; consumers are expected to react to the `change` event and update `checked` themselves.\n- `name`, `value` — identify the item when it is part of a group selection.\n- `disabled`, `soft-disabled` — disable the item. `disabled` blocks interaction and removes the item from tab order; `soft-disabled` keeps it focusable but does not trigger the toggle.\n- `label`, `secondary-label`, `tertiary-label`, `side-header-text`, `subline-text` — text rendered in the leading/trailing slots; each may also be slotted directly.\n- `arrow-position`, `arrow-direction` — optional arrow icon on the leading or trailing edge.\n- Events: `change` (after click toggles state, even in controlled mode), `click`, `focus`.\n\n### Notes\n\n- The checkbox indicator is always positioned on the leading side; toggle and checkmark indicators are always trailing — these positions are fixed by the component.\n- Choose `indicator=\"none\"` only when you provide a custom visual indicator yourself and have ensured that indicator is accessible.\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"menuitemcheckbox\"` on the host and keeps `aria-checked` in sync with the `checked` property.\n- `Enter` and `Space` toggle the checked state (in `uncontrolled` mode) and dispatch a bubbling `change` event.\n- Calling `.click()` programmatically is a no-op while `disabled` or `soft-disabled`.\n- When `disabled`/`soft-disabled`, `aria-disabled` is set and slotted controls are disabled in sync.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitemcheckbox` |\n| host | aria-checked | mirrors `checked` (`'true'` / `'false'`) |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled` |\n| host | tabindex | `-1` when `disabled`; parent menu manages the active tab stop otherwise |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menuitemcheckbox` inside an `mdc-menupopover`, `mdc-menubar`, or `mdc-menusection` so the surrounding menu manages roving tabindex and keyboard navigation.\n- Do not place focusable elements inside the item; render any visuals via the static checkbox/toggle/checkmark indicators or non-interactive content.\n\n#### Labelling\n\n- Provide an accessible name via `label`, slotted text in `leading-text-primary-label`, or `aria-label`.\n- When using `indicator=\"none\"`, ensure the alternative visual is reflected for assistive technology (for example via `aria-label` updates).",
    "sections": {
      "Overview": "`mdc-menuitemcheckbox` is a checkable menu item used inside a `mdc-menupopover`, `mdc-menubar`, or `mdc-menusection`. Its checked state can be toggled independently of other items in the same group and is reflected to assistive technology through `aria-checked`. Visual feedback is provided by a checkbox (leading), toggle (trailing), checkmark (trailing), or no indicator at all.\n\n### When to use\n\n- Use `mdc-menuitemcheckbox` for menu entries that toggle an independent on/off setting.\n- Use it when several items in the same menu can be checked at the same time.\n\n### When not to use\n\n- Use `mdc-menuitemradio` when only one item in a group may be selected.\n- Use `mdc-menuitem` for actions that do not have a checked state.\n- Do not use `mdc-checkbox` directly inside a menu; the dedicated `mdc-menuitemcheckbox` provides the correct role and keyboard behaviour.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menuitemcheckbox/index.js\";\nimport { MenuItemCheckbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menupopover triggerid=\"view-trigger\">\n  <mdc-menuitemcheckbox\n    label=\"Show sidebar\"\n    name=\"view\"\n    value=\"sidebar\"\n    indicator=\"checkbox\"\n    checked\n  ></mdc-menuitemcheckbox>\n  <mdc-menuitemcheckbox\n    label=\"Show notifications\"\n    indicator=\"toggle\"\n  ></mdc-menuitemcheckbox>\n</mdc-menupopover>\n```\n\nThe component should not contain its own focusable descendants — interaction must happen on the menu item itself.\n\n### Property/Attribute details\n\n- `checked` — boolean. Defaults to `false`. Toggled on click unless `control-type=\"controlled\"`. Reflected to `aria-checked`.\n- `indicator` — `'checkbox' | 'checkmark' | 'toggle' | 'none'`. Defaults to `'checkbox'`.\n  - `checkbox` renders an `mdc-staticcheckbox` on the leading side.\n  - `toggle` renders a compact `mdc-statictoggle` on the trailing side.\n  - `checkmark` renders a checkmark icon on the trailing side, visible only when `checked` is `true`.\n  - `none` renders no indicator; consumers are responsible for surfacing the state in another accessible way.\n- `control-type` — `'controlled' | 'uncontrolled'` (from the shared control-type provider). In `controlled` mode the component does not flip `checked` on click; consumers are expected to react to the `change` event and update `checked` themselves.\n- `name`, `value` — identify the item when it is part of a group selection.\n- `disabled`, `soft-disabled` — disable the item. `disabled` blocks interaction and removes the item from tab order; `soft-disabled` keeps it focusable but does not trigger the toggle.\n- `label`, `secondary-label`, `tertiary-label`, `side-header-text`, `subline-text` — text rendered in the leading/trailing slots; each may also be slotted directly.\n- `arrow-position`, `arrow-direction` — optional arrow icon on the leading or trailing edge.\n- Events: `change` (after click toggles state, even in controlled mode), `click`, `focus`.\n\n### Notes\n\n- The checkbox indicator is always positioned on the leading side; toggle and checkmark indicators are always trailing — these positions are fixed by the component.\n- Choose `indicator=\"none\"` only when you provide a custom visual indicator yourself and have ensured that indicator is accessible.",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"menuitemcheckbox\"` on the host and keeps `aria-checked` in sync with the `checked` property.\n- `Enter` and `Space` toggle the checked state (in `uncontrolled` mode) and dispatch a bubbling `change` event.\n- Calling `.click()` programmatically is a no-op while `disabled` or `soft-disabled`.\n- When `disabled`/`soft-disabled`, `aria-disabled` is set and slotted controls are disabled in sync.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitemcheckbox` |\n| host | aria-checked | mirrors `checked` (`'true'` / `'false'`) |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled` |\n| host | tabindex | `-1` when `disabled`; parent menu manages the active tab stop otherwise |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menuitemcheckbox` inside an `mdc-menupopover`, `mdc-menubar`, or `mdc-menusection` so the surrounding menu manages roving tabindex and keyboard navigation.\n- Do not place focusable elements inside the item; render any visuals via the static checkbox/toggle/checkmark indicators or non-interactive content.\n\n#### Labelling\n\n- Provide an accessible name via `label`, slotted text in `leading-text-primary-label`, or `aria-label`.\n- When using `indicator=\"none\"`, ensure the alternative visual is reflected for assistive technology (for example via `aria-label` updates)."
    },
    "dependencies": [
      "mdc-staticcheckbox",
      "mdc-statictoggle",
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "boolean. Defaults to `false`. Toggled on click unless `control-type=\"controlled\"`. Reflected to `aria-checked`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "controlType",
          "attribute": "control-type",
          "type": "select",
          "values": [],
          "description": "`'controlled' | 'uncontrolled'` (from the shared control-type provider). In `controlled` mode the component does not flip `checked` on click; consumers are expected to react to the `change` event and update `checked` themselves.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "indicator",
          "attribute": "indicator",
          "type": "select",
          "default": "'checkbox'",
          "values": [
            "none",
            "checkbox",
            "checkmark",
            "toggle"
          ],
          "description": "`'checkbox' | 'checkmark' | 'toggle' | 'none'`. Defaults to `'checkbox'`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "secondaryLabel",
          "attribute": "secondary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "leading-controls",
          "description": "slot for menu item checkbox controls to appear of leading end."
        },
        {
          "name": "leading-text-primary-label",
          "description": "slot for menu item checkbox primary label."
        },
        {
          "name": "leading-text-secondary-label",
          "description": "slot for menu item checkbox secondary label."
        },
        {
          "name": "leading-text-tertiary-label",
          "description": "slot for menu item checkbox tertiary label."
        },
        {
          "name": "trailing-text-side-header",
          "description": "slot for menu item checkbox side header text."
        },
        {
          "name": "trailing-text-subline",
          "description": "slot for menu item checkbox subline text."
        },
        {
          "name": "trailing-controls",
          "description": "slot for menu item checkbox controls to appear of trailing end."
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the menuitemcheckbox changes."
        },
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the menuitemcheckbox is clicked."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the menuitemcheckbox receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "checkmark-icon",
          "description": "Allows customization of the checkmark icon."
        },
        {
          "name": "leading",
          "description": "Allows customization of the leading part."
        },
        {
          "name": "leading-arrow",
          "description": "Allows customization of leading arrow icon."
        },
        {
          "name": "leading-text",
          "description": "Allows customization of the leading text part."
        },
        {
          "name": "trailing",
          "description": "Allows customization of the trailing part."
        },
        {
          "name": "trailing-arrow",
          "description": "Allows customization of trailing arrow icon."
        },
        {
          "name": "trailing-text",
          "description": "Allows customization of the trailing text part."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-listitem-padding-left-and-right",
          "description": "Controls the padding on the left and right of the menuitemcheckbox."
        }
      ],
      "rules": [
        "boolean. Defaults to `false`. Toggled on click unless `control-type=\"controlled\"`. Reflected to `aria-checked`.",
        "`'checkbox' | 'checkmark' | 'toggle' | 'none'`. Defaults to `'checkbox'`.",
        "`'controlled' | 'uncontrolled'` (from the shared control-type provider). In `controlled` mode the component does not flip `checked` on click; consumers are expected to react to the `change` event and update `checked` themselves."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/menuitemcheckbox/index.js\";\nimport { MenuItemCheckbox } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-menupopover triggerid=\"view-trigger\">\n  <mdc-menuitemcheckbox\n    label=\"Show sidebar\"\n    name=\"view\"\n    value=\"sidebar\"\n    indicator=\"checkbox\"\n    checked\n  ></mdc-menuitemcheckbox>\n  <mdc-menuitemcheckbox\n    label=\"Show notifications\"\n    indicator=\"toggle\"\n  ></mdc-menuitemcheckbox>\n</mdc-menupopover>"
      }
    ],
    "reactImports": [
      "MenuItemCheckbox"
    ],
    "constants": {
      "INDICATOR": {
        "NONE": "none",
        "CHECKBOX": "checkbox",
        "CHECKMARK": "checkmark",
        "TOGGLE": "toggle"
      },
      "DEFAULTS": {
        "INDICATOR": "checkbox"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "checkable",
      "checkbox",
      "checkmark",
      "component",
      "for",
      "guidelines",
      "icon",
      "indicators",
      "item",
      "mdc",
      "menu",
      "menuitemcheckbox",
      "or",
      "overview",
      "staticcheckbox",
      "statictoggle",
      "the",
      "toggle",
      "usage",
      "with"
    ]
  },
  {
    "name": "menuitemradio",
    "title": "Menuitemradio",
    "tagName": "mdc-menuitemradio",
    "summary": "Usage, guidelines, and accessibility for the mdc-menuitemradio component — a single-select menu item with radio, checkmark, or no indicator.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/menuitemradio",
      "knowledgeBasePath": "components/src/components/menuitemradio/knowledge-base/menuitemradio.component.md"
    },
    "docs": "## Overview\n\n`mdc-menuitemradio` is a checkable menu item used for single-selection within a group of related items. When checked, the component automatically un-checks every other `mdc-menuitemradio` that shares the same `name` inside the closest `mdc-menusection` or `mdc-menupopover`.\n\n### When to use\n\n- Use `mdc-menuitemradio` for mutually exclusive options inside a menu (only one item in the group may be selected at a time).\n- Group related items by giving them the same `name` and placing them inside the same `mdc-menusection` or `mdc-menupopover`.\n\n### When not to use\n\n- Use `mdc-menuitemcheckbox` when more than one item in the group can be checked simultaneously.\n- Use `mdc-menuitem` for actions that do not have a selected state.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menuitemradio/index.js\";\nimport { MenuItemRadio } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menusection header-text=\"View density\">\n  <mdc-menuitemradio label=\"Compact\" name=\"density\" value=\"compact\" checked></mdc-menuitemradio>\n  <mdc-menuitemradio label=\"Default\" name=\"density\" value=\"default\"></mdc-menuitemradio>\n  <mdc-menuitemradio label=\"Comfortable\" name=\"density\" value=\"comfortable\"></mdc-menuitemradio>\n</mdc-menusection>\n```\n\nAll members of the same radio group must share the same `name` value and live inside the same `mdc-menusection` or `mdc-menupopover`. The component should not contain its own focusable descendants.\n\n### Property/Attribute details\n\n- `checked` — boolean, default `false`. Reflected to `aria-checked`. Clicking an unchecked radio sets `checked` to `true` and clears `checked` on the other radios in the group (unless `control-type=\"controlled\"`, in which case the consumer is responsible for updating state).\n- `indicator` — `'radio' | 'checkmark' | 'none'`. Defaults to `'radio'`.\n  - `radio` renders an `mdc-staticradio` on the leading side.\n  - `checkmark` renders a checkmark icon on the trailing side, visible only when `checked` is `true`.\n  - `none` renders no indicator; consumers must surface the state in another accessible way.\n- `control-type` — `'controlled' | 'uncontrolled'`. In controlled mode the component does not mutate `checked` or sibling state on click; it still emits a bubbling `change` event.\n- `name`, `value` — `name` identifies the radio group; `value` is the value associated with this entry.\n- `disabled`, `soft-disabled` — disable interaction. Clicks and key activations on disabled or soft-disabled radios do not change selection.\n- `label`, `secondary-label`, `tertiary-label`, `side-header-text`, `subline-text` — text rendered in the leading/trailing slots.\n- `arrow-position`, `arrow-direction` — optional arrow icon on the leading or trailing edge.\n- Events: `change` (after a click selects a new value), `click`, `focus`.\n\n### Notes\n\n- The radio indicator is always positioned on the leading side; the checkmark indicator is always trailing — these positions are fixed by the component.\n- Group membership is determined by `name` plus the closest `mdc-menusection` or `mdc-menupopover` ancestor; radios in different sections never affect each other even when their `name` matches.\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"menuitemradio\"` on the host and keeps `aria-checked` in sync with the `checked` property.\n- `Enter` and `Space` activate the radio (selecting it when not already checked) and dispatch a bubbling `change` event.\n- When a radio becomes checked, other radios in the same group within the same section/popover have their `checked` attribute removed automatically.\n- Clicks on a radio that is already checked are ignored — selection is single-direction.\n- `aria-disabled` reflects the disabled or soft-disabled state; slotted controls have their `disabled` attribute mirrored from the host.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitemradio` |\n| host | aria-checked | mirrors `checked` (`'true'` / `'false'`) |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled` |\n| host | tabindex | `-1` when `disabled`; parent menu manages the active tab stop otherwise |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menuitemradio` inside an `mdc-menupopover`, `mdc-menubar`, or `mdc-menusection` so the surrounding menu manages roving tabindex and keyboard navigation.\n- Group related radios by sharing the same `name` and keeping them inside the same `mdc-menusection` or `mdc-menupopover`.\n- Do not nest focusable elements inside the item.\n\n#### Labelling\n\n- Provide an accessible name via `label`, slotted text in `leading-text-primary-label`, or `aria-label`.\n- When grouping radios inside `mdc-menusection`, give the section a `header-text` so the group has a visible heading; the section forwards it to `aria-label` automatically.",
    "sections": {
      "Overview": "`mdc-menuitemradio` is a checkable menu item used for single-selection within a group of related items. When checked, the component automatically un-checks every other `mdc-menuitemradio` that shares the same `name` inside the closest `mdc-menusection` or `mdc-menupopover`.\n\n### When to use\n\n- Use `mdc-menuitemradio` for mutually exclusive options inside a menu (only one item in the group may be selected at a time).\n- Group related items by giving them the same `name` and placing them inside the same `mdc-menusection` or `mdc-menupopover`.\n\n### When not to use\n\n- Use `mdc-menuitemcheckbox` when more than one item in the group can be checked simultaneously.\n- Use `mdc-menuitem` for actions that do not have a selected state.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menuitemradio/index.js\";\nimport { MenuItemRadio } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menusection header-text=\"View density\">\n  <mdc-menuitemradio label=\"Compact\" name=\"density\" value=\"compact\" checked></mdc-menuitemradio>\n  <mdc-menuitemradio label=\"Default\" name=\"density\" value=\"default\"></mdc-menuitemradio>\n  <mdc-menuitemradio label=\"Comfortable\" name=\"density\" value=\"comfortable\"></mdc-menuitemradio>\n</mdc-menusection>\n```\n\nAll members of the same radio group must share the same `name` value and live inside the same `mdc-menusection` or `mdc-menupopover`. The component should not contain its own focusable descendants.\n\n### Property/Attribute details\n\n- `checked` — boolean, default `false`. Reflected to `aria-checked`. Clicking an unchecked radio sets `checked` to `true` and clears `checked` on the other radios in the group (unless `control-type=\"controlled\"`, in which case the consumer is responsible for updating state).\n- `indicator` — `'radio' | 'checkmark' | 'none'`. Defaults to `'radio'`.\n  - `radio` renders an `mdc-staticradio` on the leading side.\n  - `checkmark` renders a checkmark icon on the trailing side, visible only when `checked` is `true`.\n  - `none` renders no indicator; consumers must surface the state in another accessible way.\n- `control-type` — `'controlled' | 'uncontrolled'`. In controlled mode the component does not mutate `checked` or sibling state on click; it still emits a bubbling `change` event.\n- `name`, `value` — `name` identifies the radio group; `value` is the value associated with this entry.\n- `disabled`, `soft-disabled` — disable interaction. Clicks and key activations on disabled or soft-disabled radios do not change selection.\n- `label`, `secondary-label`, `tertiary-label`, `side-header-text`, `subline-text` — text rendered in the leading/trailing slots.\n- `arrow-position`, `arrow-direction` — optional arrow icon on the leading or trailing edge.\n- Events: `change` (after a click selects a new value), `click`, `focus`.\n\n### Notes\n\n- The radio indicator is always positioned on the leading side; the checkmark indicator is always trailing — these positions are fixed by the component.\n- Group membership is determined by `name` plus the closest `mdc-menusection` or `mdc-menupopover` ancestor; radios in different sections never affect each other even when their `name` matches.",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"menuitemradio\"` on the host and keeps `aria-checked` in sync with the `checked` property.\n- `Enter` and `Space` activate the radio (selecting it when not already checked) and dispatch a bubbling `change` event.\n- When a radio becomes checked, other radios in the same group within the same section/popover have their `checked` attribute removed automatically.\n- Clicks on a radio that is already checked are ignored — selection is single-direction.\n- `aria-disabled` reflects the disabled or soft-disabled state; slotted controls have their `disabled` attribute mirrored from the host.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitemradio` |\n| host | aria-checked | mirrors `checked` (`'true'` / `'false'`) |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled` |\n| host | tabindex | `-1` when `disabled`; parent menu manages the active tab stop otherwise |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menuitemradio` inside an `mdc-menupopover`, `mdc-menubar`, or `mdc-menusection` so the surrounding menu manages roving tabindex and keyboard navigation.\n- Group related radios by sharing the same `name` and keeping them inside the same `mdc-menusection` or `mdc-menupopover`.\n- Do not nest focusable elements inside the item.\n\n#### Labelling\n\n- Provide an accessible name via `label`, slotted text in `leading-text-primary-label`, or `aria-label`.\n- When grouping radios inside `mdc-menusection`, give the section a `header-text` so the group has a visible heading; the section forwards it to `aria-label` automatically."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-staticradio",
      "mdc-text",
      "mdc-tooltip"
    ],
    "api": {
      "properties": [
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "boolean, default `false`. Reflected to `aria-checked`. Clicking an unchecked radio sets `checked` to `true` and clears `checked` on the other radios in the group (unless `control-type=\"controlled\"`, in which case the consumer is responsible for updating state).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "controlType",
          "attribute": "control-type",
          "type": "select",
          "values": [],
          "description": "`'controlled' | 'uncontrolled'`. In controlled mode the component does not mutate `checked` or sibling state on click; it still emits a bubbling `change` event.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "indicator",
          "attribute": "indicator",
          "type": "select",
          "default": "'radio'",
          "values": [
            "none",
            "radio",
            "checkmark"
          ],
          "description": "`'radio' | 'checkmark' | 'none'`. Defaults to `'radio'`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "secondaryLabel",
          "attribute": "secondary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "leading-controls",
          "description": "slot for menu item radio controls to appear of leading end."
        },
        {
          "name": "leading-text-primary-label",
          "description": "slot for menu item radio primary label."
        },
        {
          "name": "leading-text-secondary-label",
          "description": "slot for menu item radio secondary label."
        },
        {
          "name": "leading-text-tertiary-label",
          "description": "slot for menu item radio tertiary label."
        },
        {
          "name": "trailing-text-side-header",
          "description": "slot for menu item radio side header text."
        },
        {
          "name": "trailing-text-subline",
          "description": "slot for menu item radio subline text."
        },
        {
          "name": "trailing-controls",
          "description": "slot for menu item radio controls to appear of trailing end."
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the menuitemradio changes."
        },
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the menuitemradio is clicked."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the menuitemradio receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "checkmark-icon",
          "description": "Allows customization of the checkmark icon."
        },
        {
          "name": "leading",
          "description": "Allows customization of the leading part."
        },
        {
          "name": "leading-arrow",
          "description": "Allows customization of leading arrow icon."
        },
        {
          "name": "leading-text",
          "description": "Allows customization of the leading text part."
        },
        {
          "name": "trailing",
          "description": "Allows customization of the trailing part."
        },
        {
          "name": "trailing-arrow",
          "description": "Allows customization of trailing arrow icon."
        },
        {
          "name": "trailing-text",
          "description": "Allows customization of the trailing text part."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-listitem-padding-left-and-right",
          "description": "Controls the padding on the left and right of the menuitemradio."
        }
      ],
      "rules": [
        "boolean, default `false`. Reflected to `aria-checked`. Clicking an unchecked radio sets `checked` to `true` and clears `checked` on the other radios in the group (unless `control-type=\"controlled\"`, in which case the consumer is responsible for updating state).",
        "`'radio' | 'checkmark' | 'none'`. Defaults to `'radio'`.",
        "`'controlled' | 'uncontrolled'`. In controlled mode the component does not mutate `checked` or sibling state on click; it still emits a bubbling `change` event."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/menuitemradio/index.js\";\nimport { MenuItemRadio } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-menusection header-text=\"View density\">\n  <mdc-menuitemradio label=\"Compact\" name=\"density\" value=\"compact\" checked></mdc-menuitemradio>\n  <mdc-menuitemradio label=\"Default\" name=\"density\" value=\"default\"></mdc-menuitemradio>\n  <mdc-menuitemradio label=\"Comfortable\" name=\"density\" value=\"comfortable\"></mdc-menuitemradio>\n</mdc-menusection>"
      }
    ],
    "reactImports": [
      "MenuItemRadio"
    ],
    "constants": {
      "INDICATOR": {
        "NONE": "none",
        "RADIO": "radio",
        "CHECKMARK": "checkmark"
      },
      "DEFAULTS": {
        "INDICATOR": "radio"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "checkmark",
      "component",
      "for",
      "guidelines",
      "icon",
      "indicator",
      "item",
      "mdc",
      "menu",
      "menuitemradio",
      "no",
      "or",
      "overview",
      "radio",
      "select",
      "single",
      "staticradio",
      "text",
      "the",
      "tooltip",
      "usage",
      "with"
    ]
  },
  {
    "name": "menupopover",
    "title": "Menupopover",
    "tagName": "mdc-menupopover",
    "summary": "Usage, guidelines, and accessibility for the mdc-menupopover component — a floating menu overlay for menu items and nested submenus.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/menupopover",
      "knowledgeBasePath": "components/src/components/menupopover/knowledge-base/menupopover.component.md"
    },
    "docs": "## Overview\n\n`mdc-menupopover` is a floating overlay that hosts menu items (`mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`) optionally grouped in `mdc-menusection`. It is anchored to a trigger element via the `triggerid` attribute and provides keyboard navigation, focus trapping, dismissal, and automatic submenu behaviour when nested inside another `mdc-menupopover` or used with `mdc-menubar`.\n\n### When to use\n\n- Use `mdc-menupopover` to surface a menu of actions from a trigger button or menu item.\n- Use it as a submenu by nesting it under another `mdc-menupopover` or by anchoring it to an `mdc-menuitem` inside an `mdc-menubar`.\n\n### When not to use\n\n- Do not use `mdc-menupopover` for generic non-menu overlays — use `mdc-popover` for those.\n- Do not use it to host a single non-menu item or arbitrary form controls; the contents should be menu items.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menupopover/index.js\";\nimport { MenuPopover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button id=\"actions-trigger\">Actions</mdc-button>\n<mdc-menupopover triggerid=\"actions-trigger\">\n  <mdc-menuitem label=\"Rename\"></mdc-menuitem>\n  <mdc-menuitem label=\"Duplicate\"></mdc-menuitem>\n  <mdc-menuitem id=\"more-trigger\" label=\"More\" arrow-position=\"trailing\"></mdc-menuitem>\n</mdc-menupopover>\n\n<mdc-menupopover triggerid=\"more-trigger\">\n  <mdc-menuitem label=\"Archive\"></mdc-menuitem>\n  <mdc-menuitem label=\"Delete\"></mdc-menuitem>\n</mdc-menupopover>\n```\n\nThe `triggerid` attribute must match the `id` of the element that opens the popover. Submenus are created by adding additional `mdc-menupopover` elements whose `triggerid` references the `id` of the parent menu item.\n\n### Property/Attribute details\n\n- `triggerid` — required. The `id` of the trigger element (button, menu item, etc.).\n- `placement` — default `bottom`. Accepts any of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. When the popover is nested inside an `mdc-menubar`, the menubar overrides this to `right-start`.\n- `visible` — controls the open state (default `false`). Use `.show()` / `.hide()` for programmatic control.\n- `trigger` — space-separated trigger events: `click`, `mouseenter`, `focusin`, `manual`. Defaults to `click`.\n- `offset`, `boundary`, `boundary-root`, `boundary-padding` — position tuning options forwarded to the underlying floating-ui layout engine.\n- `append-to`, `strategy`, `z-index`, `disable-flip`, `size`, `inline`, `animation-frame` — positioning and stacking controls.\n- `aria-labelledby`, `aria-describedby` — label/description ids for assistive technology.\n- The following options are forced when the component connects and should not be overridden: `role=\"menu\"`, `aria-orientation=\"vertical\"`, `backdrop=false` (set to `true` automatically when the trigger is not a menu item), `color=\"tonal\"`, `focus-trap=true`, `focus-back-to-trigger=true`, `hide-on-escape=true`, `hide-on-outside-click=true`, `interactive=true`, `show-arrow=false`, `close-button=false`.\n- Events: `change` (when a contained `mdc-menuitemcheckbox` or `mdc-menuitemradio` toggles), `action` (when an `mdc-menuitem` is selected and the menu closes), `shown`, `hidden`, `created`, `destroyed`.\n\n### Notes\n\n- The popover automatically switches its backdrop on when opened from a non-menu trigger (so a standalone trigger button gets a clickable backdrop), and keeps it off when opened from another menu item.\n- When the popover closes, any open submenus that it owns are closed recursively.\n- Pressing `Space` on a contained `mdc-menuitemcheckbox` or `mdc-menuitemradio` does not close the menu; pressing `Enter` on a non-trigger menu item dispatches `action` and closes all menus.\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"menu\"` on the host and `aria-orientation=\"vertical\"`.\n- Manages a roving tabindex across the contained menu items (including those slotted inside `mdc-menusection`); only one item is `tabindex=\"0\"` at a time, focus moves it as the user navigates.\n- Traps focus inside the popover while it is open and returns focus to the trigger element when it closes.\n- Supports the following keys on the contained items:\n  - `Home` / `End` — focus the first / last item.\n  - `ArrowDown` / `ArrowUp` — move focus to the next / previous item, looping at the ends.\n  - `ArrowRight` — open the submenu of the focused item if one exists.\n  - `ArrowLeft` — when this popover is itself a submenu, close it and return focus to the parent.\n  - `Escape` — close this popover (if it is the topmost overlay).\n  - `Enter` — activate the focused item and (unless it opens a submenu) close all menus; dispatches a bubbling `action` event from the item.\n  - `Space` — same as `Enter` for plain menu items; for checkbox/radio items it toggles state without closing the menu.\n- Clicking outside the popover or on the backdrop closes the popover (and any deeper submenus).\n- Items marked `soft-disabled` remain focusable but cannot be activated by click, `Enter`, or `Space`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menu` |\n| host | aria-orientation | `vertical` |\n| host | aria-modal | toggled by the underlying popover when used as an interactive dialog-like surface |\n| trigger element | aria-expanded | `true` while the popover is visible, `false` otherwise (unless `disable-aria-expanded` is set) |\n| trigger element | aria-haspopup | set automatically on the trigger |\n| each menu item | tabindex | `0` for the focused item, `-1` for the rest |\n\n### Implementation requirements\n\n#### General\n\n- The element referenced by `triggerid` must exist in the same root (document or shadow root) as the popover.\n- Wrap React renders of `mdc-menupopover` with `append-to` in a stable parent element (for example a `div`), not directly as a conditional fragment, to avoid `NotFoundError` when React tries to unmount a popover that has been moved by the `append-to` logic.\n- Submenu popovers must be siblings of their parent popover (or of the menubar), each anchored to its trigger menu item via `triggerid`.\n\n#### Labelling\n\n- Provide an `aria-label` or `aria-labelledby` on the trigger so that the trigger announces what the menu does. When the popover is interactive, set `aria-labelledby` on the popover itself if it does not inherit a usable name from the trigger.",
    "sections": {
      "Overview": "`mdc-menupopover` is a floating overlay that hosts menu items (`mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`) optionally grouped in `mdc-menusection`. It is anchored to a trigger element via the `triggerid` attribute and provides keyboard navigation, focus trapping, dismissal, and automatic submenu behaviour when nested inside another `mdc-menupopover` or used with `mdc-menubar`.\n\n### When to use\n\n- Use `mdc-menupopover` to surface a menu of actions from a trigger button or menu item.\n- Use it as a submenu by nesting it under another `mdc-menupopover` or by anchoring it to an `mdc-menuitem` inside an `mdc-menubar`.\n\n### When not to use\n\n- Do not use `mdc-menupopover` for generic non-menu overlays — use `mdc-popover` for those.\n- Do not use it to host a single non-menu item or arbitrary form controls; the contents should be menu items.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menupopover/index.js\";\nimport { MenuPopover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-button id=\"actions-trigger\">Actions</mdc-button>\n<mdc-menupopover triggerid=\"actions-trigger\">\n  <mdc-menuitem label=\"Rename\"></mdc-menuitem>\n  <mdc-menuitem label=\"Duplicate\"></mdc-menuitem>\n  <mdc-menuitem id=\"more-trigger\" label=\"More\" arrow-position=\"trailing\"></mdc-menuitem>\n</mdc-menupopover>\n\n<mdc-menupopover triggerid=\"more-trigger\">\n  <mdc-menuitem label=\"Archive\"></mdc-menuitem>\n  <mdc-menuitem label=\"Delete\"></mdc-menuitem>\n</mdc-menupopover>\n```\n\nThe `triggerid` attribute must match the `id` of the element that opens the popover. Submenus are created by adding additional `mdc-menupopover` elements whose `triggerid` references the `id` of the parent menu item.\n\n### Property/Attribute details\n\n- `triggerid` — required. The `id` of the trigger element (button, menu item, etc.).\n- `placement` — default `bottom`. Accepts any of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. When the popover is nested inside an `mdc-menubar`, the menubar overrides this to `right-start`.\n- `visible` — controls the open state (default `false`). Use `.show()` / `.hide()` for programmatic control.\n- `trigger` — space-separated trigger events: `click`, `mouseenter`, `focusin`, `manual`. Defaults to `click`.\n- `offset`, `boundary`, `boundary-root`, `boundary-padding` — position tuning options forwarded to the underlying floating-ui layout engine.\n- `append-to`, `strategy`, `z-index`, `disable-flip`, `size`, `inline`, `animation-frame` — positioning and stacking controls.\n- `aria-labelledby`, `aria-describedby` — label/description ids for assistive technology.\n- The following options are forced when the component connects and should not be overridden: `role=\"menu\"`, `aria-orientation=\"vertical\"`, `backdrop=false` (set to `true` automatically when the trigger is not a menu item), `color=\"tonal\"`, `focus-trap=true`, `focus-back-to-trigger=true`, `hide-on-escape=true`, `hide-on-outside-click=true`, `interactive=true`, `show-arrow=false`, `close-button=false`.\n- Events: `change` (when a contained `mdc-menuitemcheckbox` or `mdc-menuitemradio` toggles), `action` (when an `mdc-menuitem` is selected and the menu closes), `shown`, `hidden`, `created`, `destroyed`.\n\n### Notes\n\n- The popover automatically switches its backdrop on when opened from a non-menu trigger (so a standalone trigger button gets a clickable backdrop), and keeps it off when opened from another menu item.\n- When the popover closes, any open submenus that it owns are closed recursively.\n- Pressing `Space` on a contained `mdc-menuitemcheckbox` or `mdc-menuitemradio` does not close the menu; pressing `Enter` on a non-trigger menu item dispatches `action` and closes all menus.",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"menu\"` on the host and `aria-orientation=\"vertical\"`.\n- Manages a roving tabindex across the contained menu items (including those slotted inside `mdc-menusection`); only one item is `tabindex=\"0\"` at a time, focus moves it as the user navigates.\n- Traps focus inside the popover while it is open and returns focus to the trigger element when it closes.\n- Supports the following keys on the contained items:\n  - `Home` / `End` — focus the first / last item.\n  - `ArrowDown` / `ArrowUp` — move focus to the next / previous item, looping at the ends.\n  - `ArrowRight` — open the submenu of the focused item if one exists.\n  - `ArrowLeft` — when this popover is itself a submenu, close it and return focus to the parent.\n  - `Escape` — close this popover (if it is the topmost overlay).\n  - `Enter` — activate the focused item and (unless it opens a submenu) close all menus; dispatches a bubbling `action` event from the item.\n  - `Space` — same as `Enter` for plain menu items; for checkbox/radio items it toggles state without closing the menu.\n- Clicking outside the popover or on the backdrop closes the popover (and any deeper submenus).\n- Items marked `soft-disabled` remain focusable but cannot be activated by click, `Enter`, or `Space`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menu` |\n| host | aria-orientation | `vertical` |\n| host | aria-modal | toggled by the underlying popover when used as an interactive dialog-like surface |\n| trigger element | aria-expanded | `true` while the popover is visible, `false` otherwise (unless `disable-aria-expanded` is set) |\n| trigger element | aria-haspopup | set automatically on the trigger |\n| each menu item | tabindex | `0` for the focused item, `-1` for the rest |\n\n### Implementation requirements\n\n#### General\n\n- The element referenced by `triggerid` must exist in the same root (document or shadow root) as the popover.\n- Wrap React renders of `mdc-menupopover` with `append-to` in a stable parent element (for example a `div`), not directly as a conditional fragment, to avoid `NotFoundError` when React tries to unmount a popover that has been moved by the `append-to` logic.\n- Submenu popovers must be siblings of their parent popover (or of the menubar), each anchored to its trigger menu item via `triggerid`.\n\n#### Labelling\n\n- Provide an `aria-label` or `aria-labelledby` on the trigger so that the trigger announces what the menu does. When the popover is interactive, set `aria-labelledby` on the popover itself if it does not inherit a usable name from the trigger."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "appendTo",
          "attribute": "append-to",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaDescribedby",
          "attribute": "aria-describedby",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "delay",
          "attribute": "delay",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disableFlip",
          "attribute": "disable-flip",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "offset",
          "attribute": "offset",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "select",
          "default": "bottom",
          "values": [],
          "description": "default `bottom`. Accepts any of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. When the popover is nested inside an `mdc-menubar`, the menubar overrides this to `right-start`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "preventScroll",
          "attribute": "prevent-scroll",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "trigger",
          "attribute": "trigger",
          "type": "text",
          "values": [],
          "description": "space-separated trigger events: `click`, `mouseenter`, `focusin`, `manual`. Defaults to `click`.",
          "source": "storybook"
        },
        {
          "name": "triggerID",
          "attribute": "trigger-id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "visible",
          "attribute": "visible",
          "type": "boolean",
          "values": [],
          "description": "controls the open state (default `false`). Use `.show()` / `.hide()` for programmatic control.",
          "source": "storybook"
        },
        {
          "name": "zIndex",
          "attribute": "z-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for the menu popover content"
        },
        {
          "name": "default",
          "description": "Contains the menu items to be displayed in the popover"
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when a `menuitemcheckbox`, or `menuitemradio` changes."
        },
        {
          "name": "action",
          "description": "(React: onAction) This event is dispatched when a menuItem selected and the menu closes."
        }
      ],
      "cssParts": [
        {
          "name": "popover-close",
          "description": "The close button of the menupopover."
        },
        {
          "name": "popover-content",
          "description": "The content of the menupopover."
        },
        {
          "name": "popover-hover-bridge",
          "description": "The hover bridge of the menupopover."
        }
      ],
      "cssProperties": [],
      "rules": [
        "required. The `id` of the trigger element (button, menu item, etc.).",
        "default `bottom`. Accepts any of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. When the popover is nested inside an `mdc-menubar`, the menubar overrides this to `right-start`.",
        "controls the open state (default `false`). Use `.show()` / `.hide()` for programmatic control.",
        "space-separated trigger events: `click`, `mouseenter`, `focusin`, `manual`. Defaults to `click`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/menupopover/index.js\";\nimport { MenuPopover } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"actions-trigger\">Actions</mdc-button>\n<mdc-menupopover triggerid=\"actions-trigger\">\n  <mdc-menuitem label=\"Rename\"></mdc-menuitem>\n  <mdc-menuitem label=\"Duplicate\"></mdc-menuitem>\n  <mdc-menuitem id=\"more-trigger\" label=\"More\" arrow-position=\"trailing\"></mdc-menuitem>\n</mdc-menupopover>\n\n<mdc-menupopover triggerid=\"more-trigger\">\n  <mdc-menuitem label=\"Archive\"></mdc-menuitem>\n  <mdc-menuitem label=\"Delete\"></mdc-menuitem>\n</mdc-menupopover>"
      }
    ],
    "reactImports": [
      "MenuPopover"
    ],
    "constants": {
      "DEFAULTS": {
        "ORIENTATION": "vertical"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "floating",
      "for",
      "guidelines",
      "items",
      "mdc",
      "menu",
      "menupopover",
      "nested",
      "overlay",
      "overview",
      "submenus",
      "the",
      "usage"
    ]
  },
  {
    "name": "menusection",
    "title": "Menusection",
    "tagName": "mdc-menusection",
    "summary": "Usage, guidelines, and accessibility for the mdc-menusection component — a labelled grouping of menu items inside a menu or menubar.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/menusection",
      "knowledgeBasePath": "components/src/components/menusection/knowledge-base/menusection.component.md"
    },
    "docs": "## Overview\n\n`mdc-menusection` groups a set of menu items (`mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`) under an optional visible header, optional prefix icon, and optional divider. It also enforces single-selection semantics across any `mdc-menuitemradio` children that share a `name`.\n\n### When to use\n\n- Use `mdc-menusection` to label and visually separate related entries inside an `mdc-menupopover` or `mdc-menubar`.\n- Use it to scope a `mdc-menuitemradio` group: the section bounds the single-selection rule applied to radios with the same `name`.\n\n### When not to use\n\n- Do not nest `mdc-menusection` inside another `mdc-menusection`; sections are flat.\n- Do not use it outside of a menu context (`mdc-menupopover`, `mdc-menubar`, `mdc-sidenavigation`).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menusection/index.js\";\nimport { MenuSection } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menupopover triggerid=\"settings-trigger\">\n  <mdc-menusection header-text=\"Appearance\" show-divider>\n    <mdc-menuitemradio label=\"Light\" name=\"theme\" value=\"light\" checked></mdc-menuitemradio>\n    <mdc-menuitemradio label=\"Dark\" name=\"theme\" value=\"dark\"></mdc-menuitemradio>\n  </mdc-menusection>\n\n  <mdc-menusection header-text=\"Notifications\">\n    <mdc-menuitemcheckbox label=\"Mute\" name=\"mute\"></mdc-menuitemcheckbox>\n  </mdc-menusection>\n</mdc-menupopover>\n```\n\n### Property/Attribute details\n\n- `header-text` — visible heading rendered through an internal `mdc-listheader`. When set, it is also used as the section's `aria-label` if no explicit `aria-label` is supplied.\n- `aria-label` — accessible name for the group. Defaults to `header-text` when omitted.\n- `prefix-icon` — icon name displayed next to the header text inside the `mdc-listheader`.\n- `show-divider` — boolean. Renders an `mdc-divider` after the section content.\n- `divider-variant` — `'solid' | 'gradient'`, defaults to `'solid'`. When the section is a direct child of `mdc-menubar`, the menubar overrides this to `'gradient'`.\n- `hide-header-text` — internal flag used by `mdc-sidenavigation` to hide the header when the side navigation is collapsed; do not set this manually outside of that context.\n- Events: `change` (forwarded from any contained `mdc-menuitemcheckbox`/`mdc-menuitemradio`), `action` (forwarded from any contained `mdc-menuitem`).\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"group\"` on the host so assistive technology announces the items as a labelled grouping.\n- Mirrors `header-text` to `aria-label` automatically when no explicit label is provided.\n- When connected inside `mdc-sidenavigation`, automatically hides the visible header text while the side navigation is collapsed; the group's `aria-label` is retained.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `group` |\n| host | aria-label | mirrors `header-text` unless an explicit `aria-label` is set |\n| inner header container | aria-labelledby | references the internal header text id when `header-text` is present |\n| inner slot wrapper | role | `presentation` |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menusection` inside `mdc-menupopover`, `mdc-menubar`, or `mdc-sidenavigation` so the surrounding menu manages roving tabindex across the contained items.\n- Only place menu items (`mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`) in the default slot.\n\n#### Labelling\n\n- Provide a meaningful `header-text` whenever the grouping has a visible purpose; the component will mirror it to `aria-label` automatically.\n- When the section has no visible header, set `aria-label` explicitly so the group still has an accessible name.",
    "sections": {
      "Overview": "`mdc-menusection` groups a set of menu items (`mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`) under an optional visible header, optional prefix icon, and optional divider. It also enforces single-selection semantics across any `mdc-menuitemradio` children that share a `name`.\n\n### When to use\n\n- Use `mdc-menusection` to label and visually separate related entries inside an `mdc-menupopover` or `mdc-menubar`.\n- Use it to scope a `mdc-menuitemradio` group: the section bounds the single-selection rule applied to radios with the same `name`.\n\n### When not to use\n\n- Do not nest `mdc-menusection` inside another `mdc-menusection`; sections are flat.\n- Do not use it outside of a menu context (`mdc-menupopover`, `mdc-menubar`, `mdc-sidenavigation`).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/menusection/index.js\";\nimport { MenuSection } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-menupopover triggerid=\"settings-trigger\">\n  <mdc-menusection header-text=\"Appearance\" show-divider>\n    <mdc-menuitemradio label=\"Light\" name=\"theme\" value=\"light\" checked></mdc-menuitemradio>\n    <mdc-menuitemradio label=\"Dark\" name=\"theme\" value=\"dark\"></mdc-menuitemradio>\n  </mdc-menusection>\n\n  <mdc-menusection header-text=\"Notifications\">\n    <mdc-menuitemcheckbox label=\"Mute\" name=\"mute\"></mdc-menuitemcheckbox>\n  </mdc-menusection>\n</mdc-menupopover>\n```\n\n### Property/Attribute details\n\n- `header-text` — visible heading rendered through an internal `mdc-listheader`. When set, it is also used as the section's `aria-label` if no explicit `aria-label` is supplied.\n- `aria-label` — accessible name for the group. Defaults to `header-text` when omitted.\n- `prefix-icon` — icon name displayed next to the header text inside the `mdc-listheader`.\n- `show-divider` — boolean. Renders an `mdc-divider` after the section content.\n- `divider-variant` — `'solid' | 'gradient'`, defaults to `'solid'`. When the section is a direct child of `mdc-menubar`, the menubar overrides this to `'gradient'`.\n- `hide-header-text` — internal flag used by `mdc-sidenavigation` to hide the header when the side navigation is collapsed; do not set this manually outside of that context.\n- Events: `change` (forwarded from any contained `mdc-menuitemcheckbox`/`mdc-menuitemradio`), `action` (forwarded from any contained `mdc-menuitem`).",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"group\"` on the host so assistive technology announces the items as a labelled grouping.\n- Mirrors `header-text` to `aria-label` automatically when no explicit label is provided.\n- When connected inside `mdc-sidenavigation`, automatically hides the visible header text while the side navigation is collapsed; the group's `aria-label` is retained.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `group` |\n| host | aria-label | mirrors `header-text` unless an explicit `aria-label` is set |\n| inner header container | aria-labelledby | references the internal header text id when `header-text` is present |\n| inner slot wrapper | role | `presentation` |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-menusection` inside `mdc-menupopover`, `mdc-menubar`, or `mdc-sidenavigation` so the surrounding menu manages roving tabindex across the contained items.\n- Only place menu items (`mdc-menuitem`, `mdc-menuitemcheckbox`, `mdc-menuitemradio`) in the default slot.\n\n#### Labelling\n\n- Provide a meaningful `header-text` whenever the grouping has a visible purpose; the component will mirror it to `aria-label` automatically.\n- When the section has no visible header, set `aria-label` explicitly so the group still has an accessible name."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "accessible name for the group. Defaults to `header-text` when omitted.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "visible heading rendered through an internal `mdc-listheader`. When set, it is also used as the section's `aria-label` if no explicit `aria-label` is supplied.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "text",
          "values": [],
          "description": "icon name displayed next to the header text inside the `mdc-listheader`.",
          "source": "storybook"
        },
        {
          "name": "showDivider",
          "attribute": "show-divider",
          "type": "boolean",
          "values": [],
          "description": "boolean. Renders an `mdc-divider` after the section content.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for inserting `menuitem`, `menuitemcheckbox`, or `menuitemradio`"
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when a `menuitemcheckbox`, or `menuitemradio` changes."
        },
        {
          "name": "action",
          "description": "(React: onAction) This event is dispatched when a `menuitem` selected."
        }
      ],
      "cssParts": [
        {
          "name": "header",
          "description": "The header of the menusection."
        },
        {
          "name": "align-header",
          "description": "The header of the menusection when it is aligned to the start."
        },
        {
          "name": "container",
          "description": "The container of the menusection."
        },
        {
          "name": "divider",
          "description": "The divider of the menusection."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-menusection-divider-margin-block",
          "description": "Sets the margin block of the divider."
        },
        {
          "name": "--mdc-menusection-gap",
          "description": "Sets the gap between items in the menusection."
        },
        {
          "name": "--mdc-menusection-divider-width",
          "description": "Sets the width of the divider."
        },
        {
          "name": "--mdc-menusection-header-padding",
          "description": "Sets the padding of the header, if present."
        }
      ],
      "rules": [
        "visible heading rendered through an internal `mdc-listheader`. When set, it is also used as the section's `aria-label` if no explicit `aria-label` is supplied.",
        "accessible name for the group. Defaults to `header-text` when omitted.",
        "icon name displayed next to the header text inside the `mdc-listheader`.",
        "boolean. Renders an `mdc-divider` after the section content.",
        "`'solid' | 'gradient'`, defaults to `'solid'`. When the section is a direct child of `mdc-menubar`, the menubar overrides this to `'gradient'`.",
        "internal flag used by `mdc-sidenavigation` to hide the header when the side navigation is collapsed; do not set this manually outside of that context."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/menusection/index.js\";\nimport { MenuSection } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-menupopover triggerid=\"settings-trigger\">\n  <mdc-menusection header-text=\"Appearance\" show-divider>\n    <mdc-menuitemradio label=\"Light\" name=\"theme\" value=\"light\" checked></mdc-menuitemradio>\n    <mdc-menuitemradio label=\"Dark\" name=\"theme\" value=\"dark\"></mdc-menuitemradio>\n  </mdc-menusection>\n\n  <mdc-menusection header-text=\"Notifications\">\n    <mdc-menuitemcheckbox label=\"Mute\" name=\"mute\"></mdc-menuitemcheckbox>\n  </mdc-menusection>\n</mdc-menupopover>"
      }
    ],
    "reactImports": [
      "MenuSection"
    ],
    "constants": {
      "DEFAULTS": {
        "DIVIDER_VARIANT": "solid"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "for",
      "grouping",
      "guidelines",
      "inside",
      "items",
      "labelled",
      "mdc",
      "menu",
      "menubar",
      "menusection",
      "of",
      "or",
      "overview",
      "the",
      "usage"
    ]
  },
  {
    "name": "navmenuitem",
    "title": "Navmenuitem",
    "tagName": "mdc-navmenuitem",
    "summary": "Usage, guidelines, and accessibility for the mdc-navmenuitem component — a navigation-styled menu item with leading icon, optional badge, and tooltip support.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/navmenuitem",
      "knowledgeBasePath": "components/src/components/navmenuitem/knowledge-base/navmenuitem.component.md"
    },
    "docs": "## Overview\n\n`mdc-navmenuitem` is a `menuitem`-role component styled as a navigation tab, supporting a leading icon, optional badge, and dynamic text rendering. It can operate as a simple item, as a parent with a flyout submenu (`mdc-menupopover`), or as a parent with an inline dropdown submenu, and a tooltip can be attached for collapsed states or when a nested child is active.\n\n### When to use\n\n- Use `mdc-navmenuitem` for primary navigation entries inside `mdc-sidenavigation` (or an `mdc-menubar` used as a side navigation tree).\n- Use the active/inactive state to reflect which destination corresponds to the current route or view.\n\n### When not to use\n\n- Do not use `mdc-navmenuitem` for action menu items inside `mdc-menupopover`; use `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` there.\n- Do not use it as a stand-alone link or button outside a navigation context — use `mdc-link` or `mdc-button` instead.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/navmenuitem/index.js\";\nimport { NavMenuItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-navmenuitem\n  nav-id=\"inbox\"\n  icon-name=\"email-regular\"\n  label=\"Inbox\"\n  badge-type=\"counter\"\n  counter=\"3\"\n  tooltip-text=\"Inbox\"\n  active\n></mdc-navmenuitem>\n```\n\nA unique `nav-id` is required so the surrounding `mdc-sidenavigation` can manage active state and badge placement. When the navigation is collapsed and `tooltip-text` is provided, the component creates and attaches an `mdc-tooltip` as a sibling for the collapsed state automatically.\n\nThe component supports three usage configurations:\n\n1. **Simple navmenuitem** — Contains text and optionally an icon and/or badge. Can be active or inactive to reflect the current route.\n2. **Navmenuitem with flyout submenu** — Contains a nested `mdc-menupopover` that opens on hover or click (configurable). The parent receives `aria-haspopup=\"true\"` and the popover gets `role=\"menu\"`. When a child is active, the parent receives `active` styling but not `aria-current`, to avoid confusion for screen readers. If `is-active-parent-tooltip-text` is provided, a tooltip is shown on hover of the parent.\n3. **Navmenuitem with dropdown submenu** — Contains a sibling `div[data-trigger]` that opens on click. The parent receives `aria-expanded=\"true\"` when open and `aria-expanded=\"false\"` when closed. Active styling is only applied to the parent when the dropdown is closed; when open, only the active child carries active styling. If `is-active-parent-tooltip-text` is provided, a tooltip is shown on hover of the parent.\n\n### Property/Attribute details\n\n- `nav-id` — required unique identifier used by `mdc-sidenavigation` to track the item. If omitted, the component logs an error via `onerror`.\n- `active` — boolean. Reflects whether the item is currently selected. When `true`, the surrounding navigation will also set `aria-current=\"page\"` (unless `cannot-activate` or `disable-aria-current` are set).\n- `cannot-activate` — boolean, default `false`. Prevents the surrounding `mdc-sidenavigation` from toggling `active` when the item is clicked (use for items that act like external links or buttons that do not change the current page). The active styling can still be applied manually.\n- `disable-aria-current` — boolean. Keeps the visual active styling but suppresses the automatic `aria-current=\"page\"` attribute. Use when you manage `aria-current` yourself.\n- `icon-name` — leading icon name. The component automatically derives the filled icon variant used when the item is active.\n- `badge-type` — `'dot'` (notification) or `'counter'`. When omitted, no badge is rendered.\n- `counter` — number rendered in the counter badge.\n- `max-counter` — `9`, `99`, or `999`, default `99`. Values exceeding the max render as `N+`.\n- `show-label` — boolean. Controlled automatically by `mdc-sidenavigation` based on its expanded state for top-level items; nested items always show the label.\n- `aria-label` — explicit accessible name. When the navigation collapses (`show-label=false`) and no `aria-label` is set, the visible `label` is mirrored to `aria-label` so the icon-only state still announces correctly.\n- `tooltip-text` — text displayed in the auto-managed tooltip. Shown only when `show-label` is false (i.e. when the side navigation is collapsed) unless `tooltip-appearance=\"always\"`.\n- `tooltip-placement` — popover placement for the tooltip, default `right`.\n- `tooltip-type` — `'description' | 'label' | 'none'`. Choose `none` when an `aria-label` is already set so the tooltip does not duplicate it.\n- `tooltip-appearance` — `'when-collapsed'` (default) or `'always'`.\n- `tooltip-boundary-padding` — number of pixels of padding between the tooltip and the viewport edges; default `0`.\n- `is-active-parent-tooltip-text` — text displayed when this parent item has an active descendant in a nested navmenu (for example, `\"Messaging, contains active navmenuitem\"`).\n- Plus all menu-item attributes (`label`, `disabled`, `soft-disabled`, `arrow-position`, `arrow-direction`, `name`, `value`).\n- Events: `click`, `keydown`, `keyup`, `focus`, `activechange` (custom event with `detail: { navId, active }` dispatched after a non-disabled click that did not set `cannot-activate`).\n\n### Notes\n\n- `mdc-navmenuitem` is intended to be used inside `mdc-menubar` as part of `mdc-sidenavigation`. Its structure, spacing, and interactions are designed to align with the visual and functional requirements of side-navigation layouts.\n- The tooltip is created and removed programmatically as a sibling of the navmenuitem; it is re-rendered whenever `tooltip-text`, `show-label`, or the parent's active-child state changes. When `tooltip-text` is set, the component renders a tooltip on hover — particularly useful when the side navigation is collapsed or when an active navmenuitem is nested within a submenu.\n- When the item is nested under an `mdc-menupopover`, an internal `in-menupopover` attribute is added and the item always shows its label (the collapse logic only applies to top-level items inside `mdc-sidenavigation`).\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"menuitem\"` on the host and supports `Enter` (on `keydown`) and `Space` (on `keyup`) to trigger a click — matching native menu item activation semantics.\n- When used as a parent with a flyout submenu, sets `aria-haspopup=\"true\"` on the host and the `mdc-menupopover` receives `role=\"menu\"`. When a child is active, the parent receives `active` styling but not `aria-current`, to avoid confusion for screen readers.\n- When used as a parent with a dropdown submenu, sets `aria-expanded=\"true\"` when open and `aria-expanded=\"false\"` when closed. Active styling on the parent is suppressed while the dropdown is open so only the active child navmenuitem carries the indicator.\n- When the item becomes active, the surrounding navigation sets `aria-current=\"page\"` on it (unless `disable-aria-current` is set). The component also sets `aria-current=\"page\"` on initial render when `active` is already `true`.\n- When the navigation collapses, the component copies `label` to `aria-label` if no `aria-label` is already set, so the icon-only display remains announceable.\n- When `tooltip-text` or `is-active-parent-tooltip-text` is provided, the component automatically generates an `mdc-tooltip` sibling, assigns the navmenuitem a unique `id` if missing, and points the tooltip's `triggerid` at it.\n- Disables interaction (`disabled`) entirely or visually (`soft-disabled`) using the same rules as `mdc-menuitem`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitem` |\n| host | aria-current | `page` while `active` (unless `disable-aria-current` or `cannot-activate`) |\n| host | aria-haspopup | `true` when the host contains a flyout submenu (`mdc-menupopover`) |\n| host | aria-expanded | `true` when a dropdown submenu is open; `false` when closed |\n| host | aria-label | mirrors `label` when `show-label` is false and no explicit `aria-label` is set |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled` |\n| host | tabindex | managed by the parent navigation/menu roving tabindex |\n| generated tooltip | triggerid | matches the auto-assigned `id` of the navmenuitem |\n| generated tooltip | placement | mirrors `tooltip-placement` |\n\n### Implementation requirements\n\n#### General\n\n- Always provide a unique `nav-id`; the surrounding `mdc-sidenavigation` relies on it to coordinate active state and badge placement.\n- Use `cannot-activate` for items that should behave like buttons or external links rather than navigation destinations.\n- When the navigation can be collapsed, supply `tooltip-text` so collapsed items expose their name on hover/focus.\n\n#### Labelling\n\n- Provide a `label` for the visible text; set `aria-label` explicitly only when the visible label is not descriptive enough or when the item renders icon-only without a `tooltip-text`.\n- For a parent navmenuitem whose nested child can be active, provide `is-active-parent-tooltip-text` (for example `\"Messaging, contains active navmenuitem\"`) so the collapsed parent still communicates the active descendant.\n- Set `tooltip-type=\"none\"` when you have already supplied an explicit `aria-label` to avoid duplicate announcements.",
    "sections": {
      "Overview": "`mdc-navmenuitem` is a `menuitem`-role component styled as a navigation tab, supporting a leading icon, optional badge, and dynamic text rendering. It can operate as a simple item, as a parent with a flyout submenu (`mdc-menupopover`), or as a parent with an inline dropdown submenu, and a tooltip can be attached for collapsed states or when a nested child is active.\n\n### When to use\n\n- Use `mdc-navmenuitem` for primary navigation entries inside `mdc-sidenavigation` (or an `mdc-menubar` used as a side navigation tree).\n- Use the active/inactive state to reflect which destination corresponds to the current route or view.\n\n### When not to use\n\n- Do not use `mdc-navmenuitem` for action menu items inside `mdc-menupopover`; use `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` there.\n- Do not use it as a stand-alone link or button outside a navigation context — use `mdc-link` or `mdc-button` instead.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/navmenuitem/index.js\";\nimport { NavMenuItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-navmenuitem\n  nav-id=\"inbox\"\n  icon-name=\"email-regular\"\n  label=\"Inbox\"\n  badge-type=\"counter\"\n  counter=\"3\"\n  tooltip-text=\"Inbox\"\n  active\n></mdc-navmenuitem>\n```\n\nA unique `nav-id` is required so the surrounding `mdc-sidenavigation` can manage active state and badge placement. When the navigation is collapsed and `tooltip-text` is provided, the component creates and attaches an `mdc-tooltip` as a sibling for the collapsed state automatically.\n\nThe component supports three usage configurations:\n\n1. **Simple navmenuitem** — Contains text and optionally an icon and/or badge. Can be active or inactive to reflect the current route.\n2. **Navmenuitem with flyout submenu** — Contains a nested `mdc-menupopover` that opens on hover or click (configurable). The parent receives `aria-haspopup=\"true\"` and the popover gets `role=\"menu\"`. When a child is active, the parent receives `active` styling but not `aria-current`, to avoid confusion for screen readers. If `is-active-parent-tooltip-text` is provided, a tooltip is shown on hover of the parent.\n3. **Navmenuitem with dropdown submenu** — Contains a sibling `div[data-trigger]` that opens on click. The parent receives `aria-expanded=\"true\"` when open and `aria-expanded=\"false\"` when closed. Active styling is only applied to the parent when the dropdown is closed; when open, only the active child carries active styling. If `is-active-parent-tooltip-text` is provided, a tooltip is shown on hover of the parent.\n\n### Property/Attribute details\n\n- `nav-id` — required unique identifier used by `mdc-sidenavigation` to track the item. If omitted, the component logs an error via `onerror`.\n- `active` — boolean. Reflects whether the item is currently selected. When `true`, the surrounding navigation will also set `aria-current=\"page\"` (unless `cannot-activate` or `disable-aria-current` are set).\n- `cannot-activate` — boolean, default `false`. Prevents the surrounding `mdc-sidenavigation` from toggling `active` when the item is clicked (use for items that act like external links or buttons that do not change the current page). The active styling can still be applied manually.\n- `disable-aria-current` — boolean. Keeps the visual active styling but suppresses the automatic `aria-current=\"page\"` attribute. Use when you manage `aria-current` yourself.\n- `icon-name` — leading icon name. The component automatically derives the filled icon variant used when the item is active.\n- `badge-type` — `'dot'` (notification) or `'counter'`. When omitted, no badge is rendered.\n- `counter` — number rendered in the counter badge.\n- `max-counter` — `9`, `99`, or `999`, default `99`. Values exceeding the max render as `N+`.\n- `show-label` — boolean. Controlled automatically by `mdc-sidenavigation` based on its expanded state for top-level items; nested items always show the label.\n- `aria-label` — explicit accessible name. When the navigation collapses (`show-label=false`) and no `aria-label` is set, the visible `label` is mirrored to `aria-label` so the icon-only state still announces correctly.\n- `tooltip-text` — text displayed in the auto-managed tooltip. Shown only when `show-label` is false (i.e. when the side navigation is collapsed) unless `tooltip-appearance=\"always\"`.\n- `tooltip-placement` — popover placement for the tooltip, default `right`.\n- `tooltip-type` — `'description' | 'label' | 'none'`. Choose `none` when an `aria-label` is already set so the tooltip does not duplicate it.\n- `tooltip-appearance` — `'when-collapsed'` (default) or `'always'`.\n- `tooltip-boundary-padding` — number of pixels of padding between the tooltip and the viewport edges; default `0`.\n- `is-active-parent-tooltip-text` — text displayed when this parent item has an active descendant in a nested navmenu (for example, `\"Messaging, contains active navmenuitem\"`).\n- Plus all menu-item attributes (`label`, `disabled`, `soft-disabled`, `arrow-position`, `arrow-direction`, `name`, `value`).\n- Events: `click`, `keydown`, `keyup`, `focus`, `activechange` (custom event with `detail: { navId, active }` dispatched after a non-disabled click that did not set `cannot-activate`).\n\n### Notes\n\n- `mdc-navmenuitem` is intended to be used inside `mdc-menubar` as part of `mdc-sidenavigation`. Its structure, spacing, and interactions are designed to align with the visual and functional requirements of side-navigation layouts.\n- The tooltip is created and removed programmatically as a sibling of the navmenuitem; it is re-rendered whenever `tooltip-text`, `show-label`, or the parent's active-child state changes. When `tooltip-text` is set, the component renders a tooltip on hover — particularly useful when the side navigation is collapsed or when an active navmenuitem is nested within a submenu.\n- When the item is nested under an `mdc-menupopover`, an internal `in-menupopover` attribute is added and the item always shows its label (the collapse logic only applies to top-level items inside `mdc-sidenavigation`).",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"menuitem\"` on the host and supports `Enter` (on `keydown`) and `Space` (on `keyup`) to trigger a click — matching native menu item activation semantics.\n- When used as a parent with a flyout submenu, sets `aria-haspopup=\"true\"` on the host and the `mdc-menupopover` receives `role=\"menu\"`. When a child is active, the parent receives `active` styling but not `aria-current`, to avoid confusion for screen readers.\n- When used as a parent with a dropdown submenu, sets `aria-expanded=\"true\"` when open and `aria-expanded=\"false\"` when closed. Active styling on the parent is suppressed while the dropdown is open so only the active child navmenuitem carries the indicator.\n- When the item becomes active, the surrounding navigation sets `aria-current=\"page\"` on it (unless `disable-aria-current` is set). The component also sets `aria-current=\"page\"` on initial render when `active` is already `true`.\n- When the navigation collapses, the component copies `label` to `aria-label` if no `aria-label` is already set, so the icon-only display remains announceable.\n- When `tooltip-text` or `is-active-parent-tooltip-text` is provided, the component automatically generates an `mdc-tooltip` sibling, assigns the navmenuitem a unique `id` if missing, and points the tooltip's `triggerid` at it.\n- Disables interaction (`disabled`) entirely or visually (`soft-disabled`) using the same rules as `mdc-menuitem`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `menuitem` |\n| host | aria-current | `page` while `active` (unless `disable-aria-current` or `cannot-activate`) |\n| host | aria-haspopup | `true` when the host contains a flyout submenu (`mdc-menupopover`) |\n| host | aria-expanded | `true` when a dropdown submenu is open; `false` when closed |\n| host | aria-label | mirrors `label` when `show-label` is false and no explicit `aria-label` is set |\n| host | aria-disabled | `true` when `disabled` or `soft-disabled` |\n| host | tabindex | managed by the parent navigation/menu roving tabindex |\n| generated tooltip | triggerid | matches the auto-assigned `id` of the navmenuitem |\n| generated tooltip | placement | mirrors `tooltip-placement` |\n\n### Implementation requirements\n\n#### General\n\n- Always provide a unique `nav-id`; the surrounding `mdc-sidenavigation` relies on it to coordinate active state and badge placement.\n- Use `cannot-activate` for items that should behave like buttons or external links rather than navigation destinations.\n- When the navigation can be collapsed, supply `tooltip-text` so collapsed items expose their name on hover/focus.\n\n#### Labelling\n\n- Provide a `label` for the visible text; set `aria-label` explicitly only when the visible label is not descriptive enough or when the item renders icon-only without a `tooltip-text`.\n- For a parent navmenuitem whose nested child can be active, provide `is-active-parent-tooltip-text` (for example `\"Messaging, contains active navmenuitem\"`) so the collapsed parent still communicates the active descendant.\n- Set `tooltip-type=\"none\"` when you have already supplied an explicit `aria-label` to avoid duplicate announcements."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text",
      "mdc-badge",
      "mdc-tooltip"
    ],
    "api": {
      "properties": [
        {
          "name": "active",
          "attribute": "active",
          "type": "boolean",
          "values": [],
          "description": "boolean. Reflects whether the item is currently selected. When `true`, the surrounding navigation will also set `aria-current=\"page\"` (unless `cannot-activate` or `disable-aria-current` are set).",
          "source": "storybook"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "undefined",
          "values": [],
          "description": "explicit accessible name. When the navigation collapses (`show-label=false`) and no `aria-label` is set, the visible `label` is mirrored to `aria-label` so the icon-only state still announces correctly.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "badgeType",
          "attribute": "badge-type",
          "type": "radio",
          "values": [
            null
          ],
          "description": "`'dot'` (notification) or `'counter'`. When omitted, no badge is rendered.",
          "source": "storybook"
        },
        {
          "name": "cannotActivate",
          "attribute": "cannot-activate",
          "type": "boolean",
          "values": [],
          "description": "boolean, default `false`. Prevents the surrounding `mdc-sidenavigation` from toggling `active` when the item is clicked (use for items that act like external links or buttons that do not change the current page). The active styling can still be applied manually.",
          "source": "storybook"
        },
        {
          "name": "counter",
          "attribute": "counter",
          "type": "number",
          "values": [],
          "description": "number rendered in the counter badge.",
          "source": "storybook"
        },
        {
          "name": "disableAriaCurrent",
          "attribute": "disable-aria-current",
          "type": "boolean",
          "values": [],
          "description": "boolean. Keeps the visual active styling but suppresses the automatic `aria-current=\"page\"` attribute. Use when you manage `aria-current` yourself.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "hasActiveChild",
          "attribute": "tooltip-boundary-padding",
          "type": "boolean",
          "default": "0",
          "values": [],
          "description": "number of pixels of padding between the tooltip and the viewport edges; default `0`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "text",
          "values": [],
          "description": "leading icon name. The component automatically derives the filled icon variant used when the item is active.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "Label text displayed for the nav item.",
          "source": "storybook"
        },
        {
          "name": "maxCounter",
          "attribute": "max-counter",
          "type": "unknown",
          "default": "undefined",
          "values": [],
          "description": "`9`, `99`, or `999`, default `99`. Values exceeding the max render as `N+`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "navId",
          "attribute": "nav-id",
          "type": "text",
          "values": [],
          "description": "required unique identifier used by `mdc-sidenavigation` to track the item. If omitted, the component logs an error via `onerror`.",
          "source": "storybook"
        },
        {
          "name": "showLabel",
          "attribute": "show-label",
          "type": "boolean",
          "values": [],
          "description": "boolean. Controlled automatically by `mdc-sidenavigation` based on its expanded state for top-level items; nested items always show the label.",
          "source": "storybook"
        },
        {
          "name": "tooltipAppearance",
          "attribute": "tooltip-appearance",
          "type": "select",
          "default": "undefined",
          "values": [
            "when-collapsed",
            "always"
          ],
          "description": "`'when-collapsed'` (default) or `'always'`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "tooltipBoundaryPadding",
          "attribute": "tooltip-boundary-padding",
          "type": "number",
          "values": [],
          "description": "number of pixels of padding between the tooltip and the viewport edges; default `0`.",
          "source": "storybook"
        },
        {
          "name": "tooltipPlacement",
          "attribute": "tooltip-placement",
          "type": "select",
          "default": "undefined",
          "values": [],
          "description": "popover placement for the tooltip, default `right`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "tooltipText",
          "attribute": "tooltip-text",
          "type": "text",
          "values": [],
          "description": "text displayed in the auto-managed tooltip. Shown only when `show-label` is false (i.e. when the side navigation is collapsed) unless `tooltip-appearance=\"always\"`.",
          "source": "storybook"
        },
        {
          "name": "tooltipType",
          "attribute": "tooltip-type",
          "type": "select",
          "values": [
            "description",
            "label",
            "none"
          ],
          "description": "`'description' | 'label' | 'none'`. Choose `none` when an `aria-label` is already set so the tooltip does not duplicate it.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the navmenuitem is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the navmenuitem."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the navmenuitem."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the navmenuitem receives focus."
        },
        {
          "name": "activechange",
          "description": "(React: onActiveChange) Dispatched when the active state of the navmenuitem changes."
        }
      ],
      "cssParts": [
        {
          "name": "arrow",
          "description": "The arrow of the navmenuitem."
        },
        {
          "name": "badge",
          "description": "The badge of the navmenuitem."
        },
        {
          "name": "icon-container",
          "description": "The container of the icon."
        },
        {
          "name": "text-container",
          "description": "The container of the text."
        },
        {
          "name": "trailing-arrow",
          "description": "The trailing arrow of the navmenuitem when it has a flyout menu."
        },
        {
          "name": "trailing-arrow-dropdown",
          "description": "The trailing arrow of the navmenuitem when it has a dropdown menu."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-navmenuitem-in-sidenav-expanded-width",
          "description": "Width of the navmenuitem when expanded."
        },
        {
          "name": "--mdc-navmenuitem-in-sidenav-expanded-margin-left",
          "description": "Left margin of the navmenuitem, when expanded."
        },
        {
          "name": "--mdc-navmenuitem-in-sidenav-expanded-margin-right",
          "description": "Right margin of the navmenuitem, when expanded."
        },
        {
          "name": "--mdc-navmenuitem-in-sidenav-collapsed-width",
          "description": "Width of the navmenuitem when collapsed."
        },
        {
          "name": "--mdc-navmenuitem-in-sidenav-collapsed-margin-left",
          "description": "Left margin of the navmenuitem, when collapsed."
        },
        {
          "name": "--mdc-navmenuitem-in-sidenav-collapsed-margin-right",
          "description": "Right margin of the navmenuitem, when collapsed."
        },
        {
          "name": "--mdc-navmenuitem-color",
          "description": "Text color of the navmenuitem in its normal state."
        },
        {
          "name": "--mdc-navmenuitem-notch-color",
          "description": "Notch color of the navmenuitem (if applicable) when active."
        },
        {
          "name": "--mdc-navmenuitem-disabled-color",
          "description": "Text color of the navmenuitem when disabled."
        },
        {
          "name": "--mdc-navmenuitem-rest-active-background-color",
          "description": "Background color of the active nav item in its rest state."
        },
        {
          "name": "--mdc-navmenuitem-hover-background-color",
          "description": "Background color of the navmenuitem when hovered."
        },
        {
          "name": "--mdc-navmenuitem-hover-active-background-color",
          "description": "Background color of the active navmenuitem when hovered."
        },
        {
          "name": "--mdc-navmenuitem-pressed-background-color",
          "description": "Background color of the navmenuitem when pressed."
        },
        {
          "name": "--mdc-navmenuitem-pressed-active-background-color",
          "description": "Background color of the active navmenuitem when pressed."
        },
        {
          "name": "--mdc-navmenuitem-disabled-background-color",
          "description": "Background color of the navmenuitem when disabled."
        },
        {
          "name": "--mdc-navmenuitem-disabled-active-background-color",
          "description": "Background color of the active navmenuitem when disabled."
        }
      ],
      "rules": [
        "required unique identifier used by `mdc-sidenavigation` to track the item. If omitted, the component logs an error via `onerror`.",
        "boolean. Reflects whether the item is currently selected. When `true`, the surrounding navigation will also set `aria-current=\"page\"` (unless `cannot-activate` or `disable-aria-current` are set).",
        "boolean, default `false`. Prevents the surrounding `mdc-sidenavigation` from toggling `active` when the item is clicked (use for items that act like external links or buttons that do not change the current page). The active styling can still be applied manually.",
        "boolean. Keeps the visual active styling but suppresses the automatic `aria-current=\"page\"` attribute. Use when you manage `aria-current` yourself.",
        "leading icon name. The component automatically derives the filled icon variant used when the item is active.",
        "`'dot'` (notification) or `'counter'`. When omitted, no badge is rendered.",
        "number rendered in the counter badge.",
        "`9`, `99`, or `999`, default `99`. Values exceeding the max render as `N+`.",
        "boolean. Controlled automatically by `mdc-sidenavigation` based on its expanded state for top-level items; nested items always show the label.",
        "explicit accessible name. When the navigation collapses (`show-label=false`) and no `aria-label` is set, the visible `label` is mirrored to `aria-label` so the icon-only state still announces correctly.",
        "text displayed in the auto-managed tooltip. Shown only when `show-label` is false (i.e. when the side navigation is collapsed) unless `tooltip-appearance=\"always\"`.",
        "popover placement for the tooltip, default `right`.",
        "`'description' | 'label' | 'none'`. Choose `none` when an `aria-label` is already set so the tooltip does not duplicate it.",
        "`'when-collapsed'` (default) or `'always'`.",
        "number of pixels of padding between the tooltip and the viewport edges; default `0`.",
        "text displayed when this parent item has an active descendant in a nested navmenu (for example, `\"Messaging, contains active navmenuitem\"`)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/navmenuitem/index.js\";\nimport { NavMenuItem } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-navmenuitem\n  nav-id=\"inbox\"\n  icon-name=\"email-regular\"\n  label=\"Inbox\"\n  badge-type=\"counter\"\n  counter=\"3\"\n  tooltip-text=\"Inbox\"\n  active\n></mdc-navmenuitem>"
      }
    ],
    "reactImports": [
      "NavMenuItem"
    ],
    "constants": {
      "ALLOWED_BADGE_TYPES": {},
      "ICON_NAME": {
        "RIGHT_ARROW": "'arrow-right-bold' as Extract<IconNames, 'arrow-right-bold'>",
        "DOWN_ARROW": "'arrow-down-bold' as Extract<IconNames, 'arrow-down-bold'>"
      },
      "DEFAULTS": {
        "MAX_COUNTER": 99,
        "CANNOT_ACTIVATE": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "badge",
      "component",
      "for",
      "guidelines",
      "icon",
      "item",
      "leading",
      "mdc",
      "menu",
      "navigation",
      "navmenuitem",
      "optional",
      "overview",
      "styled",
      "support",
      "text",
      "the",
      "tooltip",
      "usage",
      "with"
    ]
  },
  {
    "name": "optgroup",
    "title": "Optgroup",
    "tagName": "mdc-optgroup",
    "summary": "Usage, guidelines, and accessibility for the mdc-optgroup component — a labelled grouping of mdc-option elements inside a listbox.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/optgroup",
      "knowledgeBasePath": "components/src/components/optgroup/knowledge-base/optgroup.component.md"
    },
    "docs": "## Overview\n\n`mdc-optgroup` groups a set of `mdc-option` elements under a shared label inside a listbox (typically the `mdc-selectlistbox` rendered by `mdc-select`). It exposes the grouping to assistive technology via `role=\"group\"` and an associated label.\n\n### When to use\n\n- Use `mdc-optgroup` to organise related options inside an `mdc-selectlistbox` or `mdc-listbox` (for example, grouping countries by continent).\n- Use it to disable a whole set of options at once via the `disabled` attribute.\n\n### When not to use\n\n- Do not use `mdc-optgroup` for grouping menu items — use `mdc-menusection` inside `mdc-menupopover` or `mdc-menubar`.\n- Do not nest `mdc-optgroup` inside another `mdc-optgroup`.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/optgroup/index.js\";\nimport { Optgroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\">\n  <mdc-optgroup label=\"Europe\">\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"fr\" label=\"France\"></mdc-option>\n  </mdc-optgroup>\n  <mdc-optgroup label=\"Asia\" disabled>\n    <mdc-option value=\"jp\" label=\"Japan\"></mdc-option>\n  </mdc-optgroup>\n</mdc-select>\n```\n\n### Property/Attribute details\n\n- `label` — visible heading rendered above the options. When set, the heading is also referenced as the group's accessible name via `aria-labelledby`.\n- `data-aria-label` — fallback accessible name applied to the inner group element when no `label` is provided.\n- `disabled` — boolean. When toggled, the component applies or removes the `disabled` attribute on every slotted `mdc-option` child.\n\n## Accessibility\n\n### Built-in features\n\n- Wraps slotted options in a `role=\"group\"` container.\n- When `label` is set, the heading text is referenced by `aria-labelledby` so screen readers announce the group label.\n- When `label` is not set, the value of `data-aria-label` is used as `aria-label` on the group container.\n- Cascades `disabled` to every slotted `mdc-option`, so disabling the group disables all of its options.\n- The inner `<slot>` is marked `role=\"presentation\"` so the visual grouping wrapper does not introduce additional semantics between the group and its options.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| inner group wrapper | role | `group` |\n| inner group wrapper | aria-labelledby | references the internal header text id when `label` is set |\n| inner group wrapper | aria-label | mirrors `data-aria-label` when `label` is not set |\n| inner slot | role | `presentation` |\n| each slotted `mdc-option` | disabled | reflects the optgroup's `disabled` state |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-optgroup` inside an `mdc-selectlistbox` (within `mdc-select`) or another listbox-style container.\n- Only place `mdc-option` elements in the default slot.\n\n#### Labelling\n\n- Provide a `label` whenever the grouping is visible in the UI; this also supplies the accessible group name automatically.\n- When the group has no visible label, supply `data-aria-label` so assistive technology can still announce the group.",
    "sections": {
      "Overview": "`mdc-optgroup` groups a set of `mdc-option` elements under a shared label inside a listbox (typically the `mdc-selectlistbox` rendered by `mdc-select`). It exposes the grouping to assistive technology via `role=\"group\"` and an associated label.\n\n### When to use\n\n- Use `mdc-optgroup` to organise related options inside an `mdc-selectlistbox` or `mdc-listbox` (for example, grouping countries by continent).\n- Use it to disable a whole set of options at once via the `disabled` attribute.\n\n### When not to use\n\n- Do not use `mdc-optgroup` for grouping menu items — use `mdc-menusection` inside `mdc-menupopover` or `mdc-menubar`.\n- Do not nest `mdc-optgroup` inside another `mdc-optgroup`.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/optgroup/index.js\";\nimport { Optgroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\">\n  <mdc-optgroup label=\"Europe\">\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"fr\" label=\"France\"></mdc-option>\n  </mdc-optgroup>\n  <mdc-optgroup label=\"Asia\" disabled>\n    <mdc-option value=\"jp\" label=\"Japan\"></mdc-option>\n  </mdc-optgroup>\n</mdc-select>\n```\n\n### Property/Attribute details\n\n- `label` — visible heading rendered above the options. When set, the heading is also referenced as the group's accessible name via `aria-labelledby`.\n- `data-aria-label` — fallback accessible name applied to the inner group element when no `label` is provided.\n- `disabled` — boolean. When toggled, the component applies or removes the `disabled` attribute on every slotted `mdc-option` child.",
      "Accessibility": "### Built-in features\n\n- Wraps slotted options in a `role=\"group\"` container.\n- When `label` is set, the heading text is referenced by `aria-labelledby` so screen readers announce the group label.\n- When `label` is not set, the value of `data-aria-label` is used as `aria-label` on the group container.\n- Cascades `disabled` to every slotted `mdc-option`, so disabling the group disables all of its options.\n- The inner `<slot>` is marked `role=\"presentation\"` so the visual grouping wrapper does not introduce additional semantics between the group and its options.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| inner group wrapper | role | `group` |\n| inner group wrapper | aria-labelledby | references the internal header text id when `label` is set |\n| inner group wrapper | aria-label | mirrors `data-aria-label` when `label` is not set |\n| inner slot | role | `presentation` |\n| each slotted `mdc-option` | disabled | reflects the optgroup's `disabled` state |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-optgroup` inside an `mdc-selectlistbox` (within `mdc-select`) or another listbox-style container.\n- Only place `mdc-option` elements in the default slot.\n\n#### Labelling\n\n- Provide a `label` whenever the grouping is visible in the UI; this also supplies the accessible group name automatically.\n- When the group has no visible label, supply `data-aria-label` so assistive technology can still announce the group."
    },
    "dependencies": [
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "fallback accessible name applied to the inner group element when no `label` is provided.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "boolean. When toggled, the component applies or removes the `disabled` attribute on every slotted `mdc-option` child.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible heading rendered above the options. When set, the heading is also referenced as the group's accessible name via `aria-labelledby`.",
          "source": "storybook"
        },
        {
          "name": "options",
          "attribute": "options",
          "type": "Array<HTMLElement>",
          "values": [],
          "description": "/\nclass OptGroup extends DataAriaLabelMixin(DisabledMixin(Component)) {\n/**\nThe header text to be displayed on the top of the options list.",
          "reflected": true,
          "source": "component"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default slot for mdc-option elements."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "header-text",
          "description": "The header text of the optgroup."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-optgroup-disabled-color",
          "description": "Allows customization of the disabled option color."
        }
      ],
      "rules": [
        "visible heading rendered above the options. When set, the heading is also referenced as the group's accessible name via `aria-labelledby`.",
        "fallback accessible name applied to the inner group element when no `label` is provided.",
        "boolean. When toggled, the component applies or removes the `disabled` attribute on every slotted `mdc-option` child."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/optgroup/index.js\";\nimport { Optgroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-select label=\"Country\">\n  <mdc-optgroup label=\"Europe\">\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"fr\" label=\"France\"></mdc-option>\n  </mdc-optgroup>\n  <mdc-optgroup label=\"Asia\" disabled>\n    <mdc-option value=\"jp\" label=\"Japan\"></mdc-option>\n  </mdc-optgroup>\n</mdc-select>"
      }
    ],
    "reactImports": [
      "Optgroup"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "elements",
      "for",
      "grouping",
      "guidelines",
      "inside",
      "labelled",
      "listbox",
      "mdc",
      "of",
      "optgroup",
      "option",
      "overview",
      "text",
      "the",
      "usage"
    ]
  },
  {
    "name": "option",
    "title": "Option",
    "tagName": "mdc-option",
    "summary": "Usage, guidelines, and accessibility for the mdc-option component — a selectable option inside a listbox, typically used within mdc-select.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/option",
      "knowledgeBasePath": "components/src/components/option/knowledge-base/option.component.md"
    },
    "docs": "## Overview\n\n`mdc-option` is a selectable entry rendered inside a listbox (`mdc-selectlistbox` / `mdc-listbox`), typically as a child of `mdc-select`. It supports a primary and secondary label, an optional prefix icon, and renders a checkmark on the trailing side when selected. The component participates in form submission through its associated form-control behaviour.\n\n### When to use\n\n- Use `mdc-option` for each selectable value inside an `mdc-select`, `mdc-selectlistbox`, or `mdc-listbox`.\n- Group related options together with `mdc-optgroup`.\n\n### When not to use\n\n- Do not use `mdc-option` inside menus — use `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` inside `mdc-menupopover`/`mdc-menubar`.\n- Do not use it as a stand-alone list row; use `mdc-listitem` for generic lists.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/option/index.js\";\nimport { Option } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\">\n  <mdc-option value=\"de\" label=\"Germany\" prefix-icon=\"flag-de-regular\"></mdc-option>\n  <mdc-option value=\"fr\" label=\"France\" prefix-icon=\"flag-fr-regular\" selected></mdc-option>\n</mdc-select>\n```\n\nIf the option label can be long enough to truncate, connect an `mdc-tooltip` via its `triggerid` to the option's `id` so the full text is exposed on hover and focus — the component does not create the tooltip automatically.\n\n### Property/Attribute details\n\n- `label`, `secondary-label` — primary and secondary text rendered on the leading side. May be replaced by slotting text into `leading-text-primary-label` / `leading-text-secondary-label`.\n- `value` — the submitted form value for the option.\n- `selected` — boolean, default `false`. Mirrors to `aria-selected` and renders the trailing checkmark icon. Selecting/unselecting also dispatches `selected` / `unselected` lifecycle events.\n- `prefix-icon` — icon name shown on the leading side. The icon is rendered via the leading-controls slot using `mdc-icon`.\n- `aria-label` — accessible name override. Useful when extra context (for example, a country code) should be announced even though it is not in the visible label.\n- `disabled` — boolean. When set, `aria-disabled=\"true\"` is reflected and the option is removed from tab order.\n- `soft-disabled` — boolean. Appears disabled but remains focusable.\n- `active` — visual-only flag for hover/active styling.\n- `tertiary-label`, `side-header-text`, `subline-text`, `name` — not used by `mdc-option`; the component explicitly clears `name`, `sideHeaderText`, and `sublineText` when connected.\n- Events: `click`, `keydown`, `keyup`, `focus`.\n\n### Notes\n\n- Only `label`, `secondary-label`, and `prefix-icon` are rendered; tertiary text, side header text, and subline text are intentionally not displayed for options.\n\n## Accessibility\n\n### Built-in features\n\n- Sets `role=\"option\"` on the host and keeps `aria-selected` in sync with the `selected` property.\n- Sets `aria-disabled` based on the `disabled` property; while disabled, the option is removed from the tab order and click/key events are stopped.\n- `Enter` (on `keydown`) and `Space` (on `keyup`) trigger a click on the option, matching native option behaviour.\n- Renders a trailing checkmark icon as a visual indicator when `selected` is `true`; assistive technology relies on `aria-selected` for state, not the icon.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `option` |\n| host | aria-selected | mirrors `selected` (`'true'` / `'false'`) |\n| host | aria-disabled | mirrors `disabled` (`'true'` / `'false'`) |\n| host | tabindex | `-1` when `disabled`; otherwise managed by the parent listbox roving tabindex |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-option` inside an `mdc-selectlistbox` (within `mdc-select`) or another listbox-style container that manages roving tabindex, the listbox role, and selection.\n- Provide a `value` so the surrounding form-associated `mdc-select` can submit the selected value.\n\n#### Labelling\n\n- Provide an accessible name via `label`, slotted text in `leading-text-primary-label`, or `aria-label`.\n- When several options share the same visible label, set `aria-label` (or include disambiguating `secondary-label` text) so screen readers can distinguish them.",
    "sections": {
      "Overview": "`mdc-option` is a selectable entry rendered inside a listbox (`mdc-selectlistbox` / `mdc-listbox`), typically as a child of `mdc-select`. It supports a primary and secondary label, an optional prefix icon, and renders a checkmark on the trailing side when selected. The component participates in form submission through its associated form-control behaviour.\n\n### When to use\n\n- Use `mdc-option` for each selectable value inside an `mdc-select`, `mdc-selectlistbox`, or `mdc-listbox`.\n- Group related options together with `mdc-optgroup`.\n\n### When not to use\n\n- Do not use `mdc-option` inside menus — use `mdc-menuitem`, `mdc-menuitemcheckbox`, or `mdc-menuitemradio` inside `mdc-menupopover`/`mdc-menubar`.\n- Do not use it as a stand-alone list row; use `mdc-listitem` for generic lists.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/option/index.js\";\nimport { Option } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\">\n  <mdc-option value=\"de\" label=\"Germany\" prefix-icon=\"flag-de-regular\"></mdc-option>\n  <mdc-option value=\"fr\" label=\"France\" prefix-icon=\"flag-fr-regular\" selected></mdc-option>\n</mdc-select>\n```\n\nIf the option label can be long enough to truncate, connect an `mdc-tooltip` via its `triggerid` to the option's `id` so the full text is exposed on hover and focus — the component does not create the tooltip automatically.\n\n### Property/Attribute details\n\n- `label`, `secondary-label` — primary and secondary text rendered on the leading side. May be replaced by slotting text into `leading-text-primary-label` / `leading-text-secondary-label`.\n- `value` — the submitted form value for the option.\n- `selected` — boolean, default `false`. Mirrors to `aria-selected` and renders the trailing checkmark icon. Selecting/unselecting also dispatches `selected` / `unselected` lifecycle events.\n- `prefix-icon` — icon name shown on the leading side. The icon is rendered via the leading-controls slot using `mdc-icon`.\n- `aria-label` — accessible name override. Useful when extra context (for example, a country code) should be announced even though it is not in the visible label.\n- `disabled` — boolean. When set, `aria-disabled=\"true\"` is reflected and the option is removed from tab order.\n- `soft-disabled` — boolean. Appears disabled but remains focusable.\n- `active` — visual-only flag for hover/active styling.\n- `tertiary-label`, `side-header-text`, `subline-text`, `name` — not used by `mdc-option`; the component explicitly clears `name`, `sideHeaderText`, and `sublineText` when connected.\n- Events: `click`, `keydown`, `keyup`, `focus`.\n\n### Notes\n\n- Only `label`, `secondary-label`, and `prefix-icon` are rendered; tertiary text, side header text, and subline text are intentionally not displayed for options.",
      "Accessibility": "### Built-in features\n\n- Sets `role=\"option\"` on the host and keeps `aria-selected` in sync with the `selected` property.\n- Sets `aria-disabled` based on the `disabled` property; while disabled, the option is removed from the tab order and click/key events are stopped.\n- `Enter` (on `keydown`) and `Space` (on `keyup`) trigger a click on the option, matching native option behaviour.\n- Renders a trailing checkmark icon as a visual indicator when `selected` is `true`; assistive technology relies on `aria-selected` for state, not the icon.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| host | role | `option` |\n| host | aria-selected | mirrors `selected` (`'true'` / `'false'`) |\n| host | aria-disabled | mirrors `disabled` (`'true'` / `'false'`) |\n| host | tabindex | `-1` when `disabled`; otherwise managed by the parent listbox roving tabindex |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-option` inside an `mdc-selectlistbox` (within `mdc-select`) or another listbox-style container that manages roving tabindex, the listbox role, and selection.\n- Provide a `value` so the surrounding form-associated `mdc-select` can submit the selected value.\n\n#### Labelling\n\n- Provide an accessible name via `label`, slotted text in `leading-text-primary-label`, or `aria-label`.\n- When several options share the same visible label, set `aria-label` (or include disambiguating `secondary-label` text) so screen readers can distinguish them."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "accessible name override. Useful when extra context (for example, a country code) should be announced even though it is not in the visible label.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "boolean. When set, `aria-disabled=\"true\"` is reflected and the option is removed from tab order.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "text",
          "values": [],
          "description": "icon name shown on the leading side. The icon is rendered via the leading-controls slot using `mdc-icon`.",
          "source": "storybook"
        },
        {
          "name": "secondaryLabel",
          "attribute": "secondary-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "selected",
          "attribute": "selected",
          "type": "boolean",
          "values": [],
          "description": "boolean, default `false`. Mirrors to `aria-selected` and renders the trailing checkmark icon. Selecting/unselecting also dispatches `selected` / `unselected` lifecycle events.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "boolean. Appears disabled but remains focusable.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "the submitted form value for the option.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the option is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the option."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the option."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the option receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "leading",
          "description": "Allows customization of the leading part."
        },
        {
          "name": "leading-text",
          "description": "Allows customization of the leading text part."
        },
        {
          "name": "trailing",
          "description": "Allows customization of the trailing part."
        },
        {
          "name": "trailing-text",
          "description": "Allows customization of the trailing text part."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-listitem-column-gap",
          "description": "gap between columns in the option"
        },
        {
          "name": "--mdc-option-icon-width",
          "description": "width of the icon in the option"
        }
      ],
      "rules": [
        "the submitted form value for the option.",
        "boolean, default `false`. Mirrors to `aria-selected` and renders the trailing checkmark icon. Selecting/unselecting also dispatches `selected` / `unselected` lifecycle events.",
        "icon name shown on the leading side. The icon is rendered via the leading-controls slot using `mdc-icon`.",
        "accessible name override. Useful when extra context (for example, a country code) should be announced even though it is not in the visible label.",
        "boolean. When set, `aria-disabled=\"true\"` is reflected and the option is removed from tab order.",
        "boolean. Appears disabled but remains focusable.",
        "visual-only flag for hover/active styling."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/option/index.js\";\nimport { Option } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-select label=\"Country\">\n  <mdc-option value=\"de\" label=\"Germany\" prefix-icon=\"flag-de-regular\"></mdc-option>\n  <mdc-option value=\"fr\" label=\"France\" prefix-icon=\"flag-fr-regular\" selected></mdc-option>\n</mdc-select>"
      }
    ],
    "reactImports": [
      "Option"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "for",
      "guidelines",
      "icon",
      "inside",
      "listbox",
      "mdc",
      "option",
      "overview",
      "select",
      "selectable",
      "text",
      "the",
      "typically",
      "usage",
      "used",
      "within"
    ]
  },
  {
    "name": "password",
    "title": "Password",
    "tagName": "mdc-password",
    "summary": "Usage, guidelines, and accessibility for the mdc-password component — a password input with a built-in show/hide toggle.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/password",
      "knowledgeBasePath": "components/src/components/password/knowledge-base/password.component.md"
    },
    "docs": "## Overview\n\n`mdc-password` is a form-associated password input. It renders the same label, help-text, validation, prefix, and leading-icon surface as `mdc-input`, but the input type defaults to `password` and a trailing show/hide button is always present so users can reveal or mask the entered value.\n\n### When to use\n\n- Use `mdc-password` whenever the user needs to enter a credential or other secret string that should be visually masked.\n- Use it as the password field in sign-in, sign-up, and credential-change forms inside a `form` element.\n\n### When not to use\n\n- Do not use `mdc-password` for regular text — use `mdc-input` instead.\n- Do not use it for multi-line secrets — use `mdc-textarea`.\n- Do not use it for one-time codes that should be visible as the user types — use `mdc-input`.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/password/index.js\";\nimport { Password } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<form>\n  <mdc-password\n    label=\"Password\"\n    name=\"password\"\n    required\n    show-button-aria-label=\"Show password\"\n    hide-button-aria-label=\"Hide password\"\n  ></mdc-password>\n</form>\n```\n\nThe component is form-associated; placing it inside a `form` element registers its `name` / `value` pair for submission. Provide both `show-button-aria-label` and `hide-button-aria-label` so the toggle button has an accessible name in each state.\n\n### Property/Attribute details\n\n- `show-button-aria-label` — accessible label applied to the trailing toggle when the password is currently masked. Required for accessibility.\n- `hide-button-aria-label` — accessible label applied to the trailing toggle when the password is currently visible. Required for accessibility.\n- The trailing toggle is rendered automatically when the field has a value (or when explicitly forced). The component overrides the `trailing-button` flag from `mdc-input` to `true` on connect; the standard clear button is replaced with the show/hide button.\n- The visibility state is internal; toggling the button flips the rendered `input` between `type=\"password\"` and `type=\"text\"` without exposing a property.\n- All `mdc-input` attributes are available:\n  - `label`, `required`, `name`, `value`, `placeholder`, `pattern`, `minlength`, `maxlength`, `size`, `autocapitalize`, `autocomplete`, `dirname`, `list`.\n  - `help-text`, `validation-message`, `help-text-type` (`'default' | 'error' | 'warning' | 'success' | 'priority'`).\n  - `disabled`, `readonly`.\n  - `leading-icon`, `prefix-text`, `toggletip-text`.\n  - `data-aria-label`, `data-aria-describedby` — used for the inner `<input>` accessible name and description respectively.\n- Slots (inherited): `label`, `toggletip`, `help-icon`, `help-text`, `input`, `input-leading-icon`, `input-prefix-text`, `trailing-button` (overriding the built-in toggle).\n- Events: `input`, `change`, `focus`, `blur`.\n\n### Limitations\n\n- The component is form-associated through its host element. Older browsers without `ElementInternals` support will not include the value in standard form submissions and require a polyfill.\n\n### Notes\n\n- The show/hide button uses the `mdc-button` tertiary variant; the icon updates between the bold show and hide icons based on the current visibility state.\n- When the field has no value (and no leading button forced), the trailing toggle is hidden until the user starts typing.\n\n## Accessibility\n\n### Built-in features\n\n- The underlying `<input>` exposes `aria-invalid=\"true\"` when `help-text-type=\"error\"`, `aria-describedby` linked to the help-text element when help text is rendered, and `aria-label` from `data-aria-label`.\n- The form label is rendered via the shared label surface and is associated with the input element through the input id.\n- The show/hide button uses the supplied `show-button-aria-label` / `hide-button-aria-label` so its purpose updates as the visibility state flips.\n- `required`, `pattern`, `minlength`, `maxlength`, and custom `validation-message` values participate in native form validation; the component sets `setCustomValidity` based on the current `validation-message` and reflects the result through `ElementInternals`.\n- A live screen-reader announcer (when a `max-character-limit` is configured) announces when the user crosses the character limit warning thresholds.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| inner `<input>` | type | `password` while hidden, `text` while shown |\n| inner `<input>` | aria-label | mirrors `data-aria-label` |\n| inner `<input>` | aria-describedby | references the help-text element when help text is present, otherwise `data-aria-describedby` |\n| inner `<input>` | aria-invalid | `true` when `help-text-type=\"error\"`, otherwise `false` |\n| trailing toggle button | aria-label | mirrors `hide-button-aria-label` when shown, `show-button-aria-label` when hidden |\n| inner screen-reader announcer | data-aria-live | `polite` |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-password` inside a `<form>` so the `name` / `value` participate in submission.\n- Provide a `name` attribute so the value can be retrieved from `FormData`.\n- Use `validation-message` together with `help-text-type=\"error\"` to surface custom validation results.\n\n#### Labelling\n\n- Always set a `label` (or slot a custom one into the `label` slot) so the field has a visible accessible name.\n- Always provide `show-button-aria-label` and `hide-button-aria-label`; without them the toggle has no accessible name.\n- Use `data-aria-label` when the visible label alone is insufficient (for example, when the label is shared with another field).",
    "sections": {
      "Overview": "`mdc-password` is a form-associated password input. It renders the same label, help-text, validation, prefix, and leading-icon surface as `mdc-input`, but the input type defaults to `password` and a trailing show/hide button is always present so users can reveal or mask the entered value.\n\n### When to use\n\n- Use `mdc-password` whenever the user needs to enter a credential or other secret string that should be visually masked.\n- Use it as the password field in sign-in, sign-up, and credential-change forms inside a `form` element.\n\n### When not to use\n\n- Do not use `mdc-password` for regular text — use `mdc-input` instead.\n- Do not use it for multi-line secrets — use `mdc-textarea`.\n- Do not use it for one-time codes that should be visible as the user types — use `mdc-input`.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/password/index.js\";\nimport { Password } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<form>\n  <mdc-password\n    label=\"Password\"\n    name=\"password\"\n    required\n    show-button-aria-label=\"Show password\"\n    hide-button-aria-label=\"Hide password\"\n  ></mdc-password>\n</form>\n```\n\nThe component is form-associated; placing it inside a `form` element registers its `name` / `value` pair for submission. Provide both `show-button-aria-label` and `hide-button-aria-label` so the toggle button has an accessible name in each state.\n\n### Property/Attribute details\n\n- `show-button-aria-label` — accessible label applied to the trailing toggle when the password is currently masked. Required for accessibility.\n- `hide-button-aria-label` — accessible label applied to the trailing toggle when the password is currently visible. Required for accessibility.\n- The trailing toggle is rendered automatically when the field has a value (or when explicitly forced). The component overrides the `trailing-button` flag from `mdc-input` to `true` on connect; the standard clear button is replaced with the show/hide button.\n- The visibility state is internal; toggling the button flips the rendered `input` between `type=\"password\"` and `type=\"text\"` without exposing a property.\n- All `mdc-input` attributes are available:\n  - `label`, `required`, `name`, `value`, `placeholder`, `pattern`, `minlength`, `maxlength`, `size`, `autocapitalize`, `autocomplete`, `dirname`, `list`.\n  - `help-text`, `validation-message`, `help-text-type` (`'default' | 'error' | 'warning' | 'success' | 'priority'`).\n  - `disabled`, `readonly`.\n  - `leading-icon`, `prefix-text`, `toggletip-text`.\n  - `data-aria-label`, `data-aria-describedby` — used for the inner `<input>` accessible name and description respectively.\n- Slots (inherited): `label`, `toggletip`, `help-icon`, `help-text`, `input`, `input-leading-icon`, `input-prefix-text`, `trailing-button` (overriding the built-in toggle).\n- Events: `input`, `change`, `focus`, `blur`.\n\n### Limitations\n\n- The component is form-associated through its host element. Older browsers without `ElementInternals` support will not include the value in standard form submissions and require a polyfill.\n\n### Notes\n\n- The show/hide button uses the `mdc-button` tertiary variant; the icon updates between the bold show and hide icons based on the current visibility state.\n- When the field has no value (and no leading button forced), the trailing toggle is hidden until the user starts typing.",
      "Accessibility": "### Built-in features\n\n- The underlying `<input>` exposes `aria-invalid=\"true\"` when `help-text-type=\"error\"`, `aria-describedby` linked to the help-text element when help text is rendered, and `aria-label` from `data-aria-label`.\n- The form label is rendered via the shared label surface and is associated with the input element through the input id.\n- The show/hide button uses the supplied `show-button-aria-label` / `hide-button-aria-label` so its purpose updates as the visibility state flips.\n- `required`, `pattern`, `minlength`, `maxlength`, and custom `validation-message` values participate in native form validation; the component sets `setCustomValidity` based on the current `validation-message` and reflects the result through `ElementInternals`.\n- A live screen-reader announcer (when a `max-character-limit` is configured) announces when the user crosses the character limit warning thresholds.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| inner `<input>` | type | `password` while hidden, `text` while shown |\n| inner `<input>` | aria-label | mirrors `data-aria-label` |\n| inner `<input>` | aria-describedby | references the help-text element when help text is present, otherwise `data-aria-describedby` |\n| inner `<input>` | aria-invalid | `true` when `help-text-type=\"error\"`, otherwise `false` |\n| trailing toggle button | aria-label | mirrors `hide-button-aria-label` when shown, `show-button-aria-label` when hidden |\n| inner screen-reader announcer | data-aria-live | `polite` |\n\n### Implementation requirements\n\n#### General\n\n- Place `mdc-password` inside a `<form>` so the `name` / `value` participate in submission.\n- Provide a `name` attribute so the value can be retrieved from `FormData`.\n- Use `validation-message` together with `help-text-type=\"error\"` to surface custom validation results.\n\n#### Labelling\n\n- Always set a `label` (or slot a custom one into the `label` slot) so the field has a visible accessible name.\n- Always provide `show-button-aria-label` and `hide-button-aria-label`; without them the toggle has no accessible name.\n- Use `data-aria-label` when the visible label alone is insufficient (for example, when the label is shared with another field)."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text",
      "mdc-button",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dirname",
          "attribute": "dirname",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "Helper text for the password",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "hideButtonAriaLabel",
          "attribute": "hide-button-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible label applied to the trailing toggle when the password is currently visible. Required for accessibility.",
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "description": "The unique id of the password field. It is used to link the password field with the label.",
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "The label of the password field. It is linked to the password field using the for attribute.",
          "source": "storybook"
        },
        {
          "name": "maxlength",
          "attribute": "maxlength",
          "type": "number",
          "values": [],
          "description": "The maximum number of characters that the password field can accept.",
          "source": "storybook"
        },
        {
          "name": "minlength",
          "attribute": "minlength",
          "type": "number",
          "values": [],
          "description": "The minimum number of characters that the password field can accept.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "The name of the password field. It is used to identify the password field in a form.",
          "source": "storybook"
        },
        {
          "name": "pattern",
          "attribute": "pattern",
          "type": "text",
          "values": [],
          "description": "The pattern attribute of the password field. Specifies a regular expression that the password ",
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "description": "The placeholder text that is displayed when the password field is empty.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "readonly attribute of the password field. If true, the password field is read-only.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "The required attribute to indicate that the password field is required. ",
          "source": "storybook"
        },
        {
          "name": "showButtonAriaLabel",
          "attribute": "show-button-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible label applied to the trailing toggle when the password is currently masked. Required for accessibility.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "number",
          "values": [],
          "description": "The size attribute of the password field. Specifies the width of the password field.",
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "validationMessage",
          "attribute": "validation-message",
          "type": "text",
          "values": [],
          "description": "Custom validation message that will override the default message and displayed when the password is invalid.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        },
        {
          "name": "input",
          "description": "Slot for the input element. If not provided, the input field will be rendered."
        },
        {
          "name": "input-leading-icon",
          "description": "Slot for the leading icon before the input field. If not provided, the `leadingIcon` property will be used to render the leading icon."
        },
        {
          "name": "input-prefix-text",
          "description": "Slot for the prefix text before the input field. If not provided, the `prefixText` property will be used to render the prefix text."
        },
        {
          "name": "trailing-button",
          "description": "Slot for the trailing button to clear the input field. If not provided, the clear button will be rendered when `trailingButton` property is set to true."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the value of the password field changes (every press)."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the value of the password field changes (on blur)."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the password receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the password loses focus."
        }
      ],
      "cssParts": [
        {
          "name": "input-container",
          "description": "The container that wraps the input field, leading icon, prefix text, and trailing button."
        },
        {
          "name": "input-section",
          "description": "The container that wraps the input field and prefix text."
        },
        {
          "name": "input-text",
          "description": "The input field element."
        },
        {
          "name": "trailing-button",
          "description": "The trailing button element."
        },
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "helper-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element."
        },
        {
          "name": "toggletip",
          "description": "The toggletip icon button element."
        },
        {
          "name": "toggletip-text",
          "description": "The toggletip text element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-input-text-color",
          "description": "Text color for the input field"
        },
        {
          "name": "--mdc-input-border-color",
          "description": "Border color for the input container"
        },
        {
          "name": "--mdc-input-background-color",
          "description": "Background color for the input field"
        },
        {
          "name": "--mdc-input-selection-text-color",
          "description": "Text color for the selected text"
        },
        {
          "name": "--mdc-input-selection-background-color",
          "description": "Background color for the selected text"
        }
      ],
      "rules": [
        "accessible label applied to the trailing toggle when the password is currently masked. Required for accessibility.",
        "accessible label applied to the trailing toggle when the password is currently visible. Required for accessibility."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/password/index.js\";\nimport { Password } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<form>\n  <mdc-password\n    label=\"Password\"\n    name=\"password\"\n    required\n    show-button-aria-label=\"Show password\"\n    hide-button-aria-label=\"Hide password\"\n  ></mdc-password>\n</form>"
      }
    ],
    "reactImports": [
      "Password"
    ],
    "constants": {
      "PASSWORD_VISIBILITY_ICONS": {
        "HIDE_BOLD": "'hide-bold' as Extract<IconNames, 'hide-bold'>",
        "SHOW_BOLD": "'show-bold' as Extract<IconNames, 'show-bold'>"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "built",
      "button",
      "component",
      "for",
      "guidelines",
      "hide",
      "icon",
      "in",
      "input",
      "mdc",
      "overview",
      "password",
      "show",
      "text",
      "the",
      "toggle",
      "toggletip",
      "usage",
      "with"
    ]
  },
  {
    "name": "popover",
    "title": "Popover",
    "tagName": "mdc-popover",
    "summary": "Usage, guidelines, and accessibility for the mdc-popover component — a generic, floating overlay anchored to a trigger element with configurable trigger, placement, focus trap, and dismiss behaviour.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/popover",
      "knowledgeBasePath": "components/src/components/popover/knowledge-base/popover.component.md"
    },
    "docs": "## Overview\n\nThe popover is a generic overlay that can be anchored to any actionable element on the page (identified by its `id`) and used to host tooltips, dropdowns, menus, or any other contextual content. It automatically positions itself based on available space, can resize to fit the viewport, and uses [Floating UI](https://floating-ui.com/) to keep its position correct as the page scrolls or resizes.\n\n### When to use\n\n- Use `mdc-popover` to render contextual UI floating next to a trigger element — additional information, action menus, or interactive panels.\n- Use it when you need fine control over trigger events (click, hover, focus, manual), placement, offset, arrow, focus trap, or backdrop behaviour.\n\n### When not to use\n\n- Use `mdc-tooltip` for short, non-interactive descriptive text shown on hover/focus.\n- Use `mdc-toggletip` for short text shown on click that does not need focus trapping.\n- Use `mdc-dialog` for modal flows that interrupt the user, render in the centre of the viewport, and own their own header/footer chrome.\n- Use `mdc-menupopover` / `mdc-coachmark` / `mdc-searchpopover` when one of those higher-level widgets already composes a popover for the use case.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/popover/index.js\"; // custom element registration\n// or via React wrapper\nimport { Popover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example — the popover is a sibling of the trigger and connects to it via `triggerid`:\n\n```html\n<mdc-button id=\"popover-trigger\">Open popover</mdc-button>\n\n<mdc-popover triggerid=\"popover-trigger\" placement=\"bottom-start\" show-arrow>\n  Popover content\n</mdc-popover>\n```\n\nInteractive popover with focus trap, Escape-to-close, and focus restored to the trigger:\n\n```html\n<mdc-button id=\"settings-trigger\">Settings</mdc-button>\n\n<mdc-popover\n  triggerid=\"settings-trigger\"\n  interactive\n  focus-trap\n  hide-on-escape\n  hide-on-outside-click\n  focus-back-to-trigger\n  close-button\n  close-button-aria-label=\"Close settings\"\n  aria-label=\"Settings\"\n>\n  <!-- interactive content -->\n</mdc-popover>\n```\n\nListen for `shown`/`hidden` to react to visibility changes, and for `created`/`destroyed` to react when the popover is added to or removed from the DOM.\n\n### Property/Attribute details\n\n- `triggerid` — id of the element that owns the popover. Required; without it the popover never opens.\n- `trigger` — space-separated list of events that open the popover: `click`, `mouseenter`, `focusin`, or `manual`. When `mouseenter` is set on an interactive popover it is rewritten to `mouseenter click` (so the popover can be re-opened after closing with Escape); on a non-interactive popover it is rewritten to `mouseenter focusin` so keyboard focus also opens it. Default `click`.\n- `placement` — `top` / `top-start` / `top-end` / `bottom` / `bottom-start` / `bottom-end` / `left` / `left-start` / `left-end` / `right` / `right-start` / `right-end`. In RTL layouts, `left`/`right` placements are flipped automatically. Default `bottom`.\n- `color` — `tonal` or `contrast`. Default `tonal`.\n- `visible` — opens or closes the popover programmatically. Default `false`.\n- `offset` — distance in pixels from the trigger on the placement's main axis. Accepts a number (reflected as an attribute), an object (`{ mainAxis, crossAxis, alignmentAxis }`), or a function for full control. Default `4`.\n- `strategy` — `absolute` (positioned relative to the nearest positioned ancestor) or `fixed` (positioned relative to the viewport — use when a scrollable container clips the popover). Default `absolute`.\n- `boundary` — `clippingAncestors` or any CSS selector; describes what overflow is checked against. Default `clippingAncestors`.\n- `boundary-root` — `viewport` or `document`; the root boundary used for overflow detection. Default `viewport`.\n- `boundary-padding` — virtual padding in pixels around the boundary. Default unset.\n- `disable-flip` — when `true`, prevents the popover from flipping placement to stay in view. Default `false`.\n- `size` — when `true`, constrains the popover's max-height to the available space and adds a vertical scrollbar to the content when needed. Default `false`.\n- `inline` — when `true`, uses Floating UI's `inline` middleware so that the popover positions correctly against multi-line inline trigger elements (e.g. hyperlinks, range selections). Default `false`.\n- `animation-frame` — when `true`, repositions on every animation frame. Default `false`.\n- `show-arrow` — renders a directional arrow that points at the trigger. Default `false`.\n- `close-button` — renders a built-in close button in the corner of the popover. Default `false`.\n- `close-button-aria-label` — accessible name for the built-in close button. Required when `close-button` is `true`.\n- `interactive` — when `true`, marks the popover as containing interactive content. Required for `focus-trap`, and changes hover behaviour so the popover does not close while the pointer is over it.\n- `focus-trap` — when `true` and `interactive` is `true`, traps keyboard focus inside the popover while it is visible.\n- `element-index-to-receive-focus` — zero-based index of the focusable child that should receive focus when the popover opens with `focus-trap` enabled. Default `null` (the first focusable element).\n- `prevent-scroll` — when `true`, prevents scrolling outside the popover while it is visible. Default `false`.\n- `backdrop` — when `true`, renders a backdrop covering the rest of the page (the trigger remains visible above the backdrop). Default `false`.\n- `backdrop-append-to` — id of the element the backdrop will be appended to. Default unset.\n- `is-backdrop-invisible` — when `false`, the backdrop is dimmed instead of invisible. Default `true`.\n- `delay` — `\"openMs,closeMs\"` open/close delay string (e.g. `\"200,300\"`). Invalid values are coerced to `\"0,0\"`. Default `\"0,0\"`.\n- `hide-on-escape` — when `true`, the Escape key closes the popover. Default `false`.\n- `propagate-event-on-escape` — when `true` and `hide-on-escape` is `true`, the Escape keydown event bubbles up after closing the popover. Default `false`.\n- `hide-on-blur` — when `true`, the popover closes when focus leaves it. Default `false`.\n- `hide-on-outside-click` — when `true`, a click outside the popover (including on the backdrop) closes it. Default `false`.\n- `focus-back-to-trigger` — when `true`, focus returns to the trigger element after the popover closes. Default `false`.\n- `append-to` — id of an element the popover should be physically moved to in the DOM (used to escape clipping ancestors). The original DOM location is occupied by an internal `mdc-popoverportal` element so removal stays consistent. Default unset.\n- `z-index` — explicit z-index. When not set, an internal depth manager computes a z-index so stacked popovers layer correctly. Default computed.\n- `stack-group-name` — popovers sharing the same name are stacked as a group by the depth manager. Default `''`.\n- `role` — ARIA role applied to the popover host. Default `dialog`. When set to `dialog` or `alertdialog`, the host also gets `aria-modal=\"true\"`; for any other role the `aria-modal` attribute is removed.\n- `aria-labelledby` — id pointing to the element that names the popover. For interactive popovers, defaults to the trigger element when no `aria-label` is provided.\n- `aria-describedby` — id pointing to descriptive text for the popover.\n- `disable-aria-expanded` — when `true`, the popover does not write `aria-expanded` or `aria-haspopup` on the trigger element. Use this when multiple popovers share the same trigger so they do not fight over the trigger's ARIA state. Default `false`.\n- `keep-connected-tooltip-open` — when `true`, a tooltip wired to the same trigger remains visible while this popover is open. When `false`, the tooltip is closed and re-enabled after the popover closes. Default `false`.\n\nThe `shown`, `hidden`, `created`, and `destroyed` events bubble and are composed. `show()` and `hide()` are public methods that respect the configured open/close delay.\n\n### Limitations\n\n- React with `append-to`: when React directly mounts/unmounts the popover and the popover moves itself to a different element, React loses track of the moved DOM node and throws a `NotFoundError`. Wrap the popover in another element (e.g. a `<div>`) so React can manage the wrapper instead. A `React.Fragment` is not sufficient.\n\n## Accessibility\n\n### Built-in features\n\nThe popover renders as a `role=\"dialog\"` overlay by default and adds `aria-modal=\"true\"` automatically when the role is `dialog` or `alertdialog`. When `interactive` and `focus-trap` are both `true`, keyboard focus is trapped inside the popover while it is visible; on opening, focus is moved to the first focusable element (or to the index in `element-index-to-receive-focus`). When `hide-on-escape` is `true`, pressing `Escape` while focus is inside the popover (or anywhere on the page) closes it; the Escape keydown is `stopPropagation`ed unless `propagate-event-on-escape` is set. When `focus-back-to-trigger` is `true`, closing the popover returns focus to the trigger element. When `prevent-scroll` is `true`, scrolling outside the popover is suppressed while it is open.\n\nThe popover writes `aria-expanded` and `aria-haspopup` on the trigger element whenever it opens or closes, so assistive technologies announce the trigger's expanded state without extra wiring (set `disable-aria-expanded` to opt out when multiple popovers share a trigger). When the popover is `interactive` and no `aria-label`/`aria-labelledby` is supplied, the trigger's accessible name (`aria-label` or text content) is mirrored onto the popover so it is named for screen readers.\n\nThe built-in close button is rendered as an `mdc-button` with its own `aria-label` (`close-button-aria-label`).\n\nHover triggering uses delegated `mouseover`/`mouseout` listeners on `document` so the popover continues to work when the trigger lives inside a shadow root (`mdc-iconprovider`, `mdc-themeprovider`, etc.) — the spec'd `mouseenter`/`mouseleave` events do not cross shadow boundaries.\n\n#### Internal ARIA managed by the component\n\n| Element           | Attribute        | Value                                                                                                                       |\n| ----------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------- |\n| Popover host      | `role`           | mirrors `role` (default `dialog`); set to `null` if `role=\"\"`                                                               |\n| Popover host      | `aria-modal`     | `true` when `role` is `dialog` or `alertdialog`; removed otherwise                                                          |\n| Popover host      | `aria-label`     | for interactive popovers, falls back to the trigger's `aria-label`/text content when none is provided                       |\n| Popover host      | `aria-labelledby`| falls back to the trigger element's `id` for interactive popovers when no `aria-label` and no explicit value is provided    |\n| Popover host      | `aria-describedby`| mirrors `aria-describedby`                                                                                                 |\n| Trigger element   | `aria-expanded`  | `true` while visible, `false` while hidden; not written when `disable-aria-expanded` is `true`                              |\n| Trigger element   | `aria-haspopup`  | mirrors trigger's existing `aria-haspopup` if set, otherwise the popover's `role` (or `dialog`); skipped when `disable-aria-expanded` is `true` |\n| Built-in close button | `aria-label` | mirrors `close-button-aria-label`                                                                                           |\n\n### Implementation requirements\n\n#### General\n\n- Set a unique `id` on the trigger element and the same value on the popover's `triggerid` attribute; otherwise the popover never opens.\n- For `interactive` popovers that must keep keyboard focus inside, also set `focus-trap` — `interactive` alone does not enable the trap.\n- For popovers that should close on Escape, set `hide-on-escape` (the popover does not close on Escape by default).\n- When multiple popovers are wired to the same trigger, set `disable-aria-expanded` on all but one so the trigger's `aria-expanded`/`aria-haspopup` state remains coherent.\n\n#### Labelling\n\n- For interactive popovers, provide `aria-label` or `aria-labelledby` describing what the popover contains. If neither is supplied, the popover falls back to the trigger's accessible name.\n- When `close-button` is `true`, provide `close-button-aria-label` so the close button has an accessible name.",
    "sections": {
      "Overview": "The popover is a generic overlay that can be anchored to any actionable element on the page (identified by its `id`) and used to host tooltips, dropdowns, menus, or any other contextual content. It automatically positions itself based on available space, can resize to fit the viewport, and uses [Floating UI](https://floating-ui.com/) to keep its position correct as the page scrolls or resizes.\n\n### When to use\n\n- Use `mdc-popover` to render contextual UI floating next to a trigger element — additional information, action menus, or interactive panels.\n- Use it when you need fine control over trigger events (click, hover, focus, manual), placement, offset, arrow, focus trap, or backdrop behaviour.\n\n### When not to use\n\n- Use `mdc-tooltip` for short, non-interactive descriptive text shown on hover/focus.\n- Use `mdc-toggletip` for short text shown on click that does not need focus trapping.\n- Use `mdc-dialog` for modal flows that interrupt the user, render in the centre of the viewport, and own their own header/footer chrome.\n- Use `mdc-menupopover` / `mdc-coachmark` / `mdc-searchpopover` when one of those higher-level widgets already composes a popover for the use case.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/popover/index.js\"; // custom element registration\n// or via React wrapper\nimport { Popover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example — the popover is a sibling of the trigger and connects to it via `triggerid`:\n\n```html\n<mdc-button id=\"popover-trigger\">Open popover</mdc-button>\n\n<mdc-popover triggerid=\"popover-trigger\" placement=\"bottom-start\" show-arrow>\n  Popover content\n</mdc-popover>\n```\n\nInteractive popover with focus trap, Escape-to-close, and focus restored to the trigger:\n\n```html\n<mdc-button id=\"settings-trigger\">Settings</mdc-button>\n\n<mdc-popover\n  triggerid=\"settings-trigger\"\n  interactive\n  focus-trap\n  hide-on-escape\n  hide-on-outside-click\n  focus-back-to-trigger\n  close-button\n  close-button-aria-label=\"Close settings\"\n  aria-label=\"Settings\"\n>\n  <!-- interactive content -->\n</mdc-popover>\n```\n\nListen for `shown`/`hidden` to react to visibility changes, and for `created`/`destroyed` to react when the popover is added to or removed from the DOM.\n\n### Property/Attribute details\n\n- `triggerid` — id of the element that owns the popover. Required; without it the popover never opens.\n- `trigger` — space-separated list of events that open the popover: `click`, `mouseenter`, `focusin`, or `manual`. When `mouseenter` is set on an interactive popover it is rewritten to `mouseenter click` (so the popover can be re-opened after closing with Escape); on a non-interactive popover it is rewritten to `mouseenter focusin` so keyboard focus also opens it. Default `click`.\n- `placement` — `top` / `top-start` / `top-end` / `bottom` / `bottom-start` / `bottom-end` / `left` / `left-start` / `left-end` / `right` / `right-start` / `right-end`. In RTL layouts, `left`/`right` placements are flipped automatically. Default `bottom`.\n- `color` — `tonal` or `contrast`. Default `tonal`.\n- `visible` — opens or closes the popover programmatically. Default `false`.\n- `offset` — distance in pixels from the trigger on the placement's main axis. Accepts a number (reflected as an attribute), an object (`{ mainAxis, crossAxis, alignmentAxis }`), or a function for full control. Default `4`.\n- `strategy` — `absolute` (positioned relative to the nearest positioned ancestor) or `fixed` (positioned relative to the viewport — use when a scrollable container clips the popover). Default `absolute`.\n- `boundary` — `clippingAncestors` or any CSS selector; describes what overflow is checked against. Default `clippingAncestors`.\n- `boundary-root` — `viewport` or `document`; the root boundary used for overflow detection. Default `viewport`.\n- `boundary-padding` — virtual padding in pixels around the boundary. Default unset.\n- `disable-flip` — when `true`, prevents the popover from flipping placement to stay in view. Default `false`.\n- `size` — when `true`, constrains the popover's max-height to the available space and adds a vertical scrollbar to the content when needed. Default `false`.\n- `inline` — when `true`, uses Floating UI's `inline` middleware so that the popover positions correctly against multi-line inline trigger elements (e.g. hyperlinks, range selections). Default `false`.\n- `animation-frame` — when `true`, repositions on every animation frame. Default `false`.\n- `show-arrow` — renders a directional arrow that points at the trigger. Default `false`.\n- `close-button` — renders a built-in close button in the corner of the popover. Default `false`.\n- `close-button-aria-label` — accessible name for the built-in close button. Required when `close-button` is `true`.\n- `interactive` — when `true`, marks the popover as containing interactive content. Required for `focus-trap`, and changes hover behaviour so the popover does not close while the pointer is over it.\n- `focus-trap` — when `true` and `interactive` is `true`, traps keyboard focus inside the popover while it is visible.\n- `element-index-to-receive-focus` — zero-based index of the focusable child that should receive focus when the popover opens with `focus-trap` enabled. Default `null` (the first focusable element).\n- `prevent-scroll` — when `true`, prevents scrolling outside the popover while it is visible. Default `false`.\n- `backdrop` — when `true`, renders a backdrop covering the rest of the page (the trigger remains visible above the backdrop). Default `false`.\n- `backdrop-append-to` — id of the element the backdrop will be appended to. Default unset.\n- `is-backdrop-invisible` — when `false`, the backdrop is dimmed instead of invisible. Default `true`.\n- `delay` — `\"openMs,closeMs\"` open/close delay string (e.g. `\"200,300\"`). Invalid values are coerced to `\"0,0\"`. Default `\"0,0\"`.\n- `hide-on-escape` — when `true`, the Escape key closes the popover. Default `false`.\n- `propagate-event-on-escape` — when `true` and `hide-on-escape` is `true`, the Escape keydown event bubbles up after closing the popover. Default `false`.\n- `hide-on-blur` — when `true`, the popover closes when focus leaves it. Default `false`.\n- `hide-on-outside-click` — when `true`, a click outside the popover (including on the backdrop) closes it. Default `false`.\n- `focus-back-to-trigger` — when `true`, focus returns to the trigger element after the popover closes. Default `false`.\n- `append-to` — id of an element the popover should be physically moved to in the DOM (used to escape clipping ancestors). The original DOM location is occupied by an internal `mdc-popoverportal` element so removal stays consistent. Default unset.\n- `z-index` — explicit z-index. When not set, an internal depth manager computes a z-index so stacked popovers layer correctly. Default computed.\n- `stack-group-name` — popovers sharing the same name are stacked as a group by the depth manager. Default `''`.\n- `role` — ARIA role applied to the popover host. Default `dialog`. When set to `dialog` or `alertdialog`, the host also gets `aria-modal=\"true\"`; for any other role the `aria-modal` attribute is removed.\n- `aria-labelledby` — id pointing to the element that names the popover. For interactive popovers, defaults to the trigger element when no `aria-label` is provided.\n- `aria-describedby` — id pointing to descriptive text for the popover.\n- `disable-aria-expanded` — when `true`, the popover does not write `aria-expanded` or `aria-haspopup` on the trigger element. Use this when multiple popovers share the same trigger so they do not fight over the trigger's ARIA state. Default `false`.\n- `keep-connected-tooltip-open` — when `true`, a tooltip wired to the same trigger remains visible while this popover is open. When `false`, the tooltip is closed and re-enabled after the popover closes. Default `false`.\n\nThe `shown`, `hidden`, `created`, and `destroyed` events bubble and are composed. `show()` and `hide()` are public methods that respect the configured open/close delay.\n\n### Limitations\n\n- React with `append-to`: when React directly mounts/unmounts the popover and the popover moves itself to a different element, React loses track of the moved DOM node and throws a `NotFoundError`. Wrap the popover in another element (e.g. a `<div>`) so React can manage the wrapper instead. A `React.Fragment` is not sufficient.",
      "Accessibility": "### Built-in features\n\nThe popover renders as a `role=\"dialog\"` overlay by default and adds `aria-modal=\"true\"` automatically when the role is `dialog` or `alertdialog`. When `interactive` and `focus-trap` are both `true`, keyboard focus is trapped inside the popover while it is visible; on opening, focus is moved to the first focusable element (or to the index in `element-index-to-receive-focus`). When `hide-on-escape` is `true`, pressing `Escape` while focus is inside the popover (or anywhere on the page) closes it; the Escape keydown is `stopPropagation`ed unless `propagate-event-on-escape` is set. When `focus-back-to-trigger` is `true`, closing the popover returns focus to the trigger element. When `prevent-scroll` is `true`, scrolling outside the popover is suppressed while it is open.\n\nThe popover writes `aria-expanded` and `aria-haspopup` on the trigger element whenever it opens or closes, so assistive technologies announce the trigger's expanded state without extra wiring (set `disable-aria-expanded` to opt out when multiple popovers share a trigger). When the popover is `interactive` and no `aria-label`/`aria-labelledby` is supplied, the trigger's accessible name (`aria-label` or text content) is mirrored onto the popover so it is named for screen readers.\n\nThe built-in close button is rendered as an `mdc-button` with its own `aria-label` (`close-button-aria-label`).\n\nHover triggering uses delegated `mouseover`/`mouseout` listeners on `document` so the popover continues to work when the trigger lives inside a shadow root (`mdc-iconprovider`, `mdc-themeprovider`, etc.) — the spec'd `mouseenter`/`mouseleave` events do not cross shadow boundaries.\n\n#### Internal ARIA managed by the component\n\n| Element           | Attribute        | Value                                                                                                                       |\n| ----------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------- |\n| Popover host      | `role`           | mirrors `role` (default `dialog`); set to `null` if `role=\"\"`                                                               |\n| Popover host      | `aria-modal`     | `true` when `role` is `dialog` or `alertdialog`; removed otherwise                                                          |\n| Popover host      | `aria-label`     | for interactive popovers, falls back to the trigger's `aria-label`/text content when none is provided                       |\n| Popover host      | `aria-labelledby`| falls back to the trigger element's `id` for interactive popovers when no `aria-label` and no explicit value is provided    |\n| Popover host      | `aria-describedby`| mirrors `aria-describedby`                                                                                                 |\n| Trigger element   | `aria-expanded`  | `true` while visible, `false` while hidden; not written when `disable-aria-expanded` is `true`                              |\n| Trigger element   | `aria-haspopup`  | mirrors trigger's existing `aria-haspopup` if set, otherwise the popover's `role` (or `dialog`); skipped when `disable-aria-expanded` is `true` |\n| Built-in close button | `aria-label` | mirrors `close-button-aria-label`                                                                                           |\n\n### Implementation requirements\n\n#### General\n\n- Set a unique `id` on the trigger element and the same value on the popover's `triggerid` attribute; otherwise the popover never opens.\n- For `interactive` popovers that must keep keyboard focus inside, also set `focus-trap` — `interactive` alone does not enable the trap.\n- For popovers that should close on Escape, set `hide-on-escape` (the popover does not close on Escape by default).\n- When multiple popovers are wired to the same trigger, set `disable-aria-expanded` on all but one so the trigger's `aria-expanded`/`aria-haspopup` state remains coherent.\n\n#### Labelling\n\n- For interactive popovers, provide `aria-label` or `aria-labelledby` describing what the popover contains. If neither is supplied, the popover falls back to the trigger's accessible name.\n- When `close-button` is `true`, provide `close-button-aria-label` so the close button has an accessible name."
    },
    "dependencies": [
      "mdc-button"
    ],
    "api": {
      "properties": [
        {
          "name": "animationFrame",
          "attribute": "animation-frame",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, repositions on every animation frame. Default `false`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "appendTo",
          "attribute": "append-to",
          "type": "text",
          "values": [],
          "description": "id of an element the popover should be physically moved to in the DOM (used to escape clipping ancestors). The original DOM location is occupied by an internal `mdc-popoverportal` element so removal stays consistent. Default unset.",
          "source": "storybook"
        },
        {
          "name": "ariaDescribedby",
          "attribute": "aria-describedby",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "id pointing to descriptive text for the popover.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "ariaLabelledby",
          "attribute": "aria-labelledby",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "id pointing to the element that names the popover. For interactive popovers, defaults to the trigger element when no `aria-label` is provided.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "backdrop",
          "attribute": "backdrop",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, renders a backdrop covering the rest of the page (the trigger remains visible above the backdrop). Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "backdropAppendTo",
          "attribute": "backdrop-append-to",
          "type": "text",
          "values": [],
          "description": "id of the element the backdrop will be appended to. Default unset.",
          "source": "storybook"
        },
        {
          "name": "boundary",
          "attribute": "boundary",
          "type": "text",
          "default": "'clippingAncestors'",
          "values": [
            "clippingAncestors"
          ],
          "description": "`clippingAncestors` or any CSS selector; describes what overflow is checked against. Default `clippingAncestors`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "boundaryPadding",
          "attribute": "boundary-padding",
          "type": "number",
          "values": [],
          "description": "virtual padding in pixels around the boundary. Default unset.",
          "source": "storybook"
        },
        {
          "name": "boundaryRoot",
          "attribute": "boundary-root",
          "type": "select",
          "default": "'viewport'",
          "values": [
            "viewport",
            "document"
          ],
          "description": "`viewport` or `document`; the root boundary used for overflow detection. Default `viewport`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "closeButton",
          "attribute": "close-button",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "renders a built-in close button in the corner of the popover. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "closeButtonAriaLabel",
          "attribute": "close-button-aria-label",
          "type": "text",
          "default": "null",
          "values": [],
          "description": "accessible name for the built-in close button. Required when `close-button` is `true`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "default": "tonal",
          "values": [
            "tonal",
            "contrast"
          ],
          "description": "`tonal` or `contrast`. Default `tonal`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "delay",
          "attribute": "delay",
          "type": "text",
          "default": "0,0",
          "values": [],
          "description": "`\"openMs,closeMs\"` open/close delay string (e.g. `\"200,300\"`). Invalid values are coerced to `\"0,0\"`. Default `\"0,0\"`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "disableAriaExpanded",
          "attribute": "disable-aria-expanded",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the popover does not write `aria-expanded` or `aria-haspopup` on the trigger element. Use this when multiple popovers share the same trigger so they do not fight over the trigger's ARIA state. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "disableFlip",
          "attribute": "disable-flip",
          "type": "boolean",
          "default": "''",
          "values": [],
          "description": "when `true`, prevents the popover from flipping placement to stay in view. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "elementIndexToReceiveFocus",
          "attribute": "element-index-to-receive-focus",
          "type": "number",
          "default": "null",
          "values": [],
          "description": "zero-based index of the focusable child that should receive focus when the popover opens with `focus-trap` enabled. Default `null` (the first focusable element).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "focusBackToTrigger",
          "attribute": "focus-back-to-trigger",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, focus returns to the trigger element after the popover closes. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "focusTrap",
          "attribute": "focus-trap",
          "type": "boolean",
          "default": "undefined",
          "values": [],
          "description": "when `true` and `interactive` is `true`, traps keyboard focus inside the popover while it is visible.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "hideOnBlur",
          "attribute": "hide-on-blur",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the popover closes when focus leaves it. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "hideOnEscape",
          "attribute": "hide-on-escape",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the Escape key closes the popover. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "hideOnOutsideClick",
          "attribute": "hide-on-outside-click",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, a click outside the popover (including on the backdrop) closes it. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "default": "''",
          "values": [],
          "description": "/\nclass Popover\nextends KeyDownHandledMixin(KeyToActionMixin(BackdropMixin(PreventScrollMixin(FocusTrapMixin(Component)))))\nimplements StackedOverlayComponent\n{\n/** track the depth of the popover for z-index calculation\n/\nprotected depthManager = new DepthManager(this);\n/**\nThe unique ID of the popover.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "inline",
          "attribute": "inline",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, uses Floating UI's `inline` middleware so that the popover positions correctly against multi-line inline trigger elements (e.g. hyperlinks, range selections). Default `false`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "interactive",
          "attribute": "interactive",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, marks the popover as containing interactive content. Required for `focus-trap`, and changes hover behaviour so the popover does not close while the pointer is over it.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "isBackdropInvisible",
          "attribute": "is-backdrop-invisible",
          "type": "boolean",
          "values": [],
          "description": "when `false`, the backdrop is dimmed instead of invisible. Default `true`.",
          "source": "storybook"
        },
        {
          "name": "keepConnectedTooltipOpen",
          "attribute": "keep-connected-tooltip-open",
          "type": "boolean",
          "default": "DEFAULTS.KEEP_CONNECTED_TOOLTIP_OPEN",
          "values": [],
          "description": "when `true`, a tooltip wired to the same trigger remains visible while this popover is open. When `false`, the tooltip is closed and re-enabled after the popover closes. Default `false`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "offset",
          "attribute": "offset",
          "type": "number",
          "default": "4",
          "values": [],
          "description": "distance in pixels from the trigger on the placement's main axis. Accepts a number (reflected as an attribute), an object (`{ mainAxis, crossAxis, alignmentAxis }`), or a function for full control. Default `4`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "select",
          "default": "bottom",
          "values": [
            "left-start",
            "left",
            "left-end",
            "right-start",
            "right",
            "right-end",
            "top-start",
            "top",
            "top-end",
            "bottom-start",
            "bottom",
            "bottom-end"
          ],
          "description": "`top` / `top-start` / `top-end` / `bottom` / `bottom-start` / `bottom-end` / `left` / `left-start` / `left-end` / `right` / `right-start` / `right-end`. In RTL layouts, `left`/`right` placements are flipped automatically. Default `bottom`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "preventScroll",
          "attribute": "prevent-scroll",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, prevents scrolling outside the popover while it is visible. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "propagateEventOnEscape",
          "attribute": "propagate-event-on-escape",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true` and `hide-on-escape` is `true`, the Escape keydown event bubbles up after closing the popover. Default `false`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "role",
          "attribute": "role",
          "type": "text",
          "default": "dialog",
          "values": [
            "role"
          ],
          "description": "ARIA role applied to the popover host. Default `dialog`. When set to `dialog` or `alertdialog`, the host also gets `aria-modal=\"true\"`; for any other role the `aria-modal` attribute is removed.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "shouldFocusTrapWrap",
          "attribute": "should-focus-trap-wrap",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "showArrow",
          "attribute": "show-arrow",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "renders a directional arrow that points at the trigger. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, constrains the popover's max-height to the available space and adds a vertical scrollbar to the content when needed. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "stackGroupName",
          "attribute": "stack-group-name",
          "type": "string",
          "default": "''",
          "values": [],
          "description": "popovers sharing the same name are stacked as a group by the depth manager. Default `''`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "strategy",
          "attribute": "strategy",
          "type": "select",
          "default": "absolute",
          "values": [
            "absolute",
            "fixed"
          ],
          "description": "`absolute` (positioned relative to the nearest positioned ancestor) or `fixed` (positioned relative to the viewport — use when a scrollable container clips the popover). Default `absolute`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "trigger",
          "attribute": "trigger",
          "type": "text",
          "default": "click",
          "values": [
            "click",
            "mouseenter",
            "focusin",
            "manual"
          ],
          "description": "space-separated list of events that open the popover: `click`, `mouseenter`, `focusin`, or `manual`. When `mouseenter` is set on an interactive popover it is rewritten to `mouseenter click` (so the popover can be re-opened after closing with Escape); on a non-interactive popover it is rewritten to `mouseenter focusin` so keyboard focus also opens it. Default `click`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "triggerID",
          "attribute": "trigger-id",
          "type": "text",
          "default": "''",
          "values": [],
          "description": "The ID of the element that triggers the popover.\nThis attribute is required for the popover to work.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "visible",
          "attribute": "visible",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "opens or closes the popover programmatically. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "zIndex",
          "attribute": "z-index",
          "type": "number",
          "values": [],
          "description": "explicit z-index. When not set, an internal depth manager computes a z-index so stacked popovers layer correctly. Default computed.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for the popover content"
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) This event is dispatched when the popover is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) This event is dispatched when the popover is hidden"
        },
        {
          "name": "created",
          "description": "(React: onCreated) This event is dispatched when the popover is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) This event is dispatched when the popover is destroyed (removed from the DOM)"
        }
      ],
      "cssParts": [
        {
          "name": "popover-close",
          "description": "The close button of the popover."
        },
        {
          "name": "popover-content",
          "description": "The content of the popover."
        },
        {
          "name": "popover-hover-bridge",
          "description": "The hover bridge of the popover."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-popover-arrow-border-radius",
          "description": "radius of the arrow border"
        },
        {
          "name": "--mdc-popover-arrow-border",
          "description": "border of the arrow"
        },
        {
          "name": "--mdc-popover-primary-background-color",
          "description": "primary background color of the popover"
        },
        {
          "name": "--mdc-popover-border-color",
          "description": "border color of the popover"
        },
        {
          "name": "--mdc-popover-inverted-background-color",
          "description": "inverted background color of the popover"
        },
        {
          "name": "--mdc-popover-inverted-border-color",
          "description": "inverted border color of the popover"
        },
        {
          "name": "--mdc-popover-inverted-text-color",
          "description": "inverted text color of the popover"
        },
        {
          "name": "--mdc-popover-elevation-3",
          "description": "elevation of the popover"
        },
        {
          "name": "--mdc-popover-max-width",
          "description": "max width of the popover"
        },
        {
          "name": "--mdc-popover-max-height",
          "description": "max height of the popover"
        },
        {
          "name": "--mdc-popover-width",
          "description": "width of the popover"
        },
        {
          "name": "--mdc-popover-backdrop-color",
          "description": "background color of the backdrop (if backdrop is enabled)"
        }
      ],
      "rules": [
        "id of the element that owns the popover. Required; without it the popover never opens.",
        "space-separated list of events that open the popover: `click`, `mouseenter`, `focusin`, or `manual`. When `mouseenter` is set on an interactive popover it is rewritten to `mouseenter click` (so the popover can be re-opened after closing with Escape); on a non-interactive popover it is rewritten to `mouseenter focusin` so keyboard focus also opens it. Default `click`.",
        "`top` / `top-start` / `top-end` / `bottom` / `bottom-start` / `bottom-end` / `left` / `left-start` / `left-end` / `right` / `right-start` / `right-end`. In RTL layouts, `left`/`right` placements are flipped automatically. Default `bottom`.",
        "`tonal` or `contrast`. Default `tonal`.",
        "opens or closes the popover programmatically. Default `false`.",
        "distance in pixels from the trigger on the placement's main axis. Accepts a number (reflected as an attribute), an object (`{ mainAxis, crossAxis, alignmentAxis }`), or a function for full control. Default `4`.",
        "`absolute` (positioned relative to the nearest positioned ancestor) or `fixed` (positioned relative to the viewport — use when a scrollable container clips the popover). Default `absolute`.",
        "`clippingAncestors` or any CSS selector; describes what overflow is checked against. Default `clippingAncestors`.",
        "`viewport` or `document`; the root boundary used for overflow detection. Default `viewport`.",
        "virtual padding in pixels around the boundary. Default unset.",
        "when `true`, prevents the popover from flipping placement to stay in view. Default `false`.",
        "when `true`, constrains the popover's max-height to the available space and adds a vertical scrollbar to the content when needed. Default `false`.",
        "when `true`, uses Floating UI's `inline` middleware so that the popover positions correctly against multi-line inline trigger elements (e.g. hyperlinks, range selections). Default `false`.",
        "when `true`, repositions on every animation frame. Default `false`.",
        "renders a directional arrow that points at the trigger. Default `false`.",
        "renders a built-in close button in the corner of the popover. Default `false`.",
        "accessible name for the built-in close button. Required when `close-button` is `true`.",
        "when `true`, marks the popover as containing interactive content. Required for `focus-trap`, and changes hover behaviour so the popover does not close while the pointer is over it.",
        "when `true` and `interactive` is `true`, traps keyboard focus inside the popover while it is visible.",
        "zero-based index of the focusable child that should receive focus when the popover opens with `focus-trap` enabled. Default `null` (the first focusable element).",
        "when `true`, prevents scrolling outside the popover while it is visible. Default `false`.",
        "when `true`, renders a backdrop covering the rest of the page (the trigger remains visible above the backdrop). Default `false`.",
        "id of the element the backdrop will be appended to. Default unset.",
        "when `false`, the backdrop is dimmed instead of invisible. Default `true`.",
        "`\"openMs,closeMs\"` open/close delay string (e.g. `\"200,300\"`). Invalid values are coerced to `\"0,0\"`. Default `\"0,0\"`.",
        "when `true`, the Escape key closes the popover. Default `false`.",
        "when `true` and `hide-on-escape` is `true`, the Escape keydown event bubbles up after closing the popover. Default `false`.",
        "when `true`, the popover closes when focus leaves it. Default `false`.",
        "when `true`, a click outside the popover (including on the backdrop) closes it. Default `false`.",
        "when `true`, focus returns to the trigger element after the popover closes. Default `false`.",
        "id of an element the popover should be physically moved to in the DOM (used to escape clipping ancestors). The original DOM location is occupied by an internal `mdc-popoverportal` element so removal stays consistent. Default unset.",
        "explicit z-index. When not set, an internal depth manager computes a z-index so stacked popovers layer correctly. Default computed.",
        "popovers sharing the same name are stacked as a group by the depth manager. Default `''`.",
        "ARIA role applied to the popover host. Default `dialog`. When set to `dialog` or `alertdialog`, the host also gets `aria-modal=\"true\"`; for any other role the `aria-modal` attribute is removed.",
        "id pointing to the element that names the popover. For interactive popovers, defaults to the trigger element when no `aria-label` is provided.",
        "id pointing to descriptive text for the popover.",
        "when `true`, the popover does not write `aria-expanded` or `aria-haspopup` on the trigger element. Use this when multiple popovers share the same trigger so they do not fight over the trigger's ARIA state. Default `false`.",
        "when `true`, a tooltip wired to the same trigger remains visible while this popover is open. When `false`, the tooltip is closed and re-enabled after the popover closes. Default `false`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/popover/index.js\"; // custom element registration\n// or via React wrapper\nimport { Popover } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"popover-trigger\">Open popover</mdc-button>\n\n<mdc-popover triggerid=\"popover-trigger\" placement=\"bottom-start\" show-arrow>\n  Popover content\n</mdc-popover>"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"settings-trigger\">Settings</mdc-button>\n\n<mdc-popover\n  triggerid=\"settings-trigger\"\n  interactive\n  focus-trap\n  hide-on-escape\n  hide-on-outside-click\n  focus-back-to-trigger\n  close-button\n  close-button-aria-label=\"Close settings\"\n  aria-label=\"Settings\"\n>\n  <!-- interactive content -->\n</mdc-popover>"
      }
    ],
    "reactImports": [
      "Popover"
    ],
    "constants": {
      "POPOVER_PLACEMENT": {
        "LEFT_START": "left-start",
        "LEFT": "left",
        "LEFT_END": "left-end",
        "RIGHT_START": "right-start",
        "RIGHT": "right",
        "RIGHT_END": "right-end",
        "TOP_START": "top-start",
        "TOP": "top",
        "TOP_END": "top-end",
        "BOTTOM_START": "bottom-start",
        "BOTTOM": "bottom",
        "BOTTOM_END": "bottom-end"
      },
      "TRIGGER": {
        "CLICK": "click",
        "MOUSEENTER": "mouseenter",
        "FOCUSIN": "focusin",
        "MANUAL": "manual"
      },
      "COLOR": {
        "TONAL": "tonal",
        "CONTRAST": "contrast"
      },
      "STRATEGY": {
        "ABSOLUTE": "absolute",
        "FIXED": "fixed"
      },
      "BOUNDARY_ROOT": {
        "VIEWPORT": "viewport",
        "DOCUMENT": "document"
      },
      "DEFAULTS": {
        "PLACEMENT": "bottom",
        "TRIGGER": "click",
        "COLOR": "tonal",
        "STRATEGY": "absolute",
        "OFFSET": 4,
        "BOUNDARY": "clippingAncestors",
        "BOUNDARY_ROOT": "viewport",
        "BOUNDARY_PADDING": 0,
        "VISIBLE": false,
        "ARROW": false,
        "CLOSE_BUTTON": false,
        "FOCUS_TRAP": false,
        "INTERACTIVE": false,
        "PREVENT_SCROLL": false,
        "HIDE_ON_ESCAPE": false,
        "HIDE_ON_BLUR": false,
        "HIDE_ON_CLICK_OUTSIDE": false,
        "FOCUS_BACK": false,
        "BACKDROP": false,
        "DISABLE_FLIP": false,
        "SIZE": false,
        "DELAY": "0,0",
        "Z_INDEX": 1000,
        "DISABLE_ARIA_EXPANDED": false,
        "PROPAGATE_EVENT_ON_ESCAPE": false,
        "KEEP_CONNECTED_TOOLTIP_OPEN": false,
        "IS_BACKDROP_INVISIBLE": true,
        "ANIMATION_FRAME": false,
        "INLINE": false
      },
      "TIMEOUTS": {
        "OPEN": "open",
        "HOVER": "hover",
        "CLOSE": "close"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "anchored",
      "and",
      "behaviour",
      "button",
      "component",
      "configurable",
      "dismiss",
      "element",
      "floating",
      "focus",
      "for",
      "generic",
      "guidelines",
      "mdc",
      "overlay",
      "overview",
      "placement",
      "popover",
      "the",
      "to",
      "trap",
      "trigger",
      "usage",
      "with"
    ]
  },
  {
    "name": "presence",
    "title": "Presence",
    "tagName": "mdc-presence",
    "summary": "Usage, guidelines, and accessibility for the mdc-presence component — a small status badge that visually represents the availability or activity state of a user or entity.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/presence",
      "knowledgeBasePath": "components/src/components/presence/knowledge-base/presence.component.md"
    },
    "docs": "## Overview\n\nThe presence is a small status badge that visually represents the availability or activity state of a user or entity (for example: active, away, busy, do-not-disturb, on a call, presenting). It is sized to be paired with an avatar so the status renders as a corner indicator on top of the avatar artwork.\n\n### When to use\n\n- Use `mdc-presence` next to or on top of an `mdc-avatar` to indicate the represented user/entity's current availability or activity.\n- Use it whenever the application has a meaningful status signal to communicate alongside an avatar (online, in a meeting, away, etc.).\n\n### When not to use\n\n- Do not use it as a standalone status indicator divorced from an avatar — pair it with `mdc-avatar` so the icon size matches the avatar size.\n- Use `mdc-badge` for counts, dots, or non-status decorations.\n- Use `mdc-icon` if you need an arbitrary icon at custom sizes outside the supported avatar size set.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/presence/index.js\"; // custom element registration\n// or via React wrapper\nimport { Presence } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-presence type=\"active\" size=\"32\"></mdc-presence>\n```\n\n### Property/Attribute details\n\n- `type` — the presence state: `active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`. If an unknown value is supplied the component falls back to `active`. Default `active`.\n- `size` — overall size of the presence badge in pixels: `24`, `32`, `48`, `64`, `72`, `88`, `124`. Default `32`. Presence icons have a minimum rendered size of 14px, so sizes `24`, `32`, and `48` all render the icon at 14px.\n\n## Accessibility\n\n### Built-in features\n\nThe badge is rendered with a presentational icon and an icon-specific background colour driven by the `type`. The component does not own an accessible name — labelling is the consumer's responsibility (typically via the `aria-label` on the surrounding `mdc-avatar`).",
    "sections": {
      "Overview": "The presence is a small status badge that visually represents the availability or activity state of a user or entity (for example: active, away, busy, do-not-disturb, on a call, presenting). It is sized to be paired with an avatar so the status renders as a corner indicator on top of the avatar artwork.\n\n### When to use\n\n- Use `mdc-presence` next to or on top of an `mdc-avatar` to indicate the represented user/entity's current availability or activity.\n- Use it whenever the application has a meaningful status signal to communicate alongside an avatar (online, in a meeting, away, etc.).\n\n### When not to use\n\n- Do not use it as a standalone status indicator divorced from an avatar — pair it with `mdc-avatar` so the icon size matches the avatar size.\n- Use `mdc-badge` for counts, dots, or non-status decorations.\n- Use `mdc-icon` if you need an arbitrary icon at custom sizes outside the supported avatar size set.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/presence/index.js\"; // custom element registration\n// or via React wrapper\nimport { Presence } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-presence type=\"active\" size=\"32\"></mdc-presence>\n```\n\n### Property/Attribute details\n\n- `type` — the presence state: `active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`. If an unknown value is supplied the component falls back to `active`. Default `active`.\n- `size` — overall size of the presence badge in pixels: `24`, `32`, `48`, `64`, `72`, `88`, `124`. Default `32`. Presence icons have a minimum rendered size of 14px, so sizes `24`, `32`, and `48` all render the icon at 14px.",
      "Accessibility": "### Built-in features\n\nThe badge is rendered with a presentational icon and an icon-specific background colour driven by the `type`. The component does not own an accessible name — labelling is the consumer's responsibility (typically via the `aria-label` on the surrounding `mdc-avatar`)."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "size",
          "attribute": "size",
          "type": "unknown",
          "default": "32",
          "values": [
            24,
            32,
            48,
            64,
            72,
            88,
            124
          ],
          "description": "overall size of the presence badge in pixels: `24`, `32`, `48`, `64`, `72`, `88`, `124`. Default `32`. Presence icons have a minimum rendered size of 14px, so sizes `24`, `32`, and `48` all render the icon at 14px.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "unknown",
          "default": "active",
          "values": [
            "active",
            "away",
            "away-calling",
            "busy",
            "dnd",
            "meeting",
            "on-call",
            "on-device",
            "on-mobile",
            "pause",
            "pto",
            "presenting",
            "quiet",
            "scheduled"
          ],
          "description": "the presence state: `active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`. If an unknown value is supplied the component falls back to `active`. Default `active`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "presence-content",
          "description": "The presence content."
        },
        {
          "name": "mdc-presence-icon",
          "description": "The presence icon."
        },
        {
          "name": "mdc-presence-icon__active",
          "description": "The presence icon for active."
        },
        {
          "name": "mdc-presence-icon__away",
          "description": "The presence icon for away."
        },
        {
          "name": "mdc-presence-icon__away-calling",
          "description": "The presence icon for away-calling."
        },
        {
          "name": "mdc-presence-icon__busy",
          "description": "The presence icon for busy."
        },
        {
          "name": "mdc-presence-icon__dnd",
          "description": "The presence icon for dnd."
        },
        {
          "name": "mdc-presence-icon__meeting",
          "description": "The presence icon for meeting."
        },
        {
          "name": "mdc-presence-icon__on-call",
          "description": "The presence icon for on-call."
        },
        {
          "name": "mdc-presence-icon__on-device",
          "description": "The presence icon for on-device."
        },
        {
          "name": "mdc-presence-icon__on-mobile",
          "description": "The presence icon for on-mobile."
        },
        {
          "name": "mdc-presence-icon__pause",
          "description": "The presence icon for pause."
        },
        {
          "name": "mdc-presence-icon__pto",
          "description": "The presence icon for pto."
        },
        {
          "name": "mdc-presence-icon__presenting",
          "description": "The presence icon for presenting."
        },
        {
          "name": "mdc-presence-icon__quiet",
          "description": "The presence icon for quiet."
        },
        {
          "name": "mdc-presence-icon__scheduled",
          "description": "The presence icon for scheduled."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-presence-active-background-color",
          "description": "The background color of the presence type active."
        },
        {
          "name": "--mdc-presence-away-background-color",
          "description": "The background color of the presence type away."
        },
        {
          "name": "--mdc-presence-away-calling-background-color",
          "description": "The background color of the presence type away-calling."
        },
        {
          "name": "--mdc-presence-busy-background-color",
          "description": "The background color of the presence type busy."
        },
        {
          "name": "--mdc-presence-dnd-background-color",
          "description": "The background color of the presence type dnd."
        },
        {
          "name": "--mdc-presence-meeting-background-color",
          "description": "The background color of the presence type meeting."
        },
        {
          "name": "--mdc-presence-on-call-background-color",
          "description": "The background color of the presence type on-call."
        },
        {
          "name": "--mdc-presence-on-device-background-color",
          "description": "The background color of the presence type on-device."
        },
        {
          "name": "--mdc-presence-on-mobile-background-color",
          "description": "The background color of the presence type on-mobile."
        },
        {
          "name": "--mdc-presence-pause-background-color",
          "description": "The background color of the presence type pause."
        },
        {
          "name": "--mdc-presence-pto-background-color",
          "description": "The background color of the presence type pto."
        },
        {
          "name": "--mdc-presence-presenting-background-color",
          "description": "The background color of the presence type presenting."
        },
        {
          "name": "--mdc-presence-quiet-background-color",
          "description": "The background color of the presence type quiet."
        },
        {
          "name": "--mdc-presence-scheduled-background-color",
          "description": "The background color of the presence type scheduled."
        },
        {
          "name": "--mdc-presence-overlay-background-color",
          "description": "The background color of the presence type overlay."
        }
      ],
      "rules": [
        "the presence state: `active`, `away`, `away-calling`, `busy`, `dnd`, `meeting`, `on-call`, `on-device`, `on-mobile`, `pause`, `pto`, `presenting`, `quiet`, `scheduled`. If an unknown value is supplied the component falls back to `active`. Default `active`.",
        "overall size of the presence badge in pixels: `24`, `32`, `48`, `64`, `72`, `88`, `124`. Default `32`. Presence icons have a minimum rendered size of 14px, so sizes `24`, `32`, and `48` all render the icon at 14px."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/presence/index.js\"; // custom element registration\n// or via React wrapper\nimport { Presence } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-presence type=\"active\" size=\"32\"></mdc-presence>"
      }
    ],
    "reactImports": [
      "Presence"
    ],
    "constants": {
      "PRESENCE_TYPE": {
        "ACTIVE": "active",
        "AWAY": "away",
        "AWAY_CALLING": "away-calling",
        "BUSY": "busy",
        "DND": "dnd",
        "MEETING": "meeting",
        "ON_CALL": "on-call",
        "ON_DEVICE": "on-device",
        "ON_MOBILE": "on-mobile",
        "PAUSE": "pause",
        "PTO": "pto",
        "PRESENTING": "presenting",
        "QUIET": "quiet",
        "SCHEDULED": "scheduled"
      },
      "PRESENCE_SIZE": {
        "24": 24,
        "32": 32,
        "48": 48,
        "64": 64,
        "72": 72,
        "88": 88,
        "124": 124
      },
      "DEFAULTS": {
        "TYPE": "active",
        "SIZE": 32
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "activity",
      "and",
      "availability",
      "badge",
      "component",
      "entity",
      "for",
      "guidelines",
      "icon",
      "mdc",
      "of",
      "or",
      "overview",
      "presence",
      "represents",
      "small",
      "state",
      "status",
      "that",
      "the",
      "usage",
      "user",
      "visually"
    ]
  },
  {
    "name": "progressbar",
    "title": "Progressbar",
    "tagName": "mdc-progressbar",
    "summary": "Usage, guidelines, and accessibility for the mdc-progressbar component — a determinate, linear progress indicator with optional label, percentage readout, and helper/validation text.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/progressbar",
      "knowledgeBasePath": "components/src/components/progressbar/knowledge-base/progressbar.component.md"
    },
    "docs": "## Overview\n\nThe progressbar is a determinate, linear progress indicator that visually represents the completion state of an ongoing process (loading, uploading, syncing, etc.). It renders an optional label, an optional helper/validation text, and a coloured bar that fills from 0 to 100 percent.\n\n### When to use\n\n- Use `mdc-progressbar` for a determinate task whose completion percentage is known.\n- Use the `default` variant when the progressbar should stand on its own with a label above it and a percentage readout.\n- Use the `inline` variant when the progressbar must sit on a single line next to its label (e.g. inside a list row).\n\n### When not to use\n\n- Use `mdc-progressspinner` for a circular determinate indicator (for example to indicate progress in a compact area or to surface a completion/error glyph at 100%).\n- Use `mdc-spinner` for an indeterminate \"work in progress\" state where no percentage is known.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/progressbar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Progressbar } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-progressbar\n  label=\"Uploading\"\n  value=\"42\"\n  data-aria-label=\"Upload progress\"\n></mdc-progressbar>\n```\n\nInline variant with helper text:\n\n```html\n<mdc-progressbar\n  variant=\"inline\"\n  label=\"Sync\"\n  value=\"80\"\n  help-text=\"80% complete\"\n  data-aria-label=\"Synchronisation progress\"\n></mdc-progressbar>\n```\n\n### Property/Attribute details\n\n- `variant` — `default` (label and percentage rendered above the bar, helper text below) or `inline` (label rendered next to the bar on one line, no percentage readout). Default `default`.\n- `value` — current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. Default `\"0\"`.\n- `error` — when `true`, switches the bar to the error visual treatment, fills the bar to 100% width, and forces `help-text-type` to `error`. Default `false`.\n- `label` — visible label shown above the bar (default variant) or next to it (inline variant).\n- `help-text` — helper/validation text rendered below the bar.\n- `help-text-type` — derived automatically: `error` when `error` is `true`, `success` when value reaches 100, otherwise `default`. The matching helper icon is rendered.\n- `data-aria-label` — accessible name applied to the bar element (`role=\"progressbar\"`). Required for screen-reader users since the visible label does not provide an accessible name to the role.\n\n## Accessibility\n\n### Built-in features\n\nThe bar element is rendered with `role=\"progressbar\"`, `aria-valuemin=\"0\"`, `aria-valuemax=\"100\"`, and `aria-valuenow` reflecting the clamped percentage so assistive technologies announce progress changes as the value updates. The bar's accessible name is supplied from `data-aria-label`. When `value` reaches 100 the helper icon switches to the success state, and when `error` is `true` it switches to the error state — both convey state visually as well as via the underlying validation type.\n\n#### Internal ARIA managed by the component\n\n| Element             | Attribute       | Value                                                |\n| ------------------- | --------------- | ---------------------------------------------------- |\n| Progress container  | `role`          | `progressbar`                                        |\n| Progress container  | `aria-valuemin` | `0`                                                  |\n| Progress container  | `aria-valuemax` | `100`                                                |\n| Progress container  | `aria-valuenow` | clamped numeric value (`0`–`100`)                    |\n| Progress container  | `aria-label`    | mirrors `data-aria-label`                            |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide `data-aria-label` describing what the progressbar represents (e.g. `\"Upload progress\"`) — without it the progressbar has no accessible name.\n- Use the `label` property for a visible human-readable label; pair it with `data-aria-label` for the same text when no other context is provided.",
    "sections": {
      "Overview": "The progressbar is a determinate, linear progress indicator that visually represents the completion state of an ongoing process (loading, uploading, syncing, etc.). It renders an optional label, an optional helper/validation text, and a coloured bar that fills from 0 to 100 percent.\n\n### When to use\n\n- Use `mdc-progressbar` for a determinate task whose completion percentage is known.\n- Use the `default` variant when the progressbar should stand on its own with a label above it and a percentage readout.\n- Use the `inline` variant when the progressbar must sit on a single line next to its label (e.g. inside a list row).\n\n### When not to use\n\n- Use `mdc-progressspinner` for a circular determinate indicator (for example to indicate progress in a compact area or to surface a completion/error glyph at 100%).\n- Use `mdc-spinner` for an indeterminate \"work in progress\" state where no percentage is known.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/progressbar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Progressbar } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-progressbar\n  label=\"Uploading\"\n  value=\"42\"\n  data-aria-label=\"Upload progress\"\n></mdc-progressbar>\n```\n\nInline variant with helper text:\n\n```html\n<mdc-progressbar\n  variant=\"inline\"\n  label=\"Sync\"\n  value=\"80\"\n  help-text=\"80% complete\"\n  data-aria-label=\"Synchronisation progress\"\n></mdc-progressbar>\n```\n\n### Property/Attribute details\n\n- `variant` — `default` (label and percentage rendered above the bar, helper text below) or `inline` (label rendered next to the bar on one line, no percentage readout). Default `default`.\n- `value` — current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. Default `\"0\"`.\n- `error` — when `true`, switches the bar to the error visual treatment, fills the bar to 100% width, and forces `help-text-type` to `error`. Default `false`.\n- `label` — visible label shown above the bar (default variant) or next to it (inline variant).\n- `help-text` — helper/validation text rendered below the bar.\n- `help-text-type` — derived automatically: `error` when `error` is `true`, `success` when value reaches 100, otherwise `default`. The matching helper icon is rendered.\n- `data-aria-label` — accessible name applied to the bar element (`role=\"progressbar\"`). Required for screen-reader users since the visible label does not provide an accessible name to the role.",
      "Accessibility": "### Built-in features\n\nThe bar element is rendered with `role=\"progressbar\"`, `aria-valuemin=\"0\"`, `aria-valuemax=\"100\"`, and `aria-valuenow` reflecting the clamped percentage so assistive technologies announce progress changes as the value updates. The bar's accessible name is supplied from `data-aria-label`. When `value` reaches 100 the helper icon switches to the success state, and when `error` is `true` it switches to the error state — both convey state visually as well as via the underlying validation type.\n\n#### Internal ARIA managed by the component\n\n| Element             | Attribute       | Value                                                |\n| ------------------- | --------------- | ---------------------------------------------------- |\n| Progress container  | `role`          | `progressbar`                                        |\n| Progress container  | `aria-valuemin` | `0`                                                  |\n| Progress container  | `aria-valuemax` | `100`                                                |\n| Progress container  | `aria-valuenow` | clamped numeric value (`0`–`100`)                    |\n| Progress container  | `aria-label`    | mirrors `data-aria-label`                            |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide `data-aria-label` describing what the progressbar represents (e.g. `\"Upload progress\"`) — without it the progressbar has no accessible name.\n- Use the `label` property for a visible human-readable label; pair it with `data-aria-label` for the same text when no other context is provided."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the bar element (`role=\"progressbar\"`). Required for screen-reader users since the visible label does not provide an accessible name to the role.",
          "source": "storybook"
        },
        {
          "name": "error",
          "attribute": "error",
          "type": "boolean",
          "values": [],
          "description": "when `true`, switches the bar to the error visual treatment, fills the bar to 100% width, and forces `help-text-type` to `error`. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper/validation text rendered below the bar.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label shown above the bar (default variant) or next to it (inline variant).",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "default": "0",
          "values": [],
          "description": "current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. Default `\"0\"`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "unknown",
          "default": "default",
          "values": [
            "default",
            "inline"
          ],
          "description": "`default` (label and percentage rendered above the bar, helper text below) or `inline` (label rendered next to the bar on one line, no percentage readout). Default `default`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-progressbar-background-color",
          "description": "Background color of the remaining progressbar portion."
        },
        {
          "name": "--mdc-progressbar-progress-background-color",
          "description": "Background color of the elapsed progressbar portion."
        },
        {
          "name": "--mdc-progressbar-height",
          "description": "The height of the progressbar."
        },
        {
          "name": "--mdc-progressbar-border-radius",
          "description": "The border radius of the progressbar."
        }
      ],
      "rules": [
        "`default` (label and percentage rendered above the bar, helper text below) or `inline` (label rendered next to the bar on one line, no percentage readout). Default `default`.",
        "current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. Default `\"0\"`.",
        "when `true`, switches the bar to the error visual treatment, fills the bar to 100% width, and forces `help-text-type` to `error`. Default `false`.",
        "visible label shown above the bar (default variant) or next to it (inline variant).",
        "helper/validation text rendered below the bar.",
        "derived automatically: `error` when `error` is `true`, `success` when value reaches 100, otherwise `default`. The matching helper icon is rendered.",
        "accessible name applied to the bar element (`role=\"progressbar\"`). Required for screen-reader users since the visible label does not provide an accessible name to the role."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/progressbar/index.js\"; // custom element registration\n// or via React wrapper\nimport { Progressbar } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-progressbar\n  label=\"Uploading\"\n  value=\"42\"\n  data-aria-label=\"Upload progress\"\n></mdc-progressbar>"
      },
      {
        "language": "html",
        "code": "<mdc-progressbar\n  variant=\"inline\"\n  label=\"Sync\"\n  value=\"80\"\n  help-text=\"80% complete\"\n  data-aria-label=\"Synchronisation progress\"\n></mdc-progressbar>"
      }
    ],
    "reactImports": [
      "Progressbar"
    ],
    "constants": {
      "VARIANT": {
        "DEFAULT": "default",
        "INLINE": "inline"
      },
      "DEFAULTS": {
        "VARIANT": "default"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "determinate",
      "for",
      "guidelines",
      "helper",
      "icon",
      "indicator",
      "label",
      "linear",
      "mdc",
      "optional",
      "overview",
      "percentage",
      "progress",
      "progressbar",
      "readout",
      "text",
      "the",
      "usage",
      "validation",
      "with"
    ]
  },
  {
    "name": "progressspinner",
    "title": "Progressspinner",
    "tagName": "mdc-progressspinner",
    "summary": "Usage, guidelines, and accessibility for the mdc-progressspinner component — a determinate, circular progress indicator with success and error end states.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/progressspinner",
      "knowledgeBasePath": "components/src/components/progressspinner/knowledge-base/progressspinner.component.md"
    },
    "docs": "## Overview\n\nThe progressspinner is a determinate, circular progress indicator built from two concentric SVG arcs — one representing completed work and one representing remaining work. When the value reaches 100 the spinner is replaced by a check-circle success icon; when the component is in an error state it is replaced by an error icon.\n\n### When to use\n\n- Use `mdc-progressspinner` for a determinate task whose completion percentage is known and where a compact, circular indicator fits better than a linear bar.\n- Use it when the final state should be communicated with a clear success or error glyph rather than a filled bar.\n\n### When not to use\n\n- Use `mdc-progressbar` when a linear bar with an explicit percentage readout and label is more appropriate.\n- Use `mdc-spinner` for an indeterminate spinner where no percentage is known.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/progressspinner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Progressspinner } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-progressspinner\n  value=\"60\"\n  data-aria-label=\"Loading\"\n></mdc-progressspinner>\n```\n\nError state:\n\n```html\n<mdc-progressspinner\n  error\n  data-aria-label=\"Upload failed\"\n></mdc-progressspinner>\n```\n\n### Property/Attribute details\n\n- `value` — current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. When `value` reaches `100`, the component renders a check-circle success icon instead of the arcs. Default `\"0\"`.\n- `error` — when `true`, the component renders an error icon in place of the arcs. Default `false`.\n- `data-aria-label` — accessible name applied to the spinner element (`role=\"progressbar\"`) and to the success/error end-state icons.\n\n## Accessibility\n\n### Built-in features\n\nThe spinner element is rendered with `role=\"progressbar\"`, `aria-valuemin=\"0\"`, `aria-valuemax=\"100\"`, and `aria-valuenow` reflecting the clamped percentage. The success and error end-state icons are decorated with `aria-label` from `data-aria-label` so they remain announceable when they replace the spinner.\n\n#### Internal ARIA managed by the component\n\n| Element             | Attribute       | Value                                       |\n| ------------------- | --------------- | ------------------------------------------- |\n| Spinner container   | `role`          | `progressbar`                               |\n| Spinner container   | `aria-valuemin` | `0`                                         |\n| Spinner container   | `aria-valuemax` | `100`                                       |\n| Spinner container   | `aria-valuenow` | clamped numeric value (`0`–`100`)           |\n| Spinner container   | `aria-label`    | mirrors `data-aria-label`                   |\n| Success icon (100%) | `aria-label`    | mirrors `data-aria-label`                   |\n| Error icon (`error`)| `aria-label`    | mirrors `data-aria-label`                   |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide `data-aria-label` describing what is in progress (e.g. `\"Loading\"`, `\"Uploading file\"`) — without it the indicator has no accessible name.",
    "sections": {
      "Overview": "The progressspinner is a determinate, circular progress indicator built from two concentric SVG arcs — one representing completed work and one representing remaining work. When the value reaches 100 the spinner is replaced by a check-circle success icon; when the component is in an error state it is replaced by an error icon.\n\n### When to use\n\n- Use `mdc-progressspinner` for a determinate task whose completion percentage is known and where a compact, circular indicator fits better than a linear bar.\n- Use it when the final state should be communicated with a clear success or error glyph rather than a filled bar.\n\n### When not to use\n\n- Use `mdc-progressbar` when a linear bar with an explicit percentage readout and label is more appropriate.\n- Use `mdc-spinner` for an indeterminate spinner where no percentage is known.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/progressspinner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Progressspinner } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-progressspinner\n  value=\"60\"\n  data-aria-label=\"Loading\"\n></mdc-progressspinner>\n```\n\nError state:\n\n```html\n<mdc-progressspinner\n  error\n  data-aria-label=\"Upload failed\"\n></mdc-progressspinner>\n```\n\n### Property/Attribute details\n\n- `value` — current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. When `value` reaches `100`, the component renders a check-circle success icon instead of the arcs. Default `\"0\"`.\n- `error` — when `true`, the component renders an error icon in place of the arcs. Default `false`.\n- `data-aria-label` — accessible name applied to the spinner element (`role=\"progressbar\"`) and to the success/error end-state icons.",
      "Accessibility": "### Built-in features\n\nThe spinner element is rendered with `role=\"progressbar\"`, `aria-valuemin=\"0\"`, `aria-valuemax=\"100\"`, and `aria-valuenow` reflecting the clamped percentage. The success and error end-state icons are decorated with `aria-label` from `data-aria-label` so they remain announceable when they replace the spinner.\n\n#### Internal ARIA managed by the component\n\n| Element             | Attribute       | Value                                       |\n| ------------------- | --------------- | ------------------------------------------- |\n| Spinner container   | `role`          | `progressbar`                               |\n| Spinner container   | `aria-valuemin` | `0`                                         |\n| Spinner container   | `aria-valuemax` | `100`                                       |\n| Spinner container   | `aria-valuenow` | clamped numeric value (`0`–`100`)           |\n| Spinner container   | `aria-label`    | mirrors `data-aria-label`                   |\n| Success icon (100%) | `aria-label`    | mirrors `data-aria-label`                   |\n| Error icon (`error`)| `aria-label`    | mirrors `data-aria-label`                   |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide `data-aria-label` describing what is in progress (e.g. `\"Loading\"`, `\"Uploading file\"`) — without it the indicator has no accessible name."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the spinner element (`role=\"progressbar\"`) and to the success/error end-state icons.",
          "source": "storybook"
        },
        {
          "name": "error",
          "attribute": "error",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the component renders an error icon in place of the arcs. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "number",
          "values": [],
          "description": "current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. When `value` reaches `100`, the component renders a check-circle success icon instead of the arcs. Default `\"0\"`.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "error-icon",
          "description": "The error icon of the progressspinner."
        },
        {
          "name": "spinner-base",
          "description": "The base of the progressspinner."
        },
        {
          "name": "spinner-container",
          "description": "The container of the progressspinner."
        },
        {
          "name": "spinner-progress",
          "description": "The progress of the progressspinner."
        },
        {
          "name": "spinner-track",
          "description": "The track of the progressspinner."
        },
        {
          "name": "success-icon",
          "description": "The success icon of the progressspinner."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-spinner-size",
          "description": "The size of the spinner."
        },
        {
          "name": "--mdc-track-color",
          "description": "The color of the spinner track."
        },
        {
          "name": "--mdc-progress-color",
          "description": "The color of the spinner progress."
        },
        {
          "name": "--mdc-progress-success-color",
          "description": "The color of the spinner when in success state."
        },
        {
          "name": "--mdc-progress-error-color",
          "description": "The color of the spinner when in error state."
        }
      ],
      "rules": [
        "current progress as a string percentage from `\"0\"` to `\"100\"`. Values are clamped to that range; non-numeric values are treated as `0`. When `value` reaches `100`, the component renders a check-circle success icon instead of the arcs. Default `\"0\"`.",
        "when `true`, the component renders an error icon in place of the arcs. Default `false`.",
        "accessible name applied to the spinner element (`role=\"progressbar\"`) and to the success/error end-state icons."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/progressspinner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Progressspinner } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-progressspinner\n  value=\"60\"\n  data-aria-label=\"Loading\"\n></mdc-progressspinner>"
      },
      {
        "language": "html",
        "code": "<mdc-progressspinner\n  error\n  data-aria-label=\"Upload failed\"\n></mdc-progressspinner>"
      }
    ],
    "reactImports": [
      "Progressspinner"
    ],
    "constants": {
      "DEFAULTS": {
        "RADIUS": 20,
        "CIRCUMFERENCE": "2 * Math.PI * 20",
        "STROKE_WIDTH": "4, // Default stroke width or border width",
        "GAP_SIZE": "7.5, // Gap size between the spinner and the track",
        "ERROR_ICON_SIZE": 2,
        "SUCCESS_ICON_SIZE": 3,
        "ICON_LENGTH_UNIT": "em"
      },
      "ICON_NAME": {
        "CHECK_CIRCLE_BOLD": "'check-circle-bold' as Extract<IconNames, 'check-circle-bold'>",
        "ERROR_LEGACY_FILLED": "'error-legacy-filled' as Extract<IconNames, 'error-legacy-filled'>"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "circular",
      "component",
      "determinate",
      "end",
      "error",
      "for",
      "guidelines",
      "indicator",
      "mdc",
      "overview",
      "progress",
      "progressspinner",
      "states",
      "success",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "radio",
    "title": "Radio",
    "tagName": "mdc-radio",
    "summary": "Usage, guidelines, and accessibility for the mdc-radio component — a single, form-associated radio button with label, optional helper text, custom indicator slot, and keyboard navigation.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/radio",
      "knowledgeBasePath": "components/src/components/radio/knowledge-base/radio.component.md"
    },
    "docs": "## Overview\n\nThe radio is a single, form-associated radio button that lets the user pick exactly one option from a set of mutually exclusive choices. Radios that share a `name` attribute are grouped together — selecting one unchecks the others — and arrow-key navigation moves focus and selection between members of the group.\n\n### When to use\n\n- Use `mdc-radio` (typically inside `mdc-radiogroup`) when the user must pick exactly one option from a short, mutually exclusive list.\n- Use it in forms, surveys, and settings where every option needs to be visible at once.\n\n### When not to use\n\n- Use `mdc-checkbox` when the user can pick zero, one, or more options.\n- Use `mdc-select` or `mdc-combobox` when the list of options is long or should be revealed on demand.\n- Use `mdc-cardradio` when each option should render as a larger, card-shaped surface.\n- Use `mdc-toggle` for an on/off binary control.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/radio/index.js\"; // custom element registration\n// or via React wrapper\nimport { Radio } from '@momentum-design/components/dist/react';\n```\n\nMinimal group — wrap radios in `mdc-radiogroup` so they share the same `name` and so the group is announced as a `radiogroup` to assistive technologies:\n\n```html\n<mdc-radiogroup label=\"Notification preference\" name=\"notifications\">\n  <mdc-radio value=\"email\" label=\"Email\" checked></mdc-radio>\n  <mdc-radio value=\"sms\" label=\"SMS\"></mdc-radio>\n  <mdc-radio value=\"push\" label=\"Push notification\"></mdc-radio>\n</mdc-radiogroup>\n```\n\nStandalone radios that share a `name` attribute behave as a group even without `mdc-radiogroup`:\n\n```html\n<mdc-radio name=\"plan\" value=\"free\" label=\"Free\"></mdc-radio>\n<mdc-radio name=\"plan\" value=\"pro\" label=\"Pro\" checked></mdc-radio>\n```\n\nListen for `change`/`input` to react when the selected option changes; the host emits both events in the same order as the native radio input.\n\n### Property/Attribute details\n\n- `checked` — whether this radio is the selected option in its group. Only one radio per group (`name`) can be checked at a time; the component unchecks the others automatically on selection. Default `false`.\n- `name` — groups radios together. Radios with the same `name` form one radiogroup; navigation and exclusive selection are scoped to the group. Required for group behaviour.\n- `value` — value submitted under `name` when this radio is the checked one in the group. If checked and no `value` is set, the form value is `'on'`.\n- `label` — visible label text rendered next to the radio.\n- `required` — when `true`, at least one radio in the group must be checked for the form to be considered valid. Note: no red asterisk is rendered next to a required radio.\n- `disabled` — when `true`, the radio is non-interactive and removed from the tab order.\n- `readonly` — when `true`, the radio is non-interactive but remains focusable (Space and Enter are suppressed).\n- `soft-disabled` — when `true`, the radio looks disabled but remains focusable so assistive technology can read it.\n- `data-aria-label` — accessible name. Falls back to the `label` property; only required when no visible label is present.\n- `auto-focus-on-mount` — when `true`, the radio is focused after first render.\n- `validation-message` — custom validation message used by native form validation. When set and the radio is required-but-unchecked, the message is shown in a native tooltip on submit (radios do not render a visible validation message themselves — for that, wrap the group in `mdc-radiogroup` and use its `help-text`).\n- `name`, `value`, `validation-message` participate in form submission through ElementInternals; `formResetCallback` unchecks every radio in the group, and `formStateRestoreCallback` rechecks the radio whose `value` matches the restored state.\n\nEvents:\n\n- `input` (React: `onInput`) — fires when the selection changes (before `change`).\n- `change` (React: `onChange`) — fires when the selection changes (after `input`).\n- `focus` (React: `onFocus`) — fires when the radio receives focus.\n\n## Accessibility\n\n### Built-in features\n\nThe host element has `role=\"radio\"` and `aria-checked` is kept in sync with the `checked` property. The component manages roving tab focus within the group automatically: if any radio in the group is checked, only the checked one is tabbable (`tabindex=\"0\"`) and the rest are removed from the tab sequence (`tabindex=\"-1\"`); if no radio is checked, the first non-disabled radio in the group is tabbable. When spatial navigation is active, every radio in the group is tabbable.\n\nKeyboard interaction inside a group: `Arrow Down`/`Arrow Right` moves focus and selection to the next enabled radio; `Arrow Up`/`Arrow Left` moves focus and selection to the previous enabled radio (both wrap around the ends). `Space` selects the focused radio. `Enter` submits the surrounding form via `form.requestSubmit()`. When spatial navigation is active, arrow keys are left to the spatial navigation context and only `Enter` activates the focused radio.\n\nA custom indicator slotted into `indicator` is supported: when content is slotted into the indicator slot, the focus ring is shifted from the default static radio to the entire host element so keyboard focus stays visible.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute      | Value                                                                                                            |\n| -------------- | -------------- | ---------------------------------------------------------------------------------------------------------------- |\n| Radio host     | `role`         | `radio`                                                                                                          |\n| Radio host     | `aria-checked` | mirrors `checked` (`\"true\"` / `\"false\"`)                                                                         |\n| Radio host     | `aria-label`   | mirrors `label` (or `data-aria-label` when no label)                                                             |\n| Radio host     | `tabindex`     | `0` for the checked radio (or the first enabled radio when none is checked); `-1` for the others; `0` for all in spatial-navigation mode |\n| Native label   | `for`          | the `id` of the radio host                                                                                       |\n\n### Implementation requirements\n\n#### General\n\n- Group related radios with the same `name` attribute, or wrap them in `mdc-radiogroup`. Without a shared `name`, exclusive selection and arrow-key navigation do not work.\n- To show a validation message for an invalid group, wrap the radios in `mdc-radiogroup` and set its `help-text` — the radio itself does not render a visible validation message.\n\n#### Labelling\n\n- Provide a `label` on every radio describing the option. When no visible label is present, set `data-aria-label`.\n- Provide a label on the surrounding `mdc-radiogroup` describing what the group represents.\n\n### Notes\n\n- `required` does not render the standard red-asterisk required indicator on radios, even though the native required validation still applies.\n- The `validation-message` set on a radio appears in a native browser tooltip when the radio is invalid on submit; it is not rendered inline by the component.",
    "sections": {
      "Overview": "The radio is a single, form-associated radio button that lets the user pick exactly one option from a set of mutually exclusive choices. Radios that share a `name` attribute are grouped together — selecting one unchecks the others — and arrow-key navigation moves focus and selection between members of the group.\n\n### When to use\n\n- Use `mdc-radio` (typically inside `mdc-radiogroup`) when the user must pick exactly one option from a short, mutually exclusive list.\n- Use it in forms, surveys, and settings where every option needs to be visible at once.\n\n### When not to use\n\n- Use `mdc-checkbox` when the user can pick zero, one, or more options.\n- Use `mdc-select` or `mdc-combobox` when the list of options is long or should be revealed on demand.\n- Use `mdc-cardradio` when each option should render as a larger, card-shaped surface.\n- Use `mdc-toggle` for an on/off binary control.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/radio/index.js\"; // custom element registration\n// or via React wrapper\nimport { Radio } from '@momentum-design/components/dist/react';\n```\n\nMinimal group — wrap radios in `mdc-radiogroup` so they share the same `name` and so the group is announced as a `radiogroup` to assistive technologies:\n\n```html\n<mdc-radiogroup label=\"Notification preference\" name=\"notifications\">\n  <mdc-radio value=\"email\" label=\"Email\" checked></mdc-radio>\n  <mdc-radio value=\"sms\" label=\"SMS\"></mdc-radio>\n  <mdc-radio value=\"push\" label=\"Push notification\"></mdc-radio>\n</mdc-radiogroup>\n```\n\nStandalone radios that share a `name` attribute behave as a group even without `mdc-radiogroup`:\n\n```html\n<mdc-radio name=\"plan\" value=\"free\" label=\"Free\"></mdc-radio>\n<mdc-radio name=\"plan\" value=\"pro\" label=\"Pro\" checked></mdc-radio>\n```\n\nListen for `change`/`input` to react when the selected option changes; the host emits both events in the same order as the native radio input.\n\n### Property/Attribute details\n\n- `checked` — whether this radio is the selected option in its group. Only one radio per group (`name`) can be checked at a time; the component unchecks the others automatically on selection. Default `false`.\n- `name` — groups radios together. Radios with the same `name` form one radiogroup; navigation and exclusive selection are scoped to the group. Required for group behaviour.\n- `value` — value submitted under `name` when this radio is the checked one in the group. If checked and no `value` is set, the form value is `'on'`.\n- `label` — visible label text rendered next to the radio.\n- `required` — when `true`, at least one radio in the group must be checked for the form to be considered valid. Note: no red asterisk is rendered next to a required radio.\n- `disabled` — when `true`, the radio is non-interactive and removed from the tab order.\n- `readonly` — when `true`, the radio is non-interactive but remains focusable (Space and Enter are suppressed).\n- `soft-disabled` — when `true`, the radio looks disabled but remains focusable so assistive technology can read it.\n- `data-aria-label` — accessible name. Falls back to the `label` property; only required when no visible label is present.\n- `auto-focus-on-mount` — when `true`, the radio is focused after first render.\n- `validation-message` — custom validation message used by native form validation. When set and the radio is required-but-unchecked, the message is shown in a native tooltip on submit (radios do not render a visible validation message themselves — for that, wrap the group in `mdc-radiogroup` and use its `help-text`).\n- `name`, `value`, `validation-message` participate in form submission through ElementInternals; `formResetCallback` unchecks every radio in the group, and `formStateRestoreCallback` rechecks the radio whose `value` matches the restored state.\n\nEvents:\n\n- `input` (React: `onInput`) — fires when the selection changes (before `change`).\n- `change` (React: `onChange`) — fires when the selection changes (after `input`).\n- `focus` (React: `onFocus`) — fires when the radio receives focus.",
      "Accessibility": "### Built-in features\n\nThe host element has `role=\"radio\"` and `aria-checked` is kept in sync with the `checked` property. The component manages roving tab focus within the group automatically: if any radio in the group is checked, only the checked one is tabbable (`tabindex=\"0\"`) and the rest are removed from the tab sequence (`tabindex=\"-1\"`); if no radio is checked, the first non-disabled radio in the group is tabbable. When spatial navigation is active, every radio in the group is tabbable.\n\nKeyboard interaction inside a group: `Arrow Down`/`Arrow Right` moves focus and selection to the next enabled radio; `Arrow Up`/`Arrow Left` moves focus and selection to the previous enabled radio (both wrap around the ends). `Space` selects the focused radio. `Enter` submits the surrounding form via `form.requestSubmit()`. When spatial navigation is active, arrow keys are left to the spatial navigation context and only `Enter` activates the focused radio.\n\nA custom indicator slotted into `indicator` is supported: when content is slotted into the indicator slot, the focus ring is shifted from the default static radio to the entire host element so keyboard focus stays visible.\n\n#### Internal ARIA managed by the component\n\n| Element        | Attribute      | Value                                                                                                            |\n| -------------- | -------------- | ---------------------------------------------------------------------------------------------------------------- |\n| Radio host     | `role`         | `radio`                                                                                                          |\n| Radio host     | `aria-checked` | mirrors `checked` (`\"true\"` / `\"false\"`)                                                                         |\n| Radio host     | `aria-label`   | mirrors `label` (or `data-aria-label` when no label)                                                             |\n| Radio host     | `tabindex`     | `0` for the checked radio (or the first enabled radio when none is checked); `-1` for the others; `0` for all in spatial-navigation mode |\n| Native label   | `for`          | the `id` of the radio host                                                                                       |\n\n### Implementation requirements\n\n#### General\n\n- Group related radios with the same `name` attribute, or wrap them in `mdc-radiogroup`. Without a shared `name`, exclusive selection and arrow-key navigation do not work.\n- To show a validation message for an invalid group, wrap the radios in `mdc-radiogroup` and set its `help-text` — the radio itself does not render a visible validation message.\n\n#### Labelling\n\n- Provide a `label` on every radio describing the option. When no visible label is present, set `data-aria-label`.\n- Provide a label on the surrounding `mdc-radiogroup` describing what the group represents.\n\n### Notes\n\n- `required` does not render the standard red-asterisk required indicator on radios, even though the native required validation still applies.\n- The `validation-message` set on a radio appears in a native browser tooltip when the radio is invalid on submit; it is not rendered inline by the component."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-text",
      "mdc-staticradio",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the radio is focused after first render.",
          "source": "storybook"
        },
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "description": "whether this radio is the selected option in its group. Only one radio per group (`name`) can be checked at a time; the component unchecks the others automatically on selection. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the radio is non-interactive and removed from the tab order.",
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label text rendered next to the radio.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "groups radios together. Radios with the same `name` form one radiogroup; navigation and exclusive selection are scoped to the group. Required for group behaviour.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the radio is non-interactive but remains focusable (Space and Enter are suppressed).",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, at least one radio in the group must be checked for the form to be considered valid. Note: no red asterisk is rendered next to a required radio.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the radio looks disabled but remains focusable so assistive technology can read it.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "value submitted under `name` when this radio is the checked one in the group. If checked and no `value` is set, the form value is `'on'`.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "indicator",
          "description": "Slot for the radio indicator element. If not provided, a default styled radio will be rendered."
        },
        {
          "name": "label",
          "description": "Slot for the label of the radio."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) Event that gets dispatched when the radio state changes (before the change event)."
        },
        {
          "name": "change",
          "description": "(React: onChange) Event that gets dispatched when the radio state changes (after the input event)."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Event that gets dispatched when the radio receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "radio-indicator",
          "description": "The staticradio that provides the visual radio appearance."
        }
      ],
      "cssProperties": [],
      "rules": [
        "whether this radio is the selected option in its group. Only one radio per group (`name`) can be checked at a time; the component unchecks the others automatically on selection. Default `false`.",
        "groups radios together. Radios with the same `name` form one radiogroup; navigation and exclusive selection are scoped to the group. Required for group behaviour.",
        "value submitted under `name` when this radio is the checked one in the group. If checked and no `value` is set, the form value is `'on'`.",
        "visible label text rendered next to the radio.",
        "when `true`, at least one radio in the group must be checked for the form to be considered valid. Note: no red asterisk is rendered next to a required radio.",
        "when `true`, the radio is non-interactive and removed from the tab order.",
        "when `true`, the radio is non-interactive but remains focusable (Space and Enter are suppressed).",
        "when `true`, the radio looks disabled but remains focusable so assistive technology can read it.",
        "accessible name. Falls back to the `label` property; only required when no visible label is present.",
        "when `true`, the radio is focused after first render.",
        "custom validation message used by native form validation. When set and the radio is required-but-unchecked, the message is shown in a native tooltip on submit (radios do not render a visible validation message themselves — for that, wrap the group in `mdc-radiogroup` and use its `help-text`)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/radio/index.js\"; // custom element registration\n// or via React wrapper\nimport { Radio } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-radiogroup label=\"Notification preference\" name=\"notifications\">\n  <mdc-radio value=\"email\" label=\"Email\" checked></mdc-radio>\n  <mdc-radio value=\"sms\" label=\"SMS\"></mdc-radio>\n  <mdc-radio value=\"push\" label=\"Push notification\"></mdc-radio>\n</mdc-radiogroup>"
      },
      {
        "language": "html",
        "code": "<mdc-radio name=\"plan\" value=\"free\" label=\"Free\"></mdc-radio>\n<mdc-radio name=\"plan\" value=\"pro\" label=\"Pro\" checked></mdc-radio>"
      }
    ],
    "reactImports": [
      "Radio"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "associated",
      "button",
      "component",
      "custom",
      "for",
      "form",
      "guidelines",
      "helper",
      "icon",
      "indicator",
      "keyboard",
      "label",
      "mdc",
      "navigation",
      "optional",
      "overview",
      "radio",
      "single",
      "slot",
      "staticradio",
      "text",
      "the",
      "toggletip",
      "usage",
      "with"
    ]
  },
  {
    "name": "radiogroup",
    "title": "Radiogroup",
    "tagName": "mdc-radiogroup",
    "summary": "Usage, guidelines, and accessibility for the mdc-radiogroup component — a labelled wrapper that groups radios, propagates name and required state, and exposes the group to assistive tech.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/radiogroup",
      "knowledgeBasePath": "components/src/components/radiogroup/knowledge-base/radiogroup.component.md"
    },
    "docs": "## Overview\n\nThe radiogroup is a labelled wrapper that bundles related `mdc-radio` (or `mdc-cardradio`) elements together. It renders an optional header label and helper text, propagates a shared `name` and `required` state to its radio children, and exposes the wrapped controls as a `radiogroup` to assistive technologies.\n\n### When to use\n\n- Use `mdc-radiogroup` whenever you render a set of `mdc-radio` (or `mdc-cardradio`) options that belong to the same question.\n- Use it to surface a single label and a single helper/validation message for the whole group.\n\n### When not to use\n\n- Use `mdc-formfieldgroup` directly when grouping checkbox or toggle controls instead of radios.\n- Use `mdc-select` or `mdc-combobox` when the user should pick one option from a long or dynamic list that does not need to be visible at once.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/radiogroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { Radiogroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-radiogroup label=\"Plan\" name=\"plan\" required>\n  <mdc-radio value=\"free\" label=\"Free\"></mdc-radio>\n  <mdc-radio value=\"pro\" label=\"Pro\"></mdc-radio>\n  <mdc-radio value=\"enterprise\" label=\"Enterprise\"></mdc-radio>\n</mdc-radiogroup>\n```\n\nWith helper/validation text:\n\n```html\n<mdc-radiogroup\n  label=\"Notification preference\"\n  name=\"notifications\"\n  help-text=\"Pick at least one channel\"\n  help-text-type=\"error\"\n>\n  <mdc-radio value=\"email\" label=\"Email\"></mdc-radio>\n  <mdc-radio value=\"sms\" label=\"SMS\"></mdc-radio>\n</mdc-radiogroup>\n```\n\n### Property/Attribute details\n\n- `name` — name applied to each slotted radio child that does not already have one. Used to group the radios at the form-control level so they exchange selection. Default `''`.\n- `required` — propagated to each slotted radio child on first render (toggled via the `required` attribute on the children).\n- `label` — visible label for the group, also used as the group's `aria-label` when no `aria-label` is otherwise supplied.\n- `help-text` — helper or validation text rendered below the label; also reflected as the group's `aria-description`.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.\n- `toggletip-text` — when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.\n- `info-icon-aria-label` — accessible name for the info-icon button. Required when `toggletip-text` is set.\n- `readonly`, `soft-disabled` — visual states propagated through the wrapper. The group's individual radios own their own disabled/readonly state.\n\n## Accessibility\n\n### Built-in features\n\nThe host element has `role=\"radiogroup\"` so assistive technologies announce the wrapped radios as a single group. The visible `label` is mirrored onto `aria-label` (unless the consumer supplied one explicitly), and `help-text` is mirrored onto `aria-description` so the helper/validation text is announced when the group receives focus.\n\nOn first render, the group walks its assigned radio children and applies its own `name` to any radio that does not already have one, and propagates its `required` state to each radio. This ensures that exclusive selection and required validation are scoped correctly without per-radio wiring.\n\n#### Internal ARIA managed by the component\n\n| Element             | Attribute          | Value                                            |\n| ------------------- | ------------------ | ------------------------------------------------ |\n| Radiogroup host     | `role`             | `radiogroup`                                     |\n| Radiogroup host     | `aria-label`       | mirrors `label` when no `aria-label` is provided |\n| Radiogroup host     | `aria-description` | mirrors `help-text`                              |\n| Slotted radios      | `name`             | set to the group's `name` when missing           |\n| Slotted radios      | `required`         | toggled to match the group's `required`          |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a `label` describing what the group represents. Without a `label` and without an explicit `aria-label`, the group has no accessible name.\n- When `toggletip-text` is set, provide `info-icon-aria-label` for the info-icon button.",
    "sections": {
      "Overview": "The radiogroup is a labelled wrapper that bundles related `mdc-radio` (or `mdc-cardradio`) elements together. It renders an optional header label and helper text, propagates a shared `name` and `required` state to its radio children, and exposes the wrapped controls as a `radiogroup` to assistive technologies.\n\n### When to use\n\n- Use `mdc-radiogroup` whenever you render a set of `mdc-radio` (or `mdc-cardradio`) options that belong to the same question.\n- Use it to surface a single label and a single helper/validation message for the whole group.\n\n### When not to use\n\n- Use `mdc-formfieldgroup` directly when grouping checkbox or toggle controls instead of radios.\n- Use `mdc-select` or `mdc-combobox` when the user should pick one option from a long or dynamic list that does not need to be visible at once.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/radiogroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { Radiogroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-radiogroup label=\"Plan\" name=\"plan\" required>\n  <mdc-radio value=\"free\" label=\"Free\"></mdc-radio>\n  <mdc-radio value=\"pro\" label=\"Pro\"></mdc-radio>\n  <mdc-radio value=\"enterprise\" label=\"Enterprise\"></mdc-radio>\n</mdc-radiogroup>\n```\n\nWith helper/validation text:\n\n```html\n<mdc-radiogroup\n  label=\"Notification preference\"\n  name=\"notifications\"\n  help-text=\"Pick at least one channel\"\n  help-text-type=\"error\"\n>\n  <mdc-radio value=\"email\" label=\"Email\"></mdc-radio>\n  <mdc-radio value=\"sms\" label=\"SMS\"></mdc-radio>\n</mdc-radiogroup>\n```\n\n### Property/Attribute details\n\n- `name` — name applied to each slotted radio child that does not already have one. Used to group the radios at the form-control level so they exchange selection. Default `''`.\n- `required` — propagated to each slotted radio child on first render (toggled via the `required` attribute on the children).\n- `label` — visible label for the group, also used as the group's `aria-label` when no `aria-label` is otherwise supplied.\n- `help-text` — helper or validation text rendered below the label; also reflected as the group's `aria-description`.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.\n- `toggletip-text` — when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.\n- `info-icon-aria-label` — accessible name for the info-icon button. Required when `toggletip-text` is set.\n- `readonly`, `soft-disabled` — visual states propagated through the wrapper. The group's individual radios own their own disabled/readonly state.",
      "Accessibility": "### Built-in features\n\nThe host element has `role=\"radiogroup\"` so assistive technologies announce the wrapped radios as a single group. The visible `label` is mirrored onto `aria-label` (unless the consumer supplied one explicitly), and `help-text` is mirrored onto `aria-description` so the helper/validation text is announced when the group receives focus.\n\nOn first render, the group walks its assigned radio children and applies its own `name` to any radio that does not already have one, and propagates its `required` state to each radio. This ensures that exclusive selection and required validation are scoped correctly without per-radio wiring.\n\n#### Internal ARIA managed by the component\n\n| Element             | Attribute          | Value                                            |\n| ------------------- | ------------------ | ------------------------------------------------ |\n| Radiogroup host     | `role`             | `radiogroup`                                     |\n| Radiogroup host     | `aria-label`       | mirrors `label` when no `aria-label` is provided |\n| Radiogroup host     | `aria-description` | mirrors `help-text`                              |\n| Slotted radios      | `name`             | set to the group's `name` when missing           |\n| Slotted radios      | `required`         | toggled to match the group's `required`          |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a `label` describing what the group represents. Without a `label` and without an explicit `aria-label`, the group has no accessible name.\n- When `toggletip-text` is set, provide `info-icon-aria-label` for the info-icon button."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "unknown",
          "values": [],
          "description": "helper or validation text rendered below the label; also reflected as the group's `aria-description`.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "unknown",
          "values": [],
          "description": "visible label for the group, also used as the group's `aria-label` when no `aria-label` is otherwise supplied.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "unknown",
          "values": [],
          "description": "name applied to each slotted radio child that does not already have one. Used to group the radios at the form-control level so they exchange selection. Default `''`.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "propagated to each slotted radio child on first render (toggled via the `required` attribute on the children).",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "name applied to each slotted radio child that does not already have one. Used to group the radios at the form-control level so they exchange selection. Default `''`.",
        "propagated to each slotted radio child on first render (toggled via the `required` attribute on the children).",
        "visible label for the group, also used as the group's `aria-label` when no `aria-label` is otherwise supplied.",
        "helper or validation text rendered below the label; also reflected as the group's `aria-description`.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon.",
        "when set, renders an info-icon button next to the label that opens an `mdc-toggletip`.",
        "accessible name for the info-icon button. Required when `toggletip-text` is set."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/radiogroup/index.js\"; // custom element registration\n// or via React wrapper\nimport { Radiogroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-radiogroup label=\"Plan\" name=\"plan\" required>\n  <mdc-radio value=\"free\" label=\"Free\"></mdc-radio>\n  <mdc-radio value=\"pro\" label=\"Pro\"></mdc-radio>\n  <mdc-radio value=\"enterprise\" label=\"Enterprise\"></mdc-radio>\n</mdc-radiogroup>"
      },
      {
        "language": "html",
        "code": "<mdc-radiogroup\n  label=\"Notification preference\"\n  name=\"notifications\"\n  help-text=\"Pick at least one channel\"\n  help-text-type=\"error\"\n>\n  <mdc-radio value=\"email\" label=\"Email\"></mdc-radio>\n  <mdc-radio value=\"sms\" label=\"SMS\"></mdc-radio>\n</mdc-radiogroup>"
      }
    ],
    "reactImports": [
      "Radiogroup"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "assistive",
      "component",
      "exposes",
      "for",
      "group",
      "groups",
      "guidelines",
      "labelled",
      "mdc",
      "name",
      "overview",
      "propagates",
      "radiogroup",
      "radios",
      "required",
      "state",
      "tech",
      "that",
      "the",
      "to",
      "usage",
      "wrapper"
    ]
  },
  {
    "name": "responsivesettingsprovider",
    "title": "Responsivesettingsprovider",
    "tagName": "mdc-responsivesettingsprovider",
    "summary": "Usage and guidelines for the mdc-responsivesettingsprovider component — a provider supplying responsive context (media type, popover positioning, fullscreen dialog) to descendants.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/responsivesettingsprovider",
      "knowledgeBasePath": "components/src/components/responsivesettingsprovider/knowledge-base/responsivesettingsprovider.component.md"
    },
    "docs": "## Overview\n\nThe responsivesettingsprovider is a provider component that supplies a shared responsive-settings context to every descendant component that consumes it. The provider does not detect screen size itself — the consumer chooses when and how to update its attributes (via media queries, device-detection scripts, or any other strategy) so the same descendant components can adapt their layout without each one re-implementing breakpoint logic.\n\n### When to use\n\n- Wrap a subtree (typically near the application root) with `mdc-responsivesettingsprovider` when descendant components should adapt their layout based on a shared responsive signal — for example, rendering popovers centred on small screens or forcing dialogs to fullscreen on mobile.\n\n### When not to use\n\n- Do not use it as a generic media-query helper for arbitrary CSS — components that don't read this context will not be affected.\n- Do not use it for theme tokens (use `mdc-themeprovider`) or icon set configuration (use `mdc-iconprovider`).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/responsivesettingsprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Responsivesettingsprovider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example — update attributes from your own breakpoint logic:\n\n```html\n<mdc-responsivesettingsprovider\n  media=\"mobile\"\n  popover-positioning=\"centered\"\n  force-fullscreen-dialog\n>\n  <!-- app content -->\n</mdc-responsivesettingsprovider>\n```\n\nDescendant components can read the context, and CSS rules can target the provider's attributes (e.g. `mdc-some-component[media=\"mobile\"] { … }`) when fine-grained styling is needed.\n\n### Property/Attribute details\n\n- `media` — generic media type indicating the current device class. Validated against a supported set; values outside the set are ignored. Default `unknown`, so descendant components fall back to their default behaviour when no responsive decision has been made yet.\n- `popover-positioning` — controls how popovers position themselves: e.g. anchored close to the trigger or centred on the viewport for small screens. Validated against a supported set; values outside the set are ignored. Default `float`.\n- `force-fullscreen-dialog` — when `true`, descendant dialogs render in fullscreen mode (useful on small screens). Default `false`.\n\nThe provider exposes a static `Context` accessor for descendants and custom consumer code to subscribe to context updates.\n\n## Accessibility\n\n### Built-in features\n\nThe provider is a non-visual context container — it has no interactive surface, no focusable elements, and no ARIA roles of its own. Accessibility of the affected descendant behaviour (focus management, labelling, escape handling) is owned by those descendant components.",
    "sections": {
      "Overview": "The responsivesettingsprovider is a provider component that supplies a shared responsive-settings context to every descendant component that consumes it. The provider does not detect screen size itself — the consumer chooses when and how to update its attributes (via media queries, device-detection scripts, or any other strategy) so the same descendant components can adapt their layout without each one re-implementing breakpoint logic.\n\n### When to use\n\n- Wrap a subtree (typically near the application root) with `mdc-responsivesettingsprovider` when descendant components should adapt their layout based on a shared responsive signal — for example, rendering popovers centred on small screens or forcing dialogs to fullscreen on mobile.\n\n### When not to use\n\n- Do not use it as a generic media-query helper for arbitrary CSS — components that don't read this context will not be affected.\n- Do not use it for theme tokens (use `mdc-themeprovider`) or icon set configuration (use `mdc-iconprovider`).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/responsivesettingsprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Responsivesettingsprovider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example — update attributes from your own breakpoint logic:\n\n```html\n<mdc-responsivesettingsprovider\n  media=\"mobile\"\n  popover-positioning=\"centered\"\n  force-fullscreen-dialog\n>\n  <!-- app content -->\n</mdc-responsivesettingsprovider>\n```\n\nDescendant components can read the context, and CSS rules can target the provider's attributes (e.g. `mdc-some-component[media=\"mobile\"] { … }`) when fine-grained styling is needed.\n\n### Property/Attribute details\n\n- `media` — generic media type indicating the current device class. Validated against a supported set; values outside the set are ignored. Default `unknown`, so descendant components fall back to their default behaviour when no responsive decision has been made yet.\n- `popover-positioning` — controls how popovers position themselves: e.g. anchored close to the trigger or centred on the viewport for small screens. Validated against a supported set; values outside the set are ignored. Default `float`.\n- `force-fullscreen-dialog` — when `true`, descendant dialogs render in fullscreen mode (useful on small screens). Default `false`.\n\nThe provider exposes a static `Context` accessor for descendants and custom consumer code to subscribe to context updates.",
      "Accessibility": "### Built-in features\n\nThe provider is a non-visual context container — it has no interactive surface, no focusable elements, and no ARIA roles of its own. Accessibility of the affected descendant behaviour (focus management, labelling, escape handling) is owned by those descendant components."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "forceFullscreenDialog",
          "attribute": "force-fullscreen-dialog",
          "type": "boolean",
          "default": "DEFAULTS.FORCE_FULLSCREEN_DIALOG",
          "values": [],
          "description": "when `true`, descendant dialogs render in fullscreen mode (useful on small screens). Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "media",
          "attribute": "media",
          "type": "select",
          "values": [],
          "description": "generic media type indicating the current device class. Validated against a supported set; values outside the set are ignored. Default `unknown`, so descendant components fall back to their default behaviour when no responsive decision has been made yet.",
          "source": "storybook"
        },
        {
          "name": "popoverPositioning",
          "attribute": "popover-positioning",
          "type": "select",
          "values": [],
          "description": "controls how popovers position themselves: e.g. anchored close to the trigger or centred on the viewport for small screens. Validated against a supported set; values outside the set are ignored. Default `float`.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "generic media type indicating the current device class. Validated against a supported set; values outside the set are ignored. Default `unknown`, so descendant components fall back to their default behaviour when no responsive decision has been made yet.",
        "controls how popovers position themselves: e.g. anchored close to the trigger or centred on the viewport for small screens. Validated against a supported set; values outside the set are ignored. Default `float`.",
        "when `true`, descendant dialogs render in fullscreen mode (useful on small screens). Default `false`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/responsivesettingsprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Responsivesettingsprovider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-responsivesettingsprovider\n  media=\"mobile\"\n  popover-positioning=\"centered\"\n  force-fullscreen-dialog\n>\n  <!-- app content -->\n</mdc-responsivesettingsprovider>"
      }
    ],
    "reactImports": [
      "Responsivesettingsprovider"
    ],
    "constants": {
      "DEFAULTS": {
        "FORCE_FULLSCREEN_DIALOG": false,
        "POPOVER_POSITIONING": "float",
        "MEDIA": "unknown"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "context",
      "descendants",
      "dialog",
      "for",
      "fullscreen",
      "guidelines",
      "mdc",
      "media",
      "overview",
      "popover",
      "positioning",
      "provider",
      "responsive",
      "responsivesettingsprovider",
      "supplying",
      "the",
      "to",
      "type",
      "usage"
    ]
  },
  {
    "name": "screenreaderannouncer",
    "title": "Screenreaderannouncer",
    "tagName": "mdc-screenreaderannouncer",
    "summary": "Usage, guidelines, and accessibility for the mdc-screenreaderannouncer component — a non-visual helper pushing messages into a shared aria-live region for assistive tech announcements.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/screenreaderannouncer",
      "knowledgeBasePath": "components/src/components/screenreaderannouncer/knowledge-base/screenreaderannouncer.component.md"
    },
    "docs": "## Overview\n\nThe screenreaderannouncer is a non-visual helper component that pushes text into a shared aria-live region in the light DOM so that screen readers announce it. It manages the live-region container, debouncing, and clean-up so consumers only need to set an `announcement` (or call the public `announce()` method) when they want a message read out.\n\n### When to use\n\n- Use it to surface a status update that has no visible UI counterpart — for example, \"5 new messages\", \"Saved\", or \"Failed to load\" — so screen-reader users hear the change.\n- Use it when a visible status message exists but is rendered outside the natural reading order or only briefly, and you want to guarantee assistive-technology announcement.\n\n### When not to use\n\n- Do not use it for content that is already in a visible, ARIA-live container, or for content that the screen reader will naturally announce because of focus changes.\n- Do not use it as a logging or toast surface — it is not a UI component and renders nothing visible.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/screenreaderannouncer/index.js\"; // custom element registration\n// or via React wrapper\nimport { Screenreaderannouncer } from '@momentum-design/components/dist/react';\n```\n\nDeclarative usage — set the `announcement` attribute whenever you want a message read out:\n\n```html\n<mdc-screenreaderannouncer\n  announcement=\"Saved\"\n  data-aria-live=\"polite\"\n></mdc-screenreaderannouncer>\n```\n\nImperative usage — call the public `announce()` method to make ad-hoc announcements with per-call overrides:\n\n```ts\nconst announcer = document.querySelector('mdc-screenreaderannouncer');\nannouncer.announce({\n  announcement: 'File uploaded',\n  ariaLive: 'assertive',\n  delay: 0,\n  timeout: 5000,\n});\n```\n\n### Property/Attribute details\n\n- `announcement` — string that triggers an announcement when set to a non-empty value. After being announced the component clears the attribute back to an empty string. Default `''`.\n- `identity` — id of the element in the light DOM that announcement containers are appended to. When empty, the component sets it to `mdc-screenreaderannouncer-identity` and creates a visually hidden `<div>` with that id; consumers can supply their own id and ensure the matching element exists in the DOM and is visually hidden. Default `''`.\n- `data-aria-live` — `polite` or `assertive`. Sets the `aria-live` value used when creating announcement containers. Default `polite`.\n- `delay` — milliseconds to wait between appending the live-region container and inserting the announcement text inside it. The live region needs to exist before its contents change for assistive technologies to react. Default `150`.\n- `timeout` — milliseconds after which the announcement container is removed from the DOM. Default `20000`.\n- `debounce-time` — milliseconds the component waits after the most recent change to `announcement` before triggering the announcement; coalesces rapid updates. Default `500`.\n- `announce(options)` — public method. `options.announcement` is required; `delay`, `timeout`, and `ariaLive` are optional and override the corresponding instance properties for that call.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders nothing visible. On connect it ensures a visually hidden live-region element exists at `identity` (creating one with `aria-live` set from `data-aria-live` if needed) and on disconnect it removes the live-region element if no other instance is still using the same identity. Each announcement is performed by appending a fresh `<div aria-live=\"…\">` to the identity element, then inserting a `<p>` with the announcement text after the configured `delay` so the live region is observed by assistive technologies before the text changes. Pending timeouts and announcement nodes are cleared on disconnect.\n\n#### Internal ARIA managed by the component\n\n| Element                                | Attribute   | Value                                                                            |\n| -------------------------------------- | ----------- | -------------------------------------------------------------------------------- |\n| Identity element (created when missing)| `id`        | value of `identity` (defaults to `mdc-screenreaderannouncer-identity`)           |\n| Identity element (created when missing)| `class`     | `mdc-screenreaderannouncer__visually-hidden` (visually hidden CSS)               |\n| Per-announcement container             | `aria-live` | per-call override or `data-aria-live` (`polite` by default)                      |\n\n### Notes\n\n- The default `delay` of 150 ms exists because the live region itself is created dynamically. Lowering it can cause assistive technologies to miss the announcement.\n- If several `mdc-screenreaderannouncer` instances share the same `identity`, the `aria-live` value is effectively determined by the first instance that creates an announcement for that identity. Changing `data-aria-live` on later instances does not retroactively update already-created live-region containers.\n- When no `identity` is provided, all instances share a single `mdc-screenreaderannouncer-identity` element appended to `document.body` (or to the closest `aria-modal=\"true\"` ancestor's shadow root when inside a modal dialog).\n- When a custom `identity` is provided, the consumer is responsible for ensuring the element exists in the DOM and is visually hidden, for example:\n\n```css\n#your-custom-announcer-id {\n  clip: rect(0 0 0 0);\n  clip-path: inset(50%);\n  height: 1px;\n  overflow: hidden;\n  position: absolute;\n  white-space: nowrap;\n  width: 1px;\n}\n```",
    "sections": {
      "Overview": "The screenreaderannouncer is a non-visual helper component that pushes text into a shared aria-live region in the light DOM so that screen readers announce it. It manages the live-region container, debouncing, and clean-up so consumers only need to set an `announcement` (or call the public `announce()` method) when they want a message read out.\n\n### When to use\n\n- Use it to surface a status update that has no visible UI counterpart — for example, \"5 new messages\", \"Saved\", or \"Failed to load\" — so screen-reader users hear the change.\n- Use it when a visible status message exists but is rendered outside the natural reading order or only briefly, and you want to guarantee assistive-technology announcement.\n\n### When not to use\n\n- Do not use it for content that is already in a visible, ARIA-live container, or for content that the screen reader will naturally announce because of focus changes.\n- Do not use it as a logging or toast surface — it is not a UI component and renders nothing visible.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/screenreaderannouncer/index.js\"; // custom element registration\n// or via React wrapper\nimport { Screenreaderannouncer } from '@momentum-design/components/dist/react';\n```\n\nDeclarative usage — set the `announcement` attribute whenever you want a message read out:\n\n```html\n<mdc-screenreaderannouncer\n  announcement=\"Saved\"\n  data-aria-live=\"polite\"\n></mdc-screenreaderannouncer>\n```\n\nImperative usage — call the public `announce()` method to make ad-hoc announcements with per-call overrides:\n\n```ts\nconst announcer = document.querySelector('mdc-screenreaderannouncer');\nannouncer.announce({\n  announcement: 'File uploaded',\n  ariaLive: 'assertive',\n  delay: 0,\n  timeout: 5000,\n});\n```\n\n### Property/Attribute details\n\n- `announcement` — string that triggers an announcement when set to a non-empty value. After being announced the component clears the attribute back to an empty string. Default `''`.\n- `identity` — id of the element in the light DOM that announcement containers are appended to. When empty, the component sets it to `mdc-screenreaderannouncer-identity` and creates a visually hidden `<div>` with that id; consumers can supply their own id and ensure the matching element exists in the DOM and is visually hidden. Default `''`.\n- `data-aria-live` — `polite` or `assertive`. Sets the `aria-live` value used when creating announcement containers. Default `polite`.\n- `delay` — milliseconds to wait between appending the live-region container and inserting the announcement text inside it. The live region needs to exist before its contents change for assistive technologies to react. Default `150`.\n- `timeout` — milliseconds after which the announcement container is removed from the DOM. Default `20000`.\n- `debounce-time` — milliseconds the component waits after the most recent change to `announcement` before triggering the announcement; coalesces rapid updates. Default `500`.\n- `announce(options)` — public method. `options.announcement` is required; `delay`, `timeout`, and `ariaLive` are optional and override the corresponding instance properties for that call.",
      "Accessibility": "### Built-in features\n\nThe component renders nothing visible. On connect it ensures a visually hidden live-region element exists at `identity` (creating one with `aria-live` set from `data-aria-live` if needed) and on disconnect it removes the live-region element if no other instance is still using the same identity. Each announcement is performed by appending a fresh `<div aria-live=\"…\">` to the identity element, then inserting a `<p>` with the announcement text after the configured `delay` so the live region is observed by assistive technologies before the text changes. Pending timeouts and announcement nodes are cleared on disconnect.\n\n#### Internal ARIA managed by the component\n\n| Element                                | Attribute   | Value                                                                            |\n| -------------------------------------- | ----------- | -------------------------------------------------------------------------------- |\n| Identity element (created when missing)| `id`        | value of `identity` (defaults to `mdc-screenreaderannouncer-identity`)           |\n| Identity element (created when missing)| `class`     | `mdc-screenreaderannouncer__visually-hidden` (visually hidden CSS)               |\n| Per-announcement container             | `aria-live` | per-call override or `data-aria-live` (`polite` by default)                      |\n\n### Notes\n\n- The default `delay` of 150 ms exists because the live region itself is created dynamically. Lowering it can cause assistive technologies to miss the announcement.\n- If several `mdc-screenreaderannouncer` instances share the same `identity`, the `aria-live` value is effectively determined by the first instance that creates an announcement for that identity. Changing `data-aria-live` on later instances does not retroactively update already-created live-region containers.\n- When no `identity` is provided, all instances share a single `mdc-screenreaderannouncer-identity` element appended to `document.body` (or to the closest `aria-modal=\"true\"` ancestor's shadow root when inside a modal dialog).\n- When a custom `identity` is provided, the consumer is responsible for ensuring the element exists in the DOM and is visually hidden, for example:\n\n```css\n#your-custom-announcer-id {\n  clip: rect(0 0 0 0);\n  clip-path: inset(50%);\n  height: 1px;\n  overflow: hidden;\n  position: absolute;\n  white-space: nowrap;\n  width: 1px;\n}\n```"
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "announcement",
          "attribute": "announcement",
          "type": "text",
          "values": [],
          "description": "string that triggers an announcement when set to a non-empty value. After being announced the component clears the attribute back to an empty string. Default `''`.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLive",
          "attribute": "data-aria-live",
          "type": "select",
          "default": "'polite'",
          "values": [
            "assertive",
            "polite",
            "off"
          ],
          "description": "`polite` or `assertive`. Sets the `aria-live` value used when creating announcement containers. Default `polite`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "debounceTime",
          "attribute": "debounce-time",
          "type": "number",
          "default": "500",
          "values": [],
          "description": "milliseconds the component waits after the most recent change to `announcement` before triggering the announcement; coalesces rapid updates. Default `500`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "delay",
          "attribute": "delay",
          "type": "number",
          "default": "150",
          "values": [],
          "description": "milliseconds to wait between appending the live-region container and inserting the announcement text inside it. The live region needs to exist before its contents change for assistive technologies to react. Default `150`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "identity",
          "attribute": "identity",
          "type": "text",
          "default": "''",
          "values": [],
          "description": "id of the element in the light DOM that announcement containers are appended to. When empty, the component sets it to `mdc-screenreaderannouncer-identity` and creates a visually hidden `<div>` with that id; consumers can supply their own id and ensure the matching element exists in the DOM and is visually hidden. Default `''`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "timeout",
          "attribute": "timeout",
          "type": "number",
          "default": "20_000",
          "values": [],
          "description": "milliseconds after which the announcement container is removed from the DOM. Default `20000`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "string that triggers an announcement when set to a non-empty value. After being announced the component clears the attribute back to an empty string. Default `''`.",
        "id of the element in the light DOM that announcement containers are appended to. When empty, the component sets it to `mdc-screenreaderannouncer-identity` and creates a visually hidden `<div>` with that id; consumers can supply their own id and ensure the matching element exists in the DOM and is visually hidden. Default `''`.",
        "`polite` or `assertive`. Sets the `aria-live` value used when creating announcement containers. Default `polite`.",
        "milliseconds to wait between appending the live-region container and inserting the announcement text inside it. The live region needs to exist before its contents change for assistive technologies to react. Default `150`.",
        "milliseconds after which the announcement container is removed from the DOM. Default `20000`.",
        "milliseconds the component waits after the most recent change to `announcement` before triggering the announcement; coalesces rapid updates. Default `500`.",
        "public method. `options.announcement` is required; `delay`, `timeout`, and `ariaLive` are optional and override the corresponding instance properties for that call."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/screenreaderannouncer/index.js\"; // custom element registration\n// or via React wrapper\nimport { Screenreaderannouncer } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-screenreaderannouncer\n  announcement=\"Saved\"\n  data-aria-live=\"polite\"\n></mdc-screenreaderannouncer>"
      },
      {
        "language": "ts",
        "code": "const announcer = document.querySelector('mdc-screenreaderannouncer');\nannouncer.announce({\n  announcement: 'File uploaded',\n  ariaLive: 'assertive',\n  delay: 0,\n  timeout: 5000,\n});"
      },
      {
        "language": "css",
        "code": "#your-custom-announcer-id {\n  clip: rect(0 0 0 0);\n  clip-path: inset(50%);\n  height: 1px;\n  overflow: hidden;\n  position: absolute;\n  white-space: nowrap;\n  width: 1px;\n}"
      }
    ],
    "reactImports": [
      "Screenreaderannouncer"
    ],
    "constants": {
      "ARIA_LIVE_VALUES": {
        "ASSERTIVE": "assertive",
        "POLITE": "polite",
        "OFF": "off"
      },
      "DEFAULTS": {
        "ARIA_LIVE": "polite",
        "DELAY": 150,
        "IDENTITY": "mdc-screenreaderannouncer-identity",
        "TIMEOUT": "20_000",
        "DEBOUNCE": 500
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "announcements",
      "aria",
      "assistive",
      "component",
      "for",
      "guidelines",
      "helper",
      "into",
      "live",
      "mdc",
      "messages",
      "non",
      "overview",
      "pushing",
      "region",
      "screenreaderannouncer",
      "shared",
      "tech",
      "the",
      "usage",
      "visual"
    ]
  },
  {
    "name": "searchfield",
    "title": "Searchfield",
    "tagName": "mdc-searchfield",
    "summary": "Usage, guidelines, and accessibility for the mdc-searchfield component — a single-line search input that supports inline chip filters and keyboard navigation between chips and text.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/searchfield",
      "knowledgeBasePath": "components/src/components/searchfield/knowledge-base/searchfield.component.md"
    },
    "docs": "## Overview\n\nThe searchfield is a single-line text input tailored for search. It renders a leading search icon, a trailing clear button, and an inline area in front of the text caret where chip filters can be slotted. Chips behave like single characters in the input flow: they share the cursor with the text and can be focused, navigated, and removed with the keyboard.\n\n### When to use\n\n- Use it whenever the user types a query to search a list, table, or other content rendered on the same page.\n- Use it with slotted chip filters (`mdc-inputchip`, `mdc-chip`, `mdc-alertchip`) when the search query is refined by removable filter tokens that should appear inline with the input text.\n\n### When not to use\n\n- Do not use it for a search that opens a popover / listbox of suggestions or results. Use `mdc-searchpopover` instead — pairing this component with a Popover or Listbox is not supported.\n- Do not use it for free-form text entry that is not a search query — use `mdc-input` (or `mdc-textarea`) directly.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/searchfield/index.js\"; // custom element registration\n// or via React wrapper\nimport { Searchfield } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-searchfield placeholder=\"Search messages\"></mdc-searchfield>\n```\n\nWith chip filters slotted in front of the input text:\n\n```html\n<mdc-searchfield placeholder=\"Search\">\n  <mdc-inputchip slot=\"filters\" label=\"From: Ada\"></mdc-inputchip>\n  <mdc-inputchip slot=\"filters\" label=\"Has: attachment\"></mdc-inputchip>\n</mdc-searchfield>\n```\n\n### Property/Attribute details\n\nInherited from `mdc-input` (and `mdc-formfieldwrapper`) — see those components for the full list; the ones most relevant to searchfield are:\n\n- `name`, `value` — form-control identity and current text value.\n- `placeholder` — placeholder text displayed when empty.\n- `readonly`, `disabled`, `soft-disabled`, `autofocus` — visual / interaction states.\n- `clearAriaLabel` — accessible name for the trailing clear button.\n- `max-length`, `min-length` — input length constraints.\n- `label`, `toggletip-text`, `info-icon-aria-label` — label, optional toggletip text, and the accessible name for the info-icon button when the toggletip is enabled.\n\nThe searchfield itself adds:\n\n- `control-type` — `controlled` or `uncontrolled` (default). Controls how chip removal interacts with the DOM (see Notes below).\n- The `leading-icon` defaults to a search icon, the `trailing-button` defaults to a clear button, and the validation/required surfaces (`help-text`, `help-text-type`, `required`, `validation-message`, `prefix-text`) are removed at connect time so they never render — searchfield is not a form-validation surface.\n\n### Notes\n\n- In **uncontrolled** mode (default), when a chip is removed via the UI the component removes the chip from the DOM and dispatches `chipRemove` with the removed chip in `event.detail.chip`. Consumers may listen to the event but do not need to update the DOM themselves.\n- In **controlled** mode (`control-type=\"controlled\"`), the component does NOT remove the chip from the DOM. It still dispatches `chipRemove`; consumers must remove the chip themselves (typically by updating the state that drives which chips are rendered).\n\n## Accessibility\n\n### Built-in features\n\nThe searchfield uses a native `<input type=\"search\">` so screen readers announce it as a search edit field. The slotted chip filters are removed from the natural tab order (`tabindex=\"-1\"`) and reached from the input via keyboard navigation: with the cursor at position 0, `ArrowLeft` focuses the trailing chip; `Backspace` removes the trailing chip; `Home` focuses the first chip from anywhere in the input. Once a chip is focused, `ArrowLeft` / `ArrowRight` move between chips, `Backspace` / `Delete` removes the focused chip, `Escape` clears the whole input, `Home` jumps to the first chip, `End` returns to the input with the caret at the end, and any printable character automatically returns focus to the input so the character is typed there. Removing a chip moves focus to the next chip (or back to the input if no chip remains). When the input regains focus from outside the component the chips area scrolls back to the start so the chips remain visible. `Escape` from the input clears the text and removes all chips, and the `clear` event is fired so consumers can react.\n\n#### Internal ARIA managed by the component\n\n| Element                          | Attribute   | Value                                                           |\n| -------------------------------- | ----------- | --------------------------------------------------------------- |\n| Internal `<input>` element       | `type`      | `search`                                                        |\n| Internal `<input>` element       | `aria-label`| label / `data-aria-label` (inherited from input)                |\n| Slotted chip filters             | `tabindex`  | `-1` (focused chip is temporarily set to `0`)                   |\n| Trailing clear button            | `aria-label`| `clearAriaLabel`                                                |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`) so the input has an accessible name. Without one, the search field has no announceable name.\n- Provide a `clearAriaLabel` so the trailing clear button is announced (for example \"Clear search\").\n- Search results that respond to the field must be rendered inline on the page; if results need to appear in a popover, use `mdc-searchpopover` instead — combining `mdc-searchfield` with `mdc-popover` or `mdc-listbox` is not supported.",
    "sections": {
      "Overview": "The searchfield is a single-line text input tailored for search. It renders a leading search icon, a trailing clear button, and an inline area in front of the text caret where chip filters can be slotted. Chips behave like single characters in the input flow: they share the cursor with the text and can be focused, navigated, and removed with the keyboard.\n\n### When to use\n\n- Use it whenever the user types a query to search a list, table, or other content rendered on the same page.\n- Use it with slotted chip filters (`mdc-inputchip`, `mdc-chip`, `mdc-alertchip`) when the search query is refined by removable filter tokens that should appear inline with the input text.\n\n### When not to use\n\n- Do not use it for a search that opens a popover / listbox of suggestions or results. Use `mdc-searchpopover` instead — pairing this component with a Popover or Listbox is not supported.\n- Do not use it for free-form text entry that is not a search query — use `mdc-input` (or `mdc-textarea`) directly.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/searchfield/index.js\"; // custom element registration\n// or via React wrapper\nimport { Searchfield } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-searchfield placeholder=\"Search messages\"></mdc-searchfield>\n```\n\nWith chip filters slotted in front of the input text:\n\n```html\n<mdc-searchfield placeholder=\"Search\">\n  <mdc-inputchip slot=\"filters\" label=\"From: Ada\"></mdc-inputchip>\n  <mdc-inputchip slot=\"filters\" label=\"Has: attachment\"></mdc-inputchip>\n</mdc-searchfield>\n```\n\n### Property/Attribute details\n\nInherited from `mdc-input` (and `mdc-formfieldwrapper`) — see those components for the full list; the ones most relevant to searchfield are:\n\n- `name`, `value` — form-control identity and current text value.\n- `placeholder` — placeholder text displayed when empty.\n- `readonly`, `disabled`, `soft-disabled`, `autofocus` — visual / interaction states.\n- `clearAriaLabel` — accessible name for the trailing clear button.\n- `max-length`, `min-length` — input length constraints.\n- `label`, `toggletip-text`, `info-icon-aria-label` — label, optional toggletip text, and the accessible name for the info-icon button when the toggletip is enabled.\n\nThe searchfield itself adds:\n\n- `control-type` — `controlled` or `uncontrolled` (default). Controls how chip removal interacts with the DOM (see Notes below).\n- The `leading-icon` defaults to a search icon, the `trailing-button` defaults to a clear button, and the validation/required surfaces (`help-text`, `help-text-type`, `required`, `validation-message`, `prefix-text`) are removed at connect time so they never render — searchfield is not a form-validation surface.\n\n### Notes\n\n- In **uncontrolled** mode (default), when a chip is removed via the UI the component removes the chip from the DOM and dispatches `chipRemove` with the removed chip in `event.detail.chip`. Consumers may listen to the event but do not need to update the DOM themselves.\n- In **controlled** mode (`control-type=\"controlled\"`), the component does NOT remove the chip from the DOM. It still dispatches `chipRemove`; consumers must remove the chip themselves (typically by updating the state that drives which chips are rendered).",
      "Accessibility": "### Built-in features\n\nThe searchfield uses a native `<input type=\"search\">` so screen readers announce it as a search edit field. The slotted chip filters are removed from the natural tab order (`tabindex=\"-1\"`) and reached from the input via keyboard navigation: with the cursor at position 0, `ArrowLeft` focuses the trailing chip; `Backspace` removes the trailing chip; `Home` focuses the first chip from anywhere in the input. Once a chip is focused, `ArrowLeft` / `ArrowRight` move between chips, `Backspace` / `Delete` removes the focused chip, `Escape` clears the whole input, `Home` jumps to the first chip, `End` returns to the input with the caret at the end, and any printable character automatically returns focus to the input so the character is typed there. Removing a chip moves focus to the next chip (or back to the input if no chip remains). When the input regains focus from outside the component the chips area scrolls back to the start so the chips remain visible. `Escape` from the input clears the text and removes all chips, and the `clear` event is fired so consumers can react.\n\n#### Internal ARIA managed by the component\n\n| Element                          | Attribute   | Value                                                           |\n| -------------------------------- | ----------- | --------------------------------------------------------------- |\n| Internal `<input>` element       | `type`      | `search`                                                        |\n| Internal `<input>` element       | `aria-label`| label / `data-aria-label` (inherited from input)                |\n| Slotted chip filters             | `tabindex`  | `-1` (focused chip is temporarily set to `0`)                   |\n| Trailing clear button            | `aria-label`| `clearAriaLabel`                                                |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`) so the input has an accessible name. Without one, the search field has no announceable name.\n- Provide a `clearAriaLabel` so the trailing clear button is announced (for example \"Clear search\").\n- Search results that respond to the field must be rendered inline on the page; if results need to appear in a popover, use `mdc-searchpopover` instead — combining `mdc-searchfield` with `mdc-popover` or `mdc-listbox` is not supported."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "autocapitalize",
          "attribute": "autocapitalize",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "autocomplete",
          "attribute": "autocomplete",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "clearAriaLabel",
          "attribute": "clear-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name for the trailing clear button.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dirname",
          "attribute": "dirname",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "list",
          "attribute": "list",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "maxlength",
          "attribute": "maxlength",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "minlength",
          "attribute": "minlength",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "pattern",
          "attribute": "pattern",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "description": "placeholder text displayed when empty.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "filters",
          "description": "Slot for chip filters rendered inline with the input text"
        },
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        },
        {
          "name": "input",
          "description": "Slot for the input element. If not provided, the input field will be rendered."
        },
        {
          "name": "input-leading-icon",
          "description": "Slot for the leading icon before the input field. If not provided, the `leadingIcon` property will be used to render the leading icon."
        },
        {
          "name": "input-prefix-text",
          "description": "Slot for the prefix text before the input field. If not provided, the `prefixText` property will be used to render the prefix text."
        },
        {
          "name": "trailing-button",
          "description": "Slot for the trailing button to clear the input field. If not provided, the clear button will be rendered when `trailingButton` property is set to true."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the value of the input field changes (every press)."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the value of the input field changes (on blur)."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the input receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the input loses focus."
        },
        {
          "name": "clear",
          "description": "(React: onClear) This event is dispatched when the input text is cleared."
        },
        {
          "name": "chipRemove",
          "description": "(React: onChipRemove) This event is dispatched when a chip filter is removed. The removed chip element is available in event.detail.chip. In `uncontrolled` mode (default) the chip is removed from the DOM automatically; in `controlled` mode only the event is fired and the consumer is responsible for removing the chip."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "leading-icon",
          "description": "The leading icon element that is displayed before the input field."
        },
        {
          "name": "prefix-text",
          "description": "The prefix text element that is displayed before the input field."
        },
        {
          "name": "input-container",
          "description": "The container for the input field, leading icon, prefix text, and trailing button elements."
        },
        {
          "name": "input-section",
          "description": "The container for the input field, leading icon, and prefix text elements."
        },
        {
          "name": "input-text",
          "description": "The input field element."
        },
        {
          "name": "trailing-button",
          "description": "The trailing button element that is displayed to clear the input field when the `trailingButton` property is set to true."
        },
        {
          "name": "searchfield-container",
          "description": "The inline flow container for chips and the input field."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-input-text-color",
          "description": "Text color for the input field"
        },
        {
          "name": "--mdc-input-border-color",
          "description": "Border color for the input container"
        },
        {
          "name": "--mdc-input-background-color",
          "description": "Background color for the input field"
        },
        {
          "name": "--mdc-input-support-text-color",
          "description": "Text color for the help text"
        },
        {
          "name": "--mdc-input-selection-text-color",
          "description": "Text color for the selected text"
        },
        {
          "name": "--mdc-input-selection-background-color",
          "description": "Background color for the selected text"
        }
      ],
      "rules": [
        "placeholder text displayed when empty.",
        "accessible name for the trailing clear button.",
        "`controlled` or `uncontrolled` (default). Controls how chip removal interacts with the DOM (see Notes below)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/searchfield/index.js\"; // custom element registration\n// or via React wrapper\nimport { Searchfield } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-searchfield placeholder=\"Search messages\"></mdc-searchfield>"
      },
      {
        "language": "html",
        "code": "<mdc-searchfield placeholder=\"Search\">\n  <mdc-inputchip slot=\"filters\" label=\"From: Ada\"></mdc-inputchip>\n  <mdc-inputchip slot=\"filters\" label=\"Has: attachment\"></mdc-inputchip>\n</mdc-searchfield>"
      }
    ],
    "reactImports": [
      "Searchfield"
    ],
    "constants": {
      "DEFAULTS": {
        "ICON": "'search-bold' as Extract<IconNames, 'search-bold'>",
        "CLOSE_BTN": true
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "between",
      "chip",
      "chips",
      "component",
      "filters",
      "for",
      "guidelines",
      "inline",
      "input",
      "keyboard",
      "line",
      "mdc",
      "navigation",
      "overview",
      "search",
      "searchfield",
      "single",
      "supports",
      "text",
      "that",
      "the",
      "usage"
    ]
  },
  {
    "name": "searchpopover",
    "title": "Searchpopover",
    "tagName": "mdc-searchpopover",
    "summary": "Usage, guidelines, and accessibility for the mdc-searchpopover component — a searchfield wired to a popover so results or suggestions can appear below with combobox semantics.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/searchpopover",
      "knowledgeBasePath": "components/src/components/searchpopover/knowledge-base/searchpopover.component.md"
    },
    "docs": "## Overview\n\nThe searchpopover is a widget built on top of `mdc-searchfield` that adds a connected `mdc-popover` below the input. The input is exposed as a combobox that owns the popover, so assistive technologies can announce when the popover is open and which content it controls. It is intended for searches whose results are individually actionable (for example navigating to a different URL).\n\n### When to use\n\n- Use it when search results or suggestions appear in a popover anchored to the input and each result performs its own action.\n- Use it together with chip filters (`mdc-inputchip`, `mdc-chip`, `mdc-alertchip`) when the search query is refined by removable tokens.\n\n### When not to use\n\n- Do not use it when search results render inline on the page — use `mdc-searchfield` instead.\n- Do not use it when the popover is a list of options being filtered down by the input — use `mdc-combobox` instead.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/searchpopover/index.js\"; // custom element registration\n// or via React wrapper\nimport { Searchpopover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example — toggle `display-popover` from your own state when there are results to show:\n\n```html\n<mdc-searchpopover\n  placeholder=\"Search\"\n  display-popover\n  popover-aria-label=\"Search results\"\n>\n  <mdc-list>\n    <mdc-listitem label=\"Result 1\"></mdc-listitem>\n    <mdc-listitem label=\"Result 2\"></mdc-listitem>\n  </mdc-list>\n</mdc-searchpopover>\n```\n\nWith chip filters slotted in front of the input text:\n\n```html\n<mdc-searchpopover placeholder=\"Search\" popover-aria-label=\"Suggestions\">\n  <mdc-inputchip slot=\"filters\" label=\"From: Ada\"></mdc-inputchip>\n  <!-- result content in the default slot -->\n</mdc-searchpopover>\n```\n\n### Property/Attribute details\n\nInherits everything from `mdc-searchfield` (and through it from `mdc-input` / `mdc-formfieldwrapper`), notably `name`, `value`, `placeholder`, `readonly`, `disabled`, `soft-disabled`, `clearAriaLabel`, `label`, `data-aria-label`, `max-length`, `min-length`, `control-type` (controlled / uncontrolled chip removal), and the inline chip-filter behaviour.\n\nThe searchpopover adds:\n\n- `display-popover` — when `true`, the popover is visible. Default `false`. Consumers drive this from their own state (for example based on whether results are available).\n- `placement` — popover placement relative to the input. `top-start` or `bottom-start`. Default `bottom-start`.\n- `popover-z-index` — z-index for the popover. Override when the popover must sit above other stacking contexts. Default `1000` (applied by the popover).\n- `popover-aria-label` — accessible name applied to the popover (for example \"Search results\"). Required for an announceable popover.\n\n### Notes\n\n- In **uncontrolled** mode (default), removing a chip via the UI removes it from the DOM and dispatches `chipRemove` with the removed chip in `event.detail.chip`. Consumers may listen but do not need to update the DOM themselves.\n- In **controlled** mode (`control-type=\"controlled\"`), the chip is NOT removed from the DOM — only the `chipRemove` event is fired, and consumers must remove the chip themselves.\n- When the popover hides, focus is returned to the input only if focus is currently inside the searchpopover; if focus moved elsewhere it is left alone.\n- The popover ships with `hide-on-outside-click` and `hide-on-escape`; it does not manage `aria-expanded` itself because the input owns that attribute (see Internal ARIA).\n\n## Accessibility\n\n### Built-in features\n\nThe internal `<input>` is given `role=\"combobox\"` and is wired to the popover via `aria-controls`, `aria-owns`, and `aria-haspopup=\"dialog\"`. `aria-expanded` reflects the `display-popover` state so assistive technologies announce when results are available. The input's `aria-label` mirrors `data-aria-label`, and `aria-describedby` mirrors `data-aria-describedby` (or the helper-text id when helper text is present). The popover is exposed as a dialog with `aria-label` set from `popover-aria-label`. Chip filters behave exactly as in `mdc-searchfield` — they are removed from the natural tab order and navigated via arrow keys / Home / End / Backspace, and the search input continues to scroll the chips into view as focus moves between them. When the popover closes, focus returns to the input as long as focus was inside the component.\n\n#### Internal ARIA managed by the component\n\n| Element                          | Attribute        | Value                                                                       |\n| -------------------------------- | ---------------- | --------------------------------------------------------------------------- |\n| Internal `<input>` element       | `role`           | `combobox`                                                                  |\n| Internal `<input>` element       | `aria-expanded`  | `true` / `false` mirroring `display-popover`                                |\n| Internal `<input>` element       | `aria-controls`  | id of the internal popover                                                  |\n| Internal `<input>` element       | `aria-owns`      | id of the internal popover                                                  |\n| Internal `<input>` element       | `aria-haspopup`  | `dialog`                                                                    |\n| Internal `<input>` element       | `aria-label`     | mirrors `data-aria-label`                                                   |\n| Internal `<input>` element       | `aria-describedby` | helper-text id when present, otherwise mirrors `data-aria-describedby`    |\n| Internal `<input>` element       | `aria-invalid`   | `true` when `helpTextType` is `error`                                       |\n| Internal popover                 | `aria-label`     | `popover-aria-label`                                                        |\n| Slotted chip filters             | `tabindex`       | `-1` (focused chip is temporarily set to `0`)                               |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`) so the input/combobox has an accessible name.\n- Provide `popover-aria-label` so the popover has an announceable name (for example \"Search results\" or \"Suggestions\").\n- Provide `clearAriaLabel` so the trailing clear button is announced (for example \"Clear search\").",
    "sections": {
      "Overview": "The searchpopover is a widget built on top of `mdc-searchfield` that adds a connected `mdc-popover` below the input. The input is exposed as a combobox that owns the popover, so assistive technologies can announce when the popover is open and which content it controls. It is intended for searches whose results are individually actionable (for example navigating to a different URL).\n\n### When to use\n\n- Use it when search results or suggestions appear in a popover anchored to the input and each result performs its own action.\n- Use it together with chip filters (`mdc-inputchip`, `mdc-chip`, `mdc-alertchip`) when the search query is refined by removable tokens.\n\n### When not to use\n\n- Do not use it when search results render inline on the page — use `mdc-searchfield` instead.\n- Do not use it when the popover is a list of options being filtered down by the input — use `mdc-combobox` instead.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/searchpopover/index.js\"; // custom element registration\n// or via React wrapper\nimport { Searchpopover } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example — toggle `display-popover` from your own state when there are results to show:\n\n```html\n<mdc-searchpopover\n  placeholder=\"Search\"\n  display-popover\n  popover-aria-label=\"Search results\"\n>\n  <mdc-list>\n    <mdc-listitem label=\"Result 1\"></mdc-listitem>\n    <mdc-listitem label=\"Result 2\"></mdc-listitem>\n  </mdc-list>\n</mdc-searchpopover>\n```\n\nWith chip filters slotted in front of the input text:\n\n```html\n<mdc-searchpopover placeholder=\"Search\" popover-aria-label=\"Suggestions\">\n  <mdc-inputchip slot=\"filters\" label=\"From: Ada\"></mdc-inputchip>\n  <!-- result content in the default slot -->\n</mdc-searchpopover>\n```\n\n### Property/Attribute details\n\nInherits everything from `mdc-searchfield` (and through it from `mdc-input` / `mdc-formfieldwrapper`), notably `name`, `value`, `placeholder`, `readonly`, `disabled`, `soft-disabled`, `clearAriaLabel`, `label`, `data-aria-label`, `max-length`, `min-length`, `control-type` (controlled / uncontrolled chip removal), and the inline chip-filter behaviour.\n\nThe searchpopover adds:\n\n- `display-popover` — when `true`, the popover is visible. Default `false`. Consumers drive this from their own state (for example based on whether results are available).\n- `placement` — popover placement relative to the input. `top-start` or `bottom-start`. Default `bottom-start`.\n- `popover-z-index` — z-index for the popover. Override when the popover must sit above other stacking contexts. Default `1000` (applied by the popover).\n- `popover-aria-label` — accessible name applied to the popover (for example \"Search results\"). Required for an announceable popover.\n\n### Notes\n\n- In **uncontrolled** mode (default), removing a chip via the UI removes it from the DOM and dispatches `chipRemove` with the removed chip in `event.detail.chip`. Consumers may listen but do not need to update the DOM themselves.\n- In **controlled** mode (`control-type=\"controlled\"`), the chip is NOT removed from the DOM — only the `chipRemove` event is fired, and consumers must remove the chip themselves.\n- When the popover hides, focus is returned to the input only if focus is currently inside the searchpopover; if focus moved elsewhere it is left alone.\n- The popover ships with `hide-on-outside-click` and `hide-on-escape`; it does not manage `aria-expanded` itself because the input owns that attribute (see Internal ARIA).",
      "Accessibility": "### Built-in features\n\nThe internal `<input>` is given `role=\"combobox\"` and is wired to the popover via `aria-controls`, `aria-owns`, and `aria-haspopup=\"dialog\"`. `aria-expanded` reflects the `display-popover` state so assistive technologies announce when results are available. The input's `aria-label` mirrors `data-aria-label`, and `aria-describedby` mirrors `data-aria-describedby` (or the helper-text id when helper text is present). The popover is exposed as a dialog with `aria-label` set from `popover-aria-label`. Chip filters behave exactly as in `mdc-searchfield` — they are removed from the natural tab order and navigated via arrow keys / Home / End / Backspace, and the search input continues to scroll the chips into view as focus moves between them. When the popover closes, focus returns to the input as long as focus was inside the component.\n\n#### Internal ARIA managed by the component\n\n| Element                          | Attribute        | Value                                                                       |\n| -------------------------------- | ---------------- | --------------------------------------------------------------------------- |\n| Internal `<input>` element       | `role`           | `combobox`                                                                  |\n| Internal `<input>` element       | `aria-expanded`  | `true` / `false` mirroring `display-popover`                                |\n| Internal `<input>` element       | `aria-controls`  | id of the internal popover                                                  |\n| Internal `<input>` element       | `aria-owns`      | id of the internal popover                                                  |\n| Internal `<input>` element       | `aria-haspopup`  | `dialog`                                                                    |\n| Internal `<input>` element       | `aria-label`     | mirrors `data-aria-label`                                                   |\n| Internal `<input>` element       | `aria-describedby` | helper-text id when present, otherwise mirrors `data-aria-describedby`    |\n| Internal `<input>` element       | `aria-invalid`   | `true` when `helpTextType` is `error`                                       |\n| Internal popover                 | `aria-label`     | `popover-aria-label`                                                        |\n| Slotted chip filters             | `tabindex`       | `-1` (focused chip is temporarily set to `0`)                               |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a `label` (or `data-aria-label`) so the input/combobox has an accessible name.\n- Provide `popover-aria-label` so the popover has an announceable name (for example \"Search results\" or \"Suggestions\").\n- Provide `clearAriaLabel` so the trailing clear button is announced (for example \"Clear search\")."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "autocapitalize",
          "attribute": "autocapitalize",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "autocomplete",
          "attribute": "autocomplete",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "clearAriaLabel",
          "attribute": "clear-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dirname",
          "attribute": "dirname",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "displayPopover",
          "attribute": "display-popover",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the popover is visible. Default `false`. Consumers drive this from their own state (for example based on whether results are available).",
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "list",
          "attribute": "list",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "maxlength",
          "attribute": "maxlength",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "minlength",
          "attribute": "minlength",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "pattern",
          "attribute": "pattern",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "display-popover",
          "type": "Placement",
          "default": "false",
          "values": [
            "bottom-start",
            "top-start"
          ],
          "description": "popover placement relative to the input. `top-start` or `bottom-start`. Default `bottom-start`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "popoverAriaLabel",
          "attribute": "popover-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the popover (for example \"Search results\"). Required for an announceable popover.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot (=children) for the popover content"
        },
        {
          "name": "filters",
          "description": "Slot for input chips"
        },
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        },
        {
          "name": "input",
          "description": "Slot for the input element. If not provided, the input field will be rendered."
        },
        {
          "name": "input-leading-icon",
          "description": "Slot for the leading icon before the input field. If not provided, the `leadingIcon` property will be used to render the leading icon."
        },
        {
          "name": "input-prefix-text",
          "description": "Slot for the prefix text before the input field. If not provided, the `prefixText` property will be used to render the prefix text."
        },
        {
          "name": "trailing-button",
          "description": "Slot for the trailing button to clear the input field. If not provided, the clear button will be rendered when `trailingButton` property is set to true."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the value of the input field changes (every press)."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the value of the input field changes (on blur)."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the input receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the input loses focus."
        },
        {
          "name": "clear",
          "description": "(React: onClear) This event is dispatched when the input text is cleared."
        },
        {
          "name": "chipRemove",
          "description": "(React: onChipRemove) This event is dispatched when a chip filter is removed. The removed chip element is available in event.detail.chip."
        },
        {
          "name": "shown",
          "description": "(React: onShown) This event is dispatched when the popover is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) This event is dispatched when the popover is hidden"
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "leading-icon",
          "description": "The leading icon element that is displayed before the input field."
        },
        {
          "name": "prefix-text",
          "description": "The prefix text element that is displayed before the input field."
        },
        {
          "name": "input-container",
          "description": "The container for the input field, leading icon, prefix text, and trailing button elements."
        },
        {
          "name": "input-section",
          "description": "The container for the input field, leading icon, and prefix text elements."
        },
        {
          "name": "input-text",
          "description": "The input field element."
        },
        {
          "name": "trailing-button",
          "description": "The trailing button element that is displayed to clear the input field when the `trailingButton` property is set to true."
        },
        {
          "name": "popover-content",
          "description": "The popover content element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-input-text-color",
          "description": "Text color for the input field"
        },
        {
          "name": "--mdc-input-border-color",
          "description": "Border color for the input container"
        },
        {
          "name": "--mdc-input-background-color",
          "description": "Background color for the input field"
        },
        {
          "name": "--mdc-input-support-text-color",
          "description": "Text color for the help text"
        },
        {
          "name": "--mdc-input-selection-text-color",
          "description": "Text color for the selected text"
        },
        {
          "name": "--mdc-input-selection-background-color",
          "description": "Background color for the selected text"
        },
        {
          "name": "--mdc-searchpopover-width",
          "description": "Width of the searchpopover component"
        },
        {
          "name": "--mdc-searchpopover-popover-width",
          "description": "Width of the popover within the searchpopover component"
        },
        {
          "name": "--mdc-searchpopover-popover-height",
          "description": "Height of the popover within the searchpopover component"
        }
      ],
      "rules": [
        "when `true`, the popover is visible. Default `false`. Consumers drive this from their own state (for example based on whether results are available).",
        "popover placement relative to the input. `top-start` or `bottom-start`. Default `bottom-start`.",
        "z-index for the popover. Override when the popover must sit above other stacking contexts. Default `1000` (applied by the popover).",
        "accessible name applied to the popover (for example \"Search results\"). Required for an announceable popover."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/searchpopover/index.js\"; // custom element registration\n// or via React wrapper\nimport { Searchpopover } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-searchpopover\n  placeholder=\"Search\"\n  display-popover\n  popover-aria-label=\"Search results\"\n>\n  <mdc-list>\n    <mdc-listitem label=\"Result 1\"></mdc-listitem>\n    <mdc-listitem label=\"Result 2\"></mdc-listitem>\n  </mdc-list>\n</mdc-searchpopover>"
      },
      {
        "language": "html",
        "code": "<mdc-searchpopover placeholder=\"Search\" popover-aria-label=\"Suggestions\">\n  <mdc-inputchip slot=\"filters\" label=\"From: Ada\"></mdc-inputchip>\n  <!-- result content in the default slot -->\n</mdc-searchpopover>"
      }
    ],
    "reactImports": [
      "Searchpopover"
    ],
    "constants": {
      "DEFAULTS": {
        "DISPLAY_POPOVER": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "appear",
      "below",
      "can",
      "combobox",
      "component",
      "for",
      "guidelines",
      "mdc",
      "or",
      "overview",
      "popover",
      "results",
      "searchfield",
      "searchpopover",
      "semantics",
      "so",
      "suggestions",
      "the",
      "to",
      "usage",
      "wired",
      "with"
    ]
  },
  {
    "name": "select",
    "title": "Select",
    "tagName": "mdc-select",
    "summary": "Usage, guidelines, and accessibility for the mdc-select component — a form-associated dropdown that lets users pick a single value from a predefined list of options.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/select",
      "knowledgeBasePath": "components/src/components/select/knowledge-base/select.component.md"
    },
    "docs": "## Overview\n\nThe select is a form-associated dropdown control that lets the user pick a single value from a predefined list of options. It exposes the standard form field surface (label, help text, validation, required indicator, info toggletip) and renders a popover-backed listbox built from slotted `mdc-option` and `mdc-optgroup` children.\n\nThe component is form-associated and participates in native form submission, validation, and reset.\n\n### When to use\n\n- Use `mdc-select` when the user must pick a single value from a fixed list and typing-to-filter is not required.\n- Use it inside a form when you want native form submission and validation for the selected value.\n- Use it when options are grouped (`mdc-optgroup`) and an optional `mdc-divider` is needed between groups.\n\n### When not to use\n\n- Use `mdc-combobox` when the user benefits from typing to filter the list.\n- Use `mdc-radiogroup` when there are only a handful of options and they should all be visible at once.\n- Use `mdc-input` when free-form text is the goal.\n- Use `mdc-searchfield` when the user is searching content rather than picking a single value.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/select/index.js\"; // custom element registration\n// or via React wrapper\nimport { Select, SelectListBox, Option, OptGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\" name=\"country\" placeholder=\"Pick a country\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"ch\" label=\"Switzerland\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-select>\n```\n\nStructural requirements:\n\n- Wrap every `mdc-option` (and any `mdc-optgroup`) inside an `mdc-selectlistbox`. The `mdc-selectlistbox` carries `role=\"listbox\"` and is required for correct screen-reader behaviour.\n- Give every `mdc-option` a `value` attribute — form submission and value-based selection rely on it.\n- A default option can be marked with the `selected` attribute on the `mdc-option`, or by setting `value` on `mdc-select` to match an option's `value`.\n- Place per-option `mdc-tooltip` siblings outside `mdc-select` and connect them via `triggerid` pointing at the option's `id`.\n- Optionally add `mdc-divider` after each `mdc-optgroup` to separate groups visually.\n\nListen for `change` and `input` to react to a committed selection; both events fire with `{ value, label }` in their `detail`. `click`, `keydown`, and `focus` events also bubble from the host.\n\n### Content guidance\n\n- Provide a visible `label` describing what the user is selecting; reach for `data-aria-label` only when a visible label is not possible.\n- Use `placeholder` as a short hint about the expected selection (\"Pick a country\"), not as a replacement for the label.\n- Keep option labels short — they are rendered inside a fixed-width trigger control.\n\n### Property/Attribute details\n\n- `value` — value of the currently selected option. Setting this attribute updates the visible selection and the form value.\n- `name` — form field name submitted with the form.\n- `placeholder` — placeholder text shown when no value is selected. When a `placeholder` is set and no option carries `selected`, the select starts with no selection; otherwise the first valid option becomes the initial selection.\n- `placement` — popover placement relative to the trigger. `bottom-start` (default) or `top-start`.\n- `boundary` — clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Pair with `strategy=\"fixed\"` to avoid clipping inside scrollable parents.\n- `strategy` — popover positioning strategy. `absolute` (default) or `fixed`.\n- `popover-z-index` — z-index override for the popover (defaults to `1000`).\n- `disable-flip` — when `true`, the dropdown does not flip its position when it hits the boundary. Defaults to `false`.\n- `backdrop-append-to` — id of the element the popover backdrop is appended to (defaults to the select's parent).\n- `label` — visible label rendered above the field; used as the accessible name when set.\n- `data-aria-label` — accessible name fallback when no visible label is rendered.\n- `help-text` — helper or validation text below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n- `required` — when `true`, the form is invalid unless a value is selected.\n- `validation-message` — custom validation message reported through `setCustomValidity` when the select is required but empty.\n- `disabled` — fully disabled; the host is removed from the tab order and the popover cannot open.\n- `soft-disabled` — visually disabled but focusable; the popover cannot open.\n- `readonly` — non-interactive but focusable; the popover cannot open.\n- `auto-focus-on-mount` — when `true`, focuses the select trigger on first render.\n\nEvents dispatched by the host:\n\n- `change` — committed selection (bubbles, composed); `event.detail = { value, label }`.\n- `input` — fires alongside `change` whenever the selection changes; `event.detail = { value, label }`.\n- `click`, `keydown`, `focus` — standard DOM events forwarded from the visual trigger.\n\n## Accessibility\n\n### Built-in features\n\nThe component implements the WAI-ARIA combobox + listbox pattern. The visible trigger carries `role=\"combobox\"` and `aria-haspopup=\"listbox\"`, the popover hosts a slotted `mdc-selectlistbox` (with `role=\"listbox\"`), and the trigger's `aria-controls` is wired to the listbox id. The popover traps focus while open, hides on outside click and Escape, and returns focus to the trigger on close.\n\nKeyboard interaction:\n\n- `ArrowDown`, `ArrowUp`, `Enter` — open the popover from the trigger (without changing the selection).\n- `Space` — opens the popover (handled on `keyup` to mirror native button behaviour); prevents page scroll.\n- `Home` — opens the popover and focuses the first option.\n- `End` — opens the popover and focuses the last option.\n- Any printable character — opens the popover and focuses the next option whose label starts with the typed string (typeahead, with a 500 ms reset between keys).\n- Inside the open popover: `ArrowDown` / `ArrowUp` / `Home` / `End` move focus through the options; printable characters continue typeahead; `Escape` closes the popover and returns focus to the trigger.\n- `Tab` — closes the popover and moves focus out.\n\nForm integration: the component is form-associated, exposes `name` / `value`, participates in form reset (back to the initially selected option) and form state restoration, and reports validity via the hidden native input.\n\nDisabled states: when `disabled`, `soft-disabled`, or `readonly` becomes true while the popover is open, the popover is forced closed.\n\n#### Internal ARIA managed by the component\n\n| Element            | Attribute          | Value                                                                |\n| ------------------ | ------------------ | -------------------------------------------------------------------- |\n| Visual trigger     | `role`             | `combobox`                                                           |\n| Visual trigger     | `aria-haspopup`    | `listbox`                                                            |\n| Visual trigger     | `aria-controls`    | id of the slotted listbox                                            |\n| Visual trigger     | `aria-expanded`    | `true` when the popover is open, otherwise `false`                   |\n| Visual trigger     | `aria-required`    | mirrors `required`                                                   |\n| Visual trigger     | `aria-invalid`     | `true` when `help-text-type=\"error\"`, otherwise `false`              |\n| Visual trigger     | `aria-disabled`    | mirrors `disabled` or `soft-disabled` when set                       |\n| Visual trigger     | `aria-readonly`    | mirrors `readonly` when set                                          |\n| Visual trigger     | `aria-label`       | mirrors `data-aria-label`                                            |\n| Visual trigger     | `aria-labelledby`  | id of the label when `label` is set                                  |\n| Visual trigger     | `tabindex`         | `0` when enabled; `-1` when `disabled`                               |\n| Slotted listbox    | `role`             | `listbox`                                                            |\n| Native hidden input| `aria-hidden`      | `true` (used only for form submission and validity reporting)        |\n| Native hidden input| `tabindex`         | `-1`                                                                 |\n| Option (slotted)   | `tabindex`         | `0` on the focused / selected option, `-1` on the rest               |\n| Option (slotted)   | `selected`         | reflects the currently selected option                               |\n\n### Implementation requirements\n\n#### General\n\n- Always wrap options in `mdc-selectlistbox`; without it the listbox role and option focus semantics are missing.\n- Set a unique `value` on every `mdc-option` so the form value, programmatic `value` updates, and typeahead all work as expected.\n- Place per-option `mdc-tooltip` siblings outside `mdc-select` and connect them via `triggerid` — tooltips slotted inside the listbox break announcement and focus order.\n- For required selects, pair `required` with a meaningful `validation-message`.\n\n#### Labelling\n\n- Provide a `label` whenever possible — `role=\"combobox\"` requires an accessible name.\n- When no visible label is shown, set `data-aria-label` on the host.\n- Use `help-text` with `help-text-type=\"error\"` for validation feedback; the icon and `aria-describedby`-equivalent wiring are handled internally.",
    "sections": {
      "Overview": "The select is a form-associated dropdown control that lets the user pick a single value from a predefined list of options. It exposes the standard form field surface (label, help text, validation, required indicator, info toggletip) and renders a popover-backed listbox built from slotted `mdc-option` and `mdc-optgroup` children.\n\nThe component is form-associated and participates in native form submission, validation, and reset.\n\n### When to use\n\n- Use `mdc-select` when the user must pick a single value from a fixed list and typing-to-filter is not required.\n- Use it inside a form when you want native form submission and validation for the selected value.\n- Use it when options are grouped (`mdc-optgroup`) and an optional `mdc-divider` is needed between groups.\n\n### When not to use\n\n- Use `mdc-combobox` when the user benefits from typing to filter the list.\n- Use `mdc-radiogroup` when there are only a handful of options and they should all be visible at once.\n- Use `mdc-input` when free-form text is the goal.\n- Use `mdc-searchfield` when the user is searching content rather than picking a single value.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/select/index.js\"; // custom element registration\n// or via React wrapper\nimport { Select, SelectListBox, Option, OptGroup } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\" name=\"country\" placeholder=\"Pick a country\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"ch\" label=\"Switzerland\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-select>\n```\n\nStructural requirements:\n\n- Wrap every `mdc-option` (and any `mdc-optgroup`) inside an `mdc-selectlistbox`. The `mdc-selectlistbox` carries `role=\"listbox\"` and is required for correct screen-reader behaviour.\n- Give every `mdc-option` a `value` attribute — form submission and value-based selection rely on it.\n- A default option can be marked with the `selected` attribute on the `mdc-option`, or by setting `value` on `mdc-select` to match an option's `value`.\n- Place per-option `mdc-tooltip` siblings outside `mdc-select` and connect them via `triggerid` pointing at the option's `id`.\n- Optionally add `mdc-divider` after each `mdc-optgroup` to separate groups visually.\n\nListen for `change` and `input` to react to a committed selection; both events fire with `{ value, label }` in their `detail`. `click`, `keydown`, and `focus` events also bubble from the host.\n\n### Content guidance\n\n- Provide a visible `label` describing what the user is selecting; reach for `data-aria-label` only when a visible label is not possible.\n- Use `placeholder` as a short hint about the expected selection (\"Pick a country\"), not as a replacement for the label.\n- Keep option labels short — they are rendered inside a fixed-width trigger control.\n\n### Property/Attribute details\n\n- `value` — value of the currently selected option. Setting this attribute updates the visible selection and the form value.\n- `name` — form field name submitted with the form.\n- `placeholder` — placeholder text shown when no value is selected. When a `placeholder` is set and no option carries `selected`, the select starts with no selection; otherwise the first valid option becomes the initial selection.\n- `placement` — popover placement relative to the trigger. `bottom-start` (default) or `top-start`.\n- `boundary` — clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Pair with `strategy=\"fixed\"` to avoid clipping inside scrollable parents.\n- `strategy` — popover positioning strategy. `absolute` (default) or `fixed`.\n- `popover-z-index` — z-index override for the popover (defaults to `1000`).\n- `disable-flip` — when `true`, the dropdown does not flip its position when it hits the boundary. Defaults to `false`.\n- `backdrop-append-to` — id of the element the popover backdrop is appended to (defaults to the select's parent).\n- `label` — visible label rendered above the field; used as the accessible name when set.\n- `data-aria-label` — accessible name fallback when no visible label is rendered.\n- `help-text` — helper or validation text below the field.\n- `help-text-type` — `default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.\n- `toggletip-text` / `toggletip-placement` / `toggletip-strategy` / `info-icon-aria-label` — opt-in info icon button next to the label that opens an `mdc-toggletip`.\n- `required` — when `true`, the form is invalid unless a value is selected.\n- `validation-message` — custom validation message reported through `setCustomValidity` when the select is required but empty.\n- `disabled` — fully disabled; the host is removed from the tab order and the popover cannot open.\n- `soft-disabled` — visually disabled but focusable; the popover cannot open.\n- `readonly` — non-interactive but focusable; the popover cannot open.\n- `auto-focus-on-mount` — when `true`, focuses the select trigger on first render.\n\nEvents dispatched by the host:\n\n- `change` — committed selection (bubbles, composed); `event.detail = { value, label }`.\n- `input` — fires alongside `change` whenever the selection changes; `event.detail = { value, label }`.\n- `click`, `keydown`, `focus` — standard DOM events forwarded from the visual trigger.",
      "Accessibility": "### Built-in features\n\nThe component implements the WAI-ARIA combobox + listbox pattern. The visible trigger carries `role=\"combobox\"` and `aria-haspopup=\"listbox\"`, the popover hosts a slotted `mdc-selectlistbox` (with `role=\"listbox\"`), and the trigger's `aria-controls` is wired to the listbox id. The popover traps focus while open, hides on outside click and Escape, and returns focus to the trigger on close.\n\nKeyboard interaction:\n\n- `ArrowDown`, `ArrowUp`, `Enter` — open the popover from the trigger (without changing the selection).\n- `Space` — opens the popover (handled on `keyup` to mirror native button behaviour); prevents page scroll.\n- `Home` — opens the popover and focuses the first option.\n- `End` — opens the popover and focuses the last option.\n- Any printable character — opens the popover and focuses the next option whose label starts with the typed string (typeahead, with a 500 ms reset between keys).\n- Inside the open popover: `ArrowDown` / `ArrowUp` / `Home` / `End` move focus through the options; printable characters continue typeahead; `Escape` closes the popover and returns focus to the trigger.\n- `Tab` — closes the popover and moves focus out.\n\nForm integration: the component is form-associated, exposes `name` / `value`, participates in form reset (back to the initially selected option) and form state restoration, and reports validity via the hidden native input.\n\nDisabled states: when `disabled`, `soft-disabled`, or `readonly` becomes true while the popover is open, the popover is forced closed.\n\n#### Internal ARIA managed by the component\n\n| Element            | Attribute          | Value                                                                |\n| ------------------ | ------------------ | -------------------------------------------------------------------- |\n| Visual trigger     | `role`             | `combobox`                                                           |\n| Visual trigger     | `aria-haspopup`    | `listbox`                                                            |\n| Visual trigger     | `aria-controls`    | id of the slotted listbox                                            |\n| Visual trigger     | `aria-expanded`    | `true` when the popover is open, otherwise `false`                   |\n| Visual trigger     | `aria-required`    | mirrors `required`                                                   |\n| Visual trigger     | `aria-invalid`     | `true` when `help-text-type=\"error\"`, otherwise `false`              |\n| Visual trigger     | `aria-disabled`    | mirrors `disabled` or `soft-disabled` when set                       |\n| Visual trigger     | `aria-readonly`    | mirrors `readonly` when set                                          |\n| Visual trigger     | `aria-label`       | mirrors `data-aria-label`                                            |\n| Visual trigger     | `aria-labelledby`  | id of the label when `label` is set                                  |\n| Visual trigger     | `tabindex`         | `0` when enabled; `-1` when `disabled`                               |\n| Slotted listbox    | `role`             | `listbox`                                                            |\n| Native hidden input| `aria-hidden`      | `true` (used only for form submission and validity reporting)        |\n| Native hidden input| `tabindex`         | `-1`                                                                 |\n| Option (slotted)   | `tabindex`         | `0` on the focused / selected option, `-1` on the rest               |\n| Option (slotted)   | `selected`         | reflects the currently selected option                               |\n\n### Implementation requirements\n\n#### General\n\n- Always wrap options in `mdc-selectlistbox`; without it the listbox role and option focus semantics are missing.\n- Set a unique `value` on every `mdc-option` so the form value, programmatic `value` updates, and typeahead all work as expected.\n- Place per-option `mdc-tooltip` siblings outside `mdc-select` and connect them via `triggerid` — tooltips slotted inside the listbox break announcement and focus order.\n- For required selects, pair `required` with a meaningful `validation-message`.\n\n#### Labelling\n\n- Provide a `label` whenever possible — `role=\"combobox\"` requires an accessible name.\n- When no visible label is shown, set `data-aria-label` on the host.\n- Use `help-text` with `help-text-type=\"error\"` for validation feedback; the icon and `aria-describedby`-equivalent wiring are handled internally."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-popover",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, focuses the select trigger on first render.",
          "source": "storybook"
        },
        {
          "name": "backdropAppendTo",
          "attribute": "backdrop-append-to",
          "type": "text",
          "values": [],
          "description": "id of the element the popover backdrop is appended to (defaults to the select's parent).",
          "source": "storybook"
        },
        {
          "name": "boundary",
          "attribute": "boundary",
          "type": "text",
          "default": "'clippingAncestors'",
          "values": [
            "clippingAncestors"
          ],
          "description": "clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Pair with `strategy=\"fixed\"` to avoid clipping inside scrollable parents.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name fallback when no visible label is rendered.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disabled; the host is removed from the tab order and the popover cannot open.",
          "source": "storybook"
        },
        {
          "name": "disableFlip",
          "attribute": "disable-flip",
          "type": "boolean",
          "default": "1000",
          "values": [],
          "description": "when `true`, the dropdown does not flip its position when it hits the boundary. Defaults to `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper or validation text below the field.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "description": "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.",
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label rendered above the field; used as the accessible name when set.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "form field name submitted with the form.",
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "description": "placeholder text shown when no value is selected. When a `placeholder` is set and no option carries `selected`, the select starts with no selection; otherwise the first valid option becomes the initial selection.",
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "select",
          "default": "POPOVER_PLACEMENT.BOTTOM_START",
          "values": [
            "bottom-start",
            "top-start"
          ],
          "description": "popover placement relative to the trigger. `bottom-start` (default) or `top-start`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "popoverZIndex",
          "attribute": "popover-z-index",
          "type": "number",
          "values": [],
          "description": "z-index override for the popover (defaults to `1000`).",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "non-interactive but focusable; the popover cannot open.",
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the form is invalid unless a value is selected.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "visually disabled but focusable; the popover cannot open.",
          "source": "storybook"
        },
        {
          "name": "strategy",
          "attribute": "strategy",
          "type": "select",
          "default": "absolute",
          "values": [],
          "description": "popover positioning strategy. `absolute` (default) or `fixed`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "visualCombobox",
          "attribute": "backdrop-append-to",
          "type": "HTMLDivElement",
          "values": [],
          "description": "id of the element the popover backdrop is appended to (defaults to the select's parent).",
          "reflected": true,
          "source": "component"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot for Selectlistbox including options and/or option group."
        },
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the select is clicked."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the select is changed."
        },
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the select is changed."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the select."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the select receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-select-background-color",
          "description": "The background color of the combobox of select."
        },
        {
          "name": "--mdc-select-text-color",
          "description": "The text color of the select."
        },
        {
          "name": "--mdc-select-border-color",
          "description": "The border color of the select."
        },
        {
          "name": "--mdc-select-height",
          "description": "The height of the select trigger control."
        },
        {
          "name": "--mdc-select-width",
          "description": "The width of the select."
        },
        {
          "name": "--mdc-select-listbox-height",
          "description": "The height of the listbox inside the select."
        },
        {
          "name": "--mdc-select-listbox-width",
          "description": "The width of the listbox inside the select (default: `--mdc-select-width`)."
        }
      ],
      "rules": [
        "value of the currently selected option. Setting this attribute updates the visible selection and the form value.",
        "form field name submitted with the form.",
        "placeholder text shown when no value is selected. When a `placeholder` is set and no option carries `selected`, the select starts with no selection; otherwise the first valid option becomes the initial selection.",
        "popover placement relative to the trigger. `bottom-start` (default) or `top-start`.",
        "clipping ancestor used by the popover (`clippingAncestors` by default, or any CSS selector). Pair with `strategy=\"fixed\"` to avoid clipping inside scrollable parents.",
        "popover positioning strategy. `absolute` (default) or `fixed`.",
        "z-index override for the popover (defaults to `1000`).",
        "when `true`, the dropdown does not flip its position when it hits the boundary. Defaults to `false`.",
        "id of the element the popover backdrop is appended to (defaults to the select's parent).",
        "visible label rendered above the field; used as the accessible name when set.",
        "accessible name fallback when no visible label is rendered.",
        "helper or validation text below the field.",
        "`default`, `error`, `warning`, `success`, or `priority`. Drives the helper icon and error styling.",
        "when `true`, the form is invalid unless a value is selected.",
        "custom validation message reported through `setCustomValidity` when the select is required but empty.",
        "fully disabled; the host is removed from the tab order and the popover cannot open.",
        "visually disabled but focusable; the popover cannot open.",
        "non-interactive but focusable; the popover cannot open.",
        "when `true`, focuses the select trigger on first render.",
        "committed selection (bubbles, composed); `event.detail = { value, label }`.",
        "fires alongside `change` whenever the selection changes; `event.detail = { value, label }`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/select/index.js\"; // custom element registration\n// or via React wrapper\nimport { Select, SelectListBox, Option, OptGroup } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-select label=\"Country\" name=\"country\" placeholder=\"Pick a country\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n    <mdc-option value=\"ch\" label=\"Switzerland\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-select>"
      }
    ],
    "reactImports": [
      "Select",
      "SelectListBox",
      "Option",
      "OptGroup"
    ],
    "constants": {
      "ARROW_ICON": {
        "ARROW_UP": "'arrow-up-bold' as Extract<IconNames, 'arrow-up-bold'>",
        "ARROW_DOWN": "'arrow-down-bold' as Extract<IconNames, 'arrow-down-bold'>"
      },
      "DEFAULTS": {
        "DISABLE_FLIP": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "associated",
      "button",
      "component",
      "dropdown",
      "for",
      "form",
      "from",
      "guidelines",
      "icon",
      "lets",
      "list",
      "mdc",
      "of",
      "options",
      "overview",
      "pick",
      "popover",
      "predefined",
      "select",
      "single",
      "text",
      "that",
      "the",
      "toggletip",
      "usage",
      "users",
      "value"
    ]
  },
  {
    "name": "selectlistbox",
    "title": "Select List Box",
    "tagName": "mdc-selectlistbox",
    "summary": "Usage, guidelines, and accessibility for the mdc-selectlistbox component — a light-DOM wrapper that gives `mdc-option` children the correct listbox role inside `mdc-select` and `mdc-combobox`.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/selectlistbox",
      "knowledgeBasePath": "components/src/components/selectlistbox/knowledge-base/selectlistbox.component.md"
    },
    "docs": "## Overview\n\n`mdc-selectlistbox` is a light-DOM wrapper that groups `mdc-option` (and `mdc-optgroup`) children and exposes them with `role=\"listbox\"` so screen readers announce the option list correctly. It is required as a slotted child of `mdc-select` and `mdc-combobox`.\n\n### When to use\n\n- Use `mdc-selectlistbox` as the direct child of `mdc-select` or `mdc-combobox`, wrapping the slotted `mdc-option` and `mdc-optgroup` elements.\n\n### When not to use\n\n- Do not use it outside `mdc-select` or `mdc-combobox`. For a standalone listbox UI, use the dedicated `mdc-listbox` component instead.\n- Do not place `mdc-option` directly inside `mdc-select` or `mdc-combobox` without this wrapper — the listbox role and screen-reader semantics would be missing.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/selectlistbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { SelectListBox, Option } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-select>\n```\n\n### Notes\n\n`mdc-selectlistbox` renders into the light DOM (it does not create its own shadow root) so the slotted `mdc-option` children stay accessible to the parent `mdc-select` / `mdc-combobox` for keyboard navigation, selection, and form handling. Once the platform supports `ElementInternals.ariaOwnsElements`, this wrapper component is expected to be removed in favour of `aria-owns` wiring between the parent and the options.\n\n## Accessibility\n\n### Built-in features\n\nOn `connectedCallback` the host sets `role=\"listbox\"` on itself. The parent `mdc-select` or `mdc-combobox` is responsible for wiring `aria-controls`, `aria-expanded`, and focus management between its trigger and the listbox.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value     |\n| ------- | --------- | --------- |\n| Host    | `role`    | `listbox` |\n\n### Implementation requirements\n\n#### General\n\n- Always use `mdc-selectlistbox` as the direct child of `mdc-select` or `mdc-combobox`.\n- Place every `mdc-option` (and `mdc-optgroup`) inside this wrapper.\n\n#### Labelling\n\n- The parent (`mdc-select` / `mdc-combobox`) supplies the accessible name; no labelling work is required on `mdc-selectlistbox` itself.",
    "sections": {
      "Overview": "`mdc-selectlistbox` is a light-DOM wrapper that groups `mdc-option` (and `mdc-optgroup`) children and exposes them with `role=\"listbox\"` so screen readers announce the option list correctly. It is required as a slotted child of `mdc-select` and `mdc-combobox`.\n\n### When to use\n\n- Use `mdc-selectlistbox` as the direct child of `mdc-select` or `mdc-combobox`, wrapping the slotted `mdc-option` and `mdc-optgroup` elements.\n\n### When not to use\n\n- Do not use it outside `mdc-select` or `mdc-combobox`. For a standalone listbox UI, use the dedicated `mdc-listbox` component instead.\n- Do not place `mdc-option` directly inside `mdc-select` or `mdc-combobox` without this wrapper — the listbox role and screen-reader semantics would be missing.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/selectlistbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { SelectListBox, Option } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-select label=\"Country\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-select>\n```\n\n### Notes\n\n`mdc-selectlistbox` renders into the light DOM (it does not create its own shadow root) so the slotted `mdc-option` children stay accessible to the parent `mdc-select` / `mdc-combobox` for keyboard navigation, selection, and form handling. Once the platform supports `ElementInternals.ariaOwnsElements`, this wrapper component is expected to be removed in favour of `aria-owns` wiring between the parent and the options.",
      "Accessibility": "### Built-in features\n\nOn `connectedCallback` the host sets `role=\"listbox\"` on itself. The parent `mdc-select` or `mdc-combobox` is responsible for wiring `aria-controls`, `aria-expanded`, and focus management between its trigger and the listbox.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value     |\n| ------- | --------- | --------- |\n| Host    | `role`    | `listbox` |\n\n### Implementation requirements\n\n#### General\n\n- Always use `mdc-selectlistbox` as the direct child of `mdc-select` or `mdc-combobox`.\n- Place every `mdc-option` (and `mdc-optgroup`) inside this wrapper.\n\n#### Labelling\n\n- The parent (`mdc-select` / `mdc-combobox`) supplies the accessible name; no labelling work is required on `mdc-selectlistbox` itself."
    },
    "dependencies": [],
    "api": {
      "properties": [],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot, which can be used to insert mdc-option components."
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/selectlistbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { SelectListBox, Option } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-select label=\"Country\">\n  <mdc-selectlistbox>\n    <mdc-option value=\"at\" label=\"Austria\"></mdc-option>\n    <mdc-option value=\"de\" label=\"Germany\"></mdc-option>\n  </mdc-selectlistbox>\n</mdc-select>"
      }
    ],
    "reactImports": [
      "SelectListBox",
      "Option"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "box",
      "children",
      "combobox",
      "component",
      "correct",
      "dom",
      "for",
      "gives",
      "guidelines",
      "inside",
      "light",
      "list",
      "listbox",
      "mdc",
      "option",
      "overview",
      "role",
      "select",
      "selectlistbox",
      "that",
      "the",
      "usage",
      "wrapper"
    ]
  },
  {
    "name": "sidenavigation",
    "title": "Side Navigation",
    "tagName": "mdc-sidenavigation",
    "summary": "Usage, guidelines, and accessibility for the mdc-sidenavigation component — a vertically stacked navigation landmark with expand/collapse behaviour and support for nested menu items.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/sidenavigation",
      "knowledgeBasePath": "components/src/components/sidenavigation/knowledge-base/sidenavigation.component.md"
    },
    "docs": "## Overview\n\nThe side navigation is a vertically stacked navigation landmark, typically used as a persistent or collapsible sidebar. It supports five layout variants, toggleable expand/collapse behaviour, an optional brand logo, an optional footer text, and acts as a context provider for nested `mdc-menubar` and `mdc-navmenuitem` elements.\n\n### When to use\n\n- Use `mdc-sidenavigation` for primary, persistent in-app navigation rendered alongside the main content area.\n- Use it when you need an expandable/collapsible sidebar with icon-only and icon-plus-label modes.\n- Use it as the host of nested `mdc-menubar` and `mdc-navmenuitem` content (with optional submenus via `mdc-menupopover`).\n\n### When not to use\n\n- Use `mdc-appheader` for top-of-page navigation.\n- Use `mdc-menubar` on its own when no expand/collapse, branding, or fixed footer area is needed.\n- Use `mdc-tablist` when navigation is scoped to switching between sibling views inside a single page.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/sidenavigation/index.js\"; // custom element registration\n// or via React wrapper\nimport { SideNavigation, MenuBar, NavMenuItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-sidenavigation\n  variant=\"flexible\"\n  footer-text=\"Acme Corp\"\n  grabber-btn-aria-label=\"Toggle navigation\"\n>\n  <mdc-menubar slot=\"scrollable-menubar\" aria-label=\"Main\">\n    <mdc-navmenuitem nav-id=\"home\" aria-label=\"Home\" icon-name=\"home-bold\"></mdc-navmenuitem>\n    <mdc-navmenuitem nav-id=\"messages\" aria-label=\"Messages\" icon-name=\"chat-bold\"></mdc-navmenuitem>\n  </mdc-menubar>\n\n  <mdc-icon slot=\"brand-logo\" name=\"logo-bold\"></mdc-icon>\n</mdc-sidenavigation>\n```\n\nSlot structure:\n\n- `scrollable-section` — full custom content for the scrollable area. Replaces the default menubar wrapper.\n- `scrollable-menubar` — items rendered inside the default scrollable `mdc-menubar` wrapper.\n- `fixed-section` — full custom content for the bottom fixed area.\n- `fixed-menubar` — items rendered inside the default fixed `mdc-menubar` wrapper.\n- `brand-logo` — informative icon or image rendered next to the footer text (only visible when `footer-text` is set).\n\nNavMenuItem patterns:\n\n1. **Simple navmenuitem** — No submenu or action beyond selection.\n\n2. **NavMenuItem with flyout submenu**:\n   - Add an `id` to the `mdc-navmenuitem`.\n   - Set `triggerId` on the linked `mdc-menupopover` to match that `id`.\n   - Set `is-active-parent-tooltip-text` to convey which submenu item is currently active when the flyout is open.\n   - Flyout menus work in both expanded and collapsed navigation states.\n   - The component automatically adds `aria-haspopup=\"menu\"` to the parent navmenuitem.\n\n3. **NavMenuItem with dropdown submenu**:\n   - Add an `id` to the `mdc-navmenuitem` and set `submenu-type=\"dropdown\"`.\n   - Add a sibling `div` with `data-trigger` set to the navmenuitem's `id` and nest the dropdown submenu items inside it.\n   - Dropdowns only display in expanded mode. In collapsed mode they automatically convert to flyout menus and revert to dropdowns when the navigation is expanded again.\n   - Dropdown submenu items never render when the navigation is collapsed, regardless of `submenu-type`.\n   - Only one level of dropdown submenus is supported. For multi-level navigation use flyouts instead.\n   - The component manages `aria-expanded` on the parent navmenuitem automatically.\n\n4. **Action-based navmenuitem (no submenu)** — performs an action such as navigation or an alert trigger. Set `disable-aria-current=\"true\"` to keep the visual active state without announcing the item as the current page to screen readers; this is ideal for items that trigger actions rather than navigate.\n\nListen for `toggle` to react to user-driven expand/collapse and `activechange` to react to the active nav item changing.\n\n### Content guidance\n\n- Use `mdc-text` for section headers.\n- Use `mdc-divider` with `variant=\"gradient\"` to separate sections.\n- Use an informative icon for the brand logo slot (refer to the Momentum Informative Icons set).\n- Use icons for parent navmenuitems only. For submenu items (in both flyouts and dropdowns), use text labels without icons to ensure readability.\n\n### Property/Attribute details\n\n- `variant` — layout variant. One of:\n  - `fixed-collapsed` — icons only, fixed width (4.5 rem); `expanded` is locked to `false`.\n  - `fixed-expanded` — icons and labels, fixed width (15 rem); `expanded` is locked to `true`.\n  - `flexible` — user-togglable; the grabber button is always visible.\n  - `flexible-on-hover` — user-togglable; the grabber button only appears on hover or keyboard focus.\n  - `hidden` — the component renders nothing.\n  - Default: `flexible`.\n- `expanded` — whether the navigation is expanded. For `fixed-*` variants this is forced; for `flexible` and `flexible-on-hover` it can be set or toggled by the user (defaults to `true` on first render).\n- `footer-text` — text rendered in the bottom brand-logo area. When empty, the entire brand-logo container (including the `brand-logo` slot) is not rendered.\n- `grabber-btn-aria-label` — accessible name applied to the expand/collapse grabber button.\n- `hide-fixed-section-divider` — when `true`, hides the divider between the scrollable and fixed sections. Default `false`.\n- `submenu-type` — controls how child submenu items are presented on a parent navmenuitem. Set to `\"dropdown\"` to render an inline dropdown in expanded mode. In collapsed mode the component automatically promotes a dropdown to a flyout. Omit or leave unset for flyout-only submenus using `mdc-menupopover`.\n\nEvents dispatched by the host:\n\n- `toggle` — `event.detail = { expanded }`; dispatched when the grabber button is clicked.\n- `activechange` — dispatched when the active state of a nested `mdc-navmenuitem` changes.\n\n### Limitations\n\n- For `fixed-collapsed` and `fixed-expanded` variants, `expanded` is hard-set internally and cannot be changed by the user or consumer.\n- When `variant=\"hidden\"`, no DOM is rendered for the navigation.\n- Dropdown submenus support only one level of nesting. For multi-level navigation use flyout menus (`mdc-menupopover`) instead.\n- Dropdown submenu items do not render when the navigation is collapsed; they are automatically promoted to flyout menus in that state and revert to dropdowns when expanded again.\n\n## Accessibility\n\n### Built-in features\n\nThe host is registered as a navigation landmark with `role=\"navigation\"`. The expand/collapse grabber button (rendered for `flexible` and `flexible-on-hover` variants) exposes `aria-expanded` reflecting the current `expanded` state and `aria-controls` pointing at the container id. Pressing Space on the scrollable section is intercepted to prevent page scroll.\n\nFor the `flexible-on-hover` variant, the grabber button is only made visible (via a `data-grabber-visible` attribute) when the user hovers the side navigation or moves keyboard focus into it via `:focus-visible`; non-focus-visible focus moves (e.g. mouse-driven focus into a navmenuitem) do not reveal the grabber.\n\n#### Internal ARIA managed by the component\n\n| Element                       | Attribute       | Value                                                            |\n| ----------------------------- | --------------- | ---------------------------------------------------------------- |\n| Host                          | `role`          | `navigation`                                                     |\n| Grabber button                | `aria-label`    | mirrors `grabber-btn-aria-label`                                 |\n| Grabber button                | `aria-expanded` | reflects the current `expanded` state                            |\n| Grabber button                | `aria-controls` | id of the side navigation container                              |\n| NavMenuItem (flyout parent)   | `aria-haspopup` | `\"menu\"` — set automatically when linked to an `mdc-menupopover` |\n| NavMenuItem (dropdown parent) | `aria-expanded` | `\"true\"` when the dropdown is open, `\"false\"` when closed        |\n\n### Implementation requirements\n\n#### General\n\n- Provide a meaningful `aria-label` on every nested `mdc-menubar` and `mdc-navmenuitem` so screen readers can identify them.\n- Pair each flyout submenu pattern with `is-active-parent-tooltip-text` on the `mdc-navmenuitem` so users can tell which submenu item is currently active.\n- For dropdown submenus, the parent navmenuitem receives `aria-expanded` automatically — do not override it manually.\n\n#### Labelling\n\n- Always set `grabber-btn-aria-label` for `flexible` and `flexible-on-hover` variants; the grabber button is unlabelled otherwise.\n- The host has `role=\"navigation\"`; if multiple navigation landmarks exist on the page, add a host-level `aria-label` to distinguish them.",
    "sections": {
      "Overview": "The side navigation is a vertically stacked navigation landmark, typically used as a persistent or collapsible sidebar. It supports five layout variants, toggleable expand/collapse behaviour, an optional brand logo, an optional footer text, and acts as a context provider for nested `mdc-menubar` and `mdc-navmenuitem` elements.\n\n### When to use\n\n- Use `mdc-sidenavigation` for primary, persistent in-app navigation rendered alongside the main content area.\n- Use it when you need an expandable/collapsible sidebar with icon-only and icon-plus-label modes.\n- Use it as the host of nested `mdc-menubar` and `mdc-navmenuitem` content (with optional submenus via `mdc-menupopover`).\n\n### When not to use\n\n- Use `mdc-appheader` for top-of-page navigation.\n- Use `mdc-menubar` on its own when no expand/collapse, branding, or fixed footer area is needed.\n- Use `mdc-tablist` when navigation is scoped to switching between sibling views inside a single page.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/sidenavigation/index.js\"; // custom element registration\n// or via React wrapper\nimport { SideNavigation, MenuBar, NavMenuItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-sidenavigation\n  variant=\"flexible\"\n  footer-text=\"Acme Corp\"\n  grabber-btn-aria-label=\"Toggle navigation\"\n>\n  <mdc-menubar slot=\"scrollable-menubar\" aria-label=\"Main\">\n    <mdc-navmenuitem nav-id=\"home\" aria-label=\"Home\" icon-name=\"home-bold\"></mdc-navmenuitem>\n    <mdc-navmenuitem nav-id=\"messages\" aria-label=\"Messages\" icon-name=\"chat-bold\"></mdc-navmenuitem>\n  </mdc-menubar>\n\n  <mdc-icon slot=\"brand-logo\" name=\"logo-bold\"></mdc-icon>\n</mdc-sidenavigation>\n```\n\nSlot structure:\n\n- `scrollable-section` — full custom content for the scrollable area. Replaces the default menubar wrapper.\n- `scrollable-menubar` — items rendered inside the default scrollable `mdc-menubar` wrapper.\n- `fixed-section` — full custom content for the bottom fixed area.\n- `fixed-menubar` — items rendered inside the default fixed `mdc-menubar` wrapper.\n- `brand-logo` — informative icon or image rendered next to the footer text (only visible when `footer-text` is set).\n\nNavMenuItem patterns:\n\n1. **Simple navmenuitem** — No submenu or action beyond selection.\n\n2. **NavMenuItem with flyout submenu**:\n   - Add an `id` to the `mdc-navmenuitem`.\n   - Set `triggerId` on the linked `mdc-menupopover` to match that `id`.\n   - Set `is-active-parent-tooltip-text` to convey which submenu item is currently active when the flyout is open.\n   - Flyout menus work in both expanded and collapsed navigation states.\n   - The component automatically adds `aria-haspopup=\"menu\"` to the parent navmenuitem.\n\n3. **NavMenuItem with dropdown submenu**:\n   - Add an `id` to the `mdc-navmenuitem` and set `submenu-type=\"dropdown\"`.\n   - Add a sibling `div` with `data-trigger` set to the navmenuitem's `id` and nest the dropdown submenu items inside it.\n   - Dropdowns only display in expanded mode. In collapsed mode they automatically convert to flyout menus and revert to dropdowns when the navigation is expanded again.\n   - Dropdown submenu items never render when the navigation is collapsed, regardless of `submenu-type`.\n   - Only one level of dropdown submenus is supported. For multi-level navigation use flyouts instead.\n   - The component manages `aria-expanded` on the parent navmenuitem automatically.\n\n4. **Action-based navmenuitem (no submenu)** — performs an action such as navigation or an alert trigger. Set `disable-aria-current=\"true\"` to keep the visual active state without announcing the item as the current page to screen readers; this is ideal for items that trigger actions rather than navigate.\n\nListen for `toggle` to react to user-driven expand/collapse and `activechange` to react to the active nav item changing.\n\n### Content guidance\n\n- Use `mdc-text` for section headers.\n- Use `mdc-divider` with `variant=\"gradient\"` to separate sections.\n- Use an informative icon for the brand logo slot (refer to the Momentum Informative Icons set).\n- Use icons for parent navmenuitems only. For submenu items (in both flyouts and dropdowns), use text labels without icons to ensure readability.\n\n### Property/Attribute details\n\n- `variant` — layout variant. One of:\n  - `fixed-collapsed` — icons only, fixed width (4.5 rem); `expanded` is locked to `false`.\n  - `fixed-expanded` — icons and labels, fixed width (15 rem); `expanded` is locked to `true`.\n  - `flexible` — user-togglable; the grabber button is always visible.\n  - `flexible-on-hover` — user-togglable; the grabber button only appears on hover or keyboard focus.\n  - `hidden` — the component renders nothing.\n  - Default: `flexible`.\n- `expanded` — whether the navigation is expanded. For `fixed-*` variants this is forced; for `flexible` and `flexible-on-hover` it can be set or toggled by the user (defaults to `true` on first render).\n- `footer-text` — text rendered in the bottom brand-logo area. When empty, the entire brand-logo container (including the `brand-logo` slot) is not rendered.\n- `grabber-btn-aria-label` — accessible name applied to the expand/collapse grabber button.\n- `hide-fixed-section-divider` — when `true`, hides the divider between the scrollable and fixed sections. Default `false`.\n- `submenu-type` — controls how child submenu items are presented on a parent navmenuitem. Set to `\"dropdown\"` to render an inline dropdown in expanded mode. In collapsed mode the component automatically promotes a dropdown to a flyout. Omit or leave unset for flyout-only submenus using `mdc-menupopover`.\n\nEvents dispatched by the host:\n\n- `toggle` — `event.detail = { expanded }`; dispatched when the grabber button is clicked.\n- `activechange` — dispatched when the active state of a nested `mdc-navmenuitem` changes.\n\n### Limitations\n\n- For `fixed-collapsed` and `fixed-expanded` variants, `expanded` is hard-set internally and cannot be changed by the user or consumer.\n- When `variant=\"hidden\"`, no DOM is rendered for the navigation.\n- Dropdown submenus support only one level of nesting. For multi-level navigation use flyout menus (`mdc-menupopover`) instead.\n- Dropdown submenu items do not render when the navigation is collapsed; they are automatically promoted to flyout menus in that state and revert to dropdowns when expanded again.",
      "Accessibility": "### Built-in features\n\nThe host is registered as a navigation landmark with `role=\"navigation\"`. The expand/collapse grabber button (rendered for `flexible` and `flexible-on-hover` variants) exposes `aria-expanded` reflecting the current `expanded` state and `aria-controls` pointing at the container id. Pressing Space on the scrollable section is intercepted to prevent page scroll.\n\nFor the `flexible-on-hover` variant, the grabber button is only made visible (via a `data-grabber-visible` attribute) when the user hovers the side navigation or moves keyboard focus into it via `:focus-visible`; non-focus-visible focus moves (e.g. mouse-driven focus into a navmenuitem) do not reveal the grabber.\n\n#### Internal ARIA managed by the component\n\n| Element                       | Attribute       | Value                                                            |\n| ----------------------------- | --------------- | ---------------------------------------------------------------- |\n| Host                          | `role`          | `navigation`                                                     |\n| Grabber button                | `aria-label`    | mirrors `grabber-btn-aria-label`                                 |\n| Grabber button                | `aria-expanded` | reflects the current `expanded` state                            |\n| Grabber button                | `aria-controls` | id of the side navigation container                              |\n| NavMenuItem (flyout parent)   | `aria-haspopup` | `\"menu\"` — set automatically when linked to an `mdc-menupopover` |\n| NavMenuItem (dropdown parent) | `aria-expanded` | `\"true\"` when the dropdown is open, `\"false\"` when closed        |\n\n### Implementation requirements\n\n#### General\n\n- Provide a meaningful `aria-label` on every nested `mdc-menubar` and `mdc-navmenuitem` so screen readers can identify them.\n- Pair each flyout submenu pattern with `is-active-parent-tooltip-text` on the `mdc-navmenuitem` so users can tell which submenu item is currently active.\n- For dropdown submenus, the parent navmenuitem receives `aria-expanded` automatically — do not override it manually.\n\n#### Labelling\n\n- Always set `grabber-btn-aria-label` for `flexible` and `flexible-on-hover` variants; the grabber button is unlabelled otherwise.\n- The host has `role=\"navigation\"`; if multiple navigation landmarks exist on the page, add a host-level `aria-label` to distinguish them."
    },
    "dependencies": [
      "mdc-text",
      "mdc-button",
      "mdc-divider",
      "mdc-menubar"
    ],
    "api": {
      "properties": [
        {
          "name": "expanded",
          "attribute": "expanded",
          "type": "boolean",
          "values": [],
          "description": "whether the navigation is expanded. For `fixed-*` variants this is forced; for `flexible` and `flexible-on-hover` it can be set or toggled by the user (defaults to `true` on first render).",
          "source": "storybook"
        },
        {
          "name": "footerText",
          "attribute": "footer-text",
          "type": "string",
          "default": "''",
          "values": [],
          "description": "text rendered in the bottom brand-logo area. When empty, the entire brand-logo container (including the `brand-logo` slot) is not rendered.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "grabberBtnAriaLabel",
          "attribute": "grabber-btn-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name applied to the expand/collapse grabber button.",
          "source": "storybook"
        },
        {
          "name": "hideFixedSectionDivider",
          "attribute": "hide-fixed-section-divider",
          "type": "boolean",
          "default": "true",
          "values": [],
          "description": "when `true`, hides the divider between the scrollable and fixed sections. Default `false`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "submenuType",
          "attribute": "submenu-type",
          "type": "select",
          "default": "flyout",
          "values": [],
          "description": "controls how child submenu items are presented on a parent navmenuitem. Set to `\"dropdown\"` to render an inline dropdown in expanded mode. In collapsed mode the component automatically promotes a dropdown to a flyout. Omit or leave unset for flyout-only submenus using `mdc-menupopover`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "flexible",
          "values": [
            "fixed-expanded",
            "fixed-collapsed",
            "flexible",
            "flexible-on-hover",
            "hidden",
            "flyout",
            "dropdown",
            null
          ],
          "description": "layout variant. One of:",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "scrollable-section",
          "description": "Slot for the scrollable content area of the side navigation."
        },
        {
          "name": "scrollable-menubar",
          "description": "Slot for the menubar inside the scrollable section."
        },
        {
          "name": "fixed-section",
          "description": "Slot for the fixed content area of the side navigation."
        },
        {
          "name": "fixed-menubar",
          "description": "Slot for the menubar inside the fixed section."
        },
        {
          "name": "brand-logo",
          "description": "Slot for the brand logo (e.g., icon or img)."
        }
      ],
      "events": [
        {
          "name": "activechange",
          "description": "(React: onActiveChange) Dispatched when the active state of the navmenuitem changes."
        },
        {
          "name": "toggle",
          "description": "(React: onToggle) Dispatched when the grabber button is clicked to expand/collapse the sidenavigation."
        },
        {
          "name": "activechange",
          "description": "(React: onActiveChange) Dispatched when the active state of a nested navmenuitem changes."
        }
      ],
      "cssParts": [
        {
          "name": "side-navigation-container",
          "description": "The main container wrapping the entire side navigation."
        },
        {
          "name": "scrollable-section",
          "description": "The scrollable section of the side navigation."
        },
        {
          "name": "scrollable-menubar",
          "description": "The menubar inside the scrollable section."
        },
        {
          "name": "separator",
          "description": "The divider between the scrollable and fixed sections."
        },
        {
          "name": "fixed-section",
          "description": "The fixed section of the side navigation."
        },
        {
          "name": "fixed-menubar",
          "description": "The menubar inside the fixed section."
        },
        {
          "name": "brand-logo-container",
          "description": "The container wrapping the brand logo and footer text."
        },
        {
          "name": "footer-text",
          "description": "The footer text label in the fixed section."
        },
        {
          "name": "vertical-divider",
          "description": "The vertical divider between the scrollable and fixed sections."
        },
        {
          "name": "vertical-divider-button",
          "description": "The button inside the vertical divider used to toggle expand/collapse."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-sidenavigation-expanded-width",
          "description": "width of the sideNavigation when expanded"
        },
        {
          "name": "--mdc-sidenavigation-expanded-left-padding",
          "description": "padding for the left side of navmenuitems, when expanded"
        },
        {
          "name": "--mdc-sidenavigation-expanded-right-padding",
          "description": "padding for the right side of navmenuitems, when expanded"
        },
        {
          "name": "--mdc-sidenavigation-collapsed-width",
          "description": "width of the sideNavigation when collapsed"
        },
        {
          "name": "--mdc-sidenavigation-collapsed-left-padding",
          "description": "padding for the left side of navmenuitems, when collapsed"
        },
        {
          "name": "--mdc-sidenavigation-collapsed-right-padding",
          "description": "padding for the right side of navmenuitems, when collapsed"
        },
        {
          "name": "--mdc-sidenavigation-top-padding",
          "description": "padding for the top of the scrollable section - note: if setting to 0 focus ring might be cut off"
        },
        {
          "name": "--mdc-sidenavigation-bottom-padding",
          "description": "padding for the bottom of the scrollable section"
        },
        {
          "name": "--mdc-sidenavigation-vertical-divider-button-z-index",
          "description": "z-index of the vertical divider button"
        }
      ],
      "rules": [
        "layout variant. One of:",
        "whether the navigation is expanded. For `fixed-*` variants this is forced; for `flexible` and `flexible-on-hover` it can be set or toggled by the user (defaults to `true` on first render).",
        "text rendered in the bottom brand-logo area. When empty, the entire brand-logo container (including the `brand-logo` slot) is not rendered.",
        "accessible name applied to the expand/collapse grabber button.",
        "when `true`, hides the divider between the scrollable and fixed sections. Default `false`.",
        "controls how child submenu items are presented on a parent navmenuitem. Set to `\"dropdown\"` to render an inline dropdown in expanded mode. In collapsed mode the component automatically promotes a dropdown to a flyout. Omit or leave unset for flyout-only submenus using `mdc-menupopover`.",
        "`event.detail = { expanded }`; dispatched when the grabber button is clicked.",
        "dispatched when the active state of a nested `mdc-navmenuitem` changes."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/sidenavigation/index.js\"; // custom element registration\n// or via React wrapper\nimport { SideNavigation, MenuBar, NavMenuItem } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-sidenavigation\n  variant=\"flexible\"\n  footer-text=\"Acme Corp\"\n  grabber-btn-aria-label=\"Toggle navigation\"\n>\n  <mdc-menubar slot=\"scrollable-menubar\" aria-label=\"Main\">\n    <mdc-navmenuitem nav-id=\"home\" aria-label=\"Home\" icon-name=\"home-bold\"></mdc-navmenuitem>\n    <mdc-navmenuitem nav-id=\"messages\" aria-label=\"Messages\" icon-name=\"chat-bold\"></mdc-navmenuitem>\n  </mdc-menubar>\n\n  <mdc-icon slot=\"brand-logo\" name=\"logo-bold\"></mdc-icon>\n</mdc-sidenavigation>"
      }
    ],
    "reactImports": [
      "SideNavigation",
      "MenuBar",
      "NavMenuItem"
    ],
    "constants": {
      "VARIANTS": {
        "FIXED_EXPANDED": "fixed-expanded",
        "FIXED_COLLAPSED": "fixed-collapsed",
        "FLEXIBLE": "flexible",
        "FLEXIBLE_ON_HOVER": "flexible-on-hover",
        "HIDDEN": "hidden",
        "FLYOUT": "flyout",
        "DROPDOWN": "dropdown",
        "VARIANT": "flexible"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "behaviour",
      "button",
      "collapse",
      "component",
      "divider",
      "expand",
      "for",
      "guidelines",
      "items",
      "landmark",
      "mdc",
      "menu",
      "menubar",
      "navigation",
      "nested",
      "overview",
      "side",
      "sidenavigation",
      "stacked",
      "support",
      "text",
      "the",
      "usage",
      "vertically",
      "with"
    ]
  },
  {
    "name": "skeleton",
    "title": "Skeleton",
    "tagName": "mdc-skeleton",
    "summary": "Usage, guidelines, and accessibility for the mdc-skeleton component — a grey placeholder block that signals loading content.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/skeleton",
      "knowledgeBasePath": "components/src/components/skeleton/knowledge-base/skeleton.component.md"
    },
    "docs": "## Overview\n\nThe skeleton is a grey placeholder block that signals to users that real content is being loaded. It can wrap a child element to take that element's dimensions, or stretch to fill its parent when used standalone.\n\n### When to use\n\n- Use `mdc-skeleton` to indicate that content is loading, in place of the real content while it is being fetched or rendered.\n- Use it to wrap a known-shape child (button, avatar, paragraph) so the placeholder matches the eventual layout.\n\n### When not to use\n\n- Use `mdc-spinner` when the loading duration is short or you only need a single inline indicator rather than a layout-matching placeholder.\n- Use `mdc-progressbar` when there is a known, measurable progress value to communicate.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/skeleton/index.js\"; // custom element registration\n// or via React wrapper\nimport { Skeleton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup examples:\n\n```html\n<!-- Standalone placeholder that fills its parent -->\n<mdc-skeleton variant=\"rounded\" style=\"--mdc-skeleton-height: 1rem;\"></mdc-skeleton>\n\n<!-- Wrapping content so the placeholder matches the eventual layout -->\n<mdc-skeleton variant=\"button\">\n  <mdc-button>Loading button</mdc-button>\n</mdc-skeleton>\n```\n\n### Property/Attribute details\n\n- `variant` — visual variant. One of:\n  - `rectangular` — default, 0.25 rem border radius.\n  - `rounded` — 0.5 rem border radius.\n  - `circular` — 50% border radius for circular shapes (e.g. avatar placeholders).\n  - `button` — 1.25 rem border radius, sized for button placeholders.\n- Sizing:\n  - When the default slot has content, the skeleton takes the dimensions of that content (and toggles a `has-content` attribute on the host).\n  - Otherwise it grows to fill the parent container. Use the `--mdc-skeleton-width` and `--mdc-skeleton-height` CSS custom properties to size standalone skeletons.\n\n## Accessibility\n\n### Built-in features\n\nThe host sets `aria-hidden=\"true\"` on `connectedCallback`, so assistive technologies ignore the placeholder entirely. The slot is observed, and the host toggles a `has-content` attribute so styling can switch between \"fit content\" and \"fill parent\" sizing modes.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute     | Value  |\n| ------- | ------------- | ------ |\n| Host    | `aria-hidden` | `true` |\n\n### Notes\n\nBecause the host is `aria-hidden=\"true\"`, the skeleton itself contributes nothing to the accessibility tree. When the loading state is important for screen readers, announce it from a sibling region (for example via `mdc-screenreaderannouncer` or an `aria-live` region) rather than relying on the skeleton.",
    "sections": {
      "Overview": "The skeleton is a grey placeholder block that signals to users that real content is being loaded. It can wrap a child element to take that element's dimensions, or stretch to fill its parent when used standalone.\n\n### When to use\n\n- Use `mdc-skeleton` to indicate that content is loading, in place of the real content while it is being fetched or rendered.\n- Use it to wrap a known-shape child (button, avatar, paragraph) so the placeholder matches the eventual layout.\n\n### When not to use\n\n- Use `mdc-spinner` when the loading duration is short or you only need a single inline indicator rather than a layout-matching placeholder.\n- Use `mdc-progressbar` when there is a known, measurable progress value to communicate.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/skeleton/index.js\"; // custom element registration\n// or via React wrapper\nimport { Skeleton } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup examples:\n\n```html\n<!-- Standalone placeholder that fills its parent -->\n<mdc-skeleton variant=\"rounded\" style=\"--mdc-skeleton-height: 1rem;\"></mdc-skeleton>\n\n<!-- Wrapping content so the placeholder matches the eventual layout -->\n<mdc-skeleton variant=\"button\">\n  <mdc-button>Loading button</mdc-button>\n</mdc-skeleton>\n```\n\n### Property/Attribute details\n\n- `variant` — visual variant. One of:\n  - `rectangular` — default, 0.25 rem border radius.\n  - `rounded` — 0.5 rem border radius.\n  - `circular` — 50% border radius for circular shapes (e.g. avatar placeholders).\n  - `button` — 1.25 rem border radius, sized for button placeholders.\n- Sizing:\n  - When the default slot has content, the skeleton takes the dimensions of that content (and toggles a `has-content` attribute on the host).\n  - Otherwise it grows to fill the parent container. Use the `--mdc-skeleton-width` and `--mdc-skeleton-height` CSS custom properties to size standalone skeletons.",
      "Accessibility": "### Built-in features\n\nThe host sets `aria-hidden=\"true\"` on `connectedCallback`, so assistive technologies ignore the placeholder entirely. The slot is observed, and the host toggles a `has-content` attribute so styling can switch between \"fit content\" and \"fill parent\" sizing modes.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute     | Value  |\n| ------- | ------------- | ------ |\n| Host    | `aria-hidden` | `true` |\n\n### Notes\n\nBecause the host is `aria-hidden=\"true\"`, the skeleton itself contributes nothing to the accessibility tree. When the loading state is important for screen readers, announce it from a sibling region (for example via `mdc-screenreaderannouncer` or an `aria-live` region) rather than relying on the skeleton."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "motion",
          "attribute": "motion",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "false",
          "values": [
            "button",
            "circular",
            "rectangular",
            "rounded"
          ],
          "description": "visual variant. One of:",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Content to wrap (optional). When provided, skeleton takes dimensions of this content."
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-skeleton-background-color",
          "description": "background color of the skeleton"
        },
        {
          "name": "--mdc-skeleton-height",
          "description": "height of the skeleton"
        },
        {
          "name": "--mdc-skeleton-width",
          "description": "width of the skeleton"
        }
      ],
      "rules": [
        "visual variant. One of:"
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/skeleton/index.js\"; // custom element registration\n// or via React wrapper\nimport { Skeleton } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<!-- Standalone placeholder that fills its parent -->\n<mdc-skeleton variant=\"rounded\" style=\"--mdc-skeleton-height: 1rem;\"></mdc-skeleton>\n\n<!-- Wrapping content so the placeholder matches the eventual layout -->\n<mdc-skeleton variant=\"button\">\n  <mdc-button>Loading button</mdc-button>\n</mdc-skeleton>"
      }
    ],
    "reactImports": [
      "Skeleton"
    ],
    "constants": {
      "SKELETON_VARIANTS": {
        "BUTTON": "button",
        "CIRCULAR": "circular",
        "RECTANGULAR": "rectangular",
        "ROUNDED": "rounded"
      },
      "DEFAULTS": {
        "MOTION": false,
        "VARIANT": "rectangular"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "block",
      "component",
      "content",
      "for",
      "grey",
      "guidelines",
      "loading",
      "mdc",
      "overview",
      "placeholder",
      "signals",
      "skeleton",
      "that",
      "the",
      "usage"
    ]
  },
  {
    "name": "slider",
    "title": "Slider",
    "tagName": "mdc-slider",
    "summary": "Usage, guidelines, and accessibility for the mdc-slider component — a single-value or range input rendered as one or two thumbs on a track.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/slider",
      "knowledgeBasePath": "components/src/components/slider/knowledge-base/slider.component.md"
    },
    "docs": "## Overview\n\nThe slider lets users pick a value, or a range of values, from a defined numeric range by dragging one or two thumbs along a track. When the step is greater than 1, the slider also renders tick marks and snaps to the nearest tick.\n\n### When to use\n\n- Use `mdc-slider` when the user needs to pick a numeric value from a continuous (or stepped) range, and the relative position matters (volume, brightness, zoom, etc.).\n- Use `range` when the user needs to pick both ends of a numeric interval (price range, time window).\n\n### When not to use\n\n- Use `mdc-stepper` when only discrete numeric increments are meaningful and precise typing is important.\n- Use `mdc-input type=\"number\"` when the user needs to type an exact value.\n- Use `mdc-toggle` for binary on/off choices.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/slider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Slider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup examples:\n\n```html\n<!-- Single-value slider -->\n<mdc-slider\n  label=\"Volume\"\n  name=\"volume\"\n  min=\"0\"\n  max=\"100\"\n  step=\"1\"\n  value=\"40\"\n></mdc-slider>\n\n<!-- Range slider with tick marks -->\n<mdc-slider\n  range\n  label=\"Price range\"\n  name-start=\"min-price\"\n  name-end=\"max-price\"\n  min=\"0\"\n  max=\"100\"\n  step=\"10\"\n  value-start=\"20\"\n  value-end=\"80\"\n  start-aria-label=\"Minimum price\"\n  end-aria-label=\"Maximum price\"\n></mdc-slider>\n```\n\nListen for `input` while the user drags, and for `change` once the value is committed:\n\n- Single value: `event.detail = { value }`.\n- Range: `event.detail = { valueStart, valueEnd }`.\n\n### Property/Attribute details\n\n- `range` — when `true`, renders two thumbs (start and end) instead of one. Default `false`.\n- `min` — minimum value. Default `0`.\n- `max` — maximum value. Default `100`.\n- `step` — increment between values. Default `1`. When `step > 1`, tick marks are rendered and thumbs snap to ticks.\n- `value` — current value when `range` is `false`.\n- `value-start` / `value-end` — current start/end values when `range` is `true`. Defaults to `min` / `max` when omitted. Thumbs cannot cross — if `value-start` is dragged past `value-end` it is clamped, and vice versa.\n- `name` — form field name for the single-value slider.\n- `name-start` / `name-end` — form field names for the range thumbs.\n- `label` — visible label rendered above the slider.\n- `label-start` / `label-end` — labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).\n- `value-label` — optional textual label shown for the value in the tooltip (single-value slider). Defaults to the numeric value.\n- `value-label-start` / `value-label-end` — optional textual labels for the start/end thumb tooltips (range slider).\n- `leading-icon` / `trailing-icon` — icon names rendered on the leading and trailing ends (e.g. muted speaker / full-volume speaker).\n- `disabled` — fully disabled; the thumbs are removed from the tab order.\n- `soft-disabled` — visually disabled but focusable; user input is suppressed and the tooltip is shown on focus.\n- `data-aria-label` — `aria-label` for the single thumb.\n- `start-aria-label` / `end-aria-label` — `aria-label`s for the start and end thumbs in range mode.\n- `data-aria-valuetext` — `aria-valuetext` for the single thumb (falls back to `value-label`, then to the numeric value).\n- `start-aria-valuetext` / `end-aria-valuetext` — `aria-valuetext`s for the start and end thumbs (fall back to the matching `value-label-*`, then to the numeric value).\n- `hide-tooltip` — when `true`, the value tooltip is not shown on focus or hover.\n\nEvents dispatched by the host:\n\n- `input` — fires continuously while the user drags or steps the slider; `event.detail` contains the current `value` (single) or `{ valueStart, valueEnd }` (range).\n- `change` — fires once the value is committed (mouse up / keyboard release); `event.detail` matches `input`.\n\n## Accessibility\n\n### Built-in features\n\nEach thumb is a native `<input type=\"range\">` with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, `aria-label`, and `aria-valuetext` set from the component's properties. Native screen-reader announcements for slider value changes therefore work without extra wiring.\n\nKeyboard interaction:\n\n- `ArrowLeft` / `ArrowRight` (or `ArrowUp` / `ArrowDown`, `Home`, `End`, `PageUp`, `PageDown`) — adjust the focused thumb by `step` (provided by the native range input).\n- `Tab` — move focus to the next thumb or out of the slider.\n- In spatial navigation mode, the slider has a dedicated edit mode: focus the thumb, press `Enter` to enter edit mode, use Left/Right to adjust the value, and press `Enter` or `Escape` (or `Up`/`Down`) to leave edit mode. This prevents arrow keys from being captured for value changes while the user is navigating between focusable elements with arrows. The range slider also exposes `data-spatial-left` / `data-spatial-right` between the two thumbs so spatial navigation moves directly between them.\n\nSoft-disabled state: `aria-disabled=\"true\"` is set on the native inputs while `soft-disabled` is true, and pointer/keyboard interactions are suppressed (Tab still moves focus through). Disabled state: the native inputs are `disabled` and `tabindex=\"-1\"`.\n\nTooltip: the value tooltip is rendered with `aria-hidden=\"true\"` because the same value is already announced through the native `aria-valuetext`/`aria-valuenow` on the input.\n\n#### Internal ARIA managed by the component\n\n| Element                         | Attribute         | Value                                                          |\n| ------------------------------- | ----------------- | -------------------------------------------------------------- |\n| Single thumb `<input>`          | `type`            | `range`                                                        |\n| Single thumb `<input>`          | `aria-valuemin`   | mirrors `min`                                                  |\n| Single thumb `<input>`          | `aria-valuemax`   | mirrors `max`                                                  |\n| Single thumb `<input>`          | `aria-valuenow`   | mirrors `value` (or `min` when unset)                          |\n| Single thumb `<input>`          | `aria-label`      | mirrors `data-aria-label` (falls back to `label`)              |\n| Single thumb `<input>`          | `aria-valuetext`  | mirrors `data-aria-valuetext` (falls back to `value-label`, then to the numeric value) |\n| Single thumb `<input>`          | `tabindex`        | `0` when enabled; `-1` when `disabled`                         |\n| Single thumb `<input>`          | `aria-disabled`   | `true` when `soft-disabled` is true                            |\n| Start thumb `<input>` (range)   | `aria-valuenow`   | mirrors `value-start` (or `min` when unset)                    |\n| Start thumb `<input>` (range)   | `aria-label`      | mirrors `start-aria-label` (falls back to `label`)             |\n| Start thumb `<input>` (range)   | `aria-valuetext`  | mirrors `start-aria-valuetext` (falls back to `value-label-start`, then to the numeric value) |\n| End thumb `<input>` (range)     | `aria-valuenow`   | mirrors `value-end` (or `max` when unset)                      |\n| End thumb `<input>` (range)     | `aria-label`      | mirrors `end-aria-label` (falls back to `label`)               |\n| End thumb `<input>` (range)     | `aria-valuetext`  | mirrors `end-aria-valuetext` (falls back to `value-label-end`, then to the numeric value) |\n| Value tooltip                   | `aria-hidden`     | `true`                                                         |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a visible `label` whenever possible — it is used as the accessible name fallback for the thumb(s).\n- For range sliders, set `start-aria-label` and `end-aria-label` so each thumb is individually identifiable (e.g. \"Minimum price\", \"Maximum price\"). Without these the same `label` value is used for both thumbs.\n- When the numeric value alone is not meaningful (currencies, dates, named buckets), set `data-aria-valuetext` / `start-aria-valuetext` / `end-aria-valuetext` so screen readers announce a human-readable value.",
    "sections": {
      "Overview": "The slider lets users pick a value, or a range of values, from a defined numeric range by dragging one or two thumbs along a track. When the step is greater than 1, the slider also renders tick marks and snaps to the nearest tick.\n\n### When to use\n\n- Use `mdc-slider` when the user needs to pick a numeric value from a continuous (or stepped) range, and the relative position matters (volume, brightness, zoom, etc.).\n- Use `range` when the user needs to pick both ends of a numeric interval (price range, time window).\n\n### When not to use\n\n- Use `mdc-stepper` when only discrete numeric increments are meaningful and precise typing is important.\n- Use `mdc-input type=\"number\"` when the user needs to type an exact value.\n- Use `mdc-toggle` for binary on/off choices.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/slider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Slider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup examples:\n\n```html\n<!-- Single-value slider -->\n<mdc-slider\n  label=\"Volume\"\n  name=\"volume\"\n  min=\"0\"\n  max=\"100\"\n  step=\"1\"\n  value=\"40\"\n></mdc-slider>\n\n<!-- Range slider with tick marks -->\n<mdc-slider\n  range\n  label=\"Price range\"\n  name-start=\"min-price\"\n  name-end=\"max-price\"\n  min=\"0\"\n  max=\"100\"\n  step=\"10\"\n  value-start=\"20\"\n  value-end=\"80\"\n  start-aria-label=\"Minimum price\"\n  end-aria-label=\"Maximum price\"\n></mdc-slider>\n```\n\nListen for `input` while the user drags, and for `change` once the value is committed:\n\n- Single value: `event.detail = { value }`.\n- Range: `event.detail = { valueStart, valueEnd }`.\n\n### Property/Attribute details\n\n- `range` — when `true`, renders two thumbs (start and end) instead of one. Default `false`.\n- `min` — minimum value. Default `0`.\n- `max` — maximum value. Default `100`.\n- `step` — increment between values. Default `1`. When `step > 1`, tick marks are rendered and thumbs snap to ticks.\n- `value` — current value when `range` is `false`.\n- `value-start` / `value-end` — current start/end values when `range` is `true`. Defaults to `min` / `max` when omitted. Thumbs cannot cross — if `value-start` is dragged past `value-end` it is clamped, and vice versa.\n- `name` — form field name for the single-value slider.\n- `name-start` / `name-end` — form field names for the range thumbs.\n- `label` — visible label rendered above the slider.\n- `label-start` / `label-end` — labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).\n- `value-label` — optional textual label shown for the value in the tooltip (single-value slider). Defaults to the numeric value.\n- `value-label-start` / `value-label-end` — optional textual labels for the start/end thumb tooltips (range slider).\n- `leading-icon` / `trailing-icon` — icon names rendered on the leading and trailing ends (e.g. muted speaker / full-volume speaker).\n- `disabled` — fully disabled; the thumbs are removed from the tab order.\n- `soft-disabled` — visually disabled but focusable; user input is suppressed and the tooltip is shown on focus.\n- `data-aria-label` — `aria-label` for the single thumb.\n- `start-aria-label` / `end-aria-label` — `aria-label`s for the start and end thumbs in range mode.\n- `data-aria-valuetext` — `aria-valuetext` for the single thumb (falls back to `value-label`, then to the numeric value).\n- `start-aria-valuetext` / `end-aria-valuetext` — `aria-valuetext`s for the start and end thumbs (fall back to the matching `value-label-*`, then to the numeric value).\n- `hide-tooltip` — when `true`, the value tooltip is not shown on focus or hover.\n\nEvents dispatched by the host:\n\n- `input` — fires continuously while the user drags or steps the slider; `event.detail` contains the current `value` (single) or `{ valueStart, valueEnd }` (range).\n- `change` — fires once the value is committed (mouse up / keyboard release); `event.detail` matches `input`.",
      "Accessibility": "### Built-in features\n\nEach thumb is a native `<input type=\"range\">` with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, `aria-label`, and `aria-valuetext` set from the component's properties. Native screen-reader announcements for slider value changes therefore work without extra wiring.\n\nKeyboard interaction:\n\n- `ArrowLeft` / `ArrowRight` (or `ArrowUp` / `ArrowDown`, `Home`, `End`, `PageUp`, `PageDown`) — adjust the focused thumb by `step` (provided by the native range input).\n- `Tab` — move focus to the next thumb or out of the slider.\n- In spatial navigation mode, the slider has a dedicated edit mode: focus the thumb, press `Enter` to enter edit mode, use Left/Right to adjust the value, and press `Enter` or `Escape` (or `Up`/`Down`) to leave edit mode. This prevents arrow keys from being captured for value changes while the user is navigating between focusable elements with arrows. The range slider also exposes `data-spatial-left` / `data-spatial-right` between the two thumbs so spatial navigation moves directly between them.\n\nSoft-disabled state: `aria-disabled=\"true\"` is set on the native inputs while `soft-disabled` is true, and pointer/keyboard interactions are suppressed (Tab still moves focus through). Disabled state: the native inputs are `disabled` and `tabindex=\"-1\"`.\n\nTooltip: the value tooltip is rendered with `aria-hidden=\"true\"` because the same value is already announced through the native `aria-valuetext`/`aria-valuenow` on the input.\n\n#### Internal ARIA managed by the component\n\n| Element                         | Attribute         | Value                                                          |\n| ------------------------------- | ----------------- | -------------------------------------------------------------- |\n| Single thumb `<input>`          | `type`            | `range`                                                        |\n| Single thumb `<input>`          | `aria-valuemin`   | mirrors `min`                                                  |\n| Single thumb `<input>`          | `aria-valuemax`   | mirrors `max`                                                  |\n| Single thumb `<input>`          | `aria-valuenow`   | mirrors `value` (or `min` when unset)                          |\n| Single thumb `<input>`          | `aria-label`      | mirrors `data-aria-label` (falls back to `label`)              |\n| Single thumb `<input>`          | `aria-valuetext`  | mirrors `data-aria-valuetext` (falls back to `value-label`, then to the numeric value) |\n| Single thumb `<input>`          | `tabindex`        | `0` when enabled; `-1` when `disabled`                         |\n| Single thumb `<input>`          | `aria-disabled`   | `true` when `soft-disabled` is true                            |\n| Start thumb `<input>` (range)   | `aria-valuenow`   | mirrors `value-start` (or `min` when unset)                    |\n| Start thumb `<input>` (range)   | `aria-label`      | mirrors `start-aria-label` (falls back to `label`)             |\n| Start thumb `<input>` (range)   | `aria-valuetext`  | mirrors `start-aria-valuetext` (falls back to `value-label-start`, then to the numeric value) |\n| End thumb `<input>` (range)     | `aria-valuenow`   | mirrors `value-end` (or `max` when unset)                      |\n| End thumb `<input>` (range)     | `aria-label`      | mirrors `end-aria-label` (falls back to `label`)               |\n| End thumb `<input>` (range)     | `aria-valuetext`  | mirrors `end-aria-valuetext` (falls back to `value-label-end`, then to the numeric value) |\n| Value tooltip                   | `aria-hidden`     | `true`                                                         |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a visible `label` whenever possible — it is used as the accessible name fallback for the thumb(s).\n- For range sliders, set `start-aria-label` and `end-aria-label` so each thumb is individually identifiable (e.g. \"Minimum price\", \"Maximum price\"). Without these the same `label` value is used for both thumbs.\n- When the numeric value alone is not meaningful (currencies, dates, named buckets), set `data-aria-valuetext` / `start-aria-valuetext` / `end-aria-valuetext` so screen readers announce a human-readable value."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "`aria-label` for the single thumb.",
          "source": "storybook"
        },
        {
          "name": "dataAriaValuetext",
          "attribute": "data-aria-valuetext",
          "type": "text",
          "values": [],
          "description": "`aria-valuetext` for the single thumb (falls back to `value-label`, then to the numeric value).",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "fully disabled; the thumbs are removed from the tab order.",
          "source": "storybook"
        },
        {
          "name": "endAriaLabel",
          "attribute": "end-aria-label",
          "type": "text",
          "values": [],
          "description": "`aria-label`s for the start and end thumbs in range mode.",
          "source": "storybook"
        },
        {
          "name": "endAriaValuetext",
          "attribute": "end-aria-valuetext",
          "type": "text",
          "values": [],
          "description": "`aria-valuetext`s for the start and end thumbs (fall back to the matching `value-label-*`, then to the numeric value).",
          "source": "storybook"
        },
        {
          "name": "hideTooltip",
          "attribute": "hide-tooltip",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the value tooltip is not shown on focus or hover.",
          "source": "storybook"
        },
        {
          "name": "inputElements",
          "attribute": "label-start",
          "type": "HTMLInputElement[]",
          "values": [],
          "description": "labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label rendered above the slider.",
          "source": "storybook"
        },
        {
          "name": "labelEnd",
          "attribute": "label-end",
          "type": "text",
          "values": [],
          "description": "labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).",
          "source": "storybook"
        },
        {
          "name": "labelStart",
          "attribute": "label-start",
          "type": "text",
          "values": [],
          "description": "labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).",
          "source": "storybook"
        },
        {
          "name": "leadingIcon",
          "attribute": "leading-icon",
          "type": "text",
          "values": [],
          "description": "icon names rendered on the leading and trailing ends (e.g. muted speaker / full-volume speaker).",
          "source": "storybook"
        },
        {
          "name": "max",
          "attribute": "max",
          "type": "number",
          "default": "100",
          "values": [],
          "description": "maximum value. Default `100`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "min",
          "attribute": "min",
          "type": "number",
          "default": "false",
          "values": [],
          "description": "minimum value. Default `0`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "form field name for the single-value slider.",
          "source": "storybook"
        },
        {
          "name": "nameEnd",
          "attribute": "name-end",
          "type": "text",
          "values": [],
          "description": "form field names for the range thumbs.",
          "source": "storybook"
        },
        {
          "name": "nameStart",
          "attribute": "name-start",
          "type": "text",
          "values": [],
          "description": "form field names for the range thumbs.",
          "source": "storybook"
        },
        {
          "name": "range",
          "attribute": "range",
          "type": "boolean",
          "values": [],
          "description": "when `true`, renders two thumbs (start and end) instead of one. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "visually disabled but focusable; user input is suppressed and the tooltip is shown on focus.",
          "source": "storybook"
        },
        {
          "name": "startAriaLabel",
          "attribute": "start-aria-label",
          "type": "text",
          "values": [],
          "description": "`aria-label`s for the start and end thumbs in range mode.",
          "source": "storybook"
        },
        {
          "name": "startAriaValuetext",
          "attribute": "start-aria-valuetext",
          "type": "text",
          "values": [],
          "description": "`aria-valuetext`s for the start and end thumbs (fall back to the matching `value-label-*`, then to the numeric value).",
          "source": "storybook"
        },
        {
          "name": "step",
          "attribute": "step",
          "type": "number",
          "default": "DEFAULTS.STEP",
          "values": [],
          "description": "increment between values. Default `1`. When `step > 1`, tick marks are rendered and thumbs snap to ticks.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "trailingIcon",
          "attribute": "trailing-icon",
          "type": "text",
          "values": [],
          "description": "icon names rendered on the leading and trailing ends (e.g. muted speaker / full-volume speaker).",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "number",
          "values": [],
          "description": "current value when `range` is `false`.",
          "source": "storybook"
        },
        {
          "name": "valueEnd",
          "attribute": "value-end",
          "type": "number",
          "values": [],
          "description": "current start/end values when `range` is `true`. Defaults to `min` / `max` when omitted. Thumbs cannot cross — if `value-start` is dragged past `value-end` it is clamped, and vice versa.",
          "source": "storybook"
        },
        {
          "name": "valueLabel",
          "attribute": "value-label",
          "type": "text",
          "values": [],
          "description": "optional textual label shown for the value in the tooltip (single-value slider). Defaults to the numeric value.",
          "source": "storybook"
        },
        {
          "name": "valueLabelEnd",
          "attribute": "value-label-end",
          "type": "text",
          "values": [],
          "description": "optional textual labels for the start/end thumb tooltips (range slider).",
          "source": "storybook"
        },
        {
          "name": "valueLabelStart",
          "attribute": "value-label-start",
          "type": "text",
          "values": [],
          "description": "optional textual labels for the start/end thumb tooltips (range slider).",
          "source": "storybook"
        },
        {
          "name": "valueStart",
          "attribute": "value-start",
          "type": "number",
          "values": [],
          "description": "current start/end values when `range` is `true`. Defaults to `min` / `max` when omitted. Thumbs cannot cross — if `value-start` is dragged past `value-end` it is clamped, and vice versa.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "input",
          "description": "Fired when the slider value changes"
        },
        {
          "name": "change",
          "description": "Fired when the slider value is committed"
        }
      ],
      "cssParts": [
        {
          "name": "slider-tooltip",
          "description": "The tooltip of the slider"
        },
        {
          "name": "slider-track",
          "description": "The track of the slider"
        },
        {
          "name": "slider-wrapper",
          "description": "The wrapper around the slider input(s)"
        },
        {
          "name": "slider-ticks",
          "description": "The container for the tick marks"
        },
        {
          "name": "slider-tick",
          "description": "The individual tick marks"
        },
        {
          "name": "slider-input",
          "description": "The input element of the slider"
        },
        {
          "name": "slider-label",
          "description": "The label of the slider"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-slider-thumb-color",
          "description": "The color of the slider thumb"
        },
        {
          "name": "--mdc-slider-thumb-border-color",
          "description": "The color of the slider thumb border"
        },
        {
          "name": "--mdc-slider-thumb-size",
          "description": "The size of the slider thumb"
        },
        {
          "name": "--mdc-slider-input-size",
          "description": "The height of the slider input"
        },
        {
          "name": "--mdc-slider-tick-size",
          "description": "The size of the slider tick marks"
        },
        {
          "name": "--mdc-slider-track-height",
          "description": "The height of the slider track"
        },
        {
          "name": "--mdc-slider-tick-color",
          "description": "The color of the slider tick marks"
        },
        {
          "name": "--mdc-slider-progress-color",
          "description": "The color of the slider progress"
        },
        {
          "name": "--mdc-slider-track-color",
          "description": "The color of the slider track"
        },
        {
          "name": "--mdc-slider-tooltip-left",
          "description": "The left position of the slider tooltip"
        },
        {
          "name": "--mdc-slider-tick-left",
          "description": "The left position of the slider tick marks"
        }
      ],
      "rules": [
        "when `true`, renders two thumbs (start and end) instead of one. Default `false`.",
        "minimum value. Default `0`.",
        "maximum value. Default `100`.",
        "increment between values. Default `1`. When `step > 1`, tick marks are rendered and thumbs snap to ticks.",
        "current value when `range` is `false`.",
        "current start/end values when `range` is `true`. Defaults to `min` / `max` when omitted. Thumbs cannot cross — if `value-start` is dragged past `value-end` it is clamped, and vice versa.",
        "current start/end values when `range` is `true`. Defaults to `min` / `max` when omitted. Thumbs cannot cross — if `value-start` is dragged past `value-end` it is clamped, and vice versa.",
        "form field name for the single-value slider.",
        "form field names for the range thumbs.",
        "form field names for the range thumbs.",
        "visible label rendered above the slider.",
        "labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).",
        "labels rendered below the slider on the leading and trailing sides (commonly used to label the min/max ends of the range).",
        "optional textual label shown for the value in the tooltip (single-value slider). Defaults to the numeric value.",
        "optional textual labels for the start/end thumb tooltips (range slider).",
        "optional textual labels for the start/end thumb tooltips (range slider).",
        "icon names rendered on the leading and trailing ends (e.g. muted speaker / full-volume speaker).",
        "icon names rendered on the leading and trailing ends (e.g. muted speaker / full-volume speaker).",
        "fully disabled; the thumbs are removed from the tab order.",
        "visually disabled but focusable; user input is suppressed and the tooltip is shown on focus.",
        "`aria-label` for the single thumb.",
        "`aria-label`s for the start and end thumbs in range mode.",
        "`aria-label`s for the start and end thumbs in range mode.",
        "`aria-valuetext` for the single thumb (falls back to `value-label`, then to the numeric value).",
        "`aria-valuetext`s for the start and end thumbs (fall back to the matching `value-label-*`, then to the numeric value).",
        "`aria-valuetext`s for the start and end thumbs (fall back to the matching `value-label-*`, then to the numeric value).",
        "when `true`, the value tooltip is not shown on focus or hover.",
        "fires continuously while the user drags or steps the slider; `event.detail` contains the current `value` (single) or `{ valueStart, valueEnd }` (range).",
        "fires once the value is committed (mouse up / keyboard release); `event.detail` matches `input`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/slider/index.js\"; // custom element registration\n// or via React wrapper\nimport { Slider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<!-- Single-value slider -->\n<mdc-slider\n  label=\"Volume\"\n  name=\"volume\"\n  min=\"0\"\n  max=\"100\"\n  step=\"1\"\n  value=\"40\"\n></mdc-slider>\n\n<!-- Range slider with tick marks -->\n<mdc-slider\n  range\n  label=\"Price range\"\n  name-start=\"min-price\"\n  name-end=\"max-price\"\n  min=\"0\"\n  max=\"100\"\n  step=\"10\"\n  value-start=\"20\"\n  value-end=\"80\"\n  start-aria-label=\"Minimum price\"\n  end-aria-label=\"Maximum price\"\n></mdc-slider>"
      }
    ],
    "reactImports": [
      "Slider"
    ],
    "constants": {
      "THUMBSTATE": {
        "START": "start",
        "END": "end",
        "UNDEFINED": "undefined"
      },
      "DEFAULTS": {
        "MIN": 0,
        "MAX": 100,
        "STEP": 1,
        "STATE": "undefined",
        "ICON_SIZE": 1.25,
        "ICON_LENGTH_UNIT": "rem"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "as",
      "component",
      "for",
      "guidelines",
      "icon",
      "input",
      "mdc",
      "on",
      "one",
      "or",
      "overview",
      "range",
      "rendered",
      "single",
      "slider",
      "the",
      "thumbs",
      "track",
      "two",
      "usage",
      "value"
    ]
  },
  {
    "name": "spatialnavigationprovider",
    "title": "Spatial Navigation Provider",
    "tagName": "mdc-spatialnavigationprovider",
    "summary": "Usage, guidelines, and accessibility for the mdc-spatialnavigationprovider component — an application-root context provider that moves focus on a 2D plane in response to arrow, Enter, and Escape keys.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/spatialnavigationprovider",
      "knowledgeBasePath": "components/src/components/spatialnavigationprovider/knowledge-base/spatialnavigationprovider.component.md"
    },
    "docs": "## Overview\n\nThe spatial navigation provider implements [spatial navigation](https://en.wikipedia.org/wiki/Spatial_navigation): it lets users move focus between elements on a 2D plane using arrow keys, Enter, and Escape, the way TV remotes and game controllers do. It listens to keyboard events at the document level, computes the next focus target from element positions, and dispatches cancellable events around every navigation step.\n\nOnly one instance is supported per application, placed at the root.\n\n### When to use\n\n- Use `mdc-spatialnavigationprovider` for TV, set-top-box, console, or kiosk apps where the primary input is a remote or directional pad.\n- Use it when consumers should be able to override or intercept focus movement via data attributes or DOM events.\n\n### When not to use\n\n- Do not use it in a typical pointer-and-keyboard web app where browser-native focus order is sufficient.\n- Do not nest providers — only one instance per application is supported.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/spatialnavigationprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { SpatialNavigationProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-spatialnavigationprovider>\n  <!-- application root -->\n  <main>\n    <mdc-button data-spatial-go-back>Back</mdc-button>\n    <mdc-button>Play</mdc-button>\n    <mdc-button>Pause</mdc-button>\n  </main>\n</mdc-spatialnavigationprovider>\n```\n\n### Property/Attribute details\n\n- `navigationKeyMapping` — key mapping for the six spatial navigation actions. Defaults are: `up: \"ArrowUp\"`, `down: \"ArrowDown\"`, `left: \"ArrowLeft\"`, `right: \"ArrowRight\"`, `enter: \"Enter\"`, `escape: \"Escape\"`. Override to support remotes that emit different key names (e.g. `\"GoBack\"`).\n- `distanceCalculationWeights` — weights for the [W3C \"find the shortest distance\" algorithm](https://www.w3.org/TR/css-nav-1/#find-the-shortest-distance) used when no override attribute is present. Tune for your UI layout.\n\nControl data attributes (set on focusable descendants):\n\n| Attribute                    | Value                        | Description                                                                                                                        |\n| ---------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |\n| `data-spatial-left`          | empty / id / selector        | Prevent native navigation in the Left direction, focus the target if focusable, otherwise limit the search to that subtree.        |\n| `data-spatial-up`            | empty / id / selector        | Same as above for the Up direction.                                                                                                |\n| `data-spatial-right`         | empty / id / selector        | Same as above for the Right direction.                                                                                             |\n| `data-spatial-down`          | empty / id / selector        | Same as above for the Down direction.                                                                                              |\n| `data-spatial-go-back`       | n/a                          | First focusable element with this attribute is clicked on Back/Escape.                                                             |\n| `data-spatial-focusable`     | n/a                          | Treat the element as focusable even when it normally is not (e.g. `tabindex=\"-1\"`).                                                |\n| `data-spatial-exclude`       | n/a                          | Exclude the focusable element (and its subtree) from navigation.                                                                   |\n| `data-spatial-noscroll`      | n/a                          | Prevent automatic scrolling for the active element inside a scrollable area even if it does not fit in view.                       |\n| `data-spatial-scroll-parent` | n/a                          | When the focusable item is not a direct child of the scrollable area, mark the scrollable ancestor with this attribute.            |\n\nEvents dispatched by the provider (all cancellable unless noted):\n\n- `navbeforeprocess` — dispatched on the currently focused element before any spatial navigation action is processed. Call `preventDefault()` to stop the provider from handling the key (use this when a component handles its own navigation, e.g. List, Combobox).\n- `navbeforefocus` — dispatched on the currently focused element before focus moves. Call `preventDefault()` to cancel the focus change. Mirrors the [CSS Spatial Navigation `navbeforefocus`](https://www.w3.org/TR/css-nav-1/#event-type-navbeforefocus) event.\n- `navback` — dispatched on the provider when a back action is triggered. `event.detail` contains the `goBackElement` (if any). Cancel to suppress the click on the go-back element.\n- `navnotarget` — dispatched on the provider when there is no focus target in the requested direction within the current focus area.\n\nPublic methods:\n\n- `isNavigationKey(key)` / `isDirectionKey(key)` — true if `key` matches one of the configured navigation / direction keys.\n- `pressEnter()` — triggers `.click()` on the currently focused element; returns `true` if a click was triggered.\n- `goBack()` — dispatches `navback`; if a `data-spatial-go-back` element exists (and the event is not prevented), clicks it. Otherwise calls `history.back()` provided history was pushed since the provider was initialized. Returns `true` if back was handled.\n\n### Notes\n\n**How focus is computed.** On each keydown, the provider:\n\n1. Handles `keydown` in the capture phase. If the active element has a matching `data-spatial-{direction}` attribute, the provider runs its own handler immediately. If the active element is inside a scrollable parent and is not fully visible in the requested direction (and has no `data-spatial-noscroll`), navigation is suppressed and the parent scrolls by half its size.\n2. Lets the component's own `keydown` handler run (bubble phase) — for example a list moving focus internally.\n3. Runs the provider's bubble-phase `keydown`:\n   - Dispatches `navbeforeprocess`. If prevented, stop.\n   - For arrow keys, evaluates `data-spatial-{direction}` (id first, CSS selector fallback). If the target is focusable, focus moves there; otherwise the search is constrained to that subtree.\n   - Otherwise, finds the nearest focus area (scrollable container or active focus trap), collects focusable elements in that area, and computes distances using the W3C shortest-distance algorithm. The closest candidate wins.\n   - Dispatches `navbeforefocus` on the current element. If prevented, no focus change. Otherwise focus moves.\n   - If no candidate exists, dispatches `navnotarget`.\n\n`data-spatial-focusable` and `data-spatial-exclude` extend or trim the focusable set respectively.\n\n**Enter and Escape actions.**\n\n- `Enter` triggers `.click()` on the focused element. Prevent with `navbeforeprocess` to handle Enter yourself.\n- `Escape` (or the configured `escape` key) dispatches `navback`. If not prevented and a `data-spatial-go-back` element exists, it is clicked. Otherwise the provider calls `history.back()` if history has been pushed since initialization.\n\n**Form inputs and text fields.** Native inputs often submit on Enter. In spatial navigation contexts, prefer toggling/activating controls and provide a dedicated submit button. On TV-like platforms without a physical keyboard, focusing a text input should open the virtual keyboard; users close it (Escape) to resume spatial navigation. Letter-key mappings (e.g. `w`/`a`/`s`/`d`) collide with typing in text inputs and should be edited via the virtual keyboard.\n\n**Helper mixins for complex components.**\n\n- `KeyToActionMixin` — maps key events to action names; provides `getActionForKeyEvent` and `getKeyboardNavMode` (returns spatial vs default mode).\n- `KeyDownHandledMixin` — call `keyDownEventHandled()` whenever your component handled a keydown internally so the provider's bubble handler can skip it.\n\n**Debugging.**\n\n- The Storybook toolbar exposes a \"Spatial navigation\" mode that maps to the default key set.\n- With spatial navigation enabled, holding `Shift` + a navigation key renders a visual debugger overlay: a star marks the chosen next element, `#{n}` shows candidate order by distance, and `D: {distance}` shows the computed distance.\n\n### Limitations\n\n- Only one provider instance is supported per application.\n- The distance-based algorithm cannot guarantee that every element is reachable in all four directions. Isolated elements should be linked explicitly with `data-spatial-{direction}` attributes; favour stable layouts, dedicated list/menu components, and avoid heavy overlap or nested focusable elements.\n- Responsive layouts with shifting positions can make navigation unpredictable, which is especially visible when resizing Storybook stories.\n\n## Accessibility\n\n### Built-in features\n\nThe component provides focus management only — it does not render any UI or expose ARIA semantics of its own. Focus moves with `.focus()` on the chosen target, so the target's own roles, names, and states drive what screen readers announce.\n\n### Notes\n\nBecause the provider listens to `keydown` at the document level in the capture phase, components or pages that rely on default browser behaviour for arrow keys, Enter, or Escape should opt out via `navbeforeprocess` (`event.preventDefault()`) to keep their own handlers in control.",
    "sections": {
      "Overview": "The spatial navigation provider implements [spatial navigation](https://en.wikipedia.org/wiki/Spatial_navigation): it lets users move focus between elements on a 2D plane using arrow keys, Enter, and Escape, the way TV remotes and game controllers do. It listens to keyboard events at the document level, computes the next focus target from element positions, and dispatches cancellable events around every navigation step.\n\nOnly one instance is supported per application, placed at the root.\n\n### When to use\n\n- Use `mdc-spatialnavigationprovider` for TV, set-top-box, console, or kiosk apps where the primary input is a remote or directional pad.\n- Use it when consumers should be able to override or intercept focus movement via data attributes or DOM events.\n\n### When not to use\n\n- Do not use it in a typical pointer-and-keyboard web app where browser-native focus order is sufficient.\n- Do not nest providers — only one instance per application is supported.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/spatialnavigationprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { SpatialNavigationProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-spatialnavigationprovider>\n  <!-- application root -->\n  <main>\n    <mdc-button data-spatial-go-back>Back</mdc-button>\n    <mdc-button>Play</mdc-button>\n    <mdc-button>Pause</mdc-button>\n  </main>\n</mdc-spatialnavigationprovider>\n```\n\n### Property/Attribute details\n\n- `navigationKeyMapping` — key mapping for the six spatial navigation actions. Defaults are: `up: \"ArrowUp\"`, `down: \"ArrowDown\"`, `left: \"ArrowLeft\"`, `right: \"ArrowRight\"`, `enter: \"Enter\"`, `escape: \"Escape\"`. Override to support remotes that emit different key names (e.g. `\"GoBack\"`).\n- `distanceCalculationWeights` — weights for the [W3C \"find the shortest distance\" algorithm](https://www.w3.org/TR/css-nav-1/#find-the-shortest-distance) used when no override attribute is present. Tune for your UI layout.\n\nControl data attributes (set on focusable descendants):\n\n| Attribute                    | Value                        | Description                                                                                                                        |\n| ---------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |\n| `data-spatial-left`          | empty / id / selector        | Prevent native navigation in the Left direction, focus the target if focusable, otherwise limit the search to that subtree.        |\n| `data-spatial-up`            | empty / id / selector        | Same as above for the Up direction.                                                                                                |\n| `data-spatial-right`         | empty / id / selector        | Same as above for the Right direction.                                                                                             |\n| `data-spatial-down`          | empty / id / selector        | Same as above for the Down direction.                                                                                              |\n| `data-spatial-go-back`       | n/a                          | First focusable element with this attribute is clicked on Back/Escape.                                                             |\n| `data-spatial-focusable`     | n/a                          | Treat the element as focusable even when it normally is not (e.g. `tabindex=\"-1\"`).                                                |\n| `data-spatial-exclude`       | n/a                          | Exclude the focusable element (and its subtree) from navigation.                                                                   |\n| `data-spatial-noscroll`      | n/a                          | Prevent automatic scrolling for the active element inside a scrollable area even if it does not fit in view.                       |\n| `data-spatial-scroll-parent` | n/a                          | When the focusable item is not a direct child of the scrollable area, mark the scrollable ancestor with this attribute.            |\n\nEvents dispatched by the provider (all cancellable unless noted):\n\n- `navbeforeprocess` — dispatched on the currently focused element before any spatial navigation action is processed. Call `preventDefault()` to stop the provider from handling the key (use this when a component handles its own navigation, e.g. List, Combobox).\n- `navbeforefocus` — dispatched on the currently focused element before focus moves. Call `preventDefault()` to cancel the focus change. Mirrors the [CSS Spatial Navigation `navbeforefocus`](https://www.w3.org/TR/css-nav-1/#event-type-navbeforefocus) event.\n- `navback` — dispatched on the provider when a back action is triggered. `event.detail` contains the `goBackElement` (if any). Cancel to suppress the click on the go-back element.\n- `navnotarget` — dispatched on the provider when there is no focus target in the requested direction within the current focus area.\n\nPublic methods:\n\n- `isNavigationKey(key)` / `isDirectionKey(key)` — true if `key` matches one of the configured navigation / direction keys.\n- `pressEnter()` — triggers `.click()` on the currently focused element; returns `true` if a click was triggered.\n- `goBack()` — dispatches `navback`; if a `data-spatial-go-back` element exists (and the event is not prevented), clicks it. Otherwise calls `history.back()` provided history was pushed since the provider was initialized. Returns `true` if back was handled.\n\n### Notes\n\n**How focus is computed.** On each keydown, the provider:\n\n1. Handles `keydown` in the capture phase. If the active element has a matching `data-spatial-{direction}` attribute, the provider runs its own handler immediately. If the active element is inside a scrollable parent and is not fully visible in the requested direction (and has no `data-spatial-noscroll`), navigation is suppressed and the parent scrolls by half its size.\n2. Lets the component's own `keydown` handler run (bubble phase) — for example a list moving focus internally.\n3. Runs the provider's bubble-phase `keydown`:\n   - Dispatches `navbeforeprocess`. If prevented, stop.\n   - For arrow keys, evaluates `data-spatial-{direction}` (id first, CSS selector fallback). If the target is focusable, focus moves there; otherwise the search is constrained to that subtree.\n   - Otherwise, finds the nearest focus area (scrollable container or active focus trap), collects focusable elements in that area, and computes distances using the W3C shortest-distance algorithm. The closest candidate wins.\n   - Dispatches `navbeforefocus` on the current element. If prevented, no focus change. Otherwise focus moves.\n   - If no candidate exists, dispatches `navnotarget`.\n\n`data-spatial-focusable` and `data-spatial-exclude` extend or trim the focusable set respectively.\n\n**Enter and Escape actions.**\n\n- `Enter` triggers `.click()` on the focused element. Prevent with `navbeforeprocess` to handle Enter yourself.\n- `Escape` (or the configured `escape` key) dispatches `navback`. If not prevented and a `data-spatial-go-back` element exists, it is clicked. Otherwise the provider calls `history.back()` if history has been pushed since initialization.\n\n**Form inputs and text fields.** Native inputs often submit on Enter. In spatial navigation contexts, prefer toggling/activating controls and provide a dedicated submit button. On TV-like platforms without a physical keyboard, focusing a text input should open the virtual keyboard; users close it (Escape) to resume spatial navigation. Letter-key mappings (e.g. `w`/`a`/`s`/`d`) collide with typing in text inputs and should be edited via the virtual keyboard.\n\n**Helper mixins for complex components.**\n\n- `KeyToActionMixin` — maps key events to action names; provides `getActionForKeyEvent` and `getKeyboardNavMode` (returns spatial vs default mode).\n- `KeyDownHandledMixin` — call `keyDownEventHandled()` whenever your component handled a keydown internally so the provider's bubble handler can skip it.\n\n**Debugging.**\n\n- The Storybook toolbar exposes a \"Spatial navigation\" mode that maps to the default key set.\n- With spatial navigation enabled, holding `Shift` + a navigation key renders a visual debugger overlay: a star marks the chosen next element, `#{n}` shows candidate order by distance, and `D: {distance}` shows the computed distance.\n\n### Limitations\n\n- Only one provider instance is supported per application.\n- The distance-based algorithm cannot guarantee that every element is reachable in all four directions. Isolated elements should be linked explicitly with `data-spatial-{direction}` attributes; favour stable layouts, dedicated list/menu components, and avoid heavy overlap or nested focusable elements.\n- Responsive layouts with shifting positions can make navigation unpredictable, which is especially visible when resizing Storybook stories.",
      "Accessibility": "### Built-in features\n\nThe component provides focus management only — it does not render any UI or expose ARIA semantics of its own. Focus moves with `.focus()` on the chosen target, so the target's own roles, names, and states drive what screen readers announce.\n\n### Notes\n\nBecause the provider listens to `keydown` at the document level in the capture phase, components or pages that rely on default browser behaviour for arrow keys, Enter, or Escape should opt out via `navbeforeprocess` (`event.preventDefault()`) to keep their own handlers in control."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "distanceCalculationWeights",
          "type": "ShortestDistanceWeights",
          "default": "DEFAULTS.WEIGHTS",
          "values": [],
          "description": "weights for the [W3C \"find the shortest distance\" algorithm](https://www.w3.org/TR/css-nav-1/#find-the-shortest-distance) used when no override attribute is present. Tune for your UI layout.",
          "reflected": false,
          "source": "component"
        },
        {
          "name": "navigationKeyMapping",
          "type": "SpatialNavigationActionToKeyMap",
          "default": "DEFAULTS.SPATIAL_NAVIGATION_KEY_MAPPING",
          "values": [],
          "description": "key mapping for the six spatial navigation actions. Defaults are: `up: \"ArrowUp\"`, `down: \"ArrowDown\"`, `left: \"ArrowLeft\"`, `right: \"ArrowRight\"`, `enter: \"Enter\"`, `escape: \"Escape\"`. Override to support remotes that emit different key names (e.g. `\"GoBack\"`).",
          "reflected": false,
          "source": "component"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "navbeforeprocess",
          "description": "(React: onNavBeforeProcess) This event dispatched before spatial navigation process any key event."
        },
        {
          "name": "navbeforefocus",
          "description": "(React: onNavBeforeFocus) This event is dispatched before the focus is changing to the next element."
        },
        {
          "name": "navback",
          "description": "(React: onNavBack) This event dispatched a back navigation triggered by the user."
        },
        {
          "name": "navnotarget",
          "description": "(React: onNavNoTarget) This event is dispatched when there is no target to focus in the current focus area and"
        }
      ],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "key mapping for the six spatial navigation actions. Defaults are: `up: \"ArrowUp\"`, `down: \"ArrowDown\"`, `left: \"ArrowLeft\"`, `right: \"ArrowRight\"`, `enter: \"Enter\"`, `escape: \"Escape\"`. Override to support remotes that emit different key names (e.g. `\"GoBack\"`).",
        "weights for the [W3C \"find the shortest distance\" algorithm](https://www.w3.org/TR/css-nav-1/#find-the-shortest-distance) used when no override attribute is present. Tune for your UI layout.",
        "dispatched on the currently focused element before any spatial navigation action is processed. Call `preventDefault()` to stop the provider from handling the key (use this when a component handles its own navigation, e.g. List, Combobox).",
        "dispatched on the currently focused element before focus moves. Call `preventDefault()` to cancel the focus change. Mirrors the [CSS Spatial Navigation `navbeforefocus`](https://www.w3.org/TR/css-nav-1/#event-type-navbeforefocus) event.",
        "dispatched on the provider when a back action is triggered. `event.detail` contains the `goBackElement` (if any). Cancel to suppress the click on the go-back element.",
        "dispatched on the provider when there is no focus target in the requested direction within the current focus area.",
        "true if `key` matches one of the configured navigation / direction keys.",
        "true if `key` matches one of the configured navigation / direction keys.",
        "triggers `.click()` on the currently focused element; returns `true` if a click was triggered.",
        "dispatches `navback`; if a `data-spatial-go-back` element exists (and the event is not prevented), clicks it. Otherwise calls `history.back()` provided history was pushed since the provider was initialized. Returns `true` if back was handled."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/spatialnavigationprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { SpatialNavigationProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-spatialnavigationprovider>\n  <!-- application root -->\n  <main>\n    <mdc-button data-spatial-go-back>Back</mdc-button>\n    <mdc-button>Play</mdc-button>\n    <mdc-button>Pause</mdc-button>\n  </main>\n</mdc-spatialnavigationprovider>"
      }
    ],
    "reactImports": [
      "SpatialNavigationProvider"
    ],
    "constants": {
      "DATA_ATTRIBUTES": {
        "DIRECTION_LEFT": "data-spatial-left",
        "DIRECTION_RIGHT": "data-spatial-right",
        "DIRECTION_UP": "data-spatial-up",
        "DIRECTION_DOWN": "data-spatial-down",
        "GO_BACK": "data-spatial-go-back",
        "FOCUSABLE": "data-spatial-focusable",
        "EXCLUDE": "data-spatial-exclude",
        "NO_SCROLL": "data-spatial-noscroll",
        "SCROLL_PARENT": "data-spatial-scroll-parent",
        "SPATIAL_NAVIGATION_KEY_MAPPING": "{",
        "left": "ArrowLeft",
        "right": "ArrowRight",
        "up": "ArrowUp",
        "down": "ArrowDown",
        "escape": "Escape",
        "enter": "Enter",
        "WEIGHTS": "{",
        "alignWeight": 5,
        "orthogonalWeight": "{",
        "vertical": 2,
        "horizontal": 30
      }
    },
    "keywords": [
      "2d",
      "a",
      "accessibility",
      "an",
      "and",
      "application",
      "arrow",
      "component",
      "context",
      "enter",
      "escape",
      "focus",
      "for",
      "guidelines",
      "in",
      "keys",
      "mdc",
      "moves",
      "navigation",
      "on",
      "overview",
      "plane",
      "provider",
      "response",
      "root",
      "spatial",
      "spatialnavigationprovider",
      "that",
      "the",
      "to",
      "usage"
    ]
  },
  {
    "name": "spinner",
    "title": "Spinner",
    "tagName": "mdc-spinner",
    "summary": "Usage, guidelines, and accessibility for the mdc-spinner component — an indeterminate loading indicator for processes with unknown or variable duration.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/spinner",
      "knowledgeBasePath": "components/src/components/spinner/knowledge-base/spinner.component.md"
    },
    "docs": "## Overview\n\nThe spinner is an indeterminate progress indicator, best suited for cases where the duration or progress of an operation is variable or unknown. It is rendered as a rotating circular arc and supports multiple sizes, two colour variants, and a button-friendly variant for in-button loading states.\n\n### When to use\n\n- Use `mdc-spinner` to signal that something is happening but you cannot communicate concrete progress.\n- Use the `button` variant inside `mdc-button` to indicate that the button's action is in progress.\n- Use the small size inline with loading text, the midsize for most cases, and the large size for full-region or full-page loading.\n\n### When not to use\n\n- Use `mdc-progressbar` when you can show concrete, measurable progress.\n- Use `mdc-skeleton` when you want a placeholder that matches the layout of the content being loaded.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/spinner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Spinner } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup examples:\n\n```html\n<!-- Decorative spinner ignored by screen readers -->\n<mdc-spinner size=\"midsize\"></mdc-spinner>\n\n<!-- Spinner with an accessible name announced as an image -->\n<mdc-spinner size=\"large\" aria-label=\"Loading dashboard\"></mdc-spinner>\n\n<!-- Inverted spinner for use inside coachmarks and other inverted surfaces -->\n<mdc-spinner inverted></mdc-spinner>\n\n<!-- Button-variant spinner inside a button -->\n<mdc-button>\n  <mdc-spinner variant=\"button\" size=\"small\"></mdc-spinner>\n  Saving…\n</mdc-button>\n```\n\n### Property/Attribute details\n\n- `size` — visual size. One of `small` (24 px, use inline with text), `midsize` (48 px, default), `large` (96 px, use when replacing entire regions/pages), or `undefined`. When set to `undefined`, use the `--mdc-spinner-size` CSS custom property to set a custom size.\n- `variant` — `standalone` (default; blue track) or `button` (to be used inside a button; matches the button icon/label colour to keep at least a 3:1 contrast ratio).\n- `inverted` — when `true`, uses the inverted colour set. Intended for inverted surfaces such as coachmarks. Default `false`.\n- `aria-label` — accessible name for the spinner. When set, the host becomes `role=\"img\"` and `aria-hidden=\"false\"`; when omitted, the host has no role and `aria-hidden=\"true\"`.\n\n## Accessibility\n\n### Built-in features\n\nAccessibility behaviour is driven entirely by `aria-label`:\n\n- When `aria-label` is set, the host is exposed as `role=\"img\"` with the provided name and `aria-hidden=\"false\"` — screen readers announce it as a labelled image.\n- When `aria-label` is omitted, the host has no role and `aria-hidden=\"true\"` — screen readers ignore the spinner entirely.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute     | Value                                                |\n| ------- | ------------- | ---------------------------------------------------- |\n| Host    | `role`        | `img` when `aria-label` is set; otherwise unset      |\n| Host    | `aria-hidden` | `false` when `aria-label` is set; otherwise `true`   |\n\n### Notes\n\nThe spinner is indeterminate — it does not expose `aria-valuenow`/`aria-valuetext`. If progress is known, use `mdc-progressbar` instead so screen readers can announce the current value.",
    "sections": {
      "Overview": "The spinner is an indeterminate progress indicator, best suited for cases where the duration or progress of an operation is variable or unknown. It is rendered as a rotating circular arc and supports multiple sizes, two colour variants, and a button-friendly variant for in-button loading states.\n\n### When to use\n\n- Use `mdc-spinner` to signal that something is happening but you cannot communicate concrete progress.\n- Use the `button` variant inside `mdc-button` to indicate that the button's action is in progress.\n- Use the small size inline with loading text, the midsize for most cases, and the large size for full-region or full-page loading.\n\n### When not to use\n\n- Use `mdc-progressbar` when you can show concrete, measurable progress.\n- Use `mdc-skeleton` when you want a placeholder that matches the layout of the content being loaded.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/spinner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Spinner } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup examples:\n\n```html\n<!-- Decorative spinner ignored by screen readers -->\n<mdc-spinner size=\"midsize\"></mdc-spinner>\n\n<!-- Spinner with an accessible name announced as an image -->\n<mdc-spinner size=\"large\" aria-label=\"Loading dashboard\"></mdc-spinner>\n\n<!-- Inverted spinner for use inside coachmarks and other inverted surfaces -->\n<mdc-spinner inverted></mdc-spinner>\n\n<!-- Button-variant spinner inside a button -->\n<mdc-button>\n  <mdc-spinner variant=\"button\" size=\"small\"></mdc-spinner>\n  Saving…\n</mdc-button>\n```\n\n### Property/Attribute details\n\n- `size` — visual size. One of `small` (24 px, use inline with text), `midsize` (48 px, default), `large` (96 px, use when replacing entire regions/pages), or `undefined`. When set to `undefined`, use the `--mdc-spinner-size` CSS custom property to set a custom size.\n- `variant` — `standalone` (default; blue track) or `button` (to be used inside a button; matches the button icon/label colour to keep at least a 3:1 contrast ratio).\n- `inverted` — when `true`, uses the inverted colour set. Intended for inverted surfaces such as coachmarks. Default `false`.\n- `aria-label` — accessible name for the spinner. When set, the host becomes `role=\"img\"` and `aria-hidden=\"false\"`; when omitted, the host has no role and `aria-hidden=\"true\"`.",
      "Accessibility": "### Built-in features\n\nAccessibility behaviour is driven entirely by `aria-label`:\n\n- When `aria-label` is set, the host is exposed as `role=\"img\"` with the provided name and `aria-hidden=\"false\"` — screen readers announce it as a labelled image.\n- When `aria-label` is omitted, the host has no role and `aria-hidden=\"true\"` — screen readers ignore the spinner entirely.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute     | Value                                                |\n| ------- | ------------- | ---------------------------------------------------- |\n| Host    | `role`        | `img` when `aria-label` is set; otherwise unset      |\n| Host    | `aria-hidden` | `false` when `aria-label` is set; otherwise `true`   |\n\n### Notes\n\nThe spinner is indeterminate — it does not expose `aria-valuenow`/`aria-valuetext`. If progress is known, use `mdc-progressbar` instead so screen readers can announce the current value."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "string | null",
          "default": "false",
          "values": [],
          "description": "accessible name for the spinner. When set, the host becomes `role=\"img\"` and `aria-hidden=\"false\"`; when omitted, the host has no role and `aria-hidden=\"true\"`.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "inverted",
          "attribute": "inverted",
          "type": "boolean",
          "values": [],
          "description": "when `true`, uses the inverted colour set. Intended for inverted surfaces such as coachmarks. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "values": [],
          "description": "visual size. One of `small` (24 px, use inline with text), `midsize` (48 px, default), `large` (96 px, use when replacing entire regions/pages), or `undefined`. When set to `undefined`, use the `--mdc-spinner-size` CSS custom property to set a custom size.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "standalone",
          "values": [
            "standalone",
            "button"
          ],
          "description": "`standalone` (default; blue track) or `button` (to be used inside a button; matches the button icon/label colour to keep at least a 3:1 contrast ratio).",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "container",
          "description": "The svg which contains the circle spinner."
        },
        {
          "name": "circle",
          "description": "The circle of the spinner."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-spinner-default-color",
          "description": "Allows customization of the default spinner color."
        },
        {
          "name": "--mdc-spinner-inverted-color",
          "description": "Allows customization of the inverted spinner color."
        },
        {
          "name": "--mdc-spinner-button-variant-color",
          "description": "Allows customization of the spinner Button variant color."
        },
        {
          "name": "--mdc-spinner-size",
          "description": "Allows customization of the spinner size."
        }
      ],
      "rules": [
        "visual size. One of `small` (24 px, use inline with text), `midsize` (48 px, default), `large` (96 px, use when replacing entire regions/pages), or `undefined`. When set to `undefined`, use the `--mdc-spinner-size` CSS custom property to set a custom size.",
        "`standalone` (default; blue track) or `button` (to be used inside a button; matches the button icon/label colour to keep at least a 3:1 contrast ratio).",
        "when `true`, uses the inverted colour set. Intended for inverted surfaces such as coachmarks. Default `false`.",
        "accessible name for the spinner. When set, the host becomes `role=\"img\"` and `aria-hidden=\"false\"`; when omitted, the host has no role and `aria-hidden=\"true\"`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/spinner/index.js\"; // custom element registration\n// or via React wrapper\nimport { Spinner } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<!-- Decorative spinner ignored by screen readers -->\n<mdc-spinner size=\"midsize\"></mdc-spinner>\n\n<!-- Spinner with an accessible name announced as an image -->\n<mdc-spinner size=\"large\" aria-label=\"Loading dashboard\"></mdc-spinner>\n\n<!-- Inverted spinner for use inside coachmarks and other inverted surfaces -->\n<mdc-spinner inverted></mdc-spinner>\n\n<!-- Button-variant spinner inside a button -->\n<mdc-button>\n  <mdc-spinner variant=\"button\" size=\"small\"></mdc-spinner>\n  Saving…\n</mdc-button>"
      }
    ],
    "reactImports": [
      "Spinner"
    ],
    "constants": {
      "SPINNER_SIZES": {
        "LARGE": "large",
        "MIDSIZE": "midsize",
        "SMALL": "small"
      },
      "SPINNER_VARIANT": {
        "STANDALONE": "standalone",
        "BUTTON": "button"
      },
      "DEFAULTS": {
        "VARIANT": "standalone",
        "SIZE": "midsize",
        "INVERTED": false
      }
    },
    "keywords": [
      "accessibility",
      "an",
      "and",
      "component",
      "duration",
      "for",
      "guidelines",
      "indeterminate",
      "indicator",
      "loading",
      "mdc",
      "or",
      "overview",
      "processes",
      "spinner",
      "the",
      "unknown",
      "usage",
      "variable",
      "with"
    ]
  },
  {
    "name": "staticcheckbox",
    "title": "Static Checkbox",
    "tagName": "mdc-staticcheckbox",
    "summary": "Usage, guidelines, and accessibility for the mdc-staticcheckbox component — a decorative, non-interactive checkbox for read-only or summary views.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/staticcheckbox",
      "knowledgeBasePath": "components/src/components/staticcheckbox/knowledge-base/staticcheckbox.component.md"
    },
    "docs": "## Overview\n\nThe static checkbox is a decorative, non-interactive checkbox used to display checkbox states in read-only contexts (summary views, confirmation screens, historical form data). It supports checked, indeterminate, disabled, soft-disabled, and read-only visual states but does not handle user interaction or participate in forms.\n\n### When to use\n\n- Use `mdc-staticcheckbox` to render the visual shape of a checkbox in read-only UIs such as summary screens, confirmation screens, or historical form data.\n- Use it inside lists or cards where the checkbox is part of the display, not a control.\n\n### When not to use\n\n- Use `mdc-checkbox` for any interactive checkbox in a form.\n- Do not use `mdc-staticcheckbox` as a replacement for `mdc-checkbox` — it cannot be focused, toggled, or submitted with a form.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/staticcheckbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticCheckbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-staticcheckbox checked>Newsletter subscription</mdc-staticcheckbox>\n<mdc-staticcheckbox indeterminate>Partial selection</mdc-staticcheckbox>\n<mdc-staticcheckbox disabled>Unavailable option</mdc-staticcheckbox>\n```\n\n### Property/Attribute details\n\n- `checked` — when `true`, renders the checked check mark. Default `false`.\n- `indeterminate` — when `true`, renders the indeterminate (mixed) mark. Typically used for \"partial selection\" rollups. Default `false`. When both `checked` and `indeterminate` are set, the indeterminate mark wins.\n- `disabled` — when `true`, applies the standard disabled appearance. Purely visual.\n- `soft-disabled` — when `true`, applies a less prominent disabled appearance. Purely visual.\n- `readonly` — when `true`, applies the read-only appearance. Purely visual.\n\n### Limitations\n\n- Not interactive: cannot be focused, toggled, or activated by the user.\n- Not form-associated: has no `name`/`value` and is not submitted with a form. Use `mdc-checkbox` instead when form participation is needed.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders the slotted label content inside its light DOM but does not expose any checkbox semantics — it has no role, no `aria-checked`, and is not focusable. Screen readers will read any slotted label as plain text.\n\n### Notes\n\nBecause the component does not expose checkbox semantics, screen-reader users will not hear \"checkbox, checked\" / \"unchecked\" / \"mixed\" announcements. When the checked or indeterminate state carries meaning, include that state in the surrounding text or a hidden description (e.g. write \"Newsletter subscription: enabled\" instead of relying on the visual check mark alone), or use the interactive `mdc-checkbox` instead.",
    "sections": {
      "Overview": "The static checkbox is a decorative, non-interactive checkbox used to display checkbox states in read-only contexts (summary views, confirmation screens, historical form data). It supports checked, indeterminate, disabled, soft-disabled, and read-only visual states but does not handle user interaction or participate in forms.\n\n### When to use\n\n- Use `mdc-staticcheckbox` to render the visual shape of a checkbox in read-only UIs such as summary screens, confirmation screens, or historical form data.\n- Use it inside lists or cards where the checkbox is part of the display, not a control.\n\n### When not to use\n\n- Use `mdc-checkbox` for any interactive checkbox in a form.\n- Do not use `mdc-staticcheckbox` as a replacement for `mdc-checkbox` — it cannot be focused, toggled, or submitted with a form.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/staticcheckbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticCheckbox } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-staticcheckbox checked>Newsletter subscription</mdc-staticcheckbox>\n<mdc-staticcheckbox indeterminate>Partial selection</mdc-staticcheckbox>\n<mdc-staticcheckbox disabled>Unavailable option</mdc-staticcheckbox>\n```\n\n### Property/Attribute details\n\n- `checked` — when `true`, renders the checked check mark. Default `false`.\n- `indeterminate` — when `true`, renders the indeterminate (mixed) mark. Typically used for \"partial selection\" rollups. Default `false`. When both `checked` and `indeterminate` are set, the indeterminate mark wins.\n- `disabled` — when `true`, applies the standard disabled appearance. Purely visual.\n- `soft-disabled` — when `true`, applies a less prominent disabled appearance. Purely visual.\n- `readonly` — when `true`, applies the read-only appearance. Purely visual.\n\n### Limitations\n\n- Not interactive: cannot be focused, toggled, or activated by the user.\n- Not form-associated: has no `name`/`value` and is not submitted with a form. Use `mdc-checkbox` instead when form participation is needed.",
      "Accessibility": "### Built-in features\n\nThe component renders the slotted label content inside its light DOM but does not expose any checkbox semantics — it has no role, no `aria-checked`, and is not focusable. Screen readers will read any slotted label as plain text.\n\n### Notes\n\nBecause the component does not expose checkbox semantics, screen-reader users will not hear \"checkbox, checked\" / \"unchecked\" / \"mixed\" announcements. When the checked or indeterminate state carries meaning, include that state in the surrounding text or a hidden description (e.g. write \"Newsletter subscription: enabled\" instead of relying on the visual check mark alone), or use the interactive `mdc-checkbox` instead."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "description": "when `true`, renders the checked check mark. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, applies the standard disabled appearance. Purely visual.",
          "source": "storybook"
        },
        {
          "name": "indeterminate",
          "attribute": "indeterminate",
          "type": "boolean",
          "values": [],
          "description": "when `true`, renders the indeterminate (mixed) mark. Typically used for \"partial selection\" rollups. Default `false`. When both `checked` and `indeterminate` are set, the indeterminate mark wins.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "when `true`, applies the read-only appearance. Purely visual.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, applies a less prominent disabled appearance. Purely visual.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for label text."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "icon-container",
          "description": "The container for the checkbox icon."
        },
        {
          "name": "checkbox-icon",
          "description": "The checkbox icon element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-staticcheckbox-border-color",
          "description": "Border color of the checkbox."
        },
        {
          "name": "--mdc-staticcheckbox-background-color",
          "description": "Background color of the checkbox."
        },
        {
          "name": "--mdc-staticcheckbox-icon-color",
          "description": "Icon color of the checkbox."
        },
        {
          "name": "--mdc-staticcheckbox-size",
          "description": "Size of the checkbox."
        }
      ],
      "rules": [
        "when `true`, renders the checked check mark. Default `false`.",
        "when `true`, renders the indeterminate (mixed) mark. Typically used for \"partial selection\" rollups. Default `false`. When both `checked` and `indeterminate` are set, the indeterminate mark wins.",
        "when `true`, applies the standard disabled appearance. Purely visual.",
        "when `true`, applies a less prominent disabled appearance. Purely visual.",
        "when `true`, applies the read-only appearance. Purely visual."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/staticcheckbox/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticCheckbox } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-staticcheckbox checked>Newsletter subscription</mdc-staticcheckbox>\n<mdc-staticcheckbox indeterminate>Partial selection</mdc-staticcheckbox>\n<mdc-staticcheckbox disabled>Unavailable option</mdc-staticcheckbox>"
      }
    ],
    "reactImports": [
      "StaticCheckbox"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "checkbox",
      "component",
      "decorative",
      "for",
      "guidelines",
      "icon",
      "interactive",
      "mdc",
      "non",
      "only",
      "or",
      "overview",
      "read",
      "static",
      "staticcheckbox",
      "summary",
      "the",
      "usage",
      "views"
    ]
  },
  {
    "name": "staticchip",
    "title": "Static Chip",
    "tagName": "mdc-staticchip",
    "summary": "Usage, guidelines, and accessibility for the mdc-staticchip component — a non-interactive chip with a label and optional leading icon, used to display short metadata or status.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/staticchip",
      "knowledgeBasePath": "components/src/components/staticchip/knowledge-base/staticchip.component.md"
    },
    "docs": "## Overview\n\nThe static chip is a small, non-interactive element used to display a short label with an optional leading icon. It is intended for displaying metadata, tags, or status, and supports a fixed set of colour variants.\n\n### When to use\n\n- Use `mdc-staticchip` to display a short label or status that the user cannot interact with (e.g. a tag, category, or read-only attribute).\n- Use it inside lists, cards, or summary rows where the chip is purely informational.\n\n### When not to use\n\n- Use `mdc-chip` when the chip should respond to user interaction (click, selection).\n- Use `mdc-filterchip` for chips that toggle on/off as filters.\n- Use `mdc-inputchip` for editable, removable chips inside an input.\n- Use `mdc-badge` when you need a smaller, status-only indicator (especially for counts or notifications).\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/staticchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticChip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-staticchip label=\"In progress\" color=\"cobalt\" icon-name=\"clock-bold\"></mdc-staticchip>\n<mdc-staticchip label=\"Done\" color=\"mint\"></mdc-staticchip>\n```\n\n### Content guidance\n\n- Keep the label short. Limit to a maximum of 20 characters, including spaces, so the chip stays compact.\n- Use sentence case for the label.\n\n### Property/Attribute details\n\n- `label` — visible label text rendered inside the chip. Defaults to `undefined` (renders nothing if omitted).\n- `color` — colour variant. One of `default`, `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`. Defaults to `default`.\n- `icon-name` — name of the icon rendered before the label. When omitted, no icon is shown.\n\n### Limitations\n\n- Not interactive: cannot be focused, clicked, or activated.\n- Not form-associated: has no `name`/`value` and is not submitted with a form.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders the label inside an `mdc-text` element and the optional icon inside an `mdc-icon` element. It does not expose any chip-specific role or state; the label is read as plain text.\n\n### Notes\n\nBecause the component has no interactive role, ensure the surrounding context conveys what the chip represents (e.g. a label or heading describing the chip's purpose). When the chip carries semantic meaning that is not obvious from its label, include that meaning in the surrounding text.",
    "sections": {
      "Overview": "The static chip is a small, non-interactive element used to display a short label with an optional leading icon. It is intended for displaying metadata, tags, or status, and supports a fixed set of colour variants.\n\n### When to use\n\n- Use `mdc-staticchip` to display a short label or status that the user cannot interact with (e.g. a tag, category, or read-only attribute).\n- Use it inside lists, cards, or summary rows where the chip is purely informational.\n\n### When not to use\n\n- Use `mdc-chip` when the chip should respond to user interaction (click, selection).\n- Use `mdc-filterchip` for chips that toggle on/off as filters.\n- Use `mdc-inputchip` for editable, removable chips inside an input.\n- Use `mdc-badge` when you need a smaller, status-only indicator (especially for counts or notifications).",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/staticchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticChip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-staticchip label=\"In progress\" color=\"cobalt\" icon-name=\"clock-bold\"></mdc-staticchip>\n<mdc-staticchip label=\"Done\" color=\"mint\"></mdc-staticchip>\n```\n\n### Content guidance\n\n- Keep the label short. Limit to a maximum of 20 characters, including spaces, so the chip stays compact.\n- Use sentence case for the label.\n\n### Property/Attribute details\n\n- `label` — visible label text rendered inside the chip. Defaults to `undefined` (renders nothing if omitted).\n- `color` — colour variant. One of `default`, `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`. Defaults to `default`.\n- `icon-name` — name of the icon rendered before the label. When omitted, no icon is shown.\n\n### Limitations\n\n- Not interactive: cannot be focused, clicked, or activated.\n- Not form-associated: has no `name`/`value` and is not submitted with a form.",
      "Accessibility": "### Built-in features\n\nThe component renders the label inside an `mdc-text` element and the optional icon inside an `mdc-icon` element. It does not expose any chip-specific role or state; the label is read as plain text.\n\n### Notes\n\nBecause the component has no interactive role, ensure the surrounding context conveys what the chip represents (e.g. a label or heading describing the chip's purpose). When the chip carries semantic meaning that is not obvious from its label, include that meaning in the surrounding text."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "default": "default",
          "values": [
            "default",
            "cobalt",
            "gold",
            "lime",
            "mint",
            "orange",
            "pink",
            "purple",
            "slate",
            "violet"
          ],
          "description": "colour variant. One of `default`, `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`. Defaults to `default`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "select",
          "values": [],
          "description": "name of the icon rendered before the label. When omitted, no icon is shown.",
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label text rendered inside the chip. Defaults to `undefined` (renders nothing if omitted).",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "label",
          "description": "The label of the static chip."
        },
        {
          "name": "icon",
          "description": "The icon of the static chip."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-chip-color",
          "description": "The color of the static chip."
        },
        {
          "name": "--mdc-chip-border-color",
          "description": "The border color of the static chip."
        },
        {
          "name": "--mdc-chip-background-color",
          "description": "The background color of the static chip."
        }
      ],
      "rules": [
        "visible label text rendered inside the chip. Defaults to `undefined` (renders nothing if omitted).",
        "colour variant. One of `default`, `cobalt`, `gold`, `lime`, `mint`, `orange`, `pink`, `purple`, `slate`, `violet`. Defaults to `default`.",
        "name of the icon rendered before the label. When omitted, no icon is shown."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/staticchip/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticChip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-staticchip label=\"In progress\" color=\"cobalt\" icon-name=\"clock-bold\"></mdc-staticchip>\n<mdc-staticchip label=\"Done\" color=\"mint\"></mdc-staticchip>"
      }
    ],
    "reactImports": [
      "StaticChip"
    ],
    "constants": {
      "COLOR": {
        "DEFAULT": "default",
        "COBALT": "cobalt",
        "GOLD": "gold",
        "LIME": "lime",
        "MINT": "mint",
        "ORANGE": "orange",
        "PINK": "pink",
        "PURPLE": "purple",
        "SLATE": "slate",
        "VIOLET": "violet"
      },
      "DEFAULTS": {
        "COLOR": "default"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "chip",
      "component",
      "display",
      "for",
      "guidelines",
      "icon",
      "interactive",
      "label",
      "leading",
      "mdc",
      "metadata",
      "non",
      "optional",
      "or",
      "overview",
      "short",
      "static",
      "staticchip",
      "status",
      "text",
      "the",
      "to",
      "usage",
      "used",
      "with"
    ]
  },
  {
    "name": "staticradio",
    "title": "Static Radio",
    "tagName": "mdc-staticradio",
    "summary": "Usage, guidelines, and accessibility for the mdc-staticradio component — a decorative, non-interactive radio button for read-only or summary views.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/staticradio",
      "knowledgeBasePath": "components/src/components/staticradio/knowledge-base/staticradio.component.md"
    },
    "docs": "## Overview\n\nThe static radio is a decorative, non-interactive radio button used to display radio states in read-only contexts (summary views, confirmation screens, historical form data). It supports checked, disabled, soft-disabled, and read-only visual states but does not handle user interaction or participate in forms.\n\n### When to use\n\n- Use `mdc-staticradio` to render the visual shape of a radio button in read-only UIs such as summary screens, confirmation screens, or historical form data.\n- Use it inside lists or cards where the radio is part of the display, not a control.\n\n### When not to use\n\n- Use `mdc-radio` for any interactive radio in a form.\n- Do not use `mdc-staticradio` as a replacement for `mdc-radio` — it cannot be focused, toggled, or submitted with a form.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/staticradio/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticRadio } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-staticradio checked>Email</mdc-staticradio>\n<mdc-staticradio>SMS</mdc-staticradio>\n<mdc-staticradio disabled>Pager</mdc-staticradio>\n```\n\n### Property/Attribute details\n\n- `checked` — when `true`, renders the inner filled circle to indicate the checked state. Default `false`.\n- `disabled` — when `true`, applies the standard disabled appearance. Purely visual.\n- `soft-disabled` — when `true`, applies a less prominent disabled appearance. Purely visual.\n- `readonly` — when `true`, applies the read-only appearance. Purely visual.\n\n### Limitations\n\n- Not interactive: cannot be focused, toggled, or activated by the user.\n- Not form-associated: has no `name`/`value` and is not submitted with a form. Use `mdc-radio` (inside an `mdc-radiogroup`) when form participation is needed.\n- Has no group semantics — multiple `mdc-staticradio` elements do not enforce mutual exclusivity. Display the appropriate `checked` state directly on the elements.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders the slotted label content inside its light DOM but does not expose any radio semantics — it has no role, no `aria-checked`, and is not focusable. Screen readers will read any slotted label as plain text.\n\n### Notes\n\nBecause the component does not expose radio semantics, screen-reader users will not hear \"radio, selected\" / \"not selected\" announcements. When the checked state carries meaning, include that state in the surrounding text (e.g. write \"Email: selected\" instead of relying on the visual filled circle alone), or use the interactive `mdc-radio` inside an `mdc-radiogroup` instead.",
    "sections": {
      "Overview": "The static radio is a decorative, non-interactive radio button used to display radio states in read-only contexts (summary views, confirmation screens, historical form data). It supports checked, disabled, soft-disabled, and read-only visual states but does not handle user interaction or participate in forms.\n\n### When to use\n\n- Use `mdc-staticradio` to render the visual shape of a radio button in read-only UIs such as summary screens, confirmation screens, or historical form data.\n- Use it inside lists or cards where the radio is part of the display, not a control.\n\n### When not to use\n\n- Use `mdc-radio` for any interactive radio in a form.\n- Do not use `mdc-staticradio` as a replacement for `mdc-radio` — it cannot be focused, toggled, or submitted with a form.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/staticradio/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticRadio } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-staticradio checked>Email</mdc-staticradio>\n<mdc-staticradio>SMS</mdc-staticradio>\n<mdc-staticradio disabled>Pager</mdc-staticradio>\n```\n\n### Property/Attribute details\n\n- `checked` — when `true`, renders the inner filled circle to indicate the checked state. Default `false`.\n- `disabled` — when `true`, applies the standard disabled appearance. Purely visual.\n- `soft-disabled` — when `true`, applies a less prominent disabled appearance. Purely visual.\n- `readonly` — when `true`, applies the read-only appearance. Purely visual.\n\n### Limitations\n\n- Not interactive: cannot be focused, toggled, or activated by the user.\n- Not form-associated: has no `name`/`value` and is not submitted with a form. Use `mdc-radio` (inside an `mdc-radiogroup`) when form participation is needed.\n- Has no group semantics — multiple `mdc-staticradio` elements do not enforce mutual exclusivity. Display the appropriate `checked` state directly on the elements.",
      "Accessibility": "### Built-in features\n\nThe component renders the slotted label content inside its light DOM but does not expose any radio semantics — it has no role, no `aria-checked`, and is not focusable. Screen readers will read any slotted label as plain text.\n\n### Notes\n\nBecause the component does not expose radio semantics, screen-reader users will not hear \"radio, selected\" / \"not selected\" announcements. When the checked state carries meaning, include that state in the surrounding text (e.g. write \"Email: selected\" instead of relying on the visual filled circle alone), or use the interactive `mdc-radio` inside an `mdc-radiogroup` instead."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "description": "when `true`, renders the inner filled circle to indicate the checked state. Default `false`.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, applies the standard disabled appearance. Purely visual.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "description": "when `true`, applies the read-only appearance. Purely visual.",
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "description": "when `true`, applies a less prominent disabled appearance. Purely visual.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for the label of the radio."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "radio-icon",
          "description": "The radio icon element."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-staticradio-inner-circle-size",
          "description": "Size of the inner circle when checked."
        },
        {
          "name": "--mdc-staticradio-outer-circle-size",
          "description": "Size of the outer circle border."
        },
        {
          "name": "--mdc-staticradio-inner-circle-background-color",
          "description": "Background color of the inner circle when checked."
        },
        {
          "name": "--mdc-staticradio-outer-circle-border-color",
          "description": "Border color of the outer circle."
        },
        {
          "name": "--mdc-staticradio-outer-circle-background-color",
          "description": "Background color of the outer circle."
        }
      ],
      "rules": [
        "when `true`, renders the inner filled circle to indicate the checked state. Default `false`.",
        "when `true`, applies the standard disabled appearance. Purely visual.",
        "when `true`, applies a less prominent disabled appearance. Purely visual.",
        "when `true`, applies the read-only appearance. Purely visual."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/staticradio/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticRadio } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-staticradio checked>Email</mdc-staticradio>\n<mdc-staticradio>SMS</mdc-staticradio>\n<mdc-staticradio disabled>Pager</mdc-staticradio>"
      }
    ],
    "reactImports": [
      "StaticRadio"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "button",
      "component",
      "decorative",
      "for",
      "guidelines",
      "interactive",
      "mdc",
      "non",
      "only",
      "or",
      "overview",
      "radio",
      "read",
      "static",
      "staticradio",
      "summary",
      "the",
      "usage",
      "views"
    ]
  },
  {
    "name": "statictoggle",
    "title": "Static Toggle",
    "tagName": "mdc-statictoggle",
    "summary": "Usage, guidelines, and accessibility for the mdc-statictoggle component — a decorative non-interactive toggle switch used to display on/off state visually.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/statictoggle",
      "knowledgeBasePath": "components/src/components/statictoggle/knowledge-base/statictoggle.component.md"
    },
    "docs": "## Overview\n\nThe static toggle is a decorative, non-interactive toggle switch used for visual presentation. It shares the same styling as the interactive `mdc-toggle` but does not provide user interaction or form participation.\n\n### When to use\n\n- Use to display toggle state in read-only contexts such as summary views, confirmation screens, or list rows where state is reported but not editable.\n- Use as a visual building block inside custom interactive components (e.g. composed inside a list item that handles the click).\n\n### When not to use\n\n- Do not use as a replacement for an interactive toggle in forms — use `mdc-toggle` instead.\n- Do not use when users need to change the value directly on this control.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/statictoggle/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticToggle } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-statictoggle checked size=\"default\"></mdc-statictoggle>\n```\n\n### Property/Attribute details\n\n- `checked` (boolean, default `false`) — controls the visual state. `true` renders the \"on\" state with the check icon; `false` renders the \"off\" state.\n- `size` (`'default' | 'compact'`, default `'default'`) — `'default'` renders a 1.5rem-tall switch; `'compact'` renders a 1rem-tall switch with a smaller icon.\n- `readonly` (boolean, default `false`) — applies the read-only visual style. Decorative only.\n- `soft-disabled` (boolean, default `false`) — applies the soft-disabled visual style. Decorative only; the element stays in the visual flow but reads as inactive.\n- `disabled` (boolean) — applies the disabled visual style. Decorative only on this component.\n\n### Limitations\n\n- The component is non-interactive: it does not emit `click`/`change` events of its own, it is not focusable by default, and it is not form-associated. Wire interaction and form participation on the parent element that hosts the static toggle.\n\n## Accessibility\n\n### Notes\n\n- Because the component is purely decorative, it exposes no ARIA state on its own. When embedded inside an interactive parent, that parent is responsible for setting the appropriate role (e.g. `switch`, `checkbox`) and state attributes (`aria-checked`, `aria-disabled`, etc.) so assistive technologies announce the value correctly.\n- Do not rely on this component to convey state to assistive technologies on its own.",
    "sections": {
      "Overview": "The static toggle is a decorative, non-interactive toggle switch used for visual presentation. It shares the same styling as the interactive `mdc-toggle` but does not provide user interaction or form participation.\n\n### When to use\n\n- Use to display toggle state in read-only contexts such as summary views, confirmation screens, or list rows where state is reported but not editable.\n- Use as a visual building block inside custom interactive components (e.g. composed inside a list item that handles the click).\n\n### When not to use\n\n- Do not use as a replacement for an interactive toggle in forms — use `mdc-toggle` instead.\n- Do not use when users need to change the value directly on this control.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/statictoggle/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticToggle } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-statictoggle checked size=\"default\"></mdc-statictoggle>\n```\n\n### Property/Attribute details\n\n- `checked` (boolean, default `false`) — controls the visual state. `true` renders the \"on\" state with the check icon; `false` renders the \"off\" state.\n- `size` (`'default' | 'compact'`, default `'default'`) — `'default'` renders a 1.5rem-tall switch; `'compact'` renders a 1rem-tall switch with a smaller icon.\n- `readonly` (boolean, default `false`) — applies the read-only visual style. Decorative only.\n- `soft-disabled` (boolean, default `false`) — applies the soft-disabled visual style. Decorative only; the element stays in the visual flow but reads as inactive.\n- `disabled` (boolean) — applies the disabled visual style. Decorative only on this component.\n\n### Limitations\n\n- The component is non-interactive: it does not emit `click`/`change` events of its own, it is not focusable by default, and it is not form-associated. Wire interaction and form participation on the parent element that hosts the static toggle.",
      "Accessibility": "### Notes\n\n- Because the component is purely decorative, it exposes no ARIA state on its own. When embedded inside an interactive parent, that parent is responsible for setting the appropriate role (e.g. `switch`, `checkbox`) and state attributes (`aria-checked`, `aria-disabled`, etc.) so assistive technologies announce the value correctly.\n- Do not rely on this component to convey state to assistive technologies on its own."
    },
    "dependencies": [
      "mdc-icon"
    ],
    "api": {
      "properties": [
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "inline-radio",
          "default": "false",
          "values": [
            "default",
            "compact"
          ],
          "description": "/\nclass StaticToggle extends DisabledMixin(Component) {\n/**\nControls the visual state of the toggle.\nWhen true, the toggle appears in the \"on\" or \"active\" state.\nWhen false, the toggle appears in the \"off\" or \"inactive\" state.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for slotted content (typically used by parent `mdc-toggle` for the checkbox input)."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "slider",
          "description": "The slider part of the toggle."
        },
        {
          "name": "toggle-icon",
          "description": "The icon part of the toggle."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-statictoggle-width",
          "description": "Width of the static toggle."
        },
        {
          "name": "--mdc-statictoggle-height",
          "description": "Height of the static toggle."
        },
        {
          "name": "--mdc-statictoggle-border-radius",
          "description": "Border radius of the static toggle."
        },
        {
          "name": "--mdc-statictoggle-border-color",
          "description": "Border color of the static toggle."
        },
        {
          "name": "--mdc-statictoggle-background-color",
          "description": "Background color of the static toggle."
        },
        {
          "name": "--mdc-statictoggle-icon-color",
          "description": "Icon color of the static toggle."
        },
        {
          "name": "--mdc-statictoggle-icon-background-color",
          "description": "Icon background color of the static toggle."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/statictoggle/index.js\"; // custom element registration\n// or via React wrapper\nimport { StaticToggle } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-statictoggle checked size=\"default\"></mdc-statictoggle>"
      }
    ],
    "reactImports": [
      "StaticToggle"
    ],
    "constants": {
      "TOGGLE_SIZE": {
        "DEFAULT": "default",
        "COMPACT": "compact"
      },
      "ICON_NAME": {
        "CHECKED": "'check-bold' as Extract<IconNames, 'check-bold'>",
        "UNCHECKED": "'cancel-bold' as Extract<IconNames, 'cancel-bold'>"
      },
      "ICON_SIZE_IN_REM": {
        "compact": 0.75,
        "default": 1.25
      },
      "DEFAULTS": {
        "SIZE": "default"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "decorative",
      "display",
      "for",
      "guidelines",
      "icon",
      "interactive",
      "mdc",
      "non",
      "off",
      "on",
      "overview",
      "state",
      "static",
      "statictoggle",
      "switch",
      "the",
      "to",
      "toggle",
      "usage",
      "used",
      "visually"
    ]
  },
  {
    "name": "statusmessage",
    "title": "Status Message",
    "tagName": "mdc-statusmessage",
    "summary": "Usage, guidelines, and accessibility for the mdc-statusmessage component - an inline severity message with optional icon.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/statusmessage",
      "knowledgeBasePath": "components/src/components/statusmessage/knowledge-base/statusmessage.component.md"
    },
    "docs": "## Overview\n\nThe status message is a compact inline message used to communicate helper, validation, or status text with an optional severity icon. It is intentionally lightweight and does not behave like an alert or live region by default.\n\n### When to use\n\n- Use `mdc-statusmessage` for short inline messages that sit near a related control, setting, or content region.\n- Use a severity when the message needs a visual state such as error, warning, success, or priority.\n- Use the `icon` slot when a custom visual icon is required instead of the built-in severity icon.\n\n### When not to use\n\n- Use `mdc-banner`, `mdc-toast`, or `mdc-announcementdialog` when the message needs more prominence, actions, dismissal, or system-level announcement behaviour.\n- Use form components' `help-text` APIs for standard field helper or validation text; those components compose `mdc-statusmessage` internally.\n- Use plain `mdc-text` when no severity styling or icon treatment is needed.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/statusmessage/index.js\"; // custom element registration\n// or via React wrapper\nimport { StatusMessage } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-statusmessage severity=\"warning\" message=\"Connection is unstable\"></mdc-statusmessage>\n```\n\nUse the default slot when the message needs markup or should not be provided through an attribute:\n\n```html\n<mdc-statusmessage severity=\"error\">\n  Choose a password with at least 12 characters.\n</mdc-statusmessage>\n```\n\nUse the `icon` slot to override the built-in severity icon:\n\n```html\n<mdc-statusmessage severity=\"priority\" message=\"Review before publishing\">\n  <mdc-icon slot=\"icon\" name=\"info-badge-filled\"></mdc-icon>\n</mdc-statusmessage>\n```\n\n### Content guidance\n\n- Keep messages concise and actionable.\n- Prefer direct wording that tells the user what happened or what to do next.\n- Do not rely on color or icon alone; the text should carry the meaning of the severity.\n\n### Property/Attribute details\n\n- `severity` — controls color and the default icon. Supported values are `default`, `error`, `warning`, `success`, and `priority`. `default` renders no icon.\n- `message` — text fallback rendered when no meaningful default slot content is provided.\n- Default slot — message content. Meaningful slotted content takes precedence over the `message` attribute.\n- `icon` slot — custom icon content. When provided, it replaces the built-in severity icon.\n\n### Limitations\n\n- The component does not set `role`, `aria-live`, or alert semantics automatically.\n- The component is not a container for long-form guidance; use concise text that can sit inline with surrounding UI.\n\n## Accessibility\n\n### Built-in features\n\nThe component renders visible message text and an optional icon, but does not add live-region or alert semantics on its own. This keeps it reusable for static helper text and non-urgent inline status messages.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-statusmessage`) | `role` | None set by the component |\n| Host (`mdc-statusmessage`) | `aria-live` | None set by the component |\n\n### Implementation requirements\n\n#### General\n\n- Add `role`, `aria-live`, or `aria-describedby` on the consuming surface when the message needs to be announced or associated with another control.\n- For form fields, prefer the field component's helper-text API so the component can keep its own label and helper-text ARIA wiring consistent.\n- Treat icons as supporting visuals; the message text must communicate the actual state.\n\n#### Labelling\n\n- If a custom icon carries additional meaning beyond the visible message text, provide an accessible name on the icon or include that meaning in the message text.\n- If the icon is decorative, leave it unnamed and ensure the visible message is complete on its own.",
    "sections": {
      "Overview": "The status message is a compact inline message used to communicate helper, validation, or status text with an optional severity icon. It is intentionally lightweight and does not behave like an alert or live region by default.\n\n### When to use\n\n- Use `mdc-statusmessage` for short inline messages that sit near a related control, setting, or content region.\n- Use a severity when the message needs a visual state such as error, warning, success, or priority.\n- Use the `icon` slot when a custom visual icon is required instead of the built-in severity icon.\n\n### When not to use\n\n- Use `mdc-banner`, `mdc-toast`, or `mdc-announcementdialog` when the message needs more prominence, actions, dismissal, or system-level announcement behaviour.\n- Use form components' `help-text` APIs for standard field helper or validation text; those components compose `mdc-statusmessage` internally.\n- Use plain `mdc-text` when no severity styling or icon treatment is needed.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/statusmessage/index.js\"; // custom element registration\n// or via React wrapper\nimport { StatusMessage } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-statusmessage severity=\"warning\" message=\"Connection is unstable\"></mdc-statusmessage>\n```\n\nUse the default slot when the message needs markup or should not be provided through an attribute:\n\n```html\n<mdc-statusmessage severity=\"error\">\n  Choose a password with at least 12 characters.\n</mdc-statusmessage>\n```\n\nUse the `icon` slot to override the built-in severity icon:\n\n```html\n<mdc-statusmessage severity=\"priority\" message=\"Review before publishing\">\n  <mdc-icon slot=\"icon\" name=\"info-badge-filled\"></mdc-icon>\n</mdc-statusmessage>\n```\n\n### Content guidance\n\n- Keep messages concise and actionable.\n- Prefer direct wording that tells the user what happened or what to do next.\n- Do not rely on color or icon alone; the text should carry the meaning of the severity.\n\n### Property/Attribute details\n\n- `severity` — controls color and the default icon. Supported values are `default`, `error`, `warning`, `success`, and `priority`. `default` renders no icon.\n- `message` — text fallback rendered when no meaningful default slot content is provided.\n- Default slot — message content. Meaningful slotted content takes precedence over the `message` attribute.\n- `icon` slot — custom icon content. When provided, it replaces the built-in severity icon.\n\n### Limitations\n\n- The component does not set `role`, `aria-live`, or alert semantics automatically.\n- The component is not a container for long-form guidance; use concise text that can sit inline with surrounding UI.",
      "Accessibility": "### Built-in features\n\nThe component renders visible message text and an optional icon, but does not add live-region or alert semantics on its own. This keeps it reusable for static helper text and non-urgent inline status messages.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-statusmessage`) | `role` | None set by the component |\n| Host (`mdc-statusmessage`) | `aria-live` | None set by the component |\n\n### Implementation requirements\n\n#### General\n\n- Add `role`, `aria-live`, or `aria-describedby` on the consuming surface when the message needs to be announced or associated with another control.\n- For form fields, prefer the field component's helper-text API so the component can keep its own label and helper-text ARIA wiring consistent.\n- Treat icons as supporting visuals; the message text must communicate the actual state.\n\n#### Labelling\n\n- If a custom icon carries additional meaning beyond the visible message text, provide an accessible name on the icon or include that meaning in the message text.\n- If the icon is decorative, leave it unnamed and ensure the visible message is complete on its own."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "message",
          "attribute": "message",
          "type": "text",
          "values": [],
          "description": "text fallback rendered when no meaningful default slot content is provided.",
          "source": "storybook"
        },
        {
          "name": "severity",
          "attribute": "severity",
          "type": "select",
          "default": "default",
          "values": [
            "default",
            "error",
            "priority",
            "success",
            "warning"
          ],
          "description": "controls color and the default icon. Supported values are `default`, `error`, `warning`, `success`, and `priority`. `default` renders no icon.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Message content. Slotted content takes precedence over the `message` attribute."
        },
        {
          "name": "icon",
          "description": "Custom icon content. Overrides the default severity icon."
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "container",
          "description": "The container for the icon and message text."
        },
        {
          "name": "icon",
          "description": "The default severity icon slot or rendered icon."
        },
        {
          "name": "text",
          "description": "The rendered message text when using the `message` attribute."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-statusmessage-color",
          "description": "Color for the icon and message text."
        },
        {
          "name": "--mdc-statusmessage-font-size",
          "description": "Font size for the message text."
        },
        {
          "name": "--mdc-statusmessage-font-weight",
          "description": "Font weight for the message text."
        },
        {
          "name": "--mdc-statusmessage-line-height",
          "description": "Line height for the message text."
        },
        {
          "name": "--mdc-statusmessage-gap",
          "description": "Gap between the icon and message text."
        },
        {
          "name": "--mdc-statusmessage-icon-size",
          "description": "Width and height of the default severity icon."
        }
      ],
      "rules": [
        "controls color and the default icon. Supported values are `default`, `error`, `warning`, `success`, and `priority`. `default` renders no icon.",
        "text fallback rendered when no meaningful default slot content is provided."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/statusmessage/index.js\"; // custom element registration\n// or via React wrapper\nimport { StatusMessage } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-statusmessage severity=\"warning\" message=\"Connection is unstable\"></mdc-statusmessage>"
      },
      {
        "language": "html",
        "code": "<mdc-statusmessage severity=\"error\">\n  Choose a password with at least 12 characters.\n</mdc-statusmessage>"
      },
      {
        "language": "html",
        "code": "<mdc-statusmessage severity=\"priority\" message=\"Review before publishing\">\n  <mdc-icon slot=\"icon\" name=\"info-badge-filled\"></mdc-icon>\n</mdc-statusmessage>"
      }
    ],
    "reactImports": [
      "StatusMessage"
    ],
    "constants": {
      "STATUSMESSAGE_SEVERITY": {
        "DEFAULT": "default",
        "ERROR": "error",
        "PRIORITY": "priority",
        "SUCCESS": "success",
        "WARNING": "warning"
      },
      "STATUSMESSAGE_ICON_NAMES": {
        "ERROR": "'error-legacy-badge-filled' as Extract<IconNames, 'error-legacy-badge-filled'>",
        "WARNING": "'warning-badge-filled' as Extract<IconNames, 'warning-badge-filled'>",
        "SUCCESS": "'check-circle-badge-filled' as Extract<IconNames, 'check-circle-badge-filled'>",
        "PRIORITY": "'priority-badge-filled' as Extract<IconNames, 'priority-badge-filled'>"
      },
      "STATUSMESSAGE_ICON_NAME_BY_SEVERITY": {},
      "DEFAULTS": {
        "SEVERITY": "default"
      }
    },
    "keywords": [
      "accessibility",
      "an",
      "and",
      "component",
      "for",
      "guidelines",
      "icon",
      "inline",
      "mdc",
      "message",
      "optional",
      "overview",
      "severity",
      "status",
      "statusmessage",
      "text",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "stepper",
    "title": "Stepper",
    "tagName": "mdc-stepper",
    "summary": "Usage, guidelines, and accessibility for the mdc-stepper component — the container that orchestrates a sequence of steps and their connectors.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/stepper",
      "knowledgeBasePath": "components/src/components/stepper/knowledge-base/stepper.component.md"
    },
    "docs": "## Overview\n\nThe stepper is the container component that arranges a sequence of `mdc-stepperitem` elements interleaved with `mdc-stepperconnector` elements. It provides the shared orientation and variant context that the items and connectors read so they render consistently with the group.\n\n### When to use\n\n- Use to communicate progress through a multi-step linear process (e.g. an onboarding flow, a checkout, a wizard).\n- Use horizontally for top-of-page progress indicators and vertically when the steps need richer help text alongside each entry.\n\n### When not to use\n\n- Do not use for non-sequential navigation between unrelated views — use `mdc-tablist` or another navigation pattern instead.\n- Do not use when there is only one step.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/stepper/index.js\"; // custom element registration\nimport \"@momentum-design/components/dist/components/stepperitem/index.js\";\nimport \"@momentum-design/components/dist/components/stepperconnector/index.js\";\n// or via React wrapper\nimport { Stepper, StepperItem, StepperConnector } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-stepper orientation=\"horizontal\" variant=\"inline\">\n  <mdc-stepperitem status=\"completed\" step-number=\"1\" label=\"Account\" aria-label=\"Step 1, Account, completed\"></mdc-stepperitem>\n  <mdc-stepperconnector status=\"complete\"></mdc-stepperconnector>\n  <mdc-stepperitem status=\"current\" step-number=\"2\" label=\"Profile\" aria-current=\"step\" aria-label=\"Step 2, Profile, current\"></mdc-stepperitem>\n  <mdc-stepperconnector></mdc-stepperconnector>\n  <mdc-stepperitem status=\"not-started\" step-number=\"3\" label=\"Review\" aria-label=\"Step 3, Review, not started\"></mdc-stepperitem>\n</mdc-stepper>\n```\n\n### Property/Attribute details\n\n- `orientation` (`'horizontal' | 'vertical'`, default `'horizontal'`) — propagates to every child `mdc-stepperitem` and `mdc-stepperconnector` through the stepper context, so children automatically follow the group's layout.\n- `variant` (`'inline' | 'stacked'`, default `'inline'`) — propagates to every child `mdc-stepperitem` through the stepper context, controlling whether label and help text sit beside or below the status indicator.\n\n## Accessibility\n\n### Built-in features\n\n- The container element exposes `role=\"list\"` so assistive technologies announce the steps as a grouped sequence.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host element | `role` | `list` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a descriptive label on the host (for example via `aria-label`) so the list is announced with a meaningful name such as \"Checkout steps\".\n- Set `aria-current=\"step\"` on the single `mdc-stepperitem` representing the current step. Remove it from all other steps so only one item is announced as current at a time.\n\n### Notes\n\n- The stepper does not move focus between steps automatically. Manage focus and selection on the consumer side when steps are interactive.",
    "sections": {
      "Overview": "The stepper is the container component that arranges a sequence of `mdc-stepperitem` elements interleaved with `mdc-stepperconnector` elements. It provides the shared orientation and variant context that the items and connectors read so they render consistently with the group.\n\n### When to use\n\n- Use to communicate progress through a multi-step linear process (e.g. an onboarding flow, a checkout, a wizard).\n- Use horizontally for top-of-page progress indicators and vertically when the steps need richer help text alongside each entry.\n\n### When not to use\n\n- Do not use for non-sequential navigation between unrelated views — use `mdc-tablist` or another navigation pattern instead.\n- Do not use when there is only one step.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/stepper/index.js\"; // custom element registration\nimport \"@momentum-design/components/dist/components/stepperitem/index.js\";\nimport \"@momentum-design/components/dist/components/stepperconnector/index.js\";\n// or via React wrapper\nimport { Stepper, StepperItem, StepperConnector } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-stepper orientation=\"horizontal\" variant=\"inline\">\n  <mdc-stepperitem status=\"completed\" step-number=\"1\" label=\"Account\" aria-label=\"Step 1, Account, completed\"></mdc-stepperitem>\n  <mdc-stepperconnector status=\"complete\"></mdc-stepperconnector>\n  <mdc-stepperitem status=\"current\" step-number=\"2\" label=\"Profile\" aria-current=\"step\" aria-label=\"Step 2, Profile, current\"></mdc-stepperitem>\n  <mdc-stepperconnector></mdc-stepperconnector>\n  <mdc-stepperitem status=\"not-started\" step-number=\"3\" label=\"Review\" aria-label=\"Step 3, Review, not started\"></mdc-stepperitem>\n</mdc-stepper>\n```\n\n### Property/Attribute details\n\n- `orientation` (`'horizontal' | 'vertical'`, default `'horizontal'`) — propagates to every child `mdc-stepperitem` and `mdc-stepperconnector` through the stepper context, so children automatically follow the group's layout.\n- `variant` (`'inline' | 'stacked'`, default `'inline'`) — propagates to every child `mdc-stepperitem` through the stepper context, controlling whether label and help text sit beside or below the status indicator.",
      "Accessibility": "### Built-in features\n\n- The container element exposes `role=\"list\"` so assistive technologies announce the steps as a grouped sequence.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host element | `role` | `list` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Provide a descriptive label on the host (for example via `aria-label`) so the list is announced with a meaningful name such as \"Checkout steps\".\n- Set `aria-current=\"step\"` on the single `mdc-stepperitem` representing the current step. Remove it from all other steps so only one item is announced as current at a time.\n\n### Notes\n\n- The stepper does not move focus between steps automatically. Manage focus and selection on the consumer side when steps are interactive."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "radio",
          "default": "\"horizontal\"",
          "values": [],
          "description": "/\nclass Stepper extends Provider<StepperContext> {\n/**\nThe orientation of the stepperconnector (vertical or horizontal)",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "radio",
          "default": "'inline'",
          "values": [],
          "description": "The variant of the stepper item, which can be `inline` or `stacked`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Pass the list of `mdc-stepperitem` and `mdc-stepperconnector` elements to be rendered inside the stepper."
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/stepper/index.js\"; // custom element registration\nimport \"@momentum-design/components/dist/components/stepperitem/index.js\";\nimport \"@momentum-design/components/dist/components/stepperconnector/index.js\";\n// or via React wrapper\nimport { Stepper, StepperItem, StepperConnector } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-stepper orientation=\"horizontal\" variant=\"inline\">\n  <mdc-stepperitem status=\"completed\" step-number=\"1\" label=\"Account\" aria-label=\"Step 1, Account, completed\"></mdc-stepperitem>\n  <mdc-stepperconnector status=\"complete\"></mdc-stepperconnector>\n  <mdc-stepperitem status=\"current\" step-number=\"2\" label=\"Profile\" aria-current=\"step\" aria-label=\"Step 2, Profile, current\"></mdc-stepperitem>\n  <mdc-stepperconnector></mdc-stepperconnector>\n  <mdc-stepperitem status=\"not-started\" step-number=\"3\" label=\"Review\" aria-label=\"Step 3, Review, not started\"></mdc-stepperitem>\n</mdc-stepper>"
      }
    ],
    "reactImports": [
      "Stepper",
      "StepperItem",
      "StepperConnector"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "connectors",
      "container",
      "for",
      "guidelines",
      "mdc",
      "of",
      "orchestrates",
      "overview",
      "sequence",
      "stepper",
      "steps",
      "that",
      "the",
      "their",
      "usage"
    ]
  },
  {
    "name": "stepperconnector",
    "title": "Stepper Connector",
    "tagName": "mdc-stepperconnector",
    "summary": "Usage, guidelines, and accessibility for the mdc-stepperconnector component — the visual line drawn between two stepper items.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/stepperconnector",
      "knowledgeBasePath": "components/src/components/stepperconnector/knowledge-base/stepperconnector.component.md"
    },
    "docs": "## Overview\n\nThe stepper connector is the decorative line drawn between two `mdc-stepperitem` elements inside an `mdc-stepper`. Its colour reflects whether the connection between the two steps is complete or incomplete.\n\n### When to use\n\n- Place between every pair of adjacent `mdc-stepperitem` elements so the visual progress chain is unbroken.\n\n### When not to use\n\n- Do not use outside of an `mdc-stepper` — without the parent context the orientation will not follow the group.\n- Do not use as a generic divider; use a divider component instead.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/stepperconnector/index.js\"; // custom element registration\n// or via React wrapper\nimport { StepperConnector } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-stepper orientation=\"horizontal\">\n  <mdc-stepperitem status=\"completed\" step-number=\"1\" label=\"One\"></mdc-stepperitem>\n  <mdc-stepperconnector status=\"complete\"></mdc-stepperconnector>\n  <mdc-stepperitem status=\"current\" step-number=\"2\" label=\"Two\" aria-current=\"step\"></mdc-stepperitem>\n</mdc-stepper>\n```\n\n### Property/Attribute details\n\n- `status` (`'complete' | 'incomplete'`, default `'incomplete'`) — `'complete'` renders the connector with the completed colour, indicating the user has passed this transition; `'incomplete'` renders the muted colour.\n- `orientation` (`'horizontal' | 'vertical'`, default `'horizontal'`) — sets the connector direction. When the connector is placed inside an `mdc-stepper`, the parent's `orientation` is read from context and overrides any locally set value on every update.\n\n## Accessibility\n\n### Notes\n\n- The connector is purely decorative and does not expose ARIA state. Convey progress through the `status` and `aria-current` attributes on the surrounding `mdc-stepperitem` elements.",
    "sections": {
      "Overview": "The stepper connector is the decorative line drawn between two `mdc-stepperitem` elements inside an `mdc-stepper`. Its colour reflects whether the connection between the two steps is complete or incomplete.\n\n### When to use\n\n- Place between every pair of adjacent `mdc-stepperitem` elements so the visual progress chain is unbroken.\n\n### When not to use\n\n- Do not use outside of an `mdc-stepper` — without the parent context the orientation will not follow the group.\n- Do not use as a generic divider; use a divider component instead.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/stepperconnector/index.js\"; // custom element registration\n// or via React wrapper\nimport { StepperConnector } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-stepper orientation=\"horizontal\">\n  <mdc-stepperitem status=\"completed\" step-number=\"1\" label=\"One\"></mdc-stepperitem>\n  <mdc-stepperconnector status=\"complete\"></mdc-stepperconnector>\n  <mdc-stepperitem status=\"current\" step-number=\"2\" label=\"Two\" aria-current=\"step\"></mdc-stepperitem>\n</mdc-stepper>\n```\n\n### Property/Attribute details\n\n- `status` (`'complete' | 'incomplete'`, default `'incomplete'`) — `'complete'` renders the connector with the completed colour, indicating the user has passed this transition; `'incomplete'` renders the muted colour.\n- `orientation` (`'horizontal' | 'vertical'`, default `'horizontal'`) — sets the connector direction. When the connector is placed inside an `mdc-stepper`, the parent's `orientation` is read from context and overrides any locally set value on every update.",
      "Accessibility": "### Notes\n\n- The connector is purely decorative and does not expose ARIA state. Convey progress through the `status` and `aria-current` attributes on the surrounding `mdc-stepperitem` elements."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "orientation",
          "attribute": "orientation",
          "type": "radio",
          "default": "\"horizontal\"",
          "values": [
            "vertical",
            "horizontal"
          ],
          "description": "The orientation of the connector (vertical or horizontal)",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "status",
          "attribute": "status",
          "type": "radio",
          "default": "\"incomplete\"",
          "values": [
            "complete",
            "incomplete"
          ],
          "description": "/\nclass StepperConnector extends Component {\n/**\nThe status of the connector (complete or incomplete)",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [],
      "cssParts": [
        {
          "name": "connector",
          "description": "The main connector line between steps"
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-stepperconnector-complete-background",
          "description": "Background color for the complete connector"
        },
        {
          "name": "--mdc-stepperconnector-incomplete-background",
          "description": "Background color for the incomplete connector"
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/stepperconnector/index.js\"; // custom element registration\n// or via React wrapper\nimport { StepperConnector } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-stepper orientation=\"horizontal\">\n  <mdc-stepperitem status=\"completed\" step-number=\"1\" label=\"One\"></mdc-stepperitem>\n  <mdc-stepperconnector status=\"complete\"></mdc-stepperconnector>\n  <mdc-stepperitem status=\"current\" step-number=\"2\" label=\"Two\" aria-current=\"step\"></mdc-stepperitem>\n</mdc-stepper>"
      }
    ],
    "reactImports": [
      "StepperConnector"
    ],
    "constants": {
      "STATUS": {
        "COMPLETE": "complete",
        "INCOMPLETE": "incomplete"
      },
      "ORIENTATION": {
        "VERTICAL": "vertical",
        "HORIZONTAL": "horizontal"
      },
      "DEFAULTS": {
        "STATUS": "incomplete",
        "ORIENTATION": "horizontal"
      }
    },
    "keywords": [
      "accessibility",
      "and",
      "between",
      "component",
      "connector",
      "drawn",
      "for",
      "guidelines",
      "items",
      "line",
      "mdc",
      "overview",
      "stepper",
      "stepperconnector",
      "the",
      "two",
      "usage",
      "visual"
    ]
  },
  {
    "name": "stepperitem",
    "title": "Stepper Item",
    "tagName": "mdc-stepperitem",
    "summary": "Usage, guidelines, and accessibility for the mdc-stepperitem component — a single step inside an mdc-stepper sequence.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/stepperitem",
      "knowledgeBasePath": "components/src/components/stepperitem/knowledge-base/stepperitem.component.md"
    },
    "docs": "## Overview\n\nThe stepper item represents a single step inside an `mdc-stepper`. It renders a status indicator (completed checkmark, current pencil, error icon, or step number), a label, and an optional help text line. The component is uncontrolled — consumers drive the `status` of each step.\n\n### When to use\n\n- Use one stepper item per step in a sequenced process, placed inside an `mdc-stepper` with `mdc-stepperconnector` elements between adjacent items.\n\n### When not to use\n\n- Do not use as a standalone clickable card; for selectable cards use `mdc-cardbutton` or similar.\n- Do not use outside of `mdc-stepper`, since orientation and variant come from the parent context.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/stepperitem/index.js\"; // custom element registration\n// or via React wrapper\nimport { StepperItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-stepperitem\n  status=\"current\"\n  step-number=\"2\"\n  label=\"Profile\"\n  help-text=\"Tell us about yourself\"\n  aria-current=\"step\"\n  aria-label=\"Step 2 of 3, Profile, current\"\n></mdc-stepperitem>\n```\n\n### Property/Attribute details\n\n- `status` (`'completed' | 'current' | 'not-started' | 'error-current' | 'error-incomplete'`, default `'not-started'`):\n  - `completed` renders the check icon.\n  - `current` and `error-current` render the pencil icon.\n  - `not-started` and `error-incomplete` render the `step-number` value (if provided) inside the status badge.\n  - The error variants also prepend an error icon next to the help text.\n- `variant` (`'inline' | 'stacked'`, default `'inline'`) — placed inside `mdc-stepper`, this value is overwritten by the parent context on every update.\n- `label` (string, default `''`) — main step label rendered next to (or below) the status indicator.\n- `help-text` (string) — optional supporting text rendered beneath the label.\n- `step-number` (number) — numeric indicator displayed inside the status badge for `not-started` and `error-incomplete` statuses.\n- `tabindex` (number, default `0`) — the host is focusable by default. Adjust if the consumer manages a different focus strategy.\n\nThe component dispatches the bubbling `click`, `keydown`, and `keyup` events. Pressing Enter dispatches a synthetic `click`, releasing Space dispatches a synthetic `click`; both keys apply the `pressed` class while held to reflect the visual press state, and both call `preventDefault()` to avoid scrolling or double-firing.\n\n## Accessibility\n\n### Built-in features\n\n- The host element exposes `role=\"listitem\"` so it nests semantically inside the parent stepper list.\n- The component reflects `tabindex` so each step participates in the tab order; consumers can override per item to implement roving tabindex.\n- Enter and Space activate the step just like a native button.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host element | `role` | `listitem` |\n| Host element | `tabindex` | `0` (default; reflects the `tabindex` property) |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always set `aria-label` on each stepper item with the full context (position, label, status). The visible label alone is not enough for screen reader users to understand where they are in the sequence.\n- Set `aria-current=\"step\"` on the single item representing the current step (only valid when `status` is `current` or `error-current`). Remove it from every other item.\n\n### Notes\n\n- Without an explicit `aria-label`, screen readers announce only the visible label text, which omits the step number, total count, and status. Always provide an `aria-label`.\n- Only one item in a stepper may carry `aria-current=\"step\"` at a time.",
    "sections": {
      "Overview": "The stepper item represents a single step inside an `mdc-stepper`. It renders a status indicator (completed checkmark, current pencil, error icon, or step number), a label, and an optional help text line. The component is uncontrolled — consumers drive the `status` of each step.\n\n### When to use\n\n- Use one stepper item per step in a sequenced process, placed inside an `mdc-stepper` with `mdc-stepperconnector` elements between adjacent items.\n\n### When not to use\n\n- Do not use as a standalone clickable card; for selectable cards use `mdc-cardbutton` or similar.\n- Do not use outside of `mdc-stepper`, since orientation and variant come from the parent context.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/stepperitem/index.js\"; // custom element registration\n// or via React wrapper\nimport { StepperItem } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-stepperitem\n  status=\"current\"\n  step-number=\"2\"\n  label=\"Profile\"\n  help-text=\"Tell us about yourself\"\n  aria-current=\"step\"\n  aria-label=\"Step 2 of 3, Profile, current\"\n></mdc-stepperitem>\n```\n\n### Property/Attribute details\n\n- `status` (`'completed' | 'current' | 'not-started' | 'error-current' | 'error-incomplete'`, default `'not-started'`):\n  - `completed` renders the check icon.\n  - `current` and `error-current` render the pencil icon.\n  - `not-started` and `error-incomplete` render the `step-number` value (if provided) inside the status badge.\n  - The error variants also prepend an error icon next to the help text.\n- `variant` (`'inline' | 'stacked'`, default `'inline'`) — placed inside `mdc-stepper`, this value is overwritten by the parent context on every update.\n- `label` (string, default `''`) — main step label rendered next to (or below) the status indicator.\n- `help-text` (string) — optional supporting text rendered beneath the label.\n- `step-number` (number) — numeric indicator displayed inside the status badge for `not-started` and `error-incomplete` statuses.\n- `tabindex` (number, default `0`) — the host is focusable by default. Adjust if the consumer manages a different focus strategy.\n\nThe component dispatches the bubbling `click`, `keydown`, and `keyup` events. Pressing Enter dispatches a synthetic `click`, releasing Space dispatches a synthetic `click`; both keys apply the `pressed` class while held to reflect the visual press state, and both call `preventDefault()` to avoid scrolling or double-firing.",
      "Accessibility": "### Built-in features\n\n- The host element exposes `role=\"listitem\"` so it nests semantically inside the parent stepper list.\n- The component reflects `tabindex` so each step participates in the tab order; consumers can override per item to implement roving tabindex.\n- Enter and Space activate the step just like a native button.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host element | `role` | `listitem` |\n| Host element | `tabindex` | `0` (default; reflects the `tabindex` property) |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always set `aria-label` on each stepper item with the full context (position, label, status). The visible label alone is not enough for screen reader users to understand where they are in the sequence.\n- Set `aria-current=\"step\"` on the single item representing the current step (only valid when `status` is `current` or `error-current`). Remove it from every other item.\n\n### Notes\n\n- Without an explicit `aria-label`, screen readers announce only the visible label text, which omits the step number, total count, and status. Always provide an `aria-label`.\n- Only one item in a stepper may carry `aria-current=\"step\"` at a time."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "unknown",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "unknown",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "unknown",
          "default": "''",
          "values": [],
          "description": "The label for the stepper item, which is displayed as the main text of the step.\nThis label is typically used to describe the step or action that the step represents.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "status",
          "attribute": "status",
          "type": "unknown",
          "default": "'not-started'",
          "values": [
            "completed",
            "current",
            "error-current",
            "error-incomplete",
            "not-started"
          ],
          "description": "The status of the stepper item, which can be `completed`, `current`, `not-started`, `error-current`, or `error-incomplete`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "stepNumber",
          "attribute": "step-number",
          "type": "unknown",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "unknown",
          "default": "'inline'",
          "values": [
            "inline",
            "stacked"
          ],
          "description": "/\nclass StepperItem extends KeyDownHandledMixin(KeyToActionMixin(TabIndexMixin(Component))) {\n/**\nThe variant of the stepper item, which can be `inline` or `stacked`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the stepperitem is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the stepperitem."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the stepperitem."
        }
      ],
      "cssParts": [
        {
          "name": "status-container",
          "description": "The container for the status icon or step number."
        },
        {
          "name": "label-container",
          "description": "The container for the label and help text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the help text and error icon when applicable."
        },
        {
          "name": "status-icon",
          "description": "The icon representing the status of the stepper item."
        },
        {
          "name": "step-number",
          "description": "The text representing the step number."
        },
        {
          "name": "label",
          "description": "The text representing the label of the stepper item."
        },
        {
          "name": "help-text",
          "description": "The text providing additional information about the stepper item."
        },
        {
          "name": "help-icon",
          "description": "The icon representing an error in the stepper item."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-stepperitem-status-container-background",
          "description": "The background color of the status container."
        },
        {
          "name": "--mdc-stepperitem-status-container-border-color",
          "description": "The border color of the status container."
        },
        {
          "name": "--mdc-stepperitem-label-color",
          "description": "The color of the label text."
        },
        {
          "name": "--mdc-stepperitem-help-text-color",
          "description": "The color of the optional label text."
        },
        {
          "name": "--mdc-stepperitem-label-container-background",
          "description": "The background color of the label container."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/stepperitem/index.js\"; // custom element registration\n// or via React wrapper\nimport { StepperItem } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-stepperitem\n  status=\"current\"\n  step-number=\"2\"\n  label=\"Profile\"\n  help-text=\"Tell us about yourself\"\n  aria-current=\"step\"\n  aria-label=\"Step 2 of 3, Profile, current\"\n></mdc-stepperitem>"
      }
    ],
    "reactImports": [
      "StepperItem"
    ],
    "constants": {
      "VARIANT": {
        "INLINE": "inline",
        "STACKED": "stacked"
      },
      "STATUS": {
        "COMPLETED": "completed",
        "CURRENT": "current",
        "ERROR_CURRENT": "error-current",
        "ERROR_INCOMPLETE": "error-incomplete",
        "NOT_STARTED": "not-started"
      },
      "STATUS_ICON": {
        "COMPLETED": "'check-bold' as Extract<IconNames, 'check-bold'>",
        "ERROR": "'error-legacy-badge-filled' as Extract<IconNames, 'error-legacy-badge-filled'>",
        "PENCIL": "'edit-bold' as Extract<IconNames, 'edit-bold'>"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "an",
      "and",
      "component",
      "for",
      "guidelines",
      "icon",
      "inside",
      "item",
      "mdc",
      "overview",
      "sequence",
      "single",
      "step",
      "stepper",
      "stepperitem",
      "text",
      "the",
      "usage"
    ]
  },
  {
    "name": "tab",
    "title": "Tab",
    "tagName": "mdc-tab",
    "summary": "Usage, guidelines, and accessibility for the mdc-tab component — an individual tab control used inside mdc-tablist.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/tab",
      "knowledgeBasePath": "components/src/components/tab/knowledge-base/tab.component.md"
    },
    "docs": "## Overview\n\nThe tab is the individual control rendered inside an `mdc-tablist`. It displays an optional icon, a text label, and optional postfix content (badge or chip), and reflects its active state to assistive technologies.\n\n### When to use\n\n- Use inside an `mdc-tablist` to represent one panel of content the user can switch to.\n- Use to provide a compact, single-line label for content panels related to one another.\n\n### When not to use\n\n- Do not use tabs to navigate between unrelated pages — use a navigation pattern.\n- Do not use a tab outside of an `mdc-tablist` if the tabs pattern semantics are needed; the list owns roving focus and selection wiring.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/tab/index.js\"; // custom element registration\n// or via React wrapper\nimport { Tab } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-tablist data-aria-label=\"Account sections\">\n  <mdc-tab tab-id=\"overview\" text=\"Overview\" icon-name=\"info-circle-regular\" active></mdc-tab>\n  <mdc-tab tab-id=\"activity\" text=\"Activity\" icon-name=\"activity-regular\">\n    <mdc-badge slot=\"postfix\" type=\"dot\"></mdc-badge>\n  </mdc-tab>\n</mdc-tablist>\n```\n\n### Property/Attribute details\n\n- `tab-id` (string) — unique identifier used by `mdc-tablist` to track selection. Required and must be unique within the list; the parent list reports an error otherwise.\n- `text` (string) — label rendered inside the tab. When omitted, the tab renders only the icon and the consumer must supply an `aria-label`.\n- `icon-name` (string) — name of the regular icon to render in the prefix area. The component automatically renders the matching `-filled` icon and swaps to it when the tab becomes active.\n- `variant` (`'glass' | 'line' | 'pill'`, default `'pill'`) — controls the background and indicator styling. Unknown values fall back to `'pill'`.\n- `size` (`32 | 28`, default `32`) — `32` is 2rem; `28` is the compact 1.75rem variant. Unknown values fall back to `32`.\n- `active` (boolean, default `false`) — toggles the visual active state. The component automatically reflects this to `aria-selected` on the host (see Accessibility).\n- `disabled` (boolean) — when set, removes the tab from the tab order and marks it as `aria-disabled`. `mdc-tablist` skips disabled tabs during roving-tabindex navigation.\n- `soft-disabled` (boolean) — visually disabled but still focusable and clickable, with `aria-disabled=\"true\"`. Consumers must ensure the tab does not perform any action while soft-disabled.\n\nThe component dispatches:\n\n- `click` / `keydown` / `keyup` / `focus` — native DOM events with React equivalents.\n- `activechange` — bubbling `CustomEvent` with `detail: { tabId, active }` fired when the active state changes. When the tab is used inside `mdc-tablist`, the list listens for this internally and consumers should listen for the list's `change` event instead.\n\nSlots:\n\n- `prefix` — content before the text; defaults to the icon rendered from `icon-name`.\n- `postfix` — content after the text, typically `mdc-badge` or `mdc-chip`. Do not use a badge and a chip in the same tab.\n\n## Accessibility\n\n### Built-in features\n\n- The host element exposes `role=\"tab\"`.\n- The active state is mirrored to `aria-selected` on the host (the `ariaStateKey` is set to `aria-selected`, so `active=\"true\"` writes `aria-selected=\"true\"`, `false` writes `aria-selected=\"false\"`, and `undefined` removes the attribute).\n- Enter activates the tab; Space activates on key-up. The host also listens for blur to clear the visual `pressed` state if the key is released outside the element.\n- The native form `type` attribute is cleared on connect so the tab does not submit forms when nested inside one.\n- When `disabled` is set, the tab is removed from the tab order, marked `aria-disabled=\"true\"`, and the parent list skips it during arrow-key navigation.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host element | `role` | `tab` |\n| Host element | `aria-selected` | `true` when `active`, `false` when not active, removed when `active` is `undefined` |\n| Host element | `aria-disabled` | `true` when `disabled` or `soft-disabled` |\n| Host element | `aria-pressed` | not used (overridden to `aria-selected`) |\n\n### Implementation requirements\n\n#### General\n\n- Wire each tab to its panel by setting `aria-controls` on the tab pointing at the panel element's `id`.\n- The corresponding content panel element must have `role=\"tabpanel\"` and `aria-labelledby` referencing the tab's `id`.\n- If a tab opens a popup menu, set `aria-haspopup` on it to either `menu` or `true`.\n\n#### Labelling\n\n- When `text` is omitted, provide an `aria-label` on the tab so screen reader users hear a meaningful name.\n\n### Notes\n\n- When the tab lives inside `mdc-tablist`, listen to the list's `change` event for selection changes rather than the tab's `activechange` event to avoid duplicate handlers.",
    "sections": {
      "Overview": "The tab is the individual control rendered inside an `mdc-tablist`. It displays an optional icon, a text label, and optional postfix content (badge or chip), and reflects its active state to assistive technologies.\n\n### When to use\n\n- Use inside an `mdc-tablist` to represent one panel of content the user can switch to.\n- Use to provide a compact, single-line label for content panels related to one another.\n\n### When not to use\n\n- Do not use tabs to navigate between unrelated pages — use a navigation pattern.\n- Do not use a tab outside of an `mdc-tablist` if the tabs pattern semantics are needed; the list owns roving focus and selection wiring.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/tab/index.js\"; // custom element registration\n// or via React wrapper\nimport { Tab } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-tablist data-aria-label=\"Account sections\">\n  <mdc-tab tab-id=\"overview\" text=\"Overview\" icon-name=\"info-circle-regular\" active></mdc-tab>\n  <mdc-tab tab-id=\"activity\" text=\"Activity\" icon-name=\"activity-regular\">\n    <mdc-badge slot=\"postfix\" type=\"dot\"></mdc-badge>\n  </mdc-tab>\n</mdc-tablist>\n```\n\n### Property/Attribute details\n\n- `tab-id` (string) — unique identifier used by `mdc-tablist` to track selection. Required and must be unique within the list; the parent list reports an error otherwise.\n- `text` (string) — label rendered inside the tab. When omitted, the tab renders only the icon and the consumer must supply an `aria-label`.\n- `icon-name` (string) — name of the regular icon to render in the prefix area. The component automatically renders the matching `-filled` icon and swaps to it when the tab becomes active.\n- `variant` (`'glass' | 'line' | 'pill'`, default `'pill'`) — controls the background and indicator styling. Unknown values fall back to `'pill'`.\n- `size` (`32 | 28`, default `32`) — `32` is 2rem; `28` is the compact 1.75rem variant. Unknown values fall back to `32`.\n- `active` (boolean, default `false`) — toggles the visual active state. The component automatically reflects this to `aria-selected` on the host (see Accessibility).\n- `disabled` (boolean) — when set, removes the tab from the tab order and marks it as `aria-disabled`. `mdc-tablist` skips disabled tabs during roving-tabindex navigation.\n- `soft-disabled` (boolean) — visually disabled but still focusable and clickable, with `aria-disabled=\"true\"`. Consumers must ensure the tab does not perform any action while soft-disabled.\n\nThe component dispatches:\n\n- `click` / `keydown` / `keyup` / `focus` — native DOM events with React equivalents.\n- `activechange` — bubbling `CustomEvent` with `detail: { tabId, active }` fired when the active state changes. When the tab is used inside `mdc-tablist`, the list listens for this internally and consumers should listen for the list's `change` event instead.\n\nSlots:\n\n- `prefix` — content before the text; defaults to the icon rendered from `icon-name`.\n- `postfix` — content after the text, typically `mdc-badge` or `mdc-chip`. Do not use a badge and a chip in the same tab.",
      "Accessibility": "### Built-in features\n\n- The host element exposes `role=\"tab\"`.\n- The active state is mirrored to `aria-selected` on the host (the `ariaStateKey` is set to `aria-selected`, so `active=\"true\"` writes `aria-selected=\"true\"`, `false` writes `aria-selected=\"false\"`, and `undefined` removes the attribute).\n- Enter activates the tab; Space activates on key-up. The host also listens for blur to clear the visual `pressed` state if the key is released outside the element.\n- The native form `type` attribute is cleared on connect so the tab does not submit forms when nested inside one.\n- When `disabled` is set, the tab is removed from the tab order, marked `aria-disabled=\"true\"`, and the parent list skips it during arrow-key navigation.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host element | `role` | `tab` |\n| Host element | `aria-selected` | `true` when `active`, `false` when not active, removed when `active` is `undefined` |\n| Host element | `aria-disabled` | `true` when `disabled` or `soft-disabled` |\n| Host element | `aria-pressed` | not used (overridden to `aria-selected`) |\n\n### Implementation requirements\n\n#### General\n\n- Wire each tab to its panel by setting `aria-controls` on the tab pointing at the panel element's `id`.\n- The corresponding content panel element must have `role=\"tabpanel\"` and `aria-labelledby` referencing the tab's `id`.\n- If a tab opens a popup menu, set `aria-haspopup` on it to either `menu` or `true`.\n\n#### Labelling\n\n- When `text` is omitted, provide an `aria-label` on the tab so screen reader users hear a meaningful name.\n\n### Notes\n\n- When the tab lives inside `mdc-tablist`, listen to the list's `change` event for selection changes rather than the tab's `activechange` event to avoid duplicate handlers."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "active",
          "attribute": "active",
          "type": "boolean",
          "values": [],
          "description": "Tab can be active or inactive. Active tab means the tab is selected.",
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "iconName",
          "attribute": "icon-name",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "select",
          "default": "32",
          "values": [
            28,
            32
          ],
          "description": "Size of the tab in pixels.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "tabIndex",
          "attribute": "tab-index",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "text",
          "attribute": "text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "false",
          "values": [
            "glass",
            "line",
            "pill"
          ],
          "description": "<br />\nEvent Data: `detail: { tabId: this.tabId, active }`\n<br />\nNote: the activechange event is used by the tab list component to react to the change in state of the tab,\nso this event won't be needed if the tab list is used.\n/\nclass Tab extends IconNameMixin(LifeCycleMixin(Buttonsimple)) {\n/**\nThe tab's active state indicates whether it is currently toggled on (active) or off (inactive).\nWhen the active state is true, the tab is considered to be in an active state, meaning it is toggled on.\nConversely, when the active state is false, the tab is in an inactive state, indicating it is toggled off.\nThis attribute also controls the \"aria-selected\" attribute of the tab.\nWhen the tab is active, \"aria-selected\" is set to true, indicating that the tab is selected.\nWhen the tab is inactive, \"aria-selected\" is set to false, indicating that the tab is not selected.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "prefix",
          "description": "The slot for the content before the text, typically used for the icon."
        },
        {
          "name": "postfix",
          "description": "The slot for the content after the text, typically used for the badge or chip."
        }
      ],
      "events": [
        {
          "name": "click",
          "description": "(React: onClick) This event is dispatched when the tab is clicked."
        },
        {
          "name": "keydown",
          "description": "(React: onKeyDown) This event is dispatched when a key is pressed down on the tab."
        },
        {
          "name": "keyup",
          "description": "(React: onKeyUp) This event is dispatched when a key is released on the tab."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the tab receives focus."
        },
        {
          "name": "activechange",
          "description": "(React: onActiveChange) This event is dispatched when the active state of the tab changes"
        }
      ],
      "cssParts": [
        {
          "name": "container",
          "description": "The container of the tab."
        },
        {
          "name": "regular-icon",
          "description": "The icon of the tab, if inactive."
        },
        {
          "name": "filled-icon",
          "description": "The icon of the tab, if active."
        },
        {
          "name": "indicator",
          "description": "The indicator of the tab."
        },
        {
          "name": "text",
          "description": "The text of the tab."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-tab-height",
          "description": "The height of the tab."
        },
        {
          "name": "--mdc-tab-padding-left",
          "description": "The left padding of the tab."
        },
        {
          "name": "--mdc-tab-padding-right",
          "description": "The right padding of the tab."
        },
        {
          "name": "--mdc-tab-content-gap",
          "description": "The gap between the icon and text in the tab."
        },
        {
          "name": "--mdc-tab-background-color",
          "description": "The background color of the tab."
        },
        {
          "name": "--mdc-tab-color",
          "description": "The text color of the tab."
        },
        {
          "name": "--mdc-tab-border-radius",
          "description": "The border radius of the tab."
        },
        {
          "name": "--mdc-tab-content-justification",
          "description": "The justification of the content in the tab."
        },
        {
          "name": "--mdc-tab-line-active-indicator-height",
          "description": "The height of the active indicator line."
        },
        {
          "name": "--mdc-tab-line-active-indicator-width",
          "description": "The width of the active indicator line."
        },
        {
          "name": "--mdc-tab-line-border-bottom-left-radius",
          "description": "The border bottom left radius of the active indicator line."
        },
        {
          "name": "--mdc-tab-line-border-bottom-right-radius",
          "description": "The border bottom right radius of the active indicator line."
        },
        {
          "name": "--mdc-tab-line-border-top-left-radius",
          "description": "The border top left radius of the active indicator line."
        },
        {
          "name": "--mdc-tab-line-border-top-right-radius",
          "description": "The border top right radius of the active indicator line."
        }
      ],
      "rules": [
        "bubbling `CustomEvent` with `detail: { tabId, active }` fired when the active state changes. When the tab is used inside `mdc-tablist`, the list listens for this internally and consumers should listen for the list's `change` event instead.",
        "content before the text; defaults to the icon rendered from `icon-name`.",
        "content after the text, typically `mdc-badge` or `mdc-chip`. Do not use a badge and a chip in the same tab."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/tab/index.js\"; // custom element registration\n// or via React wrapper\nimport { Tab } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-tablist data-aria-label=\"Account sections\">\n  <mdc-tab tab-id=\"overview\" text=\"Overview\" icon-name=\"info-circle-regular\" active></mdc-tab>\n  <mdc-tab tab-id=\"activity\" text=\"Activity\" icon-name=\"activity-regular\">\n    <mdc-badge slot=\"postfix\" type=\"dot\"></mdc-badge>\n  </mdc-tab>\n</mdc-tablist>"
      }
    ],
    "reactImports": [
      "Tab"
    ],
    "constants": {
      "TAB_VARIANTS": {
        "GLASS": "glass",
        "LINE": "line",
        "PILL": "pill"
      },
      "TAB_SIZES": {
        "28": 28,
        "32": 32
      },
      "DEFAULTS": {
        "VARIANT": "pill",
        "ACTIVE": false,
        "SIZE": 32
      }
    },
    "keywords": [
      "accessibility",
      "an",
      "and",
      "component",
      "control",
      "for",
      "guidelines",
      "icon",
      "individual",
      "inside",
      "mdc",
      "overview",
      "tab",
      "tablist",
      "text",
      "the",
      "usage",
      "used"
    ]
  },
  {
    "name": "tablist",
    "title": "Tab List",
    "tagName": "mdc-tablist",
    "summary": "Usage, guidelines, and accessibility for the mdc-tablist component — the container that organizes mdc-tab elements with roving focus and overflow scrolling.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/tablist",
      "knowledgeBasePath": "components/src/components/tablist/knowledge-base/tablist.component.md"
    },
    "docs": "## Overview\n\nThe tab list organises a row of `mdc-tab` elements into a horizontal container. It implements the WAI-ARIA tabs pattern: roving focus with arrow keys, click or Enter/Space activation, and automatic scroll arrow buttons when the tabs overflow their container.\n\n### When to use\n\n- Use to switch between several related content panels in the same view.\n- Use when one panel is visible at a time and the user can move between them.\n\n### When not to use\n\n- Do not use for primary navigation between pages.\n- Do not use when there is only one panel or when the panels are conceptually unrelated.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/tablist/index.js\"; // custom element registration\nimport \"@momentum-design/components/dist/components/tab/index.js\";\n// or via React wrapper\nimport { TabList, Tab } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-tablist data-aria-label=\"Account sections\">\n  <mdc-tab tab-id=\"overview\" text=\"Overview\" active></mdc-tab>\n  <mdc-tab tab-id=\"activity\" text=\"Activity\"></mdc-tab>\n  <mdc-tab tab-id=\"billing\" text=\"Billing\"></mdc-tab>\n</mdc-tablist>\n\n<section id=\"overview-panel\" role=\"tabpanel\" aria-labelledby=\"overview\">…</section>\n```\n\nWire selection by listening to `change`:\n\n```js\ndocument.querySelector('mdc-tablist').addEventListener('change', (e) => {\n  showPanel(e.detail.tabId);\n});\n```\n\nFor each tab, set `aria-controls=\"<panel-id>\"` and on each panel set `role=\"tabpanel\"` and `aria-labelledby=\"<tab-id>\"`.\n\n### Property/Attribute details\n\n- `data-aria-label` (string) — label for the tablist container. Used when a visible label is not present and surfaced as the `aria-label` of the inner `tablist` element.\n- Active tab is tracked internally via the child tab `tab-id`. If no tab starts active, the first non-disabled tab is activated after the first render.\n\nThe component dispatches:\n\n- `change` — bubbling `CustomEvent` fired when the user changes the active tab. The new tab id is on `event.detail.tabId`.\n\nSlots:\n\n- Default slot accepts `mdc-tab` elements. Disabled tabs are skipped during arrow-key navigation and from initial-active selection.\n\n### Limitations\n\n- A tab list with no child `mdc-tab` elements reports an error via `onerror` and skips initialization.\n- Every child tab must have a unique `tab-id`; duplicates trigger an `onerror` report.\n\n## Accessibility\n\n### Built-in features\n\n- The inner container exposes `role=\"tablist\"` with `tabindex=\"-1\"` and the `aria-label` from `data-aria-label`.\n- Arrow Left/Right move focus between tabs with roving `tabindex` (the focused tab gets `tabindex=\"0\"`, all others `tabindex=\"-1\"`).\n- Home/End jump to the first or last tab (handled by the shared list navigation behaviour).\n- Disabled tabs are skipped during arrow-key navigation.\n- The active tab is the only one with `aria-selected=\"true\"`; all other tabs have `aria-selected=\"false\"` (set by `mdc-tab`).\n- Calling `.focus()` on the host forwards focus to the currently active tab rather than the shadow host. The list also uses `delegatesFocus` on its shadow root.\n- When focus enters the tablist from outside, focus is moved to the active tab automatically.\n- Forward/backward scroll arrow buttons appear automatically when the tabs overflow their container. Their visibility is recalculated via a `ResizeObserver`. The buttons swap sides in RTL layouts. If the focused arrow button is hidden, focus moves to the other arrow button, or to the active tab when both arrows disappear.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Inner container | `role` | `tablist` |\n| Inner container | `tabindex` | `-1` |\n| Inner container | `aria-label` | value of `data-aria-label` (when set) |\n| Scroll arrow `mdc-button` | `aria-label` | \"Scroll tabs left\" / \"Scroll tabs right\" (direction reflects LTR/RTL) |\n| Each child `mdc-tab` | `tabindex` | `0` for the active/focused tab, `-1` for all others (roving tabindex) |\n| Each child `mdc-tab` | `aria-selected` | `true` when active, `false` otherwise |\n\n### Implementation requirements\n\n#### General\n\n- Each tab must have an `aria-controls` attribute pointing at the `id` of its associated panel.\n- Each panel element must have `role=\"tabpanel\"` and `aria-labelledby` referencing its tab.\n- If a tab opens a popup menu, set `aria-haspopup` on that tab to `menu` or `true`.\n\n#### Labelling\n\n- Provide `data-aria-label` when the tablist does not have a visible heading directly above it.\n\n### Notes\n\n- The tablist defines tabs semantics (`tablist`, `tab`, `aria-selected`) implicitly. Consumers must explicitly provide the panel semantics, the tab/panel wiring (`aria-controls` and `aria-labelledby`), and the accessible label for the list.",
    "sections": {
      "Overview": "The tab list organises a row of `mdc-tab` elements into a horizontal container. It implements the WAI-ARIA tabs pattern: roving focus with arrow keys, click or Enter/Space activation, and automatic scroll arrow buttons when the tabs overflow their container.\n\n### When to use\n\n- Use to switch between several related content panels in the same view.\n- Use when one panel is visible at a time and the user can move between them.\n\n### When not to use\n\n- Do not use for primary navigation between pages.\n- Do not use when there is only one panel or when the panels are conceptually unrelated.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/tablist/index.js\"; // custom element registration\nimport \"@momentum-design/components/dist/components/tab/index.js\";\n// or via React wrapper\nimport { TabList, Tab } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-tablist data-aria-label=\"Account sections\">\n  <mdc-tab tab-id=\"overview\" text=\"Overview\" active></mdc-tab>\n  <mdc-tab tab-id=\"activity\" text=\"Activity\"></mdc-tab>\n  <mdc-tab tab-id=\"billing\" text=\"Billing\"></mdc-tab>\n</mdc-tablist>\n\n<section id=\"overview-panel\" role=\"tabpanel\" aria-labelledby=\"overview\">…</section>\n```\n\nWire selection by listening to `change`:\n\n```js\ndocument.querySelector('mdc-tablist').addEventListener('change', (e) => {\n  showPanel(e.detail.tabId);\n});\n```\n\nFor each tab, set `aria-controls=\"<panel-id>\"` and on each panel set `role=\"tabpanel\"` and `aria-labelledby=\"<tab-id>\"`.\n\n### Property/Attribute details\n\n- `data-aria-label` (string) — label for the tablist container. Used when a visible label is not present and surfaced as the `aria-label` of the inner `tablist` element.\n- Active tab is tracked internally via the child tab `tab-id`. If no tab starts active, the first non-disabled tab is activated after the first render.\n\nThe component dispatches:\n\n- `change` — bubbling `CustomEvent` fired when the user changes the active tab. The new tab id is on `event.detail.tabId`.\n\nSlots:\n\n- Default slot accepts `mdc-tab` elements. Disabled tabs are skipped during arrow-key navigation and from initial-active selection.\n\n### Limitations\n\n- A tab list with no child `mdc-tab` elements reports an error via `onerror` and skips initialization.\n- Every child tab must have a unique `tab-id`; duplicates trigger an `onerror` report.",
      "Accessibility": "### Built-in features\n\n- The inner container exposes `role=\"tablist\"` with `tabindex=\"-1\"` and the `aria-label` from `data-aria-label`.\n- Arrow Left/Right move focus between tabs with roving `tabindex` (the focused tab gets `tabindex=\"0\"`, all others `tabindex=\"-1\"`).\n- Home/End jump to the first or last tab (handled by the shared list navigation behaviour).\n- Disabled tabs are skipped during arrow-key navigation.\n- The active tab is the only one with `aria-selected=\"true\"`; all other tabs have `aria-selected=\"false\"` (set by `mdc-tab`).\n- Calling `.focus()` on the host forwards focus to the currently active tab rather than the shadow host. The list also uses `delegatesFocus` on its shadow root.\n- When focus enters the tablist from outside, focus is moved to the active tab automatically.\n- Forward/backward scroll arrow buttons appear automatically when the tabs overflow their container. Their visibility is recalculated via a `ResizeObserver`. The buttons swap sides in RTL layouts. If the focused arrow button is hidden, focus moves to the other arrow button, or to the active tab when both arrows disappear.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Inner container | `role` | `tablist` |\n| Inner container | `tabindex` | `-1` |\n| Inner container | `aria-label` | value of `data-aria-label` (when set) |\n| Scroll arrow `mdc-button` | `aria-label` | \"Scroll tabs left\" / \"Scroll tabs right\" (direction reflects LTR/RTL) |\n| Each child `mdc-tab` | `tabindex` | `0` for the active/focused tab, `-1` for all others (roving tabindex) |\n| Each child `mdc-tab` | `aria-selected` | `true` when active, `false` otherwise |\n\n### Implementation requirements\n\n#### General\n\n- Each tab must have an `aria-controls` attribute pointing at the `id` of its associated panel.\n- Each panel element must have `role=\"tabpanel\"` and `aria-labelledby` referencing its tab.\n- If a tab opens a popup menu, set `aria-haspopup` on that tab to `menu` or `true`.\n\n#### Labelling\n\n- Provide `data-aria-label` when the tablist does not have a visible heading directly above it.\n\n### Notes\n\n- The tablist defines tabs semantics (`tablist`, `tab`, `aria-selected`) implicitly. Consumers must explicitly provide the panel semantics, the tab/panel wiring (`aria-controls` and `aria-labelledby`), and the accessible label for the list."
    },
    "dependencies": [
      "mdc-tab",
      "mdc-button"
    ],
    "api": {
      "properties": [
        {
          "name": "activeTabId",
          "attribute": "active-tab-id",
          "type": "select",
          "values": [
            "calls-tab",
            "videos-tab",
            "music-tab",
            "documents-tab",
            "meetings-tab"
          ],
          "description": "ID of the active tab. Defaults to the first tab if not provided.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "showForwardArrowButton",
          "attribute": "data-aria-label",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "Event that fires when user changes the active tab.\nThe function sent as the argument will receive the fired event\nand the new tab id can be fetched from event.detail.tabId.\n`(event: CustomEvent) => handleTabChange(event.detail.tabId);`\n/\nclass TabList extends ListNavigationMixin(\nCaptureDestroyEventForChildElement(KeyDownHandledMixin(KeyToActionMixin(TabListComponentMixin(Component)))),\n) {\n/**\nLabel for the tablist.\nThis is used when the tablist does not have a visible label.",
          "reflected": false,
          "source": "component"
        },
        {
          "name": "tabsize",
          "attribute": "tabsize",
          "type": "select",
          "values": [],
          "description": "Set the size of tabs inside the tablist",
          "source": "storybook"
        },
        {
          "name": "tabvariant",
          "attribute": "tabvariant",
          "type": "select",
          "values": [],
          "description": "Set the variant of tab inside the tablist",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "Default slot for mdc-tab elements.",
          "description": ""
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the tab is selected."
        }
      ],
      "cssParts": [
        {
          "name": "container",
          "description": "The tablist container."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-tablist-gap",
          "description": "Gap between tabs"
        },
        {
          "name": "--mdc-tablist-width",
          "description": "Width of the tablist"
        },
        {
          "name": "--mdc-tablist-arrow-button-margin",
          "description": "Margin value for the arrow button"
        }
      ],
      "rules": [
        "bubbling `CustomEvent` fired when the user changes the active tab. The new tab id is on `event.detail.tabId`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/tablist/index.js\"; // custom element registration\nimport \"@momentum-design/components/dist/components/tab/index.js\";\n// or via React wrapper\nimport { TabList, Tab } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-tablist data-aria-label=\"Account sections\">\n  <mdc-tab tab-id=\"overview\" text=\"Overview\" active></mdc-tab>\n  <mdc-tab tab-id=\"activity\" text=\"Activity\"></mdc-tab>\n  <mdc-tab tab-id=\"billing\" text=\"Billing\"></mdc-tab>\n</mdc-tablist>\n\n<section id=\"overview-panel\" role=\"tabpanel\" aria-labelledby=\"overview\">…</section>"
      },
      {
        "language": "js",
        "code": "document.querySelector('mdc-tablist').addEventListener('change', (e) => {\n  showPanel(e.detail.tabId);\n});"
      }
    ],
    "reactImports": [
      "TabList",
      "Tab"
    ],
    "constants": {},
    "keywords": [
      "accessibility",
      "and",
      "button",
      "component",
      "container",
      "elements",
      "focus",
      "for",
      "guidelines",
      "list",
      "mdc",
      "organizes",
      "overflow",
      "overview",
      "roving",
      "scrolling",
      "tab",
      "tablist",
      "that",
      "the",
      "usage",
      "with"
    ]
  },
  {
    "name": "text",
    "title": "Text",
    "tagName": "mdc-text",
    "summary": "Usage, guidelines, and accessibility for the mdc-text component — the typography primitive that renders styled text with a configurable semantic tag.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/text",
      "knowledgeBasePath": "components/src/components/text/knowledge-base/text.component.md"
    },
    "docs": "## Overview\n\nThe text component is the typography primitive for the design system. It renders any of the standard text-bearing HTML elements (`p`, `span`, `div`, `small`, `h1`–`h6`) and applies one of the predefined token-driven type styles. Mount the component inside an `mdc-themeprovider` so colour and font tokens resolve correctly.\n\n### When to use\n\n- Use whenever you need a piece of styled text inside any component or page.\n- Use to choose the semantic tag (`tagname`) independently from the visual style (`type`), so the heading hierarchy of the page stays correct without forcing a specific size.\n\n### When not to use\n\n- Do not use to render rich interactive controls — wrap text inside the appropriate interactive component instead.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/text/index.js\"; // custom element registration\n// or via React wrapper\nimport { Text } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-themeprovider>\n  <mdc-text tagname=\"h2\" type=\"heading-midsize-bold\">Section title</mdc-text>\n  <mdc-text type=\"body-midsize-regular\">Body copy goes here.</mdc-text>\n</mdc-themeprovider>\n```\n\n### Property/Attribute details\n\n- `type` (string, default `'body-midsize-medium'`) — selects the text style. Accepted values:\n  - Body: `'body-small-regular'`, `'body-small-medium'`, `'body-small-bold'`, `'body-midsize-regular'`, `'body-midsize-medium'`, `'body-midsize-bold'`, `'body-large-regular'`, `'body-large-medium'`, `'body-large-bold'`, plus the underline variants `'body-small-regular-underline'`, `'body-small-medium-underline'`, `'body-midsize-regular-underline'`, `'body-midsize-medium-underline'`, `'body-large-regular-underline'`, `'body-large-medium-underline'`.\n  - Heading: `'heading-small-regular'`, `'heading-small-medium'`, `'heading-small-bold'`, `'heading-midsize-regular'`, `'heading-midsize-medium'`, `'heading-midsize-bold'`, `'heading-large-regular'`, `'heading-large-medium'`, `'heading-large-bold'`, `'heading-xlarge-regular'`, `'heading-xlarge-medium'`, `'heading-xlarge-bold'`.\n  - Headline: `'headline-small-light'`, `'headline-small-regular'`.\n- `tagname` (string, default `'p'`) — chooses the HTML tag the slotted content is wrapped in. Accepted values: `'h1'`, `'h2'`, `'h3'`, `'h4'`, `'h5'`, `'h6'`, `'p'`, `'small'`, `'span'`, `'div'`. Invalid values fall back to the default `'p'`. The choice of tag does not change the typography — that is driven by `type`.\n\n### Limitations\n\n- The component must be rendered inside an `mdc-themeprovider` for the colour and font tokens it relies on to be defined.\n\n## Accessibility\n\n### Notes\n\n- Choose `tagname` to preserve the semantic heading and document outline expected by assistive technologies, independently of the visual `type`.\n- For long content that may be visually truncated, the component reads truncation through its overflow handling — for inline tags (`span`, `small`) the host element itself is the overflow boundary; for block tags the inner element is.",
    "sections": {
      "Overview": "The text component is the typography primitive for the design system. It renders any of the standard text-bearing HTML elements (`p`, `span`, `div`, `small`, `h1`–`h6`) and applies one of the predefined token-driven type styles. Mount the component inside an `mdc-themeprovider` so colour and font tokens resolve correctly.\n\n### When to use\n\n- Use whenever you need a piece of styled text inside any component or page.\n- Use to choose the semantic tag (`tagname`) independently from the visual style (`type`), so the heading hierarchy of the page stays correct without forcing a specific size.\n\n### When not to use\n\n- Do not use to render rich interactive controls — wrap text inside the appropriate interactive component instead.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/text/index.js\"; // custom element registration\n// or via React wrapper\nimport { Text } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-themeprovider>\n  <mdc-text tagname=\"h2\" type=\"heading-midsize-bold\">Section title</mdc-text>\n  <mdc-text type=\"body-midsize-regular\">Body copy goes here.</mdc-text>\n</mdc-themeprovider>\n```\n\n### Property/Attribute details\n\n- `type` (string, default `'body-midsize-medium'`) — selects the text style. Accepted values:\n  - Body: `'body-small-regular'`, `'body-small-medium'`, `'body-small-bold'`, `'body-midsize-regular'`, `'body-midsize-medium'`, `'body-midsize-bold'`, `'body-large-regular'`, `'body-large-medium'`, `'body-large-bold'`, plus the underline variants `'body-small-regular-underline'`, `'body-small-medium-underline'`, `'body-midsize-regular-underline'`, `'body-midsize-medium-underline'`, `'body-large-regular-underline'`, `'body-large-medium-underline'`.\n  - Heading: `'heading-small-regular'`, `'heading-small-medium'`, `'heading-small-bold'`, `'heading-midsize-regular'`, `'heading-midsize-medium'`, `'heading-midsize-bold'`, `'heading-large-regular'`, `'heading-large-medium'`, `'heading-large-bold'`, `'heading-xlarge-regular'`, `'heading-xlarge-medium'`, `'heading-xlarge-bold'`.\n  - Headline: `'headline-small-light'`, `'headline-small-regular'`.\n- `tagname` (string, default `'p'`) — chooses the HTML tag the slotted content is wrapped in. Accepted values: `'h1'`, `'h2'`, `'h3'`, `'h4'`, `'h5'`, `'h6'`, `'p'`, `'small'`, `'span'`, `'div'`. Invalid values fall back to the default `'p'`. The choice of tag does not change the typography — that is driven by `type`.\n\n### Limitations\n\n- The component must be rendered inside an `mdc-themeprovider` for the colour and font tokens it relies on to be defined.",
      "Accessibility": "### Notes\n\n- Choose `tagname` to preserve the semantic heading and document outline expected by assistive technologies, independently of the visual `type`.\n- For long content that may be visually truncated, the component reads truncation through its overflow handling — for inline tags (`span`, `small`) the host element itself is the overflow boundary; for block tags the inner element is."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Children (passed into ",
          "source": "storybook"
        },
        {
          "name": "tagname",
          "attribute": "tagname",
          "type": "select",
          "default": "DEFAULTS.TEXT_ELEMENT_TAGNAME",
          "values": [
            "h1",
            "h2",
            "h3",
            "h4",
            "h5",
            "h6",
            "p",
            "small",
            "span",
            "div"
          ],
          "description": "Specifies the HTML tag name for the text element. The default tag name is `p`.\nThis attribute is optional. When set, it changes the tag name of the text element.\nAcceptable values include:\n- 'h1'\n- 'h2'\n- 'h3'\n- 'h4'\n- 'h5'\n- 'h6'\n- 'p'\n- 'small'\n- 'span'\n- 'div'\nFor instance, setting this attribute to `h2` will render the text element as an `h2` element.\nNote that the styling is determined by the `type` attribute.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "select",
          "default": "body-midsize-medium",
          "values": [
            "body-small-regular",
            "body-small-medium",
            "body-small-bold",
            "body-midsize-regular",
            "body-midsize-medium",
            "body-midsize-bold",
            "body-large-regular",
            "body-large-medium",
            "body-large-bold",
            "body-small-regular-underline",
            "body-small-medium-underline",
            "body-midsize-regular-underline",
            "body-midsize-medium-underline",
            "body-large-regular-underline",
            "body-large-medium-underline",
            "heading-small-regular",
            "heading-small-medium",
            "heading-small-bold",
            "heading-midsize-regular",
            "heading-midsize-medium",
            "heading-midsize-bold",
            "heading-large-regular",
            "heading-large-medium",
            "heading-large-bold",
            "heading-xlarge-regular",
            "heading-xlarge-medium",
            "heading-xlarge-bold",
            "headline-small-light",
            "headline-small-regular"
          ],
          "description": "/\nclass Text extends OverflowMixin(Component) {\n/**\n/\nprivate textPartElement!: HTMLElement;\n/**\nSpecifies the text style to be applied.\nAcceptable values include:\n- 'body-small-regular'\n- 'body-small-medium'\n- 'body-small-bold'\n- 'body-midsize-regular'\n- 'body-midsize-medium'\n- 'body-midsize-bold'\n- 'body-large-regular'\n- 'body-large-medium'\n- 'body-large-bold'\n- 'body-small-regular-underline'\n- 'body-small-medium-underline'\n- 'body-midsize-regular-underline'\n- 'body-midsize-medium-underline'\n- 'body-large-regular-underline'\n- 'body-large-medium-underline'\n- 'heading-small-regular'\n- 'heading-small-medium'\n- 'heading-small-bold'\n- 'heading-midsize-regular'\n- 'heading-midsize-medium'\n- 'heading-midsize-bold'\n- 'heading-large-regular'\n- 'heading-large-medium'\n- 'heading-large-bold'\n- 'heading-xlarge-regular'\n- 'heading-xlarge-medium'\n- 'heading-xlarge-bold'\n- 'headline-small-light'\n- 'headline-small-regular'",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for text content"
        }
      ],
      "events": [],
      "cssParts": [
        {
          "name": "text",
          "description": "The text element"
        }
      ],
      "cssProperties": [],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/text/index.js\"; // custom element registration\n// or via React wrapper\nimport { Text } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-themeprovider>\n  <mdc-text tagname=\"h2\" type=\"heading-midsize-bold\">Section title</mdc-text>\n  <mdc-text type=\"body-midsize-regular\">Body copy goes here.</mdc-text>\n</mdc-themeprovider>"
      }
    ],
    "reactImports": [
      "Text"
    ],
    "constants": {
      "TYPE": {
        "BODY_SMALL_REGULAR": "body-small-regular",
        "BODY_SMALL_MEDIUM": "body-small-medium",
        "BODY_SMALL_BOLD": "body-small-bold",
        "BODY_MIDSIZE_REGULAR": "body-midsize-regular",
        "BODY_MIDSIZE_MEDIUM": "body-midsize-medium",
        "BODY_MIDSIZE_BOLD": "body-midsize-bold",
        "BODY_LARGE_REGULAR": "body-large-regular",
        "BODY_LARGE_MEDIUM": "body-large-medium",
        "BODY_LARGE_BOLD": "body-large-bold",
        "BODY_SMALL_REGULAR_UNDERLINE": "body-small-regular-underline",
        "BODY_SMALL_MEDIUM_UNDERLINE": "body-small-medium-underline",
        "BODY_MIDSIZE_REGULAR_UNDERLINE": "body-midsize-regular-underline",
        "BODY_MIDSIZE_MEDIUM_UNDERLINE": "body-midsize-medium-underline",
        "BODY_LARGE_REGULAR_UNDERLINE": "body-large-regular-underline",
        "BODY_LARGE_MEDIUM_UNDERLINE": "body-large-medium-underline",
        "HEADING_SMALL_REGULAR": "heading-small-regular",
        "HEADING_SMALL_MEDIUM": "heading-small-medium",
        "HEADING_SMALL_BOLD": "heading-small-bold",
        "HEADING_MIDSIZE_REGULAR": "heading-midsize-regular",
        "HEADING_MIDSIZE_MEDIUM": "heading-midsize-medium",
        "HEADING_MIDSIZE_BOLD": "heading-midsize-bold",
        "HEADING_LARGE_REGULAR": "heading-large-regular",
        "HEADING_LARGE_MEDIUM": "heading-large-medium",
        "HEADING_LARGE_BOLD": "heading-large-bold",
        "HEADING_XLARGE_REGULAR": "heading-xlarge-regular",
        "HEADING_XLARGE_MEDIUM": "heading-xlarge-medium",
        "HEADING_XLARGE_BOLD": "heading-xlarge-bold",
        "HEADLINE_SMALL_LIGHT": "headline-small-light",
        "HEADLINE_SMALL_REGULAR": "headline-small-regular"
      },
      "VALID_TEXT_TAGS": {
        "H1": "h1",
        "H2": "h2",
        "H3": "h3",
        "H4": "h4",
        "H5": "h5",
        "H6": "h6",
        "P": "p",
        "SMALL": "small",
        "SPAN": "span",
        "DIV": "div"
      },
      "DEFAULTS": {
        "TYPE": "body-midsize-medium",
        "TEXT_ELEMENT_TAGNAME": "p",
        "CSS_PART_TEXT": "text",
        "CHILDREN": "The quick brown fox jumps over the lazy dog"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "configurable",
      "for",
      "guidelines",
      "mdc",
      "overview",
      "primitive",
      "renders",
      "semantic",
      "styled",
      "tag",
      "text",
      "that",
      "the",
      "typography",
      "usage",
      "with"
    ]
  },
  {
    "name": "textarea",
    "title": "Textarea",
    "tagName": "mdc-textarea",
    "summary": "Usage, guidelines, and accessibility for the mdc-textarea component — a form-associated multi-line text input with helper text, character limit, and optional keyboard-resizable height.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/textarea",
      "knowledgeBasePath": "components/src/components/textarea/knowledge-base/textarea.component.md"
    },
    "docs": "## Overview\n\nThe textarea is a form-associated multi-line text input. It renders a label, an optional info toggletip, the multi-line input, an optional character counter, helper or validation text, and an optional resize button that grows or shrinks the visible rows.\n\n### When to use\n\n- Use when users need to enter free-form text that may span multiple lines (messages, comments, descriptions).\n- Use when you want consistent label, helper-text, validation, and character-count chrome around a multi-line input.\n\n### When not to use\n\n- Do not use for short, single-line inputs — use `mdc-input` instead.\n- Do not use for structured values (email, time, password); use the dedicated form components.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/textarea/index.js\"; // custom element registration\n// or via React wrapper\nimport { Textarea } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-textarea\n  name=\"bio\"\n  label=\"Bio\"\n  help-text=\"Tell us about yourself\"\n  rows=\"4\"\n  max-character-limit=\"200\"\n></mdc-textarea>\n```\n\nHandle the character limit being exceeded by listening for `limitexceeded` and updating `help-text` and `help-text-type` so the validation message and screen-reader announcement stay in sync:\n\n```js\ntextarea.addEventListener('limitexceeded', (e) => {\n  const { currentCharacterCount, maxCharacterLimit } = e.detail;\n  textarea.helpTextType = currentCharacterCount > maxCharacterLimit ? 'error' : 'default';\n  textarea.helpText = currentCharacterCount > maxCharacterLimit\n    ? `You exceeded the limit by ${currentCharacterCount - maxCharacterLimit} characters`\n    : '';\n});\n```\n\n### Property/Attribute details\n\n- `name` (string, default `''`) — form submission key. Required for form participation.\n- `value` (string, default `''`) — current value. The initial value can also be supplied as text content inside the element; on connect, text content is trimmed and assigned to `value` if `value` is empty.\n- `placeholder` (string) — placeholder rendered when empty.\n- `rows` (number, default `5`) — visible row count of the underlying `<textarea>`. Updated by the resize button when `resizable` is enabled.\n- `cols` (number) — visible column count.\n- `wrap` (`'soft' | 'hard'`, default `'soft'`) — line-wrapping behaviour passed through to the native `<textarea>`.\n- `autocapitalize` (string, default `'off'`) — passes through to the native `<textarea>`.\n- `autocomplete` (string, default `'off'`) — passes through to the native `<textarea>`.\n- `dirname` (string) — name used to submit the input's directionality alongside the value.\n- `maxlength` / `minlength` (number) — hard character limits enforced by the native `<textarea>`.\n- `max-character-limit` (number) — soft limit that drives the character counter and the `limitexceeded` event. Independent from `maxlength`.\n- `required` (boolean, default `false`) — marks the field as required and renders the required indicator.\n- `disabled` (boolean) / `readonly` (boolean, default `false`) / `soft-disabled` (boolean, default `false`) — control interactivity. `readonly` and `disabled` block resize input as well.\n- `label` (string) — visible label rendered above the field.\n- `data-aria-label` (string) — overrides the `aria-label` on the underlying `<textarea>` when no visible label is appropriate.\n- `help-text` (string) — text rendered beneath the field. Also used as the validation message when `help-text-type` is `'error'` and the character limit is exceeded.\n- `help-text-type` (`'default' | 'error' | 'warning' | 'success' | 'priority'`, default `'default'`) — controls the helper icon and validation styling.\n- `validation-message` (string) — custom validation message used by `setCustomValidity` when the field is required and empty.\n- `toggletip-text` (string), `toggletip-placement`, `toggletip-strategy`, `info-icon-aria-label` — configure the info icon and toggletip rendered next to the label.\n- `resizable` (boolean, default `false`) — shows the resize button. When `false`, the button is hidden.\n- `resize-button-aria-label` (string, default `''`) — accessible name for the resize button. Always provide one that conveys what the button does and that Arrow Up / Arrow Down adjust the height.\n- `auto-focus-on-mount` (boolean) — when set, focuses the textarea immediately after first render.\n\nThe component dispatches:\n\n- `input` — fires on every keystroke.\n- `change` — fires on blur after a value change. Re-dispatched from inside the shadow DOM so it bubbles out.\n- `focus` / `blur` — native DOM events with React equivalents.\n- `limitexceeded` — fires once when the character count crosses or returns under the configured `max-character-limit`. Detail: `{ currentCharacterCount, maxCharacterLimit, value }`.\n\nSlots:\n\n- `label` — overrides the label rendering.\n- `toggletip` — overrides the toggletip info icon button.\n- `help-icon` — overrides the helper/validation icon.\n- `help-text` — overrides the helper/validation text element.\n\n## Accessibility\n\n### Built-in features\n\n- The component is form-associated (`ElementInternals`) — it participates in the surrounding `<form>`, supports `formResetCallback` and `formStateRestoreCallback`, and exposes `checkValidity()` / `reportValidity()` mirroring the underlying `<textarea>`.\n- The underlying `<textarea>` is linked to the label via `for`/`id` (`mdc-el-<uuid>`) so the label reads correctly to assistive technologies.\n- `aria-describedby` is set to the helper text element id whenever `help-text` is present.\n- `aria-invalid` is reflected on the `<textarea>` whenever `help-text-type` is `'error'`.\n- The required indicator (`*`) is rendered next to the label when `required` is set.\n- Character-limit announcements are pushed through an internal `mdc-screenreaderannouncer` (`aria-live=\"polite\"`, 500 ms delay) keyed to the textarea `id` whenever the value crosses the soft limit.\n- The resize button supports keyboard resizing: Arrow Up decreases the visible rows (minimum 1), Arrow Down increases them. While `readonly`, resize input is ignored. Pointer-based resizing is also supported via pointer capture.\n- When `disabled` or `readonly`, the resize button is also disabled.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| `<textarea>` | `id` | `mdc-el-<uuid>` (linked to the label) |\n| `<textarea>` | `aria-label` | value of `data-aria-label` (when no visible label is used) |\n| `<textarea>` | `aria-describedby` | helper-text id, set when `help-text` is present |\n| `<textarea>` | `aria-invalid` | `true` when `help-text-type` is `'error'`, `false` otherwise |\n| Resize `mdc-button` | `aria-label` | value of `resize-button-aria-label` |\n| `mdc-screenreaderannouncer` | `data-aria-live` | `polite` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide a `label` (or a slotted label) for screen readers to identify the textarea's purpose.\n- When `resizable` is enabled, provide a `resize-button-aria-label` that describes the button and mentions the Arrow Up/Down shortcuts.\n- When `max-character-limit` is set, also set or react to `help-text` and `help-text-type` on `limitexceeded` so the validation state and announcement match the visible counter.\n\n### Notes\n\n- The same `help-text` value is used as the custom validation message when the character limit is exceeded with `help-text-type=\"error\"`, so the message users see and the message form validation reports stay consistent.\n- The `change` event from the native `<textarea>` is not composed, so the component re-dispatches it. Handlers attached outside the shadow root will fire correctly.",
    "sections": {
      "Overview": "The textarea is a form-associated multi-line text input. It renders a label, an optional info toggletip, the multi-line input, an optional character counter, helper or validation text, and an optional resize button that grows or shrinks the visible rows.\n\n### When to use\n\n- Use when users need to enter free-form text that may span multiple lines (messages, comments, descriptions).\n- Use when you want consistent label, helper-text, validation, and character-count chrome around a multi-line input.\n\n### When not to use\n\n- Do not use for short, single-line inputs — use `mdc-input` instead.\n- Do not use for structured values (email, time, password); use the dedicated form components.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/textarea/index.js\"; // custom element registration\n// or via React wrapper\nimport { Textarea } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-textarea\n  name=\"bio\"\n  label=\"Bio\"\n  help-text=\"Tell us about yourself\"\n  rows=\"4\"\n  max-character-limit=\"200\"\n></mdc-textarea>\n```\n\nHandle the character limit being exceeded by listening for `limitexceeded` and updating `help-text` and `help-text-type` so the validation message and screen-reader announcement stay in sync:\n\n```js\ntextarea.addEventListener('limitexceeded', (e) => {\n  const { currentCharacterCount, maxCharacterLimit } = e.detail;\n  textarea.helpTextType = currentCharacterCount > maxCharacterLimit ? 'error' : 'default';\n  textarea.helpText = currentCharacterCount > maxCharacterLimit\n    ? `You exceeded the limit by ${currentCharacterCount - maxCharacterLimit} characters`\n    : '';\n});\n```\n\n### Property/Attribute details\n\n- `name` (string, default `''`) — form submission key. Required for form participation.\n- `value` (string, default `''`) — current value. The initial value can also be supplied as text content inside the element; on connect, text content is trimmed and assigned to `value` if `value` is empty.\n- `placeholder` (string) — placeholder rendered when empty.\n- `rows` (number, default `5`) — visible row count of the underlying `<textarea>`. Updated by the resize button when `resizable` is enabled.\n- `cols` (number) — visible column count.\n- `wrap` (`'soft' | 'hard'`, default `'soft'`) — line-wrapping behaviour passed through to the native `<textarea>`.\n- `autocapitalize` (string, default `'off'`) — passes through to the native `<textarea>`.\n- `autocomplete` (string, default `'off'`) — passes through to the native `<textarea>`.\n- `dirname` (string) — name used to submit the input's directionality alongside the value.\n- `maxlength` / `minlength` (number) — hard character limits enforced by the native `<textarea>`.\n- `max-character-limit` (number) — soft limit that drives the character counter and the `limitexceeded` event. Independent from `maxlength`.\n- `required` (boolean, default `false`) — marks the field as required and renders the required indicator.\n- `disabled` (boolean) / `readonly` (boolean, default `false`) / `soft-disabled` (boolean, default `false`) — control interactivity. `readonly` and `disabled` block resize input as well.\n- `label` (string) — visible label rendered above the field.\n- `data-aria-label` (string) — overrides the `aria-label` on the underlying `<textarea>` when no visible label is appropriate.\n- `help-text` (string) — text rendered beneath the field. Also used as the validation message when `help-text-type` is `'error'` and the character limit is exceeded.\n- `help-text-type` (`'default' | 'error' | 'warning' | 'success' | 'priority'`, default `'default'`) — controls the helper icon and validation styling.\n- `validation-message` (string) — custom validation message used by `setCustomValidity` when the field is required and empty.\n- `toggletip-text` (string), `toggletip-placement`, `toggletip-strategy`, `info-icon-aria-label` — configure the info icon and toggletip rendered next to the label.\n- `resizable` (boolean, default `false`) — shows the resize button. When `false`, the button is hidden.\n- `resize-button-aria-label` (string, default `''`) — accessible name for the resize button. Always provide one that conveys what the button does and that Arrow Up / Arrow Down adjust the height.\n- `auto-focus-on-mount` (boolean) — when set, focuses the textarea immediately after first render.\n\nThe component dispatches:\n\n- `input` — fires on every keystroke.\n- `change` — fires on blur after a value change. Re-dispatched from inside the shadow DOM so it bubbles out.\n- `focus` / `blur` — native DOM events with React equivalents.\n- `limitexceeded` — fires once when the character count crosses or returns under the configured `max-character-limit`. Detail: `{ currentCharacterCount, maxCharacterLimit, value }`.\n\nSlots:\n\n- `label` — overrides the label rendering.\n- `toggletip` — overrides the toggletip info icon button.\n- `help-icon` — overrides the helper/validation icon.\n- `help-text` — overrides the helper/validation text element.",
      "Accessibility": "### Built-in features\n\n- The component is form-associated (`ElementInternals`) — it participates in the surrounding `<form>`, supports `formResetCallback` and `formStateRestoreCallback`, and exposes `checkValidity()` / `reportValidity()` mirroring the underlying `<textarea>`.\n- The underlying `<textarea>` is linked to the label via `for`/`id` (`mdc-el-<uuid>`) so the label reads correctly to assistive technologies.\n- `aria-describedby` is set to the helper text element id whenever `help-text` is present.\n- `aria-invalid` is reflected on the `<textarea>` whenever `help-text-type` is `'error'`.\n- The required indicator (`*`) is rendered next to the label when `required` is set.\n- Character-limit announcements are pushed through an internal `mdc-screenreaderannouncer` (`aria-live=\"polite\"`, 500 ms delay) keyed to the textarea `id` whenever the value crosses the soft limit.\n- The resize button supports keyboard resizing: Arrow Up decreases the visible rows (minimum 1), Arrow Down increases them. While `readonly`, resize input is ignored. Pointer-based resizing is also supported via pointer capture.\n- When `disabled` or `readonly`, the resize button is also disabled.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| `<textarea>` | `id` | `mdc-el-<uuid>` (linked to the label) |\n| `<textarea>` | `aria-label` | value of `data-aria-label` (when no visible label is used) |\n| `<textarea>` | `aria-describedby` | helper-text id, set when `help-text` is present |\n| `<textarea>` | `aria-invalid` | `true` when `help-text-type` is `'error'`, `false` otherwise |\n| Resize `mdc-button` | `aria-label` | value of `resize-button-aria-label` |\n| `mdc-screenreaderannouncer` | `data-aria-live` | `polite` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Always provide a `label` (or a slotted label) for screen readers to identify the textarea's purpose.\n- When `resizable` is enabled, provide a `resize-button-aria-label` that describes the button and mentions the Arrow Up/Down shortcuts.\n- When `max-character-limit` is set, also set or react to `help-text` and `help-text-type` on `limitexceeded` so the validation state and announcement match the visible counter.\n\n### Notes\n\n- The same `help-text` value is used as the custom validation message when the character limit is exceeded with `help-text-type=\"error\"`, so the message users see and the message form validation reports stay consistent.\n- The `change` event from the native `<textarea>` is not composed, so the component re-dispatches it. Handlers attached outside the shadow root will fire correctly."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autocapitalize",
          "attribute": "autocapitalize",
          "type": "select",
          "default": "'off'",
          "values": [],
          "description": "The autocapitalize attribute of the textarea field.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "autocomplete",
          "attribute": "autocomplete",
          "type": "select",
          "default": "'off'",
          "values": [
            "off",
            "on"
          ],
          "description": "The autocomplete attribute of the textarea field.",
          "reflected": false,
          "source": "storybook"
        },
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "characterLimitAnnouncement",
          "attribute": "character-limit-announcement",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "cols",
          "attribute": "cols",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "dirname",
          "attribute": "dirname",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "overrides the helper/validation text element.",
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "inputElement",
          "attribute": "resize-button-aria-label",
          "type": "HTMLTextAreaElement",
          "default": "''",
          "values": [],
          "description": "Provides an accessible label for the resize button.\nThis value is used to set the `aria-label` attribute for the button.",
          "reflected": false,
          "source": "component"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "overrides the label rendering.",
          "source": "storybook"
        },
        {
          "name": "maxCharacterLimit",
          "attribute": "max-character-limit",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "maxlength",
          "attribute": "maxlength",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "minlength",
          "attribute": "minlength",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placeholder",
          "attribute": "placeholder",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "resizable",
          "attribute": "resizable",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "Specifies the name of the directionality of text for submission purposes (e.g., \"rtl\" for right-to-left).",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "resizeButtonAriaLabel",
          "attribute": "resize-button-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "rows",
          "attribute": "rows",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "validationMessage",
          "attribute": "validation-message",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "wrap",
          "attribute": "wrap",
          "type": "select",
          "default": "WRAP.SOFT",
          "values": [
            "hard",
            "soft"
          ],
          "description": "exceeds or restored.\n/\nclass Textarea extends CharacterLimitMixin(\nKeyDownHandledMixin(\nKeyToActionMixin(AutoFocusOnMountMixin(FormInternalsMixin(DataAriaLabelMixin(FormfieldWrapper)))),\n),\n) {\n/**\nThe placeholder text that is displayed when the textarea field is empty.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element. If not provided, the `label` property will be used to render the label."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button. If not provided, the `toggletip-text` property will be used to render the info icon button and toggletip."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon. If not provided, the icon will be rendered based on the `helpTextType` property."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text. If not provided, the `helpText` property will be used to render the helper/validation text."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the value of the textarea field changes (every press)."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the value of the textarea field changes (on blur)."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the textarea receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the textarea loses focus."
        },
        {
          "name": "limitexceeded",
          "description": "(React: onLimitExceeded) This event is dispatched once when the character limit"
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "textarea",
          "description": "The textarea element."
        },
        {
          "name": "textarea-container",
          "description": "The container element that wraps the textarea and resize button."
        },
        {
          "name": "textarea-footer",
          "description": "The footer element that contains the character counter."
        },
        {
          "name": "character-counter",
          "description": "The character counter element."
        },
        {
          "name": "resize-button",
          "description": "The resize button element (shown when `resizable` is true)."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-label-font-size",
          "description": "Font size for the label text."
        },
        {
          "name": "--mdc-label-font-weight",
          "description": "Font weight for the label text."
        },
        {
          "name": "--mdc-label-line-height",
          "description": "Line height for the label text."
        },
        {
          "name": "--mdc-label-color",
          "description": "Color for the label text."
        },
        {
          "name": "--mdc-help-text-font-size",
          "description": "Font size for the help text."
        },
        {
          "name": "--mdc-help-text-font-weight",
          "description": "Font weight for the help text."
        },
        {
          "name": "--mdc-help-text-line-height",
          "description": "Line height for the help text."
        },
        {
          "name": "--mdc-help-text-color",
          "description": "Color for the help text."
        },
        {
          "name": "--mdc-required-indicator-color",
          "description": "Color for the required indicator text."
        },
        {
          "name": "--mdc-textarea-text-color",
          "description": "Text color for the textarea field"
        },
        {
          "name": "--mdc-textarea-background-color",
          "description": "Background color for the textarea field"
        },
        {
          "name": "--mdc-textarea-border-color",
          "description": "Border color for the textarea field"
        },
        {
          "name": "--mdc-textarea-text-secondary-normal",
          "description": "Text color for the character counter"
        },
        {
          "name": "--mdc-textarea-text-font-size",
          "description": "Font size for the textarea field"
        },
        {
          "name": "--mdc-textarea-text-line-height",
          "description": "Line height for the textarea field"
        },
        {
          "name": "--mdc-textarea-container-background-color",
          "description": "Background color for the textarea container"
        }
      ],
      "rules": [
        "fires on every keystroke.",
        "fires on blur after a value change. Re-dispatched from inside the shadow DOM so it bubbles out.",
        "native DOM events with React equivalents.",
        "native DOM events with React equivalents.",
        "fires once when the character count crosses or returns under the configured `max-character-limit`. Detail: `{ currentCharacterCount, maxCharacterLimit, value }`.",
        "overrides the label rendering.",
        "overrides the toggletip info icon button.",
        "overrides the helper/validation icon.",
        "overrides the helper/validation text element."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/textarea/index.js\"; // custom element registration\n// or via React wrapper\nimport { Textarea } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-textarea\n  name=\"bio\"\n  label=\"Bio\"\n  help-text=\"Tell us about yourself\"\n  rows=\"4\"\n  max-character-limit=\"200\"\n></mdc-textarea>"
      },
      {
        "language": "js",
        "code": "textarea.addEventListener('limitexceeded', (e) => {\n  const { currentCharacterCount, maxCharacterLimit } = e.detail;\n  textarea.helpTextType = currentCharacterCount > maxCharacterLimit ? 'error' : 'default';\n  textarea.helpText = currentCharacterCount > maxCharacterLimit\n    ? `You exceeded the limit by ${currentCharacterCount - maxCharacterLimit} characters`\n    : '';\n});"
      }
    ],
    "reactImports": [
      "Textarea"
    ],
    "constants": {
      "WRAP": {
        "HARD": "hard",
        "SOFT": "soft"
      },
      "AUTO_COMPLETE": {
        "OFF": "off",
        "ON": "on"
      },
      "DEFAULTS": {
        "ROWS": 5,
        "COLS": 40,
        "WRAP": "soft"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "associated",
      "button",
      "character",
      "component",
      "for",
      "form",
      "guidelines",
      "height",
      "helper",
      "icon",
      "input",
      "keyboard",
      "limit",
      "line",
      "mdc",
      "multi",
      "optional",
      "overview",
      "resizable",
      "text",
      "textarea",
      "the",
      "toggletip",
      "usage",
      "with"
    ]
  },
  {
    "name": "themeprovider",
    "title": "Theme Provider",
    "tagName": "mdc-themeprovider",
    "summary": "Usage, guidelines, and accessibility for the mdc-themeprovider component — the context wrapper that applies a Momentum theme class to its subtree.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/themeprovider",
      "knowledgeBasePath": "components/src/components/themeprovider/knowledge-base/themeprovider.component.md"
    },
    "docs": "## Overview\n\nThe theme provider applies a Momentum theme class to its host element and exposes it as a context for descendant Lit components. CSS variables defined by the theme class drive token-based styling (colour, fonts, scrollbar). It also sets sensible defaults for text rendering (font family, weight, default text colour).\n\n### When to use\n\n- Wrap the section of the app that should render with Momentum tokens. A single provider at the application root is the most common pattern.\n- Use nested providers to render a region (for example a modal or preview pane) with a different theme — switching themes adds the new class and removes the previous one automatically.\n\n### When not to use\n\n- Do not use to scope behaviour unrelated to theming.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/themeprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { ThemeProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-themeprovider themeclass=\"mds-theme-stable-darkWebex\">\n  <!-- app or section content -->\n</mdc-themeprovider>\n```\n\nSwitching themes:\n\n```js\ndocument.querySelector('mdc-themeprovider').themeclass = 'mds-theme-stable-lightWebex';\n```\n\n### Property/Attribute details\n\n- `themeclass` (string, default `'mds-theme-stable-darkWebex'`) — fully qualified class name(s) that match the theme stylesheet. Space-separated values are supported; on each change, every class from the previous value is removed from the host `classList` and every class from the new value is added.\n\nAvailable Momentum theme classes:\n\n- `mds-theme-stable-darkWebex`, `mds-theme-stable-lightWebex`\n- `mds-theme-stable-darkBronzeWebex`, `mds-theme-stable-lightBronzeWebex`\n- `mds-theme-stable-darkIndigoWebex`, `mds-theme-stable-lightIndigoWebex`\n- `mds-theme-stable-darkJadeWebex`, `mds-theme-stable-lightJadeWebex`\n- `mds-theme-stable-darkLavenderWebex`, `mds-theme-stable-lightLavenderWebex`\n- `mds-theme-stable-darkRoseWebex`, `mds-theme-stable-lightRoseWebex`\n\nDescendant Lit components can read the current theme by consuming `ThemeProvider.Context`.\n\n### Limitations\n\n- The `themeclass` value must exactly match the class on the matching theme stylesheet — otherwise the CSS variables it depends on remain undefined.\n\n## Accessibility\n\n### Notes\n\n- The theme provider has no role, exposes no ARIA attributes, and does not appear in the accessibility tree. It is purely a visual/styling wrapper.\n- Ensure that the chosen theme provides sufficient colour contrast for the content rendered inside.",
    "sections": {
      "Overview": "The theme provider applies a Momentum theme class to its host element and exposes it as a context for descendant Lit components. CSS variables defined by the theme class drive token-based styling (colour, fonts, scrollbar). It also sets sensible defaults for text rendering (font family, weight, default text colour).\n\n### When to use\n\n- Wrap the section of the app that should render with Momentum tokens. A single provider at the application root is the most common pattern.\n- Use nested providers to render a region (for example a modal or preview pane) with a different theme — switching themes adds the new class and removes the previous one automatically.\n\n### When not to use\n\n- Do not use to scope behaviour unrelated to theming.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/themeprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { ThemeProvider } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-themeprovider themeclass=\"mds-theme-stable-darkWebex\">\n  <!-- app or section content -->\n</mdc-themeprovider>\n```\n\nSwitching themes:\n\n```js\ndocument.querySelector('mdc-themeprovider').themeclass = 'mds-theme-stable-lightWebex';\n```\n\n### Property/Attribute details\n\n- `themeclass` (string, default `'mds-theme-stable-darkWebex'`) — fully qualified class name(s) that match the theme stylesheet. Space-separated values are supported; on each change, every class from the previous value is removed from the host `classList` and every class from the new value is added.\n\nAvailable Momentum theme classes:\n\n- `mds-theme-stable-darkWebex`, `mds-theme-stable-lightWebex`\n- `mds-theme-stable-darkBronzeWebex`, `mds-theme-stable-lightBronzeWebex`\n- `mds-theme-stable-darkIndigoWebex`, `mds-theme-stable-lightIndigoWebex`\n- `mds-theme-stable-darkJadeWebex`, `mds-theme-stable-lightJadeWebex`\n- `mds-theme-stable-darkLavenderWebex`, `mds-theme-stable-lightLavenderWebex`\n- `mds-theme-stable-darkRoseWebex`, `mds-theme-stable-lightRoseWebex`\n\nDescendant Lit components can read the current theme by consuming `ThemeProvider.Context`.\n\n### Limitations\n\n- The `themeclass` value must exactly match the class on the matching theme stylesheet — otherwise the CSS variables it depends on remain undefined.",
      "Accessibility": "### Notes\n\n- The theme provider has no role, exposes no ARIA attributes, and does not appear in the accessibility tree. It is purely a visual/styling wrapper.\n- Ensure that the chosen theme provides sufficient colour contrast for the content rendered inside."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "themeclass",
          "attribute": "themeclass",
          "type": "unknown",
          "default": "DEFAULTS.THEMECLASS",
          "values": [],
          "description": "default: color-theme-text-primary-normal\ndefault: `Momentum` (from momentum-design/fonts)\ndefault: `-0.25px` (this is to match the old CiscoSans)\n/\nclass ThemeProvider extends Provider<ThemeProviderContext> {\nconstructor() {\nsuper({\ncontext: ThemeProviderContext.context,\ninitialValue: new ThemeProviderContext(DEFAULTS.THEMECLASS),\n});\n}\n/**\nContext object of the ThemeProvider, to be consumed by child components\n/\npublic static get Context() {\nreturn ThemeProviderContext.context;\n}\n/**\nTo keep track of the current theme class\n/\nprivate currentThemeClass?: string;\n/**\nCurrent theme class\nHas to be fully qualified, such that\nthe theme class matches the class of the respective\ntheme stylesheet\nDefault: 'mds-theme-stable-darkWebex'",
          "reflected": false,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "children"
        }
      ],
      "events": [],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-themeprovider-color-default",
          "description": "Option to override the default color,"
        },
        {
          "name": "--mdc-themeprovider-font-family",
          "description": "Option to override the font family,"
        },
        {
          "name": "--mdc-themeprovider-font-weight",
          "description": "Option to override the font weight, default: `400`"
        },
        {
          "name": "--mdc-themeprovider-scrollbar-track-color",
          "description": "Option to override the color of the scrollbar track."
        },
        {
          "name": "--mdc-themeprovider-scrollbar-thumb-color",
          "description": "Option to override the color of the scrollbar thumb."
        }
      ],
      "rules": []
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/themeprovider/index.js\"; // custom element registration\n// or via React wrapper\nimport { ThemeProvider } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-themeprovider themeclass=\"mds-theme-stable-darkWebex\">\n  <!-- app or section content -->\n</mdc-themeprovider>"
      },
      {
        "language": "js",
        "code": "document.querySelector('mdc-themeprovider').themeclass = 'mds-theme-stable-lightWebex';"
      }
    ],
    "reactImports": [
      "ThemeProvider"
    ],
    "constants": {
      "DEFAULTS": {
        "THEMECLASS": "'mds-theme-stable-darkWebex' as const"
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "applies",
      "class",
      "component",
      "context",
      "for",
      "guidelines",
      "its",
      "mdc",
      "momentum",
      "overview",
      "provider",
      "subtree",
      "that",
      "the",
      "theme",
      "themeprovider",
      "to",
      "usage",
      "wrapper"
    ]
  },
  {
    "name": "timepicker",
    "title": "Time Picker",
    "tagName": "mdc-timepicker",
    "summary": "Usage, guidelines, and accessibility for the mdc-timepicker component — a form-associated control for entering or selecting a time in 12-hour or 24-hour format.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/timepicker",
      "knowledgeBasePath": "components/src/components/timepicker/knowledge-base/timepicker.component.md"
    },
    "docs": "## Overview\n\nThe time picker is a composite form control for selecting a time. It exposes hour, minute, and (in 12-hour mode) AM/PM segments as ARIA spinbuttons that accept arrow-key adjustments and direct digit input, plus a dropdown arrow button that opens a list of preset times at a configurable interval.\n\n### When to use\n\n- Use when users need to enter or pick a time of day, manually or from a preset list.\n- Use inside a form when the time value should participate in form submission and validation.\n\n### When not to use\n\n- Do not use for date selection — use `mdc-datepicker` instead.\n- Do not use when you only need a free-form text field.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/timepicker/index.js\"; // custom element registration\n// or via React wrapper\nimport { TimePicker } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (12-hour mode with localised labels):\n\n```html\n<mdc-timepicker\n  name=\"startTime\"\n  label=\"Start time\"\n  time-format=\"12h\"\n  interval=\"15\"\n  locale-hours-label=\"Hours\"\n  locale-minutes-label=\"Minutes\"\n  locale-period-label=\"AM or PM\"\n  locale-hours-placeholder=\"HH\"\n  locale-minutes-placeholder=\"MM\"\n  locale-period-placeholder=\"--\"\n  locale-am-label=\"AM\"\n  locale-pm-label=\"PM\"\n  locale-show-time-picker-label=\"Show time picker\"\n  locale-time-options-label=\"Time options\"\n  locale-spinbutton-description=\"Use arrow keys to adjust, type digits to set\"\n></mdc-timepicker>\n```\n\n### Property/Attribute details\n\n- `name` (string) — form submission key.\n- `value` (string) — current time in 24-hour `HH:MM` format. Values outside `00:00`–`23:59` are rejected; out-of-format strings clear the internal state.\n- `time-format` (`'12h' | '24h'`, default `'12h'`) — `'12h'` renders a third AM/PM spinbutton and clamps hours to 1–12; `'24h'` clamps hours to 0–23 and hides the period segment.\n- `interval` (number, default `30`) — minutes between dropdown options. Clamped to the range 1–60.\n- `min` (string) / `max` (string) — minimum and maximum allowed 24-hour `HH:MM`. Dropdown options outside this range are filtered out.\n- `placement` (string, default `'bottom-start'`) — popover placement.\n- `strategy` (`'absolute' | 'fixed'`, default `'absolute'`) — popover positioning strategy.\n- `disable-flip` (boolean, default `false`) — when `true`, the popover does not flip to fit the viewport.\n- `append-to` (string) — `id` of the element the popover is portalled to. Useful when the time picker is inside a scrollable container.\n- `backdrop-append-to` (string) — `id` of the element the popover backdrop is appended to. Defaults to the parent.\n- `prefix-icon` (string) — name of the icon rendered before the spinbuttons.\n- `option-label-formatter` (function, property only) — `(defaultLabel, value24) => string` callback that customises the label for each dropdown option.\n- `disabled` / `soft-disabled` / `readonly` (boolean) — disable interaction. When any is true, the popover is forced closed.\n- `required` (boolean, default `false`) — renders the required indicator.\n- `label`, `help-text`, `help-text-type`, `validation-message`, `toggletip-text`, `toggletip-placement`, `toggletip-strategy`, `info-icon-aria-label` — label, helper, validation, and toggletip configuration shared with other form fields.\n- `data-aria-label` (string) — accessible label override.\n\nLocalisation properties — **consumers must provide a translated string for each**:\n\n- `locale-hours-label`, `locale-minutes-label`, `locale-period-label` — accessible labels for each spinbutton.\n- `locale-hours-placeholder`, `locale-minutes-placeholder`, `locale-period-placeholder` — placeholder text for each spinbutton.\n- `locale-am-label`, `locale-pm-label` — labels for the AM and PM periods. The first character (lowercased) is also used to set the period when the user types it on the period spinbutton.\n- `locale-show-time-picker-label` — accessible label for the dropdown trigger button.\n- `locale-time-options-label` — accessible label for the dropdown listbox.\n- `locale-spinbutton-description` — `aria-description` text added to every spinbutton with the interaction hint.\n\nThe component dispatches:\n\n- `input` and `change` — bubbling `CustomEvent` with `detail: { value }` fired when the committed value changes (via spinbutton edit or dropdown selection).\n- `focus` / `blur` — native DOM events with React equivalents.\n\nKeyboard behaviour:\n\n- Inside hours/minutes spinbuttons: Arrow Up / Down wrap-increment/decrement the value; Arrow Right moves to the next segment (hours → minutes → period in 12-hour); Arrow Left moves back; typing digits builds the value with auto-advance — two digits commit immediately, a single first digit that already exceeds the maximum first digit also commits, and otherwise a 1 s timeout auto-pads and commits with a single leading digit.\n- Inside the period spinbutton: Arrow Up / Down toggle AM↔PM; Arrow Left moves back to minutes; typing the first character of `locale-am-label` or `locale-pm-label` sets the period directly.\n- Anywhere in the input: Escape closes the popover if open and returns focus to the dropdown trigger.\n- Inside the popover listbox: Arrow Down / Up move focus through options with wrap-around; Enter selects the focused option; Escape closes the popover and returns focus to the trigger.\n\nSlots: `label`, `toggletip`, `help-icon`, `help-text` — for overriding the label, toggletip, helper icon, and helper text rendering.\n\n## Accessibility\n\n### Built-in features\n\n- The component is form-associated and participates in form submission, reset (`formResetCallback`), and state restoration (`formStateRestoreCallback`).\n- Each editable segment is an `<input role=\"spinbutton\">` with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, and `aria-label` set from the `locale-*-label` properties.\n- The period spinbutton uses `aria-valuetext` to read the AM/PM label.\n- Every spinbutton exposes `aria-description` from `locale-spinbutton-description` so screen reader users hear the interaction hint.\n- The dropdown trigger button advertises the popover via `aria-haspopup=\"true\"` and `aria-expanded` mirroring the open state. The popover's own `aria-expanded` management is disabled so the trigger button is the only source of truth.\n- A hidden native `<input>` carries the form value with `tabindex=\"-1\"` and `aria-hidden=\"true\"` so the assistive tree only surfaces the spinbuttons.\n- The popover is connected to the input via `triggerid` and uses `focus-trap`, `focus-back-to-trigger`, `hide-on-escape`, and `hide-on-outside-click` so keyboard users cannot lose focus into the page while the dropdown is open.\n- Focus returns to the dropdown trigger after the user picks an option or dismisses the popover.\n- The selected option is announced via `aria-selected=\"true\"` on the matching `mdc-option`.\n- Spinbutton inputs receive `tabindex=\"-1\"` when the field is disabled, removing them from the tab order.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Hours `<input>` | `role` | `spinbutton` |\n| Hours `<input>` | `aria-label` | value of `locale-hours-label` |\n| Hours `<input>` | `aria-valuemin` | `1` (12-hour) or `0` (24-hour) |\n| Hours `<input>` | `aria-valuemax` | `12` (12-hour) or `23` (24-hour) |\n| Hours `<input>` | `aria-valuenow` | current integer hour, or empty when unset |\n| Hours `<input>` | `aria-description` | value of `locale-spinbutton-description` |\n| Minutes `<input>` | `role` | `spinbutton` |\n| Minutes `<input>` | `aria-label` | value of `locale-minutes-label` |\n| Minutes `<input>` | `aria-valuemin` / `aria-valuemax` | `0` / `59` |\n| Minutes `<input>` | `aria-valuenow` | current integer minutes, or empty when unset |\n| Period `<input>` (12-hour only) | `role` | `spinbutton` |\n| Period `<input>` (12-hour only) | `aria-label` | value of `locale-period-label` |\n| Period `<input>` (12-hour only) | `aria-valuetext` | localised AM/PM label |\n| Dropdown trigger `mdc-button` | `aria-label` | value of `locale-show-time-picker-label` |\n| Dropdown trigger `mdc-button` | `aria-haspopup` | `true` |\n| Dropdown trigger `mdc-button` | `aria-expanded` | `true` when popover is open, `false` otherwise |\n| Listbox `mdc-selectlistbox` | `aria-label` | value of `locale-time-options-label` |\n| Each option | `aria-selected` | `true` for the option matching the current value, `false` otherwise |\n| Hidden native `<input>` | `aria-hidden` | `true` |\n| Hidden native `<input>` | `tabindex` | `-1` |\n| Hidden native `<input>` | `aria-disabled` | `true` when `disabled` or `soft-disabled` |\n\n### Implementation requirements\n\n#### Labelling\n\n- All `locale-*` strings must be provided and translated. Without them the spinbuttons have empty `aria-label`s and screen readers cannot describe what is being edited.\n- Provide a `label` (or a slotted label) to give the field a visible name.\n\n### Notes\n\n- The value contract is always 24-hour `HH:MM` regardless of `time-format`. Display formatting is internal.\n- The popover uses `disable-aria-expanded` so the only `aria-expanded` source is the dropdown trigger button — do not also rely on the popover's own ARIA state.",
    "sections": {
      "Overview": "The time picker is a composite form control for selecting a time. It exposes hour, minute, and (in 12-hour mode) AM/PM segments as ARIA spinbuttons that accept arrow-key adjustments and direct digit input, plus a dropdown arrow button that opens a list of preset times at a configurable interval.\n\n### When to use\n\n- Use when users need to enter or pick a time of day, manually or from a preset list.\n- Use inside a form when the time value should participate in form submission and validation.\n\n### When not to use\n\n- Do not use for date selection — use `mdc-datepicker` instead.\n- Do not use when you only need a free-form text field.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/timepicker/index.js\"; // custom element registration\n// or via React wrapper\nimport { TimePicker } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example (12-hour mode with localised labels):\n\n```html\n<mdc-timepicker\n  name=\"startTime\"\n  label=\"Start time\"\n  time-format=\"12h\"\n  interval=\"15\"\n  locale-hours-label=\"Hours\"\n  locale-minutes-label=\"Minutes\"\n  locale-period-label=\"AM or PM\"\n  locale-hours-placeholder=\"HH\"\n  locale-minutes-placeholder=\"MM\"\n  locale-period-placeholder=\"--\"\n  locale-am-label=\"AM\"\n  locale-pm-label=\"PM\"\n  locale-show-time-picker-label=\"Show time picker\"\n  locale-time-options-label=\"Time options\"\n  locale-spinbutton-description=\"Use arrow keys to adjust, type digits to set\"\n></mdc-timepicker>\n```\n\n### Property/Attribute details\n\n- `name` (string) — form submission key.\n- `value` (string) — current time in 24-hour `HH:MM` format. Values outside `00:00`–`23:59` are rejected; out-of-format strings clear the internal state.\n- `time-format` (`'12h' | '24h'`, default `'12h'`) — `'12h'` renders a third AM/PM spinbutton and clamps hours to 1–12; `'24h'` clamps hours to 0–23 and hides the period segment.\n- `interval` (number, default `30`) — minutes between dropdown options. Clamped to the range 1–60.\n- `min` (string) / `max` (string) — minimum and maximum allowed 24-hour `HH:MM`. Dropdown options outside this range are filtered out.\n- `placement` (string, default `'bottom-start'`) — popover placement.\n- `strategy` (`'absolute' | 'fixed'`, default `'absolute'`) — popover positioning strategy.\n- `disable-flip` (boolean, default `false`) — when `true`, the popover does not flip to fit the viewport.\n- `append-to` (string) — `id` of the element the popover is portalled to. Useful when the time picker is inside a scrollable container.\n- `backdrop-append-to` (string) — `id` of the element the popover backdrop is appended to. Defaults to the parent.\n- `prefix-icon` (string) — name of the icon rendered before the spinbuttons.\n- `option-label-formatter` (function, property only) — `(defaultLabel, value24) => string` callback that customises the label for each dropdown option.\n- `disabled` / `soft-disabled` / `readonly` (boolean) — disable interaction. When any is true, the popover is forced closed.\n- `required` (boolean, default `false`) — renders the required indicator.\n- `label`, `help-text`, `help-text-type`, `validation-message`, `toggletip-text`, `toggletip-placement`, `toggletip-strategy`, `info-icon-aria-label` — label, helper, validation, and toggletip configuration shared with other form fields.\n- `data-aria-label` (string) — accessible label override.\n\nLocalisation properties — **consumers must provide a translated string for each**:\n\n- `locale-hours-label`, `locale-minutes-label`, `locale-period-label` — accessible labels for each spinbutton.\n- `locale-hours-placeholder`, `locale-minutes-placeholder`, `locale-period-placeholder` — placeholder text for each spinbutton.\n- `locale-am-label`, `locale-pm-label` — labels for the AM and PM periods. The first character (lowercased) is also used to set the period when the user types it on the period spinbutton.\n- `locale-show-time-picker-label` — accessible label for the dropdown trigger button.\n- `locale-time-options-label` — accessible label for the dropdown listbox.\n- `locale-spinbutton-description` — `aria-description` text added to every spinbutton with the interaction hint.\n\nThe component dispatches:\n\n- `input` and `change` — bubbling `CustomEvent` with `detail: { value }` fired when the committed value changes (via spinbutton edit or dropdown selection).\n- `focus` / `blur` — native DOM events with React equivalents.\n\nKeyboard behaviour:\n\n- Inside hours/minutes spinbuttons: Arrow Up / Down wrap-increment/decrement the value; Arrow Right moves to the next segment (hours → minutes → period in 12-hour); Arrow Left moves back; typing digits builds the value with auto-advance — two digits commit immediately, a single first digit that already exceeds the maximum first digit also commits, and otherwise a 1 s timeout auto-pads and commits with a single leading digit.\n- Inside the period spinbutton: Arrow Up / Down toggle AM↔PM; Arrow Left moves back to minutes; typing the first character of `locale-am-label` or `locale-pm-label` sets the period directly.\n- Anywhere in the input: Escape closes the popover if open and returns focus to the dropdown trigger.\n- Inside the popover listbox: Arrow Down / Up move focus through options with wrap-around; Enter selects the focused option; Escape closes the popover and returns focus to the trigger.\n\nSlots: `label`, `toggletip`, `help-icon`, `help-text` — for overriding the label, toggletip, helper icon, and helper text rendering.",
      "Accessibility": "### Built-in features\n\n- The component is form-associated and participates in form submission, reset (`formResetCallback`), and state restoration (`formStateRestoreCallback`).\n- Each editable segment is an `<input role=\"spinbutton\">` with `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, and `aria-label` set from the `locale-*-label` properties.\n- The period spinbutton uses `aria-valuetext` to read the AM/PM label.\n- Every spinbutton exposes `aria-description` from `locale-spinbutton-description` so screen reader users hear the interaction hint.\n- The dropdown trigger button advertises the popover via `aria-haspopup=\"true\"` and `aria-expanded` mirroring the open state. The popover's own `aria-expanded` management is disabled so the trigger button is the only source of truth.\n- A hidden native `<input>` carries the form value with `tabindex=\"-1\"` and `aria-hidden=\"true\"` so the assistive tree only surfaces the spinbuttons.\n- The popover is connected to the input via `triggerid` and uses `focus-trap`, `focus-back-to-trigger`, `hide-on-escape`, and `hide-on-outside-click` so keyboard users cannot lose focus into the page while the dropdown is open.\n- Focus returns to the dropdown trigger after the user picks an option or dismisses the popover.\n- The selected option is announced via `aria-selected=\"true\"` on the matching `mdc-option`.\n- Spinbutton inputs receive `tabindex=\"-1\"` when the field is disabled, removing them from the tab order.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Hours `<input>` | `role` | `spinbutton` |\n| Hours `<input>` | `aria-label` | value of `locale-hours-label` |\n| Hours `<input>` | `aria-valuemin` | `1` (12-hour) or `0` (24-hour) |\n| Hours `<input>` | `aria-valuemax` | `12` (12-hour) or `23` (24-hour) |\n| Hours `<input>` | `aria-valuenow` | current integer hour, or empty when unset |\n| Hours `<input>` | `aria-description` | value of `locale-spinbutton-description` |\n| Minutes `<input>` | `role` | `spinbutton` |\n| Minutes `<input>` | `aria-label` | value of `locale-minutes-label` |\n| Minutes `<input>` | `aria-valuemin` / `aria-valuemax` | `0` / `59` |\n| Minutes `<input>` | `aria-valuenow` | current integer minutes, or empty when unset |\n| Period `<input>` (12-hour only) | `role` | `spinbutton` |\n| Period `<input>` (12-hour only) | `aria-label` | value of `locale-period-label` |\n| Period `<input>` (12-hour only) | `aria-valuetext` | localised AM/PM label |\n| Dropdown trigger `mdc-button` | `aria-label` | value of `locale-show-time-picker-label` |\n| Dropdown trigger `mdc-button` | `aria-haspopup` | `true` |\n| Dropdown trigger `mdc-button` | `aria-expanded` | `true` when popover is open, `false` otherwise |\n| Listbox `mdc-selectlistbox` | `aria-label` | value of `locale-time-options-label` |\n| Each option | `aria-selected` | `true` for the option matching the current value, `false` otherwise |\n| Hidden native `<input>` | `aria-hidden` | `true` |\n| Hidden native `<input>` | `tabindex` | `-1` |\n| Hidden native `<input>` | `aria-disabled` | `true` when `disabled` or `soft-disabled` |\n\n### Implementation requirements\n\n#### Labelling\n\n- All `locale-*` strings must be provided and translated. Without them the spinbuttons have empty `aria-label`s and screen readers cannot describe what is being edited.\n- Provide a `label` (or a slotted label) to give the field a visible name.\n\n### Notes\n\n- The value contract is always 24-hour `HH:MM` regardless of `time-format`. Display formatting is internal.\n- The popover uses `disable-aria-expanded` so the only `aria-expanded` source is the dropdown trigger button — do not also rely on the popover's own ARIA state."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-option",
      "mdc-popover",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "appendTo",
          "attribute": "append-to",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "backdropAppendTo",
          "attribute": "backdrop-append-to",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "disableFlip",
          "attribute": "disable-flip",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "Determines whether the dropdown should flip its position when it hits the boundary.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "dropdownButton",
          "type": "HTMLElement",
          "values": [],
          "description": "Element ID that the popover will be appended to.\nThis is useful when the timepicker is inside a scrollable container\nand the popover needs to be portalled to avoid clipping.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "helpTextType",
          "attribute": "help-text-type",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "interval",
          "attribute": "interval",
          "type": "number",
          "default": "30",
          "values": [],
          "description": "The interval in minutes between time options in the dropdown menu.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeAmLabel",
          "attribute": "locale-am-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeHoursLabel",
          "attribute": "locale-hours-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeHoursPlaceholder",
          "attribute": "locale-hours-placeholder",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeMinutesLabel",
          "attribute": "locale-minutes-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeMinutesPlaceholder",
          "attribute": "locale-minutes-placeholder",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localePeriodLabel",
          "attribute": "locale-period-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localePeriodPlaceholder",
          "attribute": "locale-period-placeholder",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localePmLabel",
          "attribute": "locale-pm-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "localeShowTimePickerLabel",
          "attribute": "locale-show-time-picker-label",
          "type": "text",
          "values": [],
          "description": "accessible label for the dropdown trigger button.",
          "source": "storybook"
        },
        {
          "name": "localeSpinbuttonDescription",
          "attribute": "locale-spinbutton-description",
          "type": "text",
          "values": [],
          "description": "`aria-description` text added to every spinbutton with the interaction hint.",
          "source": "storybook"
        },
        {
          "name": "localeTimeOptionsLabel",
          "attribute": "locale-time-options-label",
          "type": "text",
          "values": [],
          "description": "accessible label for the dropdown listbox.",
          "source": "storybook"
        },
        {
          "name": "max",
          "attribute": "max",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "min",
          "attribute": "min",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "Placement",
          "default": "'bottom-start'",
          "values": [
            "bottom-start",
            "top-start"
          ],
          "description": "The placement of the popover dropdown.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "strategy",
          "attribute": "strategy",
          "type": "PopoverStrategy",
          "default": "'absolute'",
          "values": [],
          "description": "The strategy for positioning the popover.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "timeFormat",
          "attribute": "time-format",
          "type": "select",
          "default": "'12h'",
          "values": [
            "12h",
            "24h"
          ],
          "description": "/\nclass TimePicker extends FormInternalsMixin(DataAriaLabelMixin(FormfieldWrapper)) implements AssociatedFormControl {\n/**\nThe time format to use for display.\n- `'12h'`: 12-hour format with AM/PM period\n- `'24h'`: 24-hour format without period",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "label",
          "description": "Slot for the label element."
        },
        {
          "name": "toggletip",
          "description": "Slot for the toggletip info icon button."
        },
        {
          "name": "help-icon",
          "description": "Slot for the helper/validation icon."
        },
        {
          "name": "help-text",
          "description": "Slot for the helper/validation text."
        }
      ],
      "events": [
        {
          "name": "input",
          "description": "(React: onInput) This event is dispatched when the time value changes."
        },
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the time value is committed."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) This event is dispatched when the timepicker receives focus."
        },
        {
          "name": "blur",
          "description": "(React: onBlur) This event is dispatched when the timepicker loses focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element."
        },
        {
          "name": "help-text-container",
          "description": "The container for helper/validation elements."
        },
        {
          "name": "container",
          "description": "The outer container for the input and popover."
        },
        {
          "name": "base-container",
          "description": "The input container with border and background."
        },
        {
          "name": "spinbutton-group",
          "description": "The container for spinbutton elements."
        },
        {
          "name": "spinbutton",
          "description": "A spinbutton input element."
        },
        {
          "name": "separator",
          "description": "The colon separator between spinbuttons."
        },
        {
          "name": "period",
          "description": "The AM/PM period spinbutton."
        },
        {
          "name": "icon-container",
          "description": "The dropdown arrow button container."
        },
        {
          "name": "leading-icon",
          "description": "The prefix icon element displayed before spinbuttons."
        },
        {
          "name": "native-input",
          "description": "The hidden native input for form participation."
        },
        {
          "name": "listbox",
          "description": "The dropdown list container."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-timepicker-background-color",
          "description": "Background color of the timepicker input."
        },
        {
          "name": "--mdc-timepicker-border-color",
          "description": "Border color of the timepicker input."
        },
        {
          "name": "--mdc-timepicker-text-color",
          "description": "Text color of the timepicker input."
        },
        {
          "name": "--mdc-timepicker-height",
          "description": "The height of the timepicker trigger control."
        },
        {
          "name": "--mdc-timepicker-width",
          "description": "Width of the timepicker component."
        },
        {
          "name": "--mdc-timepicker-listbox-height",
          "description": "The max-height of the dropdown listbox. Default shows ~6 items."
        }
      ],
      "rules": [
        "accessible label for the dropdown trigger button.",
        "accessible label for the dropdown listbox.",
        "`aria-description` text added to every spinbutton with the interaction hint.",
        "native DOM events with React equivalents.",
        "native DOM events with React equivalents."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/timepicker/index.js\"; // custom element registration\n// or via React wrapper\nimport { TimePicker } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-timepicker\n  name=\"startTime\"\n  label=\"Start time\"\n  time-format=\"12h\"\n  interval=\"15\"\n  locale-hours-label=\"Hours\"\n  locale-minutes-label=\"Minutes\"\n  locale-period-label=\"AM or PM\"\n  locale-hours-placeholder=\"HH\"\n  locale-minutes-placeholder=\"MM\"\n  locale-period-placeholder=\"--\"\n  locale-am-label=\"AM\"\n  locale-pm-label=\"PM\"\n  locale-show-time-picker-label=\"Show time picker\"\n  locale-time-options-label=\"Time options\"\n  locale-spinbutton-description=\"Use arrow keys to adjust, type digits to set\"\n></mdc-timepicker>"
      }
    ],
    "reactImports": [
      "TimePicker"
    ],
    "constants": {
      "ARROW_ICON": {
        "ARROW_UP": "'arrow-up-bold' as Extract<IconNames, 'arrow-up-bold'>",
        "ARROW_DOWN": "'arrow-down-bold' as Extract<IconNames, 'arrow-down-bold'>"
      },
      "TIME_FORMAT": {
        "TWELVE_HOUR": "12h",
        "TWENTY_FOUR_HOUR": "24h"
      },
      "DEFAULTS": {
        "TIME_FORMAT": "12h",
        "INTERVAL": 30,
        "MIN_HOUR_12": 1,
        "MAX_HOUR_12": 12,
        "MIN_HOUR_24": 0,
        "MAX_HOUR_24": 23,
        "MIN_MINUTE": 0,
        "MAX_MINUTE": 59,
        "DISABLE_FLIP": false
      }
    },
    "keywords": [
      "12",
      "24",
      "a",
      "accessibility",
      "and",
      "associated",
      "button",
      "component",
      "control",
      "entering",
      "for",
      "form",
      "format",
      "guidelines",
      "hour",
      "icon",
      "in",
      "mdc",
      "option",
      "or",
      "overview",
      "picker",
      "popover",
      "selecting",
      "text",
      "the",
      "time",
      "timepicker",
      "toggletip",
      "usage"
    ]
  },
  {
    "name": "toast",
    "title": "Toast",
    "tagName": "mdc-toast",
    "summary": "Usage, guidelines, and accessibility for the mdc-toast component — a lightweight, non-blocking alert used to inform users about application processes.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/toast",
      "knowledgeBasePath": "components/src/components/toast/knowledge-base/toast.component.md"
    },
    "docs": "## Overview\n\n`mdc-toast` is a lightweight, non-blocking alert used to inform users about application processes. It supports `success`, `warning`, `error`, and `custom` variants, can optionally collapse/expand a detailed body, and is designed to be controlled externally (the host application decides when to show, hide, or auto-dismiss it).\n\n### When to use\n\n- Surface short status messages about background or asynchronous processes (success confirmations, recoverable errors, warnings).\n- Provide a place to expose primary/secondary actions tied to a transient notification (e.g. \"Undo\", \"Retry\").\n- Show detailed information that the user can optionally expand via a show more/show less toggle.\n\n### When not to use\n\n- Use `mdc-banner` for persistent, in-flow messaging tied to a region of the page.\n- Use `mdc-announcementdialog` or `mdc-dialog` for modal interruptions that require acknowledgement.\n- Use `mdc-alertchip` for status indicators inline with content rather than a floating notification surface.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/toast/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toast } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-toast\n  variant=\"success\"\n  header-text=\"File uploaded\"\n  close-button-aria-label=\"Close notification\"\n>\n  <mdc-text slot=\"toast-body-normal\" tagname=\"span\">Your file is ready to share.</mdc-text>\n</mdc-toast>\n```\n\nListen for the `close` event on the host application to remove the toast from the DOM when the user dismisses it; the component itself does not auto-hide.\n\n### Content guidance\n\n- When populating `toast-body-normal` or `toast-body-detailed`, wrap the content in `<mdc-text tagname=\"span\">` so typography aligns with the toast's design.\n- Keep `header-text` concise; it renders with the typography of a bold body-large heading.\n- Provide `show-more-text` and `show-less-text` together with detailed body content so users can expand and collapse it.\n\n### Property/Attribute details\n\n- `variant` — one of `custom` (default), `success`, `warning`, `error`. Non-custom variants render a built-in prefix icon. With `custom`, supply your own icon via the `content-prefix` slot; otherwise no icon will be shown.\n- `header-text` — string title rendered at the top of the toast.\n- `header-tag-name` — HTML tag used for the header text element (default `h2`); set this to align with the surrounding document outline.\n- `aria-label` — fallback label used when `header-text` is not provided.\n- `close-button-aria-label` — accessible name for the built-in close button.\n- `show-more-text` / `show-less-text` — labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render.\n- The `close` event (`onClose` in React) is dispatched when the user activates the close button via mouse or keyboard. The event detail includes the toast's `id`.\n- Footer slots `footer-button-primary` and `footer-button-secondary` are filtered to a single `mdc-button` each; additional instances are removed. Custom footer markup can be passed via the generic `footer` slot.\n\n### Limitations\n\n- The component does not implement auto-dismiss timing or stacking; lifecycle and positioning are owned by the consuming application.\n- Whether the header text needs the show more/less toggle is measured once on first update; later changes to `header-text` will not re-evaluate overflow.\n- Automatic truncation is only applied to the header, and is not applied to the body slots.\n\n## Accessibility\n\n### Built-in features\n\nThe close button is a tertiary `mdc-button` whose accessible name is taken from `close-button-aria-label`. The expand/collapse toggle for detailed content is also keyboard accessible via `mdc-button`. When the detailed body is expanded, the host element receives a `data-expanded=\"true\"` attribute so styles and tests can react to the state.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-toast`) | `aria-label` | The consumer-supplied `aria-label`, used when no `header-text` is provided |\n| Close button | `aria-label` | The consumer-supplied `close-button-aria-label` |\n| Header text element | tag | The value of `header-tag-name` (default `h2`) so the toast contributes to the document outline |\n\n### Implementation requirements\n\n#### General\n\n- Always supply either a `header-text` or an `aria-label` so the toast has an accessible name.\n- Provide `close-button-aria-label` so users of assistive technologies can identify the close action.\n- Decide on the live-region semantics at the host level: render the toast inside an element with the appropriate `role=\"alert\"`, `role=\"status\"`, or `aria-live` region so screen readers announce it when it appears. The component itself does not declare a live region.\n\n#### Labelling\n\n- Use `header-text` for short, human-readable titles; the heading level is controlled by `header-tag-name`.\n- When the header is omitted, set `aria-label` to describe the toast's purpose.\n- For the `custom` variant, any icon placed in the `content-prefix` slot should either be decorative (visually only) or be given its own accessible name via `aria-label`.\n\n### Notes\n\n- The `close` event bubbles and is composed, so listeners on ancestor elements (including across shadow DOM) will receive it.",
    "sections": {
      "Overview": "`mdc-toast` is a lightweight, non-blocking alert used to inform users about application processes. It supports `success`, `warning`, `error`, and `custom` variants, can optionally collapse/expand a detailed body, and is designed to be controlled externally (the host application decides when to show, hide, or auto-dismiss it).\n\n### When to use\n\n- Surface short status messages about background or asynchronous processes (success confirmations, recoverable errors, warnings).\n- Provide a place to expose primary/secondary actions tied to a transient notification (e.g. \"Undo\", \"Retry\").\n- Show detailed information that the user can optionally expand via a show more/show less toggle.\n\n### When not to use\n\n- Use `mdc-banner` for persistent, in-flow messaging tied to a region of the page.\n- Use `mdc-announcementdialog` or `mdc-dialog` for modal interruptions that require acknowledgement.\n- Use `mdc-alertchip` for status indicators inline with content rather than a floating notification surface.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/toast/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toast } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-toast\n  variant=\"success\"\n  header-text=\"File uploaded\"\n  close-button-aria-label=\"Close notification\"\n>\n  <mdc-text slot=\"toast-body-normal\" tagname=\"span\">Your file is ready to share.</mdc-text>\n</mdc-toast>\n```\n\nListen for the `close` event on the host application to remove the toast from the DOM when the user dismisses it; the component itself does not auto-hide.\n\n### Content guidance\n\n- When populating `toast-body-normal` or `toast-body-detailed`, wrap the content in `<mdc-text tagname=\"span\">` so typography aligns with the toast's design.\n- Keep `header-text` concise; it renders with the typography of a bold body-large heading.\n- Provide `show-more-text` and `show-less-text` together with detailed body content so users can expand and collapse it.\n\n### Property/Attribute details\n\n- `variant` — one of `custom` (default), `success`, `warning`, `error`. Non-custom variants render a built-in prefix icon. With `custom`, supply your own icon via the `content-prefix` slot; otherwise no icon will be shown.\n- `header-text` — string title rendered at the top of the toast.\n- `header-tag-name` — HTML tag used for the header text element (default `h2`); set this to align with the surrounding document outline.\n- `aria-label` — fallback label used when `header-text` is not provided.\n- `close-button-aria-label` — accessible name for the built-in close button.\n- `show-more-text` / `show-less-text` — labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render.\n- The `close` event (`onClose` in React) is dispatched when the user activates the close button via mouse or keyboard. The event detail includes the toast's `id`.\n- Footer slots `footer-button-primary` and `footer-button-secondary` are filtered to a single `mdc-button` each; additional instances are removed. Custom footer markup can be passed via the generic `footer` slot.\n\n### Limitations\n\n- The component does not implement auto-dismiss timing or stacking; lifecycle and positioning are owned by the consuming application.\n- Whether the header text needs the show more/less toggle is measured once on first update; later changes to `header-text` will not re-evaluate overflow.\n- Automatic truncation is only applied to the header, and is not applied to the body slots.",
      "Accessibility": "### Built-in features\n\nThe close button is a tertiary `mdc-button` whose accessible name is taken from `close-button-aria-label`. The expand/collapse toggle for detailed content is also keyboard accessible via `mdc-button`. When the detailed body is expanded, the host element receives a `data-expanded=\"true\"` attribute so styles and tests can react to the state.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-toast`) | `aria-label` | The consumer-supplied `aria-label`, used when no `header-text` is provided |\n| Close button | `aria-label` | The consumer-supplied `close-button-aria-label` |\n| Header text element | tag | The value of `header-tag-name` (default `h2`) so the toast contributes to the document outline |\n\n### Implementation requirements\n\n#### General\n\n- Always supply either a `header-text` or an `aria-label` so the toast has an accessible name.\n- Provide `close-button-aria-label` so users of assistive technologies can identify the close action.\n- Decide on the live-region semantics at the host level: render the toast inside an element with the appropriate `role=\"alert\"`, `role=\"status\"`, or `aria-live` region so screen readers announce it when it appears. The component itself does not declare a live region.\n\n#### Labelling\n\n- Use `header-text` for short, human-readable titles; the heading level is controlled by `header-tag-name`.\n- When the header is omitted, set `aria-label` to describe the toast's purpose.\n- For the `custom` variant, any icon placed in the `content-prefix` slot should either be decorative (visually only) or be given its own accessible name via `aria-label`.\n\n### Notes\n\n- The `close` event bubbles and is composed, so listeners on ancestor elements (including across shadow DOM) will receive it."
    },
    "dependencies": [
      "mdc-icon",
      "mdc-text",
      "mdc-button"
    ],
    "api": {
      "properties": [
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "string | null",
          "default": "null",
          "values": [],
          "description": "fallback label used when `header-text` is not provided.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "closeButtonAriaLabel",
          "attribute": "close-button-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name for the built-in close button.",
          "source": "storybook"
        },
        {
          "name": "detailedBody",
          "attribute": "detailed-body",
          "type": "text",
          "values": [],
          "description": "Content for the detailed body of the toast, shown when expanded (slot: toast-body-detailed)",
          "source": "storybook"
        },
        {
          "name": "headerTagName",
          "attribute": "header-tag-name",
          "type": "select",
          "default": "DEFAULTS.HEADER_TAG_NAME",
          "values": [],
          "description": "HTML tag used for the header text element (default `h2`); set this to align with the surrounding document outline.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "headerText",
          "attribute": "header-text",
          "type": "text",
          "values": [],
          "description": "string title rendered at the top of the toast.",
          "source": "storybook"
        },
        {
          "name": "isDetailVisible",
          "attribute": "show-more-text",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "normalBody",
          "attribute": "normal-body",
          "type": "text",
          "values": [],
          "description": "Content for the main body of the toast (slot: toast-body-normal)",
          "source": "storybook"
        },
        {
          "name": "prefixIcon",
          "attribute": "prefix-icon",
          "type": "text",
          "values": [],
          "description": "Icon name for the content prefix (slot: content-prefix). Only for custom variant.",
          "source": "storybook"
        },
        {
          "name": "showLessText",
          "attribute": "show-less-text",
          "type": "text",
          "values": [],
          "description": "labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render.",
          "source": "storybook"
        },
        {
          "name": "showMoreText",
          "attribute": "show-more-text",
          "type": "text",
          "values": [],
          "description": "labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render.",
          "source": "storybook"
        },
        {
          "name": "variant",
          "attribute": "variant",
          "type": "select",
          "default": "'custom'",
          "values": [
            "custom",
            "success",
            "warning",
            "error"
          ],
          "description": "one of `custom` (default), `success`, `warning`, `error`. Non-custom variants render a built-in prefix icon. With `custom`, supply your own icon via the `content-prefix` slot; otherwise no icon will be shown.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "content-prefix",
          "description": "Slot for custom content before the icon (only for custom variant)."
        },
        {
          "name": "toast-body-normal",
          "description": "Slot for the main body content of the toast."
        },
        {
          "name": "toast-body-detailed",
          "description": "Slot for additional detailed content, shown when expanded."
        },
        {
          "name": "footer",
          "description": "Slot for custom footer content. Prefer using footer-button-primary and footer-button-secondary slots."
        },
        {
          "name": "footer-button-primary",
          "description": "Slot for passing the primary variant of `mdc-button` in the footer."
        },
        {
          "name": "footer-button-secondary",
          "description": "Slot for passing the secondary variant of `mdc-button` in the footer."
        }
      ],
      "events": [
        {
          "name": "close",
          "description": "(React: onClose) Dispatched when the Close Button is clicked using mouse or keyboard."
        }
      ],
      "cssParts": [
        {
          "name": "content-container",
          "description": "The container for the toast's main content, including icon, text, and close button."
        },
        {
          "name": "toast-prefix-icon",
          "description": "The icon shown at the start of the toast, styled by variant."
        },
        {
          "name": "toast-content",
          "description": "The container for the header and body content of the toast."
        },
        {
          "name": "toast-header",
          "description": "The header text of the toast."
        },
        {
          "name": "footer",
          "description": "The container for the toast's footer, including toggle and action buttons."
        },
        {
          "name": "footer-button-toggle",
          "description": "The toggle button for showing/hiding detailed content."
        },
        {
          "name": "toast-close-btn",
          "description": "The close button for the toast."
        },
        {
          "name": "has-footer-buttons",
          "description": "Applied to the footer when it contains buttons or a toggle button."
        }
      ],
      "cssProperties": [
        {
          "name": "--mdc-toast-background-color",
          "description": "Background color of the toast."
        },
        {
          "name": "--mdc-toast-border-color",
          "description": "Border color of the toast."
        },
        {
          "name": "--mdc-toast-header-text-color",
          "description": "Color of the header text in the toast."
        },
        {
          "name": "--mdc-toast-icon-color",
          "description": "Color of the icon in the toast."
        },
        {
          "name": "--mdc-toast-elevation-3",
          "description": "Elevation effect applied to the toast."
        },
        {
          "name": "--mdc-toast-width",
          "description": "Width of the toast."
        },
        {
          "name": "--mdc-toast-padding",
          "description": "Padding inside the toast."
        }
      ],
      "rules": [
        "one of `custom` (default), `success`, `warning`, `error`. Non-custom variants render a built-in prefix icon. With `custom`, supply your own icon via the `content-prefix` slot; otherwise no icon will be shown.",
        "string title rendered at the top of the toast.",
        "HTML tag used for the header text element (default `h2`); set this to align with the surrounding document outline.",
        "fallback label used when `header-text` is not provided.",
        "accessible name for the built-in close button.",
        "labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render.",
        "labels for the toggle button that reveals or hides the detailed body. Both must be set together with detailed content for the toggle to render."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/toast/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toast } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-toast\n  variant=\"success\"\n  header-text=\"File uploaded\"\n  close-button-aria-label=\"Close notification\"\n>\n  <mdc-text slot=\"toast-body-normal\" tagname=\"span\">Your file is ready to share.</mdc-text>\n</mdc-toast>"
      }
    ],
    "reactImports": [
      "Toast"
    ],
    "constants": {
      "TOAST_VARIANT": {
        "CUSTOM": "custom",
        "SUCCESS": "success",
        "WARNING": "warning",
        "ERROR": "error"
      },
      "VARIANT_ICON_NAMES": {
        "SUCCESS_ICON_NAME": "'check-circle-bold' as Extract<IconNames, 'check-circle-bold'>",
        "WARNING_ICON_NAME": "'warning-bold' as Extract<IconNames, 'warning-bold'>",
        "ERROR_ICON_NAME": "'error-legacy-bold' as Extract<IconNames, 'error-legacy-bold'>"
      },
      "DEFAULTS": {
        "HEADER_TAG_NAME": "h2",
        "CANCEL_ICON": "'cancel-bold' as Extract<IconNames, 'cancel-bold'>",
        "ARROW_UP_BOLD": "'arrow-up-bold' as Extract<IconNames, 'arrow-up-bold'>",
        "ARROW_DOWN_BOLD": "'arrow-down-bold' as Extract<IconNames, 'arrow-down-bold'>",
        "PREFIX_ICON_SIZE": 1.5,
        "BUTTON": "BUTTON_TAGNAME",
        "VARIANT": "custom"
      }
    },
    "keywords": [
      "a",
      "about",
      "accessibility",
      "alert",
      "and",
      "application",
      "blocking",
      "button",
      "component",
      "for",
      "guidelines",
      "icon",
      "inform",
      "lightweight",
      "mdc",
      "non",
      "overview",
      "processes",
      "text",
      "the",
      "to",
      "toast",
      "usage",
      "used",
      "users"
    ]
  },
  {
    "name": "toggle",
    "title": "Toggle",
    "tagName": "mdc-toggle",
    "summary": "Usage, guidelines, and accessibility for the mdc-toggle component — a form-associated switch for binary on/off settings.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/toggle",
      "knowledgeBasePath": "components/src/components/toggle/knowledge-base/toggle.component.md"
    },
    "docs": "## Overview\n\n`mdc-toggle` is an interactive switch control used to flip between two mutually exclusive states, such as on/off or active/inactive. Changes take effect immediately and the component participates in HTML forms via the platform's element internals API.\n\n### When to use\n\n- Binary settings where the change applies immediately (e.g. enabling a feature, switching a preference on or off).\n- Inside settings panels, preference dialogs, or forms where users need to flip a single option.\n- As a member of a group of related options grouped together via `mdc-formfieldgroup`.\n\n### When not to use\n\n- Use `mdc-checkbox` for options that require an explicit submit step before they take effect, or for selecting multiple items from a list.\n- Use `mdc-radio` (with `mdc-radiogroup`) when a user must choose one of several mutually exclusive options.\n- Use `mdc-button` for one-off actions that don't represent a persistent state.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/toggle/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toggle } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-toggle\n  name=\"notifications\"\n  label=\"Enable notifications\"\n  help-text=\"You can change this at any time\"\n></mdc-toggle>\n```\n\n### Property/Attribute details\n\n- `checked` — boolean reflecting the current state of the toggle. Defaults to `false`. Set this to bind the toggle to your application state.\n- `size` — `default` (1.5rem height, default) or `compact` (1rem height) for space-constrained layouts. Invalid sizes fall back to `default`.\n- `name` — name submitted with the surrounding `<form>`.\n- `value` — value submitted when the toggle is checked. If omitted, the literal `isActive` is used as the form value when `checked` is `true`.\n- `required` — when `true` and the toggle is not checked, the component reports the toggle as invalid; if `validation-message` is also set, that string is used as the custom validity message.\n- `validation-message` — overrides the default browser validation message when the toggle is invalid.\n- `disabled` / `soft-disabled` / `readonly` — `disabled` removes the toggle from the tab order and prevents interaction; `soft-disabled` keeps it focusable while preventing state changes; `readonly` prevents state changes but allows focus.\n- `label` — visible label rendered next to the toggle and linked via `for`/`id`.\n- `help-text` — helper text rendered beneath the toggle.\n- `toggletip-text` — when set, renders an info button beside the label that opens an `mdc-toggletip`; `info-icon-aria-label`, `toggletip-placement`, and `toggletip-strategy` configure that toggletip.\n- `auto-focus-on-mount` — when `true`, the underlying input is focused automatically when the component is mounted.\n- `data-aria-label` — used as the input's `aria-label` when there is no visible label.\n- `control-type` — set to `controlled` to opt out of the component flipping `checked` on click; the consumer is then responsible for updating the property.\n- The `change` event (`onChange` in React) fires when the toggle state changes and re-dispatches the underlying input event. The `focus` event (`onFocus`) fires when the input receives focus.\n- Resetting the parent form sets `checked` back to `false`; the toggle's form state is restored from a non-empty restoration value as `checked === true`.\n\n### Limitations\n\n- Toggles are intended for immediate-effect settings, so they do not expose a `helpTextType` and do not render validation icons; only the plain helper text and the underlying form-control validity API are used.\n\n## Accessibility\n\n### Built-in features\n\n- The internal control is a native checkbox input with `role=\"switch\"`, ensuring assistive technologies announce it as a switch with an on/off state.\n- The host element delegates focus to the input via shadow root focus delegation, so focus styling and tab order behave naturally.\n- Pressing Space toggles the state (suppressed while `readonly` or `soft-disabled`). Pressing Enter submits the surrounding form when the keyboard navigation mode is `DEFAULT`.\n- The component is form-associated: it participates in form submission, reset, autofill restoration, and validity reporting via `ElementInternals`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Input (`type=\"checkbox\"`) | `role` | `switch` |\n| Input | `aria-checked` | Mirrors the current `checked` state (`true`/`false`) |\n| Input | `aria-label` | Value of `data-aria-label` when set |\n| Input | `aria-describedby` | The id of the help text element when `help-text` is provided |\n| Input | `tabindex` | `-1` when `disabled`, otherwise `0` |\n| Label | `for` | Matches the input's auto-generated `id` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Prefer the `label` attribute (or a slotted `label` element) so users see a visible label alongside the toggle.\n- When no visible label is possible, set `data-aria-label` on the host so the underlying input has an accessible name.\n- When supplying additional context via `toggletip-text`, also set `info-icon-aria-label` so the info button has an accessible name.\n\n### Notes\n\n- The keyboard navigation mode is provided by the surrounding spatial-navigation context; when that mode is not `DEFAULT`, Enter will not request form submission.",
    "sections": {
      "Overview": "`mdc-toggle` is an interactive switch control used to flip between two mutually exclusive states, such as on/off or active/inactive. Changes take effect immediately and the component participates in HTML forms via the platform's element internals API.\n\n### When to use\n\n- Binary settings where the change applies immediately (e.g. enabling a feature, switching a preference on or off).\n- Inside settings panels, preference dialogs, or forms where users need to flip a single option.\n- As a member of a group of related options grouped together via `mdc-formfieldgroup`.\n\n### When not to use\n\n- Use `mdc-checkbox` for options that require an explicit submit step before they take effect, or for selecting multiple items from a list.\n- Use `mdc-radio` (with `mdc-radiogroup`) when a user must choose one of several mutually exclusive options.\n- Use `mdc-button` for one-off actions that don't represent a persistent state.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/toggle/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toggle } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-toggle\n  name=\"notifications\"\n  label=\"Enable notifications\"\n  help-text=\"You can change this at any time\"\n></mdc-toggle>\n```\n\n### Property/Attribute details\n\n- `checked` — boolean reflecting the current state of the toggle. Defaults to `false`. Set this to bind the toggle to your application state.\n- `size` — `default` (1.5rem height, default) or `compact` (1rem height) for space-constrained layouts. Invalid sizes fall back to `default`.\n- `name` — name submitted with the surrounding `<form>`.\n- `value` — value submitted when the toggle is checked. If omitted, the literal `isActive` is used as the form value when `checked` is `true`.\n- `required` — when `true` and the toggle is not checked, the component reports the toggle as invalid; if `validation-message` is also set, that string is used as the custom validity message.\n- `validation-message` — overrides the default browser validation message when the toggle is invalid.\n- `disabled` / `soft-disabled` / `readonly` — `disabled` removes the toggle from the tab order and prevents interaction; `soft-disabled` keeps it focusable while preventing state changes; `readonly` prevents state changes but allows focus.\n- `label` — visible label rendered next to the toggle and linked via `for`/`id`.\n- `help-text` — helper text rendered beneath the toggle.\n- `toggletip-text` — when set, renders an info button beside the label that opens an `mdc-toggletip`; `info-icon-aria-label`, `toggletip-placement`, and `toggletip-strategy` configure that toggletip.\n- `auto-focus-on-mount` — when `true`, the underlying input is focused automatically when the component is mounted.\n- `data-aria-label` — used as the input's `aria-label` when there is no visible label.\n- `control-type` — set to `controlled` to opt out of the component flipping `checked` on click; the consumer is then responsible for updating the property.\n- The `change` event (`onChange` in React) fires when the toggle state changes and re-dispatches the underlying input event. The `focus` event (`onFocus`) fires when the input receives focus.\n- Resetting the parent form sets `checked` back to `false`; the toggle's form state is restored from a non-empty restoration value as `checked === true`.\n\n### Limitations\n\n- Toggles are intended for immediate-effect settings, so they do not expose a `helpTextType` and do not render validation icons; only the plain helper text and the underlying form-control validity API are used.",
      "Accessibility": "### Built-in features\n\n- The internal control is a native checkbox input with `role=\"switch\"`, ensuring assistive technologies announce it as a switch with an on/off state.\n- The host element delegates focus to the input via shadow root focus delegation, so focus styling and tab order behave naturally.\n- Pressing Space toggles the state (suppressed while `readonly` or `soft-disabled`). Pressing Enter submits the surrounding form when the keyboard navigation mode is `DEFAULT`.\n- The component is form-associated: it participates in form submission, reset, autofill restoration, and validity reporting via `ElementInternals`.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Input (`type=\"checkbox\"`) | `role` | `switch` |\n| Input | `aria-checked` | Mirrors the current `checked` state (`true`/`false`) |\n| Input | `aria-label` | Value of `data-aria-label` when set |\n| Input | `aria-describedby` | The id of the help text element when `help-text` is provided |\n| Input | `tabindex` | `-1` when `disabled`, otherwise `0` |\n| Label | `for` | Matches the input's auto-generated `id` |\n\n### Implementation requirements\n\n#### Labelling\n\n- Prefer the `label` attribute (or a slotted `label` element) so users see a visible label alongside the toggle.\n- When no visible label is possible, set `data-aria-label` on the host so the underlying input has an accessible name.\n- When supplying additional context via `toggletip-text`, also set `info-icon-aria-label` so the info button has an accessible name.\n\n### Notes\n\n- The keyboard navigation mode is provided by the surrounding spatial-navigation context; when that mode is not `DEFAULT`, Enter will not request form submission."
    },
    "dependencies": [
      "mdc-button",
      "mdc-icon",
      "mdc-statictoggle",
      "mdc-text",
      "mdc-toggletip"
    ],
    "api": {
      "properties": [
        {
          "name": "autoFocusOnMount",
          "attribute": "auto-focus-on-mount",
          "type": "boolean",
          "values": [],
          "description": "when `true`, the underlying input is focused automatically when the component is mounted.",
          "source": "storybook"
        },
        {
          "name": "checked",
          "attribute": "checked",
          "type": "boolean",
          "values": [],
          "description": "boolean reflecting the current state of the toggle. Defaults to `false`. Set this to bind the toggle to your application state.",
          "source": "storybook"
        },
        {
          "name": "controlType",
          "attribute": "control-type",
          "type": "select",
          "values": [],
          "description": "set to `controlled` to opt out of the component flipping `checked` on click; the consumer is then responsible for updating the property.",
          "source": "storybook"
        },
        {
          "name": "dataAriaLabel",
          "attribute": "data-aria-label",
          "type": "text",
          "values": [],
          "description": "used as the input's `aria-label` when there is no visible label.",
          "source": "storybook"
        },
        {
          "name": "disabled",
          "attribute": "disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "helpText",
          "attribute": "help-text",
          "type": "text",
          "values": [],
          "description": "helper text rendered beneath the toggle.",
          "source": "storybook"
        },
        {
          "name": "infoIconAriaLabel",
          "attribute": "info-icon-aria-label",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "label",
          "attribute": "label",
          "type": "text",
          "values": [],
          "description": "visible label rendered next to the toggle and linked via `for`/`id`.",
          "source": "storybook"
        },
        {
          "name": "name",
          "attribute": "name",
          "type": "text",
          "values": [],
          "description": "name submitted with the surrounding `<form>`.",
          "source": "storybook"
        },
        {
          "name": "readonly",
          "attribute": "readonly",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "required",
          "attribute": "required",
          "type": "boolean",
          "values": [],
          "description": "when `true` and the toggle is not checked, the component reports the toggle as invalid; if `validation-message` is also set, that string is used as the custom validity message.",
          "source": "storybook"
        },
        {
          "name": "size",
          "attribute": "size",
          "type": "inline-radio",
          "default": "false",
          "values": [
            "default",
            "compact"
          ],
          "description": "`default` (1.5rem height, default) or `compact` (1rem height) for space-constrained layouts. Invalid sizes fall back to `default`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "softDisabled",
          "attribute": "soft-disabled",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipPlacement",
          "attribute": "toggletip-placement",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipStrategy",
          "attribute": "toggletip-strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "toggletipText",
          "attribute": "toggletip-text",
          "type": "text",
          "values": [],
          "description": "when set, renders an info button beside the label that opens an `mdc-toggletip`; `info-icon-aria-label`, `toggletip-placement`, and `toggletip-strategy` configure that toggletip.",
          "source": "storybook"
        },
        {
          "name": "value",
          "attribute": "value",
          "type": "text",
          "values": [],
          "description": "value submitted when the toggle is checked. If omitted, the literal `isActive` is used as the form value when `checked` is `true`.",
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) Event that gets dispatched when the toggle state changes."
        },
        {
          "name": "focus",
          "description": "(React: onFocus) Event that gets dispatched when the toggle receives focus."
        }
      ],
      "cssParts": [
        {
          "name": "label",
          "description": "The label element."
        },
        {
          "name": "label-text",
          "description": "The container for the label and required indicator elements."
        },
        {
          "name": "required-indicator",
          "description": "The required indicator element that is displayed next to the label when the `required` property is set to true."
        },
        {
          "name": "info-icon-btn",
          "description": "The info icon button element that is displayed next to the label when the `toggletip-text` property is set."
        },
        {
          "name": "label-toggletip",
          "description": "The toggletip element that is displayed when the info icon button is clicked."
        },
        {
          "name": "help-text",
          "description": "The helper/validation text element."
        },
        {
          "name": "helper-icon",
          "description": "The helper/validation icon element that is displayed next to the helper/validation text."
        },
        {
          "name": "help-text-container",
          "description": "The container for the helper/validation icon and text elements."
        },
        {
          "name": "text-container",
          "description": "The container for the label and helper text elements."
        },
        {
          "name": "static-toggle",
          "description": "The statictoggle that provides the visual toggle switch appearance."
        },
        {
          "name": "toggle-input",
          "description": "The native input element with switch role that provides the interactive functionality."
        }
      ],
      "cssProperties": [],
      "rules": [
        "boolean reflecting the current state of the toggle. Defaults to `false`. Set this to bind the toggle to your application state.",
        "`default` (1.5rem height, default) or `compact` (1rem height) for space-constrained layouts. Invalid sizes fall back to `default`.",
        "name submitted with the surrounding `<form>`.",
        "value submitted when the toggle is checked. If omitted, the literal `isActive` is used as the form value when `checked` is `true`.",
        "when `true` and the toggle is not checked, the component reports the toggle as invalid; if `validation-message` is also set, that string is used as the custom validity message.",
        "overrides the default browser validation message when the toggle is invalid.",
        "visible label rendered next to the toggle and linked via `for`/`id`.",
        "helper text rendered beneath the toggle.",
        "when set, renders an info button beside the label that opens an `mdc-toggletip`; `info-icon-aria-label`, `toggletip-placement`, and `toggletip-strategy` configure that toggletip.",
        "when `true`, the underlying input is focused automatically when the component is mounted.",
        "used as the input's `aria-label` when there is no visible label.",
        "set to `controlled` to opt out of the component flipping `checked` on click; the consumer is then responsible for updating the property."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/toggle/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toggle } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-toggle\n  name=\"notifications\"\n  label=\"Enable notifications\"\n  help-text=\"You can change this at any time\"\n></mdc-toggle>"
      }
    ],
    "reactImports": [
      "Toggle"
    ],
    "constants": {
      "TOGGLE_SIZE": {
        "DEFAULT": "default",
        "COMPACT": "compact"
      },
      "ICON_NAME": {
        "CHECKED": "'check-bold' as Extract<IconNames, 'check-bold'>",
        "UNCHECKED": "'cancel-bold' as Extract<IconNames, 'cancel-bold'>"
      },
      "ICON_SIZE_IN_REM": {
        "compact": 0.75,
        "default": 1.25,
        "SIZE": "default",
        "VALIDATION_MESSAGE": "Please toggle this switch if you want to proceed."
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "associated",
      "binary",
      "button",
      "component",
      "for",
      "form",
      "guidelines",
      "icon",
      "mdc",
      "off",
      "on",
      "overview",
      "settings",
      "statictoggle",
      "switch",
      "text",
      "the",
      "toggle",
      "toggletip",
      "usage"
    ]
  },
  {
    "name": "toggletip",
    "title": "Toggletip",
    "tagName": "mdc-toggletip",
    "summary": "Usage, guidelines, and accessibility for the mdc-toggletip component — a click-triggered overlay for interactive contextual content.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/toggletip",
      "knowledgeBasePath": "components/src/components/toggletip/knowledge-base/toggletip.component.md"
    },
    "docs": "## Overview\n\n`mdc-toggletip` is a click-triggered overlay used to surface contextual help text or interactive content (links, buttons) next to a target element. It stays visible until the user dismisses it via the close button, an outside click, or the Escape key, and announces its content to screen readers when it appears.\n\n### When to use\n\n- Reveal additional information or interactive controls that the user needs time to read or interact with (e.g. an info button next to a form label).\n- Group an action with a small amount of explanatory content that should remain visible after the user opens it.\n\n### When not to use\n\n- Use `mdc-tooltip` for short, non-interactive hints that should appear on hover or focus and disappear automatically.\n- Use `mdc-popover` directly when you need full control over trigger semantics, placement defaults, or behavior beyond a click-triggered tip.\n- Use `mdc-dialog` for modal flows that require explicit user acknowledgement or block the rest of the page.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/toggletip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toggletip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example. The toggletip is a sibling of its target; connect it via `triggerid` pointing at the target's `id`:\n\n```html\n<mdc-button id=\"info-trigger\" prefix-icon=\"info-circle-bold\" aria-label=\"More info\"></mdc-button>\n<mdc-toggletip triggerid=\"info-trigger\" placement=\"bottom\" show-arrow>\n  Toggletips can contain <a href=\"/help\">links</a> or other interactive content.\n</mdc-toggletip>\n```\n\n### Property/Attribute details\n\n- `triggerid` — id of the target element that opens the toggletip when clicked. Required.\n- `placement` — preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom` (default), `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to `bottom`.\n- `visible` — boolean controlling the open state; set via `show()` / `hide()` or by the click trigger.\n- `screenreader-announcer-identity` — id of an existing DOM element to which the screen reader announcement element will be appended. If omitted, the component creates a visually hidden div in the DOM.\n- `close-button-aria-label` — accessible name for the built-in close button (the close button is shown by default).\n- Events: `shown` (`onShown`), `hidden` (`onHidden`), `created` (`onCreated`), and `destroyed` (`onDestroyed`) fire as the toggletip is shown, hidden, added to the DOM, or removed from the DOM.\n\n### Limitations\n\n- The toggletip cannot self-position relative to a target it does not know about; the `triggerid` must resolve to an element in the same root (document or shadow root).\n\n## Accessibility\n\n### Built-in features\n\n- Activates on click of the trigger element; pressing Escape or clicking outside closes it; the built-in close button also dismisses it.\n- The overlay traps focus while open so keyboard users can Tab between any interactive controls inside.\n- When the toggletip closes, focus returns to the trigger element.\n- A backdrop is rendered behind the toggletip; clicking the backdrop closes it.\n- When the toggletip becomes visible, its text content is announced through `mdc-screenreaderannouncer` (with a 300ms delay) so users of assistive technologies hear the contents.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-toggletip`) | `role` | `dialog` (inherits the popover default) |\n| Host | `aria-modal` | Managed by the popover; set when the role is `dialog` and the overlay is interactive |\n| Trigger element | `aria-expanded` | Reflects the toggletip's open/closed state |\n| Trigger element | `aria-haspopup` | Set on the trigger so screen readers know a popup is connected |\n| Close button | `aria-label` | The consumer-supplied `close-button-aria-label` |\n\n### Implementation requirements\n\n#### General\n\n- The target element identified by `triggerid` must be focusable; provide it via a button, link, or other natively focusable control so keyboard users can open the toggletip.\n- Use `screenreader-announcer-identity` when the toggletip is rendered in a context where appending a hidden announcer to the document body is undesirable (e.g. inside a portal you own).\n\n#### Labelling\n\n- If the trigger is icon-only, give it an accessible name via `aria-label` so users know what activating it does before the toggletip opens.\n- For interactive content inside the toggletip (links, buttons), provide their own accessible names.\n\n### Notes\n\n- Because focus is trapped inside the toggletip while open, ensure there is always a way to leave it (Escape, the close button, or an interactive control that dismisses it).",
    "sections": {
      "Overview": "`mdc-toggletip` is a click-triggered overlay used to surface contextual help text or interactive content (links, buttons) next to a target element. It stays visible until the user dismisses it via the close button, an outside click, or the Escape key, and announces its content to screen readers when it appears.\n\n### When to use\n\n- Reveal additional information or interactive controls that the user needs time to read or interact with (e.g. an info button next to a form label).\n- Group an action with a small amount of explanatory content that should remain visible after the user opens it.\n\n### When not to use\n\n- Use `mdc-tooltip` for short, non-interactive hints that should appear on hover or focus and disappear automatically.\n- Use `mdc-popover` directly when you need full control over trigger semantics, placement defaults, or behavior beyond a click-triggered tip.\n- Use `mdc-dialog` for modal flows that require explicit user acknowledgement or block the rest of the page.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/toggletip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toggletip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example. The toggletip is a sibling of its target; connect it via `triggerid` pointing at the target's `id`:\n\n```html\n<mdc-button id=\"info-trigger\" prefix-icon=\"info-circle-bold\" aria-label=\"More info\"></mdc-button>\n<mdc-toggletip triggerid=\"info-trigger\" placement=\"bottom\" show-arrow>\n  Toggletips can contain <a href=\"/help\">links</a> or other interactive content.\n</mdc-toggletip>\n```\n\n### Property/Attribute details\n\n- `triggerid` — id of the target element that opens the toggletip when clicked. Required.\n- `placement` — preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom` (default), `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to `bottom`.\n- `visible` — boolean controlling the open state; set via `show()` / `hide()` or by the click trigger.\n- `screenreader-announcer-identity` — id of an existing DOM element to which the screen reader announcement element will be appended. If omitted, the component creates a visually hidden div in the DOM.\n- `close-button-aria-label` — accessible name for the built-in close button (the close button is shown by default).\n- Events: `shown` (`onShown`), `hidden` (`onHidden`), `created` (`onCreated`), and `destroyed` (`onDestroyed`) fire as the toggletip is shown, hidden, added to the DOM, or removed from the DOM.\n\n### Limitations\n\n- The toggletip cannot self-position relative to a target it does not know about; the `triggerid` must resolve to an element in the same root (document or shadow root).",
      "Accessibility": "### Built-in features\n\n- Activates on click of the trigger element; pressing Escape or clicking outside closes it; the built-in close button also dismisses it.\n- The overlay traps focus while open so keyboard users can Tab between any interactive controls inside.\n- When the toggletip closes, focus returns to the trigger element.\n- A backdrop is rendered behind the toggletip; clicking the backdrop closes it.\n- When the toggletip becomes visible, its text content is announced through `mdc-screenreaderannouncer` (with a 300ms delay) so users of assistive technologies hear the contents.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-toggletip`) | `role` | `dialog` (inherits the popover default) |\n| Host | `aria-modal` | Managed by the popover; set when the role is `dialog` and the overlay is interactive |\n| Trigger element | `aria-expanded` | Reflects the toggletip's open/closed state |\n| Trigger element | `aria-haspopup` | Set on the trigger so screen readers know a popup is connected |\n| Close button | `aria-label` | The consumer-supplied `close-button-aria-label` |\n\n### Implementation requirements\n\n#### General\n\n- The target element identified by `triggerid` must be focusable; provide it via a button, link, or other natively focusable control so keyboard users can open the toggletip.\n- Use `screenreader-announcer-identity` when the toggletip is rendered in a context where appending a hidden announcer to the document body is undesirable (e.g. inside a portal you own).\n\n#### Labelling\n\n- If the trigger is icon-only, give it an accessible name via `aria-label` so users know what activating it does before the toggletip opens.\n- For interactive content inside the toggletip (links, buttons), provide their own accessible names.\n\n### Notes\n\n- Because focus is trapped inside the toggletip while open, ensure there is always a way to leave it (Escape, the close button, or an interactive control that dismisses it)."
    },
    "dependencies": [
      "mdc-screenreaderannouncer",
      "mdc-button"
    ],
    "api": {
      "properties": [
        {
          "name": "backdropAppendTo",
          "attribute": "backdrop-append-to",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "boundary",
          "attribute": "boundary",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "boundaryPadding",
          "attribute": "boundary-padding",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "boundaryRoot",
          "attribute": "boundary-root",
          "type": "radio",
          "values": [
            "viewport",
            "document"
          ],
          "source": "storybook"
        },
        {
          "name": "closeButton",
          "attribute": "close-button",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "closeButtonAriaLabel",
          "attribute": "close-button-aria-label",
          "type": "text",
          "values": [],
          "description": "accessible name for the built-in close button (the close button is shown by default).",
          "source": "storybook"
        },
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "isBackdropInvisible",
          "attribute": "is-backdrop-invisible",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "offset",
          "attribute": "offset",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "select",
          "default": "DEFAULTS.PLACEMENT",
          "values": [],
          "description": "preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom` (default), `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to `bottom`.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "propagateEventOnEscape",
          "attribute": "propagate-event-on-escape",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "screenreaderAnnouncerIdentity",
          "attribute": "screenreader-announcer-identity",
          "type": "text",
          "values": [],
          "description": "id of an existing DOM element to which the screen reader announcement element will be appended. If omitted, the component creates a visually hidden div in the DOM.",
          "source": "storybook"
        },
        {
          "name": "shouldFocusTrapWrap",
          "attribute": "should-focus-trap-wrap",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "showArrow",
          "attribute": "show-arrow",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "strategy",
          "attribute": "strategy",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "triggerID",
          "attribute": "trigger-id",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for the toggletip content"
        }
      ],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) This event is dispatched when the toggletip is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) This event is dispatched when the toggletip is hidden"
        },
        {
          "name": "created",
          "description": "(React: onCreated) This event is dispatched when the toggletip is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) This event is dispatched when the toggletip is destroyed (removed from the DOM)"
        }
      ],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-toggletip-max-width",
          "description": "The maximum width of the toggletip."
        },
        {
          "name": "--mdc-toggletip-text-color",
          "description": "The text color of the toggletip."
        },
        {
          "name": "--mdc-toggletip-text-color-contrast",
          "description": "The text color of the toggletip when the color is contrast."
        }
      ],
      "rules": [
        "id of the target element that opens the toggletip when clicked. Required.",
        "preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom` (default), `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to `bottom`.",
        "boolean controlling the open state; set via `show()` / `hide()` or by the click trigger.",
        "id of an existing DOM element to which the screen reader announcement element will be appended. If omitted, the component creates a visually hidden div in the DOM.",
        "accessible name for the built-in close button (the close button is shown by default)."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/toggletip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Toggletip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"info-trigger\" prefix-icon=\"info-circle-bold\" aria-label=\"More info\"></mdc-button>\n<mdc-toggletip triggerid=\"info-trigger\" placement=\"bottom\" show-arrow>\n  Toggletips can contain <a href=\"/help\">links</a> or other interactive content.\n</mdc-toggletip>"
      }
    ],
    "reactImports": [
      "Toggletip"
    ],
    "constants": {
      "DEFAULTS": {
        "CLOSE_BUTTON": true,
        "CLOSE_BUTTON_ARIA_LABEL": "Close",
        "OFFSET": 4,
        "SHOW_ARROW": true
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "button",
      "click",
      "component",
      "content",
      "contextual",
      "for",
      "guidelines",
      "interactive",
      "mdc",
      "overlay",
      "overview",
      "screenreaderannouncer",
      "the",
      "toggletip",
      "triggered",
      "usage"
    ]
  },
  {
    "name": "tooltip",
    "title": "Tooltip",
    "tagName": "mdc-tooltip",
    "summary": "Usage, guidelines, and accessibility for the mdc-tooltip component — a hover/focus-triggered overlay for short, non-interactive hints.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/tooltip",
      "knowledgeBasePath": "components/src/components/tooltip/knowledge-base/tooltip.component.md"
    },
    "docs": "## Overview\n\n`mdc-tooltip` is a lightweight overlay that surfaces a short, non-interactive hint about a target element. It opens on hover or keyboard focus of the trigger and disappears when the pointer leaves or focus moves away. It can be configured to act as the trigger's accessible description, label, or as a purely visual hint.\n\n### When to use\n\n- Reveal short, text-only context for a control whose purpose isn't fully clear from its visual presentation (e.g. icon-only buttons).\n- Provide an accessible name or description for a trigger that has no visible label.\n- Show the full text of a truncated label only when it is actually overflowing.\n\n### When not to use\n\n- Use `mdc-toggletip` when the content needs to remain visible after activation or includes interactive elements (links, buttons).\n- Use `mdc-popover` for richer overlay content, click-triggered surfaces, or when you need explicit control over open/close behavior.\n- Avoid tooltips for critical information — users on touch devices and some assistive technologies may not surface hover-triggered content.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/tooltip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Tooltip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example. The tooltip is a sibling of its target; connect it via `triggerid` pointing at the target's `id`:\n\n```html\n<mdc-button id=\"save-btn\" prefix-icon=\"save-bold\" aria-label=\"Save\"></mdc-button>\n<mdc-tooltip triggerid=\"save-btn\" tooltip-type=\"description\">\n  Saves the current document\n</mdc-tooltip>\n```\n\n### Property/Attribute details\n\n- `triggerid` — id of the target element the tooltip is attached to. Required.\n- `tooltip-type` — controls how the tooltip exposes itself to the trigger for assistive technologies. One of:\n  - `description` (default) — sets `aria-describedby` on the trigger, pointing at the tooltip's id.\n  - `label` — sets `aria-labelledby` on the trigger, pointing at the tooltip's id.\n  - `none` — does not set any aria attribute on the trigger.\n  Switching between types automatically removes the previously-set attribute on the trigger.\n- `only-show-when-trigger-overflows` — when `true`, the tooltip only opens if the trigger element's content is overflowing on the x-axis. Supported on `mdc-button` and `mdc-text` triggers.\n- `placement` — preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to the component default.\n- `offset`, `boundary`, `boundary-root`, `boundary-padding`, `strategy` — positioning controls forwarded to the underlying Floating UI computation.\n- `id` — if not set, a stable `mdc-tooltip-<uuid>` id is generated and cached so the trigger's `aria-describedby` / `aria-labelledby` remain valid across re-renders.\n- Events: `shown` (`onShown`), `hidden` (`onHidden`), `created` (`onCreated`), and `destroyed` (`onDestroyed`) fire as the tooltip opens, closes, or is added/removed from the DOM.\n\n### Limitations\n\n- The tooltip must not contain focusable or interactive content; if you need that, use `mdc-toggletip` instead.\n- `only-show-when-trigger-overflows` is implemented for `mdc-button` and `mdc-text` triggers; other trigger types always open on hover/focus.\n\n## Accessibility\n\n### Built-in features\n\n- The tooltip is shown on `mouseenter` and on keyboard focus of the trigger and closes on mouse leave or focus change. Pressing Escape while the tooltip is open also closes it.\n- The component manages `aria-describedby` or `aria-labelledby` on the trigger based on `tooltip-type`, so the trigger announces the tooltip text without the assistive technology focusing the tooltip itself.\n- The tooltip element itself is marked `aria-hidden=\"true\"` so that screen readers never reach the tooltip directly; the content is conveyed through the trigger's aria relationship.\n- Hover detection uses delegated `mouseover`/`mouseout` events so triggers inside shadow roots (for example wrapped in `mdc-iconprovider` or `mdc-themeprovider`) still open the tooltip correctly.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-tooltip`) | `role` | `tooltip` |\n| Host | `aria-hidden` | `true` (so the tooltip itself is not reached by screen readers) |\n| Trigger element (`tooltip-type=\"description\"`) | `aria-describedby` | The tooltip's `id` |\n| Trigger element (`tooltip-type=\"label\"`) | `aria-labelledby` | The tooltip's `id` |\n| Trigger element (`tooltip-type=\"none\"`) | none | No aria relationship is set |\n\n### Implementation requirements\n\n#### General\n\n- Pick `tooltip-type` based on intent: `label` when the tooltip provides the accessible name for an otherwise unlabeled trigger; `description` when it adds context to an already-labeled control; `none` when the tooltip is decorative.\n- The trigger element must be focusable so keyboard users can open the tooltip.\n\n#### Labelling\n\n- Keep tooltip content short and meaningful — it is announced as the trigger's accessible name or description.\n- If the trigger has an existing `aria-label` you don't want overridden, choose `tooltip-type=\"description\"` (or `none`) rather than `label`.\n\n### Notes\n\n- Because content inside the tooltip cannot be focused, screen readers will not visit it directly. All meaningful information must be reachable as plain text — interactive controls placed inside will be ignored by assistive technology.\n- Hover/focus-only triggers are typically inaccessible to touch users; ensure the same information is available through another channel for critical UI.",
    "sections": {
      "Overview": "`mdc-tooltip` is a lightweight overlay that surfaces a short, non-interactive hint about a target element. It opens on hover or keyboard focus of the trigger and disappears when the pointer leaves or focus moves away. It can be configured to act as the trigger's accessible description, label, or as a purely visual hint.\n\n### When to use\n\n- Reveal short, text-only context for a control whose purpose isn't fully clear from its visual presentation (e.g. icon-only buttons).\n- Provide an accessible name or description for a trigger that has no visible label.\n- Show the full text of a truncated label only when it is actually overflowing.\n\n### When not to use\n\n- Use `mdc-toggletip` when the content needs to remain visible after activation or includes interactive elements (links, buttons).\n- Use `mdc-popover` for richer overlay content, click-triggered surfaces, or when you need explicit control over open/close behavior.\n- Avoid tooltips for critical information — users on touch devices and some assistive technologies may not surface hover-triggered content.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/tooltip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Tooltip } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example. The tooltip is a sibling of its target; connect it via `triggerid` pointing at the target's `id`:\n\n```html\n<mdc-button id=\"save-btn\" prefix-icon=\"save-bold\" aria-label=\"Save\"></mdc-button>\n<mdc-tooltip triggerid=\"save-btn\" tooltip-type=\"description\">\n  Saves the current document\n</mdc-tooltip>\n```\n\n### Property/Attribute details\n\n- `triggerid` — id of the target element the tooltip is attached to. Required.\n- `tooltip-type` — controls how the tooltip exposes itself to the trigger for assistive technologies. One of:\n  - `description` (default) — sets `aria-describedby` on the trigger, pointing at the tooltip's id.\n  - `label` — sets `aria-labelledby` on the trigger, pointing at the tooltip's id.\n  - `none` — does not set any aria attribute on the trigger.\n  Switching between types automatically removes the previously-set attribute on the trigger.\n- `only-show-when-trigger-overflows` — when `true`, the tooltip only opens if the trigger element's content is overflowing on the x-axis. Supported on `mdc-button` and `mdc-text` triggers.\n- `placement` — preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to the component default.\n- `offset`, `boundary`, `boundary-root`, `boundary-padding`, `strategy` — positioning controls forwarded to the underlying Floating UI computation.\n- `id` — if not set, a stable `mdc-tooltip-<uuid>` id is generated and cached so the trigger's `aria-describedby` / `aria-labelledby` remain valid across re-renders.\n- Events: `shown` (`onShown`), `hidden` (`onHidden`), `created` (`onCreated`), and `destroyed` (`onDestroyed`) fire as the tooltip opens, closes, or is added/removed from the DOM.\n\n### Limitations\n\n- The tooltip must not contain focusable or interactive content; if you need that, use `mdc-toggletip` instead.\n- `only-show-when-trigger-overflows` is implemented for `mdc-button` and `mdc-text` triggers; other trigger types always open on hover/focus.",
      "Accessibility": "### Built-in features\n\n- The tooltip is shown on `mouseenter` and on keyboard focus of the trigger and closes on mouse leave or focus change. Pressing Escape while the tooltip is open also closes it.\n- The component manages `aria-describedby` or `aria-labelledby` on the trigger based on `tooltip-type`, so the trigger announces the tooltip text without the assistive technology focusing the tooltip itself.\n- The tooltip element itself is marked `aria-hidden=\"true\"` so that screen readers never reach the tooltip directly; the content is conveyed through the trigger's aria relationship.\n- Hover detection uses delegated `mouseover`/`mouseout` events so triggers inside shadow roots (for example wrapped in `mdc-iconprovider` or `mdc-themeprovider`) still open the tooltip correctly.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-tooltip`) | `role` | `tooltip` |\n| Host | `aria-hidden` | `true` (so the tooltip itself is not reached by screen readers) |\n| Trigger element (`tooltip-type=\"description\"`) | `aria-describedby` | The tooltip's `id` |\n| Trigger element (`tooltip-type=\"label\"`) | `aria-labelledby` | The tooltip's `id` |\n| Trigger element (`tooltip-type=\"none\"`) | none | No aria relationship is set |\n\n### Implementation requirements\n\n#### General\n\n- Pick `tooltip-type` based on intent: `label` when the tooltip provides the accessible name for an otherwise unlabeled trigger; `description` when it adds context to an already-labeled control; `none` when the tooltip is decorative.\n- The trigger element must be focusable so keyboard users can open the tooltip.\n\n#### Labelling\n\n- Keep tooltip content short and meaningful — it is announced as the trigger's accessible name or description.\n- If the trigger has an existing `aria-label` you don't want overridden, choose `tooltip-type=\"description\"` (or `none`) rather than `label`.\n\n### Notes\n\n- Because content inside the tooltip cannot be focused, screen readers will not visit it directly. All meaningful information must be reachable as plain text — interactive controls placed inside will be ignored by assistive technology.\n- Hover/focus-only triggers are typically inaccessible to touch users; ensure the same information is available through another channel for critical UI."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "color",
          "attribute": "color",
          "type": "select",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "delay",
          "attribute": "delay",
          "type": "text",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "id",
          "attribute": "id",
          "type": "text",
          "values": [],
          "description": "if not set, a stable `mdc-tooltip-<uuid>` id is generated and cached so the trigger's `aria-describedby` / `aria-labelledby` remain valid across re-renders.",
          "source": "storybook"
        },
        {
          "name": "offset",
          "attribute": "offset",
          "type": "number",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "onlyShowWhenTriggerOverflows",
          "attribute": "only-show-when-trigger-overflows",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the tooltip only opens if the trigger element's content is overflowing on the x-axis. Supported on `mdc-button` and `mdc-text` triggers.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "placement",
          "attribute": "placement",
          "type": "select",
          "values": [],
          "description": "preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to the component default.",
          "source": "storybook"
        },
        {
          "name": "showArrow",
          "attribute": "show-arrow",
          "type": "boolean",
          "values": [],
          "source": "storybook"
        },
        {
          "name": "tooltipType",
          "attribute": "tooltip-type",
          "type": "select",
          "default": "'description'",
          "values": [
            "description",
            "label",
            "none"
          ],
          "description": "controls how the tooltip exposes itself to the trigger for assistive technologies. One of:",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "triggerID",
          "attribute": "trigger-id",
          "type": "text",
          "values": [],
          "source": "storybook"
        }
      ],
      "slots": [],
      "events": [
        {
          "name": "shown",
          "description": "(React: onShown) This event is dispatched when the tooltip is shown"
        },
        {
          "name": "hidden",
          "description": "(React: onHidden) This event is dispatched when the tooltip is hidden"
        },
        {
          "name": "created",
          "description": "(React: onCreated) This event is dispatched when the tooltip is created (added to the DOM)"
        },
        {
          "name": "destroyed",
          "description": "(React: onDestroyed) This event is dispatched when the tooltip is destroyed (removed from the DOM)"
        }
      ],
      "cssParts": [],
      "cssProperties": [
        {
          "name": "--mdc-tooltip-max-width",
          "description": "The maximum width of the tooltip."
        },
        {
          "name": "--mdc-tooltip-padding",
          "description": "The padding of the tooltip."
        },
        {
          "name": "--mdc-tooltip-text-color",
          "description": "The text color of the tooltip."
        },
        {
          "name": "--mdc-tooltip-text-color-contrast",
          "description": "The text color of the tooltip when the color is contrast."
        }
      ],
      "rules": [
        "id of the target element the tooltip is attached to. Required.",
        "controls how the tooltip exposes itself to the trigger for assistive technologies. One of:",
        "when `true`, the tooltip only opens if the trigger element's content is overflowing on the x-axis. Supported on `mdc-button` and `mdc-text` triggers.",
        "preferred placement relative to the trigger. One of `top`, `top-start`, `top-end`, `bottom`, `bottom-start`, `bottom-end`, `left`, `left-start`, `left-end`, `right`, `right-start`, `right-end`. Invalid values fall back to the component default.",
        "if not set, a stable `mdc-tooltip-<uuid>` id is generated and cached so the trigger's `aria-describedby` / `aria-labelledby` remain valid across re-renders."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/tooltip/index.js\"; // custom element registration\n// or via React wrapper\nimport { Tooltip } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-button id=\"save-btn\" prefix-icon=\"save-bold\" aria-label=\"Save\"></mdc-button>\n<mdc-tooltip triggerid=\"save-btn\" tooltip-type=\"description\">\n  Saves the current document\n</mdc-tooltip>"
      }
    ],
    "reactImports": [
      "Tooltip"
    ],
    "constants": {
      "TOOLTIP_TYPES": {
        "DESCRIPTION": "description",
        "LABEL": "label",
        "NONE": "none"
      },
      "DEFAULTS": {
        "ARIA_HIDDEN": "true",
        "BACKDROP": false,
        "DELAY": "0,0",
        "DISABLE_ARIA_EXPANDED": true,
        "HIDE_ON_BLUR": true,
        "HIDE_ON_ESCAPE": true,
        "TRIGGER": "`${TRIGGER.MOUSEENTER} ${TRIGGER.FOCUSIN}`",
        "OFFSET": 4,
        "SHOW_ARROW": true,
        "TOOLTIP_TYPE": "description",
        "ONLY_SHOW_WHEN_TRIGGER_OVERFLOWS": false
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "focus",
      "for",
      "guidelines",
      "hints",
      "hover",
      "interactive",
      "mdc",
      "non",
      "overlay",
      "overview",
      "short",
      "the",
      "tooltip",
      "triggered",
      "usage"
    ]
  },
  {
    "name": "typewriter",
    "title": "Typewriter",
    "tagName": "mdc-typewriter",
    "summary": "Usage, guidelines, and accessibility for the mdc-typewriter component — an animated text component that progressively reveals its content.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/typewriter",
      "knowledgeBasePath": "components/src/components/typewriter/knowledge-base/typewriter.component.md"
    },
    "docs": "## Overview\n\n`mdc-typewriter` progressively reveals its text content character-by-character to produce a typewriter effect. It renders through `mdc-text`, so it inherits the same typography options, and exposes both a static markup mode (slotted text) and an imperative API for appending text chunks at runtime.\n\n### When to use\n\n- Stream model or assistant output where text should appear progressively rather than all at once.\n- Animate the arrival of short notifications or status messages to draw user attention.\n- Build chat-style transcripts where additional content can be appended after the initial render.\n\n### When not to use\n\n- Use `mdc-text` directly when content should appear immediately and never animate.\n- Use `mdc-toast`, `mdc-banner`, or another notification surface when you need a container and chrome — the typewriter only animates text.\n- Avoid for long, dense content where the animation would slow down comprehension.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/typewriter/index.js\"; // custom element registration\n// or via React wrapper\nimport { Typewriter } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-typewriter type=\"body-large-regular\" tagname=\"p\" speed=\"normal\">\n  Hello, world!\n</mdc-typewriter>\n```\n\nAppend additional text imperatively after the initial render:\n\n```ts\nconst tw = document.querySelector('mdc-typewriter');\ntw.addTextChunk(' How are you today?');           // animated chunk\ntw.addInstantTextChunk(' [system note]');         // appears instantly\n```\n\n### Property/Attribute details\n\n- `type` — text style forwarded to the internal `mdc-text` component. Defaults to `body-large-regular`.\n- `tagname` — HTML tag used for the internal text element. Defaults to `p`. Accepts any tag supported by `mdc-text`.\n- `speed` — milliseconds per character; either a numeric string (e.g. `\"100\"`) or one of the presets:\n  - `very-slow` — 240ms per character\n  - `slow` — 120ms per character\n  - `normal` — 60ms per character (default)\n  - `fast` — 20ms per character\n  - `very-fast` — 1ms per character\n  Numeric speeds are clamped to a minimum of 10ms per character.\n- `max-queue-size` — maximum number of chunks that can be queued via `addTextChunk` / `addInstantTextChunk`. When exceeded, the oldest queued chunks are dropped. Defaults to effectively unlimited.\n- Imperative methods:\n  - `addTextChunk(text, speed?, instant?)` — append a chunk, optionally overriding `speed` for that chunk, or rendering it instantly.\n  - `addInstantTextChunk(text)` — append a chunk that appears immediately without animation.\n  - `clearQueue()` — drop any chunks waiting to be typed.\n- Events:\n  - `typing-complete` (`onTypingComplete`) — fires when the typewriter finishes typing all queued content. Detail: `{ finalContent: string }`.\n  - `change` (`onChange`) — fires when the slotted content changes. Detail: `{ content: string, isTyping: boolean }`.\n\n### Limitations\n\n- The animation continues even when the containing tab is in the background.\n- Speed updates while the typewriter is in the middle of processing queued chunks are ignored until the queue empties; the new speed applies to subsequent animations only.\n\n## Accessibility\n\n### Built-in features\n\n- The component renders an `aria-live=\"polite\"` region around the animated text so screen readers receive an announcement when content settles, rather than per character.\n- `aria-busy` is set on the container while typing is in progress and cleared when the animation completes, letting assistive technologies know when the content is stable.\n- The internal `mdc-text` element exposes the full target text via `aria-label`, so users of assistive technologies receive the complete message instead of the partial characters drawn on screen.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Container (`[part=\"container\"]`) | `aria-live` | `polite` |\n| Container | `aria-busy` | `true` while typing, `false` once `typing-complete` fires |\n| Internal `mdc-text` | `aria-label` | The full target text (so AT reads the whole message, not the per-character animation) |\n\n### Implementation requirements\n\n#### General\n\n- When the typewriter is part of a larger live region or replaces existing content, ensure surrounding `aria-live` settings don't cause double announcements.\n- If the animated message is critical for understanding the UI, also expose it in a non-animated form (such as the underlying text in a transcript) so users who disable animations still receive it.\n\n### Notes\n\n- Because the visible text is animated, on-screen text and the announced text can briefly differ; the `aria-label` on the internal text element is the source of truth for assistive technology.",
    "sections": {
      "Overview": "`mdc-typewriter` progressively reveals its text content character-by-character to produce a typewriter effect. It renders through `mdc-text`, so it inherits the same typography options, and exposes both a static markup mode (slotted text) and an imperative API for appending text chunks at runtime.\n\n### When to use\n\n- Stream model or assistant output where text should appear progressively rather than all at once.\n- Animate the arrival of short notifications or status messages to draw user attention.\n- Build chat-style transcripts where additional content can be appended after the initial render.\n\n### When not to use\n\n- Use `mdc-text` directly when content should appear immediately and never animate.\n- Use `mdc-toast`, `mdc-banner`, or another notification surface when you need a container and chrome — the typewriter only animates text.\n- Avoid for long, dense content where the animation would slow down comprehension.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/typewriter/index.js\"; // custom element registration\n// or via React wrapper\nimport { Typewriter } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example:\n\n```html\n<mdc-typewriter type=\"body-large-regular\" tagname=\"p\" speed=\"normal\">\n  Hello, world!\n</mdc-typewriter>\n```\n\nAppend additional text imperatively after the initial render:\n\n```ts\nconst tw = document.querySelector('mdc-typewriter');\ntw.addTextChunk(' How are you today?');           // animated chunk\ntw.addInstantTextChunk(' [system note]');         // appears instantly\n```\n\n### Property/Attribute details\n\n- `type` — text style forwarded to the internal `mdc-text` component. Defaults to `body-large-regular`.\n- `tagname` — HTML tag used for the internal text element. Defaults to `p`. Accepts any tag supported by `mdc-text`.\n- `speed` — milliseconds per character; either a numeric string (e.g. `\"100\"`) or one of the presets:\n  - `very-slow` — 240ms per character\n  - `slow` — 120ms per character\n  - `normal` — 60ms per character (default)\n  - `fast` — 20ms per character\n  - `very-fast` — 1ms per character\n  Numeric speeds are clamped to a minimum of 10ms per character.\n- `max-queue-size` — maximum number of chunks that can be queued via `addTextChunk` / `addInstantTextChunk`. When exceeded, the oldest queued chunks are dropped. Defaults to effectively unlimited.\n- Imperative methods:\n  - `addTextChunk(text, speed?, instant?)` — append a chunk, optionally overriding `speed` for that chunk, or rendering it instantly.\n  - `addInstantTextChunk(text)` — append a chunk that appears immediately without animation.\n  - `clearQueue()` — drop any chunks waiting to be typed.\n- Events:\n  - `typing-complete` (`onTypingComplete`) — fires when the typewriter finishes typing all queued content. Detail: `{ finalContent: string }`.\n  - `change` (`onChange`) — fires when the slotted content changes. Detail: `{ content: string, isTyping: boolean }`.\n\n### Limitations\n\n- The animation continues even when the containing tab is in the background.\n- Speed updates while the typewriter is in the middle of processing queued chunks are ignored until the queue empties; the new speed applies to subsequent animations only.",
      "Accessibility": "### Built-in features\n\n- The component renders an `aria-live=\"polite\"` region around the animated text so screen readers receive an announcement when content settles, rather than per character.\n- `aria-busy` is set on the container while typing is in progress and cleared when the animation completes, letting assistive technologies know when the content is stable.\n- The internal `mdc-text` element exposes the full target text via `aria-label`, so users of assistive technologies receive the complete message instead of the partial characters drawn on screen.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Container (`[part=\"container\"]`) | `aria-live` | `polite` |\n| Container | `aria-busy` | `true` while typing, `false` once `typing-complete` fires |\n| Internal `mdc-text` | `aria-label` | The full target text (so AT reads the whole message, not the per-character animation) |\n\n### Implementation requirements\n\n#### General\n\n- When the typewriter is part of a larger live region or replaces existing content, ensure surrounding `aria-live` settings don't cause double announcements.\n- If the animated message is critical for understanding the UI, also expose it in a non-animated form (such as the underlying text in a transcript) so users who disable animations still receive it.\n\n### Notes\n\n- Because the visible text is animated, on-screen text and the announced text can briefly differ; the `aria-label` on the internal text element is the source of truth for assistive technology."
    },
    "dependencies": [
      "mdc-text"
    ],
    "api": {
      "properties": [
        {
          "name": "children",
          "attribute": "children",
          "type": "text",
          "values": [],
          "description": "Children (passed into ",
          "source": "storybook"
        },
        {
          "name": "maxQueueSize",
          "attribute": "max-queue-size",
          "type": "unknown",
          "default": "Number.MAX_SAFE_INTEGER (effectively unlimited)",
          "values": [],
          "description": "maximum number of chunks that can be queued via `addTextChunk` / `addInstantTextChunk`. When exceeded, the oldest queued chunks are dropped. Defaults to effectively unlimited.",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "speed",
          "attribute": "speed",
          "type": "text",
          "default": "DEFAULTS.SPEED",
          "values": [
            "slow",
            "normal",
            "fast",
            "very-slow",
            "very-fast"
          ],
          "description": "milliseconds per character; either a numeric string (e.g. `\"100\"`) or one of the presets:",
          "reflected": true,
          "source": "storybook"
        },
        {
          "name": "tagname",
          "attribute": "tagname",
          "type": "select",
          "values": [],
          "description": "HTML tag used for the internal text element. Defaults to `p`. Accepts any tag supported by `mdc-text`.",
          "source": "storybook"
        },
        {
          "name": "type",
          "attribute": "type",
          "type": "select",
          "default": "body-large-regular",
          "values": [],
          "description": "text style forwarded to the internal `mdc-text` component. Defaults to `body-large-regular`.",
          "reflected": true,
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "Default slot for text content"
        }
      ],
      "events": [
        {
          "name": "typing-complete",
          "description": "(React: onTypingComplete) Fired when the typewriter finishes typing all content."
        },
        {
          "name": "change",
          "description": "(React: onChange) Fired when the content of the typewriter changes."
        }
      ],
      "cssParts": [
        {
          "name": "container",
          "description": "Container for the text element"
        },
        {
          "name": "text",
          "description": "The text element (forwarded to mdc-text)"
        },
        {
          "name": "typewriter-hidden",
          "description": "The hidden typewriter text"
        }
      ],
      "cssProperties": [],
      "rules": [
        "text style forwarded to the internal `mdc-text` component. Defaults to `body-large-regular`.",
        "HTML tag used for the internal text element. Defaults to `p`. Accepts any tag supported by `mdc-text`.",
        "milliseconds per character; either a numeric string (e.g. `\"100\"`) or one of the presets:",
        "maximum number of chunks that can be queued via `addTextChunk` / `addInstantTextChunk`. When exceeded, the oldest queued chunks are dropped. Defaults to effectively unlimited."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/typewriter/index.js\"; // custom element registration\n// or via React wrapper\nimport { Typewriter } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-typewriter type=\"body-large-regular\" tagname=\"p\" speed=\"normal\">\n  Hello, world!\n</mdc-typewriter>"
      },
      {
        "language": "ts",
        "code": "const tw = document.querySelector('mdc-typewriter');\ntw.addTextChunk(' How are you today?');           // animated chunk\ntw.addInstantTextChunk(' [system note]');         // appears instantly"
      }
    ],
    "reactImports": [
      "Typewriter"
    ],
    "constants": {
      "SPEED": {
        "VERY_SLOW": 240,
        "SLOW": 120,
        "NORMAL": 60,
        "FAST": 20,
        "VERY_FAST": 1
      },
      "DEFAULTS": {
        "CSS_PART_TEXT": "text",
        "CSS_PART_CONTAINER": "container",
        "CSS_PART_HIDDEN": "typewriter-hidden",
        "CHILDREN": "''",
        "SPEED": "normal",
        "CUSTOM_EVENT": "{",
        "TYPING_COMPLETE": "typing-complete"
      }
    },
    "keywords": [
      "accessibility",
      "an",
      "and",
      "animated",
      "component",
      "content",
      "for",
      "guidelines",
      "its",
      "mdc",
      "overview",
      "progressively",
      "reveals",
      "text",
      "that",
      "the",
      "typewriter",
      "usage"
    ]
  },
  {
    "name": "verticaltablist",
    "title": "Verticaltablist",
    "tagName": "mdc-verticaltablist",
    "summary": "Usage, guidelines, and accessibility for the mdc-verticaltablist component — a vertically oriented tablist for switching between content panels.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/verticaltablist",
      "knowledgeBasePath": "components/src/components/verticaltablist/knowledge-base/verticaltablist.component.md"
    },
    "docs": "## Overview\n\n`mdc-verticaltablist` organises a set of tabs vertically and lets users move between them with the up/down arrow keys, Home, End, and by clicking. It manages tab selection state and the active-tab semantics while the consumer renders the corresponding `tabpanel` content elsewhere in the DOM.\n\n### When to use\n\n- Provide vertical navigation between sibling content panels (settings categories, side-by-side detail views).\n- When horizontal space is constrained or when the related content is naturally laid out beside the tab list.\n\n### When not to use\n\n- Use `mdc-tablist` for a horizontal arrangement of tabs.\n- Use `mdc-sidenavigation` for top-level navigation between different pages or views rather than between related panels of the same view.\n- Use `mdc-listbox` / `mdc-select` for selection of a value rather than switching between visible panels.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/verticaltablist/index.js\"; // custom element registration\n// or via React wrapper\nimport { Verticaltablist } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example. Each `mdc-tab` must point at its associated panel via `aria-controls`, and each panel must point back via `aria-labelledby`:\n\n```html\n<mdc-verticaltablist data-aria-label=\"Settings sections\">\n  <mdc-tab id=\"tab-account\" tab-id=\"account\" text=\"Account\" aria-controls=\"panel-account\" active></mdc-tab>\n  <mdc-tab id=\"tab-billing\" tab-id=\"billing\" text=\"Billing\" aria-controls=\"panel-billing\"></mdc-tab>\n</mdc-verticaltablist>\n\n<section id=\"panel-account\" role=\"tabpanel\" aria-labelledby=\"tab-account\">...</section>\n<section id=\"panel-billing\" role=\"tabpanel\" aria-labelledby=\"tab-billing\" hidden>...</section>\n```\n\n### Content guidance\n\n- Place only `mdc-tab` elements in the default slot. The component sets `variant=\"line\"` automatically on any tab that is added; only the `line` variant is supported in vertical orientation.\n\n### Property/Attribute details\n\n- `data-aria-label` — accessible name applied to the tablist via `aria-label`. Required for screen-reader users to understand the purpose of the tab group.\n- `initial-focus` — index of the tab that should receive focus when the component is first rendered (default `0`). Out-of-range values are clamped to a valid index.\n- `loop` — `\"true\"` (default) or `\"false\"`. When `\"true\"`, arrow navigation wraps around at the ends of the list; when `\"false\"`, navigation stops at the first or last tab.\n- The `change` event (`onChange`) is dispatched when the active tab changes. The new tab id is available on `event.detail.tabId`:\n\n  ```ts\n  tablist.addEventListener('change', (event) => {\n    handleTabChange(event.detail.tabId);\n  });\n  ```\n\n### Limitations\n\n- Only the `line` variant of `mdc-tab` is supported; setting another variant on a child tab has no effect because the component overrides it on insertion.\n\n## Accessibility\n\n### Built-in features\n\n- The host element exposes the tablist semantics required by WCAG: `role=\"tablist\"`, `aria-orientation=\"vertical\"`, and `aria-label` from `data-aria-label`.\n- The active tab is given the `aria-selected=\"true\"` state by the surrounding tab management; all other tabs receive `aria-selected=\"false\"`.\n- Keyboard navigation: Up/Down arrow keys move between tabs (with optional wrap-around via `loop`), Home jumps to the first tab, End to the last, and clicks select a tab. Enter and Space activate the focused tab.\n- Roving tabindex: only the currently selected tab is in the tab order (`tabindex=\"0\"`); the others receive `tabindex=\"-1\"` so Tab moves out of the tablist instead of through every tab.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-verticaltablist`) | `role` | `tablist` |\n| Host | `aria-orientation` | `vertical` |\n| Host | `aria-label` | The value of `data-aria-label` |\n| Inner container slot | `role` | `presentation` (kept out of the accessibility tree) |\n| Each `mdc-tab` | `role` | `tab` |\n| Active `mdc-tab` | `aria-selected` | `true` |\n| Inactive `mdc-tab` | `aria-selected` | `false` |\n| Each `mdc-tab` | `tabindex` | `0` for the active tab, `-1` for the others |\n\n### Implementation requirements\n\n#### General\n\n- Render a separate element with `role=\"tabpanel\"` for each tab and toggle its visibility based on the selected tab.\n- Reflect the active tab's selection in the application state so the rendered panel matches the user's choice.\n\n#### Labelling\n\n- Always set `data-aria-label` so the tablist has an accessible name; otherwise screen readers cannot describe the purpose of the tab group.\n- Each tab needs an `aria-controls` attribute pointing at the id of its panel; each panel needs `aria-labelledby` pointing at its tab.",
    "sections": {
      "Overview": "`mdc-verticaltablist` organises a set of tabs vertically and lets users move between them with the up/down arrow keys, Home, End, and by clicking. It manages tab selection state and the active-tab semantics while the consumer renders the corresponding `tabpanel` content elsewhere in the DOM.\n\n### When to use\n\n- Provide vertical navigation between sibling content panels (settings categories, side-by-side detail views).\n- When horizontal space is constrained or when the related content is naturally laid out beside the tab list.\n\n### When not to use\n\n- Use `mdc-tablist` for a horizontal arrangement of tabs.\n- Use `mdc-sidenavigation` for top-level navigation between different pages or views rather than between related panels of the same view.\n- Use `mdc-listbox` / `mdc-select` for selection of a value rather than switching between visible panels.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/verticaltablist/index.js\"; // custom element registration\n// or via React wrapper\nimport { Verticaltablist } from '@momentum-design/components/dist/react';\n```\n\nMinimal markup example. Each `mdc-tab` must point at its associated panel via `aria-controls`, and each panel must point back via `aria-labelledby`:\n\n```html\n<mdc-verticaltablist data-aria-label=\"Settings sections\">\n  <mdc-tab id=\"tab-account\" tab-id=\"account\" text=\"Account\" aria-controls=\"panel-account\" active></mdc-tab>\n  <mdc-tab id=\"tab-billing\" tab-id=\"billing\" text=\"Billing\" aria-controls=\"panel-billing\"></mdc-tab>\n</mdc-verticaltablist>\n\n<section id=\"panel-account\" role=\"tabpanel\" aria-labelledby=\"tab-account\">...</section>\n<section id=\"panel-billing\" role=\"tabpanel\" aria-labelledby=\"tab-billing\" hidden>...</section>\n```\n\n### Content guidance\n\n- Place only `mdc-tab` elements in the default slot. The component sets `variant=\"line\"` automatically on any tab that is added; only the `line` variant is supported in vertical orientation.\n\n### Property/Attribute details\n\n- `data-aria-label` — accessible name applied to the tablist via `aria-label`. Required for screen-reader users to understand the purpose of the tab group.\n- `initial-focus` — index of the tab that should receive focus when the component is first rendered (default `0`). Out-of-range values are clamped to a valid index.\n- `loop` — `\"true\"` (default) or `\"false\"`. When `\"true\"`, arrow navigation wraps around at the ends of the list; when `\"false\"`, navigation stops at the first or last tab.\n- The `change` event (`onChange`) is dispatched when the active tab changes. The new tab id is available on `event.detail.tabId`:\n\n  ```ts\n  tablist.addEventListener('change', (event) => {\n    handleTabChange(event.detail.tabId);\n  });\n  ```\n\n### Limitations\n\n- Only the `line` variant of `mdc-tab` is supported; setting another variant on a child tab has no effect because the component overrides it on insertion.",
      "Accessibility": "### Built-in features\n\n- The host element exposes the tablist semantics required by WCAG: `role=\"tablist\"`, `aria-orientation=\"vertical\"`, and `aria-label` from `data-aria-label`.\n- The active tab is given the `aria-selected=\"true\"` state by the surrounding tab management; all other tabs receive `aria-selected=\"false\"`.\n- Keyboard navigation: Up/Down arrow keys move between tabs (with optional wrap-around via `loop`), Home jumps to the first tab, End to the last, and clicks select a tab. Enter and Space activate the focused tab.\n- Roving tabindex: only the currently selected tab is in the tab order (`tabindex=\"0\"`); the others receive `tabindex=\"-1\"` so Tab moves out of the tablist instead of through every tab.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-verticaltablist`) | `role` | `tablist` |\n| Host | `aria-orientation` | `vertical` |\n| Host | `aria-label` | The value of `data-aria-label` |\n| Inner container slot | `role` | `presentation` (kept out of the accessibility tree) |\n| Each `mdc-tab` | `role` | `tab` |\n| Active `mdc-tab` | `aria-selected` | `true` |\n| Inactive `mdc-tab` | `aria-selected` | `false` |\n| Each `mdc-tab` | `tabindex` | `0` for the active tab, `-1` for the others |\n\n### Implementation requirements\n\n#### General\n\n- Render a separate element with `role=\"tabpanel\"` for each tab and toggle its visibility based on the selected tab.\n- Reflect the active tab's selection in the application state so the rendered panel matches the user's choice.\n\n#### Labelling\n\n- Always set `data-aria-label` so the tablist has an accessible name; otherwise screen readers cannot describe the purpose of the tab group.\n- Each tab needs an `aria-controls` attribute pointing at the id of its panel; each panel needs `aria-labelledby` pointing at its tab."
    },
    "dependencies": [
      "mdc-tab"
    ],
    "api": {
      "properties": [
        {
          "name": "activeTabId",
          "attribute": "active-tab-id",
          "type": "select",
          "values": [],
          "description": "ID of the active tab. Defaults to the first tab if not provided.",
          "source": "storybook"
        },
        {
          "name": "ariaLabel",
          "attribute": "aria-label",
          "type": "text",
          "values": [],
          "description": "Label for the tab list used for accessibility. This is required for the component to be accessible.",
          "source": "storybook"
        }
      ],
      "slots": [
        {
          "name": "Default slot for mdc-tab elements.",
          "description": ""
        }
      ],
      "events": [
        {
          "name": "change",
          "description": "(React: onChange) This event is dispatched when the tab is selected."
        }
      ],
      "cssParts": [],
      "cssProperties": [],
      "rules": [
        "accessible name applied to the tablist via `aria-label`. Required for screen-reader users to understand the purpose of the tab group.",
        "index of the tab that should receive focus when the component is first rendered (default `0`). Out-of-range values are clamped to a valid index.",
        "`\"true\"` (default) or `\"false\"`. When `\"true\"`, arrow navigation wraps around at the ends of the list; when `\"false\"`, navigation stops at the first or last tab."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/verticaltablist/index.js\"; // custom element registration\n// or via React wrapper\nimport { Verticaltablist } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-verticaltablist data-aria-label=\"Settings sections\">\n  <mdc-tab id=\"tab-account\" tab-id=\"account\" text=\"Account\" aria-controls=\"panel-account\" active></mdc-tab>\n  <mdc-tab id=\"tab-billing\" tab-id=\"billing\" text=\"Billing\" aria-controls=\"panel-billing\"></mdc-tab>\n</mdc-verticaltablist>\n\n<section id=\"panel-account\" role=\"tabpanel\" aria-labelledby=\"tab-account\">...</section>\n<section id=\"panel-billing\" role=\"tabpanel\" aria-labelledby=\"tab-billing\" hidden>...</section>"
      },
      {
        "language": "ts",
        "code": "tablist.addEventListener('change', (event) => {\n    handleTabChange(event.detail.tabId);\n  });"
      }
    ],
    "reactImports": [
      "Verticaltablist"
    ],
    "constants": {},
    "keywords": [
      "a",
      "accessibility",
      "and",
      "between",
      "component",
      "content",
      "for",
      "guidelines",
      "mdc",
      "oriented",
      "overview",
      "panels",
      "switching",
      "tab",
      "tablist",
      "the",
      "usage",
      "vertically",
      "verticaltablist"
    ]
  },
  {
    "name": "virtualizedlist",
    "title": "Virtualizedlist",
    "tagName": "mdc-virtualizedlist",
    "summary": "Usage, guidelines, and accessibility for the mdc-virtualizedlist component — a windowed list that renders only the visible items.",
    "tier": 3,
    "source": {
      "componentPath": "components/src/components/virtualizedlist",
      "knowledgeBasePath": "components/src/components/virtualizedlist/knowledge-base/virtualizedlist.component.md"
    },
    "docs": "## Overview\n\n`mdc-virtualizedlist` renders only the items currently visible in the scroll viewport, using TanStack Virtual under the hood. It adds keyboard navigation, focus management, scroll anchoring, and accessibility wiring on top of the virtualization primitives so very large lists stay performant while remaining usable for keyboard and assistive technology users.\n\n### When to use\n\n- Render long or unbounded lists (chats, search results, logs) where mounting every item would be expensive.\n- Lists whose items change size dynamically and need accurate measurement (with `observe-size-changes`).\n- Bottom-anchored streams (e.g. chat transcripts) that should follow new content until the user scrolls away.\n\n### When not to use\n\n- Use `mdc-list` when the data set is small enough to render in full — virtualization adds complexity that isn't needed.\n- Use `mdc-listbox` or `mdc-select` for selecting a single value from a list of options.\n\n## Guidelines\n\n### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/virtualizedlist/index.js\"; // custom element registration\n// or via React wrapper\nimport { Virtualizedlist } from '@momentum-design/components/dist/react';\n```\n\nThe list keeps rendering framework-agnostic. The consumer subscribes to `virtualitemschange` and renders the visible window of `mdc-listitem` elements. Each rendered item must carry `data-index` so the component can resolve it back to a position in the dataset:\n\n```html\n<mdc-virtualizedlist id=\"messages\" data-aria-label=\"Messages\"></mdc-virtualizedlist>\n```\n\n```ts\nconst list = document.getElementById('messages');\n\nlist.virtualizerProps = {\n  count: items.length,\n  estimateSize: () => 48,\n  getItemKey: (index) => items[index].id,\n};\n\nlist.addEventListener('virtualitemschange', (event) => {\n  const { virtualItems } = event.detail;\n  // Render only virtualItems; each rendered <mdc-listitem> must set data-index={virtualItem.index}\n});\n```\n\n### Content guidance\n\n- Place a `mdc-listheader` in the `list-header` slot when you need a header that scrolls with the list.\n\n### Property/Attribute details\n\n- `virtualizerProps` — required object passed to TanStack's virtualizer. At minimum:\n  - `count` — total number of items in the dataset.\n  - `estimateSize(index)` — function returning the estimated pixel size of each item.\n  - `getItemKey(index)` — function returning a stable unique key for each item.\n  Other TanStack options (such as `paddingStart`, `paddingEnd`, `gap`, `rangeExtractor`, `isItemNavigable`) are forwarded directly.\n- `loop` — `\"true\"` or `\"false\"` (default `\"false\"`). When `\"true\"`, Up/Down arrow navigation wraps at the ends of the list.\n- `scroll-anchoring` — when `true`, the list keeps the user-anchored item in view as the dataset grows; otherwise it preserves the current scroll position.\n- `observe-size-changes` — when `true`, the list uses a `ResizeObserver` to re-measure items whose rendered size changes (use for chat bubbles with images, expandable rows, etc.).\n- `revert-list` — when `true`, items align to the bottom of the scroll container and the list anchors to the bottom until the user scrolls away.\n- `at-bottom-threshold` — pixel tolerance used when deciding whether the user is at the bottom of the list (relevant for scroll anchoring).\n- `initial-focus` — index of the item that should receive focus when the list is first rendered.\n- `data-aria-label` — accessible name applied to the inner list container via `aria-label`.\n- Imperative method `scrollToIndex(index, options?)` scrolls to the item at the given index using TanStack's scrolling options.\n- Events:\n  - `scroll` (`onScroll`) — re-fired from the internal scroll container so consumers can react to scroll position changes.\n  - `virtualitemschange` (`onVirtualItemsChange`) — fires whenever the set of visible virtual items changes. Detail includes the `virtualizer` instance and the `virtualItems` array.\n\n### Limitations\n\n- Orientation is always vertical; setting `orientation=\"horizontal\"` is reset back to `vertical` by the component.\n- Do not apply CSS padding or margin to the top/bottom of the list or to the first/last item — it breaks the virtualization math and may produce unnecessary scrollbars. Use `paddingStart` / `paddingEnd` (and `gap` for spacing between items) on `virtualizerProps` instead.\n- The component does not render list items itself; the consumer must subscribe to `virtualitemschange` and render the visible window.\n- List updates only react to changes in `virtualizerProps`, to size changes when `observe-size-changes` is enabled, or to explicit calls to `virtualizer.measure()`.\n\n## Accessibility\n\n### Built-in features\n\n- The inner scroll container is wrapped by an element with `role=\"list\"` and `aria-label` taken from `data-aria-label`, so assistive technologies expose the data set as a list with a name.\n- Each rendered item is assigned `aria-setsize` matching the total number of items in the dataset, so screen readers can report the size of the virtual list even though only a window is in the DOM.\n- Keyboard navigation: Up/Down arrow keys move focus between items (with optional wrap via `loop`), Home jumps to the first item, End scrolls to and focuses the last item.\n- Roving tabindex: only the focused item has `tabindex=\"0\"`; the rest are `-1` so Tab moves out of the list rather than visiting every item.\n- Items just outside the visible window (the selected item plus its immediate neighbours) are kept in the DOM as \"hidden\" entries so focus can move smoothly while scrolling.\n- Scroll anchoring keeps the focused item visible as the dataset grows or items resize, and `revert-list` keeps the latest item in view in bottom-anchored streams until the user scrolls up.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-virtualizedlist`) | `role` | unset (the inner container exposes the list role) |\n| Inner container (`[part=\"container\"]`) | `role` | `list` |\n| Inner container | `aria-label` | The value of `data-aria-label` |\n| Inner slot | `role` | `presentation` |\n| Each rendered `mdc-listitem` | `aria-setsize` | The total `count` from `virtualizerProps` |\n| Focused item | `tabindex` | `0` |\n| Other items | `tabindex` | `-1` |\n\n### Implementation requirements\n\n#### General\n\n- Set `data-index` on every rendered item to its position in the dataset; the component relies on this attribute to map between DOM nodes and the virtualized list.\n- Provide stable `getItemKey` values; changing the key as content changes will cause scroll position and selection to shift unexpectedly.\n- For dynamic item sizes, enable `observe-size-changes` so size measurements stay accurate after content updates.\n\n#### Labelling\n\n- Set `data-aria-label` so the list has an accessible name. Without it, assistive technologies will announce an unnamed list role.\n\n### Notes\n\n- Because items outside the viewport are not in the DOM, screen-reader features that rely on traversing siblings (such as listing all rows) will only encounter the rendered window plus the focus-preserving neighbours. `aria-setsize` is used to communicate the true size.",
    "sections": {
      "Overview": "`mdc-virtualizedlist` renders only the items currently visible in the scroll viewport, using TanStack Virtual under the hood. It adds keyboard navigation, focus management, scroll anchoring, and accessibility wiring on top of the virtualization primitives so very large lists stay performant while remaining usable for keyboard and assistive technology users.\n\n### When to use\n\n- Render long or unbounded lists (chats, search results, logs) where mounting every item would be expensive.\n- Lists whose items change size dynamically and need accurate measurement (with `observe-size-changes`).\n- Bottom-anchored streams (e.g. chat transcripts) that should follow new content until the user scrolls away.\n\n### When not to use\n\n- Use `mdc-list` when the data set is small enough to render in full — virtualization adds complexity that isn't needed.\n- Use `mdc-listbox` or `mdc-select` for selecting a single value from a list of options.",
      "Guidelines": "### Developer usage\n\nImport and use the component via its React wrapper or directly as a custom element:\n\n```tsx\nimport \"@momentum-design/components/dist/components/virtualizedlist/index.js\"; // custom element registration\n// or via React wrapper\nimport { Virtualizedlist } from '@momentum-design/components/dist/react';\n```\n\nThe list keeps rendering framework-agnostic. The consumer subscribes to `virtualitemschange` and renders the visible window of `mdc-listitem` elements. Each rendered item must carry `data-index` so the component can resolve it back to a position in the dataset:\n\n```html\n<mdc-virtualizedlist id=\"messages\" data-aria-label=\"Messages\"></mdc-virtualizedlist>\n```\n\n```ts\nconst list = document.getElementById('messages');\n\nlist.virtualizerProps = {\n  count: items.length,\n  estimateSize: () => 48,\n  getItemKey: (index) => items[index].id,\n};\n\nlist.addEventListener('virtualitemschange', (event) => {\n  const { virtualItems } = event.detail;\n  // Render only virtualItems; each rendered <mdc-listitem> must set data-index={virtualItem.index}\n});\n```\n\n### Content guidance\n\n- Place a `mdc-listheader` in the `list-header` slot when you need a header that scrolls with the list.\n\n### Property/Attribute details\n\n- `virtualizerProps` — required object passed to TanStack's virtualizer. At minimum:\n  - `count` — total number of items in the dataset.\n  - `estimateSize(index)` — function returning the estimated pixel size of each item.\n  - `getItemKey(index)` — function returning a stable unique key for each item.\n  Other TanStack options (such as `paddingStart`, `paddingEnd`, `gap`, `rangeExtractor`, `isItemNavigable`) are forwarded directly.\n- `loop` — `\"true\"` or `\"false\"` (default `\"false\"`). When `\"true\"`, Up/Down arrow navigation wraps at the ends of the list.\n- `scroll-anchoring` — when `true`, the list keeps the user-anchored item in view as the dataset grows; otherwise it preserves the current scroll position.\n- `observe-size-changes` — when `true`, the list uses a `ResizeObserver` to re-measure items whose rendered size changes (use for chat bubbles with images, expandable rows, etc.).\n- `revert-list` — when `true`, items align to the bottom of the scroll container and the list anchors to the bottom until the user scrolls away.\n- `at-bottom-threshold` — pixel tolerance used when deciding whether the user is at the bottom of the list (relevant for scroll anchoring).\n- `initial-focus` — index of the item that should receive focus when the list is first rendered.\n- `data-aria-label` — accessible name applied to the inner list container via `aria-label`.\n- Imperative method `scrollToIndex(index, options?)` scrolls to the item at the given index using TanStack's scrolling options.\n- Events:\n  - `scroll` (`onScroll`) — re-fired from the internal scroll container so consumers can react to scroll position changes.\n  - `virtualitemschange` (`onVirtualItemsChange`) — fires whenever the set of visible virtual items changes. Detail includes the `virtualizer` instance and the `virtualItems` array.\n\n### Limitations\n\n- Orientation is always vertical; setting `orientation=\"horizontal\"` is reset back to `vertical` by the component.\n- Do not apply CSS padding or margin to the top/bottom of the list or to the first/last item — it breaks the virtualization math and may produce unnecessary scrollbars. Use `paddingStart` / `paddingEnd` (and `gap` for spacing between items) on `virtualizerProps` instead.\n- The component does not render list items itself; the consumer must subscribe to `virtualitemschange` and render the visible window.\n- List updates only react to changes in `virtualizerProps`, to size changes when `observe-size-changes` is enabled, or to explicit calls to `virtualizer.measure()`.",
      "Accessibility": "### Built-in features\n\n- The inner scroll container is wrapped by an element with `role=\"list\"` and `aria-label` taken from `data-aria-label`, so assistive technologies expose the data set as a list with a name.\n- Each rendered item is assigned `aria-setsize` matching the total number of items in the dataset, so screen readers can report the size of the virtual list even though only a window is in the DOM.\n- Keyboard navigation: Up/Down arrow keys move focus between items (with optional wrap via `loop`), Home jumps to the first item, End scrolls to and focuses the last item.\n- Roving tabindex: only the focused item has `tabindex=\"0\"`; the rest are `-1` so Tab moves out of the list rather than visiting every item.\n- Items just outside the visible window (the selected item plus its immediate neighbours) are kept in the DOM as \"hidden\" entries so focus can move smoothly while scrolling.\n- Scroll anchoring keeps the focused item visible as the dataset grows or items resize, and `revert-list` keeps the latest item in view in bottom-anchored streams until the user scrolls up.\n\n#### Internal ARIA managed by the component\n\n| Element | Attribute | Value |\n| --- | --- | --- |\n| Host (`mdc-virtualizedlist`) | `role` | unset (the inner container exposes the list role) |\n| Inner container (`[part=\"container\"]`) | `role` | `list` |\n| Inner container | `aria-label` | The value of `data-aria-label` |\n| Inner slot | `role` | `presentation` |\n| Each rendered `mdc-listitem` | `aria-setsize` | The total `count` from `virtualizerProps` |\n| Focused item | `tabindex` | `0` |\n| Other items | `tabindex` | `-1` |\n\n### Implementation requirements\n\n#### General\n\n- Set `data-index` on every rendered item to its position in the dataset; the component relies on this attribute to map between DOM nodes and the virtualized list.\n- Provide stable `getItemKey` values; changing the key as content changes will cause scroll position and selection to shift unexpectedly.\n- For dynamic item sizes, enable `observe-size-changes` so size measurements stay accurate after content updates.\n\n#### Labelling\n\n- Set `data-aria-label` so the list has an accessible name. Without it, assistive technologies will announce an unnamed list role.\n\n### Notes\n\n- Because items outside the viewport are not in the DOM, screen-reader features that rely on traversing siblings (such as listing all rows) will only encounter the rendered window plus the focus-preserving neighbours. `aria-setsize` is used to communicate the true size."
    },
    "dependencies": [],
    "api": {
      "properties": [
        {
          "name": "atBottomThreshold",
          "attribute": "at-bottom-threshold",
          "type": "number",
          "default": "DEFAULTS.IS_AT_BOTTOM_THRESHOLD",
          "values": [],
          "description": "pixel tolerance used when deciding whether the user is at the bottom of the list (relevant for scroll anchoring).",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "loop",
          "attribute": "loop",
          "type": "'true' | 'false'",
          "default": "'false'",
          "values": [
            "true",
            "false"
          ],
          "description": "`\"true\"` or `\"false\"` (default `\"false\"`). When `\"true\"`, Up/Down arrow navigation wraps at the ends of the list.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "observeSizeChanges",
          "attribute": "observe-size-changes",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the list uses a `ResizeObserver` to re-measure items whose rendered size changes (use for chat bubbles with images, expandable rows, etc.).",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "revertList",
          "attribute": "revert-list",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, items align to the bottom of the scroll container and the list anchors to the bottom until the user scrolls away.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "scrollAnchoring",
          "attribute": "scroll-anchoring",
          "type": "boolean",
          "default": "false",
          "values": [],
          "description": "when `true`, the list keeps the user-anchored item in view as the dataset grows; otherwise it preserves the current scroll position.",
          "reflected": true,
          "source": "component"
        },
        {
          "name": "virtualizerProps",
          "attribute": "virtualizer-props",
          "type": "VirtualizerProps",
          "default": "DEFAULTS.VIRTUALIZER_PROPS",
          "values": [
            "getScrollElement",
            "count",
            "estimateSize",
            "getItemKey"
          ],
          "description": "required object passed to TanStack's virtualizer. At minimum:",
          "reflected": false,
          "source": "component"
        }
      ],
      "slots": [
        {
          "name": "default",
          "description": "This is a default/unnamed slot, where listitems can be placed."
        },
        {
          "name": "list-header",
          "description": "This slot is used to pass a header for the list, which can be a `mdc-listheader` component."
        }
      ],
      "events": [
        {
          "name": "scroll",
          "description": "(React: onScroll) Event that gets called when user scrolls inside of list."
        },
        {
          "name": "virtualitemschange",
          "description": "(React: onVirtualItemsChange) Event that gets called when the virtual items change."
        }
      ],
      "cssParts": [
        {
          "name": "container",
          "description": "The container of the virtualized list."
        },
        {
          "name": "scroll",
          "description": "The scrollable area of the virtualized list."
        }
      ],
      "cssProperties": [],
      "rules": [
        "required object passed to TanStack's virtualizer. At minimum:",
        "`\"true\"` or `\"false\"` (default `\"false\"`). When `\"true\"`, Up/Down arrow navigation wraps at the ends of the list.",
        "when `true`, the list keeps the user-anchored item in view as the dataset grows; otherwise it preserves the current scroll position.",
        "when `true`, the list uses a `ResizeObserver` to re-measure items whose rendered size changes (use for chat bubbles with images, expandable rows, etc.).",
        "when `true`, items align to the bottom of the scroll container and the list anchors to the bottom until the user scrolls away.",
        "pixel tolerance used when deciding whether the user is at the bottom of the list (relevant for scroll anchoring).",
        "index of the item that should receive focus when the list is first rendered.",
        "accessible name applied to the inner list container via `aria-label`."
      ]
    },
    "examples": [
      {
        "language": "tsx",
        "code": "import \"@momentum-design/components/dist/components/virtualizedlist/index.js\"; // custom element registration\n// or via React wrapper\nimport { Virtualizedlist } from '@momentum-design/components/dist/react';"
      },
      {
        "language": "html",
        "code": "<mdc-virtualizedlist id=\"messages\" data-aria-label=\"Messages\"></mdc-virtualizedlist>"
      },
      {
        "language": "ts",
        "code": "const list = document.getElementById('messages');\n\nlist.virtualizerProps = {\n  count: items.length,\n  estimateSize: () => 48,\n  getItemKey: (index) => items[index].id,\n};\n\nlist.addEventListener('virtualitemschange', (event) => {\n  const { virtualItems } = event.detail;\n  // Render only virtualItems; each rendered <mdc-listitem> must set data-index={virtualItem.index}\n});"
      }
    ],
    "reactImports": [
      "Virtualizedlist"
    ],
    "constants": {
      "DEFAULTS": {
        "VIRTUALIZER_PROPS": "{",
        "count": 0,
        "estimateSize": "() => 0",
        "getItemKey": "(index: number) => index",
        "isItemNavigable": "() => true",
        "LOOP": "false",
        "SCROLL_ANCHORING": false,
        "IS_AT_BOTTOM_THRESHOLD": 16
      }
    },
    "keywords": [
      "a",
      "accessibility",
      "and",
      "component",
      "for",
      "guidelines",
      "items",
      "list",
      "mdc",
      "only",
      "overview",
      "renders",
      "that",
      "the",
      "usage",
      "virtualizedlist",
      "visible",
      "windowed"
    ]
  }
];

export default componentsRegistry;
// End AI-Assisted
