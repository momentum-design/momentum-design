/**
 * Encoding to use when a builder reads a target file using `Builder.read()`
 */
const FILE_ENCODING: BufferEncoding = 'utf-8';

/**
 * Type of this Builder.
 */
const UNKNOWN_TYPE: string = 'unknown';

const CONSTANTS = {
  FILE_ENCODING,
  UNKNOWN_TYPE,
};

export default CONSTANTS;
