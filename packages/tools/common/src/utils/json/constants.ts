/**
 * Encoding to use when a builder reads a target json file using `Builder.read()`
 */
const FILE_ENCODING: BufferEncoding = 'utf-8';

/**
 * Tab spacing to use when output json to a file.
 */
const TAB_SPACING = 2;

const CONSTANTS = {
  FILE_ENCODING,
  TAB_SPACING,
};

export default CONSTANTS;
