import { CONSTANTS as COMMON_CONSTANTS } from '../../common';

const TARGET_TOKEN_TYPES = ['lineHeights', 'fontSizes'];

const CONSTANTS = {
  NAME: COMMON_CONSTANTS.TRANSFORMS.PX_TO_REM,
  TARGET_TOKEN_TYPES,
};

export default CONSTANTS;
