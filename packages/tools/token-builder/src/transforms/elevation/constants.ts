import { CONSTANTS as COMMON_CONSTANTS } from '../../common';

const TARGET_TOKEN_TYPES = [
  'boxShadow',
];

const TARGET_TOKEN_VALUE_TYPES = [
  'innerShadow',
  'dropShadow',
];

const CONSTANTS = {
  NAME: COMMON_CONSTANTS.TRANSFORMS.MD_ELEVATION,
  TARGET_TOKEN_TYPES,
  TARGET_TOKEN_VALUE_TYPES,
};

export default CONSTANTS;
