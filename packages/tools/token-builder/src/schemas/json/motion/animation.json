{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/AnimationFile",
  "definitions": {
    "TokenRef": {
      "type": "string",
      "pattern": "^\\{[a-zA-Z_][a-zA-Z0-9_]*(\\.[a-zA-Z_][a-zA-Z0-9_]*)+\\}$",
      "description": "Path to a Momentum design token"
    },
    "TokenIdentifier": {
      "type": "string",
      "pattern": "^[a-zA-Z][a-zA-Z0-9]*$",
      "description": "A camelCase identifier referencing another animation token by name"
    },
    "CssAnimatableProperty": {
      "type": "string",
      "description": "A CSS property that can be animated or transitioned",
      "enum": [
        "background-color",
        "border-color",
        "border-radius",
        "border-width",
        "box-shadow",
        "clip-path",
        "color",
        "filter",
        "font-size",
        "gap",
        "height",
        "letter-spacing",
        "max-height",
        "max-width",
        "opacity",
        "outline",
        "transform",
        "visibility",
        "width"
      ]
    },
    "AnimationFile": {
      "type": "object",
      "description": "Root structure of an animation token source file",
      "additionalProperties": false,
      "properties": {
        "animation": {
          "$ref": "#/definitions/AnimationMap"
        }
      },
      "required": ["animation"],
      "title": "AnimationFile"
    },
    "AnimationMap": {
      "type": "object",
      "description": "A map of animation token names to their token definitions",
      "patternProperties": {
        "^[a-zA-Z][a-zA-Z0-9]*$": {
          "$ref": "#/definitions/AnimationToken"
        }
      },
      "additionalProperties": false,
      "title": "AnimationMap"
    },
    "AnimationToken": {
      "type": "object",
      "description": "A single animation token of type transition, keyframe, transitionCompound, or keyframeCompound",
      "required": ["description", "type"],
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "type": {
          "type": "string",
          "enum": ["transition", "keyframe", "transitionCompound", "keyframeCompound"]
        },
        "duration": { "$ref": "#/definitions/TokenRef" },
        "easing": { "$ref": "#/definitions/TokenRef" },
        "delay": { "$ref": "#/definitions/TokenRef" },
        "properties": {
          "type": "array",
          "items": { "$ref": "#/definitions/CssAnimatableProperty" },
          "minItems": 1
        },
        "keyframes": {
          "type": "array",
          "items": { "$ref": "#/definitions/KeyframeEntry" },
          "minItems": 1,
          "uniqueItems": true
        },
        "animations": {
          "type": "array",
          "items": { "$ref": "#/definitions/TokenIdentifier" },
          "minItems": 1
        },
        "composition": {
          "type": "string",
          "enum": ["parallel", "sequential"]
        },
        "iterationCount": {
          "oneOf": [
            { "type": "string", "const": "infinite" },
            { "type": "number", "minimum": 1 }
          ]
        },
        "fillMode": {
          "type": "string",
          "enum": ["none", "forwards", "backwards", "both"]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "transition" } },
            "required": ["type"]
          },
          "then": {
            "required": ["duration", "easing", "delay", "properties"],
            "properties": {
              "keyframes": false,
              "iterationCount": false,
              "fillMode": false,
              "animations": false,
              "composition": false
            }
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "keyframe" } },
            "required": ["type"]
          },
          "then": {
            "required": ["duration", "easing", "delay", "keyframes"],
            "properties": {
              "properties": false,
              "animations": false,
              "composition": false
            }
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "transitionCompound" } },
            "required": ["type"]
          },
          "then": {
            "required": ["animations", "composition"],
            "properties": {
              "duration": false,
              "easing": false,
              "delay": false,
              "properties": false,
              "keyframes": false,
              "iterationCount": false,
              "fillMode": false
            }
          }
        },
        {
          "if": {
            "properties": { "type": { "const": "keyframeCompound" } },
            "required": ["type"]
          },
          "then": {
            "required": ["animations"],
            "properties": {
              "duration": false,
              "easing": false,
              "delay": false,
              "properties": false,
              "keyframes": false,
              "iterationCount": false,
              "fillMode": false
            }
          }
        }
      ],
      "title": "AnimationToken"
    },
    "KeyframeEntry": {
      "type": "object",
      "description": "Defines the from/to values for a single CSS property in a @keyframes block",
      "required": ["propertyName", "from", "to"],
      "additionalProperties": false,
      "properties": {
        "propertyName": { "$ref": "#/definitions/CssAnimatableProperty" },
        "from": { "type": "string" },
        "to": { "type": "string" }
      },
      "title": "KeyframeEntry"
    }
  }
}

