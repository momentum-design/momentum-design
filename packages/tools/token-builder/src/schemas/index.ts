/**
 * to generate a new compatible schema
 * go to https://app.quicktype.io/
 * enter the sample JSON (once defined)
 * and copy the output into a new file
 * named after the
 * We are foregoing the benefit of typescript types
 * for the benefit of having simplicity with JSON schemas
 * as the JSON schema is the type itself regardless
 */

import { SCHEMA_MAP } from './default';

export {
  SCHEMA_MAP,
};
