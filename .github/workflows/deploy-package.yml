
name: Deploy Package CD
run-name: ${{ github.actor }} is running Deploy Package CD

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run in dry-run mode (skip actual npm publish)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  rid: ${{ github.run_id }}-${{ github.run_number }}

jobs:
  initialize:
    name: Initialize
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'
          
      - name: Validate Dependencies
        id: validate-dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Cache Dependencies
        uses: actions/cache@v4
        if: steps.validate-dependencies.outputs.cache-hit != 'true'
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

  build-automation:
    name: Build Automation
    needs: initialize
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'
          
      - name: Uncache Dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Synchronize Packages
        run: yarn

      - name: Build Automation
        run: yarn telemetry build && yarn automation build:cli

      - name: Cache Automation
        uses: actions/cache@v4
        with:
          path: '**/automation/dist'
          key: automation-dist-${{ env.rid }}

  generate-package-matrix:
    name: Generate Package Matrix
    needs: build-automation
    runs-on: ubuntu-latest

    outputs:
      packages: ${{ steps.generate-package-matrix.outputs.packages }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'
          
      - name: Uncache Dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Uncache Automation
        uses: actions/cache@v4
        with:
          path: '**/automation/dist'
          key: automation-dist-${{ env.rid }}

      - name: Synchronize Packages
        run: yarn

      - name: Update Main Branch
        run: |
          git fetch origin main --unshallow

      - id: generate-package-matrix
        name: Generate Package Matrix
        run: echo "packages=$(yarn md-automation -- --command get-packages --changed --commit-index 1 --dependent)" >> $GITHUB_OUTPUT

      - name: Display Package Matrix
        run: echo "generated package matrix - ${{ steps.generate-package-matrix.outputs.packages }}"

  build:
    name: Build
    needs: initialize
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'
          
      - name: Uncache Dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Synchronize Packages
        run: yarn

      - name: Build
        run: yarn build

      - name: Cache Dist
        uses: actions/cache@v4
        with:
          path: '**/dist'
          key: dist-${{ env.rid }}

  deploy-npmjs:
    name: ( ${{ matrix.package }} ) Deploy - NPMJS
    needs: [build, generate-package-matrix]
    if: ${{ needs.generate-package-matrix.outputs.packages != '[]' && needs.generate-package-matrix.outputs.packages != '' }}
    runs-on: ubuntu-latest
    outputs:
      should_publish: ${{ steps.check-dist.outputs.should_publish }}

    strategy:
      matrix:
        package: ${{ fromJson(needs.generate-package-matrix.outputs.packages) }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'
          
      - name: Uncache Dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Uncache Dist
        uses: actions/cache@v4
        with:
          path: '**/dist'
          key: dist-${{ env.rid }}

      - name: Synchronize Packages
        run: yarn

      - name: Update Main Branch
        run: |
          git fetch origin main --unshallow

      - name: Check if dist changed
        id: check-dist
        run: |
          PKG_PATH=$(yarn workspaces list --json | jq -r "select(.name == \"${{ matrix.package }}\") | .location")
          if bash .github/scripts/compare-dist.sh "${{ matrix.package }}" "$PKG_PATH"; then
            echo "should_publish=true" >> $GITHUB_OUTPUT
          else
            echo "should_publish=false" >> $GITHUB_OUTPUT
          fi

      - name: Increment Packages
        if: steps.check-dist.outputs.should_publish == 'true'
        run: yarn md-automation -- --command increment-packages --packages ${{ matrix.package }}
        env:
           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Yarn
        if: steps.check-dist.outputs.should_publish == 'true'
        run: yarn config set npmAuthToken ${{ secrets.NPM_TOKEN_CISCO_MD }}

      - name: ( ${{ matrix.package }} ) Deploy - NPMJS
        if: steps.check-dist.outputs.should_publish == 'true'
        run: |
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            echo "DRY RUN: Would publish ${{ matrix.package }}"
            yarn workspace ${{ matrix.package }} pack --dry-run
          else
            yarn workspace ${{ matrix.package }} publish:npmjs
          fi

      - name: Skip Publish (dist unchanged)
        if: steps.check-dist.outputs.should_publish == 'false'
        run: echo "Skipping publish for ${{ matrix.package }} - dist unchanged"

  dispatch-telemetry:
    name: ( ${{ matrix.package }} ) Dispatch Telemetry
    needs: [deploy-npmjs, generate-package-matrix]
    runs-on: ubuntu-latest

    strategy:
      matrix:
        package: ${{ fromJson(needs.generate-package-matrix.outputs.packages) }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'yarn'
          
      - name: Uncache Dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            packages/**/node_modules
          key: node-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Uncache Dist
        uses: actions/cache@v4
        with:
          path: '**/dist'
          key: dist-${{ env.rid }}

      - name: Synchronize Packages
        run: yarn

      - name: Update Main Branch
        run: |
          git fetch origin main --unshallow
      - name: Dispatch Telemetry ( ${{ matrix.package }} )
        run: yarn md-automation -- --command dispatch-telemetry --packages ${{ matrix.package }}
        env:
          MOMENTUM_METRICS_API_KEY: ${{ secrets.MOMENTUM_METRICS_API_KEY }}

  create-release:
     name: ( ${{ matrix.package }} ) Create Release
     needs: [deploy-npmjs, generate-package-matrix]
     if: ${{ needs.deploy-npmjs.outputs.should_publish == 'true' }}
     runs-on: ubuntu-latest

     strategy:
       matrix:
         package: ${{ fromJson(needs.generate-package-matrix.outputs.packages) }}

     steps:
       - uses: actions/checkout@v4
       - uses: actions/setup-node@v4
         with:
           node-version-file: '.nvmrc'
           registry-url: 'https://registry.npmjs.org'
           cache: 'yarn'

       - name: Uncache Dependencies
         uses: actions/cache@v4
         with:
           path: |
            node_modules
            packages/**/node_modules
           key: node-modules-${{ hashFiles('**/yarn.lock') }}

       - name: Uncache Dist
         uses: actions/cache@v4
         with:
           path: '**/dist'
           key: dist-${{ env.rid }}

       - name: Synchronize Packages
         run: yarn

       - name: Update Main Branch
         run: git fetch origin main --unshallow

       - name: Create Release ( ${{ matrix.package }} )
         run: yarn md-automation -- --command create-release --packages ${{ matrix.package }} --since origin/main
         env:
           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
